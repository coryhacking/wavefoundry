# Wave Record

Owner: Engineering
Status: closed
Last verified: 2026-07-03

wave-id: `1p9j0 windows-portability-round-3`
Title: Windows Portability Round 3

## Objective

Close the native-Windows portability gaps that the round-2 audit surfaced but wave `1p9hn` (windows-portability-round-2) did not cover. When this wave closes, the `wf setup` Phase-1 child chain can no longer hang indefinitely (the likely mechanism behind the 1.9.8 field install stall), the server-side docs-lint timeout is configurable so lifecycle tools stop failing on large repos, rendered hooks decode host stdin as UTF-8, metadata writes survive a Windows sharing violation, and the remaining path/newline and dev/test-infra stragglers are fixed. Late admission (operator-directed, 2026-07-02): one non-Windows portability fix, the Apple Silicon CoreML provider-probe temp-dir CPU-fallback bug (`1p9lj`), discovered during wave `1p9jn`'s review.

## Changes

Change ID: `1p9it-bug setup-phase1-child-deadlines`
Change Status: `implemented`

Change ID: `1p9iu-bug mcp-docs-lint-timeout-configurable`
Change Status: `implemented`

Change ID: `1p9iv-bug rendered-hook-stdio-utf8`
Change Status: `implemented`

Change ID: `1p9iw-bug atomic-write-windows-share-retry`
Change Status: `implemented`

Change ID: `1p9ix-bug windows-path-newline-stragglers`
Change Status: `implemented`

Change ID: `1p9iy-bug dev-test-infra-windows-hardening`
Change Status: `implemented`

Change ID: `1p9lj-bug coreml-provider-probe-tempdir-cpu-fallback`
Change Status: `implemented`

Completed At: 2026-07-03

## Wave Summary

Wave `1p9j0` (Windows Portability Round 3) delivered 7 changes: Bound every wf setup Phase-1 child with a per-step deadline and no-progress watchdog, Configurable server-side docs-lint timeout for full-scan lifecycle tools, Rendered hooks decode host stdin payloads as UTF-8 (fix cp1252 mis-decode + simulate spawn encoding), Windows sharing-violation retry for `_atomic_write_text` meta.json replace, Fix Windows newline/path stragglers in agent-surface render and gitignore secrets filter, Dev/test-infra Windows hardening: run_tests run-lock + UTF-8 subprocess capture in build_pack, and CoreML provider probe temp-dir failure pins index rebuilds to CPU. Notable adjustments during implementation: Bound every wf setup Phase-1 child with a per-step deadline and no-progress watchdog: Scoped from the native-Windows install audit; verified all five cited sites in the 1p9hn-applied tree and corrected line numbers.; Bound every wf setup Phase-1 child with a per-step deadline and no-progress watchdog: Second delivery-council fix pass. Watchdog heartbeats: `indexer.py` `_StreamingLayerWriter` now prints one unconditional line per embed flush and one entering each layer's finalize — the council quantified that the per-50-file prints left silent windows (a 1024-chunk flush on a swap-thrashing host, and the entirely verbose-gated finalize tail) that could exceed the 1800s stall window on exactly the low-RAM machine class the watchdog targets; the heartbeats bound the silent window to a single forward-pass batch. Covering tests added for the previously-untested hardening branches: `test_main_graph_only_stall_timeout_exits_2_with_message` (stall `TimeoutError` → clean exit 2) and `test_run_indexer_post_eof_nonexiting_child_terminated` + a bounded-wait kwarg assertion in the within-deadline test (post-loop `proc.wait(timeout=…)` + expiry branch). `ModelPrewarmTimeout` message gained a secondary corrupted-cache hint (clear FASTEMBED_CACHE_PATH and rerun) since a corrupt cache that hangs the offline verify now times out with otherwise network-only guidance and never self-quarantines.; Bound every wf setup Phase-1 child with a per-step deadline and no-progress watchdog: Implemented. Two intentional deviations from the literal plan: (1) the model-warm deadline reaches `_warm_model` via a per-run module var (`_ACTIVE_MODEL_WARM_DEADLINE_SECONDS`) set once by `main` — NOT by threading root through `prewarm_models`/`_prewarm_required_model` — because the generic `warm_fn(model_name, local_files_only=…)` contract (and the tests asserting it) must stay byte-identical; `_warm_model` also accepts an explicit `deadline_seconds` for direct testability. Consequence: every `_warm_model` caller is now deadline-bounded by the code default when the var is unset — a safety win, not new watchdog machinery for out-of-scope pipelines. (Correction, delivery review: the dashboard watcher was a wrong example — it spawns `indexer.py` and never calls `_warm_model`. Also note the facet that an operator's `setup.model_warm_timeout_seconds` override applies only via `setup_index.main`; all production paths — upgrade pipeline, MCP-triggered builds — reach `main` as subprocesses, so no production path ignores the override.) (2) A uv-bootstrap timeout prints a loud stage-named message and falls back to plain pip (uv is optional) rather than aborting; venv/deps timeouts abort with `SystemExit(2)`.

**Changes delivered:**

- **Bound every wf setup Phase-1 child with a per-step deadline and no-progress watchdog** (`1p9it-bug setup-phase1-child-deadlines`) — 6 ACs completed. Key decisions: Fix at the call sites, not in `subprocess_util.isolated_run`.; Bound the in-process model warm with a watchdog thread / wall-clock deadline.
- **Configurable server-side docs-lint timeout for full-scan lifecycle tools** (`1p9iu-bug mcp-docs-lint-timeout-configurable`) — 7 ACs completed. Key decisions: Introduce a distinct `docs_lint.full_scan_timeout_seconds` key rather than reuse `docs_lint.hook_timeout_seconds`.; On timeout, have `run_validate` return `passed: False` with a legible `errors` message instead of propagating `TimeoutExpired`.
- **Rendered hooks decode host stdin payloads as UTF-8 (fix cp1252 mis-decode + simulate spawn encoding)** (`1p9iv-bug rendered-hook-stdio-utf8`) — 6 ACs completed. Key decisions: Fix stdin via `stream.reconfigure(encoding="utf-8", errors="replace")` inside `configure_utf8_stdio()` + a guarded call in `HOOK_BOOTSTRAP`, rather than setting `PYTHONUTF8=1` at hook runtime.; Use `errors="replace"` for stdin (matching stdout/stderr).
- **Windows sharing-violation retry for `_atomic_write_text` meta.json replace** (`1p9iw-bug atomic-write-windows-share-retry`) — 5 ACs completed. Key decisions: Gate retry on `os.name == "nt"` and `winerror in {5, 32}` only; POSIX single-call path untouched.; Re-raise the last exception after a fixed small retry bound instead of swallowing.
- **Fix Windows newline/path stragglers in agent-surface render and gitignore secrets filter** (`1p9ix-bug windows-path-newline-stragglers`) — 5 ACs completed. Key decisions: Fix F14 by switching `write_text` to `path.open("w", ..., newline="")` rather than pre-normalizing the source strings.; Fix F18 with `.as_posix()` rather than normalizing git's output to OS separators.
- **Dev/test-infra Windows hardening: run_tests run-lock + UTF-8 subprocess capture in build_pack** (`1p9iy-bug dev-test-infra-windows-hardening`) — 5 ACs completed. Key decisions: Mirror `dashboard_lib.py`'s `os.name` `fcntl`/`msvcrt` split in `run_tests.py` rather than adding a shared lock abstraction.; Add `errors="replace"` alongside `encoding="utf-8"` for the `run_tests.py` capture.
- **CoreML provider probe temp-dir failure pins index rebuilds to CPU** (`1p9lj-bug coreml-provider-probe-tempdir-cpu-fallback`) — 6 ACs completed. Key decisions: Treat this as a provider-selection bug, not a mitigation-only warning.
## Journal Watchpoints

- Sequencing: `1p9it` (setup Phase-1 deadlines — the likely field-install-hang mechanism) and `1p9iu` (docs-lint timeout) carry the highest user-facing value; the straggler batch (`1p9ix`) and dev/test-infra batch (`1p9iy`) are independent and can proceed in parallel.
- Shared-file coordination: changes land in mostly-independent files (`setup_index.py`, `server_impl.py`, `render_platform_surfaces.py` + `cli_stdio.py`, `indexer.py`, `render_agent_surfaces.py` + `secrets_validators.py`, `run_tests.py` + `build_pack.py`); only `1p9iv` spans two shared files.
- Watchpoint (`1p9iv`): touches `HOOK_BOOTSTRAP`/`configure_utf8_stdio` — after the change, re-render platform surfaces and confirm rendered hooks decode a non-ASCII stdin payload correctly.
- Guard (`1p9iy`): edits `run_tests.py` (the test runner itself) — verify the suite still launches on POSIX after the `fcntl`/`msvcrt` guard.
- Dependency: this wave builds on the currently-uncommitted wave `1p9hn` in the working tree; line numbers in the change docs are anchored to the post-`1p9hn` tree. Implement after `1p9hn` is committed to avoid tangling the diff.

## Review Checkpoints

- **Prepare-phase Wave Council [prepare-council] — 2026-07-02: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, architecture-reviewer, security-reviewer, qa-reviewer, reality-checker, docs-contract-reviewer; rotating-seat: reality-checker; strongest-challenge: `1p9it`'s setup-deadline defaults could false-trip slow-but-legit environments (corp proxy, low-RAM WSL2) — mitigated by conservative, per-stage configurable defaults plus the AC-5 within-deadline non-regression gate, and the honestly-recorded limit that a daemon-thread watchdog cannot truly interrupt a parked native fastembed call (the abort fails loud and returns control; the process may need to exit rather than resume); strongest-alternative: split the `1p9iy` dev/test-infra batch (F12/F13/F16) out of a shipped-runtime wave — declined because the items are low-risk and F16 blocks native-Windows contributors from running the suite at all. No required council lanes beyond `implementer`: no MCP endpoint additions, no architecture-boundary change, and the `1p9ix` secrets gitignore-filter fix is Windows/POSIX parity, not a detection narrowing. All ACs are deterministically testable.)
- **Delivery-phase Wave Council [wave-council-delivery] — 2026-07-02: PASS-WITH-FINDINGS — two blocking repairs before close** (moderator: wave-council; primer-depth: standard, declared per red-team depth rationale — local-tooling reliability paths, no trust-boundary crossing; seats: architecture-reviewer, security-reviewer, qa-reviewer, reality-checker; rotating-seat: performance-reviewer, selected because the most consequential delivered mechanism is the reader-thread/queue watchdog inserted into the index-build streaming path; red-team ran the adversarial primer first; required lanes code-reviewer/qa-reviewer/architecture-reviewer all ran; advisory factor lanes 03/05/12/13 ran per factor policy (05 and 13 clean-pass). Central computational evidence: full framework suite green — 4,152 tests across 41 files including the new `tests/test_run_tests_lock.py` (a later combined-tree run by the co-resident session recorded 4,161 OK); docs-lint clean; no `__pycache__` under scripts; rendered `.claude/hooks/*` verified carrying the guarded `configure_utf8_stdio()` bootstrap with zero CR bytes.)
- **Delivery finding DF-1 (blocking before close) — model-warm timeout abort races the cache-quarantine/retry loop** (`setup_index.py` `_run_in_process_with_deadline` → `_prewarm_required_model:1277-1289` → `_quarantine_model_cache:1191-1203`). Red-team strongest challenge, independently CONFIRMED by security-reviewer, reality-checker, qa-reviewer, and architecture-reviewer: the deadline abandons a still-writing daemon warm thread; the except-path corruption check flags the in-flight cache by design (zero-byte `.incomplete` blob is the canonical hung-TLS state); the quarantine `shutil.move` then runs against live open handles — on native Windows it raises a raw sharing-violation traceback from inside the handler that bypasses `main`'s `ModelPrewarmError` handler (defeating the AC-5 actionable-message contract in exactly the 1.9.8 field scenario `1p9it` targets), and on POSIX the retry spawns a second warm thread racing the first (worst case about twice the deadline). The timeout→quarantine→retry path has no test. Consensus remedy (all seats; matches the deadline helper's own documented exit-rather-than-resume contract): small in-place repair — distinct timeout marker on `ModelPrewarmError`, skip corruption-check/quarantine/retry for timeout aborts and propagate straight to `main`'s existing handler, plus one covering test traversing the timeout path through `_prewarm_required_model`.
- **Delivery finding DF-2 (blocking before close) — rendered-surface drift on non-Claude platforms** (`.cursor/hooks/*` 0/4, `.windsurf/hooks/*` 0/2, `.github/hooks/*` 0/2 carry `configure_utf8_stdio`; verified by grep). Only `--platform claude` was re-rendered after the shared `HOOK_BOOTSTRAP` change, so `1p9iv` Requirement 2 ("every rendered hook decodes its payload as UTF-8") is undelivered on Cursor/Windsurf/Copilot hosts and the self-hosting rule ("regenerate or document the drift") is currently satisfied by neither. Remedy: re-render all platform surfaces before close.
- **Delivery hardenings (non-blocking, agreed for the same fix pass):** (a) catch the index-build stall `TimeoutError` in `setup_index.main` and exit 2 with the stage-named message — today it escapes as a raw traceback with generic exit 1, inconsistent with the venv/deps `SystemExit(2)` and model-warm handled paths (factor-12 medium; performance/qa concur); (b) bound the post-loop `proc.wait()` in `_run_indexer` — a reader-thread exception is indistinguishable from EOF via the sentinel and can re-create an unbounded wait on a live silent child (architecture/performance/reality-checker; about two lines); (c) `_run_file` should reuse `subprocess_util.utf8_child_env` so an inherited `PYTHONIOENCODING=cp1252` cannot override the new `PYTHONUTF8=1` (factor-12, mirrors the 1p8gv helper contract and its regression test); (d) align the `docs/workflow-config.json` `setup` block to the notes-only convention — the five pre-seeded numerics equal to code defaults contradict sibling change `1p9iu`'s recorded do-not-pre-seed decision in the same wave and the framework-owns-defaults rule, creating silent default-pinning drift (factor-03/code-reviewer/architecture/qa); (e) close the test-binding gaps: forwarded-timeout assertions for `_bootstrap_uv`/`_install_deps` (currently only the venv path pins the kwarg), a kwarg-capture test for `1p9iy` AC-2 (`_run_file` encoding/env is evidence-by-diff-read only), and a stub busy-branch test for the Windows lock path (qa/factor-12).
- **Change-doc residual notes to record on resume:** `1p9it` — scope AC-3's "no orphan" wording to the direct child (multiprocessing pool grandchildren of a stall-killed indexer may survive holding RAM until they self-exit); correct the Progress Log's "incl. the dashboard watcher" example (the watcher spawns `indexer.py` and never calls `_warm_model`); name the facet that an operator `model_warm_timeout_seconds` override applies only via `setup_index.main` (all production paths — upgrade pipeline, MCP-triggered builds — do reach `main` as subprocesses, so no production path ignores it). `1p9iu` — add the accepted tradeoff that the shared knob raises the post-write lint worst-case block from 30s to 300s. `1p9iv` — note that stdin `errors="replace"` converts a rare loud decode-crash (five cp1252-undefined bytes) into a silent replacement-char path, bounded by git-delta file selection and the full-scan backstop. `1p9iy` — refresh drifted line refs (`:160`/`:206`).
- **Material disagreements and resolution:** severity of DF-1 ranged from must-fix (qa-reviewer) to minor (code-reviewer) — resolved as blocking because four seats independently confirmed the mechanism and it degrades the flagship field scenario the change exists to fix; security-reviewer asked that the real-Windows lock-semantics residual be recorded in `1p9iy` — resolved as already-satisfied (reality-checker and qa-reviewer verified it is present in the change doc's Risks table). No seat blocked; no unresolved disagreement remains.
- **Seat verdicts:** code-reviewer pass-with-findings; qa-reviewer pass-with-findings (full AC attestation: every required AC across all six changes carries real, mostly revert-sensitive evidence; zero silent unmet checkboxes; no `[~]` markers exist in this wave; no vacuous-test patterns found); architecture-reviewer pass-with-findings (boundary/layering integrity preserved; the `server_impl` timeout-helper duplication endorsed over a new `import indexer` edge whose import side effect would reconfigure the live MCP server's stdio); security-reviewer pass-with-findings — the prepare-council's parity claim for the `1p9ix` secrets filter is empirically CONFIRMED (POSIX byte-identical; committed files provably never dropped because `git check-ignore` exempts tracked paths; every quoting/error edge fails toward over-scan, never under-scan); reality-checker pass-with-findings (all spot-checked change-doc claims verified, including counterfactual execution of the cp1252 stdin test; all deviations recorded; no silent scope expansion); performance-reviewer pass-with-findings (reader-thread/queue overhead measured at about 1 microsecond per line — negligible, design endorsed as the right portable primitive; the 1800s stall window is adequate-but-not-comfortable on the low-RAM machine class because the finalize-phase prints are verbose-gated — future hardening: always-on finalize/embed progress prints).
- **Rotating-seat strongest unconsidered alternative (performance-reviewer):** rescope `_run_post_write_lint` to the already-shipped `--changed` incremental scan (1p9bm machinery) so only the five lifecycle gates pay the full-corpus cost — recorded as a candidate follow-up plan, not in-wave. Complementary defusal for DF-1 noted: set `HF_HUB_DOWNLOAD_TIMEOUT`/`HF_HUB_ETAG_TIMEOUT` in the warm env so most stalled fetches raise on the worker thread itself, keeping the wall-clock deadline as backstop.
- **AC reconciliation:** scope-gap check — all out-of-scope deferrals are explicitly recorded in the six change docs (upgrade-pipeline deadlines, lifecycle `--changed`, whole-tree encoding sweep, real Windows CI, and siblings); nothing relitigated, no silent narrowing. AC priority reconciliation — qa-reviewer attested every required row has verification evidence; the deferred-to-central items (full suite, docs-lint, `.claude` re-render) are satisfied by the central pass recorded above. Pre-implementation gate reconciliation — the prepare-council PASS above is a structured signoff recorded 2026-07-02 before the first code edit; gate honored.
- **Landing note:** the working tree co-hosts wave `1p9jn retrieval-lookup-hardening` (pause recorded in Progress Log); `server_impl.py`, `indexer.py`, `test_server_tools.py`, and `test_indexer.py` carry hunks from both waves, so landing `1p9j0` requires hunk-level attribution — a whole-file commit would smuggle `1p9jn` hunks into this wave. (Update: `1p9jn` closed 2026-07-02 by operator direction; its hunks remain uncommitted in the shared tree, so the attribution note still applies at commit time.)
- **Delivery-findings reconciliation [fix-pass] — 2026-07-02: RESOLVED.** DF-1 fixed: `ModelPrewarmTimeout(ModelPrewarmError)` marker; `_prewarm_required_model` re-raises it before the corruption-check/quarantine/retry; propagates to `main`'s existing exit-2 handler; pinned by `test_prewarm_timeout_skips_quarantine_and_propagates` (quarantine/corruption-check asserted NOT called, single attempt) + subtype test. DF-2 fixed: all-platform re-render — 12/12 rendered hooks (`.claude` 4, `.cursor` 4, `.windsurf` 2, `.github` 2) carry the guarded `configure_utf8_stdio()` bootstrap, zero CR bytes (renderer footgun noted for a follow-up plan: an explicit `--platform` list excluding copilot removes `.github/hooks` even when the repo has copilot surfaces — use one auto-detect invocation). Hardenings (a)–(e) all landed: stall `TimeoutError` caught in `main` (graph-only + build paths) → stage-named message + exit 2; post-loop `proc.wait(timeout=stall)` + terminate/reap on expiry; `_run_file` env via `subprocess_util.utf8_child_env`; workflow-config `setup` block notes-only; test-binding gaps closed (uv/deps forwarded-timeout, `RunFileEncodingTests`, Windows busy-branch). All change-doc residual notes recorded. Central verification after the fix pass + `1p9lj`: full suite 4,178 tests across 41 files OK; docs-lint clean; no `__pycache__`. Remaining before close: a delivery-review lane pass covering the late-admitted `1p9lj` implementation (the 2026-07-02 delivery council predates it) and the standard close-time gates.
- **Second delivery-phase Wave Council [wave-council-delivery] — 2026-07-03: PASS-WITH-FINDINGS, all findings resolved in-session** (moderator: wave-council; primer-depth: standard — implementation-scoped delta, no trust-boundary crossing; scope: the DF-1/DF-2 fix pass, hardenings a–e, and the late-admitted `1p9lj` implementation; seats: architecture-reviewer, security-reviewer, qa-reviewer, reality-checker; rotating-seat: performance-reviewer (re-selected — the delta's dominant behavior change is provider selection, the GPU-vs-CPU rebuild-speed field pain, plus watchdog paths); red-team ran the adversarial primer first with explicit attack candidates; required lanes code-reviewer/qa-reviewer/architecture-reviewer ran; advisory factor lane 13 ran (the doctor surface was the only factor-relevant delta). Central evidence: suite 4,178 OK pre-fix-pass-2, docs-lint clean.)
- **Second-council findings and resolutions (all landed in-session, 2026-07-03):** (F1, consensus of primer + 4 seats, empirically confirmed thrice) operator-forced CUDA/CoreML decisions reported `decision-source=fresh-probe` against the field docstring and the shipped architecture doc's promise — fixed in `select_embedding_providers` (success paths report `operator-request` when `WAVEFOUNDRY_EMBED_PROVIDER` drove the selection; the CPU fallback after a failed forced probe intentionally stays `fresh-probe`), pinned by `test_operator_forced_gpu_paths_report_operator_request`. (F2, primer strongest challenge, confirmed by reality-checker end-to-end) the shipped single-tier repair was a deterministic no-op in the fresh-process stale-TMPDIR scenario (`gettempdir()` silently falls back to `/tmp`) — fixed with the performance seat's two-tier env-derived repair (set-but-absent `TMPDIR` recreated first, then the `gettempdir()` answer), with security-seat-mandated private `0o700` on every created level via `_mkdir_private` (operator-directed cross-platform posture: `Path.mkdir(parents=True, mode=…)` modes only the leaf); the residual confstr window is recorded in the change doc with the council's SAFE-WITH-BOUNDS ruling on the deferred URL-parse alternative (unbounded parse would be attacker-steerable mkdir since ORT error prose can echo model-derived strings — the repair target must never derive from error text). (F3, code-reviewer/qa) hardenings (a)/(b) were untested — covering tests added (main exit-2 on stall; bounded post-loop wait kwarg + expiry branch). (F4, performance medium) watchdog silent windows — two unconditional heartbeat prints added in `indexer.py` (per embed flush; entering finalize), bounding the silent window to one forward-pass batch. (F5, reality-checker medium) the DF-2 all-platform re-render silently clobbered the committed `.codex/config.toml` `wave_close approval_mode = "approve"` operator guardrail — restored via `git checkout`; renderer merge behavior recorded as follow-up material alongside the explicit-`--platform` copilot-removal footgun. (F6, factor-13/architecture lows) `operator-request` was missing from the doctor docstring + spec-row enum — completed; stale "pure-introspection" test-class docstring corrected. (F7, code-reviewer advisory) `RunFileEncodingTests` now scrubs ambient `PYTHONUTF8`/`PYTHONIOENCODING` so it binds unconditionally. (F8, primer Q4) `ModelPrewarmTimeout` message gained a corrupted-cache secondary hint. Declined with rationale: marker `__cause__`-chain walk (field capture proves the top-level text carries the marker; a wrapped miss fails safe toward CPU); a dedicated `IndexBuildStallError` subtype (architecture note for a future touch); a shorter post-loop wait bound (reuses the existing knob by design).
- **Second-council seat verdicts:** code-reviewer pass-with-findings (DF-1/retry/provenance/env hardening all verified correct and revert-sensitive; quarantine path byte-identical; two untested-hardening minors → F3); architecture-reviewer pass-with-findings conditional on F1 (mechanism/policy split explicit; probe/decision-window constraint structurally enforced; `run_tests`→`subprocess_util` layering safe; `except TimeoutError` breadth enumerated and accepted); security-reviewer pass-with-findings (no trust-boundary regression; URL-parse SAFE-WITH-BOUNDS ruling; mkdir-mode low → F2's 0o700; READONLY-audit info note that `wave_gpu_doctor` transits the repair mkdir); qa-reviewer pass-with-findings (full 1p9lj AC attestation — every named test exists, passes, binds; sole `[~]` legitimate; zero silent checkboxes; AC-4 ruled incomplete-as-delivered → F1; AC-1 holds on wording with narrowing note → F2); reality-checker pass-with-findings (all fix-pass and 1p9lj claims verified; both primer findings independently reproduced; the codex-config clobber caught → F5); performance-reviewer pass-with-findings (retry cost quantified negligible — ~1.3s warm probe attempt, repair 0.06ms, provenance zero hot-path; watchdog silent-window medium → F4; two-tier TMPDIR repair proposed → adopted in F2). Material disagreements: none unresolved — the repair-fix-vs-narrow-note question was resolved by adopting the two-tier repair AND recording the narrowing; the URL-parse adoption question was resolved as deferred-with-bounds-recorded per the security ruling and proportionality.
- **Prepare-phase readiness for late admission `1p9lj` [prepare-council] — 2026-07-02: READY-WITH-NOTES, notes applied** (moderator: wave-council; primer-depth: lightweight — single small change doc, no trust boundary; seats: red-team (fixed), docs-contract-reviewer (rotating, selected because the change plan's contract wording and affected-docs list were the open risks); both seats returned READY-WITH-NOTES and every note was applied to the change doc before implementation: corrected the nonexistent `WAVEFOUNDRY_SETUP_SELECTED_PROVIDER` name to `WAVEFOUNDRY_EMBED_PROVIDER_SELECTED` (one task was unexecutable as written); re-anchored AC-4 to cache-provenance reporting plus a parity test — red-team established the probe chain is ALREADY shared and the field divergence is process-scoped cache state (setup writes the decision env var after a one-shot probe; the doctor re-probes fresh), fencing AC-4 against a selection-plumbing refactor; recorded the `accel_embedder` cached-CPU native-crash guard as a constraint (retry inside the probe/decision window only); narrowed the retry trigger (substring set, retry-count 1, cheap dir-recreate first) protecting AC-5; added operator-gated AC-7 for real-hardware repair effectiveness (simulated tests prove plumbing, not that an in-process repair clears the Apple CoreML working-directory error); firmed the affected-docs list (+`docs/specs/mcp-tool-surface.md` for the `wave_gpu_doctor` row) and added the stale "pure introspection" docstring correction; amended the wave Objective/Summary for the seventh, non-Windows change. Strongest-alternative considered: subprocess-based probe retry with sanitized TMPDIR — declared out of scope (re-opens the windowless-spawn/MCP-stdout territory this wave hardened).)

## Review Evidence

- wave-council-readiness: approved 2026-07-02 — READY. Six bug fixes drawn from the round-2 Windows-audit delta (findings confirmed not covered by wave `1p9hn`), each confined to individual functions with verified current-tree sites, deterministic ACs, and explicit out-of-scope boundaries. Residual risk is concentrated in `1p9it` (setup deadlines) and is bounded by conservative configurable defaults and an AC-5 non-regression gate. Dependency: implement after the currently-uncommitted `1p9hn` lands so line anchors hold. No security-sensitive data path and no architecture-boundary change.
- wave-council-readiness (late admission `1p9lj`): approved 2026-07-02 — READY. Focused two-seat readiness council (red-team + docs-contract-reviewer) on the seventh change only; both READY-WITH-NOTES and all notes applied to the change doc before implementation (env-var name correction, AC-4 re-anchor to cache-provenance reporting, native-crash-guard retry constraint, narrowed retry trigger, operator-gated AC-7, affected-docs additions). The original six-change readiness line below is unchanged and scoped to those six.
- wave-council-delivery: pass-with-findings 2026-07-02 — delivery council completed (red-team adversarial primer; fixed seats architecture-reviewer, security-reviewer, qa-reviewer, reality-checker; rotating seat performance-reviewer; required lanes code-reviewer/qa-reviewer/architecture-reviewer; advisory factor lanes 03/05/12/13). Every required AC across all six changes attested with real evidence; framework suite green (4,152 tests) and docs-lint clean; secrets-filter parity claim empirically confirmed. Two blocking repairs must land on resume before close: (1) DF-1 — model-warm timeout aborts must skip the corruption-quarantine/retry path (confirmed race; small in-place repair plus a covering test); (2) DF-2 — re-render all platform surfaces so the `.cursor`/`.windsurf`/`.github` hooks carry the stdin UTF-8 bootstrap. Non-blocking hardenings and change-doc residual notes are enumerated in Review Checkpoints. Wave returns to implementation (Level 2 loop) for the fix set.
- wave-council-delivery (second pass, covering the fix pass + late-admitted `1p9lj`): pass-with-findings 2026-07-03, all findings resolved in-session — delta council completed (red-team primer with explicit attack candidates; fixed seats architecture-reviewer, security-reviewer, qa-reviewer, reality-checker; rotating seat performance-reviewer; required lanes code-reviewer/qa-reviewer/architecture-reviewer; advisory factor lane 13). DF-1/DF-2 repairs verified landed and revert-sensitive; full `1p9lj` AC attestation passed (sole `[~]` = operator-gated AC-7, legitimate). Eight findings (F1–F8, enumerated in Review Checkpoints) were all fixed in the same session — the load-bearing ones: operator-forced-GPU provenance completed, the repair upgraded to the two-tier env-derived form with cross-platform private 0o700, watchdog heartbeats added, hardening branches test-covered, and the clobbered `.codex/config.toml` wave_close guardrail restored. Post-fix verification: affected modules 359 OK; full suite + docs-lint re-run recorded in the Progress Log.
- operator-signoff: approved 2026-07-03 — operator requested close in-session; `wave_close(mode="dry_run")` passed with no diagnostics, then `wave_close(mode="create")` wrote the closed checkpoint and returned lint clean.

## Progress Log

| Date | Update | Evidence |
|------|--------|----------|
| 2026-07-02 | Paused wave to free the single OPEN slot for the FTS no-position enhancement. All six admitted changes remain implemented; review/close remains pending when this wave resumes. | Operator request in session; dirty worktree contains the implemented `1p9j0` files and change docs. |
| 2026-07-02 | Delivery-phase review completed while paused (operator-invoked Review wave): red-team primer, five council seats, code-reviewer lane, four advisory factor lanes, central suite (4,152 OK) + docs-lint. Verdict PASS-WITH-FINDINGS: two blocking repairs (DF-1 model-warm timeout/quarantine race; DF-2 non-Claude rendered-hook re-render) plus small hardenings and change-doc residuals, all queued for the resume pass. Code fixes intentionally NOT applied yet — `1p9jn` holds the single OPEN slot and the co-resident session is active in the shared tree. | `## Review Checkpoints` delivery entries; `wave-council-delivery` in `## Review Evidence`. |
| 2026-07-02 | Operator closed `1p9jn` and reopened this wave; `1p9lj` admitted as the seventh change (readiness council run, notes applied). Delivery-review fix pass landed: DF-1 (`ModelPrewarmTimeout` skips quarantine/retry + covering tests), DF-2 (all-platform re-render — 12/12 hooks carry the stdin fix, 0 CR bytes), hardenings a–e (stall `TimeoutError` → clean exit 2 in `main`; bounded post-loop `proc.wait`; `_run_file` → `utf8_child_env`; workflow-config `setup` block notes-only; uv/deps forwarded-timeout + `_run_file` kwarg + Windows busy-branch tests), and all change-doc residual notes. `1p9lj` implementation in progress. | Targeted tests: `tests.test_run_tests_lock` + `SetupPhase1DeadlineTests` 24 OK; grep 12/12 hooks; change-doc Progress Logs. |
| 2026-07-02 | `1p9lj` implemented: CoreML probe temp-dir bounded repair+retry inside the probe/decision window; provider-decision provenance (`setup-cache`/`fresh-probe`/`operator-request`) across the setup print, `wave_gpu_doctor`, and `--check-gpu`; recovery guidance on persistent failure; doctor docstring corrected; architecture + tool-surface docs updated. 6 ACs `[x]`, AC-7 `[~]` operator-gated (real-hardware repair effectiveness). Wave is fully implemented (7/7); central full-suite + docs-lint verification pass running. | `tests.test_setup_index` 138 OK; change doc `1p9lj` Progress Log. |
| 2026-07-03 | Second delivery review (operator-invoked Review wave) over the fix pass + `1p9lj`: red-team primer, five seats, code-reviewer lane, factor-13. Verdict PASS-WITH-FINDINGS; all eight findings (F1–F8) fixed in-session, incl. the operator-forced-GPU provenance gap, the two-tier TMPDIR repair with cross-platform private 0o700 (operator-directed), watchdog heartbeats, hardening-branch covering tests, and restoration of the clobbered `.codex/config.toml` wave_close guardrail. Post-fix central verification: full suite 4,182 tests across 41 files OK; docs-lint clean; no `__pycache__`; `wave_review` confirms the delivery signoff with no severity flags. | Second-council checkpoint entries; `wave-council-delivery` second-pass line in Review Evidence; suite output 4,182 OK. |
| 2026-07-03 | Wave closed by operator request. Close dry-run passed with no diagnostics; close apply wrote `Status: closed`, `Completed At: 2026-07-03`, the wave summary, and returned lint clean. Final `wave_current` shows no active wave. | `wave_close(mode="dry_run")`; `wave_close(mode="create")`; `wave_validate` docs-lint clean; `wave_current`. |

## Dependencies

- No external wave dependencies.
