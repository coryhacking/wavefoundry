# Wave Record

Owner: Engineering
Status: closed
Last verified: 2026-06-13

wave-id: `1p5cg streaming-index-build`
Title: Streaming Index Build

## Objective

Bound indexer memory and make build progress file-oriented by streaming the embed/write pipeline. Today a full rebuild materializes every chunk and every vector for a layer in memory before writing (peak ≈ the whole corpus), and progress reads "chunk X / total" because the total is counted upfront. When this wave closes, a full rebuild streams files through a bounded buffer (chunk → embed a full GPU batch → append → flush), so peak memory is bounded by the buffer (not corpus size) — very large repos index without OOM — and progress reports "file N / M files".

## Changes

Change ID: `1p5ch-ref streaming-bounded-buffer-embed-pipeline`
Change Status: `implemented`

Change ID: `1p5cx-bug reindex-offline-first-model-load`
Change Status: `implemented`

Change ID: `1p5d6-enh lazy-embedder-load-per-layer`
Change Status: `implemented`

Completed At: 2026-06-13

## Wave Summary

Wave `1p5cg` (Streaming Index Build) delivered 3 changes: Stream the full-rebuild embed/write path through a bounded buffer, Load reindex embedders offline-first, falling back to online, and Load a layer's embedder only when that layer has chunks to embed. Notable adjustments during implementation: Stream the full-rebuild embed/write path through a bounded buffer: Added `StreamingRebuildParityTests` (row-identical stream-vs-batch on a fixture corpus + buffer override/floor) and `OversizedFileGuardTests`. Fixed a phantom-`docs.lance`-dir regression (0-chunk layers pre-creating the lock dir broke the incremental path) via lazy lock acquisition.; Stream the full-rebuild embed/write path through a bounded buffer: Added `test_streaming_rebuild_bounds_buffer_and_reports_file_progress` (AC-1 + AC-4): drives the real `_run_streaming_full_rebuild` over 14 files with `buffer_chunks=4`, asserts multiple flushes, every chunk written once, peak batch ≤ `buffer + max per-file chunks` (< corpus), and "indexed file N/M files" progress with no chunk-total pre-count. Fixed the full-rebuild summary line that reported "0 new" by reading the written row count. Full suite **3099 OK**; docs-lint clean.; Load a layer's embedder only when that layer has chunks to embed: Gated the two `_get_embedder` calls in `build_index` on `full or new_<layer>_chunks`. Added 3 tests (docs-only skips code embedder; code-only skips docs embedder; full loads both). Updated one existing test (`test_incremental_line_window_shift_reembeds_affected_chunks`) whose ordered-list embedder mock assumed both layers load — `notes.custom` is pure code, so it now maps embedder by model name.

**Changes delivered:**

- **Stream the full-rebuild embed/write path through a bounded buffer** (`1p5ch-ref streaming-bounded-buffer-embed-pipeline`) — 6 ACs completed. Key decisions: --------
- **Load reindex embedders offline-first, falling back to online** (`1p5cx-bug reindex-offline-first-model-load`) — 3 ACs completed. Key decisions: --------; Cached-first via `local_files_only=True`, online fallback on miss
- **Load a layer's embedder only when that layer has chunks to embed** (`1p5d6-enh lazy-embedder-load-per-layer`) — 2 ACs completed. Key decisions: --------; Gate the eager `_get_embedder` calls on per-layer work
## Journal Watchpoints

- **Sequencing (follow-up):** `1p58z` has closed and this wave is now active — the single-OPEN-wave block is cleared and implementation can proceed. Scope is independent (indexer internals only).
- **Correctness (load-bearing):** the streamed path MUST produce an index identical to the current batch path — same chunk set, same vectors, same row contents/ids. Gate with a parity test (stream vs batch on a fixture corpus → identical rows) before anything else.
- **LanceDB write:** switch the full-rebuild create-with-all-rows to create-(empty/first-batch)-then-append; build the vector (HNSW) + FTS index ONCE after all appends, not per flush. Confirm index quality/row count unchanged.
- **GPU batch efficiency:** the buffer flush threshold must be ≥ `STATIC_BATCH` (64) so streaming doesn't shrink to tiny per-file batches and lose GPU throughput. Measure that a full rebuild stays comparable to the current ~2 min on this repo.
- **Don't regress the incremental path:** the non-rebuild path already reuses vectors by content hash — keep that intact; streaming targets the full-rebuild path.
- **Framework-edit gate** before editing `indexer.py`.
- **`1p5cx` is a LATE admission (operator feedback during delivery), not covered by the prepare-council verdict** — needs a focused pre-close review like any late add. Operator-directed fold-in: the reindex pinged the HF Hub on every build (unauthenticated-request warning + latency) because the launcher prewarm's `hf_hub_download` ran unconditionally. Fix is cached-first model loads (`local_files_only=True`) in `accel_embedder` + `indexer`; no parity impact (same weights). Touches framework model-loading — gate on the full suite + a live warm-cache reindex showing zero warnings.

## Review Evidence

- wave-council-readiness: READY-WITH-NOTES — readiness sign-off recorded 2026-06-14. Single, tightly-scoped refactor (one file, one purpose) — the focused wave 1p58z's grab-bag should have been. Notes: (1) **output parity is the gating test** — the streamed full-rebuild must produce rows (ids, text, vectors) identical to today's batch path on a fixture corpus (AC-3); implement that test first and treat any divergence as a hard block. (2) **Buffer threshold ≥ `STATIC_BATCH` (64)** so streaming keeps GPU batches full — a full rebuild on this repo must stay comparable to the current ~2 min (AC-6). (3) **Full-rebuild path only** — do not touch the incremental path's content-hash vector reuse (AC-5). No security/secrets/binding-faithfulness concerns (internal restructure, identical index output).
- wave-council-delivery: READY-WITH-ONE-NOTE — delivery sign-off recorded 2026-06-13. The gating output-parity invariant (AC-3) is met: a fixture-corpus A/B test proves the streamed writer is row-identical (id, text, vector, chunk_hash) to the batch reference, and a live full GPU rebuild produced docs 13758 / code 10118 rows with `vector_idx` (HNSW) + `text_idx` (FTS) built once per layer. Memory bound (AC-1) and file-progress (AC-4) are now asserted by a dedicated test (peak batch ≤ buffer + one file's chunks, "indexed file N/M files", no chunk pre-count). Incremental path untouched (AC-5, existing tests green); full suite 3099 OK + docs-lint clean (AC-6); GPU wall-time ~2 min, comparable (AC-2/AC-6). Two in-session fixes applied during review: the full-rebuild summary line reported "0 new" (now reads the written row count), and the streaming writer's table lock is now released in a runner `finally` so a mid-stream exception frees it in-process. **One operator-review note (acceptance):** `_stream_embed_write` (the former full-rebuild writer, ~75 LOC) now has no production caller — it survives only as the differential-parity oracle in two tests. Recommend relocating it into the test module so production carries no dead code while the independent oracle is preserved; deferred to operator decision (relocate vs keep-as-documented-oracle vs delete) because it is the safety oracle, not a trivial cleanup. **Resolved 2026-06-13 — operator chose delete:** `_stream_embed_write` removed entirely; the A/B parity test was converted to a *buffer-invariance* test on `_StreamingLayerWriter` itself (one big `add()` — which IS the batch write — vs tiny-buffer many-flushes → row-identical), which proves the same invariant more directly with no second implementation to drift. `test_server_tools`'s row-count test was repointed at `_StreamingLayerWriter`. Suite 3107 OK; docs-lint clean.
- wave-council-delivery (1p5cx, late add): READY — focused delivery review of the operator-admitted offline-first fix, recorded 2026-06-13. Root cause correctly localized: the warm-cache warning is from the launcher prewarm's unconditional `hf_hub_download` (revision/etag round-trip), not `build_index` (a no-change incremental embeds nothing) — confirmed by reading the build log sequence. Fix is the first-class `local_files_only=True` idiom at the accel download chokepoint (`_hf_download_cached_first` → `_resolve_clean_onnx` / `_resolve_reranker_cpu_files` / `_ensure_fastembed_model_cached`) plus the fastembed CPU path (`_text_embedding_cached_first`), with an online fallback on a genuine cache miss so a cold cache self-heals. An earlier global-offline-toggle prototype was correctly abandoned after a test caught it stranding a cold-cache process permanently offline (the constant is captured at import; setting it post-import then "restoring" the poisoned value). No parity surface: cached vs downloaded load the same weights, vectors identical. Verified: full suite 3104 OK with per-helper cached-first/fallback tests; a live warm-cache incremental reindex prewarmed the reranker + both GPU embedders with zero unauthenticated-request warnings. No security/secrets/binding change.
- wave-council-delivery (1p5d6, late add): READY — focused delivery review of the operator-admitted lazy-embedder-load enhancement, recorded 2026-06-13. Scope is a two-condition gate (`full or new_<layer>_chunks`) on the existing `_get_embedder` calls in `build_index`; safety rests on the verified invariant that `_lance_incremental_write` only touches an embedder when a layer has chunks to embed (delete-only / no-op writes never embed), so a `None` embedder for an idle layer is correct. No retrieval/parity surface (a layer that loads no model also writes no rows). The change surfaced one stale test assumption — `test_incremental_line_window_shift_reembeds_affected_chunks` used an ordered `[docs_mock, code_mock]` mock and `notes.custom` is pure line-window code, so the single code load was grabbing the docs mock; fixed by mapping the mock by model name (the assertion — 2 code windows re-embedded, 0 docs — is unchanged and still passes). Verified: full suite 3107 OK with 3 new gating tests (docs-only skips code; code-only skips docs; full loads both); a live docs-only incremental loaded only the arctic docs model with no bge line. Out of scope (documented): the `setup_index` launcher prewarm still loads both (it runs before change detection); this fixes the direct-spawn hook path the operator observed.
- operator-signoff: approved — operator authorized closure 2026-06-13 after the wave review (streaming + offline-first + lazy-load; `_stream_embed_write` deleted and parity converted to a buffer-invariance test).

## Review Checkpoints

- **Prepare-phase Wave Council [prepare-council] — 2026-06-14: PASS WITH NOTES** (moderator: wave-council; primer-depth: standard; seats: red-team, architecture-reviewer, security-reviewer, qa-reviewer, reality-checker, security-reviewer; rotating-seat: security-reviewer; strongest-challenge: the streamed full-rebuild could silently produce a DIFFERENT search index than the batch path — row ordering, content-hash vector reuse, LanceDB create-vs-append semantics, and the single final index build are all places a divergence could hide — MITIGATED because AC-3 mandates an output-parity test (streamed rows identical to the batch path on a fixture corpus) as the gating test implemented first, the change is scoped to the full-rebuild path only so the incremental path's vector reuse is untouched (AC-5), and the GPU-batch-fullness risk from tiny per-file batches is bounded by a buffer threshold of at least STATIC_BATCH 64 plus a wall-time check (AC-1, AC-6); strongest-alternative: keep the batch path and only cap memory by processing the file list in fixed sub-batches without true streaming — rejected because it still materializes a sub-batch-sized chunk set, does not yield per-file progress, and does not remove the total-chunk pre-count, so it solves neither problem cleanly; security-reviewer note: despite the auto-flagged trust-boundary heuristic there is no auth/secrets/binding change — the refactor touches only the indexer embed/write path and the produced index content is identical, so there is no security surface)

- **Delivery-phase Wave Council [delivery-council] — 2026-06-13: PASS WITH ONE NOTE** (moderator: wave-council; primer-depth: standard; seats: red-team, architecture-reviewer, security-reviewer, qa-reviewer, reality-checker, performance-reviewer; rotating-seat: performance-reviewer; strongest-challenge: the streamed full-rebuild could silently diverge from the batch path or leak resources on an edge — but each vector was probed and refuted: output parity is proven by a row-identical A/B test (id/text/vector/chunk_hash) plus a live rebuild matching row counts with both indexes built once, the zero-chunk-layer phantom-dir regression that broke the incremental path was found and fixed via lazy lock acquisition (incremental tests green), the mid-stream-exception lock leak was closed by releasing the lock in a runner finally, and the memory bound is honestly characterized as buffer + one file's chunks (not strictly the threshold) and asserted by test; strongest-alternative: process the file list in fixed sub-batches without true per-file streaming — rejected because it still materializes a sub-batch chunk set, keeps a total-chunk pre-count, and yields no per-file progress, solving neither the memory nor the progress problem; performance-reviewer note: buffer floored at EMBED_BATCH_SIZE 64 keeps GPU batches full and a live rebuild held ~2 min, so no throughput regression; remaining item is acceptance-only — `_stream_embed_write` is now a test-only differential oracle with no production caller, recommended for relocation into the test module)

## Dependencies

- No external wave dependencies.
