# Wave Record

Owner: Engineering
Status: closed
Last verified: 2026-07-06

wave-id: `1p9q8 graph-index-accuracy`
Title: Graph Index Accuracy

## Objective

Close the four highest-value recall gaps in graph-edge extraction while preserving the never-bind-on-ambiguity faithfulness stance: Python receiver-call resolution (the framework's own language has none), same-scope disambiguation for Rust/C# (already proven for Java/Kotlin/Go), degraded line-scan extraction for files over the 2 MB parse cap (today a silent graph hole), and AST-anchored DI edges for Python/TS (today JVM/.NET only). Every change carries a calibration gate and the wave carries an adversarial faithfulness review.

## Changes

Change ID: `1p9q4-enh python-receiver-annotation-resolution`
Change Status: `implemented`

Change ID: `1p9q5-enh same-package-disambiguation-rust-csharp`
Change Status: `implemented`

Change ID: `1p9q6-enh oversized-file-degraded-extraction`
Change Status: `implemented`

Change ID: `1p9q7-enh di-signal-ast-and-language-expansion`
Change Status: `implemented`

Completed At: 2026-07-06

## Wave Summary

Wave `1p9q8` (Graph Index Accuracy) delivered 4 changes: Graph accuracy: Python receiver resolution from annotations and constructor assignments, Graph accuracy: Rust module-scope receiver disambiguation (+ C# verify-and-close), Graph accuracy: degraded line-scan extraction for files over the tree-sitter parse cap, instead of total omission, and Graph accuracy: DI binding/injection signals for Python and TypeScript, AST-anchored. Notable adjustments during implementation: Graph accuracy: Python receiver resolution from annotations and constructor assignments: Scoped from the graph-index accuracy evaluation. Confirmed: Python uses `ast.parse` with no `_resolve_python_call_target` peer to the other languages' resolvers; self-hosted graph has 7,674 function nodes with `calls` edges predominantly `EXTRACTED`; edge confidence directly weights path cost and impact propagation (`graph_query.py:836-869,886-893`).; Graph accuracy: Python receiver resolution from annotations and constructor assignments: **Freshness reconciliation (reality-checker lane).** Verdict: READY-WITH-CORRECTIONS-APPLIED. Premise still VALID — no `_resolve_python_call_target` exists (verified live); receiver-typed calls that fail to bind stay `external::`, and 1p9q4's two-signal model targets exactly that residue. STALE-ANCHOR fixes applied: `_extract_python_artifact` 5665→9807; resolver anchors 5066/3241/3493/4463 → 3619/3803/4090/4342/5915; `_path_edge_cost` 836-844→1014, `_EXTRACTED_EDGE_WEIGHT` 856→1044. **PARTIALLY-superseded rationale:** the "`calls` dominated by EXTRACTED" claim is stale post-1p7dg (v34) — Python resolved-share already lifted (EXTRACTED 90.4%→31.9%); re-baseline AC-4 against post-1p7dg state, NOT the 2026-07-03 mix. Mechanism/scope unchanged. Coordinated version bump target is now **39→40** (current `GRAPH_BUILDER_VERSION="39"`, `graph_indexer.py:38`).; Graph accuracy: Rust module-scope receiver disambiguation (+ C# verify-and-close): Scoped from the graph-index accuracy evaluation. Same-package tier gated to Java/Kotlin/Go; Rust/C# import-edge disambiguation from v25; multi-lang pack Java/Swift/JS-TS (no Rust/C# fixtures).

**Changes delivered:**

- **Graph accuracy: Python receiver resolution from annotations and constructor assignments** (`1p9q4-enh python-receiver-annotation-resolution`) — 7 ACs completed. Key decisions: Two explicit signals only — annotations + constructor assignments (approach A).; No inheritance/MRO walk — method must exist on the named class.
- **Graph accuracy: Rust module-scope receiver disambiguation (+ C# verify-and-close)** (`1p9q5-enh same-package-disambiguation-rust-csharp`) — 8 ACs completed. Key decisions: Key C# on declared namespace and Rust on module scope — language rules, not directories.; Reduce C# to a verify-and-close; the same-namespace tier already exists (Wave 1p4ev).
- **Graph accuracy: degraded line-scan extraction for files over the tree-sitter parse cap, instead of total omission** (`1p9q6-enh oversized-file-degraded-extraction`) — 8 ACs completed. Key decisions: Bounded line-anchored scan for imports + top-level definitions only, no call edges (approach A).; Fallback definitions participate fully in cross-file candidate sets.
- **Graph accuracy: DI binding/injection signals for Python and TypeScript, AST-anchored** (`1p9q7-enh di-signal-ast-and-language-expansion`) — 7 ACs completed. Key decisions: Add AST-anchored Python/TS extraction; leave the JVM/.NET regex module untouched (approach A).; Conservative named-idiom gate (FastAPI; NestJS/Inversify) rather than generic decorator heuristics.
## Journal Watchpoints

- All four changes are binding/detection-faithfulness changes: a consolidated adversarial review lane at wave review is required (security-control-faithfulness rule) — green unit tests alone are not sufficient evidence.
- Coordinate a single wave-level `GRAPH_BUILDER_VERSION` bump across all four changes at integration; `graph_indexer.py` is the shared hub (Python extractor: `1p9q4`+`1p9q7`; cross-file pass: `1p9q4`+`1p9q5`; size gate: `1p9q6`) — merge order matters more than parallelism.
- Multi-language pack fixture conventions are shared by `1p9q5` (Rust/C#) and `1p9q7` (Python/TS DI) — agree the fixture directory layout once before either lands; spawned test agents must use the `~/.wavefoundry/venv` interpreter or cross-language tests silently skip.
- Follow-up seam: `1p9q7` deliberately defers annotation-only `Depends()` to avoid entangling with `1p9q4`'s annotation model — if both land, evaluate the connection as a candidate follow-up change, not silent scope.
- If wave `1p9q3` (efficiency) implements first, its differential incremental-vs-full harness doubles as an extra oracle for these binding changes; if this wave lands first, all four re-extractions ride the full-rebuild path (version bump) with no interaction.
- **[integration 2026-07-05] `GRAPH_BUILDER_VERSION` 39→40 landed** (one coordinated bump; changelog head names all four changes' output-shape contributions). **No `CLUSTER_BUILDER_VERSION` bump** — assessed and declined with evidence: `graph_cluster.py` is untouched (its `_project_undirected_projection` and `_RELATION_WEIGHTS` are unchanged), and the cluster staleness gate (`graph_cluster.py:1064-1073`) already forces a full recluster whenever the graph's `input_fingerprint` OR `graph_builder_version` changes — both changed here, and the live rebuild confirmed a fresh recluster (47 communities, `graph_builder_version=40`, `cluster_builder_version` held at 11). The new nodes/edges (line-scan defines/imports, receiver-rebound calls, Py/TS injects/binds) flow through the identical projection with no clustering-algorithm change — same discipline as 1p7dh/1p9qd/1p9qg, which added projection-visible nodes/edges without a cluster bump. The CLUSTER version governs the community-label cache contract, which did not change.
- **Live MCP sessions must `wave_mcp_reload` after this wave** — the node/edge/property shape changed (new `CONSTRUCTION_RESOLVED` confidence, `rust_mod_decls`/`rust_inline_mods` + line-scan node properties, Py/TS DI edges). A server started against the v39 graph serves stale shape until reloaded.

## Participants

- code-reviewer — all four changes touch `.wavefoundry/framework/scripts/*.py`
- qa-reviewer — all change docs carry AC priority tables
- architecture-reviewer — extraction/resolution module seams in `graph_indexer.py` shared by all four changes
- performance-reviewer — new per-file scan patterns and extractor hot-path additions (`1p9q6` line scan; `1p9q4`/`1p9q7` AST-walk additions)
- red-team, reality-checker — council seats (prepare phase); the consolidated adversarial faithfulness lane re-runs at implementation review for all four binding changes

## Review Checkpoints

- Prepare wave — readiness verdict (2026-07-03): READY. Council ran at standard primer depth. Red-team's strongest challenge — `1p9q6` fallback definitions entering candidate sets will demote some existing binds repo-wide (previously "unique" only because the oversized twin was invisible) — was examined and accepted as designed-correct behavior: the demotions are faithfulness restorations, the calibration counts make the delta visible, and the risk table already dispositions it. Amendments applied from seat findings: `1p9q5` AC-2 gained C# namespace-key normalization across nested/compound/file-scoped declaration styles (qa seat — the likeliest silent scope-key bug); `1p9q7` Requirement 4/AC-1 gained alias-aware idiom identification with same-named-impostor refusal (red-team follow-through — both over-fire and under-fire modes); `1p9q6` Requirement 3 gained encoding robustness for BOM/decode-error/no-final-newline shapes (reality seat — big generated files are exactly where odd encodings live). Reality seat also noted `1p9q4`'s calibration may show a modest delta on the self-hosted repo given its annotation density — the doc already handles this honestly (re-scope on negligible delta rather than ship on faith). Strongest alternative (imports-only line scan for `1p9q6`, halving invention risk) recorded and declined — definitions are the half that reconnects inbound references, and the invention risk is bounded by line anchoring plus adversarial fixtures. AC priorities recorded on all four change docs. Product-owner acknowledgment: not applicable (framework-internal accuracy work; no product behavior shift).
- Security seat (rotating): the `1p9q6` line scan runs static anchored patterns over repo files with a hard byte ceiling and line-length guard — no user-supplied patterns, no unbounded regex, no denial-of-build exposure from pathological files (the ceiling degrades to logged skip). Graph edges feed impact analysis used in reviews, so over-extraction is an integrity concern, not just accuracy — covered by the exact-set DI fixtures, the refusal-by-default stance, and the mandatory adversarial faithfulness lane at implementation review. No trust boundary is crossed by any of the four changes.
- **Prepare-phase Wave Council [prepare-council] — 2026-07-03: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, architecture-reviewer, security-reviewer, qa-reviewer, reality-checker, security-reviewer; rotating-seat: security-reviewer; strongest-challenge: 1p9q6 fallback definitions demote existing binds repo-wide where uniqueness came from twin invisibility — accepted as designed-correct faithfulness restoration, visible in calibration counts; strongest-alternative: imports-only line scan halving invention risk — declined, definitions are the half that reconnects inbound references and invention risk is bounded by anchoring plus adversarial fixtures)

- **Prepare-phase Wave Council [prepare-council] — 2026-07-05: PASS WITH NOTES (independent RE-readiness; supersedes the 2026-07-03 PASS)** (moderator: wave-council; primer-depth: standard; seats: red-team, reality-checker, qa-reviewer; rotating-seat: none additional; strongest-challenge: `1p9q5`'s Rust same-scope tier was framed as a fourth instance of the Go/Java/C# pattern, but the v39 tree models NO Rust module scope at all (no `mod_item` modeling; `declared_package` is Java/Kotlin-only) and "same file" is not Rust's visibility rule (cross-file = module tree not directory; inline `mod {}` blocks are separate scopes) — so as-freshened the Rust tier was either a near-no-op or a hidden request to build Rust module-tree modeling; strongest-alternative: split the Rust module-modeling to its own plan (`1rs45`-style) or accept a same-file near-no-op — DECLINED by operator decision to FUND Rust module-tree modeling in this wave (`1p9q5` rewritten: AC-1 module-path model, AC-2 tier, AC-3 `[~]` C# verify-and-close). This re-readiness pass was run because the 2026-07-03 readiness predated four waves that rewrote `graph_indexer.py` (1p9q3/1p9qh/1p9qi/1rs44 → v39) AND its recorded roster was defective (a DUPLICATED `security-reviewer` seat with no per-seat evidence). Reality-checker (freshness lane) re-verified all four docs against v39 with every anchor confirmed accurate, found the `1p9q5` C# half ALREADY IMPLEMENTED (Wave 1p4ev — not 1p9qh), and reconciled `1p9q6`'s degradation convention to MIRROR the landed 1p9qe recovery convention and `1p9q7`'s origin-check to the landed 1p9qi convention. Red-team primer confirmed `1p9q4`/`1p9q6`/`1p9q7` BOUNDED and surfaced the Rust-scope challenge above + two bounded `1p9q7` decisions (unresolved DI → plain `external::` not `external::di::`; collect-side needs merge plumbing). QA confirmed all ACs testable/non-vacuous with the `1p9q5` re-scope correctly marked, and flagged two AC-text gaps (1p9q6 per-file count-property pin; 1p9q7 TS alias/impostor origin-check) — both APPLIED in-doc (AC-1b added to 1p9q6; AC-2 extended in 1p9q7). Readiness disposition: all four changes ready to implement in sequence 1p9q6 → 1p9q5 → 1p9q4 → 1p9q7; single coordinated 39→40 bump; the mandatory adversarial-faithfulness lane is selected for delivery review — the Rust module model is a NEW binding surface.)

- **Implementation-complete [implementation-complete] — 2026-07-05: DONE (integration lane)**. All four changes implemented and integrated under one coordinated `GRAPH_BUILDER_VERSION` 39→40 bump in `graph_indexer.py` (the shared hub). Per-change deliverables: **1p9q4** — Python receiver/construction resolution (annotated params/attrs/locals incl. string forward-refs + faithful Optional/union/generic unwrap; `local`/`self.attr`/module-global constructor assignments) binding calls at `RECEIVER_RESOLVED`/`CONSTRUCTION_RESOLVED`, plus the union/generic over-bind faithfulness fix; **1p9q5** — Rust crate-relative module-path model (`rust_mod_decls`/`rust_inline_mods` node properties, `#[path]`/inline-`mod`/crate-root/per-file-fallback) + `.rs` same-module tier (ordered after explicit-import disambiguation; bounds: no cross-crate, no re-export graph), C# verify-and-close; **1p9q6** — oversized-file line-scan tier (`extraction: "line_scan"` module/def nodes + `line_scan_defines`/`line_scan_imports`/`line_scan_skipped` properties; imports + top-level defs only, no calls/reads); **1p9q7** — AST-anchored Python (FastAPI `Depends`/`Annotated`) + TS (NestJS `@Injectable`/`@Inject`/`@Module`, Inversify `bind().to()`) `injects`/`binds` DI edges, unresolved target = plain `external::`, JVM/.NET path byte-identical. Integration: version-pin test updated to `"40"`; CLUSTER_BUILDER_VERSION assessed no-bump with evidence (see watchpoints); deferred spec/arch docs updated (`docs/architecture/graph-index-system.md` — Rust same-scope tier + module-path decision note, DI Python/TS coverage; `chunking-and-indexing-pipeline.md` line-scan tier verified already-current from 1p9q6); AC/task bump checkboxes flipped, adversarial-faithfulness review clauses kept honest (run at delivery review). Live full graph rebuild clean: **11,794 nodes / 33,160 edges** (v39 baseline 11,794 / 33,155; net +5 edges), recluster to 47 communities, builder-version mismatch forced full corpus re-extract (heal path confirmed). Honest self-host signatures (Python-heavy, Rust-free, no oversized files, no DI idioms): 1p9q4 = **200 `CONSTRUCTION_RESOLVED`** + 9,715 `RECEIVER_RESOLVED` calls edges (the productive signature); 1p9q5/1p9q6/1p9q7 = **0** on this repo (no Rust, no >2 MB files, no FastAPI/NestJS/Inversify) — as anticipated. Full suite green; `wave_validate` clean; `__pycache__` clean. The mandatory adversarial-faithfulness review lane still runs at delivery review before close. All four Change Status remain `implemented`.

- **Delivery-phase Wave Council [wave-council-delivery] — 2026-07-06: PASS WITH NOTES (seats unanimous ship)** (moderator: wave-council; primer-depth: standard; seats: red-team, code-reviewer, qa-reviewer, architecture-reviewer, security-reviewer, performance-reviewer; rotating-seat: none additional; strongest-challenge: the code lane found TWO faithfulness defects the green tests could not see — (1) `1p9q4` stamped `RECEIVER_RESOLVED`/`CONSTRUCTION_RESOLVED` on `calls` edges that dangle to unresolved `external::Type.method` targets (a dishonest confidence label that would have shipped disguised as an improvement), and (2) `1p9q7`'s TS DI `binds` path picked an arbitrary same-name candidate instead of refusing on ambiguity (a potential WRONG bind) — both fixed in the review fix round (FIX 1 = a finalize-stage `_downgrade_unresolved_typed_calls` honesty pass for dotted external calls; FIX 2 = unique-candidate refusal in `_resolve_di_target`) and verified by the delivery-council primer, which additionally found FIX 1 correctly also honests-out unresolved Java `super.`/`staticorinherited#` over-claims (behavior kept, false docstring corrected, Java regression pin added); strongest-alternative: narrow FIX 1 to Python-origin edges — DECLINED, the Java relabel is the honest label. The mandatory adversarial-faithfulness lane returned PASS — NO wrong binds on any of the four new surfaces under extensive live probing (Python receiver conflict/union/generic/inheritance refusal, Rust inline-mod/`#[path]`/orphan-fallback scope separation, line-scan string/comment/ceiling invention immunity, DI impostor refusal both languages); the two code-lane findings were a mislabel and a same-name-pick, both now fixed. Disposition: SHIP. Non-feature performance tax within noise; one recorded perf follow-up (1p9q7 DI walk got an idiom pre-check; the pre-existing `_source_location` splitlines-per-symbol hotspot recorded for a future lever). Suite 4,663; `GRAPH_BUILDER_VERSION` 40 (single coordinated bump); `CLUSTER_BUILDER_VERSION` 11 (no bump — graph-version + fingerprint change forces recluster anyway); live v39→v40 rebuild clean (11,794 nodes / 33,160 edges). **HONEST WAVE OUTCOME for operator disposition: `1p9q5`'s Rust module model is durable/correct infrastructure but its disambiguation tier produces ZERO productive binds today (a Rust module ≈ one file, so cross-file candidates never share the caller's module; the candidate index collapses same-file same-leaf symbols); the model persists as node properties and never wrong-binds — operator to decide whether the model alone justified funding it or to revisit.**)
  - Delivery-lane evidence (distinct from the verdict line): **security-reviewer** (mandatory adversarial-faithfulness lane, PASS) live-probed all four surfaces — every over-bind refused, incremental==full-rebuild, no wrong binds; only an inert cosmetic-label observation the code lane escalated. **code-reviewer** (REQUEST CHANGES → resolved) found the two faithfulness defects above, both reproduced and fixed; the disputed severity of the first was resolved to moderate (downstream inert — impact weights incoming-to-project edges, path treats external as non-transitive) but fixed for honesty. **qa-reviewer** (PASS WITH NOTES) verified every required AC's tests are load-bearing exact-set/exact-key/flip with honest zero-delta (1p9q5) and modest-delta (1p9q4 +10) reporting, suite and version-pin "40" verified; flagged the 1p9q4 doc gap (fixed). **architecture-reviewer** (APPROVE WITH NOTES) verified in code the candidate-index seam is contained, cluster no-bump holds, the DI plain-`external::` decision is principled, the line-scan/sql-recovery parallel is genuine, and the Rust module model is a coherent disclosed bet; flagged the 1p9q4 doc gap (fixed) + a NamedTuple-seam note for a future 6th language. **performance-reviewer** (APPROVE WITH FINDINGS) measured no non-feature tax at full-corpus scale, all gates cheap; found the 1p9q7 DI walk un-gated (fixed) and recorded the pre-existing `_source_location` hotspot. **red-team** (delivery-council primer over the fixed state) confirmed FIX 1/2/3 hold with correct scoping and surfaced the FIX-1 Java-super scope-claim correction (applied).

## Review Evidence

- architecture-reviewer: approved 2026-07-03 — the four changes touch the extraction/resolution seams in `graph_indexer.py` (Python extractor shared by 1p9q4/1p9q7; cross-file pass shared by 1p9q4/1p9q5; the size-gate region for 1p9q6); the integration risk is shared-hub coordination, dispositioned by the single coordinated version bump + the merge-order watchpoints; no boundary/flow/verification-architecture concern. (Recorded to complete the 2026-07-03 roster⇄evidence pairing; the 2026-07-05 re-readiness above supersedes the readiness disposition, and the C# half of 1p9q5 was since found already-implemented.)
- wave-council-readiness: approved 2026-07-03 — prepare council synthesis verdict READY after amendments (1p9q5 namespace normalization, 1p9q7 alias-aware idioms, 1p9q6 encoding robustness); no unresolved blocking findings
- red-team: re-readiness primer 2026-07-05 — code-grounded feasibility over the freshened/re-scoped v39 docs; 1p9q4/1p9q6/1p9q7 BOUNDED; surfaced the 1p9q5 Rust-no-module-model challenge (led to the operator fund-Rust-modeling decision) and two bounded 1p9q7 decisions (external:: form; collect-side merge). Recommended sequence 1p9q6→1p9q5→1p9q4→1p9q7.
- reality-checker: re-readiness freshness lane 2026-07-05 — all four docs re-verified against v39 with every code anchor confirmed accurate; found the 1p9q5 C# half already implemented (Wave 1p4ev), reconciled 1p9q6 to mirror the 1p9qe recovery convention and 1p9q7 to the 1p9qi origin-check convention; anchors corrected in-doc.
- qa-reviewer: READY WITH NOTES 2026-07-05 — all four docs' core ACs testable and non-vacuous with verified anchors and the 1p9q5 re-scope correctly `[~]`-marked; two AC-text gaps flagged (1p9q6 per-file count properties, 1p9q7 TS alias/impostor origin-check) and both applied in-doc before sign-off.
- wave-council-readiness: approved 2026-07-05 — READY (supersedes 2026-07-03). Fresh re-readiness after the v39 rewrites + the defective prior roster; 1p9q5 re-scoped and operator-funded for Rust module modeling; 1p9q6/1p9q7 convention-reconciled; both qa AC-text gaps applied; the mandatory adversarial-faithfulness lane selected for delivery review. No product-owner acknowledgment required (framework-internal accuracy work).
- code-reviewer: approved with findings, findings fixed 2026-07-06 — delivery lane; found two faithfulness defects invisible to the tests (1p9q4 confidence-label inflation on unresolved external targets; 1p9q7 TS DI binds arbitrary same-name pick), both fixed in the review fix round and primer-verified.
- qa-reviewer: approved with notes 2026-07-06 — delivery lane; every required AC backed by load-bearing exact-set/exact-key/flip tests, honest zero-delta (1p9q5) and modest-delta (1p9q4) reporting, suite 4,663 and version-pin "40" verified; the 1p9q4 architecture-doc gap it flagged was fixed.
- security-reviewer: approved 2026-07-06 — mandatory adversarial-faithfulness lane; PASS, no wrong binds on any of the four new binding/detection surfaces under extensive live probing; incremental==full-rebuild; the two escalated code-lane findings were a mislabel and a same-name pick, both fixed.
- architecture-reviewer: approved with notes 2026-07-06 — delivery lane; candidate-index seam contained, cluster no-bump reasoning holds, DI namespace + line-scan/recovery conventions coherent, Rust module model a disclosed durable bet; the 1p9q4 doc gap was fixed, NamedTuple-seam note recorded for a future 6th language tier.
- performance-reviewer: approved with findings 2026-07-06 — delivery lane; non-feature tax within noise at full-corpus scale, all gates genuinely cheap; the 1p9q7 un-gated DI walk got an idiom pre-check, and the pre-existing `_source_location` splitlines-per-symbol hotspot is recorded as a follow-up.
- wave-council-delivery: approved 2026-07-06 — PASS WITH NOTES, seats unanimous ship (structured synthesis in Review Checkpoints). Five delivery lanes + delivery-council primer; the mandatory adversarial-faithfulness lane found no wrong binds; the code lane's two faithfulness defects were fixed and adversarially re-verified; the honest wave outcome (1p9q6 real value, 1p9q4 modest +10 + two faithfulness fixes, 1p9q7 DI Python/TS with the binds bug fixed, 1p9q5 durable-but-zero-delta module model) is recorded for operator disposition. Suite 4,663; version 40; live v39→v40 heal verified; both edit gates closed.
- operator-signoff: approved 2026-07-06 — operator confirmed closure ("close and commit") after the close-readiness readout; the 1p9q5 durable-Rust-module-model / zero-delta-tier outcome accepted as recorded (model shipped as durable infrastructure; tier activation is a recorded follow-up contingent on candidate-index collapse relaxation).

## Dependencies

- No external wave dependencies.
