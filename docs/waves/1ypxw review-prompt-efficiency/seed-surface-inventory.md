# Seed and Surface Inventory

Owner: Engineering
Status: active
Last verified: 2026-09-22

## Purpose and boundary

Durable sentence-disposition, propagation and protected-anchor evidence for changes `1ypxu` and `1ypxv`. This record has a distinct lasting purpose: it captures the pre-edit text and test obligations against which their contract-prose edits and measured sizes are reviewed. It is reused for post-edit census and size results. This is implementation inventory, not independent delivery approval. No seed, template, role or prompt edit was made during inventory. The coordinator confirmed the required stable retrieval baseline before this record was written.

## Landed anchor verification

Seed 209's separate writing-hand paragraph from `1ypy6` is present under **Typed authoring and human view**. Its substrate-comparison sentence is present immediately after the pinned census sentence under **Code-Grounded Verification**. Preserve both verbatim. The change to the three-claim-shape sentence is earlier in that paragraph, before the census sentence.

The freshness anchor is the final sentence of the `approval_recheck_lanes` bullet under **Derived and lifecycle invariants**, not a separate bullet headed Approval freshness. Append the delivery-receipt explanation without deleting the existing sentence.

The packet already requires `tree_fingerprint`, `time_budget` and `sweep_rule`; their rows are pinned. Add `changed_paths`, `commands_and_results`, `test_receipt` and `unresolved_findings` only to the optional table.

Seed 239's gate says **all five conditions**, and seed 209 expressly refers to QA condition 5 for known-bad detection. Add measurement comparison as a conditional sixth condition and adjust the introductory count without renumbering condition 5. Uncontrolled deltas remain unattributed; this does not turn an otherwise valid non-regression check into causal evidence.

The three mutation-table paragraphs have the same substantive clauses (capitalization differs). Seed 209 owns the landing-rule/projection definition and its packet owns sweep and budget detail. Pointer replacements must retain the report-at-budget clause.

Seed 214 still names Wavefoundry in its opening and `server.py` in its parser-coupling passage. Seed 221 still contains the `accel_embedder` recorded case. The project role-doc architecture passage likewise names the stale server owner, and the code-reviewer role doc lacks the recorded case. MCP exact-token retrieval confirms `_TS_SYMBOL_LANG_MAP` is declared in `server_impl.py` and read in `codenav_handlers.py`.

## Bounded implementation clarifications

1. **Preserve integration obligations when replacing evidence lists.** `POLICY_CLAUSES` in the host-neutral orchestration test class in `test_render_agent_surfaces.py` pins `Worker completion is not wave completion`, `reviewed revision or tree fingerprint`, and `a moved tree requires affected evidence to be refreshed` inside seed 180's Host-neutral orchestration section. Replace duplicate field enumeration with the seed-209 packet pointer, retaining those clauses, coordinator reread/integration, existing tracking, honest measurement limitations and no-extra-artifact policy. These are unique obligations, not duplicate packet fields.
2. **Make checkpoint permission consistent.** Seed 180's Action bullet currently permits a named reviewer checkpoint only at Level 2. Its planned ordinary per-change delivery checkpoint must also be permitted there. This is consistency with Requirement 4, not new scope. The new sentence must distinguish a fresh context started for delivery review at a per-change checkpoint from a retained inventory or non-delivery checkpoint context, which supplies findings/evidence but no approval.
3. **Preserve the template's one orchestration paragraph.** The lifecycle pointer assertion requires exactly one paragraph containing the seed-180 owner path. Retain the existing paragraph; use seed 209 for the new review-packet reference.
4. **Keep seed 180 task 6 unchanged.** The existing `agent-team-workflow.md` reference remains an expected carrier. The unrelated orchestration choices and independent-review clauses remain intact.

## Seed sentence disposition and propagation

Mechanisms: **direct** means participants read canonical seed 209/180; **upgrade** means seed 160's authored reconciliation, not renderer prose copying; **local** means the sibling `1ypxv` authored edit; **fresh** means the packaged missing-only review-wave template. Existing seed-160 anchors are **Changed project-owned review carriers**, **AC verification truth hierarchy**, and **Host-neutral orchestration reconciliation**; seed 100 uses repo-local prompt reconciliation. Renderer-owned evidence text is independently produced by `render_agent_surfaces.py` and owned by `1yoy2`.

| Seed anchor / changed sentence | Disposition | Destinations and mechanism |
| --- | --- | --- |
| 209 freshness: delivery approvals are finding-scoped, not receipt-scoped | Add | Direct canonical reading; upgrade review-wave/coordinator/review-and-evals and applicable reviewer carriers |
| 209 freshness: receipt publication lapses readiness only | Add | Same direct and upgrade carriers; tooling status projection is independently changed by `1yoy2` |
| 209 freshness: affected synthesis/blocking head determines delivery currency | Add | Same direct and upgrade carriers |
| 209 freshness: do not re-record solely for receipt movement | Add | Same direct and upgrade carriers |
| 209 claim-shape sentence | Reword to four shapes | Direct; upgrade review-wave/coordinator/review-and-evals and QA contracts; local generic template points to canonical evidence protocol |
| 209 ten judgment facts enumeration | Cut, tool schema owns enumeration | Direct canonical text; upgrade relevant authored review carriers; no renderer constant copies this sentence |
| 209 six repair/proportionality facts enumeration | Cut, tool schema owns enumeration | Same |
| 209 finding and approval evidence field-list sentence | Cut, tool schema owns enumeration | Same |
| 209 integrity-check object enumeration sentence | Cut, tool schema owns enumeration | Same; registry ownership, phase rule, tool-derived fields and writing-hand paragraph retained |
| 209 optional changed_paths row | Add | Direct packet consumers; upgrade relevant evidence/handoff carriers; seed 180 pointer |
| 209 optional commands_and_results row | Add | Same |
| 209 optional test_receipt row | Add | Same |
| 209 optional unresolved_findings row | Add | Same |
| 239 integrity-gate introductory count | Reword | QA seed direct; upgrade QA role and wrapper; no automatic whole-role replacement |
| 239 conditional comparison condition and substrate pointer | Add | Same QA carriers; canonical direct protocol supplies comparison details |
| 214 mutation paragraph | Pointer with budget clause | Direct seed; local architecture-reviewer role; upgrade architecture reviewer and enabled wrapper |
| 221 mutation paragraph | Pointer with budget clause | Direct seed; local code-reviewer role; upgrade code reviewer and enabled wrapper |
| 239 mutation paragraph | Pointer with budget clause | Direct seed; local QA role; upgrade QA reviewer and enabled wrapper |
| 180 readiness-agent bullet later-through-Review-wave clause | Cut duplicate | Direct orchestration; upgrade implement-feature/implement-wave/coordinator and relevant lifecycle carriers |
| 180 Review-wave shortcut/checkpoint statement | Reword to one owner statement | Same; all per-change approvals remain lane/wave scoped |
| 180 Action checkpoint limitation | Reword permission consistently | Same |
| 180 Level 2 exceptional sentence | Reword ordinary sequencing | Same |
| 180 finding-classification exceptional prefix | Reword | Same |
| 180 routine inferential fan-out sentence | Collapse into owner statement | Same |
| 180 task 10 routine review handoff sentence | Collapse into owner statement | Same; computational sensor obligations retained |
| 180 Accept integrated evidence field-list sentence | Packet pointer; preserve unique obligations | Direct seed-209 packet; upgrade orchestration carriers |
| 180 Hand off through existing artifacts field-list sentence | Packet pointer; preserve unique obligations | Same |
| 100 review-wave phase sentence | Add | Fresh template and local review prompt; upgrade existing review prompts via seed-100 reconciliation |
| 100 review-wave provenance sentence | Add | Fresh template; upgrade existing review prompts; direct seed contract. Local prompt consumption also reaches canonical protocol |
| 100 item 14 fresh-template carrier statement | Add | Install/bootstrap instructions read seed; fresh template implemented by sibling change |
| 214 project-named opening | Reword generically | Direct seed; upgrade role/wrapper preserves project extensions; local role keeps project identity |
| 214 coupling passage | Replace with stack-neutral dependency/grammar/lazy-load rule | Direct seed; upgrade reviewer role/wrapper; local architecture role retains corrected project detail |
| 221 recorded unreachability case | Remove module name, retain three causal facts | Direct seed; upgrade code reviewer role/wrapper; local code-reviewer receives original named case |

The changed-carrier upgrade clause explicitly lists plan-feature, implement-wave, review-wave, close-wave, architecture/code/QA reviewer roles, wave-coordinator, and review-and-evals. Seed-to-prompt mapping and orchestration reconciliation additionally cover implement-feature and lifecycle companions. This table identifies possible relevant destinations, not a claim that every sentence belongs in every carrier. Existing customized destinations require an authored merge at the unique relevant anchor.

**Not automatically propagated:** no changed authored seed sentence is copied into existing project prose by `wf render-surfaces`. Local carriers outside the sibling surface change's scope are not rewritten by that change; their direct canonical pointers remain effective, and their authored upgrade reconciliation remains the mechanism. Native wrappers and owned blocks are renderer-controlled; no prose replacement is claimed for them unless the renderer diff proves it. The four new optional packet fields are not a new renderer constant.

**Per-row local destination (this repository's own carriers, recorded after the independent objective evaluation):**

| Table rows | Local carrier that could hold the sentence | Local status |
| --- | --- | --- |
| 209 freshness (four rows) | `docs/agents/wave-coordinator.md`, `docs/contributing/review-and-evals.md`, `docs/prompts/review-wave.prompt.md` | Not propagated locally; participants read seed 209 directly. Upgrade reconciliation is the mechanism for target repositories. |
| 209 claim-shape sentence | `docs/agents/qa-reviewer.md`, `docs/contributing/review-and-evals.md` | Not propagated locally; direct canonical reading. |
| 209 four cut enumerations | none (no local copy existed) | Nothing to remove locally. |
| 209 four optional packet rows | `docs/agents/implementer.md`, `docs/prompts/implement-wave.prompt.md` | Not propagated locally; the packet is read from seed 209. |
| 239 count and comparison rows | `docs/agents/qa-reviewer.md` | Not propagated locally; the role doc carries the pointer sentence only. |
| 214/221/239 mutation pointer | `docs/agents/architecture-reviewer.md`, `code-reviewer.md`, `qa-reviewer.md` | Propagated by `1ypxv` (pointer plus budget clause). |
| 180 rows (nine) | `docs/prompts/implement-wave.prompt.md`, `docs/prompts/implement-feature.prompt.md`, `docs/agents/wave-coordinator.md` | Not propagated locally; those prompts carry no copy of the reworded sentences. |
| 100 phase sentence | `docs/prompts/review-wave.prompt.md` | Propagated by `1ypxv`. |
| 100 provenance sentence | `docs/prompts/review-wave.prompt.md`, `docs/prompts/agents/review-wave.prompt.md` | Not propagated locally; present in the fresh template only. |
| 100 item 14 | packaged template | Propagated by `1ypxv` (fresh template). |
| 214/221 de-localized passages | `docs/agents/architecture-reviewer.md`, `docs/agents/code-reviewer.md` | Propagated by `1ypxv`; the role docs keep the project detail the seeds lost. |

## Public surface sentence classification

This covers every existing sentence by contiguous paragraph or numbered-step boundary; where a paragraph contains multiple sentences, the stated disposition applies to all unless separately identified. Owners are seeds 100 (review workflow), 180 (orchestration), 209 (review evidence), 170 (checkbox state), and 210/memory-review (memory workflow). Project-specific facts remain locally owned.

| Existing public surface unit | Disposition | Owner / pin |
| --- | --- | --- |
| Metadata and shortcut | Keep; refresh verification date | Local metadata |
| Host-neutral orchestration paragraph, every sentence | Keep | Seed 180; lifecycle carrier pointer tests |
| Purpose's two sentences | Keep; add delivery/tool entry sentence | Seed 100; new phase pin |
| Retrieval Rules paragraph | Keep | Seed 020; existing carrier contract |
| Step 1 | Keep | Seed 100 |
| Step 2 lane list and other-lanes reference | Keep | Seed 100/project policy |
| Step 2 packet sentence | Keep verbatim | Seed 209; `ReviewCycleChurnControlPinTests` |
| Step 2 frozen-tree paragraph, all sentences | Keep verbatim | Seed 209; same test pins |
| Step 3 council trigger | Reword to current receipt required_council_signoffs | Seed 100; new receipt-trigger pin |
| Step 3 protocol, signoff/projection, roster/disagreement, readiness-authority and legacy sentences | Keep | Seeds 100/209/215 |
| Step 4 AC scope gap sentence | Reword as conditional on AC Priority table | Seed 100; admitted surface requirement |
| Step 5 reconciliation sentence | Reword as conditional on AC Priority table | Seed 100 |
| Step 5 marked-AC verification and silent-marker finding sentences | Keep obligations; combine canonical pointer into relevant sentence | Seed 170 |
| Step 5 standalone See seed-170 sentence | Move to pointer within verification sentence | Seed 170 |
| Step 6 findings recording sentence | Reword typed event authority; checkpoints narrative only | Seed 209 |
| Step 7 repair cycle, independent reverification, editorial repair, escalation, false-claim and owning-section sentences | Keep | Seeds 180/209 |
| Three Code Review Specifics bullets | Keep | Local project facts |
| Truth Hierarchy introduction and numbered sources | Keep | Seeds 100/170/180 |
| Truth Hierarchy checkbox, evidence, incomplete and rationale sentences | Keep | Same |
| Required Before Close lane-reconciliation sentence | Reword current typed authority plus narrative summary | Seed 209 |
| Required Before Close council sentence | Reword receipt-required trigger | Seed 100; new receipt pin |
| Required Before Close exit boundary | Add no close, completion or commit | Seed 100/local operator authority |
| Memory candidate-generation, focused validation, zero-memory, privacy and archival sentences | Move to two-line memory-review pointer | Memory workflow owner; preserve `memory_add(status='candidate'` pin |
| Executable review evidence owned block, every sentence | Preserve producer output; coordinator owns prior tooling change | `render_agent_surfaces.py`; carrier tests |
| Review-policy delivery owned block, every sentence | Keep | Review-policy renderer; carrier obligation anchors |

## Agent body sentence classification

| Existing unit | Disposition | Owner / pin |
| --- | --- | --- |
| Metadata/context | Keep; refresh date | Local |
| Seven lane descriptions and two external prompt references | Keep | Local project review policy |
| Five self-referential inline cells | Cut | No separate semantic content |
| Council paragraph, all sentences | Keep | Seed 100/215; already receipt-conditional |
| Guru introduction, five tool rows, fallback/citation sentences | Keep | Guru contract |
| AC Priority Reconciliation, both sentences | Keep | Seed 100 |
| Findings Level 1 | Keep | Seed 180 |
| Findings Level 2 before semicolon | Keep | Seed 209 |
| Findings Level 2 re-Prepare exception | Reword to `review_policy_reprepare_required` | Seed 180/current tool diagnostic; new pin |
| Findings Level 3 | Keep | Seed 180 |
| Both owned blocks, every sentence | Preserve producer output | Renderer and policy carriers |

## Packaged template and role-doc plan

Keep the template's orchestration paragraph, existing review-method investigation and repair obligations, approval independence and pending operator signoff. Add delivery phase/context provenance and no-tool judgment handoff after Purpose; point to seed 209's packet rather than copy its checklist. Add seed-100 council, required code review, stateful verification, AC scope/priority/QA attestation and marked-AC rules. Add truth hierarchy, current-approval exit criteria and empty Project review specifics heading outside markers. No Python/project artifact assumption belongs in the generic prose.

Replace local role mutation paragraphs with pointers preserving the exact budget tail ``; report at the budget and list what was not run (seed 209, wave `1wuju`)``. Keep the architecture coupling rule but name `server_impl.py` and `codenav_handlers.py`. Add the named `accel_embedder` case to code-reviewer; generic seed keeps only its causal facts. Preserve all other role prose and owned regions.

## Baseline sizes

Measured before edits from bytes on disk; estimates are UTF-8 bytes divided by four, not model-token measurements. Post-edit values include the tooling producer's shared-block change and the authored surface changes. No total-read-cost claim is made once readers follow pointers.

| Surface | Before bytes | Before estimated tokens | After bytes / estimated tokens |
| --- | ---: | ---: | --- |
| docs/prompts/review-wave.prompt.md | 10,221 | 2,555.25 | 9,127 / 2281.75 |
| docs/prompts/agents/review-wave.prompt.md | 5,968 | 1,492 | 4,780 / 1195 |
| .wavefoundry/framework/install/lifecycle-prompts/review-wave.prompt.md | 3,099 | 774.75 | 5,874 / 1468.5 |

## Targeted test stage

Inventory read assertions through MCP `code_keyword` and `code_read`; no test execution is claimed here. The docs/seed bulk reads were explicitly authorized for inventory. Execute after the corresponding edits:

- `test_docs_lint.py`: `ReviewCycleChurnControlPinTests`; update seed and role mutation literals only, retain public packet/frozen-tree, seed-209 rows/census and unrelated historical assertions. Add phase, receipt-trigger and agent diagnostic pins. Record the `1wuju` pointer amendment in the existing Decision Log.
- `test_render_agent_surfaces.py`: host-neutral orchestration contract/carrier class, fresh lifecycle render and repeat-render preservation; add generic review-template phase/provenance/exit content assertion and a deliberate template mutant that fails it. Keep original pointer mutant controls.
- `test_memory_records.py`: retain the public surface's literal `memory_add(status='candidate'` and run its existing prompt tests.
- `test_events_only_residue_census.py`: run residue controls; do not introduce retired lifecycle strings. Its exceptional-checkpoint fixture strings are in-test examples rather than direct seed pins.
- `test_setup_wavefoundry.py` and `test_upgrade_wavefoundry.py`: seed-byte/package propagation assertions use the shipped seed, so preserving source/carrier semantics should require no pin weakening.
- Coordinator owns `render_agent_surfaces.py` and the four owned-carrier pin updates for the tool change. Surface work must not race those files or claim the owned block is unchanged relative to the pre-tool baseline; it is unchanged relative to the completed tooling producer output.
- Run public `wf render-surfaces` at the coordinated boundaries; compare protected marker bytes, customized prose and manifest. Finish with full docs validation and framework suite through the canonical runner. Record tests as evidence of contract presence/propagation, not proof of reviewer adherence.

## Progress Log

| Date | Update | Evidence |
| --- | --- | --- |
| 2026-09-22 | Captured read-only seed anchors after `1ypy6`, sentence dispositions, baseline sizes and test obligations; preserved integration clauses and clarified checkpoint permission without expanding scope. Durable record written only after coordinator confirmed stable baseline completion. | This inventory; named seed headings and test assertions above. |

## Executed verification and census

The seed edit window ended with `wf_close_gate(gate='seed_edit_allowed')` before authored surface work began. The coordinator's final `wf_sync_surfaces(mode='run')` reported `written: []` and clean lint. The edit script asserted every existing owned marker-region byte was unchanged by the authored public/agent/role edits; final rendering made no further change. The prompt-surface manifest is byte-identical to the pre-wave snapshot, preserving the earlier wave's uncommitted manifest.

Executed focused command (repository root, bytecode disabled):

```sh
PYTHONPATH=.wavefoundry/framework/scripts:.wavefoundry/framework/scripts/tests python3 -B -m unittest test_docs_lint.ReviewCycleChurnControlPinTests test_render_agent_surfaces.HostNeutralOrchestrationCarrierTests test_render_agent_surfaces.MemoryReviewPromptTests test_events_only_residue_census test_memory_records
```

Result: 258 tests passed. After the coordinator clarified that `required_council_signoffs` is a receipt-derived response field rather than a raw receipt member, the public/template wording and pins were updated together; rerunning the first two classes passed all 11 tests. Earlier seed-only and orchestration selection passed 7 tests. The full suite and independent delivery review are coordinator-owned and not claimed by this implementation record.

The new `HostNeutralOrchestrationCarrierTests.test_fresh_review_template_contract_and_known_bad_controls` invokes the real renderer against copied package templates, then checks the generated public review prompt against AC-derived obligations. It detects all three shipped known-bad controls: delivery phase changed to readiness, retained contexts allowed to approve, and review allowed to close. The conforming render also survives a second render byte-identically. These prove content propagation and assertion sensitivity, not that a reviewer obeys prose.

Pre-wave versus final seed verification confirmed seed 180 task 6 is byte-identical and seed 209's landed controlled-substrate sentence remains verbatim. The four removed Typed authoring sentences form exactly the span between the ten-judgment-facts enumeration and registry-ownership sentence; the writing-hand paragraph and every other sentence remain.

Exact naming census predicate: `Wavefoundry|_TS_SYMBOL_LANG_MAP|accel_embedder|server\.py`, across the six edited seeds plus the explicitly retained seed 212. Every remaining matching line is classified here (line numbers from the completed edits):

| Seed | Remaining lines | Classification and reason |
| --- | --- | --- |
| 100 | 51, 83, 96, 106, 107, 108, 125 | Intentionally retained, pre-existing public product commands, MCP/tool routing or install/upgrade guidance; none is a newly added project-specific example. |
| 209 | 239 | Intentionally retained, pre-existing framework ledger/rollback policy that names the product, outside the edited prose. |
| 212 | 3 | Intentionally retained, pre-existing generic MCP tool-posture line. |
| 212 | 20, 34 | Intentionally retained, pre-existing project opening and `server.py` query-path heading; seed 212 is outside this wave's admitted scope. |
| 214, 221, 239 | 3 each | Intentionally retained, pre-existing “Wavefoundry MCP” tool-posture line. |
| 180 | None | No match. |

There are zero remaining `_TS_SYMBOL_LANG_MAP`, `accel_embedder` or `server.py` hits in edited seeds 214 and 221. The generic coupling and three-fact unreachability rules remain; their named local examples live in the role docs. Existing `(wave 1wuju)` parentheticals remain intentional provenance, including the retained budget clause. New sentences contain no wave IDs or project artifact names beyond the generic framework entry points/pointers they describe.

Mutation-paragraph census over reviewer seeds 214/221/239 and local architecture/code/QA role docs: six pointers to seed 209's Landing rule, zero copies of the original per-mechanism paragraph. Seed 209 remains the owner of the mutation-table definition; its packet retains sweep and budget detail. Other canonical protocol mentions of mutation evidence are intentionally retained, not counted as duplicate lane paragraphs.

The public and agent entry surfaces shrink by 1,094 and 1,188 bytes respectively; the package template grows by 2,775 bytes to carry missing delivery obligations. UTF-8 bytes divided by four are estimates only. No total-read-cost or reviewer-performance claim follows from those sizes.
