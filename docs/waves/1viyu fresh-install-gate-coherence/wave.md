# Wave Record

Owner: Engineering
Status: closed
Last verified: 2026-08-17
review-evidence-source: events.jsonl

review-policy-reprepare-required: false
wave-id: `1viyu fresh-install-gate-coherence`
Title: Fresh Install Gate Coherence

## Objective

A fresh target-repo install passes its own docs gate without hand-authoring policy or papering over dangling references: the seven lint-required `workflow-config.json` sections are provisioned by setup, the change-doc scaffold is a shipped file rather than agent prose, `wf_audit_install` can return `next_step` on a fresh Phase 2 entry while still blocking on real defects, and the install-log template, seed-010/012/040, and README agree with the pack. Prompted by the 2026-08-17 fresh-install field report (144 lint errors, two cleanup passes).

## Changes

Change ID: `1vim5-bug workflow-config-required-sections-have-no-install-owner`
Change Status: `implemented`

Change ID: `1vitq-bug plan-template-authored-from-prose-declares-targets`
Change Status: `implemented`

Change ID: `1vitr-bug audit-install-lint-gate-blocks-fresh-phase-2-entry`
Change Status: `implemented`

Change ID: `1viyt-bug install-log-and-install-seed-reference-drift`
Change Status: `implemented`

## Participants

- Coordinator: wave-coordinator
- Write-owning roles: implementer (selected during Prepare wave)
- Requested review lanes: qa-reviewer, code-reviewer
- Required review lanes: code-reviewer, qa-reviewer, docs-contract-reviewer, release-reviewer

Completed At: 2026-08-18

## Wave Summary

Wave `1viyu` (Fresh Install Gate Coherence) delivered 4 changes: The seven lint-required `workflow-config.json` sections have no install owner since the 1p35d install split, The install authors `docs/plans/plan-template.md` from seed-040 prose, so every fresh install ships a scaffold that declares review targets, `wf_audit_install` runs the whole-repo docs gate before the next-row check, so a fresh Phase 2 entry can never receive `next_step`, and The install-log template, seed-012, seed-010, and seed-040 disagree with each other and with the shipped pack. Notable adjustments during implementation: The seven lint-required `workflow-config.json` sections have no install owner since the 1p35d install split: Thought: every required public carrier needs an executable owner. Observe: `RT-FINAL-006` found the framework README rewrite was scope-owned but not test-owned; Requirement 5/AC-5 now pin its required anchors with focused mutations.; The seven lint-required `workflow-config.json` sections have no install owner since the 1p35d install split: Readiness amendment from the docs-contract seat (DC-6, DC-7, DC-8, DC-9): seed-040 line 64 rewrite stated precisely (design keys are added directly, not by Step 0); seed-160 line 516 also gains `wave_review`; the self-hosted values that must NOT be copied into the defaults are named; per-file (not directory) root-then-module resolution.; The install authors `docs/plans/plan-template.md` from seed-040 prose, so every fresh install ships a scaffold that declares review targets: Readiness amendment from the red-team primer (RT-3, RT-4, RT-5, RT-6): adopted code materialization via the lifecycle-baseline pattern, root-then-module resolution for the shipped file, date stamping for `check_metadata`, seed-160 line 198 added to scope, test (e) added.

**Changes delivered:**

- **The seven lint-required `workflow-config.json` sections have no install owner since the 1p35d install split** (`1vim5-bug workflow-config-required-sections-have-no-install-owner`) — 5 ACs completed. Key decisions: Ship the seven sections as a data file and apply them from setup Step 0, key-wise absent-only; `wave_review` pinned by test to `migrate_wave_review_policy(None)`.
- **The install authors `docs/plans/plan-template.md` from seed-040 prose, so every fresh install ships a scaffold that declares review targets** (`1vitq-bug plan-template-authored-from-prose-declares-targets`) — 4 ACs completed. Key decisions: Ship the template as a file, make `_default_template()` read it (root-then-module), and have `render_agent_surfaces` materialize `docs/plans/plan-template.md` missing-only at setup and upgrade, with seed-040 reduced to verify/copy-when-absent plus the fence rule as backstop.
- **`wf_audit_install` runs the whole-repo docs gate before the next-row check, so a fresh Phase 2 entry can never receive `next_step`** (`1vitr-bug audit-install-lint-gate-blocks-fresh-phase-2-entry`) — 4 ACs completed. Key decisions: Classify lint errors against the log's pending state (marker tuple + path-absent + any seed row pending) and block only on the rest, carrying absences as `pending_lint`; final gate unchanged.
- **The install-log template, seed-012, seed-010, and seed-040 disagree with each other and with the shipped pack** (`1viyt-bug install-log-and-install-seed-reference-drift`) — 8 ACs completed. Key decisions: Row 2.15 prepares the summary; delivery occurs only after the terminal audit returns `complete`.; Promote bootstrap-file removal to instruction row 2.14 and observe `complete` only after instruction row 2.15 is terminal; this supersedes the earlier decision to keep bootstrap removal rowless.
## Watchpoints

- Shared files and line-level ownership: `install/install-log.template.md` (1vim5 row 1.2; 1vitr row 2.1 only; 1viyt rows 2.10/2.12/2.14/2.15 and all row-kind/action parity, with no renumber); `install/install-log-format.md` and its byte-identical `docs/references/` twin (1vitr audit status/transition semantics, 1viyt row-kind and citation semantics, edited together); `seeds/012` (1vitr step 2.1 audit wording and runtime semantics only; 1vim5 seed-100 render line; 1viyt all heading numbers plus 2.14/2.15 instruction actions and terminal postcondition); `seeds/040` (1vim5 line 64, 1vitq task 11, 1viyt token grammar); `seeds/160` (1vitq plan-template rules, 1vim5 workflow keys, 1viyt step citations); `seeds/011` (1vim5 Step 0, 1viyt log references); `setup_wavefoundry.py` (1vim5 Step 0 code, 1viyt module docstring); `server_impl.py` (1vitq `_default_template`/`new_change`, 1vitr audit response/registered description); `docs/references/install-assets.md` (both new asset rows); root `README.md` (1vitr Phase 2 audit only); `.wavefoundry/framework/README.md` (1vim5 workflow anchors, 1viyt dead seed/journal references). Sequence 1viyt row/heading reconciliation first, then consumers; docs-lint after each; no existing row number changes.
- Gates: seed edits under `seed_edit_allowed` (010, 011, 012, 040, 160); script edits under `framework_edit_allowed`; close both immediately after each editing pass.
- 1vitr test (a) must run the REAL `run_validate` on a Phase-1-complete scratch tree (the existing `wf_audit_install` tests mock it); after 1vim5 and 1vitq land, the fresh-repo error set shrinks, and that test re-derives it.
- 1viyt keeps `_DOT_PATH_RE` unchanged; widening the token grammar is an ADR-shaped decision, not this wave.
- Docs-contract seat amendments 2026-08-17 (DC-1 to DC-13): seed-012 step citations in seeds 011/160/012 move with the renumber (mention-resolution test); phantom zip-root `wavefoundry-install-log.md` name folded into 1viyt; `install-log-format.md` has a byte-identical twin at `docs/references/` (suite invariant), edited in lockstep by 1vitr and 1viyt; `install-log-format.md` lines 26/61 and the final-gate `complete` expectation corrected by 1vitr; a real `wf_audit_install` Tool Detail entry is created (none exists); seed-040 line 64 rewrite stated precisely; excluded self-hosted values named for the defaults; per-file root-then-module resolution and `_default_template(root=None)`; `install-assets.md` rows for the two new assets.
- Readiness amendments 2026-08-17 (red-team primer RT-1 to RT-15): no template renumber; plan-template materialized by code (lifecycle-baseline pattern) with root-then-module resolution and date stamping; `wave_review` default pinned to `migrate_wave_review_policy(None)`; `wf_audit_install` expected returns corrected (`phase_complete` with `phase=1`), `ERROR:` prefix stripping, accepted global-pending limitation with test (f); seed-160 lines 198/199/516 in scope. All recorded in the change docs before the receipt minted.
- Follow-up candidate (not admitted): README journal-philosophy prose (lines 53, 65, 312, 448, 450) still describes the retired journal system; `docs/architecture/threat-model.md` line 60 still says `seed-130` (self-hosted doc); `install/lifecycle-prompts/memory-review.prompt.md` keeps a static `Last verified: 2026-08-02` while its five siblings use `{{generated_at}}` (lint-valid; one-line alignment).
- Follow-up candidate from the delivery council (RTD-2, not admitted, not bounded for this wave): three copies of root-then-module install-asset resolution (`_resolve_install_asset`, `setup_wavefoundry._workflow_defaults_path`, `server_impl._default_template`) and two read/stamp/write baseline loops (`reconcile_lifecycle_prompt_baselines`, `reconcile_scaffold_baselines`) are the CODE-DEL-1 defect class (one family stamped, one not); a shared `resolve_install_asset` + `materialize_baseline` helper would remove it. Recorded as debt, not a typed finding.
- Delivery-review ownership additions (2026-08-17): `install/lifecycle-prompts/{close,create,implement,prepare,review}-wave.prompt.md` and `render_agent_surfaces.reconcile_lifecycle_prompt_baselines` / `_initial_review_carrier_text` (1vitr, CODE-DEL-1 repair; the scaffold-baseline pattern itself stays 1vitq's); `setup_wavefoundry._provision_workflow_defaults_if_absent` writer (1vim5, via `upgrade_wavefoundry._atomic_write_json`); `CHANGELOG.md` `[Unreleased]` (wave-level).
- Implementation template diff (2026-08-17): the shipped `install/plan-template.md` is intentionally a compact, framework-generic baseline with a stamped `Last verified` placeholder, while this repository's `docs/plans/plan-template.md` retains richer self-hosted lifecycle guidance. Both keep the required heading contract, fence examples, and declare no targets; `FreshPlanTemplateTests` and the explicit file diff record the shared invariants and intentional local refinement.

## Review Checkpoints

- **Delivery-phase Wave Council [delivery-council] — 2026-08-17: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, docs-contract-reviewer; rotating-seat: docs-contract-reviewer; strongest-challenge: the delivered proof stops at Phase 1, and because the classifier's pending rule is global nothing binds a deferred absence to the row that must create it, so a milder wall could reappear at the final rows if a Phase 2 seed row is marked without producing its non-row artifacts; accepted and disclosed (CHECK 2 still catches a marked row's own artifact, the final gate blocks everything once no seed row pends, and every one of the 15 deferred absences on a faithful tree has a Phase 2 seed owner, seeds 040/100/110/120); strongest-alternative: one shared install-asset helper (resolve + stamp + write) for both baseline families, the workflow-defaults path, and `_default_template`, since three resolver copies and two stamp loops are the CODE-DEL-1 defect class; recorded as follow-up debt RTD-2 in Watchpoints, not taken in this delivery)
- **Delivery review, cycle 0 (2026-08-17, four required lanes, fresh isolated contexts, executed evidence)**: release-reviewer APPROVE-with-editorial (scratch pack via `build_zip`: both new assets present and byte-identical, phantom root member absent; absent-only merge and missing-only materialization proven; `test_upgrade_wavefoundry` 501 OK; REL-DEL-1 CHANGELOG missing, REL-DEL-2 defaults writer `ensure_ascii`/atomicity); docs-contract-reviewer APPROVE-with-editorial (every delivered seed/template/spec/README line verified against the code it describes; four scratch mutations killed the parity, seed-ref, step-mention, README tests; DC-DEL-1 two more dead seed filenames in the framework README, DC-DEL-2 stale cross-references, DC-DEL-3 rendered install prompt ordering, DC-DEL-4/5 note-only); code-reviewer WITHHOLD (CODE-DEL-1 broad: on a faithful Phase-1-complete tree the real validator emitted 21 blocking metadata errors from renderer-materialized carriers so `next_step`/`phase_complete` were unreachable and the shipped AC-1 fixture could not see it; CODE-DEL-2 medium: fail-open when lint fails without `ERROR:` lines; CODE-DEL-3 medium: prefix-strip mutant survives; CODE-DEL-4/5/6 low); qa-reviewer WITHHOLD (QA-DEL-1 = CODE-DEL-1 found independently on its own faithful tree; QA-DEL-2/3 test-fidelity siblings of CODE-DEL-3; QA-DEL-4 stale AC-priority summary; 27 scratch mutants, 23 killed, 4 survived and became findings). Full suite before repair 7302 OK.
- **Repair cycle 3 (2026-08-17)**: typed findings CODE-DEL-1/2/3 recorded by code-reviewer (source lanes code + qa), `repair_start` by implementer, repairs applied under the gates: metadata blocks with `{{generated_at}}` in the five shipped lifecycle prompt baselines and stamping in `reconcile_lifecycle_prompt_baselines`; metadata in the pointer-form `_initial_review_carrier_text`; AC-1 fixture rebuilt as `_build_phase_one_complete_tree` (shipped seeds + install, both Step 0 provisioners, full render, real `run_validate`); fail-closed synthesized entry (later hardened per RTD-1 to bypass the classifier); prefix-polarity, seed-row-only, and `pending_lint` matrix tests; test (f). Editorial repairs in the same cycle: CHANGELOG `[Unreleased]` with the four fixes and the upgrade-impact disclosure; defaults writer via `_atomic_write_json`; README seeds 130/210 and `memory root` anchor with the generalized seed-filename test; template line 54 and seed-012 closing sentence; rendered install prompt items 6/7; AC-priority summary. Reverification by fresh independent actors: code-reviewer (context b) all three REPAIRED with three CODE-DEL-1 mutants, the guard-removal mutant, and five CODE-DEL-3 mutants killed; qa-reviewer (context b) CODE-DEL-1 REPAIRED on its own faithful tree and through the production `render_platform_surfaces.py` entry, nine mutants killed, 21/21 ACs and 23/23 tasks attested; docs-contract and release rechecks (context b) APPROVE on the repaired tree (twins identical, pack rebuilt with metadata-bearing templates, defaults writer atomic and non-ASCII-safe, upgrade impact disclosed: existing repos with older metadata-less lifecycle prompt copies keep failing `check_metadata` after upgrade; hand-add the block or delete-and-re-render). Full suite after repair 7302 OK; docs-lint ok. The 1vitr Scope addition re-digested the receipt to `review-policy-3cf44464c4fe57d3ad18` (delivery council now required); readiness re-council on the amendment (red-team lightweight primer PASS, docs-contract seat APPROVE) recorded a fresh `wave-council-readiness` bound to that receipt.
- **Delivery council seats**: red-team (context c, standard depth, adversarial probe of the fresh-install path A0..A9 on a full framework copy through the production render subprocess: `phase_complete`, `next_step 2.1`, mark-and-advance to 2.2, `[~]` rows skip without bypassing the final gate, CRLF log parses, CHECK 2 unaffected, a metadata defect on a materialized carrier blocks, no `{{generated_at}}` leak, docs-less root fails closed; findings RTD-1 low fixed in-cycle, RTD-2 low follow-up debt, RTD-3/4 note-only; verdict APPROVE); docs-contract-reviewer (rotating seat, context b, verdict APPROVE, see above). wave-council synthesis: no material disagreement between seats or lanes; the two primer questions are answered by (1) the accepted-limitation note plus seed-012's final-gate wording, and (2) RTD-2 recorded as debt rather than opened as an in-wave repair. Editorial-only residue after cycle 3: DC-RV-1 wording nit in the rendered install prompt, DC-RV-2 pre-existing README journal prose, RTD-3 note wording (all `maybe_later`/note-only, none blocking).
- **AC scope gap check (2026-08-17)**: every required and important AC across the four change docs is met and attested (qa-reviewer census 21/21 ACs, 23/23 tasks, no `[~]`); no important/nice-to-have item sits outside admitted scope; not-this-scope deferrals are the change docs' Out of scope lists plus the Watchpoints follow-up candidates.

- **Prepare-phase Wave Council [prepare-council] — 2026-08-17: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, docs-contract-reviewer; rotating-seat: docs-contract-reviewer; strongest-challenge: parser invisibility, a circular final verify expectation, premature summary delivery, and a phantom package allowlist member could each make a fresh install appear complete without truthful executable evidence; resolved with parser-visible instruction rows 2.14/2.15, an exact three-state audit progression, final audit before summary delivery, and an exact package negative; strongest-alternative: add an atomic finalize-install operation instead of preserving instruction rows; rejected as disproportionate to the narrow coherence repair)
- **Final Re-Prepare evidence (fresh, isolated seats)**: code-reviewer proved the exact runtime tail (`[ ][ ]` → `next_step` 2.14, `[x][ ]` → `next_step` 2.15, `[x][x]` → `complete`) and approved the single three-argument classifier contract; docs-contract-reviewer and red-team independently confirmed the current plans keep both instruction rows parser-visible, assign row definitions solely to 1viyt and runtime consumption solely to 1vitr, require prepare → mark → final audit → deliver ordering, and pin the framework README, setup docstring, and build-pack phantom-member negative. Known-bad probes reproduced the current failures before implementation; all eight readiness findings are terminal in the typed ledger.
- **Prepare-phase Wave Council [prepare-council] — 2026-08-17: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, docs-contract-reviewer; rotating-seat: docs-contract-reviewer; strongest-challenge: the newly discovered root README and live MCP-description carriers initially lacked mutation-sensitive tests, and the audit task contradicted its requirement by allowing `pending_lint` on `unparseable_log`; both were repaired and independently reverified with in-memory carrier mutations plus a UTF-16/failing-lint known-bad probe; strongest-alternative: define one explicit seven-status field matrix and hold each public carrier to semantic anchors; adopted in 1vitr Requirement 2/4 and Tasks)
- **Re-Prepare council evidence (fresh, isolated seats)**: red-team `RT-READY-003` APPROVE after current-plan/live-registration probes rejected the pre-implementation carriers and six focused in-memory carrier/ownership mutations; docs-contract-reviewer `DOC-READY-004` APPROVE after a temporary UTF-16-BOM log on current code produced the known-bad `lint_errors` result and one `run_validate` call, proving the amended parse-before-lint/zero-call test is non-vacuous. The original omissions `DOC-READY-001` and `DOC-READY-002` were separately reverified through a five-section carrier matrix before this final amendment; their ownership remains present.
- **Prepare-phase Wave Council [prepare-council] — 2026-08-17: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, docs-contract-reviewer; rotating-seat: docs-contract-reviewer; strongest-challenge: 1viyt planned a contiguous install-log row renumber that the shipped row-format contract forbids and its seed-012 parity target ignored row-less sub-steps; resolved in-plan before the receipt (no renumber, gap at 2.12, lettered sub-steps); strongest-alternative: materialize shipped scaffolds by code through the lifecycle-baseline mechanism instead of seed prose; adopted for the plan template in 1vitq with per-file root-then-module resolution and date stamping)
- **red-team (adversarial primer, fresh context, read-only, code-grounded)**: verified 27 plan claims against the tree (all HOLD except two PARTIAL: seed-160 line 482 vs 199 as the fence-verify carrier; renumber was a contract violation, not merely a risk); 15 findings RT-1 to RT-15, none blocking after amendment: RT-1/RT-2 (1viyt renumber and parity scope), RT-3 (root-vs-module resolution of both new shipped assets), RT-4 (`Last verified` placeholder would fail `check_metadata`), RT-5/RT-6 (materializer alternative; seed-160 line 198), RT-7 (`ERROR:` prefix in the classifier path rule), RT-8 (seed-012 line 17 expects an unreachable `next_step row 2.2` for `phase=1`; reachable return is `phase_complete`), RT-9 (global pending rule defers done-row non-row absences; accepted, test f added), RT-10/RT-11 (explicit `delivery_mode` and the upgrade-prompt carrier region; `wave_review` value must equal `migrate_wave_review_policy(None)`), RT-12 (seed-160 line 516 key spelling), RT-13 (shared-file sequencing), RT-14 (README line 192), RT-15 (which statuses carry `pending_lint`). Every fix-before-ready item was written into the change docs 2026-08-17 before readiness was recorded; the fix-during-implement items are named in the affected change's Requirements or Tasks. Verdict PASS-with-conditions, conditions met.
- **docs-contract-reviewer (rotating seat, fresh context, read-only)**: engaged both primer questions (row-numbering authority honored, not amended; assets per-file root-then-module and code-materialized) and rated RT-1 to RT-15 resolved-in-plan except RT-8's sibling at the final verify row (DC-5, now in 1vitr). 13 findings DC-1 to DC-13, none blocking after amendment: DC-1 (seed-012 step citations in seeds 011/160/012 must move with the renumber; mention-resolution test added), DC-2 (phantom zip-root `wavefoundry-install-log.md` in seeds 010/011 and the setup docstring; folded into 1viyt), DC-3 (`install-log-format.md` has a byte-identical canonical twin under `docs/references/`, suite invariant; both plans now name it), DC-4 (lines 26/61 of that file carry the RT-8 expectation; 1vitr scope), DC-5 (final-gate `complete` expectation; verify-row convention stated), DC-6 (seed-040 line 64 rewrite made precise), DC-7 (seed-160 line 516 also omits `wave_review`), DC-8 (self-hosted values that must not enter the defaults, named), DC-9 (per-file fallback and `preflight_agent_surface_paths`), DC-10 (`_default_template(root=None)`), DC-11 (`mcp-tool-surface.md` has no `wf_audit_install` entry today; one is added), DC-12 (token grammar digit-leading rule; `2xl` negative), DC-13 (seed-160 371-374/479-482, seed-100 step 14, `install-assets.md` rows). Confirmed aligned: no project-specific policy imported into seeds; rendered install/upgrade prompts carry none of the affected rows, statuses, or keys. Verdict PASS-with-conditions, conditions met.
- **wave-council synthesis**: no material disagreement between seats; the docs-contract seat extended the primer's RT-8 and RT-13 classes to sibling carriers rather than contradicting them. Council review found no defect in the diagnoses (every reported failure reproduces from the cited code) and no reason to split or drop a change; it moved 1vitq from prose-copy to code-materialization, removed a contract-violating renumber, and widened three scope lists to carriers the plans had missed. Readiness is recorded on the amended plans; the review-policy receipt minted after the amendments, so no re-Prepare is owed for them.
- **AC priority check** (2026-08-17, wave-coordinator; reconciled at delivery review, QA-DEL-4): every AC on all four change docs is classified with a rationale (1vim5 AC-1..3 required, AC-4..5 important; 1vitq AC-1..3 required, AC-4 important; 1vitr AC-1..3 required, AC-4 important; 1viyt AC-1..4 and AC-6 required, AC-5, AC-7, AC-8 important; the extra rows were added during the implementing session's re-prepare cycles). Interrogation of the required rows: each names the mechanism it protects (shipped data, single template source, reachable `next_step`, dangling references) rather than an outcome; the important rows are surface coherence and end-to-end proofs whose mechanism is already covered by a required row. No `[~]` ACs at admission. Not-this-scope items are recorded as Out of scope lists, not AC rows.
- **product-owner**: N/A with rationale. Framework install tooling; no product behavior, UX, or acceptance change in a target product; operator-facing acceptance is captured by the field report the wave answers and by the required ACs. Single-operator project; recorded under operator hat.
- **Prepare wave — readiness verdict (2026-08-17)**: READY. All four admitted change docs are wave-owned (`docs/waves/1viyu .../`), complete (Rationale, Requirements, Scope, ACs, Tasks, AEG, Serialization Points, Affected architecture docs, AC Priority, Decision Log, Risks), placement drift none. Required review lanes from the receipt: code-reviewer (scripts), qa-reviewer (bug kind + tests), docs-contract-reviewer (seeds, specs), release-reviewer (`build_pack.py`); recorded in Participants. `wave-council-readiness` recorded as a typed approval event. Wave stays `planned` (readied); opening is a separate step.

## Finding Synthesis

<!-- wave:finding-synthesis begin -->
| Current finding | Disposition | Open block | Repair | Approval recheck |
| --- | --- | --- | --- | --- |
| CODE-DEL-1 | do_now | no | completed | code-reviewer, qa-reviewer |
| CODE-DEL-2 | do_now | no | completed | code-reviewer, qa-reviewer |
| CODE-DEL-3 | do_now | no | completed | code-reviewer, qa-reviewer |
| CODE-READY-001 | do_now | no | completed | code-reviewer, docs-contract-reviewer, wave-council-readiness |
| DOC-FINAL-005 | do_now | no | completed | code-reviewer, docs-contract-reviewer, wave-council-readiness |
| DOC-READY-001 | do_now | no | completed | docs-contract-reviewer |
| DOC-READY-002 | do_now | no | completed | docs-contract-reviewer |
| DOC-READY-004 | do_now | no | completed | docs-contract-reviewer, wave-council-readiness |
| RT-FINAL-006 | do_now | no | completed | docs-contract-reviewer, wave-council-readiness |
| RT-FINAL-007 | do_now | no | completed | release-reviewer, wave-council-readiness |
| RT-READY-003 | do_now | no | completed | wave-council-readiness |

*Machine review state — 11 findings; current: do_now 11, maybe_later 0, dont_do_later 0, not_issue 0*
<!-- wave:finding-synthesis end -->

## Review Evidence

<!-- wave:review-status begin -->
| Signoff | State | Why | Next action |
| --- | --- | --- | --- |
| wave-council-readiness | approved | current executed approval follows every affected repair | none |
| wave-council-delivery | approved | current executed approval follows every affected repair | none |
| code-reviewer | approved | current executed approval follows every affected repair | none |
| qa-reviewer | approved | current executed approval follows every affected repair | none |
| docs-contract-reviewer | approved | current executed approval follows every affected repair | none |
| release-reviewer | approved | current executed approval follows every affected repair | none |
| operator-signoff | approved | current executed approval follows every affected repair | none |
<!-- wave:review-status end -->

- operator-signoff: <approved when operator confirms closure>

## Dependencies

- No external wave dependencies.

<!-- wave:context-efficiency begin -->
## Context Efficiency

Estimated token savings use phase-unique returned source versions and mapped workflow prompts, minus recorded request and response tokens. Saved model output or avoided tool loops count only through quality-equivalent paired evidence.

| Stage | Tool calls | Estimated token savings |
| --- | ---: | ---: |
| plan | 453 | 3,976,739 |
| implement | 206 | 404,131 |
| review | 769 | 11,406,995 |
| **Total** | **1,428** | **15,787,865** |

<!-- wave:context-efficiency-state {"generation":1271,"measurement_status":"healthy","pending":false,"schema_version":1,"stages":{"implement":{"calls":206,"content_source_credit":520882,"derived_artifact_credit":0,"direct_net":404131,"estimated_tokens_saved":404131,"matched_pair_residual":0,"paired_evaluation_count":0,"request_debit":11535,"response_debit":112538,"source_credit_count":23,"source_credit_drop_count":0,"structural_source_credit":0,"workflow_prompt_credit":7322},"plan":{"calls":453,"content_source_credit":5362185,"derived_artifact_credit":6706,"direct_net":3976739,"estimated_tokens_saved":3976739,"matched_pair_residual":0,"paired_evaluation_count":0,"request_debit":42539,"response_debit":1357499,"source_credit_count":377,"source_credit_drop_count":0,"structural_source_credit":0,"workflow_prompt_credit":7886},"review":{"calls":769,"content_source_credit":14014244,"derived_artifact_credit":3620,"direct_net":11406995,"estimated_tokens_saved":11406995,"matched_pair_residual":0,"paired_evaluation_count":0,"request_debit":41699,"response_debit":2570516,"source_credit_count":588,"source_credit_drop_count":0,"structural_source_credit":0,"workflow_prompt_credit":1346}},"store_instance_id":"f294635fbf24489a9a50af63451b2532","totals":{"calls":1428,"content_source_credit":19897311,"derived_artifact_credit":10326,"direct_net":15787865,"estimated_tokens_saved":15787865,"matched_pair_residual":0,"paired_evaluation_count":0,"request_debit":95773,"response_debit":4040553,"source_credit_count":988,"source_credit_drop_count":0,"structural_source_credit":0,"workflow_prompt_credit":16554},"wave_id":"1viyu fresh-install-gate-coherence"} -->
<!-- wave:context-efficiency end -->

## Estimated Exploration Avoided

<!-- wave:exploration-avoided begin -->

This is a bounded estimate from exact-match memory advisories. It is not added to measured Context Efficiency.

| Advisory surfaces | Citations | Records credited | Estimated tokens avoided |
| ---: | ---: | ---: | ---: |
| 54 | 0 | 15 | 26,517,134 |

estimated: a surfaced (or cited) advisory does not prove a re-exploration was avoided; this is grounded in the measured cost of the original exploration, scaled by a bounded exact-match attribution, and is NEVER summed into the measured Context Efficiency token total.

<!-- wave:exploration-avoided-state {"cited_events":0,"credited_records":15,"estimated_exploration_avoided":26517134,"surfaced_events":54} -->
<!-- wave:exploration-avoided end -->
