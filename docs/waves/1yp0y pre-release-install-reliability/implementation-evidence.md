# Install Reliability Implementation Evidence

Owner: Engineering
Status: draft
Last verified: 2026-09-22

## Implemented boundaries

The required MCP command remains `python3`. Strict setup now probes it before shared-venv activation or setup reconciliation. Strict checks ignore the legacy non-strict skip flag. Interpreter evidence distinguishes resolution failure, old version, failed exit, invalid output, launch error and timeout; a discovered `python` is evidence only. Timeout output is preserved and bounded, including byte decoding. The read-only PowerShell diagnostic is reachable before Python/MCP and the Windows launcher points to it without changing the launch command or successful exit behavior.

The framework source opts into nine mandatory internal documentation facts via `docs_lint.framework_internal_constants: true`. The census is four performance claims, four reliability claims and one MCP-contract claim. Absent/false configurations exempt consuming projects; malformed or non-boolean settings fail. Optional CHANGELOG verification and generic target validation remain active. Packaged framework scripts do not establish source identity.

Canonical guidance and authored local carriers are mapped in guidance-propagation.md. Seed-100 uses the existing public `wf_sync_surfaces(mode='run')` path; no new tool or permission authority was added. Resume guidance requires the current conditional seed catalog and actual manifest/files, not a fixed count.

## Executed checks

- Canonical focused runner: 189 tests across test_venv_bootstrap, test_setup_wavefoundry, test_render_platform_surfaces and test_python_diagnostic_windows; one native Windows test intentionally skipped on Darwin. This preceded the timeout-output regression.
- Repaired bootstrap file: 38 tests pass, including retained byte output and its 2000-character bound.
- Validator implementer: 32 tests pass, including real docs-lint CLI, all nine source missing/mismatch cases, consuming projects with packaged scripts, invalid config, and retained CHANGELOG checks.
- Public install integration: two tests pass. Real sync fills the empty upgrade-policy block, preserves authored prefix/suffix and permission rules, and writes nothing on a second run. Real audit rejects missing registered prompt and malformed manifest, returns unchecked row 2.9 after repair, then completes after the row is checked.
- Actual build-pack collector includes `scripts/diagnose_python.ps1`; local `wf.cmd` regenerated by its producer.
- Full docs validation passed before final evidence/tracking updates. First full framework run: 9,550 tests across 123 files, 13 skips; failures in dashboard process-identity tests under the sandbox and the intentional wf_sync_surfaces docstring digest change. Local process inspection (`ps`) was denied by the sandbox. Final canonical run with approved local process inspection passed: 9,550 tests across 123 files in 300.985 seconds, 13 intentional skips. The stored green receipt inputs_hash was independently recomputed and matches the current framework tree. The native Windows test is one of those skips, not qualification evidence.

## Known-bad controls

- Restoring strict acceptance of `WAVEFOUNDRY_SKIP_PYTHON_HEAL` fails `EnsurePythonResolvesTests.test_strict_check_cannot_be_skipped` (SystemExit not raised). Coordinator executed against an in-memory mutant without editing production source.
- Independent security reviewer restored the pre-repair timeout handler in a temporary source copy. `test_timeout_retains_bounded_output_even_when_subprocess_returns_bytes` fails with missing stdout; the repaired producer passes. PowerShell sibling timeout paths were source-reviewed, not executed here.
- Validator implementer bypassed the opt-in in memory: generic consumer errors rose from zero to four. Independent code/QA also substituted HEAD’s full old check: the consumer exemption test failed with nine errors.

## Limits and remaining qualification

No native Windows execution, standard-user repair, policy-blocked host repair, fresh-host MCP startup or representative generated hook is proven on this Darwin host. The native test is shipped but skipped here. Windows AC-3 and the native portion of AC-6 remain unmet. On 2026-09-22 the operator directed external testing after the next release and requested final review for closure; these are recorded deferrals, not native success. The broader diagnostic ACs are reviewed against source contracts and portable tests under that bounded qualification scope.

The install audit checks registered manifest entries; removing both a required prompt and its manifest entry can escape that audit. The new guidance requires a separate canonical-catalog inventory. This wave does not claim a new runtime catalog-completeness validator.

Independent security/architecture integration review found and reverified timeout output loss (typed finding `timeout-discards-probe-output`, repair cycle 1). It approved the scoped implementation with native limitations; this is not full-wave delivery approval. Fresh code/QA executed 119 focused tests (118 passed, one deliberate native skip), killed old opt-in and strict-bypass mutants, and found no additional defect. Fresh docs-contract review found unshipped consumer pointers, a nonexistent sync-preview claim, and first-install bootstrap ordering ambiguity. These were repaired in typed cycle 2 and independently reverified against the current text, actual public dry-run and setup order. Both reviews withhold whole-wave approval pending native Windows qualification.

## Final integration corrections

Fresh docs-contract review reverified all three guidance repairs after cycle 2. The `wf_sync_surfaces` handler digest fixture changed in exactly one entry: the admitted tool-help correction. Coordinator compared HEAD/current handler maps (only this handler differed) and ASTs after removing the docstring (identical executable body, decorators and signature). No whole-fixture refresh or tool/schema change was made. A fresh code/QA reviewer independently confirmed exactly that one fixture delta and proved the entire HEAD/current server_impl AST identical after removing only that handler’s docstring.
