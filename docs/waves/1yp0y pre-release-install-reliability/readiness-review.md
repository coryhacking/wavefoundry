# Install Reliability Readiness Review

Owner: Engineering
Status: draft
Last verified: 2026-09-22

## Original pre-implementation readiness (historical)

Wave 1yp0y is undergoing final readiness review before source edits. Guidance-only Windows remediation is selected, preserving python3 and the no-environment-healing boundary. Diagnosis covers fresh installs and already-seeded repositories checked out on a new developer workstation. The operator approved explicit docs_lint.framework_internal_constants opt-in after the planned existing source identity was not found. The repaired packet names diagnose_python.ps1 and the strict pre-mutation prerequisite check.

## Adversarial primer

Fresh independent context: /root/install_readiness_primer. Standard depth, adversarial/constructive/simplicity stances. Strongest challenge: diagnosis currently requires the broken command, while the validator plan assumes an unestablished source identity. Best alternative: bounded read-only pre-Python diagnosis, guidance-only repair and an explicit opt-in for mandatory internal documentation claims.

Questions for the subsequent seats:

1. Which exact pre-MCP entry diagnoses failure without working python3 and proves no fallthrough into setup mutation on a seeded checkout?
2. Which explicit applicability contract replaces the missing identity premise, and which source/consumer controls preserve source drift detection?

## Executed baseline observations

Coordinator ran a disposable generic target through check_docs_constants: a project-owned performance-budget document produced four missing framework model/chunker errors. A bounded mocked TimeoutExpired at the real ensure_python_resolves subprocess boundary returned warn_existing_unusable and printed version None as below 3.11. These reproduce the scoped defects; they are not native Windows qualification.

Live code_read verification confirms _run_setup performs policy provisioning, surface rendering, memory-pointer migration, dependency provisioning and reconciliation before its strict Python check. render_bin_launchers emits a wf.cmd that directly invokes python3. The diagnostic must precede setup mutation and remain reachable before Python/MCP.

Source identity: targeted code_definition/code_keyword did not find a reliable shared discriminator; _audit_stale_tool_references explicitly documents its absence. The operator approved an explicit docs-lint opt-in instead. No source code or seeds have been edited, and no readiness approval is claimed.

## Environment limitation

Current host is Darwin; neither pwsh nor wine is installed. Real native Windows standard-user, fresh-host MCP and hook evidence remains a delivery/release requirement. Tests simulating Windows are not substitutes.

## Work allocation

Coordinator owns plan, lifecycle records and eventual cross-cutting implementation. Fresh independent red-team primer completed; code and security readiness reviewers are independently checking the current packet. Required QA, architecture, docs-contract and Council synthesis remain pending. Source edits wait for current typed readiness approvals on review-policy-323dc7a2aabe22ce553f.

## Initial specialist observations

The fresh code-reviewer context /root/install_ready_code independently reproduced the timeout misdiagnosis and eight incorrect mandatory facts on generic performance/reliability documents. It confirmed the source-identity blocker against _audit_stale_tool_references and recommends bounded pre-Python diagnosis with no alternate setup continuation. No readiness approval was issued.

The fresh security-reviewer context /root/install_ready_security found guidance-only pre-Python diagnosis feasible with no established new attacker-boundary defect. Preserve shell-free bounded probes, no execution-policy bypass, no environment repair and no MCP permission expansion. The unresolved source-identity requirement prevents readiness approval.

## Final readiness review on repaired packet

Receipt: review-policy-323dc7a2aabe22ce553f. Operator approved the explicit source opt-in. Windows remediation is guidance-only; new launcher/environment writes are excluded. The pre-Python entry is diagnose_python.ps1; strict setup validation precedes reconciliation and ignores the legacy skip.

Fresh context /root/install_ready_code_arch approved code and architecture remits after live source review and an executed AST ordering control. Fresh context /root/install_readiness_remaining approved security and docs-contract remits after live public-boundary inspection, strict-skip control and nine-claim census. Fresh context /root/install_ready_qa_fresh approved QA feasibility after independently reproducing consumer false-opt-in failures, timeout misdiagnosis and strict-skip behavior; phase-specific integrity was confirmed against seed 209 and its current readiness approval recorded.

Seats were isolated by review context, with paired remits in the same context explicitly disclosed (code/architecture, security/docs-contract, QA/reality-check). Retained primer/earlier review contexts supplied historical observations only and were not represented as fresh approvals.

The rotating docs-contract seat weighed automatic launcher/environment healing and package-layout inference; both add risk without improving the primary diagnosis objective. The selected read-only diagnostic, explicit claim-set opt-in and existing sync renderer preserve the narrowest behavior and authority. Native Windows standard-user and fresh-host evidence remains unproven, not waived.

Council approved on current receipt, unanimous across the three fresh grouped remit contexts, no unresolved severity. Native Windows qualification remains pending. Current typed lane/Council approvals and readiness run are recorded in events.jsonl.

## Current readiness refresh

Implementation tracking rotated the policy receipt to `review-policy-13752f41a8f9c25b46c8`. Fresh source-grounded contexts approved continued qualification: code/QA `/root/install_ready_refresh_code_qa`; architecture/security `/root/install_ready_refresh_security_arch`; docs/red-team/council recommendation `/root/install_ready_refresh_docs_council`. Paired remits share their named context; they are not five independent seats. All concur that scope remains bounded and native qualification stays mandatory. Coordinator synthesizes readiness approval from those concurrent verdicts, not delivery approval. Full framework suite now passes 9,550 tests with 13 intentional skips; native Windows remains unproven. Historical observations above describe the pre-change source, not current behavior.
