# Wave Record

Owner: Engineering
Status: closed
Last verified: 2026-06-09

wave-id: `1p4eq cross-file-resolution-followups`
Title: Cross File Resolution Followups

## Objective

Close the cross-file resolution gaps surfaced by the **1.6.0+p4ea test pack** (3 teams: Java/Swift/JS-TS) and extend cross-file method resolution to **C#/Go/Rust**, *before* shipping 1.6.0. Lands the field-feedback fixes (a latent resolution-suppression bug, the Java same-package miss, two `code_impact` ergonomics defects) plus the cross-language resolver coverage and a shared membership-based disambiguation that generalizes `1p470`. Closes when the field fixes ship and the C#/Go/Rust coverage is in (synthetic-validated), making the 1.6.0 graph tools correct across the languages our consumer projects actually use.

## Changes

Change ID: `1p4ef-bug graph-qualified-index-leaked-loop-var`
Change Status: `complete`

Change ID: `1p4er-enh same-package-receiver-disambiguation`
Change Status: `complete`

Change ID: `1p4es-bug code-impact-response-ergonomics`
Change Status: `complete`

Change ID: `1p4et-enh go-cross-file-method-resolution`
Change Status: `complete`

Change ID: `1p4eu-enh rust-cross-file-method-resolution`
Change Status: `complete`

Change ID: `1p4ev-enh membership-based-cross-file-disambiguation`
Change Status: `complete`

Completed At: 2026-06-09

## Wave Summary

Wave `1p4eq` (Cross File Resolution Followups) delivered 6 changes: Graph `qualified_index` Leaked Loop Variable (Phantom Cross-File Candidates), Same-Package / Same-Directory Receiver Disambiguation, `code_impact` Graph-Mode Response Ergonomics (Unbounded `edges` + `resolved: null`), Go Cross-File Method Resolution (`Type.method` Keying + `qualified_type` Receivers), Resolve Rust `Type::assoc_fn()`, Let-Binding Receiver Types, and Clean Use-Import Extractor (Cross-File Method Resolution), and Membership-Based Cross-File Disambiguation (Container Membership Generalizes Import-Head Matching). Notable adjustments during implementation: Same-Package / Same-Directory Receiver Disambiguation: Implementation complete. Same-package / same-directory fallback added to the cross-file rewrite-pass disambiguation block (`graph_indexer.py:6647-6677`, "Wave 1p4er: same-package / same-directory fallback"), ordered after the import path and gated on `resolved is None` + a unique in-directory survivor. 1p4eq faithfulness fix: the fallback is now LANGUAGE-GATED to Java/Kotlin/Go (`.java/.kt/.kts/.go`, `graph_indexer.py:6668`) — Python/JS/TS/Rust require an explicit import so same-directory confers no visibility, and C# uses the `.cs`-gated namespace-membership block instead (a same-dir C# file can be a different namespace). New regression test `test_python_same_dir_unimported_receiver_stays_external` (CrossFileResolutionTests) asserts a Python same-dir unimported receiver stays `external::`. ACs verified via `test_java_same_package_ambiguous_receiver_disambiguates` + `test_same_package_fallback_no_colocated_candidate_stays_external` (CrossFileResolutionTests); no-regression via `test_java_ambiguous_import_disambiguates` + `test_ambiguous_without_import_stays_external`. Note: AC-2's caller fixture is named `Caller.java` in the test (doc prose says `Foo.java`); behavior and assertions match. Full suite 2960 green; graph builder v25.; `code_impact` Graph-Mode Response Ergonomics (Unbounded `edges` + `resolved: null`): Implementation complete: `_code_impact_graph_response` caps emitted edges at `max_results` (`response_edges`), surfaces the untruncated count as `edges_total`, sets `resolved: True`, and folds edge-truncation into `truncated`; attribution counts stay on the uncapped `impact_edges`. Two 1p4eq nits also fixed in-session: `edges_total` + the edges cap are now documented in the `code_impact` tool docstring ("Response fields (graph mode") and negative `max_results` is clamped via `max_results = max(0, max_results)`. Full suite 2960 green; graph builder v25 (consolidated wave bump — no bump from this response-layer change).; Go Cross-File Method Resolution (`Type.method` Keying + `qualified_type` Receivers): Implementation complete. Go `method_declaration` keyed `Type.method` via the `walk_definitions` Go branch (`graph_indexer.py:5794-5797`) + `_go_method_node_receiver_type` (`:2593`); `_go_simple_type_name` (`:2555`) now package-PRESERVING (`qualified_type` → `foo.Helper`) and `_search_go_declarations_in_scope` (`:2624,:2640`) infers qualified-receiver types. AC-4 SUPERSEDED by the 1p4eq adversarial-verification faithfulness fix: the resolver no longer drops the package to bare `Helper` (which bound a co-located cross-package twin — wrong RECEIVER_RESOLVED edge); it returns `foo.Helper` and a new Go block in the cross-file rewrite pass (`:6589-6615`) resolves by the candidate's package directory, staying external when no project package matches.

**Changes delivered:**

- **Graph `qualified_index` Leaked Loop Variable (Phantom Cross-File Candidates)** (`1p4ef-bug graph-qualified-index-leaked-loop-var`) — 4 ACs completed
- **Same-Package / Same-Directory Receiver Disambiguation** (`1p4er-enh same-package-receiver-disambiguation`) — 5 ACs completed
- **`code_impact` Graph-Mode Response Ergonomics (Unbounded `edges` + `resolved: null`)** (`1p4es-bug code-impact-response-ergonomics`) — 5 ACs completed
- **Go Cross-File Method Resolution (`Type.method` Keying + `qualified_type` Receivers)** (`1p4et-enh go-cross-file-method-resolution`) — 6 ACs completed
- **Resolve Rust `Type::assoc_fn()`, Let-Binding Receiver Types, and Clean Use-Import Extractor (Cross-File Method Resolution)** (`1p4eu-enh rust-cross-file-method-resolution`) — 7 ACs completed
- **Membership-Based Cross-File Disambiguation (Container Membership Generalizes Import-Head Matching)** (`1p4ev-enh membership-based-cross-file-disambiguation`) — 8 ACs completed
## Journal Watchpoints

- **Sequencing (blocking):** `1p4ef` (leaked-`qualified`) is the **lead/precondition** — phantom `qualified_index` candidates make the rewrite pass's `len(candidates) == 1` unambiguity guard unreliable, so fix it before relying on any disambiguation result. `1p4ev` (membership) **depends on** `1p4et`/`1p4eu` (which make Go/Rust methods exist as `Type.method` to be disambiguated) and on clean per-language import edges. `1p4er` (same-package) is **independent + high-value** (uses the source file's own directory, no import-substrate dependency) — land it early. `1p4es` (`code_impact` ergonomics) is **independent** (`server_impl.py`, not the graph).
- **Version-bump watchpoint (blocking):** `1p4ef`, `1p4et`, `1p4eu`, and `1p4ev` all alter node/edge shape → each requires a `GRAPH_BUILDER_VERSION` bump (`graph_indexer.py:28`, currently `"24"`). **Bump ONCE for the wave** (not per-change) to avoid redundant full rebuilds — per `feedback_graph_builder_version_bump`. `1p4er` also changes resolution output (a rewrite-pass change) → include it under the same bump. `1p4es` is query/response-layer only → no bump.
- **Edit-gate watchpoint:** `1p4ef`/`1p4er`/`1p4et`/`1p4eu`/`1p4ev` edit `graph_indexer.py` (+ tests) → `framework_edit_allowed`. `1p4es` edits `server_impl.py`/`graph_query.py` (+ tests) → `framework_edit_allowed`.
- **Shared-file serialization (blocking):** `1p4ef`/`1p4er`/`1p4ev` all touch the cross-file rewrite pass region (`graph_indexer.py:~6322-6510`); `1p4et`/`1p4eu`/`1p4ev` touch the per-language extractors/resolvers. Sequence edits to that region and land the single `GRAPH_BUILDER_VERSION` bump last.
- **Validation gap (non-blocking but important):** **no consumer team tested C#/Go/Rust resolution** — `1p4et`/`1p4eu`/`1p4ev` are **synthetic-fixture-validated only**. Treat their coverage claims as unverified-against-real-code until a typed consumer re-tests (do not repeat `1p470`'s "covers C#/Go" over-claim — scope coverage to what has a passing test). The Java (`1p4er`) and the leaked-`qualified` (`1p4ef`) fixes ARE field-motivated.
- **Ship gate:** this wave lands **before** 1.6.0 ships (operator directive 2026-06-09). VERSION stays unstamped; rebuild a fresh test pack after this wave and re-validate with the teams before stamping/tagging/shipping `v1.6.0`. CHANGELOG draft for the graph tools is already parked in the unreleased `## [1.6.0]`.
- **Faithfulness-review watchpoint:** `1p4ev`'s membership disambiguation can over-resolve if the membership key is wrong; require the unique-candidate-after-filter guard + an adversarial "never binds the wrong twin" test per language, mirroring `1p470`'s `test_*_ambiguous_import_disambiguates` (see `feedback_security_control_faithfulness_review` for the adversarial-before-close discipline).
- **Claim-discipline wave-close gate (blocking at close — prepare-council must-fix):** when the single shared `GRAPH_BUILDER_VERSION` annotation is written (one owner, consolidating all graph-touching changes' rationale), it MUST (a) **correct the stale `graph_indexer.py:28` line** that still advertises `1p470`'s import disambiguation as "language-agnostic (Python + Java/Kotlin/C#/Go)" — dead code for C#/Go/Rust today; and (b) scope every C#/Go/Rust resolution claim in the version comment + the CHANGELOG `## [1.6.0]` draft + release notes **strictly to the synthetic shapes that have a GREEN adversarial test that *ran* (not skipped) in the parser-complete venv** (`~/.wavefoundry/venv`, which has all of `tree_sitter_c_sharp`/`go`/`rust` and is what `run_tests.py` uses — so a skip there is a failure). No per-language coverage claim ships ahead of a green test. (Operationalizes the Validation-gap watchpoint into a checkable close artifact; resolves prepare-council must-fixes #5/#6 — note: the red-team's "tests vacuously skip" finding was a system-`python3` artifact; the venv runs them.)

## Review Evidence

- wave-council-readiness: READY-WITH-FIXES (all fixes APPLIED in-session at Prepare) 2026-06-09 — all 6 changes source-verified implementable (the council spot-checked every load-bearing claim to the line: leaked-`qualified` at 6328/6341, the `1p470` block at 6464, the Go mis-key, `GRAPH_BUILDER_VERSION="24"`, all named regression tests). 5 structural must-fixes resolved at Prepare: (1) `1p4ef` doc completed (AC-Priority + Risks + Wave field); (2) `1p4er`/`1p4ev` shared-block ownership recorded (`1p4ev` replaces `1p4er`'s path — one code path); (3) `1p4ev`'s **C# precondition RESOLVED by probe** — C# methods are already keyed `Namespace.Class.method`, C# classes in a namespace do NOT collapse, so there's no method-keying prerequisite; AC-3 de-conditionalized to required-pass (operator decision: KEEP `1p4ev` with C# in scope); (4) `1p4ev` AC-6 tightened to require ≥1 GREEN adversarial test that *ran* (C# required); (5) claim-discipline wave-close gate added (correct the stale `graph_indexer.py:28` over-claim + scope C#/Go/Rust CHANGELOG claims to green-tested shapes). **Correction:** the red-team's "tests vacuously skip" finding was a system-`python3` artifact — the venv (which `run_tests.py` uses) has all grammars; 19 cross-language tests verified to RUN and pass. (seats: reality-check / architecture / qa / red-team / docs-contract — all CONCERN, all resolved; strongest-challenge: ship synthetic C#/Go/Rust coverage in 1.6.0 → mechanism ships now, per-language coverage *claims* gated on a green test that ran in the parser-complete venv, v1.6.0 unstamped until a typed consumer re-validates.)
- wave-council-delivery: PASS (fixes applied + re-verified) 2026-06-09 — two delivery reviews. **(1) Delivery-phase adversarial verification** (workflow `wr1h9goj7`, 3 seats in the parser-complete venv) caught TWO over-resolution wrong-edge bugs (`1p4ev` C# nested-class namespace mis-derivation; `1p4et`+`1p4er` Go package-qualifier drop) + 1 regression concern (same-dir fallback not language-gated) + 2 nits — ALL FIXED in-session and re-verified, with 4 new adversarial never-wrong-twin tests (each verified bug-sensitive). **(2) Pre-close review** (workflow `we4m05d5l`, faithfulness seat CLEAN; the import-regression + readiness seats hit a session limit and were completed DIRECTLY): faithfulness re-verification CLEAN (every never-wrong-twin boundary holds; the cleaner-`imports_by_file` over-resolution angle binds only the imported twin or stays external); import-regression CLEAN (no legit edge dropped in any in-scope language, calls unaffected by the import-only keyword filter; Ruby/Scala observations byte-identical at HEAD = pre-existing, out of scope); readiness CLEAN (`GRAPH_BUILDER_VERSION=25` single bump, all six changes complete, self-host graph v25, docs-lint ok, full suite 2962 green). Full record in the Delivery checkpoints below. (strongest-challenge: the late import-hygiene delta's broad `source_file`/keyword changes regressing other languages — REFUTED via per-language probes + HEAD diff; calls untouched.)
- operator-signoff: APPROVED 2026-06-09 — operator directed closure ("close the wave") after validating local test pack `wavefoundry-1.6.0.p4g3.zip` on two consumer projects (Aceiss/Java + Solaris/Swift): A1–A3 + B1–B4 pass, the prior same-package MISS fixed, no functional regression, edge-count drop confirmed as heuristic/external-noise removal (not lost resolution). Pre-close review CLEAN (faithfulness re-verification + import-regression + readiness). Close only — VERSION stays unstamped (ships before 1.6.0 per operator directive); no tag/publish/commit.

## Review Checkpoints

- **Prepare-phase Wave Council [prepare-council] — 2026-06-09: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, architecture-reviewer, qa-reviewer, reality-checker, docs-contract-reviewer, security-reviewer; rotating-seat: security-reviewer; strongest-challenge: shipping synthetic-only C#/Go/Rust coverage in 1.6.0 riding the field fixes' gate — the `1p470` over-claim trap, still live at `graph_indexer.py:28` — ruled *ship the mechanism now, gate every per-language coverage CLAIM on a green adversarial test that actually RAN in the parser-complete venv, leave v1.6.0 unstamped until a typed consumer re-validates* (the red-team's "tests vacuously skip" finding was a system-`python3` artifact — the venv `run_tests.py` uses has all grammars; 19 cross-language tests verified to run and pass); strongest-alternative: defer `1p4ev` to a post-consumer-validation wave — REJECTED by operator (keep `1p4ev`, C# in scope; its C# precondition was resolved by probe — C# methods already key `Namespace.Class.method` and don't collapse in a namespace, so no method-keying prerequisite, C# is a required-pass). Verdict was READY-WITH-FIXES; all 5 structural fix-before-prepare must-fixes were applied in-session (`1p4ef` doc completeness; `1p4er`/`1p4ev` shared-block ownership; `1p4ev` C# de-conditionalized; `1p4ev` must-pass-language AC; claim-discipline wave-close gate). Security-reviewer seat READY: the bind-only-on-unique-after-filter-else-stay-external faithfulness invariant is preserved on every new path and elevated from advice to ACs; 1 P2 fix-during-impl — an explicit "mis-key-under-ambiguity stays external" test for the new Go/Rust resolver paths.)

- **Delivery-phase adversarial verification [delivery] — 2026-06-09: 2 BUGS FOUND + FIXED in-session, re-verified.** Three independent seats (faithfulness/over-resolution hunter, edge-case hunter, regression-hunter), all probing real synthetic fixtures through `update_graph_index` in the parser-complete venv (`~/.wavefoundry/venv` — all of C#/Go/Rust/Java grammars present; system `python3` has none and would falsely skip). The faithfulness seat caught **two reproducible WRONG-EDGE (wrong project twin at `RECEIVER_RESOLVED`) over-resolutions — exactly the failure mode the Faithfulness-review watchpoint named — both NEW to this wave, both now FIXED:** (1) **`1p4ev` C# nested-class** — `_cs_ns` string-stripped a fixed 2 qname segments, mis-deriving a nested-class caller's namespace (`Acme.Web.Outer.App.Run` → wrongly `Acme.Web.Outer`) and binding a coincident sibling twin; FIX derives each node's namespace from its file's DECLARED namespace nodes (`cs_file_ns`, longest-prefix, nesting-proof). (2) **`1p4et`+`1p4er` Go** — `_go_simple_type_name` dropped the package qualifier of `foo.Helper`, collapsing it onto a co-located `bar.Helper` so the same-dir fallback bound the caller's own-package twin against an explicit cross-package type; FIX preserves `pkg.Type` and resolves by the candidate's package directory (this REVERSES `1p4et` AC-4's original "return bare `Helper`" design — see that doc's supersession note). **Regression seat** raised one CONCERN, also fixed: the `1p4er` same-dir fallback was language-agnostic and fired for Python/JS/TS/Rust where same-directory confers no visibility → now GATED to Java/Kotlin/Go. **Edge-case seat: CLEAN** (no crashes / None / wrong edges across all six changes; `1p4ef`/`1p4es`/`1p4eu` faithful) plus 2 doc/hardening nits (now fixed: `edges_total`+cap in the `code_impact` docstring; negative `max_results` clamp) and ONE transient non-reproducible failure of `test_java_same_package_ambiguous_receiver_disambiguates` it could NOT reproduce across 1040 fresh builds / 26 hash seeds (no code defect identified — flagged for a CI stress-loop before close, not a blocker). **Four new adversarial never-wrong-twin tests added** (`test_csharp_nested_class_caller_binds_own_namespace_not_sibling`, `test_go_qualified_receiver_binds_named_package_not_colocated_twin`, `test_go_qualified_receiver_unknown_package_stays_external`, `test_python_same_dir_unimported_receiver_stays_external`), each verified bug-sensitive; full `run_tests.py` green at **2960** on the venv; graph builder **v25** (annotation updated to record the faithfulness fixes). Workflow `wr1h9goj7`.

- **AC-5 + import-hygiene [delivery follow-up] — 2026-06-09: implemented (operator chose implement over defer).** `1p4eu` AC-5 (clean Rust `use`-import extractor) shipped: `_rust_use_imports` emits dotted import edges with `as`-alias honoring and zero keyword noise. Building it exposed a ROOT CAUSE shared with Kotlin/Go/Swift/C — the grammar root `source_file` was substring-matched as an import (`source` ∈ `_TS_IMPORT_KEYWORDS`), so the generic relation fallback regexed the WHOLE file into junk `external::<token>` import edges; fixed by excluding `source_file` in `_ts_is_import_node` plus an **import-only** `_RELATION_KEYWORD_NOISE` filter (deliberately NOT applied to call candidates — `from`/`require`/`default` are valid method names). Operator's Java/Kotlin question answered: Java, Kotlin, and Rust imports are all fully clean — keyword noise gone, `as` aliases captured in `import_aliases`, and the redundant bare-alias `external::W` node suppressed (an import candidate that is an `as`-alias whose real target is also emitted is dropped; uniform across Kotlin + TS/JS). New tests `test_rust_generic_receiver_stays_external` (AC-6), `test_rust_use_import_extractor_clean_no_keyword_noise` (AC-5), `test_kotlin_aliased_import_no_bare_alias_node`; full suite **2962** green; self-host graph rebuilt at v25. `1p4eu` now `complete` — all six changes done.

- **Pre-close review [delivery] — 2026-06-09: CLEAN, no blocker.** Three-area adversarial review of the FINAL state, including the import-hygiene delta that landed after the first verification. **Faithfulness re-verification (workflow `we4m05d5l`): CLEAN** — every never-wrong-twin boundary re-probed in the venv (C# nested-class → same-namespace twin; Go package-qualified → named package, unknown package external; Python same-dir → external; Rust use-extractor + generic → external). The NEW over-resolution angle (cleaner `imports_by_file` with real Rust/Kotlin heads) was specifically exercised: it resolves MORE correct cases but binds only the imported twin or stays external — the disambig keys on the receiver head, not arbitrary imports. **Import-regression: CLEAN** (completed directly — the workflow's other two seats hit a session limit mid-run): no legit import edge dropped in any in-scope language (Python/TS/JS/Java/Kotlin/Go/Rust/C#/Swift/C); calls unaffected by the import-only keyword filter (Rust `C::from()` intact); the Ruby (no `require` import edge) and Scala (`external::pkg` field-only) observations are byte-identical at HEAD → pre-existing, not wave regressions, and out of scope. **Readiness: CLEAN** — `GRAPH_BUILDER_VERSION=25` (single wave bump), all six changes `complete`, self-host graph project+framework both v25, docs-lint ok, 7 new adversarial tests, full suite 2962 green. **Consumer-validation note:** Kotlin/Rust import-disambiguation fires only when the source directory path mirrors the package/module path (path-derived module == import FQN); otherwise it conservatively stays external (under-resolves, never wrong-binds) — a typed-consumer re-test should gauge how often path==package holds in real trees. Local test pack `wavefoundry-1.6.0.p4g3.zip` built for validation.

## Dependencies

- No external wave dependencies.
