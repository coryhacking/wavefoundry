# Wave Record

Owner: Engineering
Status: closed
Last verified: 2026-07-11

wave-id: `1rsh9 sqlite-index-substrate`
Title: SQLite Index Substrate

## Objective

Generalize the embedded-SQLite pattern proven by the graph state store into a semantic-index state substrate: one derived-only relational store carrying the decay/freshness sidecar (the storage path `1ro43` recommends), an FTS5/BM25 hybrid lexical layer fused into ranked retrieval, per-path build bookkeeping with `meta.json` preserved as an exported snapshot, and a per-file secret-scan cache that ends the whole-repo re-scan on every ruleset change. When this wave closes, the semantic index has a transactional relational backbone, retrieval gains the documented hybrid-lexical quality lever, and secret scanning gains crash-safe content-addressed incrementality — with zero new dependencies.

## Changes

Change ID: `1rq4h-enh sqlite-index-state-store`
Change Status: `implemented`

Change ID: `1rrr0-enh sqlite-fts5-and-index-internals`
Change Status: `implemented`

Change ID: `1rsha-enh incremental-secret-scan-cache`
Change Status: `implemented`

Completed At: 2026-07-10

Change ID: `1sauc-enh retire-lance-tantivy-fts`
Change Status: `implemented`

## Wave Summary

Wave `1rsh9` (SQLite Index Substrate) delivered 4 changes: SQLite Index-State Store (Freshness/Decay Sidecar Substrate), FTS5 Hybrid Lexical Retrieval and SQLite Index Internals, Incremental Secret-Scan Cache (SQLite, Tier-2-Ready), and Retire the Lance/Tantivy FTS — code_search Lexical Half Moves to the FTS5 Layer. Notable adjustments during implementation: SQLite Index-State Store (Freshness/Decay Sidecar Substrate): Added SQLite maintenance (Req 9) and the `wave_index_optimize` unified all-index extension (Req 10) after operator asked whether SQLite needs Lance-style maintenance. Investigation found the codebase does zero explicit SQLite maintenance (no VACUUM/checkpoint/optimize anywhere) and `wave_index_optimize` explicitly skips the graph index — so the graph store is unmaintained today. ACs 8–9 added; verification gate → AC-10. Graph-store automatic build-path maintenance deferred to a follow-up to preserve AC-7.; SQLite Index-State Store (Freshness/Decay Sidecar Substrate): Added integrity verification (Req 11, AC-10) after operator asked what corruption checks are possible. Investigation confirmed zero integrity checks exist today (no `quick_check`/`integrity_check` anywhere); the graph store only reacts to corruption when a read raises. Two-layer probe added: physical (`quick_check`/`integrity_check` + FTS `integrity-check` from `1rrr0`) and logical (source-fingerprint binding, since `integrity_check` cannot see staleness), both routing to the existing derived-only rebuild and surfaced in `wave_index_health`. Verification gate → AC-11.; FTS5 Hybrid Lexical Retrieval and SQLite Index Internals: Readiness-council amendments applied: cross-engine atomicity claim replaced with the ordered-consistency + crash-reconciliation model (Req 2, AC-1); FTS query sanitization and malformed-query degrade added (Req 3, AC-2); meta.json compatibility restated as reader-contract, not byte equality (Req 7, AC-4); retrieval eval pinned to a fixed recorded query set (Req 5).

**Changes delivered:**

- **SQLite Index-State Store (Freshness/Decay Sidecar Substrate)** (`1rq4h-enh sqlite-index-state-store`) — 11 ACs completed. Key decisions: One semantic-index state store file with schema versioning, separate from the graph state store, holding derived-only relational state.; Route all SQLite maintenance through the existing `wave_index_optimize` tool (extended to cover the SQLite stores) rather than a new tool; run automatic maintenance only on the index-state store's build path, and cover the graph store on-demand only.
- **FTS5 Hybrid Lexical Retrieval and SQLite Index Internals** (`1rrr0-enh sqlite-fts5-and-index-internals`) — 11 ACs completed. Key decisions: Lexical layer via SQLite FTS5 in the index-state store, fused pre-rerank in `search_combined`.; FTS table mode: **contentful** (plain fts5 storing chunk text), NOT contentless.
- **Incremental Secret-Scan Cache (SQLite, Tier-2-Ready)** (`1rsha-enh incremental-secret-scan-cache`) — 9 ACs completed. Key decisions: Ship Tier 1 (per-file content+rules cache) now on the `1rq4h` store, with a Tier-2-ready schema (decomposed per-rule catalog + per-file provenance persisted, delta execution deferred).; `scan-state.json` kept as the exported snapshot (its `rules_hash` still written every pass); the cache supersedes it as the working skip state.
- **Retire the Lance/Tantivy FTS — code_search Lexical Half Moves to the FTS5 Layer** (`1sauc-enh retire-lance-tantivy-fts`) — 7 ACs completed. Key decisions: Retire Tantivy fully (both tables) rather than only dropping the consumer-less docs FTS.; Filter parity via `language`/`tags` UNINDEXED columns on the FTS tables (schema 3→4), not via post-join.
## Journal Watchpoints

- Watchpoint (sequencing): `1rq4h` blocks `1rrr0` — the substrate (schema versioning, recovery, shared helpers) must land before resident schemas are added.
- Watchpoint (single store): exactly one semantic-index state store file; creating any parallel sidecar is a defect. Cross-wave seam with `1ro43` (wave 1ro44): whichever lands second consumes/migrates — coordinate per `1rq4h` Requirement 5 and record the outcome in both change docs.
- Watchpoint (derived-only): storing unrecoverable state in the store is blocked; every table must rebuild from git/Lance/repo, and corruption recovery is drop-and-rebuild with a loud diagnostic.
- Watchpoint (retrieval safety): lexical fusion default-on is blocked until the weak-pattern evaluation is recorded (`1rrr0` AC-3); the kill switch ships regardless; `code_keyword` semantics must not change.
- Watchpoint (reader contracts): `meta.json` readers keep their contract this wave — snapshot byte-compatibility is proven before the store becomes the working source of truth; reader migration is deferred, not smuggled in.
- Watchpoint (graph store stability): shared-helper extraction must leave the graph store's behavior and test suite untouched (`1rq4h` AC-7).
- Watchpoint (schema-version sequencing): `1rrr0` (FTS + bookkeeping) and `1rsha` (secret-scan cache) both add resident tables to the one store; their schema-version bumps must be sequenced, not concurrent, to avoid a version collision.
- Watchpoint (secret-scan safety): the scan cache is a skip optimization only — a differential equivalence harness must prove cache-path findings identical to a full scan, and any corrupt/missing cache fails toward a full re-scan with a loud diagnostic. A cache defect must never become a missed secret. Tier 2 (rule-delta execution) does not ship this wave; only the Tier-2-ready schema and a recorded feasibility spike do.
- Watchpoint (unified maintenance): all SQLite maintenance routes through one path — the shared-helper primitives (`1rq4h` Req 9) and the extended `wave_index_optimize` (`1rq4h` Req 10). FTS segment-merge (`1rrr0` Req 12) and the secret-scan cache (`1rsha`) plug into that path; no change may add a parallel vacuum/checkpoint/optimize surface.
- Watchpoint (unified integrity): corruption verification is one two-layer probe in the shared helper (`1rq4h` Req 11) — structural `quick_check`/`integrity_check` plus source-fingerprint binding for logical staleness — surfaced in `wave_index_health` and routed to the derived-only drop-and-rebuild. FTS `integrity-check` (`1rrr0` Req 13) and the scan-cache fingerprint (`1rsha`) contribute into that probe, not a parallel one. Detection is the whole job; recovery is already free because nothing unrecoverable is stored. Automatic maintenance applies to the index-state store's build path; the graph store gets on-demand coverage only (its build-path behavior stays untouched per `1rq4h` AC-7). The graph store's `-wal` was investigated 2026-07-05 and confirmed NOT growing (build-subprocess-only; the long-lived server never opens the state store, so it checkpoints and drops `-wal`/`-shm` per build) — no graph-store follow-up is needed. The long-lived-reader exposure is real for the new index-state store instead (query-path reads live in the server), which is why Req 9's WAL-truncate is load-bearing there.

## Participants

- code-reviewer — framework-script changes across `indexer.py`, `server_impl.py`, and the new store module
- qa-reviewer — AC priority tables present on both changes; differential-harness and eval gates verified
- architecture-reviewer — storage boundary (Lance vs SQLite vs meta.json snapshot), retrieval fusion contract
- performance-reviewer — build-transaction cost, BM25 fetch on the query path, registry-backed incremental flip, secret-scan skip vs whole-repo re-scan
- security-reviewer — FTS query input handling, store file posture, secret-scan cache-vs-full equivalence and fail-to-full-scan safety

## Review Checkpoints

- pre-implementation-review: passed (2026-07-10) — pre-mortem named five churn risks: (1) the 1rq4h/1ro43 scope boundary on churn population — resolved by reading `1ro43` Requirements 1–3, 6, 12–13 directly: 1rq4h ships the tables, the build-time freshness write path (batched local git extraction, mtime fallback), and the per-path read primitive `{age_days, churn_score, commits_since}`; attribution/drift tables ship as schema + write/read APIs with fixture coverage while their derivation logic stays in 1ro43; (2) destabilizing the graph store during helper extraction — resolved by choosing extract-alongside (new store module modeled on `GraphStateStore`, `graph_indexer.py` untouched), which satisfies AC-7 by construction; (3) the 1rycf close-time optimize overlap — `optimize_index_tables` stays Lance-only so the reviewed close-time path is untouched; SQLite maintenance is a sibling function under the same lock discipline; (4) FTS naming collision — the existing Lance/Tantivy FTS (`_create_fts_index`) is distinct from the new SQLite FTS5 layer, names and docs must disambiguate; (5) schema-version collision between 1rrr0 and 1rsha — sequenced bumps per the wave watchpoint. Packet completeness verified: all three change docs carry Requirements/ACs/AC-priority tables; council readiness recorded 2026-07-05 with amendments applied; lanes rostered (code-reviewer, qa-reviewer, architecture-reviewer, performance-reviewer, security-reviewer); MCP-grounded exploration completed against `graph_indexer.py` (GraphStateStore pattern :1126–1411), `indexer.py` (meta I/O :1189–1293, optimize/reclaim :1584–1713, incremental delta :2110–2357, build finish :3644–3776), `server_impl.py` (`search_combined` :1668–1925, `_wave_index_optimize_response` :7111, `wave_index_health_response` :6437), `scan_secrets.py` (full), `run_secrets_scan.py` (full), and the retrieval constants (VECTOR_TOP_K 30/50, AGENT_TEXT_BUDGET_CHARS 16000, AGENT_CANDIDATE_MAX 40). Ordered lane sequence: store-substrate → freshness-schema/build-integration → maintenance/integrity/health (1rq4h) → FTS layer → fusion → eval → bookkeeping → incremental-flip (1rrr0) → scan cache → equivalence harness (1rsha) → wave verification. Highest risk: the 1rrr0 incremental-path flip touching index integrity — mitigated by the differential-harness-before-flip serialization point already in the change doc.
- **Prepare-phase Wave Council [prepare-council] — 2026-07-04: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, architecture-reviewer, security-reviewer, qa-reviewer, reality-checker, performance-reviewer; rotating-seat: performance-reviewer; strongest-challenge: 1rrr0 AC-1 claimed FTS and Lance consistency at every observable point, but cross-engine atomicity between a Lance write and a SQLite transaction is impossible, making the AC untestable as written and inviting a false-confidence fixture — resolved by amendment restating the model as ordered consistency with Lance authoritative, store commit ordered after Lance writes, and a crash-window reconciliation pass with a loud diagnostic; strongest-alternative: an external lexical engine (tantivy-class) for stronger tokenization control — rejected because it adds a dependency and a second index lifecycle for capability FTS5 already provides with zero dependencies, consistent with the lean local-only posture)
- Council seat notes: red-team also pressed the byte-compatibility claim on the meta.json snapshot (unachievable and the wrong contract — resolved by amendment to reader-contract compatibility: unmodified reader tests plus semantically identical parsed content) and asked whether BM25 fetch respects the candidate/text-budget machinery (resolved by amendment naming the budget constants in Req 3). architecture-reviewer: single store file with schema versioning is the right boundary; query path must touch the store read-only — concur with the ordered-consistency amendment. security-reviewer: user query text must reach FTS5 only via parameterized statements with FTS-syntax escaping, and FTS5-rejected queries degrade to vector-only without error — amendment applied to Req 3/AC-2. qa-reviewer: crash-mid-build fixtures required between Lance write and store commit (covered by the reconciliation amendment); the weak-pattern eval needs a fixed recorded query set to prevent eval drift — amendment applied to Req 5. reality-checker: FTS5 presence in bundled Python SQLite is common but not universal — the probe-and-degrade requirement makes the claim safe; meta.json swap/retry machinery and the chunk_hash Lance-read incremental path verified against source this session. performance-reviewer (rotating): contentless vs contentful FTS mode must be audited against the bundled SQLite version's delete-support before implementation (already deferred to the Decision Log — accepted); registry-backed incremental flip requires measured timing evidence, already mandated by the audit-and-adopt discipline. seat_agreement: unanimous; no challenge round required; amendments applied in-session before readiness was recorded.
- AC priority: confirmed at prepare as proposed in each doc (1rq4h AC-1..5, 7..11 required, AC-6 important — AC-8/9 maintenance and AC-10 integrity added 2026-07-05, gate → AC-11; 1rrr0 AC-1..5, 7..11 required, AC-6 important — AC-8 secret-posture, AC-9 FTS-maintenance, AC-10 FTS-integrity added 2026-07-05, gate → AC-11; 1rsha AC-1, 2, 4, 6..9 required, AC-3/5 important). Product-owner acknowledgment: wave scope, the change split, the FTS/meta.json bundling (2026-07-04), the third change 1rsha (2026-07-05), and the SQLite maintenance + unified `wave_index_optimize` + integrity-verification additions (2026-07-05) were operator-directed in-session.
- **Prepare-phase Wave Council [prepare-council] — 2026-07-05: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, architecture-reviewer, security-reviewer, qa-reviewer, reality-checker, performance-reviewer; rotating-seat: security-reviewer; strongest-challenge: admitting the secret-scan cache (1rsha) risks the worst failure mode in the wave — a cache that causes the scanner to find FEWER secrets than a full scan — because a skip decision now sits in front of a security control; resolved before readiness by the change's own contract (differential cache-vs-full equivalence harness across the fixture matrix as AC-6, derived-only fail-to-full-scan self-heal as AC-7, and the cache explicitly non-authoritative over the scanner) plus a wave watchpoint making the fail-safe direction a hard rule; strongest-alternative: ship full Tier 2 rule-delta scanning now for the large speedup — rejected because rule-delta execution must first clear a feasibility spike (does the betterleaks engine support per-rule execution while preserving phase-2 exception/allowlist semantics) and an equivalence burden, so the wave ships Tier 1 with a Tier-2-ready schema and defers Tier 2 to a future evidence-gated change)
- Council seat notes (2026-07-05 re-review for 1rsha): security-reviewer (rotating): a scan cache in front of a security gate is acceptable only if it is provably non-suppressive — the equivalence harness and fail-to-full-scan must both be required ACs (confirmed present); also verified 1rrr0's new AC-8 secret-posture assertion is a tested control, not prose. performance-reviewer: the honest win is on the rules-change escalation (whole-repo re-scan today), not the already-git-gated incremental path — instrumentation (1rsha AC-3) must quantify skipped-vs-scanned so the Tier 2 decision rests on measured cost; content_hash cost is below scan cost and doubles as cache identity. architecture-reviewer: the secret-scan cache is a legitimate resident of the index-state store (same derived-only rule), but its schema-version bump must be sequenced with 1rrr0's — folded into a wave watchpoint. red-team: pressed whether the Tier-2-ready schema is speculative complexity — resolved because the per-rule catalog is parse/hash-only (no execution), also powers the rules-change instrumentation, and the feasibility spike prevents a doomed Tier 2 build. qa-reviewer: the differential harness must include a rules-change fixture and a revert fixture, not only add/modify/delete (confirmed in 1rsha AC-6). reality-checker: verified against source this session — scan-state.json holds only rules_hash, rules_changed forces scan_all, the secrets scan already runs in the project-layer index build, and the wave_close secrets gate is separate; the "already cheap incremental" premise holds. seat_agreement: unanimous; no challenge round; 1rq4h and 1rrr0 verdicts from 2026-07-04 stand unchanged.
- Council seat notes (2026-07-05 re-review for SQLite maintenance + unified `wave_index_optimize`, `1rq4h` Req 9–10 / `1rrr0` Req 12): reality-checker: verified the premise against source — `wave_index_optimize` is Lance-only and its docstring explicitly states "the graph index is not reclaimed this way," and a keyword sweep found zero `VACUUM`/`wal_checkpoint`/`PRAGMA optimize`/`auto_vacuum` in the scripts, so the graph store is unmaintained today; the gap is real. architecture-reviewer: routing all maintenance through the one existing tool (already auto-running at setup/upgrade, already reporting per-target reclaim) is the right boundary; the FTS merge and secret-scan cache must plug into it, not fork it — folded into a watchpoint. performance-reviewer: full `VACUUM` under the build lock could stall large-store builds — resolved by making incremental auto_vacuum the default and reserving full `VACUUM` for the on-demand path, mirroring on-demand Lance heavy compaction; end-of-build WAL truncate is cheap and bounds the realistic failure mode. red-team: strongest challenge — adding end-of-build maintenance to the shared helper risks changing the graph store's reviewed build behavior and breaking `1rq4h` AC-7; resolved by scoping automatic build-path maintenance to the index-state store only and giving the graph store on-demand coverage via the tool, with automatic graph build-path maintenance explicitly deferred to a follow-up. security-reviewer: maintenance is derived-only and never touches findings or vectors — no new exposure; concur. qa-reviewer: require a WAL-bounded fixture (repeated builds) and an FTS-segment-bounded fixture (churn), both now ACs (`1rq4h` AC-8, `1rrr0` AC-9). seat_agreement: unanimous; no challenge round; the graph-store unbounded-WAL question was subsequently investigated (2026-07-05) and closed as a non-issue — the store is build-subprocess-only and the long-lived server never opens it, so it checkpoints per build (no `-wal`/`-shm` on disk while the server runs); the exposure that justifies the maintenance is the new index-state store's query-path reads, not the graph store.
- Council seat notes (2026-07-05 re-review for integrity verification, `1rq4h` Req 11 / `1rrr0` Req 13): reality-checker: confirmed no integrity check of any kind exists today (keyword sweep: zero `quick_check`/`integrity_check`), and the graph store only reacts to corruption when a read raises — so proactive detection is a genuine gap, not gold-plating. red-team: strongest challenge — `integrity_check` is a false sense of security because it proves the b-tree is sound but NOT that rows still match source, so a stale-but-intact store would pass and mislead; resolved by making the probe two-layer (structural pragma + source-fingerprint binding for logical staleness), with the fingerprint precedent taken from the graph store's existing payload binding. security-reviewer: for the secret-scan cache this is load-bearing — a corrupt/stale cache must fail to a full re-scan, so the cache binding into the shared probe reinforces the never-a-missed-secret property (confirmed in `1rsha`). performance-reviewer: `integrity_check` is O(db size) — resolved by tiering (`quick_check` at open/routine, full `integrity_check` on-demand only), mirroring the reclaim tiering. architecture-reviewer: the probe must be one shared-helper surface that FTS and the scan cache contribute into, not three; folded into a watchpoint. qa-reviewer: require fixtures for both classes (byte-corrupt file → structural-fail → rebuild; stale fingerprint → stale-fingerprint → rebuild) plus the FTS-corruption case — now `1rq4h` AC-10 and `1rrr0` AC-10. seat_agreement: unanimous; no challenge round; the derived-only rule makes detection the whole task since recovery was already specified.

- **Prepare-phase Wave Council extension [prepare-council] — 2026-07-11: PASS for the late-admitted `1sauc-enh retire-lance-tantivy-fts`** (moderator: wave-council; operator-directed late admission — "let's roll that into this wave"; seats: reality-checker, red-team, qa-reviewer, architecture-reviewer, performance-reviewer. reality-checker: verified against source this session — `_lance_fts_search` has exactly ONE call site (`search_code`, code table; `query_type="fts"` appears once), `docs_search`'s lexical fallback is token-overlap not Lance FTS, the `language` filter is a post-filter on the row's `language` field (not the Lance where-clause, which covers only kind/tags — simplifying parity), installed lancedb 0.33.0 exposes `drop_index`, and the leak is measured live (docs `_indices/`: 83 MB FTS across 18 copies + 9 MB vector; code: 15 MB across 6 + 6 MB vector). red-team: strongest challenge — the old path is DELETED, so there is no kill switch if `code_search` quality regresses; resolved by making the before/after eval a completion gate (BEFORE snapshot already recorded: 9× rank 1, 1× rank 2 on the 10-query set) with the dense half and reranker unchanged, and by the degrade contract (empty lexical half = dense-only, the exact behavior FTS5-less interpreters already get). qa-reviewer: required fixtures — removal wiring locks, per-filter parity (kind/tags/language), v3→v4 store convergence, legacy `drop_index` cleanup, hostile-query and absent-store degrade; live `_indices/` before/after recorded as AC-4 evidence. architecture-reviewer: completes the single-lexical-engine consolidation the wave's own ADR (`1s5u9-adr`) implies; schema bump 3→4 is sequenced after 1rsha's 3 (same wave, serialized — the watchpoint holds); the FTS tables gaining `language`/`tags` UNINDEXED columns is row-carried data, not a new surface. performance-reviewer: removes a whole-FTS rebuild from every changed build (the `_create_fts_index(replace=True)` cost) plus the leak class; FTS5 lexical fetch is ~ms; no new hot-path cost. seat_agreement: unanimous; the eval-as-completion-gate amendment is recorded in the change doc's Serialization Points.)

## Review Evidence

- wave-council-readiness: approved 2026-07-05 — prepare council READY over all three changes plus the SQLite maintenance / unified `wave_index_optimize` additions (`1rq4h` Req 9–10, `1rrr0` Req 12) and the two-layer integrity verification (`1rq4h` Req 11, `1rrr0` Req 13, inherited by `1rsha`); 2026-07-04 pass stands, extended 2026-07-05 to admit 1rsha, the maintenance scope, and the integrity probe, gated on the equivalence/self-heal safety net, incremental-vacuum-by-default, index-state-store-only automatic maintenance (graph store on-demand), source-fingerprint logical-staleness detection, and WAL/FTS-segment/corruption fixtures; seats unanimous; full synthesis in Review Checkpoints
- wave-council-delivery: approved (2026-07-10 — moderator: wave-council; adversarial delivery review against the actual code, tests, and live-index evidence; no blocking findings. **code-reviewer** — the substrate is extract-alongside as decided: `graph_indexer.py` has zero edits, so AC-7 holds by construction; the store module mirrors the graph store's proven posture (WAL + synchronous=NORMAL + busy_timeout, reset-and-recreate on corrupt open, whole-store version gate) and adds what the substrate needed (auto_vacuum at creation, quick_check at open, drop-and-recreate reset that survives table-shape changes); every build-path store call is fail-safe by inspection (freshness, bookkeeping/snapshot, chunk-delta sync, reconcile all catch-and-report — a store defect can never fail a build); the fusion threads lexical hits as a third selection source so the existing key-merge produces the `sources` marker with no bespoke marker code, and vector candidates' cosine scores are never overwritten by the fallback (guarded on falsy score). Verified the two mid-implementation defects the live rollout surfaced were closed properly rather than patched around: Lance duplicate-id rows are deduped at the derived-rebuild boundary with the invariant documented, and the registry-skip/drift-repair conflict is closed at the correct layer (drift-flagged paths exempt from the skip — Lance stays the authority) with the reap now ordered before the reconcile so FTS never retains reaped content between builds. **qa-reviewer** — 82 new tests across four surfaces (25 store, 31 lexical layer, 19 scan cache, 7 server tools) covering every required AC with deterministic fixtures: corruption recovery (page-tail corruption after learning mid-page bytes land in free space quick_check legitimately ignores), WAL-bounded-at-zero after repeated builds, crash-window reconciliation with diagnostics, FTS-hostile query degrade, snapshot semantic round-trip, the registry differential (skip condition ⇒ provably no-op plan; every real-change class falls through), and the secret-scan differential harness driving the REAL scanner through a six-mutation git fixture matrix. The full-suite run earned its keep twice: the subprocess stdin-isolation guard caught the store module's raw git spawns, and `LanceDriftEligibilityBuildTests` caught the registry-skip/drift-repair equivalence break the unit-level differential could not see — both fixed and regression-locked (`test_drift_flagged_paths_are_exempt_from_the_skip`). Full suite 4,809 OK bytecode-free; `wave_validate` clean. **architecture-reviewer** — the single-store boundary held under three resident schemas with sequenced version bumps (1→2→3, per the watchpoint); maintenance and integrity each have exactly one surface (`wave_index_optimize` + the shared two-layer probe) that FTS and the scan cache contribute into rather than fork; `meta.json` readers keep their contract via the exported snapshot with reader migration correctly deferred; the 1ro43 seam ships as specified (tables + primitive here, derivation logic there; landing order resolved — 1ro43 consumes as-is). ADR `1s5u9-adr` records the substrate decision with constraints (no parallel sidecars, no parallel maintenance surfaces, stdlib only). **performance-reviewer** — every adoption is measured, not asserted: registry skip 0.14s vs 1.68s per table isolated same-inputs (the confounded full-build A/B was rejected and re-measured — graph-extraction CPU contention, a methodology catch worth keeping); lexical fusion adds two indexed FTS fetches per query with per-query latency unchanged in the eval; end-of-build WAL truncate holds the store's `-wal` at 0 bytes across repeated live builds; the one-time costs (cold scan cache full pass, rules-hash-fix full re-scan) are disclosed in the decision logs. **security-reviewer** — the scan cache is provably non-suppressive: the differential harness plus fail-toward-full-scan self-heal (absent/corrupt/version-mismatch fixtures) close the worst failure mode, and the never-suppresses property is asserted directly; the rules-fingerprint fix means the fingerprint now actually covers the 297KB framework ruleset that was silently outside it — a strengthening of an existing control, correctly treated as in-scope; FTS query text reaches SQLite only as a bound parameter with all tokens quoted (operator injection becomes literal tokens, fixture-proven); the second chunk-text copy inherits the Lance posture as a TESTED control (gitignored residency, pack exclusion, writers pinned to the two Lance-fed functions). Synthesis verdict: SHIP — all three changes deliver their required ACs with evidence; the two live-fire defects were found by the wave's own verification machinery and closed at the right layer; deferred items (Tier 2 rule-delta, meta.json reader migration, stale-content drift cross-check, graph build-path auto-maintenance) are recorded as explicit follow-ups, not silences.)
- wave-council-delivery addendum for `1sauc` (2026-07-11 — moderator: wave-council; adversarial review of the late-admitted retirement against code, tests, and live evidence; no blocking findings. **code-reviewer** — the removal is complete and locked: `_create_fts_index` and all four call sites are gone (the 1p95j change-gated rebuild block removed with an explanatory comment), `test_no_lance_fts_created_anywhere` pins both the function name and any `.create_fts_index(` call out of `indexer.py`, and the live-lancedb integration test asserts zero FTS indices exist after real builds; `_drop_legacy_fts_indices` is best-effort, runs before the tier-1 optimize so the cleanup GCs de-referenced versions, and matches by index type OR the known `text_idx` name; `_fts5_lexical_search` negates bm25 so the existing best-first sort and RRF ordering hold, and every degrade path returns `[]` (the same dense-only shape an FTS5-less interpreter gets). **qa-reviewer** — the behavior gate is the strongest evidence: rank-identical before/after on all 10 recorded queries with the language filter verified live; per-filter parity fixtures (kind exact, tags any-of, language row-carried), v3→v4 convergence fixture plus the LIVE convergence (reset → 19,942 + 14,389 rows backfilled), legacy-drop fixture plus the LIVE drop (both `text_idx` indices dropped, 148 MB reclaimed); the cold-provisioning flag is tested for the exact real-world sequence (partial in-build deltas before the reconcile) AND for flag-clearing so warm divergence stays loud; full suite 4,818 OK bytecode-free. **architecture-reviewer** — completes the single-lexical-engine consolidation the ADR records; the schema bump is sequenced (v4 after v3, same wave, serialized); no new surfaces — the lexical fetch rides the existing store module and the legacy cleanup rides the existing reclaim tiering. **performance-reviewer** — build path loses the whole-FTS rebuild on every changed pass; `_indices/` reduced to pure vector index (9.6/7.1 MB); the FTS5 lexical fetch is milliseconds; net on-disk index footprint fell from ~302 MB to ~154 MB on this repo even counting the FTS5 layer. **red-team** — pressed the no-kill-switch deletion: accepted because the eval gate ran BEFORE completion was declared, the dense half + reranker are unchanged, and the degrade contract makes the worst case identical to the long-tested FTS5-unavailable posture; also pressed whether `drop_index` could remove the vector index — the filter matches FTS index types/names only, and the fixture asserts the vector index survives. Synthesis verdict: SHIP.)
- operator-signoff: pending operator confirmation at closure

## Dependencies

- No hard external wave dependency. Recommended ordering: land before wave `1ro44` implements `1ro43`'s storage so the freshness tables are consumed directly; if `1ro44` lands first, `1ro43` ships a minimal sidecar and `1rq4h` absorbs/migrates it (both change docs carry this seam).
