# Wave Record

Owner: Engineering
Status: closed
Last verified: 2026-08-18
review-evidence-source: events.jsonl

review-policy-reprepare-required: false
wave-id: `1vj4e backstage-techdocs-baseline`
Title: Backstage Techdocs Baseline

## Objective

Give every installing project a one-command path to Backstage-eligible, human-readable TechDocs: `1vj4d` ships the conservative, missing-only catalog and TechDocs baseline as an explicit `wf techdocs-baseline` command (generated-by markers, tree-derived partial-state warning, gated on the navigation targets existing), and `1vmpz` ships the **Refresh TechDocs** workflow and `wf-techdocs` skill that runs it and then has the technical-writer specialist author the published pages with Guru, architecture, security, qa, and docs-contract collaboration. Skill-first by operator decision: nothing auto-runs at setup or upgrade in this wave; opt-in auto-generation is deferred to a later change.

## Changes

Change ID: `1vj4d-enh backstage-catalog-and-techdocs-baseline`
Change Status: `implemented`

Change ID: `1vmpz-enh refresh-techdocs-skill-and-collaboration-prompt`
Change Status: `implemented`

## Participants

- Coordinator: wave-coordinator
- Write-owning roles: implementer
- Requested review lanes: none
- Required review lanes: code-reviewer, qa-reviewer, architecture-reviewer, docs-contract-reviewer

Completed At: 2026-08-18

## Wave Summary

Wave `1vj4e` (Backstage Techdocs Baseline) delivered two changes: Backstage Catalog and TechDocs Baseline and Refresh TechDocs: a `wf-techdocs` skill and collaboration prompt that generates the baseline and authors the published pages. Notable adjustments during implementation: Backstage Catalog and TechDocs Baseline: Delivery-review cycle 2. The cycle-1 reverification found the code repair incomplete: `UnicodeDecodeError` is a `ValueError`, so a target-local template saved in another encoding escaped the `(RuntimeError, OSError)` clause entirely, and the code lane reproduced it on a real fixture (CLI traceback, empty stdout under `--json`, exit 1 with `catalog-info.yaml` already on disk; MCP entry raised with no envelope and no cache invalidation). The module write loop now catches `(RuntimeError, OSError, UnicodeDecodeError)`, matching its own sibling `techdocs_member_is_generated`, so both entries keep one typed channel; new test `test_a_non_utf8_template_is_an_envelope_not_a_traceback` (killed by reverting the clause on a scratch copy). The CLI now takes a typed `except TechdocsWriteFailed` branch (retiring the dead import) with a documented generic branch behind it; `server_impl` keeps one combined `except (TechdocsWriteFailed, RuntimeError, OSError)` reading `getattr(exc, "written_paths", ())`, which is behaviourally identical because `TechdocsWriteFailed` subclasses `RuntimeError` (a reviewer mutant removing the typed member from the tuple left all tool tests green). The first version of this row claimed both entries were restructured; two lanes caught that, and the sentence is corrected rather than the working code churned; the `OSError` test was renamed to `test_non_runtimeerror_failures_are_normalized_into_an_envelope` because its old name asserted an entry-point path that the module makes unreachable (qa proved reverting the entry clauses leaves the suite green). `test_only_the_thin_entry_calls_the_command_function` renamed to `test_only_the_two_declared_entries_call_the_command_function`: the old name was itself the DEL-3 exclusivity claim. `mcp-tool-surface.md` now records that `preserved_paths` overlaps `generated_paths` on the success path and is disjoint on the failure path. Suite 7349 tests / 63 files OK.; Backstage Catalog and TechDocs Baseline: Delivery-review repairs (cycle 1). DEL-2 (error-path truthfulness): `render_techdocs_baseline`'s write loop now raises the new `TechdocsWriteFailed` carrying the members this run wrote before failing; the CLI splits its single `except RuntimeError` into `TechdocsDestinationRefused` (preflight, nothing written, keeps the `(nothing written)` suffix) and `(RuntimeError, OSError)` (post-preflight, reports `written_paths` plus every member's state, drops the false suffix), so an unreadable template is an envelope instead of a traceback with empty stdout under `--json`; `wf_techdocs_baseline_response` catches `OSError` too and fills `written_paths` / `preserved_paths` / `generated_paths` / `absent_paths` from the new public `techdocs_member_states` instead of the mixed-only `classify_techdocs_baseline`, which reported empty lists on the dominant non-mixed failure. DEL-3 (false exclusivity): the module docstring, the family block comment, and `install-assets.md`'s consumers cell now name both callers. Carriers: `mcp-tool-surface.md` error bullet rewritten (the two pre-write shapes vs the post-preflight one), the response docstring, and the CHANGELOG exit-code phrase. Tests: `test_server_tools.TechdocsBaselineToolTests.test_oserror_after_preflight_becomes_an_envelope_not_an_exception` (new) and the write-failure test now pin the truthful lists; `test_wf_cli...test_post_preflight_failure_reports_what_was_written` (new) with a preflight-refusal control. Two mutants on a scratch copy killed by the new tests: collapsing the CLI channels back to one `except RuntimeError` fails the CLI test, and reverting the server envelope to `classify_techdocs_baseline` fails both server tests.; Backstage Catalog and TechDocs Baseline: Delta readiness pass on the Requirement 10 / AC-8 amendment (MCP tool): red-team seat, docs-contract seat/lane, code-reviewer, qa-reviewer, architecture-reviewer, all fresh, all APPROVE-with-editorial; fix-before-implement pins folded: the deliberate widening of `test_only_the_thin_entry_calls_the_command_function` to admit `server_impl.py` (RT-1/CR-MCP-1), public-module import so one patch is observed by both entry points (RT-2/CR-MCP-3), refusal scope split into `techdocs_destination_refused` (preflight, `TechdocsDestinationRefused`) vs `techdocs_write_failed` (RT-3/DC-5), `status: dry_run` for the read-first path (DC-6), `invalid_arguments` for a bad mode (CR-MCP-5/QA-MCP-2), lint attach mode and docs-index refresh on write (CR-MCP-7/RT-7), and the complete carrier list (seed 050 write-tier sentence incl. its four pre-existing omissions, seed 100 rule, the existing index row's purpose cell, the mcp-tool-surface signature-form entry, the four architecture docs by row, seed 160 phrase, README, handoff; RT-4, DC-1..4, DC-7..9, ARCH-1).

**Changes delivered:**

- **Backstage Catalog and TechDocs Baseline** (`1vj4d-enh backstage-catalog-and-techdocs-baseline`) — 8 ACs completed. Key decisions: Add the `wf_techdocs_baseline` MCP tool in this wave (operator decision after implementation, before Review wave), superseding the same-day deferral: the tool wraps the one module function with a read-first `dry_run` default and a `run` mode, registered write-tier, publication-registered, cost-accounted; the CLI stays as the no-MCP and install-row fallback.; Requirement 8 verified in a scratch environment: `mkdocs-techdocs-core` 1.7.0 declares `mkdocs>=1.6` and resolved MkDocs 1.6.1 with `pathspec` 1.1.1; MkDocs applies `exclude_docs` per file through `pathspec.gitignore.GitIgnoreSpec.match_file(src_uri)` after prepending its default `.*` and `/templates/` exclusions (no directory pruning during the walk). The golden block, run through that matcher, keeps exactly `index.md`, `ARCHITECTURE.md`, `architecture/**`, `references/**`, `prompts/index.md` and rejects `README.md`, `SECURITY.md`, `agents/**`, prompt bodies, the manifest, `plans/**`, `waves/**`, `reports/**`, `workflow-config.json`, `repo-profile.json`, `scans/**`, unknown root files and directories, and `prompts/sub/index.md`. Under pathspec each of `!/architecture/` and `!/architecture/**` (and the references pair) alone suffices, so removing one line of a pair flips nothing; the load-bearing single lines are `/*`, `/prompts/*`, `!/index.md`, `!/ARCHITECTURE.md`, `!/prompts/index.md`. One real `mkdocs build --strict` against the golden trio plus `docs/README.md`, `docs/SECURITY.md`, `docs/agents/guru.md`, `docs/prompts/plan-feature.prompt.md`, and `docs/architecture/current-state.md` exited 0 and built exactly `index`, `ARCHITECTURE`, `architecture/current-state`, `references/project-overview`, `prompts/index` (plus `404`); `README.md` is excluded by `/*` before the README/index conflict rule runs, so no conflict warning; on a case-insensitive filesystem the `ARCHITECTURE/` and `architecture/` output directories merge (a property of the pre-existing docs layout, not of the baseline). Golden `mkdocs.yml` frozen as specified.
- **Refresh TechDocs: a `wf-techdocs` skill and collaboration prompt that generates the baseline and authors the published pages** (`1vmpz-enh refresh-techdocs-skill-and-collaboration-prompt`) — 5 ACs completed. Key decisions: Ship TechDocs authoring as a doc-gated `wf-techdocs` skill over a public **Refresh TechDocs** prompt seed, coordinated by the existing `technical-writer` specialist, before any auto-generation on setup/upgrade.
## Watchpoints

- Watchpoint: preserve existing catalog/TechDocs files byte-for-byte; the generated-by marker is the single ownership signal; partial = mixed marker state; nothing is persisted in a lock and nothing auto-runs at setup or upgrade in this wave.
- Watchpoint: publish only the explicit landing/architecture/reference/prompt-index allowlist; the command classifies its three destinations through the shared containment helper (siblings' presence rule plus the deliberate dangling/non-regular refusal) before any write.
- Watchpoint: the readiness precondition (three navigation targets present) keeps every touched tree lint-clean; the faithful fresh-tree audit test in `test_server_tools.WaveInstallAuditTests` must stay green (no trio, no broken links at Phase 1), and a negative test proves render/setup/upgrade paths write none of the three files.
- Watchpoint: install-log row 2.13.5 is a decimal-extension `(seed-178)` row with `artifact: catalog-info.yaml` (never renumber; an instruction row cannot carry a stat-checked artifact); seed-012 gains the mirroring `(seed-178)` step; `test_install_log_lib.FreshInstallContractParityTests` needs the deliberate regex widening for `2.13.5`. Ownership: 1vmpz owns the row, the seed-012 step and 2.15 topics, and the seed-160 pointer + re-render note; 1vj4d owns the command, seeds 011/040, and seed-160's no-auto-generation statement. Sequence 1vj4d first.
- Watchpoint: Requirement 8 of 1vj4d (MkDocs `exclude_docs` support, `pathspec` matcher semantics, one real build with `docs/README.md` present) is verified before the golden `mkdocs.yml` is frozen; record it in the Decision Log.
- Design history: readiness pass 1 (2026-08-18, receipt `review-policy-00fe65eb0b80749ed617`) approved a Phase-1 renderer family with a two-phase upgrade-lock record; an independent plan review found the Phase-1 landing page fails docs-lint until Phase 2 (blocking) and the lock design incomplete; revision 1 (marker + end-of-Phase-2 + upgrade) was reviewed by red-team and docs-contract seats (receipt `review-policy-60592954fa6281d051c8`); revision 2 (skill-first, this state) supersedes both. Earlier `## Review checkpoints` entries are history.
- Watchpoint (added 2026-08-18 by operator decision, 1vj4d Requirement 10 / AC-8): the `wf_techdocs_baseline` MCP tool wraps the same module function as the CLI (dry_run default, run writes under the publication lock), is write-tier and publication-registered, and is the agent-first entry; the CLI stays the install-row and no-MCP fallback; the tool needs an MCP reconnect to appear.
- Follow-up (not admitted): opt-in auto-generation of the baseline at setup/upgrade (`workflow-config` flag, upgrade summary record via the public classifier, first-delivery disclosure).
- No wave is OPEN; this wave activates with `wf_implement_wave` once readied.

## Review checkpoints

- **Prepare-phase Wave Council [prepare-council] — 2026-08-18: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, docs-contract-reviewer; rotating-seat: docs-contract-reviewer; strongest-challenge: the plan's install-log carrier did not do what it said, an `(instruction)` row cannot carry a stat-checked artifact (`Row.artifact_path` is `None` for instruction rows; the format contract and the shipped parity tests forbid the shape), so CHECK 2 would have given no protection; resolved before readiness by making row 2.13.5 a `(seed-178)` row owned by `1vmpz` together with the seed-012 step and the seed-160 pointer; strongest-alternative: keep an artifact-less instruction row and drop the CHECK 2 claim (weaker, rejected), and for the writer contract an audience invariant plus a link-boundary rule so the dogfood cannot degrade agent startup-order docs or ship links that `exclude_docs` removes from the built site (adopted))
- **Skill-first readiness pass (third design, receipt superseding `review-policy-60592954fa6281d051c8`)**: red-team primer (context council-red-1vj4e-v3, fresh, read-only, standard depth) verified the CLI dispatch pattern (thin `gpu_doctor.py`-style entry; `render_platform_surfaces` has no subparsers), the shared helpers, the doc-gated registry pattern and its tests, seed-100 registration, the row grammar (`_ROW_RE` parses `2.13.5`; parity regexes need the budgeted widening; instruction rows are never stat-checked), the golden `docs/index.md` lint behavior with and without targets (executed), the faithful fresh-tree audit test's independence from the change, packaging, and the golden `exclude_docs` under `pathspec.GitIgnoreSpec` (executed on file paths; MkDocs directory-walk semantics stay in Requirement 8); findings RT-1..RT-13, fix-before-ready RT-1 (row kind/ownership), RT-2 (audience invariant, link boundary), RT-3 (re-render after the seed-100 prompt backfill so the doc-gated skill exists on the upgrade that ships it), RT-8 (render-pass leftovers), applied; RT-4/5/9/10/11 folded as implement-time notes (absent-lane fallback; docs-contract check reported as a fact outside an open wave; typed result + thin entry; hand-enumerated CLI/registry tests; no raw script invocations in seeds); verdict PASS-with-conditions, conditions met. docs-contract-reviewer (context docs-contract-1vj4e-v3, fresh, read-only) answered both standing questions code-grounded (carriers complete once seed-050, the `AGENTS.md` § Skills paragraph, `platform-mapping.md` § Skills, the specialist catalog, seed 233 and CHANGELOG are named; no project policy in 1vj4d's seeds; 1vmpz's seed needed a genericity clause because Guru and docs-contract-reviewer are conditional in targets), findings DC-1..DC-12, fix-before-ready DC-1 (same row-kind defect, independently), DC-2 (missing carriers), DC-3 (genericity clause), DC-4 (leftovers), applied; DC-5/6/7/8/11/12 folded (seed 178 canonical checklist; seed-040 wording; skill first renders at row 2.9; AEG owner; seed renumbered from 241 to 178 per the 170-199 public-shortcut band; scratch-only build smoke); verdict PASS-with-conditions, conditions met. wave-council synthesis: no disagreement; both seats found the row-kind defect independently, which is the strongest confirmation the amendment was needed; every mechanism claim of the skill-first design holds against the tree; the two docs' shared-file ownership is now single-owner per line (1vmpz owns row 2.13.5, the seed-012 step, and the seed-160 pointer; 1vj4d owns the command, seeds 011/040, and seed-160's no-auto-generation statement); Requirement 8's external checks (MkDocs `exclude_docs` support, `pathspec` matcher, one real build with `docs/README.md` present) are the only assumptions left to implement time.
- **AC priority check** (2026-08-18, wave-coordinator): 1vj4d AC-1..4 required, AC-5..7 important, AC-8 required (added with the Requirement 10 MCP-tool amendment; rationale in the change doc's AC Priority table); 1vmpz AC-1..4 required (AC-4 raised from important on the 2026-08-18 readiness-lane pass: it is the command's only fresh-install reach), AC-5 important; every row carries a rationale; required rows name the mechanism (command, marker/partial rule, precondition and negative test, containment; seed/prompt, registry entry, dogfood proof); no `[~]` at admission; not-this-scope items live in the Out of scope lists (auto-generation, opt-in flag, MCP tool, new role).
- **product-owner**: N/A with rationale (framework install/authoring tooling; operator-facing acceptance is the dogfood in this repository and the shortcut's own report; single-operator project, operator hat).
- **Prepare-phase Wave Council [prepare-council] — 2026-08-18: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, docs-contract-reviewer; rotating-seat: docs-contract-reviewer; seat-agreement: unanimous; max-severity: medium (wording); delta-scoped fourth pass, receipt `review-policy-8640821f74fe2b121025` superseding `b6729426` and `ce122fc6`: three independent readiness lanes (code-reviewer `code-reviewer-ready-1vj4e`, qa-reviewer `qa-ready-1vj4e`, architecture-reviewer `architecture-readiness-1vj4e`) all APPROVED the skill-first plans and their fix-before-implement wording pins were folded into the change docs; the red-team seat (`council-red-1vj4e-v4-delta`) and the docs-contract seat (`docs-contract-1vj4e-v4-delta`) then verified the delta against the tree and the Requirement 8 scratch evidence (both re-ran the strict MkDocs build); strongest-challenge: the delta itself introduced the only inconsistency, the whole-trio marker removal (writer edits two root members that 1vmpz's boundary rule and skill reminder said it never touches), and the recording-boundary sentence named a `run` event as an evidence carrier although `run` records are empty cycle markers; both fixed before readiness (carve-out sentence in Req 2/Req 5; facts land in the lane's delivery-approval evidence or an executed finding); strongest-alternative: let the writer own only `docs/index.md` and accept a permanent mixed-trio warning in this repository (simpler, rejected), and record the dogfood check as a standalone executed finding (acceptable, both forms named); RT-D2..D7 and DC-V4-2..5 applied as wording (wave AC-priority line, `test_server_tools.py` in 1vmpz serialization points, exit-code 2 scope, mirror marker control, escaping symlink classified project-owned, warning-contract and task wording pointing at Requirement 6, ownership harmonization, `exclusive` as a new keyword))
- **Prepare-phase Wave Council [prepare-council] — 2026-08-18: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, docs-contract-reviewer; rotating-seat: docs-contract-reviewer; seat-agreement: unanimous; max-severity: broad (one plan-text gap, fixed); delta-scoped fifth pass on the Requirement 10 / AC-8 amendment (the `wf_techdocs_baseline` MCP tool, operator decision), receipt `review-policy-5f2233e99c538016133b` superseding `fa9da538`; seats `council-red-1vj4e-v5-mcp` and `docs-contract-1vj4e-v5-mcp` plus lanes code-reviewer (`code-reviewer-ready-1vj4e-mcp`), qa-reviewer (`qa-ready-1vj4e-mcp`), architecture-reviewer (`architecture-ready-1vj4e-mcp`), all fresh read-only, all APPROVE-with-editorial; strongest-challenge: the amendment superseded the same-day MCP-tool deferral without retiring that deferral's guard, the shipped static single-caller test `test_only_the_thin_entry_calls_the_command_function` fails the moment `server_impl.py` names `render_techdocs_baseline(` (executed mutant), so the plan now names the deliberate widening (allowlist exactly render_agent_surfaces.py, techdocs_baseline.py, server_impl.py); also the "nothing written" claim was wider than the module's single RuntimeError channel (split into a preflight `TechdocsDestinationRefused` and `techdocs_write_failed`), and the carrier list omitted seed 050's "in full" write-tier sentence and seed 100's rule (both added); strongest-alternative: implement dry_run in server_impl from the helpers without touching the module signature (rejected: a second implementer of the classification order) and shell out to the CLI (rejected: second parsing layer, no dry_run, defeats the shared-function proof); every other finding folded as wording (public-module import binding, `status: dry_run`, `invalid_arguments` mode, lint attach mode, docs-index refresh, index-row placement in the existing Refresh TechDocs row, mcp-tool-surface signature-form entry, four architecture docs by row, handoff sentence))
- **Prepare wave — readiness verdict (2026-08-18)**: READY on the skill-first design. Both admitted change docs are wave-owned and complete; required lanes from the receipt: code-reviewer, qa-reviewer, architecture-reviewer, docs-contract-reviewer (recorded in Participants); `wave-council-readiness` recorded as a typed approval; wave stays `planned` (readied); opening is a separate step (`wf_implement_wave`).

- **Prepare-phase Wave Council [prepare-council] — 2026-08-18: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, docs-contract-reviewer; rotating-seat: docs-contract-reviewer; seat-agreement: unanimous; max-severity: none; strongest-challenge: multi-file writes are not atomic, so a sibling failure can leave a mixed tree; strongest-alternative: journal or stage the full trio and refuse partial publication, rejected because it adds rollback and ownership complexity while the pending-before-write record durably preserves truthful incomplete state; material-disagreements: none; receipt: review-policy-00fe65eb0b80749ed617; plan-sha256: 8c615106f962d78a059157c9eeea12a1bd68452a6c5f03dded02197736ce34a5)
  - Red-team evidence (`prepare-red-team-1vj4e-00fe65eb-20260818-twophase-e84b7a`): PASS. Standard primer used adversarial, constructive, and simplicity stances. The strongest challenge was non-atomic multi-file writes; all pre/post-write failures must retain pending evidence, and retry/all-present cleanup must not infer success. The plan answers both. A separate journal/staging flow or refusal to backfill partial trios was considered but adds disproportionate recovery/ownership complexity or sacrifices the feature.
  - Docs-contract evidence (`ctx-1vj4e-docs-contract-final-8c615106-6b4e29d7`): PASS. The rotating seat challenged the default `engineering` owner and pending `generated_paths` semantics. The plan makes `engineering` an operator-verification placeholder, and mandatory status plus incomplete detail makes pending paths intended rather than completed output. Explicit owner input or a distinct `intended_paths` field is the strongest alternative but is not required for a truthful deterministic baseline.
  - Moderator evidence (`prepare-wave-council-1vj4e-00fe65eb-20260818-twophase-4f92c1`): PASS. Independent current-plan and code-seam reread plus fresh probes confirmed pending-before-write/committed-after-durability ordering, status-aware retry/cleanup, environment restoration, and durable lock fallback; a reversed-ordering mutant was detected. No unimplemented generation or Backstage publication behavior was claimed.

- **Prepare-phase Wave Council [prepare-council] — 2026-08-18: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, docs-contract-reviewer; rotating-seat: docs-contract-reviewer; seat-agreement: unanimous; max-severity: medium; delta-scoped pass on the one post-delivery change-doc edit that re-digested the receipt, `review-policy-9c2ca01826a8d87d4fe7` superseding `a58c7aa8`: `1vmpz` AC-3 marked `[x]` with an inline status note recording the accepted link-boundary exception and the ledger record carrying the dogfood evidence. Both seats verified the note against the tree with independent instruments and reproduced all four counts (62 survivor pages, 61 relative links, 0 broken, 1 exception at `codebase-map.md` line 259), confirmed `codebase-map.md` is clean in git and generator-owned, and confirmed `ev-approval-docs-contract-reviewer-6` exists and carries all five AC evidence items; strongest-challenge: both seats independently found the note's independence claim attributed to the wrong lane, since the qa delivery approval scopes itself to AC-1/2/4/5 while the lane that re-measured the audience invariant and the citations is architecture-reviewer, and the same wrong lane was repeated in the wave record, which is how one wrong clause becomes accepted fact; red-team additionally challenged the 'pre-existing exception' framing, since `mkdocs.yml` is untracked and the publication boundary that turns the pre-existing link into a 404 is authored by this wave; strongest-alternative: leave the AC clause absolute and reconcile it in prose, rejected because an auditor re-running evidence item (b) as literally written would fail, so the clause was narrowed in place to name the exception instead; on the marker, both seats rejected `[~]` as strictly more misleading than `[x]`, since `[~]` asserts the AC was intentionally not met as a whole and would erase six verified conjuncts, and the failure mode it guards against is silent deferral. All conditions applied: the attribution corrected in both places, the authorship framing and the href-versus-page slip fixed, the AC clause narrowed, the record's letter collision disambiguated, and the follow-up plan disclosed as drafted-but-unadmitted.)
- **Delivery review — 2026-08-18: PASS after two repair cycles** (coordinator: wave-coordinator; four required lanes in fresh independent contexts, three of them run twice (the qa lane's second pass is the one recorded as a delivery-phase context; its first-round participation is visible only as a source lane on DEL-1); `initial_delivery` run `run-initial-delivery`). Cycle 1 raised four findings: DEL-1 (blocking, stale `path:line` locators on the two dogfood-published pages, found independently by all four lanes and traced to this wave's own Requirement 10 amendment inserting 185 lines into `server_impl.py` after the pages were authored), DEL-2 (failure paths misreporting the tree and an `OSError` escaping both entries), DEL-3 (two shipped "CLI only" claims the in-wave MCP tool contradicts), DEL-4 (the generic seed-178 contract asking for line-range citations nothing keeps true, plus CLI-only phrasing in its Intent and Step-1 heading). The cycle-1 reverification found the DEL-1 repair incomplete and recorded it as DEL-6: the substring sweep never saw the tool-family table's 20 bare parenthesized numbers, and the two recomputed ranges were re-broken by exactly +33 when the sibling DEL-2 repair inserted code above them — the defect class reproducing inside its own repair, and a violation of the Step 3 rule DEL-4 had just written. The same round raised DEL-5 (`UnicodeDecodeError` is a `ValueError`, so it escaped the widened clause). Cycle 2 repaired both and the reverifying lanes closed DEL-4, DEL-5 and DEL-6, each with executed mutants; the lanes' remaining low findings were folded in as editorial repairs (the Step-3 coverage predicate and the rendered twin now pinned after two falsifications proved they were not; the caller guard widened from `glob` to `rglob`; the contract prose corrected to name `UnicodeDecodeError` and to scope the member-list claim to the write-failure path; the CLI's generic branch now reports member states; Path 3a now records that the trio is not written atomically; the repair record's overstated server-side refactor corrected). Verification: full suite 7349 tests across 63 files OK, docs-lint clean, and the published-page locators re-resolved last, after every other edit, at 0 unresolvable (52 enumerated by the architecture lane, corroborated by docs-contract; the qa lane's approval scopes itself to the other ACs and is not evidence for the locator count). Delivery approvals recorded for code-reviewer, qa-reviewer, architecture-reviewer and docs-contract-reviewer; the docs-contract approval carries the dogfood record that satisfies `1vmpz` AC-3. Two lessons worth carrying: a citation sweep must cover every surface form and run after all other edits in the session, and a verification instrument written by the author of the miss is not independent — the architecture lane showed the coordinator's own checker used overlap semantics that would have passed two of the three repaired sites.

## Finding Synthesis

<!-- wave:finding-synthesis begin -->
| Current finding | Disposition | Open block | Repair | Approval recheck |
| --- | --- | --- | --- | --- |
| DEL-1 | do_now | no | completed | — |
| DEL-2 | do_now | no | completed | — |
| DEL-3 | do_now | no | completed | — |
| DEL-4 | do_now | no | completed | — |
| DEL-5 | do_now | no | completed | — |
| DEL-6 | do_now | no | completed | — |

*Machine review state — 6 findings; current: do_now 6, maybe_later 0, dont_do_later 0, not_issue 0*
<!-- wave:finding-synthesis end -->

## Review Evidence

<!-- wave:review-status begin -->
| Signoff | State | Why | Next action |
| --- | --- | --- | --- |
| wave-council-readiness | approved | current executed approval follows every affected repair | none |
| code-reviewer | approved | current executed approval follows every affected repair | none |
| qa-reviewer | approved | current executed approval follows every affected repair | none |
| architecture-reviewer | approved | current executed approval follows every affected repair | none |
| docs-contract-reviewer | approved | current executed approval follows every affected repair | none |
| operator-signoff | approved | current executed approval follows every affected repair | none |
| release-reviewer | pending | no current executed approval | record approval evidence for release-reviewer |
<!-- wave:review-status end -->

- operator-signoff: <approved when operator confirms closure>

## Dependencies

- No external wave dependencies.

<!-- wave:context-efficiency begin -->
## Context Efficiency

Estimated token savings use phase-unique returned source versions and mapped workflow prompts, minus recorded request and response tokens. Saved model output or avoided tool loops count only through quality-equivalent paired evidence.

| Stage | Tool calls | Estimated token savings |
| --- | ---: | ---: |
| plan | 554 | 6,388,658 |
| implement | 282 | 2,675,347 |
| review | 716 | 11,596,570 |
| **Total** | **1,552** | **20,660,575** |

<!-- wave:context-efficiency-state {"generation":1384,"measurement_status":"healthy","pending":false,"schema_version":1,"stages":{"implement":{"calls":282,"content_source_credit":3117869,"derived_artifact_credit":4059,"direct_net":2675347,"estimated_tokens_saved":2675347,"matched_pair_residual":0,"paired_evaluation_count":0,"request_debit":18806,"response_debit":431907,"source_credit_count":194,"source_credit_drop_count":0,"structural_source_credit":0,"workflow_prompt_credit":4132},"plan":{"calls":554,"content_source_credit":7587284,"derived_artifact_credit":5316,"direct_net":6388658,"estimated_tokens_saved":6388658,"matched_pair_residual":0,"paired_evaluation_count":0,"request_debit":39949,"response_debit":1178449,"source_credit_count":316,"source_credit_drop_count":0,"structural_source_credit":0,"workflow_prompt_credit":14456},"review":{"calls":716,"content_source_credit":13754860,"derived_artifact_credit":2033,"direct_net":11596570,"estimated_tokens_saved":11596570,"matched_pair_residual":0,"paired_evaluation_count":0,"request_debit":57062,"response_debit":2104607,"source_credit_count":473,"source_credit_drop_count":0,"structural_source_credit":0,"workflow_prompt_credit":1346}},"store_instance_id":"f294635fbf24489a9a50af63451b2532","totals":{"calls":1552,"content_source_credit":24460013,"derived_artifact_credit":11408,"direct_net":20660575,"estimated_tokens_saved":20660575,"matched_pair_residual":0,"paired_evaluation_count":0,"request_debit":115817,"response_debit":3714963,"source_credit_count":983,"source_credit_drop_count":0,"structural_source_credit":0,"workflow_prompt_credit":19934},"wave_id":"1vj4e backstage-techdocs-baseline"} -->
<!-- wave:context-efficiency end -->

## Estimated Exploration Avoided

<!-- wave:exploration-avoided begin -->

This is a bounded estimate from exact-match memory advisories. It is not added to measured Context Efficiency.

| Advisory surfaces | Citations | Records credited | Estimated tokens avoided |
| ---: | ---: | ---: | ---: |
| 36 | 0 | 15 | 16,598,886 |

estimated: a surfaced (or cited) advisory does not prove a re-exploration was avoided; this is grounded in the measured cost of the original exploration, scaled by a bounded exact-match attribution, and is NEVER summed into the measured Context Efficiency token total.

<!-- wave:exploration-avoided-state {"cited_events":0,"credited_records":15,"estimated_exploration_avoided":16598886,"surfaced_events":36} -->
<!-- wave:exploration-avoided end -->
