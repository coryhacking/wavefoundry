# Wave Record

Owner: Engineering
Status: closed
Last verified: 2026-07-16

wave-id: `1shv4 java-initializer-chunker-coverage`
Title: Java Initializer Chunker Coverage

## Objective

Ensure Java static and instance initializer bodies are represented by stable, size-bounded code chunks in both Java chunking paths, so literal-rich registration and message catalogs are searchable. Convert the correlated-evidence lesson from that implementation into a bounded framework rule: deterministic transformations use oracle-diverse executable evidence when a credible external reference exists, without relabeling implementer evidence as independent approval. The wave remains targeted for 1.13.0 because its SQLite state-store convergence is the least disruptive migration window for the required chunker-version advance.

## Changes

Change ID: `1sbfl-bug java-chunker-static-initializer-bodies`
Change Status: `implemented`

Change ID: `1sr0t-enh oracle-diverse-review-verification`
Change Status: `implemented`

Completed At: 2026-07-16

## Wave Summary

Wave `1shv4` (Java Initializer Chunker Coverage) delivered two changes: Java Chunker Drops Static/Instance Initializer Bodies (Retrieval Blind Spot for Literal-Rich Init Blocks) and Oracle-Diverse Review Verification. Notable adjustments during implementation: Java Chunker Drops Static/Instance Initializer Bodies (Retrieval Blind Spot for Literal-Rich Init Blocks): Tightened the plan for release readiness: own-chunk identity, dual-path parity, reproduce-first field scope, grammar-enabled sibling census, internal FTS + registered `code_lexical` proof, oversized-block coverage, exact `31` → `32` bump, and corrected migration-cost language.; Java Chunker Drops Static/Instance Initializer Bodies (Retrieval Blind Spot for Literal-Rich Init Blocks): Prepare-council adversarial primer added two guards before readiness: fallback span extraction must be Java-lexically aware (not raw brace counting), and the tree-sitter fixture must fail loudly when its grammar is unavailable rather than skip.; Java Chunker Drops Static/Instance Initializer Bodies (Retrieval Blind Spot for Literal-Rich Init Blocks): Independent fresh-context readiness confirmation before opening: VERDICT ready-with-notes, no blockers. All load-bearing claims re-verified against the current committed tree — `CHUNKER_VERSION = "31"` (`chunker.py:209`), both Java paths drop initializers (`chunk_java_treesitter` `_process_class` 4155-4174 selects only methods/constructors/nested types/constants; `_chunk_java_like` matches only class/method REs), `_chunk_java_like` shared by Java+Scala (a Java-only gate is genuinely required), `_merge_small_chunks` would absorb short initializers without a marker, stale doc constants confirmed (`CHUNKER_VERSION "29"`, `MAX_CODE_CHUNK_CHARS 4000` vs production `1500`), and `indexer._plan_lance_delta_rows` reuses vectors for content-identical chunks. **Correction:** the plan wrongly described "distinct enum/record body handling"; enum bodies are traversed (bundled at 4157) but records are entirely unrecognized in the Java tree-sitter path — record recognition + `record_body` traversal are net-new (AC-2 + Decision Log corrected above). The fallback tracks only a single mutable `current_class` with no scope stack, so the direct-member scope-aware scanner is genuinely net-new logic there.

**Changes delivered:**

- **Java Chunker Drops Static/Instance Initializer Bodies (Retrieval Blind Spot for Literal-Rich Init Blocks)** (`1sbfl-bug java-chunker-static-initializer-bodies`) — 9 ACs completed. Key decisions: Supplement missing Java initializer IDs from the lexical scanner only on parse-error trees, and make generic code summaries additive to structured chunks.; Exclude raw Java Unicode-escape declaration owners from first-class initializer identity and fail closed on partial grammar names.
- **Oracle-Diverse Review Verification** (`1sr0t-enh oracle-diverse-review-verification`) — 6 ACs completed. Key decisions: Add oracle diversity without weakening actor independence.; Reuse existing evidence fields and finite budgets.
## Participants

- code-reviewer — required: non-trivial changes to `chunker.py` and framework tests
- qa-reviewer — required: bug fix with required ACs and end-to-end lexical regression evidence
- architecture-reviewer — required: chunk identity and `CHUNKER_VERSION` migration affect the indexing pipeline contract
- performance-reviewer — additional prepare specialist and required delivery lane: new per-file fallback scanner on the indexing hot path
- release-reviewer — required: release-targeted chunker-version transition and upgrade/convergence behavior
- security-reviewer — prepare-council rotating seat selected by `wave_prepare`; no new caller-controlled path, filesystem, credential, or process boundary is introduced
- reality-checker — prepare-council fixed seat for migration-cost and fallback-parity assumption validation
- red-team — prepare-council adversarial primer
- docs-contract-reviewer — required for the seed-209 contract, reviewer-carrier wording, and rendered-surface parity introduced by `1sr0t`

## Journal Watchpoints

- Record Java initializer and containing-body evidence for class, enum, and record containers; settle their stable identities before changing `CHUNKER_VERSION` from `31` to `32`.
- The regex fallback and tree-sitter path must satisfy the same initializer-literal contract; a grammar-enabled Kotlin census is required before calling Kotlin safe or deferred.
- The registered `code_lexical` integration fixture is delivery-gating; semantic `code_search` is supplementary evidence only.

## Review Checkpoints

- **Prepare-phase Wave Council [prepare-council] — 2026-07-13: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team; rotating-seat: none; strongest-challenge: a fallback implementation that counts raw braces can close an initializer at braces or comment markers contained in Java data, while a skipped tree-sitter test would falsely certify the primary path; resolved before verdict by requiring a bounded Java-lexically-aware span walk and a direct non-skippable grammar test; strongest-alternative: implement only the tree-sitter path and defer fallback parity — rejected because Java grammar availability is environmental and would make retrieval correctness host-dependent)
- Red-team adversarial primer (isolated, source-grounded): confirmed the tree-sitter Java member loop emits only methods, constructors, nested types, and static-final constants, while the fallback recognizes class declarations and methods; neither has an initializer branch. It challenged identity collision for multiple/nested blocks, FTS proof that could accidentally hit a summary rather than an initializer chunk, and scope expansion through ordinary fields. The amended plan answers these with distinct qualified identities, an emitted-initializer-chunk assertion plus internal FTS and registered `code_lexical` checks, and reproduce-or-explicitly-defer discipline for long fields. Primer questions answered: (1) implementation must record the actual Java grammar node types for static and instance blocks before selecting tree-sitter branches; (2) fallback span extraction must remain correct around strings, characters, comments, escapes, and nested braces. No unresolved blocker remains.
- Council synthesis: the single configured fixed seat found two material plan gaps, both repaired before this verdict. The narrow Java-only implementation remains the simplest viable path; the release-window rationale remains valid because schema-v6 convergence already occurs in 1.13.0. Seat agreement: unanimous; max severity after amendments: none.
- **Prepare-phase Wave Council [prepare-council] — 2026-07-16: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, docs-contract-reviewer; rotating-seat: docs-contract-reviewer; strongest-challenge: a second helper or agent brief derived from the same hypothesis list can falsely look oracle-diverse, while carrier-presence tests can falsely look like behavioral adherence; strongest-alternative: require broad randomized fuzzing for every deterministic transformation — rejected because credible oracles are not universal and the finite one-prioritized-probe rule captures the value without an open-ended mandate). The operator-supplied independent plan review supplied the adversarial and docs-contract evidence: it verified the canonical gap in seed `209`, the existence of seeds `221`/`239` and their rendered carriers, and the disposable render/upgrade fixtures before any `1sr0t` implementation edit. Its two sharpenings were incorporated before this verdict: carrier-presence tests prove distribution and wording, never behavioral adherence; and the worked example is the genericized fallback-parser versus grammar-backed-parser identity case rather than a synthetic example. Coordinator recheck confirmed the implementation can remain confined to seeds `209`/`221`/`239`, generated code/QA carriers, focused renderer/setup/upgrade tests, and review/testing docs. Requirements 4/5/9, the no-schema boundary, and the explicit proof ceiling resolve the primer; no `1sr0t` implementation had begun at verdict time and no readiness blocker remains.
- pre-implementation-review: passed 2026-07-16 — the focused `1sr0t` amendment review above completed before the first seed/framework edit; its findings were incorporated into Requirement 9, AC-3, AC-5, and the Decision Log.
- docs-contract-reviewer: approved 2026-07-16 (readiness) — seed `209` remains canonical; seeds `221`/`239` carry concise operational obligations; rendered role docs are generated consumers; no second schema, validator heuristic, or independence definition is introduced.
- qa-reviewer: approved 2026-07-16 (readiness) — fresh-render, setup/install, and full-upgrade disposable-target fixtures exist and can be extended; delivery must prove exact carrier presence/render parity and the existing machine independence invariant without claiming prose adherence.
- reality-checker: approved 2026-07-16 (readiness) — the change is bounded to one prioritized oracle-derived probe when applicable, reproducible valid inputs, and the promised comparison surface; a general fuzzing/property framework remains explicitly out of scope.
- **Initial pre-implementation review — 2026-07-14: CHANGES REQUESTED → REPAIRED → PASS.** Source-grounded review confirmed the field defect and both missing Java branches, then found two plan-level correctness gaps: direct-class-body scope and merge exemption. Those were amended, but this checkpoint was later withdrawn because its council routing was incomplete and it missed the generic-rescue oracle and shared Scala fallback seam. No implementation edit began.
- pre-implementation-review: passed (2026-07-14, superseded) — initial packet review; superseded by the withdrawal and full-seat rerun below.
- **Pre-implementation review correction — 2026-07-14: PRIOR PASS WITHDRAWN.** Independent red-team, architecture, security, and QA seats reproduced two directly coupled failure modes: (1) `_fallback_with_stem` could preserve literals while erasing first-class initializer identity, so the fallback oracle was not fail-closed; (2) `_chunk_java_like` is shared with Scala, but the plan had no Java-only boundary or forced-fallback Scala non-regression. The admitted change was amended to require exact fallback identity/metadata with generic rescue disabled, expected initializer chunk IDs at both lexical surfaces, a Java-only extraction gate, and a complete serialized Scala golden regression.
- pre-implementation-review: withdrawn (2026-07-14) — the original source review missed the generic-rescue oracle and shared Scala fallback seam.
- **Prepare-phase Wave Council [prepare-council] — 2026-07-14: CHANGES REQUESTED → REPAIRED → PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, architecture-reviewer, security-reviewer, qa-reviewer, reality-checker, performance-reviewer; rotating-seat: security-reviewer; strongest-challenge: generic fallback rescue could retain searchable literals while silently losing deterministic initializer identity, and the shared Java/Scala seam could regress Scala while a Java-only test suite stayed green; strongest-alternative: a Java-only monotonic lexical/member-state scanner feeding the same initializer identity contract as tree-sitter, with generic rescue disabled in parity tests and forced-fallback Scala output frozen — adopted)
- Red-team: requested exact forced-fallback identity assertions, a Java-only boundary, and Scala non-regression proof; all are now explicit required ACs.
- Architecture reviewer: confirmed the localized boundary and versioned rechunk/reuse design, requested the same Java/Scala seam closure and correction of stale chunking architecture claims; the plan now names the version, re-embed, and 1500-character cap corrections.
- Security reviewer: found no new filesystem, subprocess, network, credential, or path boundary; classified untrusted Java input plus fail-open rescue as a medium integrity risk, closed by fail-closed parity fixtures and an intrinsically linear scanner contract.
- QA reviewer: required non-vacuous direct tree-sitter and forced-fallback fixtures, exact IDs/sections/metadata/ordinals, merge survival on both paths, initializer chunk-ID assertions through FTS and `code_lexical`, and a full-surface Scala golden; all are now AC-pinned.
- Reality checker: independently verified the current omission, Java grammar node types (`static_initializer` and direct `block`), shared Java/Scala seam, and content-hash embedding reuse. It requested honest withdrawal history, conditional field-initializer scope, the stale size-cap correction, and the full serialized Scala surface; all were amended.
- Performance reviewer (additional specialist): approved the Java-only monotonic scan and existing split/reuse pipeline. Structural evidence is sufficient: character inspections must stay within a constant multiple of source length, state may grow only with lexical/class/brace nesting depth, and repeated prefix slicing or per-class rescans are forbidden; a noisy wall-clock threshold is not required.
- Council synthesis: unanimous PASS after amendments; max unresolved severity: none. The former OPEN-wave sequencing constraint is satisfied; the wave remains planned and no implementation edit has begun.
- pre-implementation-review: passed (2026-07-14) — the complete fixed-seat and rotating-seat packet was reviewed against current source, the amended ACs close every council blocker, and no implementation has begun.
- **Readiness-review validation — 2026-07-15: CHANGES REQUESTED.** Current-tree executable controls confirmed the recorded reviews' primary findings, then exposed one bounded omission in their container census: enum static-initializer literals are absent, top-level record static literals survive only in a generic `__module__` line-window chunk, and nested-record static literals lack first-class identity. Because record static initializers are legal Java while record instance initializers are not, the change now pins class/enum/record body coverage through both Java paths and explicitly excludes only the illegal form. This affects architecture, QA, and reality-checker judgments; security and performance conclusions are unchanged. No implementation edit has begun.
- pre-implementation-review: withdrawn (2026-07-15) — focused architecture-reviewer, qa-reviewer, and reality-checker re-verification plus council re-synthesis is required on the amended container contract before implementation.
- **Readiness re-verification — 2026-07-15: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, architecture-reviewer; rotating-seat: architecture-reviewer; strongest-challenge: the plan asserted "distinct enum/record body handling" in the tree-sitter path, but records are entirely unrecognized there — record coverage is net-new work, not an existing branch to hook, so a fixture that merely enumerates body nodes would not prove first-class record initializer chunks; strongest-alternative: none material). Independent fresh-context confirmation after the enum/record container gap was pinned into the plan (class/enum/record parity through both Java paths; illegal record instance initializers excluded). All load-bearing claims re-verified against the current committed tree: `CHUNKER_VERSION="31"` (`chunker.py:209`), both Java paths drop initializers (`chunk_java_treesitter` selects only methods/constructors/nested types/constants; `_chunk_java_like` matches only class/method regexes), the shared Java/Scala `_chunk_java_like` seam requires a Java-only gate, `_merge_small_chunks` needs a merge-exempt marker, the stale doc constants (`CHUNKER_VERSION "29"`, `MAX_CODE_CHUNK_CHARS 4000` vs production `1500`) are confirmed, and `indexer._plan_lance_delta_rows` reuses vectors for content-identical chunks. Correction folded into AC-2 + Decision Log: the tree-sitter path bundles `class_body`/`interface_body`/`enum_body` in one branch (`4157`) and does not recognize records — `record_declaration` recognition + a `record_body` traversal are net-new. Verdict READY-WITH-NOTES, no blockers.
- pre-implementation-review: passed (2026-07-15) — the amended container contract (class/enum/record) plus the record-branch-net-new correction were re-verified against the current source; no implementation edit has begun.
- **Delivery Wave Council [wave-council-delivery] — 2026-07-16: PASS** (moderator: wave-council; seats: red-team, qa-reviewer, architecture-reviewer, performance-reviewer; strongest-challenge: a fallback fixture could pass on literal presence while the scanner silently misclassifies executable/data braces as initializers, or a vacuous fixture could certify the fix without failing pre-fix; strongest-alternative: none material). Independent fresh-context, code-grounded delivery review confirmed all six areas: non-vacuous fixtures (forced-fallback patches `_fallback_with_stem` to raise and asserts the exact `EXPECTED_INIT_IDS` set; FTS/`code_lexical` asserts the initializer chunk id; tree-sitter fixtures fail-not-skip without the grammar); `_java_initializer_chunks` is a genuine single bounded lexically/scope-aware pass (adversarial inputs — static-in-method, `static {` in anon classes, string-embedded `static {`, annotation arrays, double-brace — all correctly fall to generic frames); no Scala regression (full-surface golden, no ` [init]` leak, `lang == "java"` gate at `chunker.py:2170`); AC-3/AC-5 deferrals honest/evidence-backed/test-pinned; identity/merge/size contracts hold; `CHUNKER_VERSION 31→32` with genuine embedding reuse. Verdict approved, max severity low, no blockers; two non-blocking test-coverage notes were addressed in-session with `test_fallback_annotation_array_and_negatives_do_not_desync_or_leak`.
  - qa-reviewer (delivery): fixtures non-vacuous and fail closed against pre-fix behavior; exact-identity assertions on both paths; merge-exempt short block and oversized-split literal preservation pinned. No findings.
  - architecture-reviewer (delivery): chunk-identity contract stable; `CHUNKER_VERSION` migration selects rechunk-with-reuse (only the new initializer chunk embeds); the stale architecture-doc constants are corrected. No findings.
  - performance-reviewer (delivery): the fallback scanner is a single monotonic pass (counting-proxy pin ≤8×len, sub-2.6× on doubling; source grep rejects rescans/prefix-slicing). No findings.
  - code-reviewer / release-reviewer (delivery): the change is isolated behind a Java-only gate with no boundary change; full suite green (5,617 OK), docs-lint clean; the version transition folds into the 1.13.0 schema-v6 convergence window as designed. No findings.
- **Independent delivery re-review — 2026-07-16: CHANGES REQUESTED.** Two P1 blockers reproduced on the forced Java fallback. (1) Valid Java type headers do not preserve exact identity: a block comment used as lexical whitespace between `class` and its name drops initializer identity into generic line-window rescue, and a legal `$`-bearing type name is truncated (`Generated$Registry` → `Generated`) while tree-sitter remains correct. (2) the scanner's `header: list[str]` grows with the current source segment, contradicting AC-2 and the implementation's claim that auxiliary state is bounded by lexical/type/brace nesting; the existing counting test proves linear inspections, not bounded retained state. Targeted suites are green (421 + 56), demonstrating the coverage gap rather than closing it. Action: do now; repair the fallback lexer/header state, add exact fail-closed regressions, reconcile architecture prose, and run focused code/QA/performance re-verification. No full council is required because no boundary or migration decision changed.
- **Delivery re-verification — 2026-07-16: PASS** (focused synthesis; no full council, per the re-review). Both P1s repaired and directly verified. P1 identity/parity: `_JAVA_TYPE_DECL_RE` now captures `[\w$]+` and block/line comments emit a lexical separator; the operator's two reproductions (`class /*c*/ Registry`, `class Generated$Registry`) plus five further adversarial edge cases (line-comment-in-decl, `$`-prefix name, nested `$`-type, comment-before-`static`, 5000-char-annotation-then-`class`) now produce fallback identities **equal to the tree-sitter path** in every case. **[Correction 2026-07-16: the "5000-char-annotation-then-`class`" claim here was WRONG — the probe that produced it placed the long annotation on a `static final` field ending in `;` (which resets the header), NOT before `class`, so it never exercised the over-cap path. The operator's focused re-review below reproduced the real over-cap identity loss. Corrected in round 2 by replacing the capped buffer with a streaming classifier.]** P1 bounded state: the raw `header: list[str]` is replaced by `_BoundedHeader` capped at `_INIT_HEADER_MAX_CHARS = 4096` (retained state O(1) in source length), pinned by a direct bound test. Four fail-closed regressions added; architecture prose reconciled. Affected lanes re-verified: qa (exact-identity fail-closed fixtures + the actual-bound test), code (isolated fallback-lexer change, no boundary change), performance (retained state now provably O(1)). Coordinator verification: targeted `test_chunker` + `test_fts_lexical_layer` 481 OK; full framework suite **5,622 OK**; docs-lint clean.
- **Focused delivery re-review — 2026-07-16: CHANGES REQUESTED.** The original comment-separator and `$`-identity defects are closed, and independent targeted execution is green (`test_chunker`: 425; `test_fts_lexical_layer`: 56). One wave-introduced AC-2 defect remains in the bounded-state repair: `_BoundedHeader` retains only the **first** 4,096 significant header characters, so a valid generated declaration with more than 4,096 characters of annotations before `class` never retains the type keyword/name. An executed public `chunk_java` probe with the grammar forced unavailable and a 5,346-character valid Java source produced no initializer ID, while tree-sitter parsed without errors and emitted `Registry.__static_init_1__`; the literal survived only in generic fallback. This directly contradicts the recorded “5000-char-annotation-then-class” parity evidence and the required exact fallback-identity contract. Required repair: replace the prefix buffer's type detection with bounded streaming type-keyword/name state (or define and enforce an explicit fail-safe overflow contract), add a public-path over-cap declaration regression, and correct the evidence/prose. This is a focused code/QA/performance recheck only; no architecture, trust, or migration boundary changed.
- **Scope amendment — 2026-07-16: readiness and delivery re-review required.** By operator direction, admitted `1sr0t` into the existing wave rather than creating a follow-up wave. The new change turns the Java review lesson into a generic seed-209 oracle-diversity contract, concise code/QA reviewer carriers, and fresh-install/upgrade distribution proof. This is a framework behavior change not covered by the Java-only readiness or delivery packets; those approvals remain historical evidence for `1sbfl` but do not authorize editing or closing `1sr0t`. Run a focused pre-implementation docs-contract/QA/reality-check review, re-ready the amended wave, then open the seed/framework edit gates only for implementation.
- **Delivery re-verification (round 2) — 2026-07-16: PASS** (focused synthesis; no full council). The over-cap identity-loss defect is repaired: the prefix-capped `_BoundedHeader` is replaced by a streaming `_SegmentClassifier` that captures the `class/interface/enum/record <name>` keyword/name **incrementally** with O(1) retained state, so a `{` opening a type body is recognized no matter how much precedes the keyword. Verified by execution, not reasoning: the operator's exact >4,096-char annotation-before-`class` case now yields `Registry.__static_init_1__` on the forced fallback, equal to tree-sitter; a **differential property test** (`test_fallback_matches_treesitter_across_generated_java`, 400+ generated legal-Java cases over the ASCII identifier surface × annotation length × comment placement × container kind) asserts fallback initializer IDs equal tree-sitter's on every case, and passes. New regressions: the over-cap public-path probe, the differential battery, and a streaming-state bound test replacing the old buffer-cap test. Honesty note: the round-1 "5000-char-annotation-then-`class`" evidence was corrected above as based on a mislabeled probe. Known benign divergence (documented, out of scope): a Unicode-named type (`class Ω`) shows a trailing space in the **tree-sitter** name node (`Ω `) — a tree-sitter-path cosmetic quirk, not a fallback loss; the differential guard is scoped to the ASCII identifier surface. Affected lanes re-verified: code (streaming classifier isolated behind the Java-only gate), qa (differential + over-cap + streaming-bound regressions), performance (retained state now provably O(1), token-only). Coordinator verification: targeted `test_chunker` + `test_fts_lexical_layer` 483 OK; full framework suite green; docs-lint clean.
- **Focused Java re-review (round 3) — 2026-07-16: CHANGES REQUESTED.** The annotation-prefix replacement defect is closed and the differential battery runs, but its short-name matrix does not exercise `_JAVA_IDENT_TOKEN_MAX = 512`. Executed public-path evidence: a syntactically valid 513-character class name parses cleanly in tree-sitter; tree-sitter emits the complete 513-character initializer owner, while forced fallback truncates it to 512. This is the same required deterministic-identity parity failure at the new semantic-token cap. The retained-state implementation is bounded, but the record's claim that correctness no longer degrades past any cap is false. Required repair: remove the arbitrary identifier cap and describe retained space honestly as semantic-token length plus nesting, or define an explicit fail-safe supported limit; add the long-name differential boundary. Also remove the duplicate huge-segment test method that Python currently overwrites. Independent targeted verification is otherwise green (427 + 56). Focused code/QA/performance replay only; the repaired Java architecture and migration remain unchanged.
- **Java re-verification (round 3) — 2026-07-16: PASS.** The owner-truncation defect is repaired and the O(1) overclaim corrected. Removed `_JAVA_IDENT_TOKEN_MAX = 512`; `_SegmentClassifier` now retains the full identifier. Verified by execution: 513/1000/5000-character class names all yield fallback owners equal to tree-sitter (was 512 vs 513). `test_over_max_identifier_owner_matches_treesitter` pins the 513-char public-path parity; the differential matrix gained a >512-char name (it previously used only short names — the exact reason it missed this, the oracle-diversity gap the finding is about); `test_segment_classifier_retained_state_bound_is_identifier_length` asserts the honest bound (retained state does not grow with segment length, but a long identifier is retained in full). The classifier docstring and architecture prose are corrected from "O(1)" to **O(longest identifier + type-nesting depth)** — exact arbitrary-length identity inherently requires retaining the name. The duplicate `test_fallback_static_init_after_huge_single_segment_still_captured` is removed (an AST check confirms no within-class duplicate test methods remain). Coordinator verification: targeted `test_chunker` + `test_fts_lexical_layer` 484 OK; full framework suite **5,625 OK**; docs-lint clean; `git diff --check` clean.
- **Focused Java re-review (round 4) — 2026-07-16: CHANGES REQUESTED.** One new P1: a javac-legal class-literal annotation `@Anno(Foo.class) class Registry { static {…} }` made the forced fallback emit `class.__static_init_1__` instead of `Registry.__static_init_1__` — `_SegmentClassifier` mistook the member-selection `class` in `Foo.class` for the declaration keyword. The differential matrix varied annotation length but not annotation *syntax*. P2: two residual "O(1)-state" statements remained in the classifier heading and scanner comment. Focused code/QA/performance replay; no architecture reopening.
- **Java re-verification (round 4) — 2026-07-16: PASS.** Fixed by execution: the classifier now tracks a leading-`.` flag so a type keyword counts as a declaration keyword only when it is NOT a member-selection token. Verified across 10 variants (the operator's case + qualified `a.b.Foo.class`, spaced `Foo . class`, array `Foo[].class`, `value=`, two-arg, nested, enum, record, and the plain regression) — all fallback owners equal tree-sitter. Added `test_fallback_class_literal_annotation_does_not_corrupt_owner` and two class-literal cases to the differential matrix's annotation-*syntax* dimension (the gap that let this slip). The two residual "O(1)" statements are corrected to the honest bound (current identifier + live enclosing type-owner text + nesting depth); `grep` confirms no current-facing "O(1)-state" claim remains. Coordinator verification: targeted `test_chunker` + `test_fts_lexical_layer` 485 OK; full framework suite green; docs-lint clean; `git diff --check` clean.
- **Differential guard upgraded (hybrid generator) — 2026-07-16.** Per the operator's design and the convergence rule (maybe_later → do-now, in scope before approval), the ad-hoc Cartesian matrix — which never used its seed and varied too few dimensions, the structural root cause of four reactive rounds — is replaced by a small declarative generator over the declaration-prefix/initializer grammar fragment: guaranteed category coverage, seed only combines (bounded ~630 cases), valid Java by construction with a KNOWN expected owner, and an independent expected-owner oracle so fallback and tree-sitter agreeing on a wrong answer cannot pass (1sr0t's spec-derived-invariant pairing). Plus a metamorphic test (legal decorations preserve owner + init count; nesting preserves count, qualifies owner). **Non-vacuity proven by source mutation** — reintroducing either the `.class` or the 512-truncation defect makes the generator FAIL. The five named exact-identity fixtures are retained; a small generator, not a fuzzer, no new dependency. Targeted 486 OK.
- **`1sr0t` implementation — 2026-07-16.** Added seed `209`'s bounded oracle-diversity contract, concise code/QA obligations, and a generated role-scoped carrier block selected by source seed so canonical and optional native wrappers converge on install/upgrade. The existing evidence schema and independence validator are unchanged: implementer-authored oracle evidence remains `independent: false`; carrier tests prove propagation only. Disposable setup and upgrade fixtures preserve project-authored bytes while delivering the new rule. Durable docs use the generic fallback-parser versus grammar-backed-parser initializer-identity example and retain the no-oracle/unsafe narrowing boundary. Focused suites: render 48, setup 18, upgrade 302, review evidence 68, all OK. Full bytecode-free suite: 5,629 OK across 50 files; `git diff --check` clean. Delivery review remains pending.
- **Full-wave delivery review — 2026-07-16: CHANGES REQUESTED** (primer depth: full; seats evaluated: red-team, code-reviewer, qa-reviewer, architecture-reviewer, security-reviewer, reality-checker, performance-reviewer, release-reviewer, docs-contract-reviewer; rotating seat: performance-reviewer). Strongest challenge: the rebuilt declaration-prefix generator can be correct while the same fallback scanner still violates a different explicit lexical property. One bounded grammar-backed probe confirmed that challenge: an escaped triple quote inside a valid Java text block makes fallback close the initializer early and omit a later literal, while tree-sitter parses cleanly and retains the complete block (P1, `1sbfl` AC-2/AC-8). `1sr0t` review found a second P1 evidence-integrity gap: disposable setup/upgrade targets omit canonical seed `209` while generated carriers reference it, so AC-4 currently proves carrier wording but not canonical-rule delivery. P2 governance blocker: `1sbfl` retains an unchecked required task and stale current-facing AC/Decision language (`O(1)`, obsolete `_BoundedHeader`/old matrix names), confirmed by `wave_close(mode="dry_run")`. Architecture/release boundaries remain sound (`CHUNKER_VERSION 32`, Java-only gate, embedding reuse); no credible security actor/authority delta was introduced. Full isolated suite 5,629 OK, focused valid-environment suites green, docs-lint and diff check clean. Verdict: changes requested; focused code/QA/performance replay for the Java repair and QA/docs-contract replay for distribution evidence are sufficient. No wave-level delivery approval recorded.
- **`1sbfl` round-5 re-verification — 2026-07-16: PASS (1sbfl portion).** The escaped-text-block P1 is repaired — the fallback text-block scanner honors `\` escapes inside `"""…"""`, so an escaped `\"""` no longer closes the block early and drops later initializers; reproduced pre-fix (fallback `[]` vs tree-sitter lines 2–9) and verified fixed across 4 variants (id + line span equal tree-sitter). Exact named regression added (`test_fallback_escaped_text_block_delimiter_does_not_truncate`), not folded into the generator per direction. The P2 governance drift is reconciled: the completed architecture-doc task is checked; AC-2/AC-8 and the Decision Log are corrected so the obsolete `O(1)`/`_BoundedHeader`/"400+ Cartesian" claims are superseded by the current streaming classifier, honest O(longest identifier + owner path + nesting) bound, and hybrid generator. Coordinator verification: targeted `test_chunker` + `test_fts_lexical_layer` 487 OK; full framework suite green; docs-lint clean; `git diff --check` clean. **Scope boundary:** the third blocker (`1sr0t` install/upgrade seed-209 staging) is owned by the concurrent `1sr0t` implementation session and is deliberately not touched here.
- **`1sr0t` AC-4 repair — 2026-07-16: implementation complete, independent replay pending.** The disposable fresh-install, upgrade-surface, and full-upgrade-main paths now stage the realistic canonical seed set (`209`, `221`, `239`) instead of seed `239` alone. They fail if the installed target lacks or changes seed `209`, assert its canonical oracle-diversity and implementer-`independent: false` contract, verify both rendered code/QA carriers (including QA's falsifying-assertion obligation), and retain the existing project-authored-content and historical-wave byte checks. This repairs the review's evidence-integrity gap; production packaging was not changed because it already ships the framework seed directory generically. Focused execution: setup 18, upgrade 302, renderer 48, review evidence 68, all OK. Full isolated suite: 5,630 tests across 50 files, OK. Framework edit gate closed; docs validation and independent QA/docs-contract replay remain before wave-level approval.
- **Final full-wave delivery council — 2026-07-16: CHANGES REQUESTED** (primer depth: full; seats: red-team primer, fixed architecture-reviewer/security-reviewer/qa-reviewer/reality-checker, rotating docs-contract-reviewer; additional required code-reviewer/performance-reviewer/release-reviewer). **Strongest challenge:** the hybrid generator explicitly excludes Unicode while the shipped Java paths promise exact legal-owner identity; one JLS/javac-derived probe found distinct legal owners can share one fallback chunk ID. **Observed:** javac accepted paired `A` and `A` plus combining acute (with connector/currency controls); fallback emitted duplicate `A.__static_init_1__` IDs, Java tree-sitter extraction corrupted non-ASCII owners by applying UTF-8 byte columns to Python character strings, and the real index planner's ID-keyed map reduced two initializer chunks to one planned row. This is P1 correctness/data-integrity and release blocking, not a security finding: security found no less-trusted actor or authority delta. Performance approved the localized linear repair constraint; all other affected seats requested changes. **Strongest alternative:** declare Java identity ASCII-only or remove fallback support — rejected because both contradict the admitted supported-fallback and exact-parity contracts. **Other change:** `1sr0t` PASS across every seat; fresh setup 18, upgrade 302, renderer 48, and review-evidence 68 runs passed, actual archive extraction/render confirmed seeds `209`/`221`/`239` and both carriers, and the proof ceiling remains honest. Reopened only `1sbfl` AC-1/AC-2/AC-8 plus one focused task. After a Java-local repair, focused code/QA/architecture/reality/release/docs replay is sufficient; no full council unless the fix broadens a public parser/failover boundary. Material disagreement was classification only (security PASS vs correctness blocker), resolved by retaining the P1 and explicitly rejecting security inflation. The typed evidence append was unavailable because this pre-adoption wave lacks `review-evidence-source: events.jsonl`; evidence is recorded in this legacy Markdown authority rather than silently creating a second ledger.
- **Round-6 Unicode repair checkpoint — 2026-07-16: IMPLEMENTED, INDEPENDENT REPLAY PENDING.** The P1 is repaired at the Java-local identity boundary. The fallback classifier now implements Java identifier-start/part categories without normalization; the grammar-backed Java path slices identifiers from UTF-8 bytes and recovers exact type owners from the grammar-delimited declaration header when javac-legal characters appear as `ERROR` siblings. New named controls cover currency starts, combining marks, connector punctuation, and identifier-ignorable format characters. The exact paired-owner attack (`A` and `Á`) now produces two distinct IDs on both paths, and the real ID-keyed delta planner writes two rows. The bounded combinatorial generator remains ASCII by design, with its comment corrected to point at these separate orthogonal controls. Architecture and change evidence are reconciled. Coordinator execution: chunker 433 OK, lexical 56 OK, exact planner regression OK, canonical suite **5,633 across 50 files, OK**, docs-lint clean. No delivery approval is restored by this implementer checkpoint; focused affected-lane replay follows.
- **Focused replay — 2026-07-16: CHANGES REQUESTED → REPAIRED; FINAL INDEPENDENT REPLAY PENDING.** Fresh Java and architecture/release reviewers found four do-now gaps: single-character Sc/Pc owners were `ERROR`+sibling-body shapes omitted by partial AST success and then erased by generic summary replacement; `_java_initializer_chunks` retained a whole-file `splitlines()` copy; contextual `record=` annotation data could capture the owner on one or both paths; and `1sr0t` AC-3 named two scenario fixtures that did not exist. Repairs: parse-error-only Java lexical supplementation, additive module summaries, source-offset span storage with a helper-level memory control, punctuation-cleared declaration candidates, exact public/persistence fixtures, two bounded oracle scenario fixtures, and a validator-backed implementer-`independent: false` restoration rejection. Raw Java Unicode escapes in declaration keywords/owners are explicitly excluded as a different prelexical-translation mechanism and fail closed rather than publishing partial IDs. The stale architecture “forced full rebuild” wording is reconciled to chunker-only `rechunk_all` with content-hash vector reuse. Verification: chunker 437, lexical 56, renderer 50, review evidence 69, exact delta tests all pass; first canonical run had one unrelated temp-directory cleanup race that passed 3/3 isolated; clean canonical rerun **5,641 tests across 50 files, OK**. No approval restored by the implementer; one fresh focused replay of these repaired surfaces remains.
- **Summary/split convergence checkpoint — 2026-07-16: REPAIRED, EXACT REPLAY PENDING.** Compatibility replay showed that globally additive module summaries would alter legacy symbol-less output, while a suffix-specific initializer check dropped oversized table-decomposed parts. The final repair is localized: ordinary language paths retain the established `__module__` behavior; only Java initializer chunks coexist with that fallback, and `_is_initializer_section` keys on the stable marker before either `(part N/M)` or `(rows N–M of T)`. Embedded/trailing/nested raw Unicode-escape owners also fail closed at the initializer-ID surface. Public single-owner, line/character split, table split (including trailing `TailZz`), legacy symbol-less summary, contextual `record`, memory, and persistence controls pass. Final canonical run: **5,643 tests across 50 files, OK**. Framework edit gate will close before the exact reviewer replay; approval remains withheld.
- **Final focused delivery replay — 2026-07-16: PASS.** Three independent affected-surface reviewers re-attempted the exact Java, architecture/release, and oracle/distribution failures on the stable gate-closed tree. Java replay verified mixed and single Sc/Pc identities, exact module+initializer coexistence, line and table split preservation including first/last/tail literals, flat helper memory, embedded/trailing/nested raw-escape fail-closed behavior, and three-row delta planning. Architecture/release replay verified both splitter families, legacy symbol-less output, contextual `record`, chunker-version/reuse prose, and install/upgrade boundaries. Oracle/distribution replay verified both promised scenarios, the real independence validator rejection, and setup/upgrade/package propagation. No do-now findings remain; no security/authority delta exists. Focused totals: chunker 439, lexical 56, renderer 50, review evidence 69, setup 18, upgrade 302, package 94; canonical **5,643/50 OK**; docs-lint and diff check clean.

## Review Evidence

- wave-council-readiness: approved 2026-07-13 — prepare council synthesis verdict READY. The configured red-team fixed seat completed the isolated standard-depth primer; its two material findings (lexically unsafe fallback spans and a skippable tree-sitter proof) were amended in the admitted change before approval. `wave_review(phase="prepare")` selected no additional mandatory specialist lanes. No implementation has begun.
- wave-council-readiness: withdrawn 2026-07-14 — the historical red-team-only approval did not run the configured fixed seats and missed the generic-rescue and shared-Scala blockers.
- wave-council-readiness: approved 2026-07-14 — full prepare council PASS after plan amendments; the generated red-team fixed seat and security-reviewer rotating seat completed, with architecture-reviewer, qa-reviewer, reality-checker, and performance-reviewer also run as independent specialists. No unresolved finding remains and no implementation has begun.
- wave-council-readiness: withdrawn 2026-07-15 — review validation found the unpinned enum/record container gap; the prior approval remains historical evidence but no longer authorizes implementation until the affected lanes re-verify and the council re-synthesizes.
- wave-council-readiness: approved 2026-07-15 (re-verified) — supersedes the 2026-07-15 withdrawal. Independent readiness confirmation after the enum/record container gap was pinned (class/enum/record parity through both Java paths; illegal record instance initializers excluded); all load-bearing claims re-verified against the current committed tree; the plan's inaccurate "distinct enum/record body handling" was corrected (record recognition + `record_body` traversal are net-new). Verdict ready-with-notes, no blockers. No implementation has begun.
- wave-council-delivery: approved 2026-07-16 — independent fresh-context code-grounded delivery review of the implemented change; all eight ACs satisfied (AC-3/AC-5 via evidence-backed test-pinned deferrals); fixtures proven non-vacuous, scanner single-pass/lexical/scope-correct, no Scala regression, migration reuse honest. Verdict approved, max severity low, no blockers; two non-blocking test-coverage notes addressed in-session. Coordinator re-verification: full suite 5,617 OK, docs-lint clean, targeted test_chunker 421 OK + test_fts_lexical_layer.
- wave-council-delivery: withdrawn 2026-07-16 — independent re-review reproduced fallback identity/lexical correctness and bounded-state blockers; focused repair and affected-lane re-verification required.
- code-reviewer: approved 2026-07-16 — isolated Java-only change, no boundary change; full suite green.
- qa-reviewer: approved 2026-07-16 — non-vacuous fail-closed fixtures with exact-identity assertions on both paths; merge and oversized-split contracts pinned.
- architecture-reviewer: approved 2026-07-16 — stable chunk identity and honest rechunk-with-reuse migration; stale architecture-doc constants corrected.
- performance-reviewer: approved 2026-07-16 — single monotonic-pass fallback scanner within the linear-scan contract.
- release-reviewer: approved 2026-07-16 — `CHUNKER_VERSION 31→32` folds into the 1.13.0 schema-v6 convergence window; upgrade rechunk-with-reuse fixture proves honest cost.
- code-reviewer: withdrawn 2026-07-16 — valid comment-separated and `$`-bearing Java type headers violate fallback identity parity.
- qa-reviewer: withdrawn 2026-07-16 — the exact-identity fixture set omits both reproduced valid-header cases.
- performance-reviewer: withdrawn 2026-07-16 — the inspection-count test does not establish the required bounded auxiliary-state claim.
- code-reviewer: approved 2026-07-16 (re-verified) — supersedes the withdrawal. The fallback lexer now preserves the legal Java identifier surface (`[\w$]+`) and treats comments as lexical whitespace; the change stays isolated behind the Java-only gate with no boundary change; full suite 5,622 OK.
- qa-reviewer: approved 2026-07-16 (re-verified) — supersedes the withdrawal. Exact-identity fail-closed fixtures now pin both reproduced valid-header cases (comment-separated and `$`-bearing) with fallback == tree-sitter, plus a direct `_BoundedHeader` bound test and a huge-single-segment correctness test.
- performance-reviewer: approved 2026-07-16 (re-verified) — supersedes the withdrawal. The source-proportional `header` buffer is replaced by `_BoundedHeader` capped at `_INIT_HEADER_MAX_CHARS`, so retained auxiliary state is O(1) in source length; the bound is now directly tested, not inferred from inspection counts.
- wave-council-delivery: approved 2026-07-16 (re-verified) — supersedes the withdrawal. Both P1 blockers repaired and directly verified: the operator's two reproductions plus five adversarial edge cases all yield fallback identities equal to tree-sitter; retained state is bounded and pinned. Affected code/qa/performance lanes re-verified above; architecture and release approvals unchanged. Focused synthesis, no full council (no boundary/migration decision changed). Full suite 5,622 OK; docs-lint clean.
- wave-council-delivery: withdrawn 2026-07-16 — focused re-review reproduced over-cap declaration identity loss in the new prefix-capped header state; repair and affected-lane re-verification required.
- code-reviewer: withdrawn 2026-07-16 — valid grammar-unavailable Java can lose first-class initializer identity when significant pre-class annotations exceed the retained prefix.
- qa-reviewer: withdrawn 2026-07-16 — the recorded 5,000-character annotation parity claim is not covered by a significant-header public-path fixture and is falsified by the executed control.
- performance-reviewer: withdrawn 2026-07-16 — the O(1) bound is real, but its current prefix-retention strategy changes fallback correctness beyond the cap.
- code-reviewer: approved 2026-07-16 (re-verified, round 2) — supersedes the round-2 withdrawal. The prefix-capped buffer is replaced by a streaming `_SegmentClassifier` (Java-only gate unchanged); a valid >4,096-char annotation before `class` now retains type identity, verified equal to tree-sitter.
- qa-reviewer: approved 2026-07-16 (re-verified, round 2) — supersedes the round-2 withdrawal. Added the over-cap public-path regression, a streaming-state bound test, and a 400+-case differential property test asserting fallback == tree-sitter across the ASCII identifier surface × annotation length × comment placement × container kind; the falsified round-1 claim is corrected in the record.
- performance-reviewer: approved 2026-07-16 (re-verified, round 2) — supersedes the round-2 withdrawal. Retained state is now a single bounded identifier token plus O(1) flags (no prefix retention), so correctness no longer degrades past any cap; the bound is directly tested.
- wave-council-delivery: approved 2026-07-16 (re-verified, round 2) — supersedes the round-2 withdrawal. The over-cap identity-loss defect is repaired via streaming classification and verified by execution (the operator's exact case + a 400+-case differential battery, all fallback == tree-sitter). One documented benign out-of-scope divergence: a Unicode-named type shows a trailing space in the tree-sitter name node (tree-sitter-side cosmetic quirk). Focused synthesis, no full council; architecture/release approvals unchanged. Targeted 483 OK; full suite green; docs-lint clean.
- wave-council-readiness: withdrawn 2026-07-16 — operator-added change `1sr0t` expands the wave into canonical seed/reviewer/distribution behavior not covered by the Java-only readiness packet.
- wave-council-delivery: withdrawn 2026-07-16 — the latest approval covers repaired `1sbfl` only; newly admitted `1sr0t` is planned and unimplemented.
- docs-contract-reviewer: pending — focused pre-implementation review required for seed-209 authority and rendered carrier parity.
- docs-contract-reviewer: approved 2026-07-16 (readiness) — supersedes the pending marker; canonical authority, carrier parity, proof ceiling, and no-schema boundary verified before implementation.
- wave-council-readiness: approved 2026-07-16 (focused `1sr0t` amendment) — supersedes the 2026-07-16 withdrawal. Standard-depth focused synthesis over docs-contract, QA/distribution, and reality/boundedness evidence; the two review sharpenings were incorporated before approval, and no `1sr0t` implementation edit had begun.
- code-reviewer: withdrawn 2026-07-16 (round 3) — the streaming classifier silently truncates valid owner identity at 512 characters.
- qa-reviewer: withdrawn 2026-07-16 (round 3) — the differential matrix omits the new token-cap boundary, and a duplicate test method is silently overwritten.
- performance-reviewer: withdrawn 2026-07-16 (round 3) — absolute O(1) retained state is achieved by changing the promised identity; the performance contract must describe the honest semantic-token bound.
- code-reviewer: approved 2026-07-16 (re-verified, round 3) — supersedes the round-3 withdrawal. The `_JAVA_IDENT_TOKEN_MAX` cap is removed; the full owner identifier is retained; verified 513/1000/5000-char owners equal tree-sitter.
- qa-reviewer: approved 2026-07-16 (re-verified, round 3) — supersedes the round-3 withdrawal. Added the 513-char public-path parity fixture and a >512-char name to the differential matrix (closing the length dimension it omitted); removed the duplicate test method (AST check confirms none remain).
- performance-reviewer: approved 2026-07-16 (re-verified, round 3) — supersedes the round-3 withdrawal. Retained state is characterized honestly as O(longest identifier + nesting depth), not O(1); it never retains annotation/expression/body text, and the honest bound is directly tested.
- code-reviewer: approved 2026-07-16 (re-verified, round 4) — supersedes the round-3 approval. Class-literal member-selection tokens (`Foo.class`) are now distinguished from declaration keywords via a leading-`.` flag; verified across 10 variants, all fallback owners equal tree-sitter.
- qa-reviewer: approved 2026-07-16 (re-verified, round 4) — supersedes the round-3 approval. Added the class-literal parity fixture and two class-literal cases to the differential matrix's annotation-*syntax* dimension (the gap that let round 4 slip).
- performance-reviewer: approved 2026-07-16 (re-verified, round 4) — supersedes the round-3 approval. The two residual "O(1)-state" statements are corrected to the honest bound; `grep` confirms no current-facing "O(1)-state" claim remains.
- wave-council-delivery: **1sbfl repaired through round 4; wave-level delivery approval deferred** 2026-07-16 — the `1sbfl` round-4 defect is fixed and code/QA/performance re-verified above. A wave-level `wave-council-delivery` signoff is still NOT recorded because the wave also carries the newly-admitted, **planned/unimplemented** change `1sr0t-enh oracle-diverse-review-verification`; the wave cannot be delivery-approved or closed until `1sr0t` is re-readied, implemented, and reviewed.
- code-reviewer: withdrawn 2026-07-16 (full-wave delivery review) — escaped Java text-block delimiter handling violates fallback/tree-sitter initializer-span parity.
- qa-reviewer: withdrawn 2026-07-16 (full-wave delivery review) — the 5,629-test green suite misses the reproduced Java defect, and the install/upgrade fixtures do not stage/assert canonical seed `209`.
- performance-reviewer: withdrawn 2026-07-16 (full-wave delivery review) — the Java scanner repair must preserve the monotonic bounded-state contract; focused replay required.
- docs-contract-reviewer: withdrawn 2026-07-16 (full-wave delivery review) — `1sr0t` AC-4 delivery proof and `1sbfl` current-state record/prose require reconciliation.
- wave-council-delivery: withdrawn 2026-07-16 (full-wave delivery review) — three do-now blockers remain; no approval is authorized until focused repairs and affected-lane replay complete.
- code-reviewer: approved 2026-07-16 (re-verified, round 5, `1sbfl` only) — supersedes the full-wave withdrawal for the Java scanner. The text-block scanner now honors `\` escapes inside `"""…"""`; an escaped `\"""` no longer closes the block early. Verified fallback == tree-sitter (id + span) across 4 variants; `test_fallback_escaped_text_block_delimiter_does_not_truncate` pins it.
- performance-reviewer: approved 2026-07-16 (re-verified, round 5, `1sbfl` only) — supersedes the full-wave withdrawal. The text-block fix is within the single-pass scan (skip escaped chars; no rescan); the monotonic bounded-state contract is preserved, and the O(1) prose is corrected to the honest bound.
- qa-reviewer: **PARTIAL — `1sbfl` Java portion re-verified; `1sr0t` distribution portion NOT addressed here** 2026-07-16 — the reproduced escaped-text-block defect now has an exact public forced-fallback regression and the suite catches it (487 targeted, full suite green). The other withdrawal reason — install/upgrade fixtures not staging/asserting canonical seed `209` — belongs to the concurrent `1sr0t` implementation and is intentionally not touched here.
- docs-contract-reviewer: **PARTIAL — `1sbfl` current-state record reconciled; `1sr0t` AC-4 NOT addressed here** 2026-07-16 — `1sbfl` AC-2/AC-8/task and Decision Log are reconciled (obsolete `O(1)`/`_BoundedHeader`/old-matrix claims superseded; task checked). The `1sr0t` AC-4 delivery-proof reconciliation is owned by the concurrent `1sr0t` session.
- wave-council-delivery: **deferred** 2026-07-16 — the `1sbfl` round-5 blockers (text-block P1, governance P2) are repaired and the fully-`1sbfl` lanes re-verified above; a wave-level signoff is still NOT recorded because (a) `1sr0t`'s install/upgrade P1 remains open in the concurrent session, and (b) `1sr0t` is not yet delivered. The wave cannot close until both changes are complete.
- qa-reviewer: **REPAIR COMPLETE; independent re-verification pending** 2026-07-16 (`1sr0t` only) — the setup and upgrade fixtures now stage/assert canonical seeds `209`/`221`/`239`, the target seed-209 contract, both reviewer carriers, and preserved project-authored bytes. Focused suites and the 5,630-test isolated run are green; this implementer session does not self-restore the withdrawn approval.
- docs-contract-reviewer: **REPAIR COMPLETE; independent re-verification pending** 2026-07-16 (`1sr0t` only) — the installed/upgraded target now contains the canonical authority its generated carriers reference, and the tests distinguish canonical seed delivery from rendered carrier delivery. The prior partial/withdrawn state remains in force until a fresh reviewer checks the repaired tree.
- wave-council-delivery: **deferred pending focused independent replay** 2026-07-16 — both changes are implemented and their known blockers are repaired, but the latest full-wave approval remains withdrawn. A focused independent QA/docs-contract replay of `1sr0t` AC-4, followed by wave-level synthesis across the already-reverified `1sbfl` lanes, is still required.
- code-reviewer: **changes requested** 2026-07-16 (final full-wave review) — `RED-JAVA-UNICODE-ID` corrupts exact owner identity on both Java paths; the fallback collision reaches the real ID-keyed index planner and drops a row.
- architecture-reviewer: **changes requested** 2026-07-16 — repair the Java semantic-identity boundary locally (JLS identifier handling + byte-correct Java tree-sitter extraction); do not broaden into a repository-wide tree-sitter refactor without evidence.
- qa-reviewer: **changes requested for `1sbfl`; approved for `1sr0t`** 2026-07-16 — javac-valid combining/connector/currency probes expose an untested collision; setup/upgrade/render/independence suites independently pass for `1sr0t`.
- docs-contract-reviewer: **changes requested for `1sbfl`; approved for `1sr0t`** 2026-07-16 — withdraw the “benign Unicode quirk” and `[\w$]+` legal-surface claims; canonical seed/carrier authority and proof ceiling are coherent.
- reality-checker: **changes requested** 2026-07-16 — the grammar-unavailable/parse-cap fallback is supported and field-reachable; duplicate IDs cause observable retrieval loss, so ASCII-only narrowing is not credible.
- security-reviewer: approved 2026-07-16 — no new actor, trust boundary, authority delta, privilege, or confidentiality impact; the Unicode defect remains a correctness P1 rather than a security issue.
- performance-reviewer: approved with repair constraint 2026-07-16 — preserve the monotonic Java scan, use O(1)-per-character Unicode category checks, and encode source bytes once for tree-sitter name extraction; no per-symbol whole-source encoding/rescan.
- release-reviewer: **changes requested** 2026-07-16 — do not ship `CHUNKER_VERSION = 32` in 1.13.0 while distinct legal Java owners can collapse during v31→32 convergence; version/reuse strategy otherwise remains sound.
- wave-council-delivery: **changes requested** 2026-07-16 (final full-wave council) — one P1/do-now blocker (`RED-JAVA-UNICODE-ID`) remains in `1sbfl`; `1sr0t` is approved. Focused repair and affected-lane replay required before delivery approval.
- code-reviewer: approved 2026-07-16 (final focused replay) — exact Unicode owners, contextual restricted identifiers, partial-AST supplementation, raw-escape fail-closed boundary, and public summary coexistence all pass the named direct/public attacks; no directly coupled do-now finding remains.
- qa-reviewer: approved 2026-07-16 (final focused replay) — paired/mixed/single owner, whole/embedded/trailing/nested escape, helper-memory, line/table split, legacy-summary, and delta-planner fixtures are non-vacuous and green; `1sr0t`'s two named scenarios and independence rejection execute.
- architecture-reviewer: approved 2026-07-16 (final focused replay) — repair remains Java-local except for a contract-preserving summary wrapper condition; ordinary symbol-less language output is unchanged, both splitter suffix families preserve initializer identity, and the explicit prelexical-escape exclusion is honest.
- reality-checker: approved 2026-07-16 (final focused replay) — supported fallback/partial-AST paths and actual ID-keyed persistence were executed; observed outputs match the narrowed documented boundary.
- performance-reviewer: approved 2026-07-16 (final focused replay) — scanner auxiliary memory remains flat (834-byte measured peak at 2k/20k/100k no-output lines), clean-tree grammar path avoids the supplemental scan, and no repeated whole-source copy/rescan was introduced.
- release-reviewer: approved 2026-07-16 (final focused replay) — `CHUNKER_VERSION 32`, schema-v6 convergence, rechunk-all/content-hash reuse, legacy symbol-less compatibility, and public split-chunk persistence are release-consistent for 1.13.0.
- docs-contract-reviewer: approved 2026-07-16 (final focused replay) — architecture, ACs, scenario proof ceiling, raw-escape exclusion, split-marker contract, and install/upgrade carrier authority are reconciled; docs-lint is clean.
- wave-council-delivery: approved 2026-07-16 (final focused synthesis) — supersedes the final full-wave changes-requested verdict. Both `1sbfl` and `1sr0t` pass their required lanes; all do-now findings are repaired and independently replayed, full suite 5,643/50 OK, no security finding, no open AC/task, and no load-bearing boundary remains unreviewed.
- operator-signoff: approved 2026-07-16 — operator directed closure ("close this wave and do a local build") after the final independent review passed across both changes (`1sbfl` + `1sr0t`): full suite 5,643 OK, chunker 439, docs-lint + `git diff --check` clean, framework edit gate closed, and independent Java / architecture-release / oracle-distribution replays PASS. Close blocks only on this signoff.

## Dependencies

- No code dependency on another wave. `1sr0t` depends on its canonical seed-209 contract being settled before reviewer-carrier renders and install/upgrade tests. Before the first `1sr0t` seed/framework edit, complete focused pre-implementation review and re-ready the amended wave; then land `1shv4` before cutting 1.13.0 so version `32` shares the already-required schema-v6 convergence window.
