# Wave Record

Owner: Engineering
Status: closed
Last verified: 2026-07-05

wave-id: `1rs44 sql-graph-accuracy-followups`
Title: SQL Graph Accuracy Followups

## Objective

Close the bounded, non-blocking SQL-graph-accuracy residuals the `1p9qi` delivery council recorded rather than fix at close time: five bounded statement-unit/capture faithfulness fixes, two edge cases, and a clustering-asymmetry measurement. When this wave closes, the SQL data-layer edges are phantom-free for the routine/trigger/MERGE shapes the `1p9qi` census corpora never exercised. (The larger in-loop-DML-recovery residual was split to `1rs45-enh sql-plpgsql-loop-body-dml-recovery` at readiness — it is NOT in this wave.)

## Changes

Change ID: `1rrx5-enh sql-graph-accuracy-followups`
Change Status: `implemented`

Completed At: 2026-07-05

## Wave Summary

Wave `1rs44` (SQL Graph Accuracy Followups) delivered one change: SQL graph accuracy follow-ups (1p9qi review residuals). Notable adjustments during implementation: SQL graph accuracy follow-ups (1p9qi review residuals): **R8 implemented (AC-9)** — generalized the embedded-SQL prose false-positive defense to the DELETE/UPDATE/INSERT arm via interior-vs-trailing ERROR discrimination. STEP-1 empirical AST validation (venv + live tree-sitter-sql) FIRST confirmed the discriminator is clean: the binding prose `delete the row from cache` carries its `the row` ERROR at byte [7:14] BEFORE the `from cache` target [15:25] (interior), while every valid dialect tested (`RETURNING`, `ON CONFLICT DO NOTHING/UPDATE`, unmodeled `DELETE FROM t USING src`, `FROM`-clause update, CTE-led DML, subqueries, schema-qualified) leaves any ERROR AFTER the first `object_reference` (trailing) or parses clean — so NO blanket any-ERROR gate (which STEP 1 showed would drop valid dialect). Added helper `_sql_embedded_has_interior_error` (refuse when earliest ERROR start < earliest `object_reference` start) + a second gate in the embedded-SQL bind loop scoped to delete/update/insert candidates, with counter `dirty_prose_refused` + verbose-log field mirroring `dirty_truncate_refused`. TRUNCATE keeps FIX 1's any-ERROR gate unchanged. Flips: `delete the row from cache` / `update the config from source` → NO edge; `DELETE FROM t RETURNING id` / `INSERT INTO t VALUES (1) ON CONFLICT DO NOTHING` → `writes` preserved; clean `DELETE FROM sessions WHERE id=?` → binds (no regression). Fineract embedded-SQL census re-run (R8 gate active): `dirty_prose_refused=0`, bound edges 11→12 (no drop; increase is from FIX1/R5 sniff additions) → zero valid-SQL over-rejection; no candidates refused (the vector is a primer synthetic, not a live Fineract capture). SELECT-prose recorded as an out-of-scope residual (not cleanly discriminable; reads excluded from clustering). `GRAPH_BUILDER_VERSION` NOT re-bumped (stays `"39"`; R8 only removes false edges before commit).; SQL graph accuracy follow-ups (1p9qi review residuals): Delivery-review fixes applied (adversarial-faithfulness PASS WITH FINDINGS, code APPROVE, architecture APPROVE WITH NOTES). **FIX 1 — R5 TRUNCATE clean-parse tightening:** `jdbc.update("truncate events now")` (non-SQL prose leading with `truncate` at a SQL sink) confidently-wrong-bound `Dao.run --writes--> events` at `LITERAL_DERIVED` — tree-sitter-sql's TRUNCATE production accepts `TRUNCATE <ident>` with no connective keyword and tolerates trailing garbage (`now` → sibling ERROR). Added `_sql_embedded_parse_has_error` and gated the embedded-SQL bind: a candidate that produced a `statement=truncate` reference AND parses with an ERROR is refused before bind (new `dirty_truncate_refused` counter). Flip: `truncate events now` → NO edge; `TRUNCATE TABLE events` / `TRUNCATE events` (no ERROR) → `writes` preserved. DELETE/UPDATE/INSERT/ALTER/DROP untouched (they never carry `statement=truncate`). **FIX 2 — R3 %ROWTYPE/%TYPE genuine-anchor preservation:** `DECLARE r some_table%ROWTYPE` / `DECLARE v other_table.col%TYPE` — R3's blanket `function_declaration` object_reference skip dropped `some_table`/`other_table.col`, which are GENUINE table dependencies (a recall regression vs pre-R3). Gated the skip on an AST signal: the `%ROWTYPE`/`%TYPE` parses as an ERROR sibling of the object_reference, so KEEP the object_reference when a direct-child ERROR whose casefolded text contains `%rowtype`/`%type` is present; still SKIP a bare type name (`record`, custom domain — no % signal). Flip: anchored declarations preserved; `record`/`custom_domain`/`integer` still absent; default-value read still preserved. **FIX 4 — code-lane comment nit:** R2's `when_clause` comment corrected from "top-level subquery descendant" to unbounded-depth descent (any nested subquery found; bounded by the field/column reference filter, not by depth). +2 flip tests. `GRAPH_BUILDER_VERSION` NOT re-bumped (still `"39"`; these refine v39 before commit).

**Changes delivered:**

- **SQL graph accuracy follow-ups (1p9qi review residuals)** (`1rrx5-enh sql-graph-accuracy-followups`) — 9 ACs completed. Key decisions: Consolidate the `1p9qi` review residuals into ONE enhancement plan rather than leaving them as prose notes.; SPLIT R6 (in-loop DML recovery) out to `1rs45-enh sql-plpgsql-loop-body-dml-recovery`.
## Journal Watchpoints

- SPLIT DONE (readiness, 2026-07-05): the in-loop DML recovery residual is now `1rs45-enh sql-plpgsql-loop-body-dml-recovery` (planned, unadmitted) — the readiness primer showed it needs new DML-recovery vocabulary + nested-ERROR descent + a new regex-scanner CTE exclusion (the `1p9qe` tier has none of these), materially larger than the bounded fixes here. Do NOT re-fold it into this wave.
- Blocking gate before close: the consolidated **adversarial-faithfulness review lane** is required (binding changes — R1/R2/R4/R5 touch reference emission and embedded-SQL capture) — standing security-control-faithfulness rule.
- Single coordinated `GRAPH_BUILDER_VERSION` bump (38→39) at integration covering all landed edge-set changes (R1–R5 change the emitted edge set) — not per-item.
- Coordination: all requirements converge on `.wavefoundry/framework/scripts/graph_indexer.py`; parallel lanes work symbol-disjoint regions with a serialized integration step (the `1p9qi` pattern). `1rs45` (split recovery) and the planned `1p9q8 graph-index-accuracy` share the same SQL extractor region — coordinate merge order if any is in flight.
- Implementer notes from the readiness primer/seats: R3's default-value read (`DECLARE x int := (SELECT … FROM t)`) is already preserved today — the fix must not break it; R4 can reuse the existing `_SQL_IDENT_QUOTE_CHARS` translate table; verify the R5 sniff constant (`_SQL_SNIFF_KEYWORDS`) in-tree before editing.
- Follow-up candidates deliberately OUT of scope (do not fold in): JPQL binding, column-level lineage, convention-derived table names, Python/Go/TS/JS SQL sinks, Liquibase-changelog definitions; separately tracked: `1rqh2` (tomllib), `1rs45` (loop recovery), `1p9q8` wave.

## Participants

- implementer — all requirements touch `.wavefoundry/framework/scripts/graph_indexer.py`
- code-reviewer — statement-unit / capture changes
- qa-reviewer — exact-set flip tests per point fix, full suite
- architecture-reviewer — the clustering-exclusion decision (R7) and relation-consumer coherence
- security-reviewer — the mandatory adversarial-faithfulness lane (reference-emission + embedded-SQL capture surface)
- performance-reviewer — the point fixes are on already-visited AST nodes; the clustering diagnostic is read-only output
- red-team, reality-checker, senior-engineering-challenger — council seats

## Review Checkpoints

- **Prepare-phase Wave Council [prepare-council] — 2026-07-05: PASS WITH NOTES** (moderator: wave-council; primer-depth: standard; seats: red-team, reality-checker, qa-reviewer; rotating-seat: none additional; strongest-challenge: the red-team readiness primer's live-tree finding that the original R6 (in-loop DML recovery) rested on a false "reuse the `1p9qe` recovery tier unchanged" premise — the recovery scanner has NO DML patterns (only CREATE/ALTER), the `FOR…LOOP` ERROR shreds the insert target so the AST re-parse path cannot transfer, and the scanner has NO CTE exclusion (that lives on the AST path) — so nested-in-body DML recovery needs new regex DML vocabulary + new descent/attribution + a new CTE-exclusion story, a new faithfulness surface that would have understated its risk and gated the five genuinely-bounded fixes; strongest-alternative: keep R6 unified in this change — declined, R6 was SPLIT to `1rs45` with corrected (larger) scope. Readiness disposition: R1–R5 confirmed bounded against the tree with live tree-sitter parses of every anchor (the primer + reality-checker both parsed the trigger/MERGE/DECLARE/bracket shapes; all held, including R3's builtin-type exception and R4's mangled-span reproduction); the bounded fixes, the two edge cases, and the clustering instrumentation are ready. Notes applied in-doc before open: AC-6's soft-close branch now requires the `[~]` marker + inline rationale (vacuity guard); AC-7 now requires a fixture-based assertion that the diagnostic surfaces a constructed high-in-degree write-target node (vacuity guard); this wave.md's stale in-loop-recovery/`R8` framing was synced to the post-split state; R3 default-value-preservation, R4 `_SQL_IDENT_QUOTE_CHARS` reuse, and R5 sniff-constant in-tree verification recorded as implementer notes.)

- **Delivery-phase Wave Council [wave-council-delivery] — 2026-07-05: PASS WITH NOTES (seats unanimous ship)** (moderator: wave-council; primer-depth: standard; seats: red-team, code-reviewer, qa-reviewer, architecture-reviewer, security-reviewer; rotating-seat: none additional; strongest-challenge: the delivery adversarial-faithfulness lane's live-probed finding that R5's TRUNCATE sniff addition minted a CONFIDENTLY-WRONG `writes` edge to a real table from non-SQL prose at a SQL sink (`jdbc.update("truncate events now")` → `writes events`) — the one finding that broke the wave's "never a confidently-wrong edge" invariant (the rest were safe under-capture) — RESOLVED in the review fix round by a TRUNCATE clean-parse gate (refuse a truncate-bearing embedded candidate whose parse carries an ERROR; clean `TRUNCATE [TABLE] t` still binds), with the delivery-council primer confirming over the fixed state that prose is refused and clean DDL binds; the same lane's R3 `%ROWTYPE`/`%TYPE` genuine-anchor recall regression was also fixed (keep the anchor when the `%…` ERROR-sibling signal is present) and the primer confirmed it holds with no over-keep leak; strongest-alternative: gate inside the frozen statement unit — declined, it would break the names-as-written contract and change SQL-file extraction out of scope; the emit-site/bind-path locus was correct. Disposition: SHIP. Both moderate findings fixed and adversarially re-verified; two non-blocking items recorded honestly — the TRUNCATE `RESTART IDENTITY` over-rejection (safe under-capture on a rare valid form; self-mitigating on the multi-table phantom case) as an accepted limitation, and the DELETE/UPDATE prose-with-connective confidently-wrong vector (`"delete the row from cache"` → `writes cache`) as a PRE-EXISTING `1p9qi` sibling NOT introduced or worsened here, flagged to the operator as a follow-up whose correct fix is a general option-aware clean-parse gate across all sniff keywords. Suite 4,609; version 39 (single bump); live v39 upgrade-heal verified.)
  - Delivery-lane + fixed-seat evidence (distinct from the verdict line): **security-reviewer** (delivery adversarial-faithfulness lane, PASS WITH FINDINGS) ran live hostile probes across R1–R6 — R1 multi-trigger flag isolation, R2 alias non-leak, R4 frozen-unit preservation, R6a survivals, and incremental-equals-full all held; found the R5 TRUNCATE confidently-wrong edge and the R3 `%ROWTYPE` recall regression (both fixed). **code-reviewer** (APPROVE) traced R1's latching flag lifecycle and R6a's floor arithmetic by hand and confirmed both correct, all 7 tests non-vacuous, 450-test module green; flagged the R2 comment nit (fixed) and a pre-existing out-of-scope `RETURNS <custom_type>` misattribution (recorded). **architecture-reviewer** (APPROVE WITH NOTES) confirmed version discipline, R7 genuinely read-only (no `CLUSTER_BUILDER_VERSION` bump), and relation-consumer coherence with no new relation; its two architecture-doc folds (the clustering-diagnostic pointer at the "revisit if degrades" note, and the R4 emit-site normalization clause) were applied. **qa-reviewer** (PASS WITH NOTES) reconciled all eight ACs to load-bearing exact-set/flip tests, confirmed R6b correctly `[~]` with rationale and R7's fixture non-vacuous and wired into the build, no silent `[ ]`; its stale-count nit was corrected. **red-team** (delivery primer over the fixed state) adversarially verified both fixes — FIX 2 confirmed holds across every `%ROWTYPE`/`%TYPE`/`numeric(…)` variation; FIX 1 holds on its goal, surfacing the narrow `RESTART IDENTITY` over-rejection and the pre-existing DELETE-prose sibling (both recorded). **performance**: the point fixes run on already-visited AST nodes and the R7 diagnostic is verbose-log-gated read-only output; the coordinator's live v39 rebuild (11,672 nodes / 33,363 edges, secrets clean) confirms no hot-path regression.
- **Post-review scope addition — R8 (operator-directed, 2026-07-05):** the operator directed rolling the pre-existing DELETE-prose fix into this wave. R8 generalizes the embedded-SQL prose defense to the DELETE/UPDATE/INSERT arm via interior-vs-trailing ERROR discrimination (empirically validated against the live grammar before ship: `delete the row from cache` refused, valid `RETURNING`/`ON CONFLICT`/`USING`/CTE dialect kept; Fineract census `dirty_prose_refused=0`, zero valid-SQL over-rejection; suite 4,611). Its mandatory independent adversarial-faithfulness re-review returned **PASS WITH FINDINGS**: R8 closes the INTERIOR-prose half cleanly with no over-rejection and no regression (454-test module green), but the SYMMETRIC TRAILING-prose DELETE variant (`delete from sessions periodically` → `writes sessions`) still mints a confidently-wrong write — `DELETE FROM t` is a complete statement so trailing English is parse-shape-indistinguishable from a valid unmodeled trailing clause. That subclass is PRE-EXISTING (R8 is monotonic — introduces/worsens nothing) and is recorded honestly as a residual rather than closed with a fragile trailing-keyword allowlist; AC-9 was corrected to claim only the interior-half closure. Disposition: SHIP R8 as a net improvement (closes the operator's cited example + the interior class) with the trailing-prose residual flagged to the operator as a dedicated-follow-up candidate. Suite 4,611; version still 39 (R8 only removes false edges).

## Review Evidence

- red-team: readiness primer 2026-07-05 — code-grounded feasibility; found the R6 false-premise (split executed) and confirmed R1–R5/R7/R8-era items bounded with live parse evidence and per-requirement site anchors; posed the R4-scope and R6-mechanism questions the council resolved (R4 accepted as node-id hygiene; R6 mechanism deferred to `1rs45`).
- code-reviewer: approved 2026-07-05 — delivery lane; all seven edits correct (traced R1 flag-latch + R6a floor arithmetic), 7 new tests non-vacuous, 450-test module green; R2 comment nit fixed, pre-existing `RETURNS <custom_type>` observation recorded.
- security-reviewer: approved with findings, findings fixed 2026-07-05 — delivery adversarial-faithfulness lane + delivery primer; the R5 TRUNCATE confidently-wrong edge and R3 `%ROWTYPE` recall regression both fixed and adversarially re-verified; R1/R2/R4/R6a and the three cross-cutting invariants held; two non-blocking residuals recorded (TRUNCATE `RESTART IDENTITY` accepted limitation; pre-existing DELETE-prose sibling flagged to operator).
- architecture-reviewer: approved with notes, notes applied 2026-07-05 — delivery lane; version discipline clean, R7 read-only (no cluster bump), relation-consumer coherence intact; the two architecture-doc folds (clustering-diagnostic pointer + R4 emit-normalization clause) applied.
- performance-reviewer: approved 2026-07-05 — point fixes run on already-visited AST nodes; the R7 diagnostic is verbose-log-gated read-only output; live v39 rebuild (11,672 nodes / 33,363 edges) shows no hot-path regression.
- reality-checker: READY WITH NOTES 2026-07-05 — every R1–R5 anchor grammar-verified against a live `tree_sitter_sql` parse (trigger `EXECUTE FUNCTION` phantom site, MERGE `when_clause` subquery reads dropped by the merge loop, `DECLARE record` phantom vs `integer` clean, bracket-quoted `dbo].[users` mangled span, `_SQL_SNIFF_KEYWORDS` missing TRUNCATE/ALTER/DROP); the R6 split is clean in the change docs; the only defect was this wave.md's stale framing (now synced).
- qa-reviewer: READY WITH NOTES 2026-07-05 — all eight ACs testable and grounded in real code with both flip directions pinned on R1–R5; the two vacuity-risk ACs (AC-6 soft-close, AC-7 measurement-exists) tightened in-doc; AC numbering internally consistent after the split renumber; priorities sensible.
- wave-council-readiness: approved 2026-07-05 — READY. R6 split to `1rs45` on the readiness primer's tree evidence; R1–R5 bounded and grammar-verified; the two edge cases and clustering instrumentation ready; all readiness notes applied in-doc (AC-6/AC-7 vacuity guards, wave.md sync, implementer notes). The mandatory adversarial-faithfulness lane is selected for delivery review. No product-owner acknowledgment required (framework-internal accuracy work).
- security-reviewer: approved with findings 2026-07-05 (R8 mandatory re-review) — the operator-directed R8 prose-gate generalization got an independent adversarial-faithfulness pass: interior-prose closure clean, zero valid-SQL over-rejection, no regression (454-test module green); found the pre-existing TRAILING-prose DELETE residual (`delete from sessions periodically` → `writes sessions`), which R8 is monotonic against (introduces/worsens nothing) — recorded honestly, AC-9 corrected to interior-half-only, residual flagged to operator.
- wave-council-delivery: approved 2026-07-05 — PASS WITH NOTES, seats unanimous ship (structured synthesis in Review Checkpoints). Five delivery lanes green; the mandatory adversarial-faithfulness lane found one confidently-wrong edge (R5 TRUNCATE prose) + one recall regression (R3 `%ROWTYPE`), both fixed in the review fix round and adversarially re-verified by the delivery primer. Post-review at operator direction, R8 was added (DELETE/UPDATE/INSERT prose-gate generalization) and got its own mandatory adversarial re-review (PASS WITH FINDINGS — interior half closed clean; pre-existing trailing-prose residual recorded honestly, AC-9 corrected). Recorded non-blocking residuals: TRUNCATE `RESTART IDENTITY` accepted limitation; trailing-DELETE-prose + SELECT-prose (pre-existing, flagged to operator for a dedicated follow-up). Suite 4,611; `GRAPH_BUILDER_VERSION` 39 (single bump); live v39 upgrade-heal verified; `wave_validate` clean; edit gates closed.
- operator-signoff: approved 2026-07-05 — operator confirmed closure ("close now") after the R8 re-review readout; the trailing-prose + SELECT-prose residuals accepted as recorded (pre-existing, zero live-corpus occurrences) with the robust strict-validity fix captured as spike `1rtar-task spike-embedded-sql-strict-validity-gate` for a future decision.

## Dependencies

- No external wave dependencies. Builds on the landed `1p9qi` statement unit, recovery tier, and capture/bind stages. `1rs45` (split loop-recovery) and `1p9q8` share the SQL extractor region — coordinate merge order if activated concurrently.
