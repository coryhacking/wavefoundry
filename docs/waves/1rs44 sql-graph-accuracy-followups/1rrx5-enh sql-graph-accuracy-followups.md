# SQL graph accuracy follow-ups (1p9qi review residuals)

Change ID: `1rrx5-enh sql-graph-accuracy-followups`
Change Status: `implemented`
Owner: Engineering
Status: planned
Last verified: 2026-07-05
Wave: `1rs44 sql-graph-accuracy-followups`

## Rationale

Wave `1p9qi sql-graph-accuracy` shipped the graph's first code→data-layer edge family (clause-aware `reads`/`writes`, ERROR-region recovery, embedded-SQL capture/bind, ORM `maps_to`). Its delivery council fixed the one severe defect it found (routine-body write-direction inversion) and recorded a set of smaller, non-blocking residuals as honest follow-ups rather than expanding an already-large wave at close time. This change gathers the **bounded** residuals into one admittable enhancement. Every item here is either safe under-capture (a recall gap, never a confidently-wrong edge) or reserved-name/edge-case noise that `LITERAL_DERIVED` + unique-match-or-drop already contains — none is a shipped-behavior regression. The larger in-loop-DML-recovery residual was split to `1rs45-enh sql-plpgsql-loop-body-dml-recovery` at readiness (see Decision Log). The value is closing the recall gaps and removing the residual phantoms so "which code touches table X" is complete for the Postgres/PL-SQL routine-heavy shapes the `1p9qi` census corpora (MySQL/Java Fineract, Tomcat) never exercised.

## Requirements

Bounded faithfulness fixes — each is a small, self-contained correction to the `1p9qi` statement unit / capture path, confirmed bounded against the tree by the `1rs44` prepare-phase readiness primer (2026-07-05):

1. **`EXECUTE FUNCTION <name>` trigger action must not mint a phantom read.** A Postgres `CREATE TRIGGER … EXECUTE FUNCTION log_it()` currently emits a `reads` reference for the function name `log_it`. In `handle_create_routine` that trailing `object_reference` (which appears after `keyword_execute`, so *after* the legitimate ON-table read) falls to the generic walk. Extend the name-family exclusion with a `keyword_execute`-gated skip flag modeled on the existing `expect_return_type` state machine, so the action name is excluded without touching the ON-table read.
2. **MERGE `WHEN … THEN` branch subqueries must surface their table reads.** `MERGE … WHEN MATCHED THEN UPDATE SET x = (SELECT v FROM lookup_tbl)` / `WHEN NOT MATCHED THEN INSERT … VALUES ((SELECT … FROM seed_tbl))` currently drop the subquery's `lookup_tbl`/`seed_tbl` reads because the merge loop never visits `when_clause`. Add a `when_clause` case that routes its subquery nodes through `walk_reads` in a read position — bounded, because predicate columns and assignment LHS parse as `field`/`column` nodes already in `_SQL_REF_SKIP_NODE_TYPES`, and merge aliases only appear inside `field` wrappers `walk_reads` never descends into.
3. **`DECLARE <var> <type>` type names must not mint phantom reads.** A PL/pgSQL `DECLARE r record;` emits a `reads` reference for the type token `record`. (Note, per the readiness primer: this fires ONLY for non-builtin types — `DECLARE n integer` parses as `keyword_int` with no `object_reference`, already clean.) Add a `function_declaration` case to `walk_reads` that skips its direct `object_reference` type name while still descending into other children (so a `DECLARE x int := (SELECT … FROM t)` default-value read is preserved). Same "a name is not a reference" family as R1 and the landed `RETURNS <type>` skip.
4. **Bracket-quoted T-SQL identifiers on the SQL-file path must normalize to a clean external id (node-id hygiene, not a binding change).** `SELECT * FROM [dbo].[users]` currently yields the mangled external `external::dbo].[users`. Normalize (bracket-strip) ONLY at the external-id minting site (`_sql_apply_file_extraction`'s emit path), leaving `sql_statement_references()` output (`ref["name"]`, the as-written reference text) untouched so the pinned "names-as-written" unit test stays green. Scope confirmation from the readiness primer: this is node-id cleanliness, NOT a recall/binding win — the resolver is not asked to normalize both sides; a collision of two differently-quoted forms onto one `external::dbo.users` correctly points to the same external node, and cross-file binding stays unique-match-or-drop (ambiguity → drop, never wrong-bind).
5. **The embedded-SQL sniff gate must recognize DDL/DML leading keywords it currently omits.** The `1p9qf` sniff set (`SELECT/INSERT/UPDATE/DELETE/WITH/MERGE/CALL/EXEC`) drops SQL literals that lead with `TRUNCATE`/`ALTER`/`DROP` (one live Apache Fineract site observed). Add those leading keywords so schema-affecting embedded statements are captured and bound (as `writes` — the statement unit's `analyze_statement` already handles their write direction). (Implementer: verify the sniff-constant site in the `1p9qf` capture path in-tree — the readiness primer confirmed the write-direction plumbing but flagged the sniff constant as verified-by-inference, not read this pass.)

Edge-case and monitoring items (low priority; may be recorded-and-closed rather than fully implemented):

6. **Close the two recorded edge cases.** (a) A non-temp body-local `CREATE TABLE scratch` has its definition correctly dropped, but a later in-body read of `scratch` can cross-file unique-bind a same-named real table — decide whether body-local created names should suppress subsequent in-body reads of that name (like temp objects) or remain in the standard unique-bare model. (b) The routine body-definition drop is name-parse-gated (`body_def_floor` is set only when the header name parses as an `object_reference`), so the "routine bodies never define at module scope" invariant is non-total for an unnamed-but-parseable header — make the drop unconditional on routine nodes so the invariant is total.
7. **Instrument (do not pre-emptively change) the `writes`/`maps_to` clustering asymmetry.** Community detection excludes `reads` but not `writes`/`maps_to`; a hot write-target table (an audit/events log written by many routines) can bridge otherwise-unrelated modules — the exact failure mode the `reads` exclusion prevents. Add a measurement (surface high-in-degree data-layer nodes in the graph report / a diagnostic) so the decision to exclude `writes`/`maps_to` from clustering can be made on evidence rather than pre-emptively.

Added at operator direction after delivery review (2026-07-05) — closes a pre-existing confidently-wrong vector the delivery-council primer surfaced:

8. **Generalize the embedded-SQL prose false-positive defense to the DELETE/UPDATE/INSERT sniff keywords.** The delivery-council primer found a PRE-EXISTING confidently-wrong vector (`jdbc.update("delete the row from cache")` → `writes cache` at LITERAL_DERIVED): prose leading with a sniff keyword and containing a mandatory-clause connective (`from <noun>`) mis-parses as SQL and binds a real table. `delete`/`update`/`insert` were in the sniff set before this wave (from `1p9qf`/`1p9qi`); FIX 1 hardened only the TRUNCATE arm. Extend the clean-parse defense via **interior-vs-trailing ERROR discrimination**: refuse an embedded candidate when its parsed statement carries an ERROR node WITHIN the core statement span (before/among the mandatory clause tokens — e.g. `delete the row from cache`'s interior `the row`), WITHOUT over-rejecting valid trailing dialect clauses tree-sitter-sql does not model (`DELETE FROM t RETURNING id`, `INSERT INTO t … ON CONFLICT DO UPDATE …` — trailing ERRORs, kept). TRUNCATE keeps FIX 1's any-ERROR gate (its minimal `TRUNCATE <ident>` grammar makes trailing garbage indistinguishable from options — the recorded `RESTART IDENTITY` limitation stays). If the interior-vs-trailing signal is not empirically clean against the real grammar, report the tradeoff rather than shipping a blanket any-ERROR gate that would drop valid dialect SQL. Measure the recall impact on the Fineract embedded-SQL corpus (candidates newly refused must be prose/malformed, not valid SQL).

## Scope

**Problem statement:** The `1p9qi` SQL graph accuracy wave shipped with a set of recorded, non-blocking residuals. This change closes the BOUNDED ones — several small faithfulness defects (phantom reads of routine/type/action names; a dropped MERGE-subquery read; a mangled bracket-quoted external id; an incomplete embedded-SQL sniff set), two edge cases, and one monitoring decision. The larger in-loop-DML-recovery residual is split to `1rs45`.

**In scope:**

- The five bounded statement-unit / capture faithfulness fixes (R1–R5) in `.wavefoundry/framework/scripts/graph_indexer.py`, each with exact-edge-set flip tests.
- The two edge cases (R6) and the clustering instrumentation (R7).
- Whatever coordinated `GRAPH_BUILDER_VERSION` bump the emitted-edge-set changes require (R1–R5 remove phantom edges / add contained reads → an extraction-output change → fold into one bump).

**Out of scope:**

- **In-loop DML recovery for PL/pgSQL control-flow bodies — SPLIT to `1rs45-enh sql-plpgsql-loop-body-dml-recovery`** at the `1rs44` readiness primer, which showed it needs new DML-recovery vocabulary + nested-ERROR descent + a new regex-scanner CTE-exclusion (the existing recovery tier has none of these) — materially larger than these bounded fixes and a new faithfulness surface.
- The deliberately out-of-scope future features from the `1p9qi` watchpoints: JPQL binding, column-level lineage, convention-derived table names, Python/Go/TS/JS SQL sinks, and Liquibase-changelog as a higher-recall definition source. Those are roadmap, not review residuals.
- The same-file origin-scope limitation of the embedded-SQL impostor check is an ACCEPTED, documented limitation (zero real-corpus false positives) — cross-file receiver-type resolution is explicitly NOT required here.
- Separately tracked, unrelated work: `1rqh2-debt remove-tomllib-import-fallback`, the planned `1p9q8 graph-index-accuracy` wave, the standing install/TLS/GPU field-feedback items.

## Acceptance Criteria

- [x] AC-1: An exact-edge-set test proves `CREATE TRIGGER … EXECUTE FUNCTION log_it()` mints no `reads`/`external::` reference for `log_it`, and the trigger's ON-table read is preserved (R1), both flip directions pinned. Evidence: `test_sql_trigger_execute_function_action_name_not_a_reference` (unit refs `{("accounts","read")}`, no `log_it`; `EXECUTE PROCEDURE` covered; file-path parity).
- [x] AC-2: An exact-edge-set test proves MERGE `WHEN … THEN` branch subqueries surface their table reads (`lookup_tbl`/`seed_tbl` read), and non-subquery `when_clause` content (predicate columns, assignment LHS, aliases) still mints nothing (R2). Evidence: `test_sql_merge_when_clause_subquery_reads_surface_exact_set` (exact set `{target write, src read, lookup_tbl read, seed_tbl read}` + absence sweep on `t/s/x/a/id/v/c`). Note: the VALUES subquery is `list`-wrapped (walk_reads skips `list`), so the fix collects the `subquery` NODES within the when_clause and walks each — matching the change-doc's "routes its subquery nodes" wording.
- [x] AC-3: An exact-edge-set test proves a `DECLARE <var> <type>` non-builtin type name (`record`, a custom type) mints no reference, a builtin (`integer`) stays clean, and a `DECLARE … := (SELECT … FROM t)` default-value read is preserved (R3). Evidence: `test_sql_declare_type_name_not_a_reference_three_way` (exact set `{defaults_tbl read, body_tbl read}`; `record`/`custom_domain`/`integer` absent).
- [x] AC-4: A test proves `[dbo].[users]` (and `` `db`.`users` `` / `"USERS"` quoting variants) mint a clean external id (the agreed canonical form, e.g. `external::dbo.users`), with the frozen statement-unit reference text unchanged (the pinned "names-as-written" unit test stays green) AND an existing legitimate bind is not regressed (R4). Evidence: `test_sql_bracket_quoted_external_id_normalized_clean` — canonical form chosen is **`external::dbo.users`**; two quotings collapse to one node; unit still emits `dbo].[users`; local `realtbl` bind stays `RECEIVER_RESOLVED`.
- [x] AC-5: A test proves an embedded `TRUNCATE TABLE t` / `ALTER TABLE t …` / `DROP TABLE t` literal at a known sink captures and binds as a `writes` mutation, and non-SQL strings at sinks still refuse (R5). Evidence: `test_sql_embedded_ddl_sniff_captures_truncate_alter_drop` (three `writes` at LITERAL_DERIVED via `jdbc.update` sink; the non-SQL string never captured). Verified the sniff-constant site (`_SQL_SNIFF_KEYWORDS`) in-tree before editing.
- [x] AC-6: Tests pin the two edge-case resolutions (R6): the unconditional routine body-definition drop makes the module-scope-definition invariant total (a flip test with an unnamed-but-parseable header). Evidence for R6a: `test_sql_routine_body_definition_drop_is_unconditional` (unnamed `CREATE FUNCTION () RETURNS integer …` leaks no `scratch` def; a named routine still keeps its own def). [~] R6b (body-local created-name cross-bind) is **left in the unique-bare model** — a conscious decision (see Decision Log 2026-07-05): suppression would require body-scoped tracking of non-temp created names threaded through the post-walk floor-delete AND risks dropping a genuine read where a body-local name coincidentally matches a real table; the wrong-bind is already bounded (unique-match-or-drop degrades ambiguity to a drop, never a wrong bind) and low-frequency, so the cost/risk outweighs the benefit.
- [x] AC-7: The clustering-asymmetry measurement (R7) is exercised by a fixture-based test — a constructed graph with a write-target table node written by N routines above a threshold, asserting the diagnostic surfaces that node with its in-degree (not merely that a diagnostic function "exists"); the exclude-or-not decision for `writes`/`maps_to` is then recorded on that evidence. Evidence: `test_sql_hot_data_layer_diagnostic_surfaces_high_indegree_node` (`sql_hot_data_layer_nodes` surfaces `audit_log` with `in_degree=N`; below-threshold and reads-only nodes not surfaced). Decision: **no clustering change — evidence-gathering diagnostic shipped** (see Decision Log).
- [x] AC-8: Standing gates — single coordinated `GRAPH_BUILDER_VERSION` bump (38 → `"39"`) covering all landed edge-set changes; the consolidated adversarial-faithfulness review lane ran at delivery review (binding changes; PASS WITH FINDINGS — two moderate findings fixed in the review fix round); full suite green (4,609 tests after the fix round, was 4,607 at implementation); `wave_validate` clean; live upgrade-heal verified (clean v39 rebuild, 11,672 nodes / 33,363 edges). No `CLUSTER_BUILDER_VERSION` bump (R7 is read-only; no projection change).
- [x] AC-9: An exact-set test proves prose at a sink leading with a sniff keyword + a mandatory-clause connective (`jdbc.update("delete the row from cache")`, `"update the config from source"`) mints NO edge (interior-ERROR refused), while valid trailing-dialect DML (`DELETE FROM t RETURNING id`, `INSERT INTO t … ON CONFLICT DO NOTHING`) still captures + binds; the recall impact of the generalized gate is measured on the Fineract embedded-SQL corpus and recorded (newly-refused candidates are prose/malformed, not valid SQL) (R8). The re-run adversarial-faithfulness lane confirms no new confidently-wrong edge and no broad valid-SQL over-rejection. Evidence: STEP-1 empirical AST validation (`~/.wavefoundry/venv` + live `tree-sitter-sql`) confirmed the interior-vs-trailing discriminator is CLEAN for the DELETE/UPDATE/INSERT prose class — the binding prose `delete the row from cache` carries its `the row` ERROR at byte [7:14] BEFORE the `from cache` target [15:25] (interior), while EVERY valid dialect variant tested (`RETURNING`, `ON CONFLICT DO NOTHING/UPDATE`, unmodeled `DELETE FROM t USING src …`, `FROM`-clause update, CTE-led DML, subqueries, schema-qualified) leaves any ERROR AFTER the first `object_reference` (trailing) or parses clean. Rule shipped: refuse a delete/update/insert candidate when the earliest ERROR node starts before the earliest `object_reference` (the target). New helper `_sql_embedded_has_interior_error` + a second gate in the embedded-SQL bind loop (counter `dirty_prose_refused` + verbose-log field, mirroring `dirty_truncate_refused`); TRUNCATE keeps FIX 1's any-ERROR gate unchanged. Tests: `test_sql_embedded_dirty_prose_delete_update_refused_dialect_binds` (`cache`/`config`/`source` exist as candidate targets; delete-prose + update-prose → NO edge; `DELETE FROM t RETURNING id` / `INSERT INTO t … ON CONFLICT DO NOTHING` → `writes t`; clean `DELETE FROM sessions WHERE id=?` → `writes sessions`, no regression) + `test_sql_embedded_has_interior_error_discriminator` (unit-level interior/trailing pin). Fineract census re-run (`census-1p9qf/build_census.py fineract`, R8 gate active): `dirty_prose_refused=0`, bound edges 11→12 (no drop — the increase is from the FIX1/R5 sniff additions), so ZERO valid-SQL over-rejection; no candidates refused → no sample to hand-check (the confidently-wrong `delete the row from cache` vector is a primer-surfaced synthetic reproducer, not a live Fineract capture). SELECT-prose (`select all items from inventory list`) is NOT cleanly discriminable (some prose parses clean) and is correctly OUT of R8 scope (reads, excluded from clustering, lower stakes) — recorded as a residual. **R8 adversarial re-review (2026-07-05): the interior half is closed with zero valid-SQL over-rejection, BUT the symmetric TRAILING-prose DELETE variant still mints a confidently-wrong write (`delete from sessions periodically` → `writes sessions`) — `DELETE FROM t` is a complete statement, so trailing English is a trailing ERROR, indistinguishable by parse-shape from a valid unmodeled trailing clause (`RETURNING`). This trailing-prose subclass is PRE-EXISTING (R8 is monotonic — only adds refusals; it introduces/worsens nothing) and is recorded as a residual (see Decision Log). AC-9 therefore closes the INTERIOR-prose subclass of the DELETE/UPDATE/INSERT vector, not the entire class — the complete robust prose-vs-SQL discriminator across trailing cases is a dedicated follow-up, not this bounded wave.** `GRAPH_BUILDER_VERSION` NOT re-bumped (stays `"39"`; R8 only removes false edges from the not-yet-committed v39 output).

## Tasks

- [x] Implement R1–R5 as symbol-disjoint statement-unit / capture edits with per-item exact-set flip tests (verify the R5 sniff-constant site in-tree first). R5 sniff constant `_SQL_SNIFF_KEYWORDS` verified in-tree before editing.
- [x] Resolve R6 edge cases; instrument R7 and record the clustering decision. R6a implemented (unconditional drop); R6b left in unique-bare model with rationale ([~]); R7 diagnostic shipped, clustering decision = no change.
- [x] Coordinate a single `GRAPH_BUILDER_VERSION` bump at integration; run the full suite, `wave_validate`, and a live upgrade-heal. Version 38 → `"39"` (single bump); full suite green (4,609 after fix round); `wave_validate` clean; live upgrade-heal verified (clean v39 rebuild).
- [x] Implement R8 (added at operator direction after delivery review): STEP-1 empirically validate the interior-vs-trailing ERROR discriminator against the live grammar BEFORE editing; generalize the clean-parse defense to the DELETE/UPDATE/INSERT arm; measure the recall impact on the Fineract embedded-SQL corpus; do NOT re-bump `GRAPH_BUILDER_VERSION` (v39 refinement, false-edge removal only). STEP-1 discriminator confirmed clean; helper `_sql_embedded_has_interior_error` + `dirty_prose_refused` gate landed; 2 flip/unit tests; Fineract census `dirty_prose_refused=0` (zero over-rejection); version stays `"39"`.

## Agent Execution Graph


| Workstream | Owner | Depends On | Notes |
| --- | --- | --- | --- |
| bounded-fixes (R1–R5) | implementer | — | Five small statement-unit/capture corrections; symbol-disjoint, parallelizable; each with an exact-set flip test. |
| edge-and-monitor (R6, R7) | implementer | bounded-fixes | Small edge-case pins + a clustering measurement/diagnostic. |
| integration | implementer | bounded-fixes, edge-and-monitor | Single coordinated version bump + full suite + upgrade-heal; adversarial-faithfulness lane at review. |


## Serialization Points

- All workstreams converge on `.wavefoundry/framework/scripts/graph_indexer.py` — parallel lanes work symbol-disjoint regions with a serialized integration step, exactly as `1p9qi` did.
- Single coordinated `GRAPH_BUILDER_VERSION` bump at integration, not per-item.
- Cross-wave: `1rs45` (split loop-recovery) and the planned `1p9q8 graph-index-accuracy` share the same SQL extractor region — coordinate merge order if any is in flight (single-OPEN-wave rule serializes activation).

## Affected Architecture Docs

`docs/architecture/graph-index-system.md` — any clustering-exclusion decision (R7) folds into the relation/clustering notes; the same-file origin-scope limitation note is already present. No `docs/ARCHITECTURE.md` hub change expected (confined to the graph-index subsystem).

## AC Priority

Populated at Prepare wave (2026-07-05).


| AC | Priority | Rationale |
| --- | --- | --- |
| AC-1 | important | `EXECUTE FUNCTION` name phantom read — a confidently-emitted wrong reference; faithfulness. |
| AC-2 | important | MERGE `WHEN…THEN` subquery read drop — a recall gap on a real DML shape. |
| AC-3 | important | `DECLARE <type>` phantom read — wrong reference; faithfulness. |
| AC-4 | important | Bracket-quoted external-id mangling — a malformed node id; correctness of the emitted external. |
| AC-5 | important | Sniff-set `TRUNCATE`/`ALTER`/`DROP` gap — captured schema-affecting statements missed. |
| AC-6 | nice-to-have | Two edge cases (body-local cross-bind, name-parse-gated def-drop totality) — low-frequency; may be recorded-and-closed. |
| AC-7 | nice-to-have | Clustering-asymmetry instrumentation — a measurement + recorded decision, not necessarily a behavior change. |
| AC-8 | required | Standing gates — single version bump, adversarial-faithfulness lane, full suite, upgrade-heal. |
| AC-9 | important | Generalize the embedded-SQL prose defense to the DELETE/UPDATE/INSERT arm — closes a pre-existing confidently-wrong LITERAL_DERIVED write edge (`delete the row from cache` → `writes cache`); faithfulness. |


## Progress Log


| Date | Update | Evidence |
| --- | --- | --- |
| 2026-07-05 | Plan authored from the `1p9qi` delivery-council recorded follow-ups (before `1p9qi` close, at operator direction). | `1p9qi` change-doc Decision Logs (`1p9qd`/`1p9qf`) + wave.md `wave-council-delivery` synthesis. |
| 2026-07-05 | Admitted into wave `1rs44`; R6 (in-loop DML recovery) SPLIT to `1rs45` at the readiness primer; R1–R5 confirmed bounded against the tree; R3/R4 refined per primer findings; requirements/ACs renumbered. | `1rs44` prepare-phase red-team readiness primer, 2026-07-05. |
| 2026-07-05 | Implemented R1–R7 in `graph_indexer.py` (symbol-disjoint edits) + 7 exact-set/flip tests. All anchors re-verified against live tree-sitter parses before editing (R2's `seed_tbl` subquery found `list`-wrapped → collected subquery nodes; R6a repro isolated as unnamed `CREATE FUNCTION () RETURNS integer …`). Single `GRAPH_BUILDER_VERSION` bump 38→`"39"`. `Change Status: implemented`. | 7 new tests pass; full suite 4607 (↑7 from 4600 baseline), OK. |
| 2026-07-05 | **R8 implemented (AC-9)** — generalized the embedded-SQL prose false-positive defense to the DELETE/UPDATE/INSERT arm via interior-vs-trailing ERROR discrimination. STEP-1 empirical AST validation (venv + live tree-sitter-sql) FIRST confirmed the discriminator is clean: the binding prose `delete the row from cache` carries its `the row` ERROR at byte [7:14] BEFORE the `from cache` target [15:25] (interior), while every valid dialect tested (`RETURNING`, `ON CONFLICT DO NOTHING/UPDATE`, unmodeled `DELETE FROM t USING src`, `FROM`-clause update, CTE-led DML, subqueries, schema-qualified) leaves any ERROR AFTER the first `object_reference` (trailing) or parses clean — so NO blanket any-ERROR gate (which STEP 1 showed would drop valid dialect). Added helper `_sql_embedded_has_interior_error` (refuse when earliest ERROR start < earliest `object_reference` start) + a second gate in the embedded-SQL bind loop scoped to delete/update/insert candidates, with counter `dirty_prose_refused` + verbose-log field mirroring `dirty_truncate_refused`. TRUNCATE keeps FIX 1's any-ERROR gate unchanged. Flips: `delete the row from cache` / `update the config from source` → NO edge; `DELETE FROM t RETURNING id` / `INSERT INTO t VALUES (1) ON CONFLICT DO NOTHING` → `writes` preserved; clean `DELETE FROM sessions WHERE id=?` → binds (no regression). Fineract embedded-SQL census re-run (R8 gate active): `dirty_prose_refused=0`, bound edges 11→12 (no drop; increase is from FIX1/R5 sniff additions) → zero valid-SQL over-rejection; no candidates refused (the vector is a primer synthetic, not a live Fineract capture). SELECT-prose recorded as an out-of-scope residual (not cleanly discriminable; reads excluded from clustering). `GRAPH_BUILDER_VERSION` NOT re-bumped (stays `"39"`; R8 only removes false edges before commit). | 2 new tests pass; full suite 4611 (↑2), OK; `wave_validate` clean. |
| 2026-07-05 | Delivery-review fixes applied (adversarial-faithfulness PASS WITH FINDINGS, code APPROVE, architecture APPROVE WITH NOTES). **FIX 1 — R5 TRUNCATE clean-parse tightening:** `jdbc.update("truncate events now")` (non-SQL prose leading with `truncate` at a SQL sink) confidently-wrong-bound `Dao.run --writes--> events` at `LITERAL_DERIVED` — tree-sitter-sql's TRUNCATE production accepts `TRUNCATE <ident>` with no connective keyword and tolerates trailing garbage (`now` → sibling ERROR). Added `_sql_embedded_parse_has_error` and gated the embedded-SQL bind: a candidate that produced a `statement=truncate` reference AND parses with an ERROR is refused before bind (new `dirty_truncate_refused` counter). Flip: `truncate events now` → NO edge; `TRUNCATE TABLE events` / `TRUNCATE events` (no ERROR) → `writes` preserved. DELETE/UPDATE/INSERT/ALTER/DROP untouched (they never carry `statement=truncate`). **FIX 2 — R3 %ROWTYPE/%TYPE genuine-anchor preservation:** `DECLARE r some_table%ROWTYPE` / `DECLARE v other_table.col%TYPE` — R3's blanket `function_declaration` object_reference skip dropped `some_table`/`other_table.col`, which are GENUINE table dependencies (a recall regression vs pre-R3). Gated the skip on an AST signal: the `%ROWTYPE`/`%TYPE` parses as an ERROR sibling of the object_reference, so KEEP the object_reference when a direct-child ERROR whose casefolded text contains `%rowtype`/`%type` is present; still SKIP a bare type name (`record`, custom domain — no % signal). Flip: anchored declarations preserved; `record`/`custom_domain`/`integer` still absent; default-value read still preserved. **FIX 4 — code-lane comment nit:** R2's `when_clause` comment corrected from "top-level subquery descendant" to unbounded-depth descent (any nested subquery found; bounded by the field/column reference filter, not by depth). +2 flip tests. `GRAPH_BUILDER_VERSION` NOT re-bumped (still `"39"`; these refine v39 before commit). | 2 new tests pass; full suite 4609 (↑2), OK; `wave_validate` clean. |


## Decision Log


| Date | Decision | Reason | Alternatives |
| --- | --- | --- | --- |
| 2026-07-05 | Consolidate the `1p9qi` review residuals into ONE enhancement plan rather than leaving them as prose notes. | Operator direction to fold follow-ups into a plan before closing `1p9qi`; keeps the recall gaps/phantoms admittable and discoverable. | Leave as Decision-Log prose (rejected: not admittable, easy to lose); one change doc per item (rejected: too granular). |
| 2026-07-05 | SPLIT R6 (in-loop DML recovery) out to `1rs45-enh sql-plpgsql-loop-body-dml-recovery`. | The readiness primer falsified the "reuse the `1p9qe` tier unchanged" premise with live tree evidence: the recovery scanner has NO DML patterns (only CREATE/ALTER), the `FOR…LOOP` ERROR shreds the insert target so the AST re-parse path cannot transfer, and the scanner has NO CTE exclusion (that lives on the AST path). It needs new DML vocabulary + nested-ERROR descent + a new regex CTE-exclusion — materially larger and a new faithfulness surface that should not gate the five bounded flip-fixes. | Keep unified (rejected: drags clean fixes behind R6's review surface and understates its risk). |
| 2026-07-05 | R4 bracket-quote normalization targets the external-id emit site, scoped as node-id hygiene only. | The in-review attempt to normalize inside the unit broke the pinned "names-as-written" contract; the emit site leaves the frozen unit untouched. The primer confirmed R4 does not improve cross-file recall (the resolver is not asked to normalize both sides) — it cleans the emitted id. | Normalize in the frozen unit (rejected: breaks the as-written contract); normalize both resolver sides (deferred: recall improvement is a separate, larger change). |
| 2026-07-05 | R4 canonical external-id form chosen: **`external::dbo.users`** (strip bracket/backtick/quote via the existing `_SQL_IDENT_QUOTE_CHARS` table at the emit site). | Reuses the landed quote-strip helper (`_sql_normalize_object_name`); a `.`-joined schema.table is the natural canonical form and matches how a clean unquoted reference already resolves, so two differently-quoted forms correctly collapse onto one external node. | Keep dialect brackets (rejected: mangled/duplicated nodes); a `::`-joined form (rejected: collides with the reserved `external::` / `sql::` separators). |
| 2026-07-05 | R6b (body-local non-temp created-name later-read cross-bind): **leave in the unique-bare model** (AC-6 half marked `[~]`). | Suppressing subsequent in-body reads of a body-local created name would need body-scoped name tracking threaded through the post-walk floor-delete AND risks dropping a genuine read where a body-local name coincidentally matches a real table. The wrong-bind is already bounded — unique-match-or-drop degrades ambiguity to a drop, never a wrong bind — and low-frequency. Cost/risk outweighs the benefit; simplest solution first. | Suppress body-local reads like temp objects (rejected: not small/clean, adds a faithfulness-loss risk for a low-frequency case). |
| 2026-07-05 | R7 clustering asymmetry: **no clustering change — ship a read-only evidence-gathering diagnostic only** (`sql_hot_data_layer_nodes`; `_SQL_HOT_DATA_INDEGREE_THRESHOLD = 8`; surfaced in the verbose build log). | The decision to exclude `writes`/`maps_to` from community detection should be made on measured evidence of real hot write-target bridging, not pre-emptively. The diagnostic surfaces high-in-degree data-layer nodes so that evidence can be gathered on real corpora first; no projection change means no `CLUSTER_BUILDER_VERSION` bump. | Pre-emptively exclude `writes`/`maps_to` from clustering (rejected: no evidence yet that it bridges; the `reads` exclusion was itself evidence-driven). |
| 2026-07-05 | Delivery review FIX 1 — R5 TRUNCATE clean-parse tightening (adversarial finding 1, MODERATE). | tree-sitter-sql's TRUNCATE production binds `TRUNCATE <ident>` with no connective keyword and tolerates trailing garbage, so non-SQL prose at a SQL sink (`truncate events now`) confidently-wrong-bound a real table at `LITERAL_DERIVED` — the one load-bearing finding (a confidently-wrong edge, not a recall gap). Refuse the embedded candidate when it produced a truncate reference AND the parse carries an ERROR; clean `TRUNCATE [TABLE] events` (no ERROR) still binds. Scoped to the embedded-SQL bind path (the reproducer is `jdbc.update(...)`); the frozen statement unit is untouched. | Gate inside the frozen unit (rejected: breaks the names-as-written/frozen contract and would also change SQL-file extraction, out of the finding's scope); gate at each of the 6 capture sites (rejected: not the smallest robust locus — the single bind point already re-parses only for truncate-bearing candidates). |
| 2026-07-05 | Delivery review FIX 2 — R3 `%ROWTYPE`/`%TYPE` genuine-anchor preservation (adversarial finding 2, MODERATE). | R3's blanket `function_declaration` object_reference skip converted a genuine table anchor into a phantom-removal: `DECLARE r some_table%ROWTYPE` anchors the row type to `some_table`, but its object_reference is structurally identical to the `record` phantom R3 skips (the `%…` parses as an ERROR sibling), so R3 regressed the read (recall regression). Gate the skip on the `%ROWTYPE`/`%TYPE` ERROR-sibling signal → keep the anchor; still skip a bare type name. | Skip on the object_reference text ending in a known-type list (rejected: brittle — cannot enumerate custom domains; the ERROR-sibling `%` signal is exact and grammar-derived). |
| 2026-07-05 | Recorded PRE-EXISTING out-of-scope observation (code lane; NOT fixed here). | In `handle_create_routine`, the `if ctype == "object_reference" and routine_name is None` branch could misattribute a non-builtin `RETURNS <custom_type>` object_reference as the routine name in the unnamed-header scenario (an unnamed `CREATE FUNCTION` whose return type is a custom type parses as the first top-level object_reference and would be taken as the routine name). This predates this wave and is NOT worsened by R1–R7 or the delivery-review fixes — the R6a repro used a builtin (`integer`) return type, which parses as a non-object_reference keyword node and does not trip it. Recorded for a future change rather than expanded into this one (simplest-solution-first; out of the review's finding set). | Fix now (rejected: pre-existing, out of scope, not worsened; would expand the wave at close time — the same discipline that split R6 to `1rs45`). |
| 2026-07-05 | Delivery-council primer — R5 TRUNCATE `RESTART IDENTITY`/`CONTINUE IDENTITY` over-rejection: **accepted as a recorded limitation, not fixed**. | FIX 1's "refuse on any parse ERROR" gate also refuses valid `TRUNCATE TABLE t RESTART IDENTITY CASCADE` (tree-sitter-sql does not model the `RESTART/CONTINUE IDENTITY` option family, so it parses an ERROR sibling), dropping a real `writes` edge that bound pre-fix. This is safe under-capture (never a wrong edge), narrow (single-table + trailing `CASCADE`/`RESTRICT`), and self-mitigating: the *multi-table* `TRUNCATE a,b RESTART IDENTITY` form is CORRECTLY refused because keeping it would re-introduce a phantom `IDENTITY` ref, and bare `TRUNCATE t RESTART IDENTITY` wrong-bound to phantom `IDENTITY` pre-fix and now correctly refuses — a net precision gain. A clean fix (allowlist option-tail keywords) is not clean because it re-admits the multi-table phantom, so it belongs with the broader embedded-SQL sniff-hardening follow-up below. | Allowlist `RESTART/CONTINUE/IDENTITY/CASCADE/RESTRICT` in the ERROR tail (rejected: re-admits the multi-table phantom `IDENTITY` ref; needs phantom-ref filtering, a larger surface). |
| 2026-07-05 | Recorded PRE-EXISTING follow-up (delivery-council primer; NOT introduced or worsened by this wave): DELETE/UPDATE prose-with-connective can mint a confidently-wrong embedded edge. **PARTIALLY FIXED by R8** — INTERIOR-prose half closed; TRAILING-prose half remains a residual (added at operator direction after delivery review — AC-9). | `jdbc.update("delete the row from cache")` bound `cache` as `writes` at LITERAL_DERIVED — `delete` was in the `1p9qf`/`1p9qi` sniff set before this wave, and the `from <noun>` connective the safety premise relies on is present in ordinary prose. FIX 1 hardened only the `truncate` arm; the DELETE/UPDATE/INSERT arm is the same class. **R8 closes it via interior-vs-trailing ERROR discrimination** (helper `_sql_embedded_has_interior_error`): a delete/update/insert candidate is refused when its earliest ERROR node starts BEFORE the earliest `object_reference` (the target) — the prose signature (`delete the row from cache`'s interior `the row` ERROR before `from cache`). The discriminator was **empirically validated CLEAN against the live tree-sitter-sql grammar before ship** (STEP-1 AST dump): every valid trailing dialect (`RETURNING`, `ON CONFLICT`, unmodeled `USING`/option tails, CTE-led DML, subqueries) leaves any ERROR AFTER the target and is KEPT, so no blanket any-ERROR gate was needed (which would have dropped valid dialect). TRUNCATE keeps FIX 1's any-ERROR gate (its minimal grammar cannot separate interior from trailing; the `RESTART IDENTITY` limitation stays). Fineract census re-run: `dirty_prose_refused=0`, bound edges 11→12 — zero valid-SQL over-rejection. The TRUNCATE `RESTART IDENTITY` limitation is NOT subsumed (R8 scopes to the non-TRUNCATE arm to preserve interior/trailing precision). | Fix now in this wave (originally rejected as out of the bounded-follow-up scope; RE-SCOPED IN at operator direction after delivery review to close the confidently-wrong vector). Blanket any-ERROR gate for delete/update/insert (rejected: STEP-1 validation showed valid trailing dialect the grammar under-models would be dropped — interior-vs-trailing is the precise signal). Extend the gate to SELECT/WITH/MERGE prose (rejected: SELECT-prose is not cleanly discriminable — `select all items from inventory list` parses without an interior ERROR — and reads are excluded from clustering; recorded as a lower-stakes residual). |
| 2026-07-05 | Recorded RESIDUAL (R8 adversarial re-review): TRAILING-prose DELETE still mints a confidently-wrong write; NOT fixed in this wave. | The R8 re-review confirmed `delete from sessions periodically` / `delete from orders yesterday` / `delete from users who left` at a `jdbc.update(...)` sink still bind their real tables as `writes` at LITERAL_DERIVED, because `DELETE FROM t` is a complete valid statement and the trailing English parses as a trailing ERROR — parse-shape-indistinguishable from a valid unmodeled trailing clause (`RETURNING`, MySQL `ORDER BY`/`LIMIT`). R8's interior-vs-trailing rule closes only the interior half (`delete the row from cache`). This subclass is PRE-EXISTING (in the `1p9qf`/`1p9qi` sniff set; R8 is monotonic and introduces/worsens nothing) and left as an honest recorded residual rather than closed with a fragile trailing-keyword allowlist (which has its own holes — `delete from sessions where expired and stuff` leads with a real `WHERE`). Flagged to the operator; a robust embedded-prose-vs-SQL discriminator across trailing cases (all DML arms) is a dedicated follow-up candidate (`1rs45` or a new change), NOT a bounded in-wave fix. | Trailing-keyword allowlist gate (rejected: fragile — incomplete keyword set + prose that includes SQL keywords like `WHERE` still slips; each speculative gate this wave surfaced its own finding). Block the wave on it (rejected: pre-existing, not worsened, and fundamentally hard — the interior fix is a real net improvement shippable now). |


## Risks


| Risk | Mitigation |
| --- | --- |
| A point fix (R1–R5) subtly changes an existing exact-set expectation. | Each fix carries its own flip test; the full existing exact-set suite must stay green UNMODIFIED except where a test encoded the buggy behavior (fixed consciously, documented). |
| R4 normalization collides two differently-quoted names onto one external id and wrong-binds. | Normalize only the external-id string; binding stays unique-match-or-drop, so a collision degrades to ambiguous-drop, never a wrong bind; add a collision test. AC-4 adds a no-regress absence check. |
| R2's `when_clause` walk mints predicate-column or alias phantoms. | `walk_reads` mints only at `object_reference` in read positions; columns/aliases are `field`-wrapped and skipped; AC-2's absence sweep pins it. |
| This work overlaps `1rs45`/`1p9q8`'s extractor regions if any is in flight. | Single-OPEN-wave rule serializes activation; coordinate merge order at wave formation (serialization point recorded). |


## Session Handoff

See `docs/agents/session-handoff.md` for current session state.
