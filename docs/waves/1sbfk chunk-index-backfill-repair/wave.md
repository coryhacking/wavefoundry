# Wave Record

Owner: Engineering
Status: closed
Last verified: 2026-07-11

wave-id: `1sbfk chunk-index-backfill-repair`
Title: Chunk Index Backfill Repair

## Objective

Repair the release-blocking chunk-index backfill defect found by field testing RC 1.12.0: the reconcile projects a `tags` column that no production Lance table has ever had, fails on every real repo (including this one), and is swallowed invisibly — leaving the FTS/registry delta-only while health reads `ok`. When this wave closes, the backfill is schema-tolerant and heals stores in place, the primary fixtures mirror the production schema, the one-time diagnostics persist to disk, and `wave_index_health` flags under-covered chunk indexes. Late admission (operator-directed, post-field-verification): the `code_lexical` MCP tool — direct BM25 exact-token search over the FTS layer this wave made trustworthy — plus the lexical-fusion interpretation guidance (seed-211/guru/spec) the field retest showed was missing. Everything folds into 1.12.0 before it ships.

## Changes

Change ID: `1sbfj-bug chunk-index-backfill-schema-tolerance`
Change Status: `implemented`

Change ID: `1seiz-enh code-lexical-fts-search-tool`
Change Status: `implemented`

Completed At: 2026-07-11

## Wave Summary

Wave `1sbfk` (Chunk Index Backfill Repair) delivered two changes: Chunk-Index Backfill Fails Permanently on Every Real Lance Table (Schema-Tolerant Fetch + Visibility) and `code_lexical` — Direct BM25 Exact-Token Search Over the Indexed Chunk Corpus. Notable adjustments during implementation: `code_lexical` — Direct BM25 Exact-Token Search Over the Indexed Chunk Corpus: Verification complete: full suite 4,854 OK — five consecutive clean passes; two earlier intermittent test_indexer failures reproduced ONLY while concurrent live index builds ran on this machine (the code-index recovery `--rechunk` and post-edit-hook reindexes) and never in quiescent runs — machine-contention flake, not a code defect (no traceback captured; watch item). `wave_validate` clean; `server.py --dry-run` OK with 73 tool registrations including `code_lexical`. Separately: the AC-5 live-probe work EXPOSED a major pre-existing staleness defect cluster (code edits invisible to the code index; `content=code` update a no-op recovery) — root-caused and ticketed as `1sek8-bug content-scoped-builds-poison-code-index-freshness` (docs/plans, NOT in this wave); this repo's index recovered via `--content code --rechunk`.

**Changes delivered:**

- **Chunk-Index Backfill Fails Permanently on Every Real Lance Table (Schema-Tolerant Fetch + Visibility)** (`1sbfj-bug chunk-index-backfill-schema-tolerance`) — 7 ACs completed. Key decisions: Fix = column-intersection at the fetch (heal in place), NOT auto-escalating schema mismatch to a full rebuild.; Do NOT start emitting `tags` from the chunker in this wave.
- **`code_lexical` — Direct BM25 Exact-Token Search Over the Indexed Chunk Corpus** (`1seiz-enh code-lexical-fts-search-tool`) — 5 ACs completed. Key decisions: No regex/FTS-syntax passthrough — the safe literal-quoting query builder is reused unchanged.; Ship in 1.12.0 via late admission to wave 1sbfk rather than post-release.
## Journal Watchpoints

- Watchpoint (fixture fidelity): the primary reconcile/backfill fixtures MUST use the production Lance column set (`id, path, kind, language, lines, section, text, chunk_hash, vector` — no `tags`); the with-`tags` fixture divergence is exactly how a never-worked path shipped green, and it must not survive this wave as the primary path.
- Watchpoint (fail-safe posture): the reconcile stays fail-safe — a genuinely unreadable table (missing `id`/`path`/`text`) still skips without crashing or partially writing; only the false-negative on optional columns is removed. Follow-up verification: the skip reason must now be recoverable from the persisted log.
- Watchpoint (no scope creep into tagging): do NOT wire `_infer_tags` into chunk emission in this wave — that is a schema migration, not the repair. `tags` stays an optional column that defaults empty.
- Watchpoint (live heal gate): closure blocks until this repo's own store backfills to full Lance coverage on a real post-fix build (registry docs 20,020 / code 14,683) — fixture green alone is insufficient given the fixture-reality history here.

## Review Checkpoints

- **Prepare-phase Wave Council [prepare-council] — 2026-07-11: PASS** (moderator: wave-council; primer-depth: standard; seats: reality-checker, red-team, qa-reviewer, architecture-reviewer, performance-reviewer; rotating-seat: performance-reviewer; strongest-challenge: the fix as originally drafted healed only on CHANGED builds — the up-to-date early return (`indexer.py:3438`) exits before the reconcile (`:3884`), so the canonical field-retest scenario (upgrade, then re-check with no file edits) would show no heal and the fix would look like it failed; resolved by amending Req 2/AC-6 in-session with the zero-change fall-through (cheap coverage probe — one SQLite count + one Lance `count_rows()` per table — gating a reconcile run from the early-return path); strongest-alternative: emit `tags` from the chunker so the projection becomes valid — rejected as a fleet-wide schema migration (chunk-hash and Lance-schema changes forcing full re-embeds) to populate a column derivable from `path` at query time; recorded in the change doc Decision Log)
- Council seat notes: reality-checker — every load-bearing claim verified against live state THIS session: local Lance schemas are tag-less (`['id','path','kind','language','lines','section','text','chunk_hash','vector']`, probe output), this repo's own store is broken the same way as the field repo (registry docs 598/20,020, code 1,310/14,683), `_fetch_rows` hardcodes the 8-column projection (`indexer.py:2438`), `chunker.py` imports `_infer_tags` and never calls it (the schema can never gain `tags` from production writes), the early return precedes the reconcile, the fixtures hand-build rows WITH `tags` (`test_fts_lexical_layer.py`), and the store's coercion helpers (`_row_tags`/`_row_lines`, `index_state_store.py:763`) already default missing keys — so the store itself needs zero tolerance changes. red-team — pressed three ways: (1) could empty `chunk_hash` defaults churn incremental drift compares? No — `chunk_hash` exists in every real schema (field + local); its optionality is defensive, pinned by the production-schema fixture. (2) Could the coverage probe misfire on legitimately-small stores (fresh repo, few files)? The threshold must be proportional (material under-coverage), not absolute, and a cold store is expected to reconcile anyway. (3) Concurrent bounded-log appends from background indexer spawns must not corrupt — best-effort try/except append, size-capped, never fails a build. qa-reviewer — the fixture-reality divergence is itself a named defect (AC-2): the primary end-to-end fixtures MUST use the production column set, with-`tags` demoted to a secondary variant; the zero-change heal path needs its own fixture (AC-6), and the live heal of this repo's store is a close gate (AC-7) precisely because fixture-green lied here once already. architecture-reviewer — column-intersection at the fetch keeps the schema contract where it belongs (Lance is authoritative; the store is derived and coercion-tolerant); the fail-safe posture survives (required-column guard keeps genuinely unreadable tables on the skip path); the persisted-log requirement fixes a diagnosability hole the field investigation hit twice; no builder-version or store-schema-version bump needed (no shape change — a repair plus visibility). performance-reviewer (rotating) — the zero-change probe adds two metadata count reads per table to the post-edit-hook hot path, acceptable; the reconcile itself only fires on detected under-coverage; the one-time fleet-wide heal (a full FTS backfill on first post-upgrade build) is the intended cost and is bounded by SQLite insert throughput (~20k rows here, well under previously measured provisioning times). seat_agreement: unanimous after the zero-change amendment.
- AC priority: confirmed at prepare as proposed (AC-1..7 all required). Product-owner acknowledgment: operator-directed 2026-07-11 — release-blocking fix folded into the held 1.12.0 ("The fix wave goes into 1.12.0 before it ships"); root cause corrected in-session from the operator's second field report.
- **Late-admission review [1seiz, prepare-council-addendum] — 2026-07-11: PASS** (moderator: wave-council; abbreviated single-change addendum, seats: reality-checker, red-team, architecture-reviewer; operator-directed admission "let's do the new tool before we release 1.12". reality-checker — the wrapped API exists and is field-verified as of this wave (`fts_search` in `index_state_store.py` with kind filter and literal-quoting query builder; two field repos just exercised the layer end-to-end), and the motivating need is documented in the retest report (investigator resorted to raw sqlite to inspect the lexical layer). red-team — pressed release risk: the tool is additive (no existing path changes), read-only, and reuses the hostile-query-safe expression builder; the one genuine risk is response bloat on prevalent tokens, bounded by the limit cap + per-result text cap (AC-1); also pressed "zero results on a broken store reads as absent-from-corpus" — closed structurally by AC-3's coverage diagnostic. architecture-reviewer — naming and routing fit the established taxonomy (`code_keyword` = live files exact, `code_pattern` = regex, `code_search` = hybrid semantic, `code_lexical` = ranked indexed-corpus exact-token); no store or index shape change; the docs weave (AC-4) satisfies the new-primitive discoverability rule. AC priority confirmed as proposed (all required). seat_agreement: unanimous.)

## Review Evidence

- wave-council-delivery [1seiz addendum]: approved (2026-07-11 — abbreviated single-change delivery pass, seats: code-reviewer, qa-reviewer, red-team. **code-reviewer** — `code_lexical_response` validates and caps every input (limit hard-capped 50, text capped 700 with `text_truncated`), reuses the fusion path's literal-quoting expression builder via `fts_search` (no new query surface), reuses `_state_store_health_summary` for the coverage compare (one implementation of "covered", no drift), and every degrade path returns structured `ok` + recovery; the registration is read-only-annotated with the kwargs guard and timing injection, matching house tool shape; `server.py --dry-run` OK with the tool registered (73 registrations). **qa-reviewer** — 7 tests (`CodeLexicalToolTests`) cover ranking/merge/token-semantics (the partial-compound-identifier miss is pinned as EXPECTED behavior), kind filter, both caps, four hostile queries, invalid args, absent-store degrade, the under-covered partial-results diagnostic (field-defect shape), the healthy-zero-hit note, and the readonly registration pin; live probe on this repo returns the tool's own definition at rank 1 for its exact identifier. **red-team** — pressed the "zero results read as absence" trap (closed by the coverage diagnostic + the token-semantics note), response bloat (both caps fixture-pinned), and whether the docs weave reaches agents that matter (spec chooser/detail/decision-table, AGENTS.md list/chooser/backstop, seed-211 + rendered guru, seed-180 order, guru template + all five local agent allowlists — verified by re-render convergence). Synthesis verdict: SHIP with the wave.)
- wave-council-delivery: approved (2026-07-11 — moderator: wave-council; adversarial delivery review against code, tests, and live-build evidence; no blocking findings. **code-reviewer** — the projection fix is minimal and ordered correctly (required-column guard raises BEFORE any fetch, so the fail-safe skip path can never partially write — pinned by test); the zero-change fall-through sits after the reap and before the fast return, `expected` is honest (quiet only when the reap explains the divergence, cold stores stay calm via the provisioning path); `_record_chunk_sync_counts` batches count-record + cold-clear into one store open; `store_log` is best-effort by contract at every call site (including the `_iss.store_log`/`_store_log_safe` guards for older module loads); the `reconcile_chunk_index` signature change is a backward-compatible kwarg. **qa-reviewer** — 15 new tests: the FIRST end-to-end coverage the projection has ever had (every prior reconcile test passed a `lambda: rows` fetcher — `_fetch_rows` never ran against a real Lance table, which is precisely how a never-worked path shipped green), primary fixture pinned to the production column list, with-`tags` demoted to secondary, required-column skip + persisted reason, probe matrix (under-covered/cold/exact-drift/dup-id/absent-store/wiring pin), log append/bound/persist/never-raise, health coverage exact-first + fallback + diagnostic wiring; full suite 4,847 OK bytecode-free; `wave_validate` clean. **red-team** — pressed the coverage compare hardest and WON an amendment mid-implementation: the raw-vs-registry threshold misread this repo's +294 Lance dup-id margin as permanent under-coverage (`covered: false` on a fully-synced store + a quiet full-id-scan reconcile every zero-change build); resolved by exact-first sync-time counts recorded at each successful reconcile, threshold demoted to never-reconciled fallback — and the exact compare is strictly stronger (even 1-row genuine crash windows now heal on the next build). Also pressed sync-count poisoning (bounded: worst case is fallback behavior until the next changed build re-records) and confirmed the guard exception cannot leak partial writes. **architecture-reviewer** — no `STATE_STORE_SCHEMA_VERSION` bump and correctly so: new meta KEYS are additive kv rows, no resident table shape changed, and a bump would drop-and-rebuild every field store for nothing; the fail-safe posture survives end-to-end; docs updated (`data-and-control-flow.md` build-path step 12, `mcp-tool-surface.md` health shape); no seed weave needed — the new diagnostic arrives in-band in `wave_index_health` responses agents already read. Live-build evidence (the close gate the fixture-reality history demanded): this repo's own store healed on a real build (docs 622→20,043, code 1,310→14,389), the sabotage+zero-change scenario healed via the fall-through while printing "index is up to date", the healthy rerun kept the 1.2s fast exit, health reads `covered: true` on both tables, and the skip/heal lines persist in `.wavefoundry/logs/index-state.log`. Synthesis verdict: SHIP — the field scenario (upgrade-then-idle on a long-lived repo) now heals unattended, and the failure class can no longer hide.)
- wave-council-readiness: approved 2026-07-11 — prepare council synthesis verdict READY after one amendment (zero-change heal fall-through, Req 2/AC-6): without it the fix is invisible in the exact upgrade-then-idle scenario the field retest will run. All load-bearing claims are live-verified rather than assumed (this repo reproduces the field defect, which also supplies the AC-7 live close gate); fixture fidelity, fail-safe posture, no-tagging scope, and the live heal gate are pinned as watchpoints and ACs; seats unanimous; full synthesis in Review Checkpoints.
- operator-signoff: approved when operator confirms closure

## Dependencies

- No external wave dependencies. Folds into the held 1.12.0 release together with waves 1rsh9 and 1sbfi.
