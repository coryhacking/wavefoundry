# Wave Council synthesis — 1ymzq

Owner: Engineering
Status: active
Last verified: 2026-09-22

Current clarified-plan readiness: APPROVED, for receipt `94b63f24a5c63152fa15` as identified by the coordinator. Final delivery approval is WITHHELD pending R2 and its associated baseline/QA reconciliation. This is a readiness judgment based on present contracts and delivered controls, not permission to close the wave and not a conditional assertion that unfinished work passed.

Council synthesizer: fresh independent security/architecture reviewer, no implementation participation. Evidence considered against v2 fingerprint `/private/tmp/1ymzq-delivery-fingerprint-v2.json`, SHA256 `7d69750f1514d43c92f20b5b68749688b2776ccb5862b5bacd4c66d249808577`. Source remained unchanged. V1's six timestamp-only document edits were independently proven by reconstructing the original hashes and were superseded by v2. The coordinator supplies typed receipt identity/currentness; this synthesis did not inspect its JSON and does not substitute for mechanical authoring validation.

## Primer and merit-first synthesis

Depth FULL: containment failure contracts are trust-boundary-relevant even under the current trusted local operator model; six handler families touch public registration, shared state, reload and upgrade compatibility. The adversarial primer preceded seat conclusions. Its strongest challenge was that structural extraction can hide extra IO/error swallowing, stale re-export identities, inert test patches and an unproven retrieval claim. Its best alternative was retaining caller policy with only pure already-resolved comparison, leaving the indexer veto untouched and using invocation-time root lookup.

Evidence was weighed before attaching role labels:

1. Exact normalized correspondence for 111 functions and 32 objects supports preservation of computation and ownership. It is insufficient alone for invocation behavior; the actual reload and public-path observations supply that missing evidence.
2. Independent realistic filesystem probes distinguish root OSError propagation, candidate failure translation, indirect TechDocs refusal, missing write-target acceptance and symlink escape. Pure-comparison extra-resolution and swallowed-root-error controls fail as expected. Entire indexer.py equals HEAD, preserving its helper and callers.
3. Structural and runtime tests reject local sibling imports, eager root imports and copied cache/PID objects. Independent purge-omission mutations fail for all six new handlers; registered-tool reload probes consume modified scratch code. These findings answer the hidden-stale-identity challenge rather than relying on source inventory alone.
4. CE source-guard ordering, named patch seams, optimize contract/census and old-installed-tree upgrade fallback have executable evidence. A CE lock-skip mutation initially survived the narrow unknown-acquisition target; risk-directed expansion to the SourceGuard class found three failures. The survival is disclosed rather than reported as a first-target kill, and the bounded escalation meets the declared sweep rule.
5. R0/R1 evaluator and fixture identities agree, with 27 checked identity keys and zero comparison violations reported. The current complete framework receipt covers 9,508 tests and its hash was independently checked by a seat. These do not establish R2: index extraction intentionally changes evaluator identity, so the final receipt must be a new baseline. Leaving delivery withheld is the correct interpretation of that gap.

The current plan clarifications—53 index definitions, 12 objects, 13 conservative direct-response closure members, two named optimize-contract mock-owner changes with assertions retained, explicit pure comparison semantics and protected indexer exclusion—describe actual owner boundaries and the verification contract. They do not permit dropped filesystem policies, altered substrate semantics or a falsely comparable final evaluation. No additional design decision or scope reduction is required for readiness.

## Seat attribution and independence

Only after weighing the evidence above, the anonymized outputs map to:

| Seat | Evidence and judgment |
| --- | --- |
| Code | 111 functions / 32 objects normalized comparison; 30 structural/reload tests, 2 interlock tests and 11 optimize/census/identity checks; six killed mutants. Approves clarified readiness; R2 remains pending. |
| QA / release | 598 tests across seven files, one intentional Windows-only skip; eleven mutants including six purge omissions and the expanded CE lock control. Verified full-suite receipt and R0/R1 evidence. Approves readiness, withholds final QA until R2. |
| Security / architecture | 33 positive tests, nineteen detected mutations, indirect TechDocs/containment error contracts and state identity; final v2 fingerprint stable. Approves readiness and its bounded delivery lanes, reserves global AC completion/R2. |
| Rotating docs-contract / strongest alternative | Retaining local comparisons and handlers would reduce migration risk, but delivered exception, no-extra-resolution, identity and registered-reload controls justify the approved consolidation. No stronger alternative under the operator's goal. This perspective shares QA's context. |

There are **three independent reviewer contexts**, not six independent confirmations. Code is one, QA/release/rotating docs-contract is one, and security/architecture/primer/synthesis is one. No seat participated in implementation. Counts may overlap; no aggregate unique-test or unique-mutation total is asserted.

Material disagreements: none on clarified readiness. The narrow CE mutant survivor was an evidence limitation resolved by the declared bounded escalation. R2 is unresolved work, not a disagreement. The alternative of retaining local implementations was considered and rejected on goal fit after controls addressed its strongest migration-risk argument.

## Integrity, scope and final-delivery gate

```json
{"test_ran_without_unintended_skip":true,"public_path_reached":true,"boundary_values_realistic":true,"assertions_non_vacuous":true,"known_bad_detected":true,"known_bad_detection_method":"focused-mutation"}
```

These declarations apply to the actual bounded controls above. The single Windows-only skip was intended; native Windows filesystem behavior is not claimed. Public-path coverage includes registered MCP tools after real modified-scratch reload. Boundary values include real symlinks/missing paths and specific resolution failures. Negative controls disprove vacuity. No whole-suite rerun was demanded after passing bounded evidence; the current full-suite receipt is separately verified evidence.

Required next evidence before final council delivery: finish the authorized R2 new-baseline evaluation under the final evaluator identity, verify its report and baseline references, and obtain QA's AC-5/current-completion attestation. Do not compare R2 to R0/R1 as if evaluator identity were unchanged. If no reviewed source changes, this council needs focused R2 completion reconciliation rather than another implementation review round. Source changes require re-fingerprinting and affected-lane replay.

No blocking finding in the reviewed implementation and no new backlog item. Readiness approved; final delivery pending the named evidence.
