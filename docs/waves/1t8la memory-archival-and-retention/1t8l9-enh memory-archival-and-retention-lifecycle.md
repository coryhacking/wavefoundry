# Memory archival and retention lifecycle

Change ID: `1t8l9-enh memory-archival-and-retention-lifecycle`
Change Status: `implemented`
Owner: Engineering
Status: active
Last verified: 2026-07-23
Wave: `1t8la memory-archival-and-retention`

## Rationale

Agent memory is now the durable destination for project learning. Superseded,
rejected, and no-longer-operational records remain historically valuable, but
keeping their full bodies in the active memory directory and normal index makes
the working corpus noisier as a repository ages. Retain that history as
version-controlled records while moving it out of default retrieval, leaving
compact active pointers for high-value discovery.

## Requirements

1. A reconciled record may be archived only through an explicit, typed archival
   decision. The archived body retains its memory ID, evidence, targets,
   supersession links, and an `archived` status with an archive reason and date.
2. Archival moves the canonical body by a rename into
   `docs/agents/memory/archive/`; it must not implement the move as a
   copy/delete sequence.
3. Active memory retains a compact, project-visible pointer for each archived
   record containing the stable ID, title, kind, targets, searchable keywords,
   evidence summary, successor when present, and archive location.
4. Normal memory search, briefings, action-time advisories, graph extraction,
   and docs semantic retrieval exclude archived bodies. Normal search may return
   an active pointer; an explicit archive/history request may resolve the body.
5. Decisions, operator preferences, and still-relevant fragile-file records are
   not archive-eligible from age alone. Superseded records, rejected candidates,
   and stale tactical records have explicit eligibility rules and review cues.
6. Archive moves, pointer changes, and index/cache invalidation are serialized
   through the existing memory fence and are state-derived, restart-safe, and
   idempotent. A rerun after an interruption must converge without orphaning
   references or duplicating a record.
7. Existing source-event dispositions remain authoritative: archived records
   must not be regenerated by proposal, historical backfill, or close-time
   capture.
8. Install and upgrade preserve a valid project if archival work is pending or
   interrupted; they must not publish a mixed active/archive index state.

## Scope

**Problem statement:** current status filtering hides stale history from normal
memory tools, but full records remain in the active directory and ordinary docs
index. There is no physical archive, pointer surface, retention policy, or
archive-safe migration path.

**In scope:**

- Memory schema, parser/lint, reconcile/archive MCP contract, and active pointer
  representation.
- Archive directory traversal rules; exclusion of archived bodies from default
  docs indexing, graph extraction, advisories, and search.
- Explicit archive/history retrieval, retention review cues, and archive counts.
- State-derived rename migration, memory-state fencing, cache/index coherence,
  and upgrade/backfill integration.
- Tests covering rename semantics, interruption/retry, source-disposition
  suppression, index exclusion, pointers, and explicit history retrieval.
- Architecture, tool-surface, lifecycle-prompt, and memory-schema documentation.

**Out of scope:**

- Changing retrieval relevance or decay formulas; owned by
  `1t7ab-enh adaptive-memory-freshness-and-retrieval`.
- Automatic archival without an explicit reconciliation decision.
- Remote, hosted, or object-store archival.
- Deleting memory history or reviving per-wave journals as an archive format.

## Acceptance Criteria

- [x] AC-1: An archive action renames a valid memory body to the archive
  directory, preserves its ID and provenance, marks it `archived`, and creates
  one valid active pointer; a second invocation is a no-op.
- [x] AC-2: Default memory search, briefs, action-time advisories, semantic
  indexing, and graph extraction never expose an archived body; an explicit
  history/archive query can return its body through the documented contract.
- [x] AC-3: The active pointer is searchable by ID, title, targets, and keywords
  and resolves to the archived record without duplicating its body in the active
  corpus.
- [x] AC-4: Eligibility protects decisions, operator preferences, and relevant
  fragile-file records from age-only archival; every eligible archival category
  requires an explicit reason and reconciliation decision.
- [x] AC-5: Forced interruption at each archive transition and restart/retry
  converges to exactly one active-or-archived body, one correct pointer state,
  and no stranded live references or store rows.
- [x] AC-6: Proposal, backfill, and close-time capture do not regenerate an
  archived source event; upgrade/setup preserve a resumable, non-mixed state.
- [x] AC-7: Documentation, full framework tests, and docs validation are clean.

## Tasks

- [x] Define the archive status, metadata, pointer schema, and lint/parser rules.
- [x] Implement fenced, state-derived archive/recovery operations and explicit
  archive/history search.
- [x] Exclude archived bodies from default index/graph/advisory paths while
  indexing active pointers deliberately.
- [x] Add retention eligibility and lifecycle review guidance.
- [x] Integrate with proposal/backfill/upgrade state and memory invalidation.
- [x] Add interruption, migration, indexing, retrieval, and upgrade regressions.
- [x] Update architecture, MCP contract, memory README, and lifecycle prompts.

## Agent Execution Graph

| Workstream | Owner | Depends On | Notes |
| --- | --- | --- | --- |
| archive contract | implementer | — | Schema, pointer, parser, lint |
| archive transaction | implementer | archive contract | Fencing, rename, recovery |
| retrieval isolation | implementer | archive contract | Index, graph, advisories, history read |
| lifecycle and upgrade | implementer | archive transaction | Retention cues, backfill, upgrade |
| verification | qa-reviewer | all | Interruption and end-to-end matrix |

## Serialization Points

- `memory_records.py`, `server_impl.py`, the memory-state fence, and the docs/
  graph incremental-index paths must change as one coordinated contract.
- Archive migration must derive every step from the current filesystem and store
  state; do not rely on in-memory rename maps.

## Affected Architecture Docs

- `docs/architecture/current-state.md`
- `docs/architecture/domain-map.md`
- `docs/architecture/data-and-control-flow.md`
- `docs/architecture/search-architecture.md`
- `docs/architecture/testing-architecture.md`
- `docs/specs/mcp-tool-surface.md`
- `docs/agents/memory/README.md`
- ADR required for the local physical-archive and active-pointer boundary.

## AC Priority

| AC | Priority | Rationale |
| --- | --- | --- |
| AC-1 | required | Canonical archive invariant |
| AC-2 | required | Archive only helps if default retrieval is isolated |
| AC-3 | required | Discoverable history without full-body recall |
| AC-4 | required | Prevents loss of durable operating knowledge |
| AC-5 | required | Rename/migration safety is load-bearing |
| AC-6 | required | Lifecycle and upgrade correctness |
| AC-7 | required | Verification and contract consistency |

## Progress Log

| Date | Update | Evidence |
| --- | --- | --- |
| 2026-07-22 | Planned from operator direction and review of 1ro44, 1sufo, 1t3dm, 1t9w8, and 1t9wa. | Physical archive selected; retrieval work split into companion change. |
| 2026-07-22 | Thought: begin the ordered implementation with the archive schema and filesystem-state model, then carry the distinction through every default retrieval and indexing path before adding regressions. | Pre-implementation review passed; MCP retrieval identified `memory_records.py`, `server_impl.py`, index, graph, lint, proposal/backfill, and upgrade as the coordinated contract. |
| 2026-07-22 | Gapfill: MCP-first code and docs retrieval established the owning modules and cross-cutting contract. Targeted shell reads were then used for exact already-located regions, framework test fixtures (excluded from the semantic code index), diff inspection, and executable tests after MCP output truncation; no broad repository exploration was substituted for the retrieval pass. | `code_ask`/`code_read` implementation briefing; targeted `rg`/`sed`; framework test policy. |
| 2026-07-22 | Implemented the physical archive lifecycle: cross-process serialized, memory-fenced rename-first transaction; atomic archive metadata and compact pointer publication; protected-kind eligibility; explicit history retrieval; default advisory/docs/graph isolation; archived source-disposition suppression; and state-derived recovery across all three interruption windows. The rename-first ordering keeps every interruption state outside the active semantic/graph corpus before archive metadata is rewritten. | `memory_records.py`; `server_impl.py`; `indexer.py`; `graph_indexer.py`; lint validators; archive/source-disposition tests. |
| 2026-07-22 | Reconciled the public contract and lifecycle guidance, including the memory README, MCP spec, five architecture surfaces, accepted ADR, close/review prompts, and canonical finalize seed. Full framework suite passed 6,163/6,163 across 59 files; `wf_validate_docs` passed with no errors or warnings; all edit gates are closed; live MCP reload matched disk and refreshed 82 tools with the two changed descriptions propagated. | `run_tests.py` (6,163 OK); `wf_validate_docs`; `wf_gate_status`; `wf_reload_mcp`. |
| 2026-07-22 | Independent delivery review found two adjacent pending-archive gaps after the rename crash window: docs lint blocked without naming the retry, and unfiltered/history loads hid the source disposition so proposal/backfill could regenerate it. Repair: lint now identifies the pending state and exact reconcile recovery; the loader surfaces it only as `pending_archive_body` to unfiltered/history consumers, preserving default advisory/index isolation. Added direct lint, loader/history, proposal-suppression, and ambiguous-both-bodies regressions. | Findings `pending-archive-docs-gate-has-no-recovery` and `pending-archive-disposition-invisible`; targeted memory tests 161/161; targeted lint regression. |
| 2026-07-22 | Repair verification on the final tree: full framework suite 6,168/6,168 across 59 files, docs validation clean, MCP implementation reloaded and matching disk, and both implementer repair-start heads recorded. The chains intentionally remain blocking for independent code/QA reverification. | `run_tests.py` (6,168 OK); `wf_validate_docs`; `wf_reload_mcp`; cycle-1 repair evidence. |

## Decision Log

| Date | Decision | Reason | Alternatives |
| --- | --- | --- |
| 2026-07-22 | Use a physical, local archive plus compact active pointers. | Preserves Git rename history and removes full bodies from default retrieval. | Status-only archive: rejected because bodies remain in the active/indexed corpus. Copy/delete: rejected because it weakens history and interruption semantics. |
| 2026-07-22 | Archive only by explicit reconciliation. | Eligibility needs current semantic judgment; automatic retention can discard durable knowledge. | Background age-based archival: rejected because age alone is not authority. |
| 2026-07-22 | Keep retrieval scoring out of this change. | Archive storage/lifecycle migration has distinct correctness risks. | Combine with adaptive ranking: rejected; it obscures failures and defeats measured evaluation. |

## Risks

| Risk | Mitigation |
| --- | --- |
| Interrupted move strands references | State-derived recovery, fence, and crash-window tests |
| Archive bodies still surface through docs index | Explicit exclusion tests across docs, graph, and advisory paths |
| Retention harms durable knowledge | Protected kinds plus explicit review and history retrieval |
| Upgrade publishes mixed state | Reuse resumable memory backfill/index-publication discipline |

## Session Handoff

See `docs/agents/session-handoff.md` for current session state.
