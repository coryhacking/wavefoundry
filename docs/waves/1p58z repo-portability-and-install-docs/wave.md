# Wave Record

Owner: Engineering
Status: closed
Last verified: 2026-06-13

wave-id: `1p58z repo-portability-and-install-docs`
Title: Repo Portability And Install Docs

## Objective

Make a freshly-cloned wavefoundry repo work for contributors with no per-machine fixups, and give install-related docs one clear home. Today the rendered editor/MCP surfaces are committed with the author's absolute paths (so hooks and the MCP command break on clone), and install assets are scattered across four framework subtrees with a framework↔project duplicate. When this wave closes, every tracked file uses project-relative paths and install docs live in a single canonical location.

## Changes

Change ID: `1p590-maint project-relative-tracked-surfaces`
Change Status: `implemented`

Change ID: `1p591-ref install-doc-home-consolidation`
Change Status: `implemented`

Change ID: `1p5b4-ref remove-canonical-names-rename-manifest`
Change Status: `implemented`

Change ID: `1p5c4-bug guard-oversized-files-indexing`
Change Status: `implemented`

Completed At: 2026-06-13

## Wave Summary

Wave `1p58z` (Repo Portability And Install Docs) delivered 4 changes: Project-relative paths in tracked editor/MCP surfaces, Consolidate install assets under framework/install/ + discoverability index, Remove the canonical-names rename manifest (retire in 1.6), and Guard the indexer against pathologically large files. Notable adjustments during implementation: Project-relative paths in tracked editor/MCP surfaces: Implemented: `launcher_command` emits project-relative (dropped `repo_root`-absolute); Cursor/Junie MCP switched to the `.wavefoundry/bin/mcp-server` wrapper; the now-dead absolute-venv helper `_venv_python_path`/`_VENV_DEFAULT` removed; 5 committed surfaces regenerated by the renderer; `test_graph_indexer` absolute fallback derived from `__file__`; benchmark report JSONs untracked + gitignored. Renderer tests updated.; Consolidate install assets under framework/install/ + discoverability index: Initial (documentation-only) pass: added `docs/references/install-assets.md`, `test_shipped_reference_docs.py`, and the `build-and-verification.md` pointer. No asset moved.; Consolidate install assets under framework/install/ + discoverability index: **Operator-directed re-scope → real consolidation.** Moved `install-block.md` (from `framework/release/`) and `install-log-format.md` (from `framework/docs/references/`) into `framework/install/`; moved `scan-findings-format.md` up to `framework/docs/` (removed the one-file `references/` subfolder). Updated build_pack constant+docstring, seeds 012/160, test_build_pack, README, release-flow; rewrote the parity test to explicit pairs + a `*-format` coverage guard; updated the index. Canonical `docs/references/` copies unchanged (parity preserved).

**Changes delivered:**

- **Project-relative paths in tracked editor/MCP surfaces** (`1p590-maint project-relative-tracked-surfaces`) — 5 ACs completed. Key decisions: --------
- **Consolidate install assets under framework/install/ + discoverability index** (`1p591-ref install-doc-home-consolidation`) — 5 ACs completed. Key decisions: --------; Physically consolidate framework-side install assets under `framework/install/` (not index-only).
- **Remove the canonical-names rename manifest (retire in 1.6)** (`1p5b4-ref remove-canonical-names-rename-manifest`) — 5 ACs completed. Key decisions: --------
- **Guard the indexer against pathologically large files** (`1p5c4-bug guard-oversized-files-indexing`) — 5 ACs completed. Key decisions: --------
## Journal Watchpoints

- **Sequencing:** activate after `1p4wz` closes (single-open-wave rule). Both changes are independent of `1p4wz`'s embedding/retrieval scope.
- **Framework-edit gate:** `1p590` edits `render_platform_surfaces.py` + its tests; `1p591` moves framework docs and edits the scripts that reference them (`build_pack.py` reads `release/install-block.md`; setup reads the install templates). Open `framework_edit_allowed` before edits and close immediately after.
- **Blast radius — 1p590 (load-bearing):** the renderer drives EVERY downstream install's surfaces, so a relative-path regression breaks installs, not just this repo. Verify rendered output from a non-author checkout path (fresh-clone simulation) and that a case-insensitive `git grep "/Users/"` over tracked surfaces is empty.
- **1p591 consolidates install assets under `framework/install/` (operator-directed).** The framework↔project `*-format.md` pairs are an intentional shipped-template↔provisioned-canonical relationship (`1p4dc`/`1p455`) — the canonical `docs/references/` copies must NOT be removed. Only the framework-side *source/template* copies move, and EVERY consumer (`build_pack` constant, seeds 012/160, `test_build_pack`, README, release-flow) must move with them in the same change. Plus the `docs/references/install-assets.md` index and the byte-identical parity test. Gate on the full suite + docs-lint + an old-path straggler grep.
- **1p5b4 is a LATE admission to an active wave + a published-contract change.** The prepare-council verdict covered only `1p590`/`1p591`; `1p5b4` must get a focused review before close. It pulls the `removed_in: 2.0.0` config-key contract forward to 1.6 (record in an ADR + CHANGELOG) and removes docs-lint alias machinery + the `server_impl` runtime fallback — keep a hardcoded one-shot upgrade convergence so skip-version operators aren't stranded. Gate on the full suite + docs-lint + an import-grep for `canonical_names`.
- **1p5c4 is a LATE admission (operator feedback) + a graph-version bump.** Not covered by the prepare-council verdict — needs the same focused pre-close review as `1p5b4`. Bumps `GRAPH_BUILDER_VERSION` 29→30, which forces a one-time full graph re-extract on the next index (expected cost; converges oversized-file pruning). Defaults: 5 MB hard skip, 2 MB tree-sitter cap, both overridable in `docs/workflow-config.json` (`indexing.max_file_bytes` / `indexing.max_treesitter_parse_bytes`, `0` disables).
- **Decision (portability form):** operator chose project-relative paths everywhere in tracked files; only gitignored files may stay machine-specific, and the operator chose to keep the surfaces committed (portable) rather than gitignore them. Prefer relative paths; use `$CLAUDE_PROJECT_DIR` only where a tool cannot resolve a relative command — confirm per-tool support at prepare.
- **Prepare-phase Wave Council readiness review** is the last step before this wave is ready to activate.

## Review Evidence

- wave-council-readiness: READY-WITH-NOTES — readiness sign-off recorded 2026-06-13. Both changes are coherent and implementable; `1p591` was reality-corrected during prepare (see below). Notes: (1) **1p590 — confirm per-tool relative-command support before editing each surface**: Claude Code resolves `$CLAUDE_PROJECT_DIR`; Cursor/Copilot/Junie relative-vs-variable support must be verified as the first implement task (AC-2 fresh-clone resolution per detected platform is the gate; the renderer already branches per platform, so the path form may differ per tool). (2) **Verify renderer output from a non-author checkout** — the renderer drives EVERY downstream install's surfaces, so a relative-path regression breaks installs, not just this repo. (3) **1p591 is documentation-only** (no relocation): keep the byte-identical shipped-template↔canonical invariant test (AC-3) as the durable guard. No security/secrets/binding-faithfulness concerns.
- wave-council-delivery: READY-WITH-NOTES — delivery sign-off recorded 2026-06-14, with explicit focus on the three late additions the prepare-council never saw (`1p591` operator re-scope, `1p5b4`, `1p5c4`). Verified at delivery: full framework suite **3096 OK**; `wave_validate` docs-lint ok; a real full GPU rebuild of the semantic index succeeded with all four changes in place (CoreML embedders, ~2 min, graph re-extracted at builder version 30, zero index-shard skip noise after the gitignored-dir prune). Strongest challenges, all MITIGATED: (a) `1p5b4` pulls the published `removed_in: 2.0.0` config-key removal forward to 1.6 — the one-shot upgrade convergence rewrites legacy keys to canonical so upgrading targets are unaffected; the only break is a project that authors a legacy key post-1.6 and never upgrades (documented in ADR `1p5be` + CHANGELOG). (b) `1p5c4` bumps `GRAPH_BUILDER_VERSION` 29→30, a known one-time downstream full graph re-extract (validated live). (c) `1p590` ships bare-relative hook commands whose firing in Cursor/Copilot/Junie is operator-verified (AC-2); the Claude path that enforces docs-lint is the one exercised here. Standing note: the wave scope ballooned past its title (portability + install + canonical-names + indexing robustness) — sound work, but future waves should stay tighter.
- operator-signoff: approved — operator authorized closure 2026-06-14 (review → commit → close).

## Review Checkpoints

- **Prepare-phase Wave Council [prepare-council] — 2026-06-13: PASS WITH NOTES** (moderator: wave-council; primer-depth: standard; seats: red-team, architecture-reviewer, security-reviewer, qa-reviewer, reality-checker, docs-contract-reviewer; rotating-seat: docs-contract-reviewer; strongest-challenge: 1p590's per-tool relative-command resolution is unverified — a relative hook command an editor cannot resolve would silently break that editor's hooks, worse than an absolute path that at least works on the author's machine — MITIGATED because AC-2 requires fresh-clone resolution per detected platform and the renderer already branches per platform, so the emitted form can be relative where supported and CLAUDE_PROJECT_DIR/equivalent where required, with per-tool confirmation as the first implement task gated by AC-2 and the case-insensitive grep gate AC-3; strongest-alternative: gitignore the rendered surfaces and regenerate them at setup, rejected by the operator who chose committed portable forms so a fresh clone works with no setup step — also recorded, 1p591 was reality-corrected during prepare from "dedup the install-doc duplication" to documentation/discoverability only, because the framework-vs-project install-log-format.md pair is the intentional shipped-template to provisioned-canonical relationship (1p4dc) whose asset locations are load-bearing for build_pack and provisioning, and docs-contract-review confirms the new index plus byte-identical guard test must stay docs-lint-clean without breaking the lint-required install prompt or prompt-surface manifest, gated by AC-5)

- **Delivery-phase Wave Council [delivery-council] — 2026-06-14: PASS WITH NOTES** (moderator: wave-council; primer-depth: standard; seats: red-team, architecture-reviewer, security-reviewer, qa-reviewer, reality-checker, docs-contract-reviewer; rotating-seat: docs-contract-reviewer; strongest-challenge: three late additions changed downstream/runtime contracts without a prepare-council pass — `1p5b4` pulled the published 2.0.0 config-key removal to 1.6, `1p5c4` bumped the graph builder version (forced re-extract), `1p590` ships bare-relative hook commands unverified on Cursor/Copilot/Junie — MITIGATED by the one-shot convergence migration (legacy configs auto-converge on upgrade; documented in ADR 1p5be + CHANGELOG), the version bump being a known one-time re-extract validated by a live full GPU rebuild, and the Claude/wrapper paths being the proven forms with per-editor hook firing reserved as operator verification (AC-2); architecture/security/qa/docs-contract seats clean — canonical docs/references copies preserved with a byte-identical parity test, no secrets/binding changes, suite 3096 OK with per-change tests, ADR + index + pipeline doc updated; strongest-alternative: split the three late additions into their own prepare-gated waves, rejected by the operator who folded them, with this focused delivery review substituting for the missed prepare pass; reality-check: the wave scope ballooned past its title and is acknowledged as a grab-bag — future waves stay tighter)

## Dependencies

- No external wave dependencies.
