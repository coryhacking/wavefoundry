# Wave Record

Owner: Engineering
Status: closed
Last verified: 2026-06-07

wave-id: `1p41l graph-tools-field-feedback-round-5`
Title: Graph Tools Field Feedback Round 5

## Objective

Round-5 grounding of the Aceiss graph-tools field eval found ~80% already shipped by prior waves (`130rj`, `130su`, `130tw`, `1p2q4`). This wave closes the small set that is genuinely actionable: the in-repo guidance/render drift it surfaced (stale `docs/agents/guru.md`, a missing betweenness anti-pattern note, an incomplete tool-surface spec). It also scoped one net-new query-layer tool (`code_risk_score`) behind a value gate — which, when implemented and measured against the real graph, **failed AC-8 (degree proxy) and was gated out**. The wave therefore ships its high-certainty doc/render-drift fixes plus the off-theme dashboard accounting fix (`1p466`), and defers all net-new graph tools — exactly the prepare-council's strongest-alternative. (`code_coverage_gap`, `code_graph_path` narrate mode, and `code_risk_score` were all admitted then deferred/gated-out — see Review Checkpoints.)

## Changes

Change ID: `1p41m-doc complete-graph-tools-agent-guidance`
Change Status: `implemented`

Change ID: `1p41n-maint regenerate-guru-role-doc`
Change Status: `implemented`

Change ID: `1p466-bug dashboard-changed-files-untracked-dir-accounting`
Change Status: `implemented`

Change ID: `1p41q-enh code-graph-path-narrate-mode`
Change Status: `deferred`

Completed At: 2026-06-08

## Wave Summary

Wave `1p41l graph-tools-field-feedback-round-5` (Graph Tools Field Feedback Round 5) delivered 5 changes: Complete Graph-Tools Agent Guidance and Tool-Surface Spec, Regenerate docs/agents/guru.md From Seed-211 to Clear Render Drift, Dashboard Miscounts Changed Files And Added Lines For Untracked Directories, Add code_risk_score MCP Tool (Composite Symbol Risk — Blast-Radius × Degree, Extensible), and Add narrate Mode to code_graph_path (Deterministic Plain-English Per-Hop Explanation). Notable adjustments during implementation: Dashboard Miscounts Changed Files And Added Lines For Untracked Directories: Implemented the `-z` NUL-delimited fix: added `_iter_porcelain_z` (raw-path, rename-origin-consuming iterator) replacing the dead `_parse_porcelain_path`; switched both `git status --porcelain` calls to `--untracked-files=all -z` and the tile `ls-files` call to `-z`; tile file count + dialog list now share the iterator. Updated 3 `GitStatsParsingTests` mocks to the new arg-tuples + NUL values; added `GitUntrackedDirAccountingTests` (real git repo: untracked dir w/ spaced + non-ASCII names + known counts, tracked modify + tracked rename).; Dashboard Miscounts Changed Files And Added Lines For Untracked Directories: **Adversarial verification caught a self-introduced HIGH bug + fixed it.** A 3-lens verification workflow found that `collect_git_stats`'s `run()` helper `.strip()`s stdout — which corrupts the new NUL-delimited `-z` payloads at the blob boundary (a leading-space XY record like `" M a"` or a leading-space untracked filename), dropping/mis-parsing the first token and re-diverging the tile from the dialog (the exact invariant this fix targets). Added a `run_raw()` (unstripped) reader and routed the two `-z` calls through it; added `GitStatsZStripSafetyTests` (2 real-git regression cases: unstaged-modify of a 1-char file as the first record; leading-space untracked filename) that fail pre-fix and pass post-fix.

**Changes delivered:**

- **Complete Graph-Tools Agent Guidance and Tool-Surface Spec** (`1p41m-doc complete-graph-tools-agent-guidance`) — 3 ACs completed (seed-211 betweenness anti-pattern note citing `betweenness_dominated_by_generated`; `mcp-tool-surface.md` wave_graph_report section list completed with `communities`+`betweenness`).
- **Regenerate docs/agents/guru.md From Seed-211 to Clear Render Drift** (`1p41n-maint regenerate-guru-role-doc`) — 4 ACs completed. Key decisions: regenerate from the seed rather than hand-patch missing sections (seed is the single source of truth); guru.md body is byte-identical to the seed role body.
- **Dashboard Miscounts Changed Files And Added Lines For Untracked Directories** (`1p466-bug dashboard-changed-files-untracked-dir-accounting`) — 5 ACs completed. Key decisions: `--untracked-files=all -z` NUL-delimited enumeration on both `git status` calls + `ls-files -z` (reconciles tile count / dialog list / added-line total incl. non-ASCII, spaced, renamed); a `run_raw` (unstripped) reader fixed a verification-caught `.strip()` corruption of the `-z` payload.
- **Add code_risk_score MCP Tool (Composite Symbol Risk — Blast-Radius × Degree, Extensible)** (`1p41o-enh code-risk-score-tool`) — **deferred: gated out at AC-8** (degree proxy on 81% of real modules — the call graph lacks cross-file `Class.method` edges, so the blast-radius term is near-constant). Implemented + measured against the real graph, then the query-layer method was reverted. **Relocated to wave `1p47e` (2026-06-08) for a gated re-attempt behind `1p470` (cross-file resolution);** the full gate-out evidence travels with the change doc.
- **Add narrate Mode to code_graph_path (Deterministic Plain-English Per-Hop Explanation)** (`1p41q-enh code-graph-path-narrate-mode`) — **deferred**: its use case is already served by existing guru recipes + structured `path_edges`/`traversal_direction`; the realizable scope (deterministic per-hop templating, no LLM) is convenience over a redundant restatement.
## Journal Watchpoints

- **Sequencing (blocking):** `1p41n` (guru.md regen) is blocked by `1p41m` (seed-211 betweenness note) — regenerate `guru.md` only after the seed carries the note, so the render is correct (seed-first ordering).
- **Gate watchpoint:** `1p41m` and `1p41n` touch framework seed `211-guru.prompt.md` — require `seed_edit_allowed`; close the gate immediately after each edit. (Both implemented in the originating session.)
- **`1p41o` GATED OUT (resolved):** `code_risk_score` was implemented in the query layer and measured against the real graph; AC-8 returned NO-GO (degree proxy on 81% of modules — the call graph lacks cross-file `Class.method` edges, so the blast-radius term is near-constant). The query-layer method was **reverted** (`graph_query.py` byte-clean vs HEAD); the change is **retained in the wave marked `deferred`** (terminal status, dashboard-visible) so the decision + evidence stay in context. No `server_impl.py` registration was made and `GRAPH_BUILDER_VERSION` was untouched. Nothing to verify at close beyond confirming `graph_query.py`/`server_impl.py` carry no `risk_score` residue.
- **`1p466` (off-theme, batched):** `1p466-bug` (dashboard untracked-dir file/line accounting) edits `dashboard_lib.py` only → `framework_edit_allowed` gate (opened + closed during implement). Independent of the graph tools. Off-theme — surface it as a distinct line item in the close summary. Implemented + tested (150-test dashboard suite green).
- **Deferred follow-up:** (a) **real test-coverage** — ingest coverage reports (jacoco/coverage.py/lcov) as per-symbol covered/uncovered tags, then a true `code_coverage_gap` on high-degree uncovered symbols (separate, larger wave: new data pipeline + per-language formats + CI artifact dependency). The path-based proxy in the removed `1p41p` was rejected as too noisy on high-degree symbols (tested indirectly). (b) **optional** `undocumented_hotspots` `wave_graph_report` section — high-degree code nodes with no incoming `doc_references_code` edge, reusing `orphan_docs` machinery; the salvageable doc-gap half of the removed tool. (c) the two high-effort Aceiss tools (`code_advice_sites` §3.1, `wave_graph_diff` §3.4) — need new extraction / ref-snapshot infra. (d) **`code_graph_path` narrate mode** (`1p41q`, deferred 2026-06-08) — a deterministic per-hop English rendering of a path; deferred because the existing guru recipes (`guru.md:245` AOP/backward-flow, `:281` relation-inspection) already teach reading paths from structured `path_edges`/`traversal_direction`, so narration adds convenience over a redundant restatement. Retained in the wave marked `deferred`. (e) **`code_risk_score`** (`1p41o`, gated out 2026-06-08) — a composite blast-radius × degree symbol-risk ranking; implemented + measured, but AC-8 found it collapses to a `fan_in` degree proxy on 81% of real modules because the call graph lacks cross-file `Class.method` `calls` edges (the blast-radius term is near-constant). Plan + full evidence retained in the wave marked `deferred`. **Its blocker is the same as (a)'s root cause:** the call graph's cross-file resolution. Revisit `1p41o` only after a separate extractor wave gives reliable cross-file method-call edges — at which point `affected_file_count` would actually vary and the composite could add signal. Revisit each as a separate wave (or re-admit) if a field team requests one with a concrete use case.

## Review Evidence

- wave-council-readiness: approved 2026-06-07 — PASS WITH IN-SESSION FIXES (moderator: wave-council; depth: standard; seats: red-team primer, architecture/API-first, qa-reviewer, reality-checker; rotating-seat: best-alternative [defer-the-tools]; must-fix-count: 0; recommended-count: 2 applied in-session [R-1: MCP wrapper-layer regression-test AC added to 1p41o/1p41p/1p41q as AC-7 — carry-forward lesson from 130rj/130ol; R-2: code_churn_risk scoring marked a validation hypothesis with degree-dominance risk + AC-8 validation gate]; strongest-challenge: the three query-layer tools are speculative net-new surface scoped from an ~80%-stale field report — code_churn_risk formula risks degree-dominance, code_coverage_gap "untested" is a weak path proxy, code_graph_path narrate is marginal by its own rationale; strongest-alternative: ship only the doc-gap fixes 1p41m/1p41n now and defer all three tools until a field team requests one with a concrete use case — partially incorporated (tools admitted as scoped/planned, not implemented this session, per the operator's fix-gaps/scope-tools split); falsification: working verdict PASS; strongest counter is tool-speculativeness; does not change the verdict because the tools are gated behind a later implement decision, their value-uncertainty is documented and now validation-gated (AC-8), and the high-certainty doc/render-drift fix is unblocked and ready; verdict: PASS — admissible for implementation)
- wave-council-delivery: approved 2026-06-08 — PASS (moderator: wave-council; depth: standard; seats: architecture/API-first, qa-reviewer, security-reviewer, reality-checker; rotating-seat: reality-checker [value/scope]; backed by a 3-lens adversarial verification workflow [zparse-edge-cases · regression+since · gate-out-cleanliness]; must-fix-count: 0 [one HIGH found-and-fixed in-session]; shipped scope: `1p41m`/`1p41n` doc-gap fixes [guru.md regen from seed-211, betweenness anti-pattern note, mcp-tool-surface section list] + `1p466` dashboard untracked-dir `-z` accounting fix; qa: PASS — `1p466` covered by real-git fixtures incl. non-ASCII/spaced names + tracked rename [`GitUntrackedDirAccountingTests`] and the verification-found `.strip()`-corruption edge cases [`GitStatsZStripSafetyTests`]; full framework suite green [2792 tests]; architecture: PASS — `1p466` confined to `dashboard_lib.py` git enumeration, no boundary change; `1p41o` gate-out left `graph_query.py` byte-clean [no dead code], no `server_impl.py` registration, `GRAPH_BUILDER_VERSION` untouched; security: nil — read-only git enumeration, no new surface; reality-checker [rotating]: the wave deliberately ships LESS than originally scoped — both net-new tools deferred/gated — but that is the value gate working as designed, not an empty wave: real shipped value [the guru.md guidance regen + a genuine dashboard accounting bug fix] plus durable, evidence-backed deferral decisions; strongest-challenge: a wave whose two headline net-new tools both ended deferred/gated risks reading as low-value churn — counter: `1p41o` was implemented and empirically measured [AC-8: degree proxy on 81% of modules, root-caused to missing cross-file `Class.method` edges], producing a grounded no-go + the single highest-leverage graph-extraction follow-up, and the verification round caught a real self-introduced HIGH bug before it shipped; the doc-gap fixes are concrete shipped value; does not change the verdict; sensor note: `wave_review max_severity:high` is a false positive from review-evidence prose ["high-certainty"], not an unresolved finding [no computational sensors registered; the one real HIGH was fixed]; verdict: PASS — admissible for close pending operator signoff)
- operator-signoff: approved 2026-06-08 — operator confirmed close ("Let's close the wave.") after the pre-close review (PASS, no blocking findings; all provenance drift corrected in-session) and confirmation that the dashboard deferred-counting is consistent (deferred tasks + ACs both read as open while the wave is open).

## Review Checkpoints

- **Prepare-phase Wave Council [prepare-council] — 2026-06-07: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, architecture-reviewer, qa-reviewer, reality-checker, docs-contract-reviewer; rotating-seat: docs-contract-reviewer; strongest-challenge: the three query-layer tools are speculative net-new surface scoped from an ~80%-stale field report — `code_churn_risk` formula risks degree-dominance, `code_coverage_gap` "untested" is a weak path proxy, `code_graph_path` narrate is marginal by its own rationale; strongest-alternative: ship only the doc-gap fixes `1p41m`/`1p41n` now and defer the three tools until requested with a concrete use case — partially incorporated, since the tools are admitted as scoped/planned, not implemented this session, per the operator's fix-gaps / scope-tools split; docs-contract-reviewer (rotating): seed-first ordering holds (`1p41n` depends on `1p41m`), guru regeneration is faithful-from-seed with no hand-authored drift (AC-4), the note references the exact shipped field `betweenness_dominated_by_generated`, and each tool enumerates its doc surfaces (`mcp-tool-surface.md` + prompt-surface-manifest + seed-211); must-fix: 0; recommended applied in-session: R-1 wrapper-test AC-7 on all three tools, R-2 churn validation gate AC-8)
- **Post-readiness scope removal — 2026-06-07:** `1p41p code_coverage_gap` removed from the wave per operator decision. Grounding confirmed the tool is technically buildable (test→production edges exist in the graph — `include_tests` filtering at `server_impl.py:12644` proves test nodes are present and reference production symbols), but its path-based "untested" proxy ("no direct reference from a test-path file") is weakest exactly on its target population: high-degree symbols are typically tested *indirectly* via integration tests / public APIs and would systematically false-flag as untested; it also cannot measure coverage depth. Decision: defer real test-coverage to a separate wave that ingests actual coverage reports (the only trustworthy signal), and note the salvageable doc-gap half (`undocumented_hotspots`) as an optional `wave_graph_report` section (both captured under Journal Watchpoints → Deferred follow-up). This is a scope *reduction*, not addition — no council re-review required; logged per stage-gate hygiene.
- **Tool rename — 2026-06-07:** `code_churn_risk` → `code_risk_score` per operator, reframed as a general composite (degree + blast-radius now, extensible). "churn" implied git-commit churn (temporal); "refactor" was too narrow (blast-radius risk applies to any edit, not just behavior-preserving refactors). The change-doc slug is now `1p41o-enh code-risk-score-tool`; the council verdict lines above retain the original name as a point-in-time record. Full rationale in the `1p41o` Decision Log. Naming/scope-framing change only — no council re-review required.
- **Change admission + readiness — 2026-06-08:** `1p466-bug dashboard-changed-files-untracked-dir-accounting` admitted per operator direction (consolidated with this wave's resume rather than a standalone wave; off-theme from graph tools but batched). Readiness: **lightweight prepare-council PASS** (moderator: wave-council; seats: red-team, architecture-reviewer, security-reviewer, qa-reviewer, reality-checker; rotating-seat: performance-reviewer; strongest-challenge: does `--untracked-files=all` reconcile the file count + dialog list + added-line total without a `since`-path regression? — reality-checker: yes — `-uall` expands untracked dirs to individual entries so all three readouts enumerate individual files, and the dialog's untracked direct-line-count branch covers them; the flag only affects untracked listing; strongest-alternative: a shared `_changed_files()` helper [Option A, reserved]; security/architecture: nil / no boundary; performance: `-uall` per-poll cost ≈ the existing `ls-files` call; must-fix: 0; verdict: PASS). This is an addition (unlike the reduction-class changes above), so its readiness is recorded here; implement alongside or independently of the graph-tool changes.
- **Value re-assessment → defer `1p41q`, gate `1p41o` — 2026-06-08:** operator asked whether the two net-new tools carry real value and sufficient usage guidance. Grounded assessment (against the just-regenerated `guru.md`): (1) **`1p41q` `code_graph_path` narrate — deferred.** Its two headline value cases were already found unrealizable (external `::*` nodes non-transitive; no `field` edge), and the remaining forward/backward rendering is already served by existing guru recipes (`guru.md:245` reads `either`-mode paths via `traversal_direction`; `:281` directs inspecting `path_edges[i].relation`) — narration is convenience over a redundant restatement of structured data agents are already guided to read. Removed via `wave_remove_change` (relocated to `docs/plans/`). **Scope reduction — no council re-review** (same class as the `1p41p` removal above). (2) **`1p41o` `code_risk_score` — kept, but gated.** It has distinct value (a one-call *ranking* across a scope vs `code_impact`'s single-symbol blast radius), but only if non-degenerate. AC-8 elevated from "important" to a **required go/no-go**: ship only if the `risk` ranking diverges from raw `fan_in` (Spearman ρ ≤ ~0.95) — directly, or after the pre-committed rank-normalize fallback; if it stays a degree proxy, gate it out (defer like `1p41q`). New **AC-9** requires the `211-guru.prompt.md` guidance to reach the existing graph-tool recipe depth (when-to-use, the `code_risk_score` vs `code_impact` distinction, how-to-read `score_components`, an anti-pattern, a role-routing-table entry) — not the one-line pointer originally planned. AC tightening + scope reduction only; **no council re-review** (does not expand scope). Logged per stage-gate hygiene.
- **Implement phase — `1p41o` AC-8 gate run → NO-GO, gated out — 2026-06-08:** during implementation, `risk_score` was built in the query layer (`graph_query.py`) and the AC-8 go/no-go gate was run against the real project graph (6,947 nodes / 20,212 edges) across **52 in-cap modules**. Result: normalized Spearman ρ(risk, fan_in) **> 0.95 on 42/52 (81%)**; `affected_file_count` flat across all symbols in **24/52 (46%)**; the pre-committed rank-normalize fallback did not rescue it (e.g. `build_pack` 0.871→0.957, `graph_query`/`indexer`/`dashboard_server` 1.000→1.000). Adversarial self-check found the root cause: the call graph systematically lacks cross-file `Class.method()` `calls` edges (87% of `calls` edges are intra-file `EXTRACTED`), so the blast-radius term is near-constant — `GraphQueryIndex.from_root`, called from dozens of sites in `server_impl.py`, has **0** `calls` edges and `affected_files = 0`. Per AC-8 branch (c) (irreducible degree proxy → do not ship), and the operator's explicit gate authorization, **`code_risk_score` was gated out**: the query-layer method was reverted (`graph_query.py` byte-clean vs HEAD), no `server_impl.py` registration or guidance was written, and the change was relocated to `docs/plans/1p41o-enh code-risk-score-tool.md` with full evidence. This is a value-gate outcome (a scope reduction), **not a council re-review trigger**; it realizes the prepare-council's strongest-alternative (ship doc-gap fixes, defer the tools). The wave's shipped scope is now `1p41m`/`1p41n` (doc-gap fixes) + `1p466` (dashboard fix), all implemented + verified.
- **Delivery-phase Wave Council [delivery-council] — 2026-06-08: PASS** (recorded in Review Evidence as `wave-council-delivery`). Backed by a 3-lens adversarial verification workflow over the shipped diff + the gate-out: (1) the `-z` parser (`_iter_porcelain_z`) handles rename/copy origin tokens, literal `" -> "` filenames, non-ASCII/embedded-newline names without desync; (2) **one HIGH finding — `collect_git_stats` `.strip()` corrupting the NUL payload — was caught and fixed in-session** (`run_raw` + `GitStatsZStripSafetyTests`); (3) the `1p41o` gate-out is clean and its AC-8 no-go was independently re-derived against the real graph. `since`-path is improved (raw paths now resolve), and three PRE-EXISTING tile/dialog line-stat inconsistencies were logged as out-of-scope follow-ups (see `1p466` Out of scope). must-fix: 0. Verdict: PASS — admissible for close pending operator signoff.
- **Deferred changes retained in-wave (operator correction) — 2026-06-08:** the prior checkpoints relocated the deferred/gated-out tools (`1p41o`, `1p41q`) to `docs/plans/` via `wave_remove_change`. Per operator preference — a deferral decision should stay **visible in the wave**, not vanish — both were re-admitted via `wave_add_change` and set to `Change Status: deferred`. `deferred` is a first-class **terminal** change status (`wave_lint_lib/constants.py:239` `TERMINAL_CHANGE_STATUSES`), so they now render in the dashboard under this wave AND do not block close (a `deferred` change counts as terminal). Shipped code/scope is unchanged (still `1p41m`/`1p41n`/`1p466`); this changes only how the two non-shipped tools are *recorded* — provenance, not scope. `1p41p` (the earlier pre-build *rejection*) was dropped entirely — no preserved plan doc; its salvageable `undocumented_hotspots` idea lives under Deferred follow-up. **Convention going forward: mark `Change Status: deferred` in place; reserve `wave_remove_change` (relocate-to-plans) for changes that were never really part of the wave.**
- **`1p41o` relocated to wave `1p47e` for a gated re-attempt — 2026-06-08 (post-close):** operator created wave `1p47e cross-file-resolution-and-risk-score` pairing `1p470` (cross-file receiver resolution — the fix for this wave's gate-out root cause) with `1p41o` (`code_risk_score`), with a **stage gate** between them. `1p41o` was relocated out of this (closed) wave via `wave_remove_change` and re-admitted to `1p47e`, re-set to `planned` (gated). This wave keeps the historical gate-out evidence; the live re-attempt lives in `1p47e`, where `code_risk_score`'s AC-8 re-runs **only after `1p470` rebuilds the graph**. (`1p41q` stays here as `deferred` — it was a value judgment, not blocked on cross-file resolution.)

- No external wave dependencies.
