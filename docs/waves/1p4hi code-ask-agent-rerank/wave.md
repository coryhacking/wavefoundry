# Wave Record

Owner: Engineering
Status: closed
Last verified: 2026-06-11

wave-id: `1p4hi code-ask-agent-rerank`
Title: Code Ask Agent Rerank

## Objective

Make `code_ask`'s default retrieval **agent-fused** and **structure-aware**. Skip the ~30s local cross-encoder (`bge-reranker-base`) **and** the context-blind RRF cross-source merge, returning **labeled, deduped, per-index candidates** (full chunk text, with a per-index coverage floor) for the host agent — the tool's only consumer, which already synthesizes — to fuse, rank, and synthesize itself. **Five changes** (sequence `1p4hj` → `1p4mf` → `1p4lr` → `1p4ls` → `1p4hu`): `1p4hj` (the agent-fuse pipeline — default `rerank="agent"`; cross-encoder+RRF retained as opt-in `"local"`; gated on AC-10 — **landed, PASS**); **`1p4mf`** (chunk module-level constant declarations so a constant's value is retrievable at all — the **foundational** fix; `CHUNKER_VERSION` 25→26, gated on a local recall eval before fleet re-index); `1p4lr` (a candidate-side definition/constant rank boost — makes the now-retrievable but short constant chunk *rank*; **revived by `1p4mf`** and shipped coupled to it — the one class AC-10 found agent losing to the cross-encoder); `1p4ls` (graph **constant nodes + `reads` edges** — the structural complement: constants are absent from the graph today, blocking structural "what value is X" + constant blast-radius + `1p4hu`'s reach to constants; the one `GRAPH_BUILDER_VERSION` bump in the wave); and `1p4hu` (a structural **graph signal** as a candidate source so call-graph-relevant code surfaces where text similarity misses it). Two surfaces, one fix: chunks make constant values **retrievable**, the graph makes them **structural**. Cuts per-call latency + the shared-model parallel bottleneck and makes retrieval structure-aware.

## Changes

Change ID: `1p4hj-enh agent-side-rerank-mode`
Change Status: `implemented`

Change ID: `1p4hu-enh graph-signal-retrieval-fusion`
Change Status: `implemented`

Change ID: `1p4lr-enh candidate-definition-rank-boost`
Change Status: `implemented`

Change ID: `1p4ls-enh graph-constant-nodes-and-references`
Change Status: `implemented`

Change ID: `1p4mf-enh chunk-constant-declarations`
Change Status: `implemented`

Change ID: `1p4n4-enh incremental-chunker-version-reindex`
Change Status: `implemented`

Change ID: `1p4pz-enh code-constants-multi-language`
Change Status: `implemented`

Change ID: `1p4q4-enh chunk-ts-enum-members`
Change Status: `implemented`

Completed At: 2026-06-11

## Wave Summary

Wave `1p4hi` (Code Ask Agent Rerank) delivered 8 changes: Agent-Side Rerank for `code_ask` (Default: Skip the Cross-Encoder *and* RRF; the Agent Fuses Labeled Per-Index Candidates), Graph-Signal Retrieval Fusion for `code_ask` (Structural Proximity as a Candidate Source), Candidate-Side Definition/Constant Rank Boost, Graph Constant Nodes + Reference Edges, Chunk Constant Declarations, Incremental Re-Index on Chunker-Version Bump, code_constants — multi-language constant lookup, and Chunk TS enum / const-enum members (+ namespace const, declare const). Notable adjustments during implementation: Agent-Side Rerank for `code_ask` (Default: Skip the Cross-Encoder *and* RRF; the Agent Fuses Labeled Per-Index Candidates): Scoped, then readied (prepare-council PASS, READY-WITH-FIXES), then **reframed at operator request**: agent-mode now skips **both** the cross-encoder **and** the RRF cross-source merge (option 1 — the agent fuses labeled per-index candidates), with **dedup for diversity** (option 2). Graph/structural signal split into `1p4hu-enh` (same wave). This supersedes the single-RRF-window design the earlier readiness signoff covered → wave re-prepared for both changes. Grounded in `server_impl.py:15078` (no synthesis), `:836` (`_rerank` no-reranker branch), `:851` (`_rrf_merge`), `:838` vs `:14395` (full chunk vs 300-char excerpt), `:101-102` (`VECTOR_TOP_K`).; Agent-Side Rerank for `code_ask` (Default: Skip the Cross-Encoder *and* RRF; the Agent Fuses Labeled Per-Index Candidates): **Live-tool smoke matrix (workflow `wf_6cc8048b-3c1`, 9 agents) — 8 pass / 1 warn / 0 fail; `ac10_ready`.** 6 question-type probes via the MCP `code_ask` tool: navigational / explanatory / cross-modal / artifact-symbol / definitional all PASS; off-topic K8s WARN (degrades gracefully, no fabrication — but `_heuristic_confidence` reports "high" on a flat 0.66 no-answer band: pre-existing calibration nit, out of 1p4hj scope). Every agent query: `rerank_mode=agent`, `rerank_ms` 0–2ms (no cross-encoder leak), within the 24000c budget (content-rich packed 96–99.8%, drop-off-bound 74–94%), docs floor ≥3 preserved. **Agent-vs-local compare: agent matched-or-beat local recall on both AND ~9–10× faster (agent 2–4s vs local 17–40s); STRICT recall WIN on the count query — local's cross-encoder `top_n=7` cap dropped the `_agent_candidate_select` answer symbol that agent surfaced.** AC-10 fold-ins (non-blocking): probe whether 24000 is the right ceiling (4/7 bind ~99%); add `rrf_fallback` + over-budget-floor + actual-demotion cases (untested here — these explanatory pools had `demotion_count=0`; `dbg4` separately showed real demotion). Orthogonal find: `GRAPH_BUILDER_VERSION` doc staleness (`graph-index-system.md`=17, live=25).; Agent-Side Rerank for `code_ask` (Default: Skip the Cross-Encoder *and* RRF; the Agent Fuses Labeled Per-Index Candidates): **Operator: "is 20 a hard max? small chunks could go more than 20, large chunks fewer — how do we measure so we don't pollute yet get enough?" → count is now governed by a TEXT BUDGET, not a fixed N.** Found two limits where the COUNT cap (20) bound before the size cap (24000c) — wrong unit. Reworked: `AGENT_TEXT_BUDGET_CHARS`=24000 (≈6k tokens) is the PRIMARY ceiling, enforced inside `_agent_candidate_select` (packs by relevance within the budget, floor counted against it); `AGENT_CANDIDATE_MAX`=40 is now a loose count backstop; the drop-off handles "enough." Removed the redundant response-side trim (it could re-cut the floor). Real-index: cumulative ≈24000c with count varying **22/27/32/37** by chunk size (small chunks → more), and 13 when relevance runs out first (under budget). New test `test_agent_mode_count_driven_by_text_budget_not_fixed_n`; RerankerTests 91 green; MCP reloaded. **Measurement → AC-10** (recall vs context-size sweep sets the budget).

**Changes delivered:**

- **Agent-Side Rerank for `code_ask` (Default: Skip the Cross-Encoder *and* RRF; the Agent Fuses Labeled Per-Index Candidates)** (`1p4hj-enh agent-side-rerank-mode`) — 11 ACs completed. Key decisions: --------; **Default `rerank="agent"`** (operator); cross-encoder + RRF become the opt-in `"local"` mode.
- **Graph-Signal Retrieval Fusion for `code_ask` (Structural Proximity as a Candidate Source)** (`1p4hu-enh graph-signal-retrieval-fusion`) — 6 ACs completed. Key decisions: --------; Graph signal as a candidate **source/lane** in `1p4hj`'s per-index allocation (vs. a score boost on semantic candidates).
- **Candidate-Side Definition/Constant Rank Boost** (`1p4lr-enh candidate-definition-rank-boost`) — 6 ACs completed. Key decisions: --------; **Candidate-side** boost (on the chunk), not query-side.
- **Graph Constant Nodes + Reference Edges** (`1p4ls-enh graph-constant-nodes-and-references`) — 7 ACs completed. Key decisions: --------; Add **constant nodes + a new `references` edge**, not overload `calls`.
- **Chunk Constant Declarations** (`1p4mf-enh chunk-constant-declarations`) — 7 ACs completed. Key decisions: --------; **Req-7 (four-surface reconciliation): scope the two legacy tools OUT as known-divergent for 1p4mf; fold the shared-detector unification into 1p4ls.** After 1p4mf the four constant surfaces are: (1) the 1p4mf **chunk lane** — now the authoritative per-language tree-sitter detection (10 languages, mechanism+scope gate); (2) the 1p4ls **graph lane** — not yet built; (3) **`code_constants`** (`server_impl.py:9371`) — a column-0 `^NAME\s*=\s*value` regex name→value lookup (Python-shaped, name-keyed, language-agnostic); (4) **`code_outline`** (`:9739`) — a file-structure outline. Decision: **`code_constants` is COMPLEMENTARY, not harmfully divergent** (it is a deterministic exact name→value lookup, a different job from semantic retrieval) — leave as-is. **`code_outline` is the visible-incoherence risk** (it could omit a MixedCaps/camelCase constant the chunk lane now surfaces) — documented as known-divergent for this wave. The proper unification — ONE shared per-language constant detector consumed by the chunk lane AND the 1p4ls graph lane (and optionally retrofitted into the two tools) — is **folded into 1p4ls**, because that change builds the graph-lane consumer (extracting a shared detector with no second consumer would be premature). **Watchpoint:** when 1p4ls lands, extract the per-language detection into a shared helper both lanes import, and reconcile `code_outline`.
- **Incremental Re-Index on Chunker-Version Bump** (`1p4n4-enh incremental-chunker-version-reindex`) — 7 ACs completed. Key decisions: --------; **Only the chunker-only auto-escalation goes incremental; model/walker changes + `--full` stay full re-embed.**
- **code_constants — multi-language constant lookup** (`1p4pz-enh code-constants-multi-language`) — 7 ACs completed. Key decisions: --------; Reuse the `1p4mf` chunk-lane detector (not the graph constant nodes) for detection.
- **Chunk TS enum / const-enum members (+ namespace const, declare const)** (`1p4q4-enh chunk-ts-enum-members`) — 7 ACs completed. Key decisions: --------; Enum members become NODES + chunks now; member READ edges deferred.
## Journal Watchpoints

- **⚠️ CLOSE ACTION — bump `CHUNKER_VERSION` 26 → 27 at wave close (operator-confirmed 2026-06-10).** `81bae0c` (interim) committed `CHUNKER_VERSION = "26"` with **Python-only** constant chunking; the wave then extended the chunk shape to **all 11 languages** under the same `26`. Two committed `26`s with different shapes break the version=shape invariant — an index built on the interim `26` would NOT auto re-chunk on a `26→26` "upgrade". Bumping to **27** at close forces every consumer/index to re-chunk to the final all-languages shape (consumers on 25 rebuild regardless; this protects anyone on the interim `26`). The 1p4n4 `mode='rechunk'` is the local-recovery alternative, but the release needs the version bump. Do this in the close / version-stamp step.
- **Shared-file serialization (blocking):** both `1p4hj` and `1p4hu` rework `code_ask` candidate selection in `server_impl.py` (the per-index retrieval + `_rerank:833`/`_rrf_merge:851` region). **Land `1p4hj` first** — it establishes the per-index allocation + `source` labels + dedup that `1p4hu` plugs its graph source into. Sequence the edits; coordinate one mode-branch.
- **Agent fuses — skip cross-encoder AND RRF (design core):** agent-mode (the default) returns labeled per-index candidates (full chunk `text`, per-index floor, deduped), NOT a cross-encoder rescore or an RRF cross-source order. Dedup must **preserve the multi-source-agreement signal** (RRF's cross-source reward) — keep the source set, don't collapse to one.
- **Chunks not vectors:** full chunk `text` per candidate (reranker parity, `:838` — not the 300-char excerpt `:14395`); NEVER embedding vectors (uninterpretable to the agent + token-wasteful; server-internal only).
- **Default-flip gated (blocking):** agent-mode is the DEFAULT — the flip ships only if it matches-or-beats `"local"` (cross-encoder+RRF) on quality+recall (`1p4hj` AC-10). `"local"` retained for eval/determinism/offline; do NOT remove the cross-encoder or RRF.
- **Token budget (blocking):** every default call returns the per-index set → bound by a token/size budget (mirror the `1p4es` `code_impact` cap); the levers are the per-index counts + the cross-index allocation (≈ budget ÷ chunk-size), not a gut number.
- **Version-bump scope — TWO bumps (updated 2026-06-10):** `1p4hj`, `1p4lr`, `1p4hu` are query/response-layer (`server_impl.py`); `1p4hu` *consumes* the existing graph (`graph_query.py`). **`1p4ls`** changes graph node/edge shape (constant nodes + `reads` edges) → **`GRAPH_BUILDER_VERSION` 25→26**. **`1p4mf`** changes chunk shape (constant chunks) → **`CHUNKER_VERSION` 25→26**. **`1p4n4`** (added 2026-06-10) makes that bump **incremental** — re-chunk + reuse embeddings by content hash, so only the ~840 new constant chunks re-embed (not the full ~8,840 corpus); the chunker bump is **no longer a full re-encode / "heaviest lever"** (model/walker changes + `--full` still re-embed fully). **Land both bumps in ONE combined release** so consumers pay a single (now cheap) re-index window; still gate the `CHUNKER_VERSION` bump on a local AC-10 recall eval before it reaches consumers (see [[graph-builder-version-bump]]). Edit gate: `framework_edit_allowed`.
- **Sequence + shared-file (updated 2026-06-10):** `1p4hj` (landed) → `1p4mf` → `1p4lr` → `1p4ls` → `1p4hu`. **`1p4mf`** edits `chunker.py` (constant-chunk emission + `CHUNKER_VERSION` bump) — independent file, lands **FIRST** of the constant work as the substrate `1p4lr` boosts. `1p4hj`, `1p4lr`, `1p4hu` all rework the `search_combined` agent-branch candidate scoring/selection in `server_impl.py` — land in order, coordinate the one region. `1p4ls` edits `graph_indexer.py` (independent file) but its version bump forces a full graph rebuild; sequence it before `1p4hu` so `1p4hu` can traverse the new constant `reads` edges. `1p4lr`'s AC-5 value gate runs **after** `1p4mf` lands (it now has real constant chunks to move).
- **`1p4hu` bounded + fallback:** graph expansion capped (1-hop default) within the budget; graceful no-graph degrade to `1p4hj` semantic-only.
- **Constant `reads` edges — same-scope + imported (1p4ls, COMPLETE 2026-06-10):** `1p4ls` landed constant nodes + faithfulness-gated `reads` edges across all 11 languages, covering BOTH same-module reads (real-graph: 40/48 constants carry reader edges) AND cross-module imported reads (`from m import CONST; … CONST`) — the latter via a dedicated post-merge `finalize()` resolution that binds a UNIQUE constant-kind node or drops (imported functions dropped, ambiguous twins dropped). AC-2 fully met. No open follow-up.
- **Four-surface constant reconciliation (Req-7, resolved + watchpoint, 2026-06-10):** `1p4mf` made the **chunk lane** the authoritative per-language constant detector (10 languages, mechanism+scope). `code_constants` (`server_impl.py:9371`, column-0 `NAME=value` regex) is **complementary** (exact name→value lookup) — left as-is; `code_outline` (`:9739`) is **known-divergent** (may omit MixedCaps/camelCase consts the chunk lane surfaces) — documented, not changed this wave. **Watchpoint:** when **`1p4ls`** builds the graph lane, extract the per-language detection into ONE shared helper both lanes import (1p4ls Req-1 already cites the canonical 1p4mf table), and reconcile `code_outline`. Unifying earlier was premature (no second consumer yet). See `1p4mf` Decision Log.

## Review Evidence

- wave-council-readiness: READY-WITH-FIXES — readiness sign-off APPROVED (all fixes applied in-session at Prepare) 2026-06-09 — **two-change wave** (`1p4hj` agent-fuse pipeline + `1p4hu` graph signal), re-reviewed after the operator's option-1+2 reframe (skip RRF + dedup) and the `1p4hu` split. Design source-verified: `code_ask` does no synthesis (`server_impl.py:15078`); the `_rerank` no-reranker branch returns RRF-merged chunk candidates (`:836`) fed by per-index retrieval + `_rrf_merge` (`:851`); candidates carry `text` not vectors (`:838` vs the 300-char excerpt `:14395`); the keyword two-hop is generalizable to graph edges via `graph_query.py`. Seats: reality-check / architecture / qa / red-team / docs-contract; rotating: architecture-reviewer. Verdict READY-WITH-FIXES. **Operator decisions (2026-06-09):** (a) agent-rerank is the DEFAULT (cross-encoder+RRF → opt-in `"local"`); (b) agent-mode skips RRF too — labeled per-index candidates, the agent fuses (option 1); (c) dedup for diversity (option 2); (d) graph/structural signal split into its own change `1p4hu` (same wave). **Must-fixes applied in-session:** (1) red-team — RRF *rewards cross-source agreement*, not just ordering; dedup must PRESERVE the multi-source signal (source set + best per-source rank), else a candidate strong in both code+docs is lost (`1p4hj` Req 4 / AC-5); (2) per-index floor + cross-index budget allocation guarantees no modality is starved (`1p4hj` AC-4); (3) full chunk `text` reranker-parity + `source` label, never vectors (`1p4hj` AC-3); (4) distinct `rerank_mode`, not overloaded `reranked:false` (AC-7); (5) the default flip is GATED on `1p4hj` AC-10 (match-or-beat `"local"` on quality + recall + cross-modal coverage before it ships); (6) `1p4hu` bounded (1-hop cap) + graceful no-graph fallback + its own quality gate (`1p4hu` AC-5). strongest-challenge: shipping a quality regression as the new default by *also* dropping RRF → ruled *gate the flip on AC-10 (match-or-beat the cross-encoder+RRF baseline), preserve cross-source agreement on dedup, keep `"local"` as the escape hatch*; strongest-alternative: keep RRF and have the agent rerank the fused order → REJECTED by operator (RRF is context-blind + starves modalities; the agent fuses directly from labeled per-index candidates). Sequencing: `1p4hj` first; `1p4hu` plugs into its allocation (shared-file watchpoint). No `GRAPH_BUILDER_VERSION` bump; edit gate `framework_edit_allowed`. **(Superseded 2026-06-10:** wave expanded to **4 changes** — `1p4lr` + `1p4ls` added; **`1p4ls` bumps `GRAPH_BUILDER_VERSION` 25→26**, so the "no bump" claim no longer holds wave-wide. See the 2026-06-10 prepare-council line below + the updated Objective / Journal Watchpoints.)
- wave-council-delivery: READY-WITH-FIXES — delivery sign-off APPROVED (all verified blockers/majors fixed in-session or claim-corrected) 2026-06-10. **Adversarial 6-agent red-team review** of the closed 4-change scope (`1p4mf`/`1p4n4`/`1p4ls`/`1p4lr`/`1p4hu`), each dimension probed live under `~/.wavefoundry/venv` against the built v26 index ([[venv-for-test-agents]]). **Dimension E (council must-fixes + version coherence + regression) = CLEAN** — all four 1p4ls must-fixes reproduced as genuinely applied (kind-aware `resolve_symbol`, `reads` opt-in + `max_neighbors`, cluster excludes constants/`reads` at `CLUSTER_BUILDER_VERSION` 9, `reads` out of impact/call defaults); version constants cohere (`CHUNKER_VERSION` 26, `GRAPH_BUILDER_VERSION` 26, `CLUSTER_BUILDER_VERSION` 9); `mode='rechunk'` provably partial-re-embeds; HEAD-vs-working-tree diff shows calls/imports/defines edges + confidence buckets BYTE-IDENTICAL (constants/reads purely additive — no regression). **No fail-open security-control narrowing found**: the constant scope-gate is load-bearing in the SAFE direction across all 11 languages (no function-local/block/closure/instance-field/mutable-var leaked through as a constant in ANY language). **Verified findings & disposition:** FIXED IN-SESSION (code + tests, suite 3097 green, AC-5 eval 11/11, live boost probe confirms firing) — A1 Go ≤2-char consts silently dropped → now chunked; B1 Kotlin object/companion/class `const val` reads silently zeroed (const-intercept `simple_names` double-registration) → fixed; B2 imported-read bound a coincidental same-name constant in an UNRELATED module via a bare simple-name fallback → fallback removed (qualified-unique-const-or-DROP); C1+C3 the generalized boost was INERT (split-chunk `:L…`/`(part N/M)` id suffix injected junk tokens; qualified-only match missed NL method queries) → suffix stripped + leaf-name match, `WaveIndex.search_combined` now boosts (was 0); D2 cross-source dedup keyed on the full line-tuple that can never match a graph window → dedup on `(path, start_line)`. CLAIM-CORRECTED (faithfulness, no over-claim left asserting "Verified") — A2 TS enum/namespace/`declare const` scoped OUT (not chunked — pre-existing gap); B3 tree-sitter local-shadow gap + B6 doubly-nested under-fire documented as carve-outs; B4/B5 Kotlin bare `val`/TS enum graph nodes scoped to actual (deferred); F1 `code_definition` returns the declaration line via keyword-fallback (not a structural const node) — softened; A4/A5 regex-fallback "both paths" caveated; C2 1p4lr Progress-Log "function now boosts too" corrected (was aspirational, now real post-fix); graph-index-system.md (imported-reads shipped; `CLUSTER_BUILDER_VERSION` 8→9) + chunking-and-indexing-pipeline.md (`CHUNKER_VERSION` 22→26 + constant note, completing 1p4mf AC-7 doc half). DEFERRED FOLLOW-UPS (documented, non-gating) — A3 generic-walker nested-type wrong owner; A6 Rust nested-`mod`/Kotlin nested-`object`; A7 merge line-span cosmetic; D1 direction-aware graph signal (currently bidirectional bag — claim corrected); D3 query-token stoplist (main/root mis-resolve); D4 `GraphQueryIndex.from_root` per-query reload (memoize); TS-enum + Kotlin-bare-`val` graph nodes; regex-fallback const-after-class/multi-declarator; `code_definition` structural const node. **strongest-challenge:** a green test suite masking unfaithful behavior (B1/B2 invisible because fixtures only exercised top-level Kotlin + the no-twin import case) → ruled *add the missing-shape fixtures (object-const read, imported-function-with-const-twin, 3rd-party-import-with-twin, split-chunk-id boost, leaf-method boost, start-line dedup) and fix the underlying bugs* — all six added + green. **GATING close action remains:** `CHUNKER_VERSION` 26→27 bump (operator-owned — see Journal Watchpoint; verified REAL + NECESSARY: two committed `26`s with materially different chunk shape). Verdict READY-WITH-FIXES → READY to close pending that bump + the operator signoff.
- wave-council-delivery (addendum — 1p4hu graph signal empirically measured + tightened, operator-directed, 2026-06-10): A post-review **17-query live sweep** (5 classes, each graph-ON vs graph-OFF, every finding verified against the live graph via `code_callhierarchy`/`code_references`/source) found the AS-SHIPPED graph signal was weaker than AC-5's 4-query gate implied: **~3/14 wins, direction-blind, 2 relevant-displacement cases** (the gate's 4 queries were the favorable band). On the operator's direction ("tighten it; add to the results, don't kick anything out — we have room, 24k→16k"), the signal was **tightened + made additive**: (1) DIRECTION-AWARE — `one_hop_neighbors` gained a `direction`; "what calls/reads/uses X" expands ONLY the named symbol with edges INTO it (callers/readers/importers), no longer answering a callers question with the seed's callees; (2) SEED-SCOPED — targeted queries seed only the named symbol (not the co-location-noisy top-semantic breadcrumbs), behavioral queries keep both; (3) ADDITIVE — graph cands are appended after the full semantic selection, never reserving a floor slot or consuming the text budget, so they can NEVER evict a semantic cite; (4) NOISE GATES — a generic-word seed stoplist (no more `main`/`root` mis-resolve), test-file + whole-file-module suppression. **Re-measured on the SAME 17 queries: 6 clean wins, 5 correct no-fires (wrong answer → honest silence), 2 mild additive-tail cases, 0 displacement.** +4 graph regression tests (direction, additive-never-displaces, test-suppression, start-line dedup); full suite **3100 green**; AC-5 recall eval 11/11 (no boost/retrieval regression). `server_impl.py` (`_graph_signal_candidates` seed/direction/suppression, `_agent_candidate_select` additive append, `_GRAPH_SEED_STOPWORDS`/`_GRAPH_USER_INTENT_RE`/`_GRAPH_SIGNAL_CAND_KINDS`), `graph_query.py` (`one_hop_neighbors` direction).
- wave-council-delivery (addendum 2 — 1p4hu `graph_related` section, operator-directed, 2026-06-10): Operator Q: graph cands always score 0.0 and sit at the citation tail — should they be a separate "related graph results" section? Yes — the 0.0 score buries genuinely-relevant structural answers and the `source="graph"` tag discards the *relationship* (caller/reader/importer) that IS the answer to "what calls/reads X". Structural matches moved OUT of `citations` into a dedicated **`graph_related`** response section grouped by relationship (`callers`/`readers`/`importers`/`related`), each with `symbol`/`path`/`lines`/`kind`/`confidence`; `_agent_candidate_select` is now purely semantic. **Semantic dedup kept (operator: "we should still dedup right?") as TEXT-dedup:** a match that is also a citation keeps its relationship entry but is flagged `also_cited` with the duplicate `excerpt` dropped — the chunk text is never sent twice. `search_combined` returns an 8th tuple element. Live: `what calls resolve_symbol` → `callers:[callgraph, graph_impact, shortest_path]`; `what reads LANCEDB_COMPACT_THRESHOLD` → all 3 readers (vs 2 that fit in citations before). Suite **3100 green**; AC-5 eval 11/11. `server_impl.py` (`_build_graph_related`, relationship labeling `_GRAPH_REL_LABEL`/`_GRAPH_REL_BUCKET`, agent-branch wiring, response `graph_related`); `tests/test_server_tools.py` (GraphSignalTests reworked).
- wave-council-delivery (addendum 3 — adversarial RE-REVIEW of the post-signoff 1p4hu changes, 2026-06-10): The graph-signal tightening + `graph_related` section + 8-tuple plumbing + text-dedup + citation-path revert post-dated the original delivery signoff, so they got their own 6-dimension adversarial review (all probed live under `~/.wavefoundry/venv`). **NO blockers, NO majors.** The two highest-risk dimensions came back **CLEAN**: (C) 8-tuple plumbing/contract — AST-verified all 6 `search_combined` returns are 8-tuples, sole non-test consumer + all 22 test unpackers updated, `graph_related` present-when-fired/absent-when-not, local/RRF never emit it, citations backward-compatible; (D) citation regression — citations **byte-identical** graph-ON vs OFF on every firing query (deep-equality), floor/boost/cutoff intact, AC-5 11/11, suite green. The dangerous failure modes are structurally prevented (a caller can't be mislabeled a callee — `incoming` is re-derived from the edge, not trusted) or unreachable live (dedup false-positive: 0 distinct-symbol start-line collisions in real code; 16/16 live `also_cited` firings correct). **Fixed in-session (all minor/nit):** F1 stoplist gap (`entries`/`entry` → mis-resolved to a JSON node) added; A1 subject-first "X calls what" now classifies outgoing (was forced callers); A2 past-tense "is X called" now narrows to callers; stale docstring/comment ("source=graph injected" → `graph_related` section); the `build_index` callers doc example corrected (4 callers, cap shows top-3 `main`/`on_created`/`on_deleted`); the 5 "no-fires" reframed as graph-EXTRACTOR coverage gaps (answer exists in source, not as an edge — addressable, not settled); B1/B2 dedup scope documented (semantic-`results`-scoped, exact-start-line — text dup negligible). +2 regression tests (intent classification, `entries` stopword). Full suite **3102 green**; AC-5 eval 11/11. Verdict: **READY** — the post-signoff changes are now adversarially verified; remaining is the operator-owned `CHUNKER_VERSION` 26→27 + signoff.
- operator-signoff: approved

## Review Checkpoints

- **Prepare-phase Wave Council [prepare-council] — 2026-06-09: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, architecture-reviewer, qa-reviewer, reality-checker, docs-contract-reviewer; rotating-seat: architecture-reviewer; **two-change wave** — `1p4hj` (agent-fuse: skip cross-encoder *and* RRF, labeled per-index, dedup, full-chunk) + `1p4hu` (graph signal); strongest-challenge: shipping a quality regression as the new DEFAULT by dropping BOTH the cross-encoder AND RRF — a naive per-index top-N could under-recall vs RRF's cross-source fusion on every call — ruled *gate the flip on `1p4hj` AC-10 (match-or-beat the cross-encoder+RRF baseline on quality + recall + cross-modal coverage) and preserve RRF's cross-source-agreement reward by keeping the multi-source signal through dedup*; the red-team's sharpest sub-point — RRF rewards candidates appearing in multiple indexes, which a single-source dedup would discard — became must-fix #1 (`1p4hj` Req 4 / AC-5: dedup keeps the source set + best per-source rank); strongest-alternative: keep RRF and have the agent rerank the fused order — REJECTED by operator (RRF is context-blind and starves modalities; the agent fuses directly from labeled per-index candidates). Verdict READY-WITH-FIXES; all must-fixes applied in-session: multi-source-agreement preserved on dedup; per-index floor + cross-index allocation (no starved modality, `1p4hj` AC-4); full-chunk `text` + `source` label, never vectors (AC-3); distinct `rerank_mode` (AC-7); AC-10 gates the default flip; `1p4hu` bounded (1-hop cap) + no-graph fallback (AC-4) + own quality gate (AC-5). Sequencing: `1p4hj` first; `1p4hu` plugs into its per-index allocation (shared-file watchpoint, `server_impl.py` candidate-selection path). No `GRAPH_BUILDER_VERSION` bump — query/response-layer only; edit gate `framework_edit_allowed`.) **(Superseded 2026-06-10 by the 4-change scope — see the 2026-06-10 prepare-council line below; `1p4ls` bumps `GRAPH_BUILDER_VERSION`.)**
- **Prepare-phase Wave Council [prepare-council] — 2026-06-10: READY-WITH-FIXES** (moderator: wave-council; primer-depth: standard; seats: red-team, architecture-reviewer; rotating-seat: architecture-reviewer; **two new changes** — `1p4lr` (candidate-side definition/constant boost) + `1p4ls` (graph constant nodes + `references` edges, `GRAPH_BUILDER_VERSION` 25→26); **strongest-challenge: `1p4lr`'s `RERANKER_MODEL` worked example is unproducible — `chunk_python` emits no module-constant chunks (VERIFIED live: the motivating query returns `_get_reranker`/`_warm_reranker`/`_indexer_reranker_model` function bodies + a test, no declaration chunk; the value literal `bge-reranker-base` appears only in a test chunk), so the candidate-side declaration boost has nothing to fire on for Python/Go**; strongest-alternative: let `1p4ls` (the structural constant node, capturing the value) LEAD as the real multi-language fix consumed by `1p4hu`, and either re-scope `1p4lr` to declaration-LINES-within-chunks (JS/TS export-const + in-body) with AC-5 gated on a query it can actually move, or drop it. PASS-class READY-WITH-FIXES — must-fixes: (1) `1p4lr` mis-targeting + AC-5 must fail on the literal motivating query; (2) `1p4lr` top_w cutoff inflation (boost raises the global drop-off bar, can re-trim relevant answers — compute cutoff from un-boosted top); (3) `1p4ls` cluster layer — new relation absent from `_RELATION_WEIGHTS`, decide cluster participation + `CLUSTER_BUILDER_VERSION`; (4) `1p4ls` constant fan-out — hub constants balloon 1-hop neighbors, make the relation opt-in for default traversal; (5) `1p4ls` cross-kind `resolve_symbol` regression — make it kind-aware so a constant sharing a simple name doesn't break callable resolution; (6) wave stale two-change/no-bump record (this annotation). Should-fix (non-gating): rename `references`→`reads` to avoid the `doc_references_code` + `code_references`-tool-field collision; `1p4lr` strict-match precision adversarial queries; nested-scope read attribution; `report()` all-node-iterator audit. Moderator independently verified every load-bearing claim against the live codebase.)
- **Prepare-phase Wave Council [prepare-council] — 2026-06-10: READY-WITH-FIXES** (moderator: wave-council; primer-depth: standard; seats: red-team, architecture-reviewer, reality-checker; rotating-seat: architecture-reviewer; **change `1p4mf`** — chunk constant declarations, `CHUNKER_VERSION` 25→26, per-language mechanism+scope gate, gated on a local AC-10 eval, revives `1p4lr`; **strongest-challenge: AC-6 gates the irreversible fleet-wide `CHUNKER_VERSION` 25→26 re-index (auto-escalation verified live, indexer.py:2012-2024) on an "AC-10 known-answer harness" that is NOT committed anywhere (git grep empty; only the transient `/tmp/ac10_budget_sweep.py`) and whose 6 queries are all symbol-recall with ZERO "what value is X" constant probes** — the gate's target class has a known-answer set of size zero, no numeric threshold, and no v25 dilution baseline; strongest-alternative: **stage the bump** so Python (+existing JS/TS) lands first under 25→26 gated by a real committed value+recall eval, with the other eight source-absent languages as additive passes (no second bump) measured against p4ea, and **breadcrumb-prefix the constant chunk text** (`{stem} > {NAME}\n\n{decl}`, as chunkers at lines 1805/2005 already do) to raise bare-cosine retrievability for free — record bare-vs-breadcrumb rank so the bump can potentially ship decoupled from `1p4lr`. PASS-class READY-WITH-FIXES — must-fixes: (1) **commit the AC-6 gate harness** (`tests/eval/…` + runner) with ≥3-5 "what value is X" constant queries + known answers + a numeric pass bar + a v25 dilution baseline, as a TASK in this change (BLOCKER); (2) correct the Req-3/AC-3 **merge-guard** characterization (ANY 1-line const after ANY prior chunk folds and inherits the predecessor's id — not just "after a function") + exact section-suffix + both merge predicates + a ≥3-adjacent-const fixture; (3) **grammar-availability gate must FAIL not skip** on a missing tree-sitter grammar (Ruby/Kotlin/PHP `regex_fn=None`), under `~/.wavefoundry/venv`; (4) **reconcile the four constant-detection surfaces** (chunk `1p4mf` + graph `1p4ls` vs the EXISTING `code_outline` UPPER-only + `code_constants` column-0 tools) via a shared helper OR a documented-divergent follow-up + watchpoint; (5) AC-6 **dilution check must measure cutoff-driven trimming** (constant chunks raise `top_w` → trim verbose answers for unrelated queries; server_impl.py:977-978) and coordinate the cutoff-from-un-boosted-top fix with `1p4lr` (shared seam, fix once); (6) **stage the bump** — Python required+locally-gated, the 8 source-absent languages ride on fixtures + p4ea (the local eval cannot recall-gate them). Moderator independently verified all claims; corrected the reality-checker's repo-count (87 .py / 1 .js / 0 .ts — the 136js/139ts were excluded `node_modules` `.d.ts` stubs; the spec's "~85 py + 2 js" is accurate).)

## Dependencies

- No external wave dependencies.
