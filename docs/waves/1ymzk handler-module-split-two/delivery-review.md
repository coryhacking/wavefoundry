# Handler module split two delivery evidence

Owner: Engineering
Status: active
Last verified: 2026-09-21

## Baseline and sequence

Initial readiness receipt `review-policy-06694fccc8c7031f6b4e` passed Prepare and activation. The corrected 34-definition inventory wording later required receipt `review-policy-728303bb1c878ce02f91`; its required lanes and scope are unchanged, with current-packet reassessment recorded separately. Required delivery lanes: code, QA and architecture; delivery Council is not selected. TechDocs moves first and receives independent review before the memory inventory is published and reviewed.

The before run uses `docs/reports/retrieval-quality-1ymzk-before-retry1.json`; the original `retrieval-quality-1ymzk-before.json` remains an invalid historical attempt. The successful measurement used the canonical retrieval_eval CLI with the cached tool-venv interpreter, fixture corpus `docs/evals/retrieval-quality-golden.json`, and `--baseline docs/reports/retrieval-quality-1y0bf-final.json`. No full rebuild was requested. A current index, no source writes, and a periodically refreshed reindex-pending marker kept generation 1861 stable throughout. The marker refresher was stopped after the run.

Receipt content SHA-256: `dccb060e3bac40b11060f1c302bfc5b01c370696fd2f6522d026aecea174b809`. No invalidation reasons. Verdict `fail` against standing generation 1772. The independent delivery review (red-team seat, 2026-09-21) established the mechanism: generation 1861 was a POLLUTED corpus, not a pre-existing quality difference. The before receipt cites thirteen `.wavefoundry/framework/scripts/tests/` paths that the index policy (`include_tests: false`) excludes; on `agentic-holdout-native-stdout` two test files held top-10 ranks 3 and 4, and on `assessment-boundary-holdout-negative-explanatory-gapfill` test files held ranks 2, 6 and 7, displacing graded documents. Aggregate code_ask holdout recall 0.57291667 versus 0.59375 and nDCG 0.5233558 versus 0.53066709 are the pollution's effect. `.wavefoundry/logs/index-state.log` records the reaper pass at `2026-09-21T17:20:27-0600` (187 stranded paths, 3,651 rows reaped) between this run and the after run. The handler source is unchanged, so the fail says nothing about the extraction and is retained as a record of the corpus state, not relabeled.

The reranker probe rejected CoreML and used its existing CPU fallback; embedding compatibility was preserved. This query-time fallback did not rebuild the index. Raw console output: `/private/tmp/1ymzk-before-retry1.log`; the durable JSON receipt is authoritative.

## Inventory review

TechDocs inventory independently approved by `/root/split_currency` (code/QA) and `/root/merge_markers` (architecture), both read-only before implementation. Actual MCP navigation available. code_references omitted known test references; exact code_keyword searches supplied the missing test census. They verified the three functions, two caps, per-call composition-root dependencies and exact test-owner adjustments. Both contexts are independent of the builder; code/QA perspectives share one context, not two. Existing workers reused for bounded boundary verification; inherited host capability selected for import/reload judgment, runtime identity unknown.

The renderer-call allowlist replaces server_impl.py with techdocs_handlers.py rather than exempting both, retaining the exact-owner census. No implementation verdict is claimed until executed checkpoint verification.

## TechDocs independent delivery checkpoint

Code/QA context `1ymzk-techdocs-code-qa-20260921` approved with no blockers. Reviewer independently confirmed normalized AST equality for all three moved definitions, byte-identical registration against f7f95d5e, unchanged tracked fixtures and exact inventory-scoped edits. 71 tests passed with zero skips: 27 handler/reload/manifest/producer tests (8.447s), 42 golden/registry/advisory/allowlist tests (7.379s), and 2 purge tests (2.320s), canonical tool-venv Python `-B -m unittest` with scripts/tests on PYTHONPATH.

| Mutant | Detecting test | Outcome |
| --- | --- | --- |
| Bypass retained lint attachment | test_run_writes_exactly_the_absent_members_then_rerun_is_silent | Killed: missing lint |
| Write unexpected file on dry-run | test_dry_run_default_writes_nothing_and_reports_absent_paths | Killed: digest mismatch |
| Omit TechDocs purge entry in scratch copy | actual registered scratch reload | Killed: fresh handler is old handler |

Two mutation-harness setup errors were corrected and are not counted as kills. All six source/test hashes matched the builder's checkpoint at start/end. All five evidence-integrity declarations affirmed; known-bad method focused-mutation. Architecture independently approved with seven tests and two killed mutants, with nine hashes stable: [architecture report](architecture-techdocs-review.md). Code/QA and architecture used two distinct contexts, neither implemented the move. Builder additionally ran 70 focused and 1,031 broader tests; see [implementation evidence](techdocs-delivery-evidence.md). No full-wave approval is implied yet. Memory inventory publication may now proceed.

## Memory inventory checkpoint

Both independent contexts approved the published inventory before source moves. Each re-derived 34 definitions (29 move, five stay), fourteen objects, unchanged original bodies, dependency reads and patch sites. Architecture independently reproduced the 123-symbol measured-tool closure; code/QA verified evaluator attributes and golden leaves but did not claim to reproduce that count. Both found zero moved-memory intersections. `list_waves` is an additional staying helper discovered by inventory and uses the same approved function-local root lookup. Two-instance caches remain distinct but handler store lookup resolves the canonical root; the test documentation is narrowed and true child-process tests retained. No runtime execution was claimed for these inventory judgments.

## Full-suite repair checkpoint

The first canonical full-suite attempt ran 9,481 tests across 119 files (12 skips) and failed three files. Two genuine extraction omissions were recorded as `CODE-DEL-1` and `QA-DEL-1`: the record-path literal allowlist retained its old owner, and the structural publication-lock visitor only scanned the old owner and bare calls. The repairs repoint the single literal exemption and inspect both owners and exact `server_impl`-qualified lock calls, retaining the minimum count and adding negative controls. Product source is unchanged by this repair. Independent code/QA reverification is required before terminal findings or whole-wave approval.

The third file, dashboard tests, failed on unavailable process enumeration and a child unable to serve in the sandbox. With host process/localhost access the unchanged file passed all 215 tests (one expected skip) in 29.312 seconds. The initial failures remain disclosed; dashboard source was not changed. The repaired canonical suite is being run with the same required host access. Logs: `/private/tmp/1ymzk-full-suite.log`, `/private/tmp/1ymzk-dashboard-host.log`, and `/private/tmp/1ymzk-full-suite-repair1.log`.

Repaired canonical whole suite: **9,481 tests across 119 files, 12 skips, OK**, 283.551 seconds. Framework receipt `result=ok`, `inputs_hash=58bd9cc3ca90dfa056e3d7c7bd5ecbe01661aa25db1133e37a567e9eb1e020fb`. Both findings are terminal after independent code and QA reverification. The old census-owner mutation reproduces both failures; an AST-valid inverted qualified lock in an actual scratch memory module fails the intended assertion. All production hashes remain unchanged by repair.

Current-packet readiness was reapproved by code, QA, architecture and Council synthesis; Prepare ready succeeded against receipt `review-policy-728303bb1c878ce02f91`. Reused independent contexts are disclosed in the lane reports. Architecture delivery is approved; final code/QA delivery awaits retrieval evidence.

Memory capture proposed three decision candidates (`1ym21`, `1yltu`, `1yldr`); focused validation rejected all as duplicates of maintained ownership, CLI and evaluation policy. No active memories were added; rejected records retain their evidence and rationale.

## Final retrieval evidence

The after receipt [retrieval-quality-1ymzk-after.json](../../reports/retrieval-quality-1ymzk-after.json) **passes** against the required standing `1y0bf-final` reference: zero comparison violations, zero operator-review reasons and zero invalidation reasons. Start/end epoch is complete generation **1869**, attempt `cdd9fa73f64945caaaaf0a97d6367b51`. Content SHA-256 `4ae11e8e97e7604eb6aa9479e9c989b896b9e69e6ceb67a088405d190c40f0e0`. Command is the before command above with `--out docs/reports/retrieval-quality-1ymzk-after.json`; raw console output `/private/tmp/1ymzk-after.log`. The temporary quiet-period marker refresher was stopped after completion.

The supplementary [computed comparison](../../reports/retrieval-quality-1ymzk-before-after-computed.json) against the fresh before receipt passes with zero violations, but it is NOT the wave's comparable pair: the before side is the polluted generation 1861 and the after side is the reaped generation 1869 (docs corpus 34,477 versus 30,943 rows; code 13,139 versus 13,141), so the pair measures the reap, not the extraction. The evidence that the extraction did not regress retrieval is the signed after receipt against `1y0bf-final` above, where both sides are policy-conformant corpora (generation 1772: docs 30,251, code 12,780) and the evaluator identity `c0ed5e89` is identical. Reproduce with `python3 -B .wavefoundry/framework/scripts/benchmarks/compare_retrieval_receipts.py --baseline docs/reports/retrieval-quality-1ymzk-before-retry1.json --current docs/reports/retrieval-quality-1ymzk-after.json --out docs/reports/retrieval-quality-1ymzk-before-after-computed.json --note 'Supplementary fresh before/after comparison; signed after receipt compares against required standing 1y0bf-final reference.'`. This is explicitly a data-level comparison, not another signed gate receipt. Holdout code_ask recall is 0.59375 (before0.57291667), nDCG0.53066709 (before0.5233558); classification/abstention remain1.0. The improved values are attributed to the reap; all three repetitions per side are rank-identical, so the fixtures are corpus-sensitive at the top-10 boundary rather than flaky.

Ordinary incremental refreshes were used, and one reaper pass (above) removed 3,651 policy-ineligible rows between the before and after runs; the same reap recurs on this store (2026-09-19, 09-20, two on 09-21), which is a framework defect outside this wave's scope and is recorded as a follow-up. The final explicit docs/code update found both layers already current; no full rebuild or precision conversion was requested. The MCP assessment process still reports loaded-code staleness despite handler reload; a fresh CLI setup check returned ready and the benchmark ran in a fresh process. Restart the host before relying on its stale setup-assessment instance.

## Final disposition

All change ACs/tasks are complete. Code, QA and architecture delivery approvals are current against final receipt `review-policy-0bbe4d004de7d941414a`; current-packet readiness and Council synthesis were restored after the completion bookkeeping changed the policy digest. Final Prepare ready succeeded. Two workers supplied code/QA and architecture perspectives, each retained from readiness through delivery, so their ledger rows were NOT fresh contexts under seed 209 despite the `fresh_context: true` flags; the context-id suffixes exist only to avoid identity collisions. The independent delivery review of 2026-09-21 (four newly spawned contexts, none retained from readiness or repair, none the builder) re-verified the code, the tests, the two repairs and the architecture docs, and its delivery approvals in the ledger supersede the retained-context rows; see `independent-delivery-review.md`. Both blocking findings are terminal; final source, framework receipt and retrieval identities were independently recomputed. Full docs validation is clean. Framework edit gate is closed. Wave remains open, uncommitted, awaiting operator review and explicit closure instruction.
