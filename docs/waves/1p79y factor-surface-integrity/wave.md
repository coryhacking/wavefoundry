# Wave Record

Owner: Engineering
Status: closed
Last verified: 2026-06-22

wave-id: `1p79y factor-surface-integrity`
Title: Factor Surface Integrity

## Objective

Close the silent-breakage gap in the factor-review agent surface that downstream field feedback exposed: make `docs-lint` detect an `applicable` factor missing its canonical `docs/agents/factor-<nn>-<name>.md` (or a `.claude/agents/factor-*.md` wrapper without a source / without frontmatter), stop the Config Review (`seed-238`) from giving advice that contradicts the `seed-050` generation contract, and have Upgrade (`seed-160`) repair a drifted factor surface. When this closes, a wrappers-without-sources / frontmatter-less factor surface becomes an actionable gate finding instead of passing clean.

## Changes

Change ID: `1p79x-enh factor-surface-integrity`
Change Status: `implemented`

Change ID: `1p7ac-enh factor-gate-lane-aware`
Change Status: `implemented`

Change ID: `1p7bi-enh delegation-mcp-first-rule`
Change Status: `implemented`

Completed At: 2026-06-22

## Wave Summary

Wave `1p79y factor-surface-integrity` (Factor Surface Integrity) delivered 3 changes: Factor-surface integrity — declared-but-missing validator + seed reconciliation, Factor gate keys off the operational lane set (factor_review_policy.applicable_factors), and Delegation-layer MCP-first rule — route code subagents through role-typed agents or carry the directive. Notable adjustments during implementation: Factor-surface integrity — declared-but-missing validator + seed reconciliation: Implemented. Added `check_factor_surface` (`factor_review`-keyed canonical existence + orphan-wrapper + frontmatter; ERROR severity, recovery points at `seed-050` task 5 / Upgrade reconciliation) and removed the static factor entries from `_AGENT_ROLE_REQUIRED_PATHS`; wired into `cli.py`. **Self-host gap discovered + fixed:** this repo marks `03/05/07/12/13` applicable but canonical docs existed only for `03/05/12/13` — the new gate (correctly) fired on `factor-07`; authored evidence-grounded `docs/agents/factor-07-port-binding.md` (dashboard loopback port binding: `choose_port`/`_is_port_free`/`read_dashboard_config` evidence) and added its canonical row to `platform-mapping.md` (canonical-only, no wrapper). **R4 renderer audit verdict:** `render_platform_surfaces.py` (and `render_agent_surfaces.py`) do NOT render factor wrappers — they are authored by `seed-050` task 5; the renderer emits only hooks/MCP/auto-Guru surfaces. No renderer fix; the downstream symptom is a never-(re)generated seed-050 pair, repaired by the seed-160 backfill. Reconciled `seed-238` (governed-pair section), `seed-160` (merge-safe backfill), `seed-050` (gate pointer). 7 new tests; full suite 3388 (was 3367) green bytecode-free; self-host `docs-lint: ok`; `aceiss\; Factor gate keys off the operational lane set (factor_review_policy.applicable_factors): Implemented. **Re-keyed `check_factor_surface`** to require the canonical `docs/agents/factor-<nn>-<name>.md` (with `Role:`/`Category: factor`) for each factor in `docs/workflow-config.json` `factor_review_policy.applicable_factors` (read the actual policy shape: `applicable_factors`/`partial_factors`/`not_applicable_factors`), NOT for each `applicable` factor in `repo-profile.json`; absent/empty `applicable_factors` requires no canonical docs. **Non-blocking WARNING mechanism:** changed `check_factor_surface` to return `(failures, warnings)` (the same tuple contract `check_prepare_council_verdict`/`check_design_*` already use); the assessment-vs-lane drift signal (a `repo-profile` `applicable` factor not in `applicable_factors`) is appended to `warnings`, and `cli.py` routes it via `warnings.extend(...)` — warnings print as `WARNING:` and do NOT flip the returncode (only `failures` flip it to 1). **Preserved:** the lane-active ERROR (factor in `applicable_factors` with no/malformed canonical), the orphan-wrapper ERROR, and the missing-frontmatter ERROR (both wrapper checks run regardless of the lane set). **Self-host 07 reconciliation:** aligned `repo-profile.json` `factor_review["07"]` `applicable`→`partial` (matches operational config — `07` is in `partial_factors`, the dashboard port-binding is an optional local surface), removed `docs/agents/factor-07-port-binding.md` (over-generated by `1p79x`'s wrong keying, no longer gate-required), and removed its `docs/agents/platform-mapping.md` row — wavefoundry's factor surface now exactly matches its active lanes `03/05/12/13` and docs-lint is clean with **no residual drift WARNING**. **Tests:** replaced the `1p79x` repo-profile-keyed cases with lane-active→ERROR, retired-lane(+repo-profile applicable)→PASS, assessment-only→WARNING-not-ERROR (asserts returncode 0), self-host `03/05/12/13`→PASS (no drift warning), orphan-wrapper-under-empty-lane→ERROR, missing-frontmatter→ERROR; added `_set_applicable_factors` fixture helper. **Seeds:** `seed-050` task 5, `seed-160` (backfill + audit checklist + diff-summary lines), `seed-238` (governed-pair section) re-keyed to `applicable_factors`, the retired/narrower-lane-is-legitimate case, and the assessment-vs-operational relationship; removed `1p79x` text implying `repo-profile` drives the gate. Full suite **3394** green bytecode-free (was 3388, +6); self-host `docs-lint: ok` (exit 0, zero warnings/errors); `aceiss\; Delegation-layer MCP-first rule — route code subagents through role-typed agents or carry the directive: Implemented (inline, not delegated — fitting for a delegation-discipline change). Added the delegation rule to `seed-180` (after the MCP-first exploration order), `seed-100` (implement-wave carry), `seed-050` (wave-coordinator generation + the retrieval-intent backstop), and `AGENTS.md` — referencing the existing exploration order, not restating it; states subagents inherit the parent's MCP tools; extends the `Gapfill:` expectation to spawned subagents. Verified: suite 3394 green bytecode-free (unchanged — seed prose), docs-lint clean, external-project names = 0 in changed files, no VERSION bump, `seed_edit_allowed` opened+closed. All 6 ACs `[x]`..

**Changes delivered:**

- **Factor-surface integrity — declared-but-missing validator + seed reconciliation** (`1p79x-enh factor-surface-integrity`) — 9 ACs completed. Key decisions: --------; **Divergent pre-plan — selected: one coordinated change (validator + seed-238 + seed-160 + renderer audit)**
- **Factor gate keys off the operational lane set (factor_review_policy.applicable_factors)** (`1p7ac-enh factor-gate-lane-aware`) — 9 ACs completed. Key decisions: --------; **Divergent pre-plan — selected: re-key the gate to `workflow-config.json factor_review_policy.applicable_factors`**
- **Delegation-layer MCP-first rule — route code subagents through role-typed agents or carry the directive** (`1p7bi-enh delegation-mcp-first-rule`) — 6 ACs completed. Key decisions: --------; **Add a delegation-layer rule, do NOT restate the tool list**
## Journal Watchpoints

- **From downstream field feedback** (Java-agent consumer, pack `1.8.0+p79p`); a **pre-existing** gap (~1.6.x onward), independent of the closed `1p75h`. All three reported issues were verified against the code before planning.
- **Sequencing watchpoint (highest-leverage first):** the validator (`#1`) is the anchor — it converts silent breakage into a gate finding that drives the `seed-238` (`#2`) and `seed-160` (`#3`) fixes. This ordering is **blocking**: land the validator contract before the seed text references it (serialization point).
- **Self-hosting guard (blocking watchpoint):** replacing the static `_AGENT_ROLE_REQUIRED_PATHS` factor list with a `factor_review`-keyed check must NOT regress this repo's own surface — confirm wavefoundry's `factor_review` marks `03/05/12/13` applicable and their canonical docs exist (AC-4).
- **Merge-safe backfill:** `seed-160` regenerates a canonical factor doc only when **absent**; never overwrite operator-refined content.
- **Vendor-neutrality guard:** new test fixtures must introduce no external-project names (`aceiss`/`teton`/`solaris` grep stays 0).
- **Gates:** `framework_edit_allowed` for `wave_validators.py` + renderer + tests; `seed_edit_allowed` for `seed-238`/`seed-160`/`seed-050`.
- **`1p7ac` follow-up watchpoint:** `1p7ac` joined after `1p79x`'s delivery review — the `wave-council-delivery` signoff must be **re-run** to cover `1p7ac` before close. `1p7ac` corrects `1p79x`'s keying (repo-profile assessment → `workflow-config.applicable_factors` operational lane set); the two land together in 1.8.0. Watchpoint: `1p79x` over-required `factor-07-port-binding.md` on the self-host (07 is `partial` in `workflow-config`) — `1p7ac` reconciles this; do not treat the existing factor-07 doc as load-bearing.
- **`1p7bi` follow-up watchpoint:** joined after both councils — the `wave-council-delivery` signoff must be **re-run** to cover `1p7bi` before close. It is **prompt/guidance only** (`seed-180`/`seed-100`/`seed-050` wave-coordinator gen + `AGENTS.md`); no code/validator/test change, no VERSION bump. Reference the existing MCP-first exploration order — do NOT restate the tool list (it already lives in `seed-180`/`seed-211`).

## Review Evidence

- wave-council-readiness: approved (READY) — prepare-council passed 2026-06-22 (moderator: wave-council; primer-depth: standard; seats: reality-checker, red-team, architecture-reviewer, qa-reviewer, docs-contract-reviewer, security-reviewer; fixed-seat: red-team; rotating-seat: docs-contract-reviewer (seed/prompt authoring across `seed-238`/`seed-160`/`seed-050`)). Scope: one change `1p79x` — a `factor_review`-keyed declared-but-missing validator for the factor-review agent surface (canonical-source existence + orphan-wrapper + wrapper-frontmatter, replacing the static 4-factor list, reusing the `1p799` pattern), `seed-238` factor-aware reconciliation, a merge-safe `seed-160` factor backfill, and a `render_platform_surfaces.py` frontmatter audit. All three downstream-reported issues were verified against the code before planning. Strongest challenge: replacing the static `_AGENT_ROLE_REQUIRED_PATHS` factor list with a dynamic `factor_review`-keyed check could regress wavefoundry's own self-host surface if its `factor_review` does not mark `03/05/12/13` applicable or marks factors whose canonical docs are absent. Conditions into implement: (1) verify this repo's own `repo-profile.json` `factor_review` + factor docs first and keep the self-host docs-lint/suite green (AC-4 self-hosting guard); (2) reuse the `1p799` declared-but-missing validation shape; (3) land the validator contract before the seed text references it; (4) `seed-160` backfill is merge-safe (never overwrite operator-refined content); (5) introduce no external-project names in fixtures; (6) edits under `framework_edit_allowed` (validator/renderer/tests) and `seed_edit_allowed` (seeds). Strongest alternative: validator-only-now or a three-way split — rejected (one coordinated change keeps the shared contract consistent). Faithfulness N/A (validator + seed text; no detection/binding/data change; local-only).
- wave-council-delivery: approved (PASS) — delivery review 2026-06-22 (moderator: wave-council; fixed-seat: red-team; seats: reality-checker, architecture-reviewer, qa-reviewer, docs-contract-reviewer; security N/A). Computational sensors clean (`wave_review`: lint_passed, max_severity none); suite 3388 green bytecode-free (+21 tests). Lanes all PASS, zero blockers. Strongest challenge: could the new factor gate fail-open or false-positive? Verified by reading `check_factor_surface` — it no-ops only when `factor_review` is absent (cannot require an undeclared factor), excludes not-applicable/partial factors, passes a canonical-only surface, and enforces `Role`-matches-filename-slug + `Category: factor`; the orphan-wrapper and missing-frontmatter checks fire on the downstream's exact broken state. Self-host regression refuted: `docs/agents/factor-07-port-binding.md` was authored (evidence-grounded in the dashboard loopback-port logic) so docs-lint stays clean on wavefoundry itself. R4 renderer audit: `render_platform_surfaces.py` does not render factor wrappers (seed-050 generation owns them) — nothing to fix, recorded. No external-project names in any shipped file (the only `aceiss`/`teton`/`solaris` hits are rule-prose in the wave docs). Strongest alternative: a warn-then-error severity escalation instead of immediate ERROR — chose ERROR per the feedback's actionable-gate-finding intent, with the `seed-160` backfill as the repair path for drifted downstream repos. Faithfulness N/A (validator + seed text; no detection/binding/data change; local-only).
- wave-council-readiness (addendum, 2026-06-22, `1p7ac`): approved (READY) — `1p7ac-enh factor-gate-lane-aware` admitted after `1p79x`'s delivery review to correct the gate's source surface (re-key from the `repo-profile.json` assessment to `workflow-config.json` `factor_review_policy.applicable_factors`, the operational lane set), folded into the held 1.8.0. Prepare-council seats: reality-checker, red-team, architecture-reviewer, qa-reviewer, docs-contract-reviewer, security-reviewer. Well-evidenced: three independent `1.8.0+p7a6` consumers (Java happy-path, solaris retired-lane false-block, RDS 10-vs-7 forced over-generation) plus wavefoundry's own `07` drift converged on the keying bug. Strongest challenge: re-keying could mask a repo whose `applicable_factors` is stale/empty but should have lanes — mitigated by the non-blocking assessment-vs-lane drift WARNING that keeps every repo-profile-applicable-but-not-a-lane factor visible. Conditions into implement: key off `applicable_factors` (empty/absent = retired = no docs required); assessment-vs-lane drift is a WARNING not an ERROR; preserve the lane-active ERROR + orphan-wrapper + missing-frontmatter checks; reconcile wavefoundry's own `07` across `repo-profile` + `workflow-config` and keep the self-host docs-lint clean; gated edits; no external-project names; no VERSION bump. Strongest alternative: an explicit `retired` flag or keeping `repo-profile` keying — rejected (neither fixes the partial / 10-vs-7 over-require). The `wave-council-delivery` signoff must be re-run to cover `1p7ac` before close. Faithfulness N/A (validator + seed text; no detection/binding/data change; local-only).
- wave-council-delivery (addendum, 2026-06-22, `1p7ac`): approved (PASS) — delivery review of `1p7ac-enh factor-gate-lane-aware` (moderator: wave-council; fixed-seat: red-team; seats: reality-checker, architecture-reviewer, qa-reviewer, docs-contract-reviewer; security N/A). Computational sensors clean (`wave_review`: lint_passed, max_severity none); suite 3394 green bytecode-free (+6 tests). Lanes all PASS, zero blockers. Strongest challenge: does the non-blocking drift WARNING actually not block the gate? Verified by reading the code — `check_factor_surface` now returns a `(failures, warnings)` tuple and `cli.py` routes the assessment-vs-lane drift via `warnings.extend(factor_warnings)`; only `failures` set the exit code, so the WARNING is visible but never blocks. The gate keys off `workflow-config.json factor_review_policy.applicable_factors` (operational lane set); lane-active-missing-canonical still ERRORs and the orphan-wrapper + missing-frontmatter checks are preserved (with tests). Self-host reconciled: `repo-profile.json` `07` aligned to `partial` (matching `workflow-config`), `factor-07-port-binding.md` + its platform-mapping row removed; docs-lint clean with no residual drift WARNING. No external-project names in any shipped file. Strongest alternative: an explicit `retired` flag or keeping `repo-profile` keying — rejected (neither fixes the partial / 10-vs-7 over-require). Faithfulness N/A (validator + seed text; no detection/binding/data change; local-only). With this addendum the delivery signoff covers both `1p79x` and `1p7ac`.
- wave-council-readiness (addendum, 2026-06-22, `1p7bi`): approved (READY) — `1p7bi-enh delegation-mcp-first-rule` (prompt/guidance only) admitted after both councils. Prepare-council seats: reality-checker, red-team, architecture-reviewer, qa-reviewer, docs-contract-reviewer, security-reviewer. The framework already mandates MCP-first for role agents (`seed-050`/`180`/`211`, closed wave `12sfb`); the gap is the delegation layer — a bare general-purpose/Task subagent spawned for code work bypasses the role-doc contract (observed first-hand this session). Strongest challenge: redundant-guidance accretion or ignorable prose — mitigated by referencing the existing exploration order (not restating it) and extending the `Gapfill:` visibility so repeated subagent shell-by-habit surfaces as a recorded signal. Conditions into implement: add the delegation rule to `seed-180`/`seed-100` + the `wave-coordinator` generation (`seed-050`) + `AGENTS.md`; state that subagents inherit the parent's MCP tools; do NOT restate the tool list; gated seed edits; no external-project names; no VERSION bump. Strongest alternative: restate the tool list in more places — rejected (noise; the guidance already exists). The `wave-council-delivery` signoff must be re-run to cover `1p7bi` before close. Faithfulness N/A (prompt/guidance only; no code/detection/binding/data change; local-only).
- wave-council-delivery (addendum, 2026-06-22, `1p7bi`): approved (PASS) — delivery review of `1p7bi-enh delegation-mcp-first-rule` (moderator: wave-council; fixed-seat: red-team; seats: reality-checker, architecture-reviewer, qa-reviewer, docs-contract-reviewer; security N/A). Prompt/guidance only. Computational sensors clean (`wave_review`: lint_passed, max_severity none); suite 3394 green bytecode-free (unchanged — seed prose); docs-lint clean. Lanes all PASS, zero blockers. Strongest challenge: redundant-guidance accretion or ignorable prose — verified the rule references the existing exploration order (`seed-180`/`seed-211`) rather than restating the tool list, and lands in the four right surfaces (`seed-180` after the exploration order, `seed-100` implement-wave carry, `seed-050` wave-coordinator generation + the retrieval-intent backstop, `AGENTS.md`); the `Gapfill:` visibility extension makes repeated subagent shell-by-habit a recorded, correctable signal. No external-project names in changed files; no VERSION bump. Strongest alternative: restate the tool list in more places — rejected (noise). Faithfulness N/A (prompt/guidance only; no code/detection/binding/data change; local-only). With this addendum the delivery signoff covers all three changes (`1p79x`, `1p7ac`, `1p7bi`).
- operator-signoff: approved when operator confirms closure

## Review Checkpoints

- **Prepare-phase Wave Council [prepare-council] — 2026-06-22: PASS** (moderator: wave-council; primer-depth: standard; seats: reality-checker, red-team, architecture-reviewer, qa-reviewer, docs-contract-reviewer, security-reviewer; fixed-seat: red-team; rotating-seat: docs-contract-reviewer (seed/prompt authoring across seed-238/seed-160/seed-050); scope: `1p79x` factor-surface-integrity — a `factor_review`-keyed declared-but-missing validator (canonical-existence + orphan-wrapper + wrapper-frontmatter, replacing the static 4-factor list) plus seed-238 reconciliation, seed-160 backfill, and a render_platform_surfaces frontmatter audit, reusing the `1p799` pattern; strongest-challenge: replacing the static factor list with a dynamic `factor_review`-keyed gate would regress the self-host — verified at prepare that this repo marks factor-07 (Port binding) applicable with no canonical doc (applicable set 03/05/07/12/13; docs exist for 03/05/12/13), so implementation must generate docs/agents/factor-07-port-binding.md to keep docs-lint and the suite green (AC-4 self-hosting guard); strongest-alternative: validator-only-now or a three-way split — rejected, one coordinated change keeps the shared contract consistent; conditions-into-implement: generate the self-host factor-07 canonical, reuse the 1p799 shape, land the validator before the seed text references it, keep the seed-160 backfill merge-safe, introduce no external-project names, perform gated edits; faithfulness N/A — validator and seed text, no detection/binding/data change, local-only)

- **Prepare-phase Wave Council [prepare-council] — 2026-06-22 (addendum: `1p7ac`): PASS** (moderator: wave-council; primer-depth: standard; seats: reality-checker, red-team, architecture-reviewer, qa-reviewer, docs-contract-reviewer, security-reviewer; fixed-seat: red-team; rotating-seat: docs-contract-reviewer; scope: `1p7ac` re-keys `check_factor_surface` from the `repo-profile` assessment to `workflow-config` `factor_review_policy.applicable_factors` (operational lane set), adds a non-blocking assessment-vs-lane drift WARNING, and reconciles wavefoundry's own `07` drift; strongest-challenge: re-keying could mask a stale/empty `applicable_factors` that should have lanes — mitigated by the drift WARNING keeping it visible; strongest-alternative: an explicit `retired` flag or keeping `repo-profile` keying — rejected, neither fixes the partial / 10-vs-7 over-require; conditions-into-implement: empty/absent `applicable_factors` = no docs required, assessment-vs-lane drift = WARNING, preserve lane-active ERROR + orphan-wrapper + missing-frontmatter checks, reconcile self-host `07`, gated edits, no external-project names, no VERSION bump; note: the delivery council must re-run to cover `1p7ac` before close; faithfulness N/A — validator and seed text, no detection/binding/data change, local-only)

- **Delivery Wave Council [wave-council-delivery] — 2026-06-22: PASS** (moderator: wave-council; fixed-seat: red-team; seats: reality-checker, architecture-reviewer, qa-reviewer, docs-contract-reviewer; security N/A; sensors clean — lint_passed, max_severity none; suite 3388 green bytecode-free, +21; strongest-challenge: factor-gate fail-open or false-positive — REFUTED by reading `check_factor_surface` (no-ops when `factor_review` absent; excludes not-applicable/partial; canonical-only surface passes; `Role`-matches-slug + `Category: factor` enforced; orphan-wrapper + missing-frontmatter checks fire on the real broken state); self-host regression REFUTED — `factor-07-port-binding.md` authored, docs-lint clean on wavefoundry; R4 — `render_platform_surfaces.py` does not render factor wrappers, nothing to fix; strongest-alternative: warn-then-error severity escalation — chose ERROR per the feedback's actionable-gate-finding intent, with the seed-160 backfill as the downstream repair path; faithfulness N/A — validator and seed text, no detection/binding/data change, local-only)

- **Prepare-phase Wave Council [prepare-council] — 2026-06-22 (addendum: `1p7bi`): PASS** (moderator: wave-council; primer-depth: standard; seats: reality-checker, red-team, architecture-reviewer, qa-reviewer, docs-contract-reviewer, security-reviewer; fixed-seat: red-team; rotating-seat: docs-contract-reviewer; scope: `1p7bi` adds a delegation-layer MCP-first rule to `seed-180`/`seed-100` + the `wave-coordinator` generation + `AGENTS.md` — route code-work subagents through a role-typed agent or carry the MCP-first directive, and state that subagents inherit the parent's MCP tools; prompt/guidance only; strongest-challenge: redundant-guidance accretion or ignorable prose — mitigated by reference-don't-restate + the `Gapfill:` visibility extension; strongest-alternative: restate the tool list in more places — rejected as noise; conditions-into-implement: target the orchestration seeds + `AGENTS.md` not the role docs, reference the existing exploration order, gated seed edits, no external-project names, no VERSION bump; note: the delivery council must re-run to cover `1p7bi`; faithfulness N/A — prompt/guidance only, no code/detection/binding/data change, local-only)

- **Delivery Wave Council [wave-council-delivery] — 2026-06-22 (addendum: `1p7ac`): PASS** (moderator: wave-council; fixed-seat: red-team; seats: reality-checker, architecture-reviewer, qa-reviewer, docs-contract-reviewer; security N/A; sensors clean — lint_passed, max_severity none; suite 3394 green bytecode-free, +6; strongest-challenge: does the non-blocking drift WARNING actually not block — REFUTED by reading the code (`check_factor_surface` returns `(failures, warnings)`; `cli.py` routes drift via `warnings.extend`; only failures set exit 1); gate keys off `workflow-config factor_review_policy.applicable_factors`, lane-active ERROR + orphan + frontmatter checks preserved; self-host reconciled — `repo-profile` 07 → partial, factor-07 doc + platform-mapping row removed, docs-lint clean no residual warning; strongest-alternative: explicit retired flag / keep repo-profile keying — rejected; faithfulness N/A — validator and seed text, no detection/binding/data change, local-only; the delivery signoff now covers both 1p79x and 1p7ac)

- **Delivery Wave Council [wave-council-delivery] — 2026-06-22 (addendum: `1p7bi`): PASS** (moderator: wave-council; fixed-seat: red-team; seats: reality-checker, architecture-reviewer, qa-reviewer, docs-contract-reviewer; security N/A; prompt/guidance only; sensors clean — lint_passed, max_severity none; suite 3394 green bytecode-free, unchanged; docs-lint clean; strongest-challenge: redundant prose or ignorable — verified the rule references the existing exploration order not restates it, lands in `seed-180`/`seed-100`/`seed-050`/`AGENTS.md`, and extends `Gapfill:` visibility; strongest-alternative: restate the tool list in more places — rejected as noise; no external-project names in changed files; no VERSION bump; faithfulness N/A — prompt/guidance only, no code/detection/binding/data change, local-only; the delivery signoff now covers all three changes)

## Dependencies

- No external wave dependencies. Reuses the `1p799` (closed `1p75h`) declared-but-missing validation pattern as a template.
