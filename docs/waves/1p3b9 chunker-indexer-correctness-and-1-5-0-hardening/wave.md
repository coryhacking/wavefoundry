# Wave Record

Owner: Engineering
Status: closed
Last verified: 2026-06-04

wave-id: `1p3b9 chunker-indexer-correctness-and-1-5-0-hardening`
Title: Chunker oversized-chunk guard, indexer drift detection, and 1.5.0 enterprise hardening — ship 1.5.0 jointly with 1p35d

## Objective

Land the second half of the 1.5.0 release. Wave `1p35d` shipped the install-flow restructure, agent-surface enforcement, and the upgrade-migration scaffold. This wave fixes the index-quality defects that `1p35d` C3 surfaced but couldn't address in scope (chunker mega-chunks unreachable via semantic search; `file_meta` ↔ Lance drift left half the seed catalog effectively unreachable), and closes the enterprise-deployment Tier 2 findings from the `1p35d` pre-close review (lint-exclusions doc vendoring, upgrade-migration `--dry-run` preview, settings.local.json strip + recursive Role: backfill + dashboard empty-state component test). Both waves close before the **1.5.0** release tag so consumers absorb the full coherent state in one upgrade.

## Changes

Change ID: `1p397-bug chunker-mega-chunk-fallback-for-unstructured-prompts`
Change Status: `implemented`

Change ID: `1p399-bug indexer-drift-detection-between-file-meta-and-lance`
Change Status: `implemented`

Change ID: `1p3b5-enh vendor-docs-lint-exclusions-via-framework-pack`
Change Status: `implemented`

Change ID: `1p3b6-enh upgrade-dry-run-preview-of-migration-effect`
Change Status: `implemented`

Change ID: `1p3b7-enh upgrade-migration-tier-2-hardening`
Change Status: `implemented`

Completed At: 2026-06-04

## Wave Summary

Wave `1p3b9` (Chunker oversized-chunk guard, indexer drift detection, and 1.5.0 enterprise hardening — ship 1.5.0 jointly with 1p35d) delivered 5 changes: Chunker Oversized-Chunk Guard For All Text Surfaces, Indexer Drift Detection Between File_Meta And Lance, Vendor docs-lint-exclusions Doc Via Framework Pack, Upgrade Dry-Run Preview Of Migration Effect, and Upgrade Migration Tier-2 Hardening.

**Changes delivered:**

- **Chunker Oversized-Chunk Guard For All Text Surfaces** (`1p397-bug chunker-mega-chunk-fallback-for-unstructured-prompts`) — 18 ACs completed. Key decisions: Apply the size guard at the dispatcher level, not inside each individual chunker; Per-kind chunk caps (code: 1500, everything else: 2000) calibrated to the actual BGE embedder via the cached tokenizer
- **Indexer Drift Detection Between File_Meta And Lance** (`1p399-bug indexer-drift-detection-between-file-meta-and-lance`) — 11 ACs completed. Key decisions: Detect drift via Lance-vs-`file_meta` cross-check rather than via separate state file; Forced re-chunk preserves `file_meta` entry (no hash change)
- **Vendor docs-lint-exclusions Doc Via Framework Pack** (`1p3b5-enh vendor-docs-lint-exclusions-via-framework-pack`) — 8 ACs completed. Key decisions: Vendor the doc through the framework pack rather than generate per-project; Pick `.wavefoundry/framework/docs/` as the vendored location
- **Upgrade Dry-Run Preview Of Migration Effect** (`1p3b6-enh upgrade-dry-run-preview-of-migration-effect`) — 13 ACs completed. Key decisions: Add a `dry_run` attribute to `UpgradeContext` rather than a separate hook; Preview-log filename distinct from real-run filename
- **Upgrade Migration Tier-2 Hardening** (`1p3b7-enh upgrade-migration-tier-2-hardening`) — 12 ACs completed. Key decisions: Bundle F4 + F5 + F6 in one change; F4 extends settings strip, not a new helper
## Participants

- `code-reviewer` — required (chunker dispatcher changes, indexer reconciliation logic, upgrade-extension preview helpers, dashboard component test)
- `qa-reviewer` — required (chunker has many edge cases; structural-unit decomposition logic needs per-shape regression coverage; indexer drift detection needs fixture-based + scale verification)
- `architecture-reviewer` — not required (no new architectural boundary; all changes refine existing surfaces)
- `security-reviewer` — not required (no trust boundary crossed; no new write surfaces beyond upgrade-extension preview-log file)
- `release-reviewer` — required (`CHUNKER_VERSION` bump triggers consumer rebuild on upgrade; release-notes vendoring change in `1p3b5` may affect release flow)
- `docs-contract-reviewer` — required (chunker chunk-shape change is a behavioral contract for downstream search consumers; lint-exclusions doc relocation is a contract surface)
- `red-team` — Wave Council adversarial primer per `wave_review.enabled`
- `reality-checker` — required (Wave Council Phase 2 fixed seat)
- `wave-council` — required (Wave Council coordinator)

Rotating fifth Phase 2 seat: `docs-contract-reviewer` — chunker chunk-shape change is the load-bearing contract under change; structural-unit awareness affects how every downstream search consumer sees retrieved chunks.

## Journal Watchpoints

- **Watchpoint — `CHUNKER_VERSION` MUST bump in the same commit as the `chunk_markdown` structural-unit change** (`1p397` AC-17). Without the bump, the auto-escalation path in `indexer.py:1880-1897` never fires and consumer indexes keep their old chunk shape — the fix stays latent on every consumer until something else triggers a rebuild. Per the established `feedback_graph_builder_version_bump` rule, same shape for chunker. Follow-up: a future change can retire `1p35d` C3's `get_seed` disk-fallback once C1 + C2 of this wave land and the underlying drift class is closed at source.
- **`1p397` and `1p399` are orthogonal but synergistic.** `1p397` prevents NEW oversized-chunk drift from being created; `1p399` repairs EXISTING drift (where `file_meta` claims a file is indexed but Lance has zero rows for it). Implement and test independently, but verify together: after both land, the `1p35d` disk-fallback in `get_seed` should become unnecessary (and could be retired in a future change — out of scope here).
- **Joint 1.5.0 release with `1p35d`.** No version tag fires at this wave's close. `build_pack.py --version 1.5.0 --release` tags both waves' work under one **1.5.0** when this wave closes. CHANGELOG `## [1.5.0]` accumulates bullets from both waves; preserve existing 1p35d bullets when adding ours.
- **The C7-from-1p35d upgrade migration is now load-bearing for safe 1.4.x → 1.5.0 upgrades.** `1p3b6`'s `--dry-run` preview gives risk-averse enterprise operators a way to inspect what would change before committing. `1p3b7`'s recursive walk + settings.local strip make the migration robust against enterprise layouts the original `1p3ay` didn't cover. Verify the preview output matches the real-run report shape so operators can correlate.
- **Self-host first; consumer second.** This wave's chunker change re-chunks the framework's own semantic index when implemented (because `CHUNKER_VERSION` bumps). Plan for a full reindex during implementation; do not surprise the operator with a multi-minute embedding run partway through.

## Review Evidence

- operator-signoff: approved 2026-06-04 — operator-directed wave review and close authorization. Pre-close review surfaced F1 (delivery-council verdicts missing from wave.md — addressed in-session) and F3 (document `(part N/M)` / `(rows N–M of T)` conventions in seed-211 — addressed in-session). F2 (release smoke-test) is operator activity for the release step. F4 deferred until consumer evidence. Joint 1.5.0 release with closed wave `1p35d` (commit `11b3af4`); no intermediate version tag.
- wave-council-readiness: approved 2026-06-04 — five admitted changes (`1p397`, `1p399`, `1p3b5`, `1p3b6`, `1p3b7`) reviewed at plan stage. All five are responses to observed defects (1p35d C3 retrospective + 1p35d pre-close findings F2/F3/F4/F5/F6). Implementation paths are concrete; Decision Logs name rejected alternatives. Joint-release contract with `1p35d` recorded; `CHUNKER_VERSION` bump invariant captured as AC-17 on `1p397`. One must-fix to fold before implementation: MF-1 (qa-reviewer) — `1p399` AC-9 currently specifies "sub-second on 10K rows" but does not bound 100K+ row enterprise-Teton-scale case; extend AC-9 or add a separate scale AC. Three non-blocking notes (release-reviewer: smoke-test `build_pack.py --release` against multi-wave version gap; docs-contract-reviewer: document `(part N/M)` section-label convention in operator-facing retrieval surfaces; reality-checker: extend journal-skip-at-any-depth pattern if real consumer edge cases surface). Wave is ready for implementation.
- wave-council-delivery: approved 2026-06-04 — all 5 admitted changes implemented and council-passed (C1 `1p397`, C2 `1p399`, C3 `1p3b5`, C4 `1p3b6`, C5 `1p3b7`). Full framework suite **2507 tests pass** (+48 from this wave); docs-lint clean; CHANGELOG `## [1.5.0]` section accumulates bullets from both this wave and `1p35d`; both edit gates closed; all change docs `Change Status: implemented`. Per-change delivery verdicts captured under `## Review Checkpoints` below. **Wave-level synthesis:** the chunker dispatcher now applies per-kind char caps (1500 for code, 2000 for everything else) calibrated to the BGE embedder's actual 512-token budget so every emitted byte is addressable in semantic space; markdown pipe tables decompose per-row with header preservation (verified on the real-world 41K-char `1p318` Decision Log); line-wrap parts preserve the section breadcrumb on every chunk; `CHUNKER_VERSION` bumped 22 → 23 to trigger consumer-side auto-rebuild. Indexer drift detection (`_detect_lance_drift`) reconciles `file_meta` against Lance tables on every incremental update — measured 11.4ms on 100K rows (17.5× under the MF-1 200ms bound). Lint-exclusions reference doc vendored into the framework pack. Upgrade migration gains a `--dry-run` preview path that fires the migration's preview helpers (`_preview_*`) with zero filesystem mutations, writing to a distinct `.preview.log` filename; the migration also extends to settings.local.json and walks `docs/agents/` recursively (covering enterprise nested layouts). Ships jointly with `1p35d` under one **1.5.0** tag; consumers absorb both waves' work in a single upgrade.

## Review Checkpoints

- **Prepare-phase Wave Council [prepare-council] — 2026-06-04: PASS WITH NOTES** (moderator: wave-council; primer-depth: standard; seats: red-team, code-reviewer, qa-reviewer, release-reviewer, reality-checker; rotating-seat: docs-contract-reviewer; strongest-challenge: 1p397 structural-unit awareness could miscompose on a single 5K-char list item containing a code block plus prose — covered by the universal hard-wrap fallback per AC-4 chained to AC-2; strongest-alternative: bundle 1p3b5 + 1p3b6 + 1p3b7 into one change — rejected because 1p3b6 is a contract change adding UpgradeContext.dry_run while 1p3b5 is a layout change and 1p3b7 is three local refinements; council focus differs per scope shape)
- **Must-fix MF-1**: `1p399` AC-9 currently specifies "sub-second on 10K rows". Extend to bound the 100K+ row case (likely `< 200ms` on the existing Lance index — verify against the framework's own index size at implementation time) or add a separate scale AC. Owned by `1p399`; fold into the change doc before implementation begins.
- **Note N1 (release-reviewer):** smoke-test `build_pack.py --version 1.5.0 --release` against the multi-wave version gap (current tag is `v1.4.1`, target is `v1.5.0`) before actually tagging. Verify the changelog-section extraction handles the multi-bullet accumulated `## [1.5.0]` shape correctly.
- **Note N2 (docs-contract-reviewer):** document the `(part N/M)` hard-wrap section-label convention introduced by `1p397` in operator-facing retrieval surfaces (seed-211 guru prose, dashboard render-paths) so an operator seeing `Decision Log (part 2/3)` in a retrieval result knows it is an automatic split, not a doc-author convention.
- **Note N3 (reality-checker):** extend the journal-skip-at-any-depth pattern in `1p3b7`'s F6 recursive walk if a real consumer surfaces an edge case where a non-role-doc directory under `docs/agents/` exists.
- **Delivery-phase Wave Council [delivery-council] C1 — 2026-06-04: PASS WITH NOTES** (moderator: wave-council; primer-depth: standard; seats: red-team, code-reviewer, qa-reviewer, release-reviewer, docs-contract-reviewer, reality-checker; rotating-seat: docs-contract-reviewer; strongest-challenge: the original plan recommended a unified 4000-char cap reusing MAX_CODE_CHUNK_CHARS — empirical measurement during implementation showed 4000 was ~2x the BGE embedder's actual char budget so the bottom 45-62% of every chunk was invisible to semantic search; operator-directed per-kind calibration (code 1500 / everything-else 2000) replaces the plan's unified-cap decision and is recorded in the Decision Log; strongest-alternative: token-aware cap via tokenizer import in chunker.py — rejected because it adds a dependency edge and slows chunking when the per-kind char cap is a close-enough approximation). 19 ACs verified (AC-12, AC-13 flipped from `[~]` deferred to `[x]` when the operator pushed back on the table-decomposition deferral and the implementation grew to deliver per-row decomposition with header preservation). New helpers: `split_large_chunks` (generalized; per-kind cap selector), `_line_wrap_chunk` (with breadcrumb preservation), `_extract_breadcrumb_preamble`, `_decompose_oversized_table_chunk`, `_decompose_oversized_markdown_body`. `chunk_file` refactored into wrapper-around-`_chunk_file_dispatch` so the universal guard fires at every dispatch path's end. `CHUNKER_VERSION` bumped 22 → 23. 19 new chunker tests including the real-world `test_real_world_41k_decision_log` regression target against `1p318`'s 41K-char Decision Log. Verified empirically on the framework's own corpus: `1p397` change doc decomposes 25 chunks (0 over cap); `1p318` Decision Log decomposes 34 row-grouped chunks (28 of 34 preserve the canonical header). Full suite 2478 tests pass; lint clean. No follow-up advisories (the original AC-16 generative max-chars assertion deferral was the only `[~]` left after the operator's pushback resolved AC-12/AC-13).
- **Delivery-phase Wave Council [delivery-council] C2 — 2026-06-04: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, code-reviewer, qa-reviewer, release-reviewer, reality-checker; rotating-seat: docs-contract-reviewer; strongest-challenge: `_detect_lance_drift` only catches paths in file_meta MISSING from Lance — the inverse case (paths in Lance not in file_meta) is the existing `_reap_stranded_lance_rows` helper's job; the boundary is clean and the two helpers don't fight; strongest-alternative: maintain a separate consistency-state file — rejected per the change-doc Decision Log because it would add a third state to maintain and a third drift surface). 11 ACs verified. New helper `_detect_lance_drift(db_path, file_meta_paths, tables=("docs", "code"))` modeled on the existing reaper's cheap path-column read. Wired into `build_index` incremental branch immediately after `_detect_changes`: drifted paths added to `changed_broad`, stderr diagnostic emitted with first 5 paths + `+K more` tail. 8 new tests (6 unit + 2 scale). **Real measurement: 11.4ms on 100K rows + 1000 drifted (5-run avg) — 17.5x under MF-1's 200ms bound.** Full suite 2486 tests pass; lint clean. No follow-up advisories.
- **Delivery-phase Wave Council [delivery-council] C3 — 2026-06-04: PASS** (moderator: wave-council; primer-depth: lightweight; seats: red-team, code-reviewer, release-reviewer, docs-contract-reviewer; rotating-seat: docs-contract-reviewer; strongest-challenge: AC-7 self-host upgrade-simulation E2E test marked `[~]` as subsumed by AC-2's build_pack test — the pack-inclusion test verifies the doc lands in the zip; the consumer-side unzip path is mechanical and adds no new verification value; strongest-alternative: vendor the doc into `docs/references/` of every consumer at install time via seed-090 rendering — rejected per the change-doc Decision Log because the content is framework-owned, not project-specific). 9 ACs verified (AC-7 marked `[~]` with rationale). `git mv docs/references/docs-lint-exclusions.md .wavefoundry/framework/docs/lint-exclusions.md`. `collect_files` walks the framework tree so the doc is automatically included in every pack zip. Drift-guard `test_exclusion_doc_exists_and_lists_each_pattern` updated to resolve the new path. seed-090 prose gains an `Operator reference` block naming the vendored location + `LINT_EXCLUDED_TRANSIENT_DIRS` constant. 1 new build_pack test (`test_lint_exclusions_doc_ships_in_pack`). Full suite 2487 tests pass; lint clean. No follow-up advisories.
- **Delivery-phase Wave Council [delivery-council] C4 — 2026-06-04: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, code-reviewer, qa-reviewer, release-reviewer, reality-checker; rotating-seat: docs-contract-reviewer; strongest-challenge: the dry-run preview helpers must stay byte-for-byte synchronized with the action helpers — drift between the two would cause the preview to report planned actions that don't actually fire, breaking the operator-trust invariant; mitigation: `test_dry_run_zero_mutations` verifies the preview path performs zero mutations against a planted state, AND existing C7-era backfill/cleanup/strip tests continue to pass which guarantees the action-helper enumeration logic remains stable; strongest-alternative: interactive operator confirmation per migration — rejected per the change-doc Decision Log because `--dry-run` is sufficient for the documented preview need and interactive confirm adds operator friction). 18 ACs verified. `UpgradeContext.dry_run: bool = False` added with `getattr` fallback for older callers. Three preview helpers paralleling the action helpers; preview report writes to `.preview.log` (distinct filename per AC-6). `phase_dry_run` in `upgrade_wavefoundry.py` loads the new pack's extension module and invokes `post_extract(preview_ctx)` with `dry_run=True` BEFORE any side effects. 14 new tests covering each preview helper + the orchestration dry-run branch (zero-mutations guarantee + distinct-filename + version-gate + writes preview log when actions planned / doesn't when not). seed-160 `### 1.5.0 upgrade — auto-migration` subsection gains a `Migration preview (operator-side, --dry-run)` paragraph. Full suite 2501 tests pass; lint clean. No follow-up advisories.
- **Delivery-phase Wave Council [delivery-council] C5 — 2026-06-04: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, code-reviewer, qa-reviewer, release-reviewer, reality-checker; rotating-seat: docs-contract-reviewer; strongest-challenge: F4 changes the return type of `_strip_pycache_row_from_claude_settings` from `str | None` to `list[str]` — a breaking API change for any external caller still using the function name; the wave 1p35d C7 migration is the only known caller, and it lives in the same module so the change is contained; the C7 PostExtractHookOrchestrationTests confirms the migration still produces correct sections with the new list return; strongest-alternative: keep the single-file signature and add a parallel `_strip_pycache_row_from_settings_local` helper — rejected because the shared logic would either be duplicated or extracted into yet a third helper, adding maintenance surface). 12 ACs verified. F4: `_strip_pycache_row_from_claude_settings` iterates both `settings.json` and `settings.local.json` via shared `_strip_pycache_row_from_settings_file` inner helper; return is `list[str]`. F5: `test_agents_component_has_empty_state_branch` and `test_agents_component_renders_populated_branch_when_agents_present` cover both render branches (source-level string-presence pattern matching the rest of the dashboard test suite). F6: `_backfill_role_field_on_agent_docs` and `_preview_role_field_backfill` both switched from fixed-subdir iteration to `agents_root.rglob("*.md")` so enterprise nested layouts (e.g., `docs/agents/teams/<team>/<role>.md`) are covered. The `journals` skip applies at any depth via `if "journals" in path.parts`. 6 new tests covering both action and preview paths. Existing 29 backfill / strip / launcher tests continue to pass (updated to the new list return shape for the strip helper). Full suite 2507 tests pass; lint clean. No follow-up advisories.

## Dependencies

- **Wave `1p35d` (closed 2026-06-04, commit `11b3af4`)** — this wave's CHANGELOG bullets accumulate under the same `## [1.5.0]` section that `1p35d` established. `1p3b6` and `1p3b7` extend the C7 migration from `1p35d`; do not re-implement the migration helpers, only extend them.
- **No external wave dependencies.**
- **Joint release contract:** `build_pack.py --version 1.5.0 --release` waits until THIS wave closes, then tags both `1p35d` and this wave under one **1.5.0** version. No intermediate version tag between the two waves.
