# Wave Record

Owner: Engineering
Status: closed
Last verified: 2026-07-03

wave-id: `1p9qm subagent-mcp-retrieval-posture`
Title: Subagent MCP Retrieval Posture

## Objective

Make the framework's MCP-first retrieval posture actually reach spawned subagents. When this wave closes, rendered role wrappers can call the `code_*` tools their own instructions name (today every committed wrapper's allowlist forbids them — the guru wrapper instructs tools its frontmatter denies), the documented "subagents inherit MCP tools" premise is corrected to its real condition, and every lane-facing surface states the retrieval default up front, including the rule that how-many/blast-radius claims must be graph-backed, not grep-sampled.

## Changes

Change ID: `1p9qk-bug subagent-mcp-tool-access`
Change Status: `implemented`

Change ID: `1p9ql-enh subagent-retrieval-posture-guidance`
Change Status: `implemented`

Completed At: 2026-07-03

## Wave Summary

Wave `1p9qm` (Subagent MCP Retrieval Posture) delivered two changes: Subagent MCP tool access: rendered wrapper allowlists exclude the MCP tools the agents are instructed to use; documented inheritance premise is false for them and Retrieval posture for subagent lanes: shared block, front-loaded role-doc leads, and wrapper guardrail. Notable adjustments during implementation: Subagent MCP tool access: rendered wrapper allowlists exclude the MCP tools the agents are instructed to use; documented inheritance premise is false for them: Scoped from Solaris field feedback + verification census. Confirmed: all five `.claude/agents/` wrappers set `tools: Read, Grep, Glob, Bash` (no MCP, no wildcard); guru wrapper body instructs `code_ask`/`code_search` while frontmatter denies them; premise "subagents inherit the parent's MCP tools by default" in seeds 180:133, 100:81, 050:161,546 is false for allowlisted agents; edit points `render_agent_surfaces.py:94` + seed-050:211; wrappers regenerate on upgrade so downstream repos cannot self-fix. This session's own guru subagents grep-only behavior independently reproduced the defect.; Subagent MCP tool access: rendered wrapper allowlists exclude the MCP tools the agents are instructed to use; documented inheritance premise is false for them: Host-semantics research: Claude Code docs confirm `tools:` accepts exact tool names + server-level MCP patterns; omitted `tools:` inherits all; agent files documented as live-watched; unknown-entry behavior and deferred-schema interaction explicitly undocumented. Empirical probes: new agent files do NOT register mid-session in this harness, and frontmatter edits to the registered guru agent were not picked up (three spawns all reported the session-start toolset — even an added built-in `Edit` never appeared), so live-reload is not observable here and AC-2 moves to fresh-session verification. Probe side-finding: guru spawns report only `Read`+`Bash` (registry lists Read, Grep, Glob, Bash) — native Grep/Glob appear unavailable to subagents in this host build.; Subagent MCP tool access: rendered wrapper allowlists exclude the MCP tools the agents are instructed to use; documented inheritance premise is false for them: IMPLEMENTED (except AC-2 live check): renderer template updated (18 read-only MCP tools + ToolSearch); seed-050 guru template + factor-wrapper contract sentence + new "Wrapper `tools:` allowlist contract" block; premise corrections in seeds 180/100/050×2 + AGENTS.md; four factor wrappers updated (LF-preserving); surfaces re-rendered via `--repo-root`; `GuruWrapperToolAllowlistTests` (grants pin / no-mutator / body⊆frontmatter) added; suite 4,273 OK; docs-lint clean; scratch probe agents removed. Cross-host audit: Claude Code is the only host with a wrapper tools-allowlist mechanism (Cursor rule, Codex skill, Antigravity config: N/A).

**Changes delivered:**

- **Subagent MCP tool access: rendered wrapper allowlists exclude the MCP tools the agents are instructed to use; documented inheritance premise is false for them** (`1p9qk-bug subagent-mcp-tool-access`) — 6 ACs completed. Key decisions: Fix the allowlists (read-only retrieval set) + correct the documented premise; keep wrappers write-restricted (approach A).; Explicit tool enumeration as the default frontmatter syntax, wildcard only if verified supported.
- **Retrieval posture for subagent lanes: shared block, front-loaded role-doc leads, and wrapper guardrail** (`1p9ql-enh subagent-retrieval-posture-guidance`) — 6 ACs completed. Key decisions: Shared-block home = seed-020 (run contract) `## Retrieval Rules`, rendered into agent-team-workflow.md as `## Retrieval Posture (All Lanes)`.; Pointer-plus-default leads; canonical order never restated (approach A).
## Journal Watchpoints

- Hard sequencing: `1p9qk` before `1p9ql` — guidance must never name tools the wrappers cannot call (that is the guru contradiction being fixed), and both changes edit the same wrapper templates.
- Seed gate discipline: multiple seed files are edited (050, 100, 180, 211-235, 005/007 or the owning bootstrap) — open/close `seed_edit_allowed` per gated session; batch the 211-235 lead edits.
- Blocking design check inside `1p9ql`: the agent-team-workflow.md ownership census (which seed authoritatively renders it) must land before the shared block is inserted, or the block dies on next upgrade re-render.
- Coordination: the AGENTS.md auto-Guru paragraph is also touched by wave `1p9q3` (`1p9pz` docs rider) — one owner for that paragraph if both waves are in flight.
- Follow-up watchpoint: after downstream repos upgrade, ask the Solaris reporter to re-run their transcript count — that is the true field verification; record the result in the journal when it arrives.
- **Standing (until the renderer-overwrite-safety fix in wave `1p9pe` lands): every `render_agent_surfaces` run unconditionally rewrites `.codex/config.toml` and deletes the operator's `wave_close approval_mode` block — restore it (`git checkout -- .codex/config.toml` or re-add the block) after any re-render.** This wave's AC-4 re-render was the second field occurrence (first: wave `1p9j0`); caught and restored at delivery review.
- Delivery-review follow-up candidates (recorded, not admitted): registry-checked allowlist pin using `server_impl`'s read-only annotations (also resolves the `code_hover`/`code_risk_score` include decision); a docs-lint-level allowlist check for init-authored factor wrappers.
- Declined-scope record: the feedback's "avoid pre-specifying file:line in delegation prompts" suggestion is declined on evidence quality (see `1p9ql` Decision Log) — do not re-admit it without a transcript study isolating that variable.

## Participants

- code-reviewer — `render_agent_surfaces.py` template edits are framework-script code
- qa-reviewer — required (bug fix `1p9qk` per `review_policies.require_qa_reviewer_for_bug_fixes`) and both change docs carry AC priority tables
- architecture-reviewer — the corrected tool-inheritance contract is an agent-platform boundary rule
- docs-contract-reviewer — seed edits (050/100/180/211-235 + contributing-doc owners) define behavioral contracts every downstream agent relies on
- red-team, reality-checker — council seats (prepare phase)

## Review Checkpoints

- Prepare wave — readiness verdict (2026-07-03): READY. Council ran at standard primer depth. Red-team's strongest challenge — `1p9qk` rests on the unverified assumption that naming an unavailable MCP tool in a wrapper allowlist is inert; if the host instead errors on unknown entries, every wrapper on an MCP-less repo fails to spawn, a worse regression than the bug — was accepted and resolved by amendment: AC-2 now requires a no-MCP spawn verification with a named pivot (render-time conditional emission) if the assumption fails. Docs-contract seat confirmed the pointer-not-restatement design honors seed-180's own no-restatement rule and that no seed text will carry internal artifact IDs; it flagged the agent-team-workflow ownership census as the wave's genuine unknown — correctly positioned as `1p9ql`'s first task and a blocking watchpoint. Reality seat confirmed the census evidence (all five wrappers allowlist-restricted; guru self-contradiction; premise false in seeds 180:133/100:81/050:161,546) and noted this session's own guru subagents independently reproduced the defect. QA seat confirmed both flip directions are tested (with-MCP calls succeed; without-MCP spawn still works) and the renderer consistency test (frontmatter grants must cover body instructions) prevents reintroducing the contradiction. Strongest alternative (drop the allowlists entirely and inherit all tools) recorded and declined — the write/lifecycle restriction is intentional; only the omission of retrieval tools is the bug. AC priorities recorded on both change docs. Product-owner acknowledgment: not applicable (framework agent-infrastructure work).
- Docs-contract seat (rotating): the retrieval-posture block becomes a durable behavioral contract rendered into every target repo — its single-source-of-truth placement (shared block owns the tool-choice table; role-doc leads are pointers) is the right drift posture; the review-and-evals pointer must not duplicate the block. Verified the declined file:line suggestion is recorded in the change doc, not silently dropped.
- pre-implementation-review: passed (2026-07-03) — highest risk is the unverified host semantics for MCP tool names in wrapper `tools:` allowlists (unknown-entry behavior, wildcard support); addressed by resolving it against Claude Code documentation BEFORE the first renderer edit, with the render-time-conditional pivot standing by per 1p9qk AC-2. Second risk (shared-block ownership census) is sequenced as 1p9ql's blocking first task. Builder lane: coordinator-implemented (small wave, two changes, single session); reviewer lanes engage at Review wave.
- **Delivery-phase Wave Council [delivery-council] — 2026-07-03: PASS** (moderator: wave-council/coordinator; primer-depth: standard — consistent with prepare (read-only retrieval grants, clear scope); seats: red-team (primer), architecture-reviewer, security-reviewer, qa-reviewer, reality-checker; rotating fifth seat: docs-contract-reviewer, "best alternative" brief). Specialist delivery lanes ran first (code-reviewer, qa-reviewer, architecture-reviewer, docs-contract-reviewer — all pass-with-findings, verdicts in Review Evidence). **Red-team primer summary:** strongest challenge — the AC-4 re-render silently deleted the operator's `.codex/config.toml` `wave_close` approval guard (the known, unlanded renderer-overwrite defect; second field occurrence after wave `1p9j0`), unaccounted in either change doc; the adversarial stance also surfaced the registry lag (`code_hover`/`code_risk_score` registered read-only but ungranted, no recorded decision) and the AC-5 attribution confound; the first-principles stance walked the fresh-target-repo instruction chain and found three unseeded carrier links (rendered Retrieval Posture section, review-and-evals pointer, factor-wrapper body bullet). All four fixed seats engaged the primer per protocol (pre-primer reads recorded; effects: confirmed ×1, extended ×3 — each extension adding verified new facts: the erased handoff triage note; the phantom Execution-contract precedent with its stale "six rules" count and zero local instantiation; seed-050's reviewer-role carve-out; the AC-5 audit's Codex N/A wording contradicted by the deleted Codex tool-restriction setting). **seat_agreement_aggregate:** seat_agreement `unanimous` (all lanes and seats pass-with-findings; none blocking after in-session fixes); max_severity `high` at finding time, every finding at that severity resolved in-session — no challenge round triggered. **Correlated-finding flags:** the codex-clobber confirmations across seats derive from the shared primer (one signal, by design); the carrier-propagation gap was surfaced independently by the architecture and docs-contract delivery lanes (parallel, isolated — genuinely independent) before the primer generalized it. Pre-primer read quality: no verbatim primer echo; seats differed in angle (evidence-integrity, boundary, claims-vs-reality). **Rotating fifth seat (best alternative):** the access-layer design (enumerated exact-name read-only grants) is confirmed right — no stronger alternative at that layer (server-side read-only profile rejected: advisory annotations, doubled attachment surface, forfeits the proven fail-safe; renderer-emits-all-wrappers credible but a generation-architecture refactor exceeding a bug wave, recorded as the long-term shape). The strongest alternative not taken lives at the **pinning layer**: derive the required-grant set from `server_impl`'s registry at test time AND validate wrapper `tools:` lines in the docs-lint factor-surface gate (fleet-wide enforcement, where the defect actually manifested) — promoted from "candidate" to recommended follow-up change. **Recommendations verdict table (initial verdict + red-team closing reconciliation, one row per finding):** codex-config clobber → fix now — DONE (restored; disclosed in 1p9qk AC-4; standing watchpoint) — red-team: held. Carrier links unseeded → fix now — DONE (seed-020 carrier bullet; seed-150 reconcile/backfill clause; seed-160 checklist row incl. stale-count fix; seed-050 body-bullet requirement; rotating seat spot-checked all edits for contract integrity) — held. Registry lag + pin divergence → defer with named mechanism (exclusion decision + registry-checked-pin follow-up recorded in 1p9qk Decision Log) — red-team challenged the defer as a possible punt; upheld as genuine: omission fails safe, and the follow-up names the exact mechanism (`_READONLY_TOOL` oracle, both directions). Guru-only test pin → defer (docs-lint wrapper check follow-up; AC-1 note corrected now) — held. AC-5 attribution confound → fix now — DONE (recorded in AC-5 evidence with the Solaris re-count as field verification) — held. Configured-but-dead-MCP variant untested → accept (degrades to the same absent-tool state as the verified case; fails closed) — held. Subagent built-ins quirk (Read+Bash only) → accept (host-owned; recorded in 1p9qk Progress Log) — held. Contract-block "alike" wording vs guru body (rotating-seat finding) → fix now — DONE (seed-050 guru carve-out sentence) — new row added at reconciliation, then held. **Falsification check:** working verdict — PASS with all severe findings resolved in-session. Strongest argument against: the mid-council fixes were not themselves reviewed by the full lane fan-out. Why it does not change the conclusion: the rotating seat spot-checked every fix edit post-application (generic, self-contained, consistent with rendered surfaces), docs-lint and the renderer tests re-ran green, the full suite re-ran green after all edits (4,273 OK), and the fixes are prose-contract additions mirroring existing precedent wiring plus one conservative test-regex widening — no new code paths. **Improvements recommended (beyond verdict):** (1) plan the registry-derived pin + docs-lint wrapper-allowlist check as one follow-up change; (2) prioritize landing the renderer-overwrite-safety fix (wave `1p9pe`) — this wave is its second field occurrence; (3) existing watchpoint stands: Solaris post-upgrade transcript re-count is the true field verification; (4) local `docs/agents/` role docs receive their Tool posture leads at next refresh (recorded in 1p9ql AC-2). No unresolved disagreements; no waived lanes.
- **Prepare-phase Wave Council [prepare-council] — 2026-07-03: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, architecture-reviewer, security-reviewer, qa-reviewer, reality-checker, docs-contract-reviewer; rotating-seat: docs-contract-reviewer; strongest-challenge: unknown-tool allowlist entries may break wrapper spawns on MCP-less repos rather than being inert — resolved by the 1p9qk AC-2 no-MCP spawn verification with a render-time-conditional pivot; strongest-alternative: drop wrapper allowlists entirely and inherit all tools — declined, the write and lifecycle restriction is intentional and only the retrieval-tool omission is the defect)

## Review Evidence

- wave-council-readiness: approved 2026-07-03 — prepare council synthesis verdict READY after the 1p9qk no-MCP spawn-verification amendment; no unresolved blocking findings
- code-reviewer: approved 2026-07-03 — pass-with-findings; renderer template + test pin verified genuine (11/11 green, anti-vacuity checked); 22/22 seed leads clean by exhaustive count; surfaced the `.codex/config.toml` guardrail clobber (restored in-session) and three precision notes (all addressed)
- qa-reviewer: approved 2026-07-03 — pass-with-findings; every `[x]` AC independently re-verified against code/tests (suite re-run 4,273 OK; premise gate re-run exhaustively; zero silent `[~]`); evidence-note overstatements corrected in-session; AC priority tables attested
- architecture-reviewer: approved 2026-07-03 — pass-with-findings; read-only grant boundary verified exhaustively (18/18 granted tools server-annotated read-only; zero mutators across all five wrappers); surfaced the unwired carrier-propagation links (wired in-session via seeds 020/150/160/050)
- docs-contract-reviewer: approved 2026-07-03 — pass-with-findings; seed self-containment and genericness verified exhaustively (zero internal artifact IDs added); single-source-of-truth and no-restatement rules honored; surfaced the unseeded factor-wrapper body bullet (seeded in-session)
- wave-council-delivery: approved 2026-07-03 — delivery council synthesis verdict PASS; seats unanimous; every severe finding fixed in-session at operator direction (codex-config restore, carrier wiring, contract wording, test-regex hardening, evidence-note corrections); remaining items recorded as deferred follow-ups with named mechanisms; full detail in Review Checkpoints
- operator-signoff: pending operator confirmation at closure

## Dependencies

- No external wave dependencies. `docs-contract-reviewer` lane required at review (seed edits define behavioral contracts agents rely on).
