# Wave Record

Owner: Engineering
Status: closed
Last verified: 2026-07-04

wave-id: `1p9qh java-csharp-enterprise-accuracy`
Title: Java / C# Enterprise Accuracy

## Objective

Make the graph see the structures enterprise Java and C# codebases are organized around: inheritance. When this wave closes, `extends`/`implements` edges exist for both languages (today no inheritance relation exists at all), calls to supertype-defined methods and `super.` calls resolve, interface calls contribute implementations to impact analysis, and two confirmed Java import defects (wildcard truncation, spurious `external::static` edges) are fixed.

## Changes

Change ID: `1p9q9-bug java-import-wildcard-static-fixes`
Change Status: `implemented`

Change ID: `1p9qa-enh inheritance-edges-java-csharp`
Change Status: `implemented`

Change ID: `1p9qb-enh java-receiver-annotation-accuracy`
Change Status: `implemented`

All three changes are implemented (2026-07-04); the wave is ready for the implementation review, including the consolidated adversarial faithfulness lane (per the standing watchpoint) covering the wildcard-import facts (`1p9q9`), supertype binding (`1p9qa`), and the `this.<field>` receiver / declared-package keying (`1p9qb`).

Completed At: 2026-07-04

## Wave Summary

Wave `1p9qh` (Java / C# Enterprise Accuracy) delivered 3 changes: Java imports: fix wildcard-import truncation and spurious static-import edges; resolve static-import members, Graph model: extends/implements inheritance edges for Java and C#, with inherited-method resolution and dispatch-aware impact, and Java accuracy: this.field receivers, annotation-type kind fix, and package-declaration-keyed disambiguation. Notable adjustments during implementation: Java imports: fix wildcard-import truncation and spurious static-import edges; resolve static-import members: Scoped from the Java/SQL accuracy investigation. Both defects confirmed by code trace: regex character class lacks `*` (`graph_indexer.py:5240`), `static` absent from `_RELATION_KEYWORD_NOISE` (`graph_indexer.py:284-287`); no field-name match for Java `import_declaration` (`graph_indexer.py:2462`); no wildcard/static-import tests exist.; Graph model: extends/implements inheritance edges for Java and C#, with inherited-method resolution and dispatch-aware impact: Scoped from the Java/SQL accuracy investigation. Confirmed: no inheritance relations exist for any language (stop-word-only occurrences, `graph_indexer.py:1090-1096`; spec `:223,238`; live payload relation census); resolver has no supertype walk (`graph_indexer.py:5098-5102`), `super.` deferred (`:2685-2686`); Java/C# grammars expose superclass/base_list fields.; Graph model: extends/implements inheritance edges for Java and C#, with inherited-method resolution and dispatch-aware impact: Grammar probes (venv tree-sitter, both grammars): Java `class_declaration` carries `superclass` ("extends" + type) and `super_interfaces` ("implements" + `type_list`); `interface_declaration` carries `extends_interfaces`; enum/record carry `super_interfaces`; `generic_type` wraps the raw `type_identifier`; scoped supertypes are `scoped_type_identifier` (dotted text). `super.foo()` = `method_invocation` with object field node type `super`. C# `base_list` is a FLAT type list (identifier / `qualified_name` / `generic_name` / `predefined_type` for enum underlying types); `base.Foo()`'s receiver is the anonymous keyword node type `base` (NOT an identifier — the existing identifier-text check never sees it).

**Changes delivered:**

- **Java imports: fix wildcard-import truncation and spurious static-import edges; resolve static-import members** (`1p9q9-bug java-import-wildcard-static-fixes`) — 5 ACs completed. Key decisions: Structured Java import parsing (walk the `import_declaration` node) rather than widening the shared regex (approach A).; Wildcard imports participate in disambiguation with the same unique-survivor rule as explicit imports.
- **Graph model: extends/implements inheritance edges for Java and C#, with inherited-method resolution and dispatch-aware impact** (`1p9qa-enh inheritance-edges-java-csharp`) — 8 ACs completed. Key decisions: Delivery-council decision (architecture seat): the non-super inherited-bind branch of the finalize pass stays UNGATED by source-file language — a cross-language call edge whose receiver resolves by unique simple name to a Java/C# class may bind through that class's hierarchy. Accepted deliberately: phase-1 already tolerates cross-language unique-simple-name binding (the walk widens reach, not the trust model), the unique-candidate + single-definer refusals still apply, and the adversarial lane did not produce an empirical wrong bind through it. Revisit if a polyglot-repo field report shows runtime-impossible cross-language dispatch binds.; Recorded residuals (security seat, JLS corners — both narrow, documented-not-fixed): (a) JLS 8.4.8 — interface STATIC methods are not inherited, but the walk is modifier-blind, so a static interface method definer can win arbitration over a valid static import (wrong direction of F1, opposite polarity); (b) the F1 arbitration's supertype-clause gate covers `class_declaration` enclosures only — enum/record enclosures retain the pre-fix JLS-6.4.1 inversion (noted in-code). Both are recorded residuals for the next Java-accuracy pass.
- **Java accuracy: this.field receivers, annotation-type kind fix, and package-declaration-keyed disambiguation** (`1p9qb-enh java-receiver-annotation-accuracy`) — 5 ACs completed. Key decisions: Single-segment `this.field` receivers only; deeper chains keep refusing (approach A).; Package-declaration keying with directory fallback; Go unchanged.
## Journal Watchpoints

- Sequencing inside the wave: `1p9q9` (import facts) lands first; `1p9qa` supertype resolution and `1p9qb` disambiguation rekeying both consume import behavior. `1p9qa` ws3 and `1p9qb` ws1 touch disjoint branches of `_resolve_java_receiver_type` — coordinate merge order.
- Single coordinated `GRAPH_BUILDER_VERSION` bump across all three changes at integration.
- All three changes alter binding behavior: the consolidated adversarial faithfulness review lane at implementation review is required (standing security-control-faithfulness rule).
- `docs/specs/mcp-tool-surface.md` relation vocabulary is touched here (`1p9qa`) and potentially by wave `1p9qi` (`writes` relation decision) and wave `1p9q3` (report metadata) — one integration owner for that doc when multiple waves are in flight.
- Follow-up candidates deliberately not in scope: Kotlin inheritance (cheap once the model lands), deep chained receivers, `throws` edges (no consumer). Blocking: none external; C# fixtures enter the multi-language pack — coordinate fixture conventions with wave `1p9q8` if both are open near-simultaneously.
- Review follow-ups recorded, not admitted (implementation review 2026-07-04): (1) C# `using static` still emits the spurious `static` candidate through the shared regex — the sibling gap of the Java `1p9q9` fix; the AC-4 non-Java baseline pins it visible rather than fixed. (2) The 3 dot-less `external::super.<method>` markers from chained super receivers (`super.getX().y()`) are refused by the finalize pass's dot check — recall-only, no precision risk. (3) `code_references` gains no implementations bucket yet (primer F5) — the inheritance model's user-facing navigation half remains open. (4) The inheritance output pass rescans O(corpus) `calls` edges on every changed build — perf advisory; the named lever is restricting the rescan to edges whose source files or supertype adjacency changed. (5) A `binds=` counter in the graph build log (alongside existing edge counts) would make inherited-bind volume observable — nicety.

## Participants

- code-reviewer — all three changes touch `.wavefoundry/framework/scripts/*.py`
- qa-reviewer — required (bug fix `1p9q9` per `review_policies.require_qa_reviewer_for_bug_fixes`) and all change docs carry AC priority tables
- architecture-reviewer — graph-model contract change (`extends`/`implements` relation vocabulary) and resolver module seams
- performance-reviewer — resolver hot-path additions (supertype walk in the per-call resolution path)
- red-team, reality-checker — council seats (prepare phase); consolidated adversarial faithfulness lane re-runs at implementation review

## Review Checkpoints

- Prepare wave — readiness verdict (2026-07-03): READY. Council ran at standard primer depth. Red-team's strongest challenge — a single wrong supertype edge is an error *amplifier*: the inherited-method walk converts one mis-resolved `extends` target into many wrong call binds, a different risk class than one-off wrong binds — was accepted and resolved by amendment: `1p9qa` Requirement 4 now mandates bind provenance (each inherited bind records the supertype hop it resolved through), making the amplification auditable in calibration and targeted by the adversarial lane. Architecture seat confirmed the relation-vocabulary addition is the right model tier (edges, not resolution-time-only walking) and flagged the three-way `docs/specs/mcp-tool-surface.md` coordination (with waves 1p9q3 and 1p9qi) — recorded as a watchpoint. QA seat confirmed the bug change `1p9q9` carries both flip-direction tests and the no-`external::static` whole-payload assertion; reality seat confirmed grammar fields (`superclass`/`super_interfaces`/`base_list`) exist as claimed and that the C# first-base convention is inert for unresolved bases. Performance seat: the supertype walk runs only on failed direct lookups, bounded depth — acceptable; measured in calibration. Strongest alternative (resolution-time supertype walking without persisted edges) recorded and declined — the edges themselves are the durable navigation/impact value. AC priorities recorded on all three change docs. Product-owner acknowledgment: not applicable (framework-internal accuracy work).
- Security seat (rotating): no trust boundary is crossed — all three changes operate on repo-local source through existing parse paths with no new input surfaces, no user-supplied patterns, and bounded traversal (depth-capped supertype walk). Graph edges feed review-support impact analysis, so the amplification challenge above is an integrity concern; the provenance amendment plus the mandatory adversarial faithfulness lane at implementation review cover it. No security findings.
- **Prepare-phase Wave Council [prepare-council] — 2026-07-03: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, architecture-reviewer, security-reviewer, qa-reviewer, reality-checker, security-reviewer; rotating-seat: security-reviewer; strongest-challenge: a wrong supertype edge amplifies through the inherited-method walk into many wrong call binds — resolved by the 1p9qa bind-provenance amendment making amplification auditable; strongest-alternative: resolution-time supertype walking without persisted inheritance edges — declined, the edges are the durable navigation and impact value)

- pre-implementation-review: passed (2026-07-04) — Pre-mortem: (1) all three changes edit the Java extraction/resolution region of `graph_indexer.py` — strictly SEQUENTIAL builder lanes (1p9q9 → 1p9qa → 1p9qb per the recorded order; 1p9qa ws3 and 1p9qb ws1 touch disjoint branches of `_resolve_java_receiver_type`, sequenced not parallelized); (2) the supertype walk (1p9qa Req-4) is the wave's binding-faithfulness core — smallest reviewable diff, bind provenance mandatory per the council amendment, consolidated adversarial lane at review; (3) single coordinated `GRAPH_BUILDER_VERSION` bump — first lane bumps 36→37 with a changelog entry covering the wave, later lanes do NOT re-bump; (4) C# unresolved-base convention (first-base-is-class) is a recorded convention, not a guess — tests pin it; (5) calibration gates (1p9qa AC-6 precision sample ≥95%; 1p9qb AC-4 pack counts) are measurement-owned. Packet completeness: three change docs with Requirements/ACs/prepare-recorded priorities; lanes in Participants; spec-doc vocabulary edit coordinated (this wave owns `docs/specs/mcp-tool-surface.md` relation vocabulary; wave 1p9q3's edits landed first — integration owner is this wave now). Ordered lane sequence: 1p9q9 → 1p9qa → 1p9qb → integration checks. MCP-first directive carried in every lane prompt; measurements via fresh venv subprocesses (server old-code window).

- **Delivery-phase Wave Council [delivery-council] — 2026-07-04: PASS** (moderator: wave-council/coordinator; primer-depth: standard; seats: red-team (primer), architecture-reviewer, security-reviewer, qa-reviewer, reality-checker; rotating-seat: docs-contract-reviewer; strongest-challenge: the wave's riskiest mechanism shipped on n=7 fixture evidence while Tomcat-scale data sat unexamined — discharged by the scale census (2,422 `via_supertype` binds; 36/36 stratified hand-verified → precision >92% one-sided; amplification surface characterized as 187 chains / ~434 edges; post-fix recount identical); strongest-alternative: blanket static-import suppression under any supertype clause instead of the finalize marker arbitration — declined, it breaks the dominant JUnit recall case and is itself JLS-unfaithful; the deferred marker is the minimal correct shape under fragment purity). **Course of the review:** five delivery lanes (code pass-with-findings, qa PASS, architecture PASS, performance PASS — no regression, <1.5% Java-corpus overhead, dispatch sub-ms on the 38-implementation risk case with cache immutability verified) plus the mandated consolidated adversarial faithfulness lane, which found TWO BLOCKING wrong-bind classes at the mechanism-interaction seams: F1 — static-import binds preempting inherited members, inverting JLS 6.4.1 (the `import static org.junit.Assert.*` steal); F2 — the wildcard own-package-shadow guard keying on directory while the same function's tier keys on the declared package (independently converged on by the code and architecture lanes; empirically amplified through the supertype walk into untouched files). Both FIXED in-session: finalize-time `staticorinherited#` marker arbitration (JLS-faithful four-case order, refusal never falls back to the claim, unmintable invariant pinned) + shared `_pkg_key` declared-package keying for the guard; all probes re-run — 4 wrong-bind lines + 1 lost-recall attribution flipped to correct, everything else byte-identical, oracle equivalence held; 12 regression tests. A fix batch then applied the non-blocking items (graph_related inheritance groups incl. a beyond-brief emission-filter gap, Kotlin unit pin, super-marker invariant test + honest comment, doc corrections, follow-up records). **seat_agreement_aggregate:** post-fix seats `unanimous` PASS; max_severity at finding time blocking-class (the two wrong-bind findings), resolved in-session — no challenge round. Correlated-finding flags: the F2 seam was found independently by two parallel lanes before the adversarial lane confirmed it empirically (three genuine signals); seat confirmations of the primer are correlated by design. **Council items applied post-seats:** ungated non-super branch recorded as a deliberate decision with a field-report revisit trigger; JLS-corner residuals (interface-static modifier-blindness; enum/record enclosure gate) recorded; census artifact top-up note + flip-count reconciliation recorded; marker docstrings made precise (emitter-mintable scope). **Falsification check:** verdict PASS; strongest counter — the wave shipped two blocking wrong-bind classes that only the adversarial lane caught, suggesting the unit-test culture under-covers interaction seams; why it stands — that is precisely the standing security-control-faithfulness rule working as designed (the lane is mandatory for binding changes, it ran, it caught them, the fixes are probe-verified and regression-pinned, and the census bounds the shipped precision at scale). No waived lanes; no unresolved disagreements. Suite 4,438 OK post-everything.

## Review Evidence

- wave-council-readiness: approved 2026-07-03 — prepare council synthesis verdict READY after the 1p9qa bind-provenance amendment; no unresolved blocking findings
- code-reviewer: approved 2026-07-04 — pass-with-findings; seams, output-pass purity, boundary conditions and test quality verified; its lead finding (the guard keying seam) was confirmed empirically and fixed in-session
- qa-reviewer: approved 2026-07-04 — PASS; all 18 ACs attested under the truth hierarchy with independent suite re-runs; the two adversarial clauses correctly tracked open until discharged
- architecture-reviewer: approved 2026-07-04 — PASS; relation-vocabulary consumers enumerated exhaustively (one closed-set gap found and fixed), spec fidelity verified, version lattice sound
- performance-reviewer: approved 2026-07-04 — PASS; no self-hosted regression (measured), <1.5% Java-corpus overhead, sub-ms dispatch on the wide-interface risk case, cache immutability verified
- adversarial-faithfulness (consolidated, 1p9qa AC-7 + 1p9qb AC-5): approved 2026-07-04 — two blocking wrong-bind classes found, fixed in-session, probes re-run all-correct; final disposition faithful-with-residuals (residuals recorded)
- scale-calibration: approved 2026-07-04 — Tomcat census 36/36 hand-verified inherited binds, precision >92% (one-sided 95%), amplification surface characterized, post-fix recount identical
- wave-council-delivery: approved 2026-07-04 — delivery council synthesis verdict PASS; seats unanimous; both blocking findings fixed and probe-verified in-session; residuals and follow-ups recorded with named mechanisms; full synthesis in Review Checkpoints
- operator-signoff: pending operator confirmation at closure

## Dependencies

- No external wave dependencies. Synergy note: wave `1p9qi` (`1p9qg` entity mapping) builds on `1p9qa`'s annotation-argument groundwork indirectly; neither blocks the other.
