# Graph model: extends/implements inheritance edges for Java and C#, with inherited-method resolution and dispatch-aware impact

Change ID: `1p9qa-enh inheritance-edges-java-csharp`
Change Status: `implemented`
Owner: Engineering
Status: implemented
Last verified: 2026-07-04
Wave: `1p9qh java-csharp-enterprise-accuracy`

## Rationale

The graph has **no inheritance model for any language**: the emitted relation vocabulary is `defines`/`calls`/`imports`/`reads`/`reads_config`/doc-refs plus DI `binds`/`injects`; `extends`/`implements`/`throws` exist only in stop-word suppression sets (`_TS_STOP_JAVA`, `graph_indexer.py:1090-1096`), confirmed against `docs/specs/mcp-tool-surface.md:223,238` and the live payload (guru investigation, 2026-07-03).

For enterprise Java and C# — interface-driven by convention (Spring services behind interfaces, DAO/repository interfaces, .NET service abstractions) — this makes the graph blind at exactly the seams those codebases organize around:

- Interface → implementation navigation is impossible (`code_references` on an interface method finds call sites but not implementations).
- `code_impact` through an interface call dies at the interface: callers of `UserService.find()` show no blast radius into `UserServiceImpl`.
- Calls to methods defined only on a supertype never resolve: `_resolve_java_call_target` builds the literal `ReceiverType.method` and looks it up (`graph_indexer.py:5098-5102`) with no supertype walk, so `repo.save()` where `save` lives on the base class lands on `external::Repo.save` even with everything in-project.
- `super.foo()` is explicitly deferred (`graph_indexer.py:2685-2686`, "uncertain — defer inheritance walk").

Both grammars expose the facts directly (Java `superclass`/`super_interfaces` fields; C# `base_list`), and the existing unique-candidate cross-file machinery can resolve supertype names with the same discipline as receiver types. Scope is Java + C# (the two enterprise targets); Kotlin is deferred — its supertype syntax differs enough to deserve its own calibration, and the mechanism table this change builds makes adding it cheap.

## Requirements

1. **New edge relations.** `extends` (class → superclass; interface → extended interfaces) and `implements` (class → interface) join the relation vocabulary. Targets resolve through the existing import/unique-candidate machinery; unresolved supertypes get `external::<Name>` targets (qualified where the declaration is), never dropped. Confidence follows the existing taxonomy (declaration-derived resolution ≥ `RECEIVER_RESOLVED`-class evidence; ambiguous → `external::`).
2. **Java extraction.** `class_declaration.superclass`, `super_interfaces`, `interface_declaration.extends_interfaces`, `enum_declaration.interfaces`, `record_declaration.interfaces` produce the edges. Generic type arguments are stripped to the raw type name (`List<Foo>` → `List`).
3. **C# extraction.** `base_list` entries produce edges; the first base resolving to a project `class` is `extends`, project `interface` candidates are `implements`; when the kind of an unresolved base is unknowable (C# grammar does not distinguish), emit `implements` for `I`-prefixed-convention names ONLY if that convention check is measurement-validated in calibration, else emit a single conservative `extends_or_implements`-free choice: unresolved bases uniformly emit `implements` is a guess — instead emit relation `extends` for the first unresolved base and `implements` for the rest, and record the convention explicitly in the doc (C# semantics: at most one base class, listed first). Nested/qualified names normalize per `1p9q5`'s namespace keys if that change has landed; otherwise exact-string.
4. **Inherited-method resolution.** When `ReceiverType.method` fails lookup, walk the receiver's `extends`/`implements` chain (project-resolved edges only, breadth-first, bounded depth constant) and bind to `Supertype.method` when exactly **one** supertype in the walk defines it; multiple definers → `external::ReceiverType.method` (never guess an override winner). `super.foo()` resolves via the enclosing class's single `extends` target when project-resolved. **Bind provenance (council finding, prepare review 2026-07-03):** every inherited-method bind carries an edge property recording the supertype hop it resolved through — a wrong supertype edge amplifies into many wrong call binds, and provenance is what makes that failure mode auditable in calibration and adversarial review rather than invisible.
5. **Dispatch-aware impact.** `code_impact` traverses `implements`/`extends` edges in the callee direction (a call on an interface method includes implementations in the blast radius) at a distinct, documented traversal weight (down-weighted like `EXTRACTED` edges — dispatch is potential, not proven). `code_graph_path` treats inheritance edges as structural (existing high structural cost tier, like `imports`/`defines`) so call paths still dominate.
6. **Consumers + version bump.** `GRAPH_BUILDER_VERSION` bumped; `docs/specs/mcp-tool-surface.md` relation vocabulary updated; graph report/community/cluster passes accept the new relations without special-casing (verify the derived-undirected projection in `graph_cluster.py` simply includes them).
7. **Calibration gate.** Multi-language pack Java (existing) and C# fixtures gain inheritance scenarios; before/after resolution counts recorded; a hand-verified sample of inherited-method binds at ≥95% precision.
8. **Adversarial review.** Binding-faithfulness change (the supertype walk is a new binding mechanism) → adversarial review lane at wave review per the standing rule.

## Scope

**Problem statement:** No inheritance edges exist for any language, so interface→implementation structure, supertype-defined method calls, `super.` calls, and dispatch-aware impact are all invisible in the two languages whose enterprise codebases are organized around inheritance.

**In scope:**

- `extends`/`implements` extraction for Java and C#; unique-candidate supertype resolution; generic-argument stripping.
- Bounded single-definer inherited-method resolution + `super.` resolution.
- Impact/path traversal semantics for the new relations; spec doc update; version bump.
- Pack fixtures (Java + C#) with calibration counts and precision sample.

**Out of scope:**

- Kotlin, Go (embedding), Rust (traits), TS/Python inheritance — each needs its own semantics pass; the relation vocabulary this change adds is language-neutral so they can follow.
- `throws` edges (no consumer identified; revisit on demand).
- Override-graph modeling (which override wins at a virtual call site) — the single-definer rule deliberately refuses that question.
- MRO/diamond resolution beyond the single-definer refusal.

## Acceptance Criteria

- [x] AC-1: Java — classes/interfaces/enums/records with superclass/interface lists emit `extends`/`implements` edges to project-resolved targets (unique-candidate; ambiguous stays `external::`); generic arguments strip to the raw name. Unit-tested per declaration form plus an ambiguity refusal case. — `JavaCSharpInheritanceTests`: class extends+implements, generic stripping, interface `extends_interfaces`, enum/record `super_interfaces`, twin-ambiguity refusal, import disambiguation, scoped-name qualified external, non-class-twin kind-gate refusal.
- [x] AC-2: C# — `base_list` produces edges per the first-base-is-class convention; project-resolved bases get their true kind-based relation; nested/compound namespace targets resolve. Unit-tested including the unresolved-base convention case. — kind-based relations via the `declared_kind: "interface"` node property + the finalize kind-correction pass (`test_csharp_first_base_project_interface_flips_to_implements`); unresolved-base convention, interface/struct declarers, qualified base, enum-underlying-type exclusion all pinned; namespace resolution through the existing C# `cs_file_ns` machinery (`test_csharp_base_list_kind_based_relations`).
- [x] AC-3: Inherited-method resolution — `receiver.method()` where `method` is defined on exactly one project-resolved supertype binds to it; defined on two supertypes in the walk → `external::` refusal; `super.foo()` binds via the single `extends` target; depth bound respected. Unit-tested each way (single, multiple-definer refusal, super, depth cap). — bounded BFS (`_INHERITANCE_WALK_MAX_DEPTH = 5`) in the finalize output pass; every bind carries `via_supertype` hop-chain provenance (council amendment); tests cover single-definer + provenance, multi-definer refusal, Java `super.` + C# `base.` binds, unresolvable-super marker refusal, at-cap bind + over-cap refusal, never-through-external.
- [x] AC-4: `code_impact` on an interface method includes implementing classes' methods at the documented down-weighted confidence; `code_graph_path` does not prefer inheritance edges over real call chains (cost-tier test). Unit-tested against a fixture graph. — `_DEFAULT_IMPACT_RELATIONS` gains `implements`/`extends`; dispatch seed expansion (synthetic `derived: "dispatch"` edges) + relation-level `_DISPATCH_EDGE_WEIGHT` down-weight (`DispatchAwareImpactTests`, incl. `relations=` opt-out); inheritance edges cost `_PATH_COST_STRUCTURAL` in `shortest_path` (`InheritancePathCostTierTests.test_call_chain_beats_inheritance_shortcut`).
- [x] AC-5: Cluster/report/community passes ingest payloads with the new relations without error, and the graph payload `counts` remain consistent. Integration-shaped test. — verified (not assumed): `_project_undirected_projection` is relation-agnostic (`_RELATION_WEIGHTS.get(rel, 1)` default); `InheritanceRelationIngestionTests` (projection adjacency + full `update_graph_clusters` run) and `test_payload_counts_consistent_with_inheritance_edges`.
- [x] AC-6: Calibration recorded — pack Java + C# inheritance fixtures pass; before/after resolution counts and a hand-verified ≥95%-precision sample of inherited-method binds in the Progress Log. — see Progress Log 2026-07-04 rows: self-hosted census (0 Java/C# files → change inert by construction, before == after), enterprise-shaped Java+C# fixture project, 7/7 inherited binds hand-verified correct (100%, target ≥95%), 2/2 refusal traps refused. The multi-language consumer pack (`p4ea`) is an external oracle not present in this repo — fixtures landed as unit-test fixtures per its Java/C# conventions instead. Evidence upgraded 2026-07-04 (red-team primer challenge: n=7 fixture sample was anecdote-grade for the amplification risk): Tomcat 9.0.84 scale census — 1,830 inheritance edges (1,334 project-resolved), 2,422 `via_supertype` binds (1,983/407/32 by depth), amplification surface characterized (187 chains / ~434 edges), **36/36 stratified hand-verified binds correct → precision > 92% (one-sided 95% bound)**; post-F1/F2-fix recount identical. Full census row below. Artifact note (reality seat): census.json's `sample` block records the initial n=30 draw; the authoritative verification record is dossier.txt with the 12/12/12 top-up = 36 binds — the 36/36 and >92% figures are computed on the dossier. Flip accounting (reality seat reconciliation): 5 probe scenarios flipped post-fix — 4 wrong-bind lines (A8, A8b, A9, F2-AMP) plus A8-EXT, whose pre-fix state was a lost-recall external attribution rather than a wrong bind; the probe1 diff is otherwise byte-identical.
- [x] AC-7: `GRAPH_BUILDER_VERSION` bumped; `docs/specs/mcp-tool-surface.md` relation vocabulary updated; adversarial review lane run and findings dispositioned. — *version + spec halves DONE:* v37 coordinated single bump landed with `1p9q9` and its changelog covers this change (no re-bump per the pre-implementation review); spec `code_impact`/new `code_graph_path` entries + `code_search` graph-neighbors note updated; the `server_impl.py` `code_impact` MCP-wrapper docstring gained the dispatch-traversal note (down-weighted `derived: "dispatch"` edges + `relations=` opt-out; previously unclaimed here — qa lane); `docs/architecture/graph-index-system.md` Edge Relations table + single-definer decision note recorded. *Adversarial review lane runs at wave review, not in this lane — left unchecked until dispositioned.* — Adversarial clause DISCHARGED 2026-07-04: the consolidated faithfulness lane ran 26 empirical probes (JLS as external oracle); core refusal machinery held everywhere aimed (twin refusals, diamond/multi-definer, external-bridge stop, super-with-own-override, provenance honesty, incremental≡oracle at both seams), and its two blocking interaction-seam findings — static-import binds preempting inherited members (JLS 6.4.1 inversion, the JUnit-idiom steal) and the wildcard own-package-shadow guard's directory-vs-declared keying (amplifying through the walk) — were FIXED in-session (finalize-time `staticorinherited#` marker arbitration + declared-package `_pkg_key` guard rekey); all probes re-run with exactly the four wrong-bind lines flipping to correct and everything else byte-identical; 12 regression tests added. Post-fix verdict: faithful-with-residuals (residuals: documented give-ups + the recorded follow-up candidates in wave.md watchpoints).
- [x] AC-8: `python3 .wavefoundry/framework/scripts/run_tests.py` passes; `wave_validate` clean; no `__pycache__` under `scripts/`. — 4,407 tests OK across 42 files (incl. 34 new: 21 indexer + 7 query + 2 cluster + 4 incremental-differential); `wave_validate` clean; `__pycache__` cleaned.

## Tasks

- [x] Add the `extends`/`implements` relations to the graph model constants and emission paths; Java extraction from the five declaration forms; generic stripping. — `_INHERITANCE_RELATIONS`/`_INHERITANCE_WALK_MAX_DEPTH`/`_SUPER_CALL_PREFIX`; `_java_supertype_facts` + `_java_supertype_name` (superclass / super_interfaces / extends_interfaces, grammar-probed); `buffered_supertypes` drain in `_extract_tree_sitter_artifact`.
- [x] C# extraction from `base_list` with the first-base convention; namespace-key normalization hook. — `_csharp_supertype_facts` + `_csharp_supertype_name` (interface→extends, struct→implements, class/record first-base convention recorded in-code; `predefined_type` enum-underlying-clause excluded); namespace resolution rides the existing `cs_file_ns` machinery (`1p9q5` keys not landed — exact-string per the doc).
- [x] Supertype unique-candidate resolution reusing the import/disambiguation machinery (including `1p9q9`'s wildcard facts when landed). — `_resolve_fragment_edge` routes `extends`/`implements` through `_resolve_external_call_target` (imports, wildcard facts, same-dir, C# namespace) with a class-kind gate; `_edge_lookup_keys` extended so supertype edges re-resolve on symbol deltas (differential-tested: twin add demotes / delete promotes in untouched files).
- [x] Inherited-method walk (bounded BFS, single-definer rule) + `super.` resolution in `_resolve_java_call_target` and the C# resolver. — `_apply_inheritance_output_passes` (finalize OUTPUT pass, recomputed fresh each build so fragments stay pristine and incremental == full by construction); `super.`/`base.` calls emit the reserved `external::super.<Class>.<method>` marker from `_resolve_java_call_target`/`_resolve_csharp_call_target` (the 1p9qb branches of `_resolve_java_receiver_type` untouched).
- [x] Impact/path traversal weights for the new relations; spec doc vocabulary update. — `graph_query`: `_DISPATCH_RELATIONS`/`_DISPATCH_EDGE_WEIGHT` (EXTRACTED tier), dispatch seed expansion, defaults updated; `_path_edge_cost` keeps inheritance structural; spec + architecture doc updated (see AC-7 note).
- [x] Pack fixtures + unit tests per AC-1..AC-5; calibration run + precision sample. — 34 new tests across `test_graph_indexer.py` / `test_graph_query.py` / `test_graph_cluster.py` / `test_graph_incremental_merge.py`; calibration + 100% precision sample in the Progress Log (external consumer pack not in this repo — unit-test fixtures per its conventions).
- [x] Bump `GRAPH_BUILDER_VERSION`; run `run_tests.py` + `wave_validate`; clean `__pycache__`. — v37 coordinated single bump landed with `1p9q9`, changelog already covers `1p9qa` (audit-and-skip: no re-bump); 4,407 tests OK; `wave_validate` clean; `__pycache__` cleaned.

## Agent Execution Graph


| Workstream | Owner | Depends On | Notes |
| ---------- | ----- | ---------- | ----- |
| ws1-model-and-java | implementer | — | Relation constants + Java extraction + supertype resolution. |
| ws2-csharp | implementer | ws1-model-and-java | C# `base_list` extraction on the settled model. |
| ws3-inherited-resolution | implementer | ws1-model-and-java | Bounded walk + single-definer rule + `super.`; the binding core, smallest reviewable diff. |
| ws4-consumers | implementer | ws1-model-and-java | Impact/path weights; cluster/report ingestion check; spec update. |
| ws5-tests-calibration | implementer | ws2-csharp, ws3-inherited-resolution, ws4-consumers | Fixtures, adversarial cases, calibration + precision sample. |
| ws6-adversarial-review | reviewer | ws5-tests-calibration | Faithfulness red-team on the supertype walk (wrong-definer, diamond, external-bridge cases). |


## Serialization Points

- Land after `1p9q9` (import fixes) — supertype resolution consumes import facts; building on broken wildcard handling would double-test the same paths.
- The relation-vocabulary constants (ws1) gate every other workstream.
- Coordinate the wave-level `GRAPH_BUILDER_VERSION` bump; `docs/specs/mcp-tool-surface.md` is also touched by wave `1p9q3` (`wave_graph_report` metadata) — merge order on that doc at integration.

## Affected Architecture Docs

`docs/specs/mcp-tool-surface.md` — relation vocabulary (`:223,238`) gains `extends`/`implements`, and `code_impact`/`code_graph_path` entries document dispatch traversal semantics. Any capability matrix describing per-language extraction gains the inheritance column. This is a graph-model contract change — record the relation addition and the single-definer refusal stance as a decision note.

## AC Priority

(Populated at Prepare wave.)


| AC | Priority | Rationale |
| ---- | -------- | --------- |
| AC-1 | required | Java inheritance edges are the core capability. |
| AC-2 | required | C# parity is the operator-directed scope of this wave. |
| AC-3 | required | Inherited-method resolution is the recall payoff; the refusal cases are its faithfulness guard. |
| AC-4 | required | Wrong traversal semantics would corrupt impact/path results for every consumer. |
| AC-5 | required | New relations must not break downstream analysis passes. |
| AC-6 | required | Calibrate-don't-guess; the precision sample is the shipping evidence. |
| AC-7 | required | Standing version/spec/adversarial-review rules for model + binding changes. |
| AC-8 | required | Suite + docs-lint green is the standing merge gate. |


## Progress Log


| Date | Update | Evidence |
| ---- | ------ | -------- |
| 2026-07-03 | Scoped from the Java/SQL accuracy investigation. Confirmed: no inheritance relations exist for any language (stop-word-only occurrences, `graph_indexer.py:1090-1096`; spec `:223,238`; live payload relation census); resolver has no supertype walk (`graph_indexer.py:5098-5102`), `super.` deferred (`:2685-2686`); Java/C# grammars expose superclass/base_list fields. | Guru investigation 2026-07-03. |
| 2026-07-04 | Grammar probes (venv tree-sitter, both grammars): Java `class_declaration` carries `superclass` ("extends" + type) and `super_interfaces` ("implements" + `type_list`); `interface_declaration` carries `extends_interfaces`; enum/record carry `super_interfaces`; `generic_type` wraps the raw `type_identifier`; scoped supertypes are `scoped_type_identifier` (dotted text). `super.foo()` = `method_invocation` with object field node type `super`. C# `base_list` is a FLAT type list (identifier / `qualified_name` / `generic_name` / `predefined_type` for enum underlying types); `base.Foo()`'s receiver is the anonymous keyword node type `base` (NOT an identifier — the existing identifier-text check never sees it). | Probe scripts run 2026-07-04 against tree-sitter-java / tree-sitter-c-sharp in `~/.wavefoundry/venv`. |
| 2026-07-04 | Implemented: relation constants + Java/C# supertype extraction (buffered, same-file binds at RECEIVER_RESOLVED, else qualified-as-declared `external::` at EXTRACTED); cross-file supertype resolution through `_resolve_external_call_target` (imports + 1p9q9 wildcard facts + same-dir + C# namespace) with a class-kind gate and exact-unique promotion; `declared_kind: "interface"` node property; finalize OUTPUT pass `_apply_inheritance_output_passes` (C# base-relation kind correction; bounded single-definer inherited-method walk with `via_supertype` hop-chain provenance; `super.`/`base.` binding via the reserved `external::super.` marker); `_edge_lookup_keys` extended for the new relations; `graph_query` dispatch-aware impact + structural path costs. | Full suite 4,407 tests OK (34 new); differential harness extended with Java inheritance scenarios — twin add/delete demote/promote in untouched files, definer removal/restore rebinds untouched callers, super-marker equivalence (incremental == oracle at every step). |
| 2026-07-04 | AC-6 calibration, self-hosted repo (fresh venv-subprocess rebuild into a temp index dir): 547 files, 11,866 nodes, 34,935 edges; calls 15,807 = 8,629 resolved / 7,178 external; relation census contains NO `extends`/`implements` and 0 `via_supertype` binds — the repo has **zero** `.java`/`.cs` files, so the change is inert here by construction and before/after resolution counts are identical. As directed, the meaningful sample comes from fixtures (below); the external Java/C#/TS consumer projects (`p4ea` pack) remain the real-world oracle at release validation. | `/tmp/calib_selfhost.py` census 2026-07-04; `git ls-files` confirms 0 Java/C# sources. |
| 2026-07-04 | AC-6 precision sample, enterprise-shaped Java+C# fixture project (25 files: Spring-ish repository/service hierarchy, .NET service abstraction, default interface method, depth-2 chain, multi-definer trap, cross-package twin trap). All 7 inherited-method binds hand-verified against source — 7/7 correct (100%, target ≥95%): (1) Java `repo.save()` → `BaseRepository.save` via [BaseRepository]; (2) Java `repo.flush()` → `BaseRepository.flush` via [BaseRepository]; (3) Java `super.save()` → `BaseRepository.save` via [BaseRepository]; (4) Java `svc.audit()` → default interface method `IAudit.audit` via [IAudit]; (5) Java depth-2 `x.deepMethod()` → `L3.deepMethod` via [L2, L3]; (6) C# `base.Validate()` → `EntityBase.Validate` via [EntityBase]; (7) C# cross-file `svc.Validate()` → `EntityBase.Validate` via [EntityBase]. Both refusal traps refused correctly: `d.log()` (defined on base class AND interface) stayed `external::DualChild.log`; `p.render()` (supertype twin ambiguity → extends stayed external) stayed `external::Panel.render`. All 10 extends/implements edges in the fixture carry the correct relation and kind (C# first-position project interface flipped to `implements`). | `/tmp/calib_fixture.py` run 2026-07-04; bind/refusal listing recorded in the lane transcript. |
| 2026-07-04 | Scale calibration (council-mandated provenance audit; discharges the red-team primer's n=7 challenge): census of Apache Tomcat 9.0.84 `java/` (1,794 files; 49,732 nodes / 182,128 edges) built once with the working-tree indexer. Inheritance edges: 1,830 total — `extends` 1,028 (836 project-resolved / 192 external), `implements` 802 (498 / 304). `via_supertype` binds: 2,422 (100% RECEIVER_RESOLVED), depth 1/2/3+ = 1,983/407/32; 187 distinct chains routing through ~434 distinct supertype edges (159 first-hop supertypes, 151 definer classes) — mean amplification ~5.6 binds per supertype edge; top chains AbstractChunk (165 binds), ServletRequest (143). Persisting `external::super.*` markers (primer F1's unmeasured population): 386 across 172 enclosing classes — 299 are multi-definer refusals (interface redeclaration + class definition both in the walk, e.g. TcpFailureDetector 13), 71 genuinely library-extending (no project parent, e.g. UniqueAttributesImpl extending SAX AttributesImpl), 13 zero-definers-in-walk, 3 dot-less `external::super.getX` markers from chained `super.getX().y()` receivers (inert: pass 2's dot check refuses them; tiny recall gap, no precision risk). Precision: 36-bind stratified hand-verification against Tomcat source (12 depth-3+ / 12 depth-2 / 12 depth-1, seed 1909; the mandate's "all depth-2+ first" was infeasible at 439 deep binds, so risk-weighted strata instead) — per bind: caller site, receiver's declared type, full supertype chain, and definer read in Java source. Verdict 36/36 correct, 0 errors, including super-path binds where the receiver overrides the method, an override of external Ant `Task.handleOutput` correctly bound to the project override, inherited static varargs (`CloudMembershipProvider.getEnv`), package-private final (`swallowException`), interface-only definers (`Lifecycle.start` via `Container`), and nested-class hierarchies (`Node.CustomTag` via `ChildInfoBase` with sibling overrides in `JspText`/`TemplateText` correctly outside the chain). One-sided 95% lower confidence bound at n=36 / 0 errors: precision above 92.0% (0.05 to the 1/36 power); exceeds the mandated n=30 bound (90.5%). No bind routed through a wrong-looking supertype edge; confidence distribution sane by construction. | Scripts, payload, census.json, sample.json, and per-bind dossier at `/Users/coryhacking/.claude/jobs/e29ad14d/tmp/census-1p9qh/` (build_payload.py, census.py, verify_dossier.py), corpus `/Users/coryhacking/Developer/aceiss/apache/tomcat-9.0.84/java`, 2026-07-04. |
| 2026-07-04 | **Adversarial finding F1 fixed (fix lane).** Finding: static-import binds preempted inherited-member resolution, inverting **JLS 6.4.1 (the oracle)** — members in class scope INCLUDING INHERITED ones shadow single-static and static-on-demand imports, but `_resolve_java_call_target` checked only same-file definitions before the static-import claim, so `import static com.util.Maths.*;` stole `helper()` from an inherited `Base.helper` (probes A8/A8b) and even a plain JUnit-idiom wildcard erased a correct inherited bind (probe A8-EXT). Fix: when a bare call would bind via a static-import fact AND the enclosing class has a supertype clause, extraction emits the reserved deferred marker `external::staticorinherited#<Enclosing>.<method>#<claim>` (unmintable: `#` cannot appear in an identifier of any indexed language; a Java class literally named `staticorinherited` yields the non-matching dot form — invariant pinned by test); phase-1 fragment re-resolution passes it through untouched (mirrors the `super.` marker); the finalize output pass (`_arbitrate_static_or_inherited`, called from `_apply_inheritance_output_passes` with the phase-1 `resolve_ctx`) arbitrates JLS-faithfully: single inherited definer → bind with `via_supertype`; MULTIPLE definers → refuse `external::<Enclosing>.<method>` (inherited members exist, so the static import stays shadowed — never "fall back" to the claim on ambiguity); NO definer → the static claim stands, resolved exactly as phase 1 would (project bind or qualified external). No supertype clause → today's direct static bind, no marker. Markers always rewrite (never leak into payloads; arbitration runs even with an empty supertype adjacency); fragments stay pristine so incremental == full by construction — no new `_edge_lookup_keys` shape needed (documented in its docstring). Probes A8/A8b/A8-EXT flip to correct; A4/A5 (same-file precedence, two-wildcard refusal) and every other probe scenario byte-identical; B8/C7 differential oracle-equivalence still True. Tests: `JavaStaticImportInheritedShadowTests` (9: explicit + wildcard shadowed by inherited definer; JUnit idiom both directions — steal refused AND no-definer claim stands external + project; no-supertype direct bind; multi-definer + static import refusal; marker-never-leaks; marker-unmintable invariant incl. a C# `staticorinherited` class) + incremental differential `test_static_import_supertype_definer_flip_differential` (definer added/removed in a separate file flips the untouched static-importing caller, oracle-equivalent at every step). Full suite 4435 OK. No `GRAPH_BUILDER_VERSION` re-bump (v37 covers the wave; pre-release fix to unreleased behavior). | `graph_indexer.py` (`_STATIC_OR_INHERITED_PREFIX`/`_STATIC_OR_INHERITED_SEP`, `_java_enclosing_has_supertype_clause`, `_resolve_java_call_target`, `_resolve_fragment_edge` pass-through, `_arbitrate_static_or_inherited`, `_apply_inheritance_output_passes(resolve_ctx=…)`); `tests/test_graph_indexer.py::JavaStaticImportInheritedShadowTests`; `tests/test_graph_incremental_merge.py::test_static_import_supertype_definer_flip_differential`; probe re-runs 2026-07-04 (diff vs pre-fix baseline shows exactly the A8/A8b/A9/A8-EXT/F2-AMP flips); `run_tests.py` 4435 tests OK. |
| 2026-07-04 | Post-F1/F2-fix census recount on Tomcat 9.0.84: IDENTICAL to the pre-fix scale calibration — 1,830 inheritance edges / 2,422 `via_supertype` binds, same depth split (1,983/407/32). The corpus contains neither defect trigger, so both fixes are behavior-preserving at scale. | Census re-run 2026-07-04, same scripts and corpus as the scale-calibration row above. |


## Decision Log


| Date | Decision | Reason | Alternatives |
| ---- | -------- | ------ | ------------ |
| 2026-07-04 | Delivery-council decision (architecture seat): the non-super inherited-bind branch of the finalize pass stays UNGATED by source-file language — a cross-language call edge whose receiver resolves by unique simple name to a Java/C# class may bind through that class's hierarchy. Accepted deliberately: phase-1 already tolerates cross-language unique-simple-name binding (the walk widens reach, not the trust model), the unique-candidate + single-definer refusals still apply, and the adversarial lane did not produce an empirical wrong bind through it. Revisit if a polyglot-repo field report shows runtime-impossible cross-language dispatch binds. | Council Q2 disposition; adversarial lane F2 design note. |
| 2026-07-04 | Recorded residuals (security seat, JLS corners — both narrow, documented-not-fixed): (a) JLS 8.4.8 — interface STATIC methods are not inherited, but the walk is modifier-blind, so a static interface method definer can win arbitration over a valid static import (wrong direction of F1, opposite polarity); (b) the F1 arbitration's supertype-clause gate covers `class_declaration` enclosures only — enum/record enclosures retain the pre-fix JLS-6.4.1 inversion (noted in-code). Both are recorded residuals for the next Java-accuracy pass. | Security seat findings SEC-1/SEC-2, 2026-07-04. |
| 2026-07-03 | Model-level relations + single-definer inherited resolution + down-weighted dispatch traversal, Java + C# together (approach A). | One builder-version bump and one consumer migration for both enterprise languages; the single-definer rule extends the never-guess stance to inheritance (refuses override-winner questions); down-weighting keeps dispatch potential from polluting proven call paths. | (B) Java only, C# later — weakness: second model bump + second consumer pass for a language whose grammar exposes identical facts; operator directed C# inclusion. (C) Resolve inherited methods without persisting inheritance edges (walk at resolution time only) — weakness: leaves interface→implementation navigation and dispatch impact unsolved; the edges are the durable value, resolution is a beneficiary. (D) Full override modeling (virtual dispatch winners) — weakness: unsound without whole-program analysis; the single-definer refusal is the faithful boundary. |
| 2026-07-03 | Kotlin deferred. | Different supertype syntax (constructor-call supertypes, delegation `by`) deserves its own fixtures/calibration; the language-neutral relation vocabulary makes the later addition cheap. | Include Kotlin now — rejected: scope growth in the wave's riskiest change; revisit after Java/C# calibration. |
| 2026-07-03 | `throws` not added. | No consumer identified for exception-flow edges; adding unconsumed relations violates simplest-solution-first. | Add for completeness — rejected: speculative. |


## Risks


| Risk | Mitigation |
| ---- | ---------- |
| The supertype walk binds through a wrong-twin supertype (ambiguous supertype name resolved incorrectly). | Supertype names resolve through the same unique-candidate machinery with refusal; the walk uses only project-resolved edges (never walks through `external::`); adversarial review targets wrong-twin and diamond shapes; AC-3 refusal tests. |
| Dispatch traversal explodes impact results on wide interfaces (one interface, 40 implementations). | Down-weighted confidence + existing impact weakest-link combining keeps dispatch-derived paths visibly lower-confidence; result-shaping limits unchanged; calibration observes result-size deltas on pack fixtures. |
| C# first-base-is-class convention mislabels an unresolved interface as `extends`. | The relation kind for project-resolved bases uses the true target kind; the convention applies only to unresolved (`external::`) bases where the mislabel is inert for resolution and impact (both relations traverse identically); documented explicitly. |
| Downstream consumers (cluster projection, report) choke on unknown relations. | AC-5 ingestion test; the cluster projection is relation-agnostic (derived-undirected) by design — verified, not assumed. |


## Session Handoff

See `docs/agents/session-handoff.md` for current session state.
