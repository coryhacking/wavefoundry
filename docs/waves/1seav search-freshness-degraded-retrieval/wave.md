# Wave Record

Owner: Engineering
Status: closed
Last verified: 2026-07-14

wave-id: `1seav search-freshness-degraded-retrieval`
Title: Search Freshness Degraded Retrieval

## Objective

Land the two highest-value findings from the 2026-07-12 external code review, both validated against source and both riding the substrate the 1sbfk/1sc7c waves just built: (1) `code_ask`'s per-question freshness check is simultaneously the most expensive part of its envelope (an O(corpus) hash walk its own docstring forbids on the hot path) and wrong (chunker-mismatch-only, exceptions read as "current") — replace it with the cheap per-layer signal and an honest three-state contract; (2) semantic-path failure currently disables search entirely (`code_search` errors when the model is unavailable) or triggers a per-call corpus re-chunk with silent filter loss (`docs_search`'s pre-FTS fallback) — degrade FTS-first with preserved filters and a uniform typed `search_mode`/`fallback_reason` contract. Ships in the next release (1.13.0 per the 12tm5 versioning ADR — the bundle adds new tool surface and changes a field's meaning).

## Changes

Change ID: `1sbxq-bug code-ask-freshness-signal`
Change Status: `implemented`

Change ID: `1seaq-enh fts-first-degraded-search-fallback`
Change Status: `implemented`

Completed At: 2026-07-13

## Wave Summary

Wave `1seav` (Search Freshness Degraded Retrieval) delivered two changes: `code_ask` Freshness Is Hot-Path-Expensive and Wrong (Ignores Stale Paths, Maps Errors to "current") and FTS-First Degraded Search: Serve Lexical Results When the Semantic Path Is Unavailable. Notable adjustments during implementation: `code_ask` Freshness Is Hot-Path-Expensive and Wrong (Ignores Stale Paths, Maps Errors to "current"): CLOSE-READINESS REVIEW (external, executed): AC-2/AC-3 reopened and re-met. Reproduced false-current: (a) full build → ADD code file → docs-only build (the broad snapshot stamps the new file; my layer compare only iterated recorded paths) and (b) unreadable `layer_hashes` → current. FIXED: `project_layer_freshness` rewritten around ONE walk with the build's own per-layer eligibility sets — the layer compare is now SYMMETRIC (recorded-path drift/deletion AND eligible-path-never-processed), layer hashes compare against the CURRENT walk hashes, and an unreadable REQUIRED layer (non-empty eligible set) reads unknown. Fixtures: `test_added_code_file_survives_docs_only_build` (the reviewer's exact reproduction + recovery), `test_unreadable_required_layer_state_reads_unknown`, exception-seam fixtures updated to the real seams. AC-5 gap closed: `mcp-tool-surface.md` now defines the three-state `index_freshness` contract.; `code_ask` Freshness Is Hot-Path-Expensive and Wrong (Ignores Stale Paths, Maps Errors to "current"): IMPLEMENTED (helper + wiring + cache + fixtures): `indexer.project_layer_freshness` combines the stat-fast-path walk (edits since any build) with per-layer `layer_path_state` hash compare against the broad snapshot (the layer-crossing signal — pure store reads) plus the chunker-version check; store-only, meta.json-free (Req 0). `server_impl._index_freshness_verdict` caches with BOTH axes (5 s root-scoped TTL + `_epoch_state` token). `code_ask_response` freshness = the verdict's three-state; `_layer_health` call removed (source-pinned) and docstring updated. Fixtures: `ProjectLayerFreshnessTests` (current-after-build, simple edit, BOTH layer-crossing directions with the second-scoped-build persistence check, added+deleted paths, empty-layer control, chunker mismatch, no-store→unknown, exception→unknown) 9/9; `FreshnessCacheAxesTests` (TTL axis with generation pinned unchanged; epoch axis without TTL wait; unknown passthrough; exception→unknown) 4/4; envelope passthrough + hot-path source pin in CodeAskTests. AC-1 latency evidence (live, this repo): OLD per-call `_layer_health` walk 457.1 ms → NEW 292.8 ms cold (at most once per TTL window/build transition) and 0.32 ms cached median — and the cold verdict honestly read `stale` against the uncommitted working tree.; `code_ask` Freshness Is Hot-Path-Expensive and Wrong (Ignores Stale Paths, Maps Errors to "current"): PRE-IMPLEMENTATION PLAN REVIEW (external) — P0 repair: Requirement 3's "TTL OR generation-keyed" wording permitted generation-only invalidation, which cannot detect source edits (the generation advances only at build finalization — an edit between builds would read `current` indefinitely). Now BOTH axes are required: root-scoped seconds-scale TTL (bounds edit-detection latency) AND build-generation invalidation (immediate post-build refresh); AC-4 rewritten to pin both sides independently. P1 repair: AC-2 freshness coverage was asymmetric — added the inverse docs-edit/code-only-build regression (code_ask searches both layers), added/deleted-path cases, and the legitimately-empty-layer control.

**Changes delivered:**

- **`code_ask` Freshness Is Hot-Path-Expensive and Wrong (Ignores Stale Paths, Maps Errors to "current")** (`1sbxq-bug code-ask-freshness-signal`) — 5 ACs completed. Key decisions: Freshness = stat-fast-path + chunker meta, three states, short cache — NOT a per-call hash walk and NOT dropping the check entirely.
- **FTS-First Degraded Search: Serve Lexical Results When the Semantic Path Is Unavailable** (`1seaq-enh fts-first-degraded-search-fallback`) — 7 ACs completed. Key decisions: Degrade FTS-first; the live filesystem walk fires only when no complete published epoch is available — absent, uninitialized, stable building/interrupted, or unreadable/corrupt (the Requirement 2a state table).; Typed `search_mode`/`fallback_reason`, NOT correlation IDs or a failure-taxonomy framework.
## Journal Watchpoints

- Watchpoint (shared envelope): both changes touch the search response envelopes — land the field names (`index_freshness` three-state, `search_mode`, `fallback_reason`) ONCE, coherently, in the same vocabulary across `code_ask`/`code_search`/`docs_search`.
- Watchpoint (honesty rule): exceptions/undeterminable states must surface as `unknown`/typed reasons — never silently `current` or bare empty results. This is the same lesson 1sbfj already paid for; do not re-learn it.
- Watchpoint (fail-soft preserved): degradation returns results wherever the PUBLISHED store can serve (captured complete epoch); the live filesystem walk survives ONLY for the no-published-epoch states (absent/uninitialized/building/interrupted), and a pin proves it unreachable when the store is healthy.
- Watchpoint (latency evidence): AC-1 of `1sbxq` requires before/after timing on this repo — the removed walk must be visible in the numbers, not asserted.

## Review Checkpoints

- **Prepare-phase Wave Council [prepare-council] — 2026-07-12: PASS** (moderator: wave-council; primer-depth: standard; seats: reality-checker, red-team, qa-reviewer, architecture-reviewer; rotating-seat: architecture-reviewer; strongest-challenge: the replacement freshness signal (stat-fast-path + chunker meta) can false-negative on a content change that preserves mtime/size/inode — resolved by accepting the SAME documented trade the staleness monitor already makes, with two containments: the per-layer build path (1sc7c) remains the authority (this is an advisory envelope signal, not a build trigger) and indeterminacy reports `unknown`, never `current`; strongest-alternative: drop `index_freshness` from `code_ask` entirely — rejected because seed-211 documents and routes on it; an honest cheap signal beats both an expensive wrong one and none)
- Council seat notes: reality-checker — every claim source-verified THIS session against `2952df8f`: `_layer_health` called per `code_ask` (~17305) in direct contradiction of its own docstring (~639), chunker-only staleness (~17306-08) discarding the `stale_paths` just computed, `except → current` (~17309-11); `search_code` embeds before any fallback (~1670); `_live_docs_chunks` per-call walk with its own hot-path warning (~766) and tag-filter loss (~836); the reviewer's `current`-while-stale live repro is consistent with the code as read. The claimed substrate exists: `project_index_inputs_stale` (monitor-proven), `layer_path_state` (1sc7c), and `code_lexical_response` as the reusable FTS-serving path. red-team — pressed lexical-fallback quality expectations (an agent treating BM25-only results as semantic recall): contained by the explicit `search_mode` field, seed-211 interpretation guidance, and the zero-hit token-semantics note; pressed cache poisoning on the freshness verdict: TTL is seconds-scale and invalidation keys on the build/meta signature, fixture-pinned (AC-4); pressed filter-preservation completeness: `tags` is empty in production corpora (known from 1sbfj) — the fixture must pin pass-through semantics, not invent tag data. qa-reviewer — the fixture matrix is enumerated in the ACs (stale-path, exception→unknown, cache invalidation, both tools' fallbacks with every filter, live-walk unreachable-when-healthy pin, disabled-embedder live probe) and the existing coverage gap is named (chunker-mismatch-only at ~10281). architecture-reviewer — the two changes share envelope territory and MUST land one vocabulary (`index_freshness` three-state, `search_mode`, `fallback_reason`) across the three search tools; the degradation ladder (semantic → hybrid → FTS → live-walk-only-if-store-absent) belongs in `search-architecture.md`; fail-soft is preserved, visibility added — the 1sbfj pattern applied to search. seat_agreement: unanimous.
- AC priority: confirmed at prepare as proposed (all required across both changes). Product-owner acknowledgment: operator-directed 2026-07-12 ("build these waves now"); release candidate 1.12.1 alongside `1sbfl`.

- **Prepare-council amendment [prepare-council-amendment] — 2026-07-12:** external plan review caught that the recorded council lacked the brief's required rotating seat (`docs-contract-reviewer`); the seat was run post-hoc against the change docs and tree. **docs-contract-reviewer** — both changes enumerate their documentation-surface obligations completely: `1sbxq` names the spec's `code_ask` response fields and seed-211's `index_freshness` guidance (the three-state contract must land in BOTH, and seed-211's current wording documents only two states — confirmed by read); `1seaq` names the spec search contracts, `search-architecture.md` (degradation ladder), seed-211 fallback interpretation, and `data-and-control-flow.md` Path 6. Seat findings (non-blocking, folded into scope): the affected TOOL DOCSTRINGS (`docs_search`/`code_search`/`code_ask` registrations) are part of the public contract and must carry the new `search_mode`/`fallback_reason`/three-state wording in the same change — added to `1seaq` tasks by this amendment; rotating-seat labeling in the original verdict line reflected the pre-brief guess and is superseded by this amendment. Verdict unchanged: READY.

## Review Evidence

- **Pre-implementation plan review (external, 2026-07-13): BLOCKED → REPAIRED.** The automated gates passed but the substantive review found two P0 plan gaps (both post-dating the original council: wave `1sed7` shipped between readying and implementation): (1) `1seaq` left incomplete-epoch behavior undefined and its live-walk restriction conflicted with the shipped SQLite-only reader contract — repaired with a full per-state behavior table (Requirement 2a: absent/uninitialized/building/interrupted/unreadable/transition, per tool), FTS serving bound to the CAPTURED complete token under 1sed7's single-capture discipline, the strict code-tool lockout preserved verbatim, `index_not_ready` + always-present `fallback_reason: null` + `search_mode: exact` added to the typed contract, a typed `{available, failure_reason, results, coverage}` shared-serving result (replacing the exception-to-`[]` collapse), a concrete per-layer filter contract with independent FTS/live-walk fixtures, AC-6/AC-7 added to the priority table and tasks, and deduplicated store-log persistence (P2); (2) `1sbxq`'s cache clause permitted generation-only invalidation, which is blind to source edits — repaired to require BOTH a root-scoped seconds-scale TTL and build-generation invalidation, with AC-4 pinning each axis independently, plus the inverse layer-crossing regression and added/deleted/empty-layer freshness coverage. Both change docs carry the full repair rationale in their progress logs.
- wave-council-readiness: approved 2026-07-12 — prepare council synthesis verdict READY: both changes are source-validated repairs with the enabling substrate already shipped (1sbfk/1sc7c/`code_lexical`), the fixture matrices are enumerated in the ACs, and the one accepted trade (stat-fast-path false negatives) is contained by authority separation plus the `unknown` state. Seats unanimous; full synthesis in Review Checkpoints.
- **Delivery verification [delivery-council] — 2026-07-13:** implementation verified by a fresh-context adversarial agent with EXECUTED probes (the 1sed7 lesson applied): epoch discipline end-to-end through the real registered tools (zero FTS serves across all non-complete captured states; exactly one state capture per call; mid-operation fence discarded with no payload leak; live-walk single call site reachable only under a non-complete token — executed pin), full filter contract on a real store, cache-axis correctness (no stale-after-finalize window; root isolation; 8-thread hammer clean), and edge-case honesty (symlinks, permissions, missing config). Two findings fixed and fixture-locked same-session: the unreadable-modified-file current-coercion in the freshness walk (now unknown), and partial-store-corruption zero-hit masking in the FTS serving path (now typed query_failed via the new `fts_probe`, with the zero-hit note gated on a WORKING fallback). One release-note item recorded: `docs_search`'s `search_mode: lexical_fallback` changed meaning (was the live walk; now BM25-from-FTS with `live_fallback` for the walk). Suite 4,940 OK ×2; live probes: disabled-embedder serves real BM25 results on all three tools with the typed contract; healthy paths carry `fallback_reason: null`; `code_ask` freshness honestly reads `stale` against the uncommitted tree at 0.32 ms cached (was a 457 ms per-call walk).
- wave-council-delivery(superseded): the 2026-07-13 approval was WITHDRAWN — the external close-readiness review executed adversarial probes that reproduced a P0 (code_search's epoch fence closed BEFORE graph augmentation, serving mixed graph output) and six P1s the in-wave verification had not exercised (freshness false-current on added-then-stamped files and unreadable layer state; live-walk tag loss — the ORIGINAL 1seaq defect; partial-FTS-corruption masking on mixed tables; typed-contract gaps end-to-end incl. refusal/validation envelopes and escaped raw exceptions; code_ask fallback purity — live keyword citations mixed into a lexical_fallback envelope, null sources, missing coverage/zero-note). Delivery evidence was overstated (full-filter-coverage and complete-corruption-handling claims disproven) and the spec lacked the AC-5 index_freshness definition.
- **Close-readiness review (external, executed probes) [delivery-review-2] — 2026-07-13: BLOCKED → all findings fixed and fixture-locked.** P0: the code_search post-compare now closes AFTER graph augmentation (race fixture at the registered tool with a fencing augmentation hijack). Freshness: symmetric per-layer eligibility compare (added-file-across-scoped-build fixture, both the reviewer's exact reproduction and recovery), unreadable required layer → unknown, current requires a POSITIVELY determined walk. Live walk: `search_docs_lexical` gains the tags parameter with any-of semantics (independent live-fallback tag fixture + signature pin). FTS serving: every requested table probed up front (mixed-corruption fixture: broken fts_code + working fts_docs → query_failed, no partial serve). Typed end-to-end: unexpected exceptions in both search tools → query_failed; live-walk failure → typed live_fallback_failed; `_index_rebuilding_response` and the validation-error envelopes carry the always-present search_mode/fallback_reason. code_ask fallback purity: the thin-citation keyword pass is suppressed in lexical_fallback (assert-not-called fixture), degraded citations carry per-table source, the envelope carries coverage, and working-lexical zero-hits carry the token-semantics guidance. P2: the degradation-log state marks only after a successful persist (retry fixture), and mock-root path fabrication is guarded (the stray MagicMock dirs are gone). Spec now defines the three-state index_freshness; stale store-absent-only prose corrected.
- pre-implementation-review: operator-waived 2026-07-13 — the actual sequence: the external pre-implementation plan review BLOCKED the wave; all plan repairs were made and presented to the operator with the recommendation that the reviewer confirm them; the operator instead directed "implement wave" directly, which is recorded here as an explicit operator waiver of a separate reviewer confirmation step (no reviewer pre-edit checkpoint occurred).
- wave-council-delivery(superseded-2): the round-2 approval was WITHDRAWN by the operator's full release review (CHANGES REQUESTED): FTS corruption could still masquerade (truncation/shadow-table damage passed the probe), the seed resource remained an unfenced indexed reader, the always-present contract had literal holes (unknown-args, missing coverage on collection failure, local-mode source loss, undocumented null mode), zero/failure wording misled in three places, required AC fixtures were missing (code kind/tags, exact-mode envelope), and the log dedup was not concurrency-safe. An independently-run re-verification of the prior fix round (five fixes VERIFIED with regressions clean) had convergently found two of the same defects (keyword-pass masking; the false FTS-blame on generic failures).
- **Release review round [delivery-review-3] — 2026-07-13: all findings fixed + fixture-locked** (see the 1seaq progress log for the full list): MATCH-path + parity `fts_probe`, filesystem-only seed resource, contract holes closed at all five tools, single-signal failure wording, outage-masking closed (keyword pass + reranker gap suppressed on infrastructure failure; infra diagnostic unconditional), thread-safe log dedup, and the review-required AC-1/AC-3 fixtures added. Evidence corrections: the wave summary/watchpoint no longer say store-absent-only; the pre-implementation-review line now records the ACTUAL sequence (operator-waived — "implement wave" was directed after the plan repairs were presented, with no separate reviewer pre-edit checkpoint); seed-211's reranked:false description matches the implementation (vector/coverage order; BM25 in lexical fallback); the packaging doc describes the semver `--version`/`--release` pipeline.
- wave-council-delivery(superseded-3): the round-3 approval was WITHDRAWN by the operator's extra review pass (CHANGES REQUESTED): the docsize shadow-table attack still passed the probe (miss-token MATCH never evaluates bm25), coverage was absent on failed-fallback and live-fallback envelopes, the AC-1 kind fixture was vacuous, citation parity was incomplete (keyword-pass source, local-mode excerpt truncation), the log persist could land out of order vs the in-memory state, and four doc/contract statements were stale.
- **Release review round 2 fixes [delivery-review-4] — 2026-07-13:** all findings fixed and fixture-locked (full detail in the 1seaq progress log): deterministic docsize/content shadow parity in `fts_probe` (live-store-verified before adoption), uniform coverage-on-every-degraded/failed-envelope rule with failure-path fixtures, non-vacuous kind fixture, keyword-pass source + local-mode excerpt parity, persist-inside-the-lock ordering (structural pin), and the four doc corrections (packaging semver pipeline, 1.13.0 target, null-mode in the spec vocabulary + coverage rule, keyword-pass suppression conditions). Suite 4,972 OK, zero failures.
- **Release review round 3 fixes [delivery-review-5] — 2026-07-13:** the P0 lifecycle-gate defect (withdrawn signoffs read as recorded) fixed with latest-state parsing, verified against every active and recent-closed wave record and end-to-end through the live gate (wave_review and wave_close dry-run now block on this wave's withdrawn delivery signoff); store_log returns a durable-append bool and the degradation-log caller gates on it (obstructed-path retry fixture); the --release-dry-run contract is honest (local build runs, no remote side effects); failed-FTS search_mode unified to lexical_fallback across all three tools; the remaining retrieval/coverage/release prose drifts corrected (spec, architecture, docstrings, release-flow, handoff). Suite 4,980 OK, zero failures.
- **Release review round 4 [delivery-review-6] — 2026-07-13: CHANGES REQUESTED → all prescribed repairs implemented.** P0 signoff parsing rewritten FAIL-CLOSED per the reviewer's prescription: markdown stripped, exact key before the first colon (the lane or its `-signoff` form — `qa` never matches `qa-reviewer`, `operator-signoff-notes` never matches `operator-signoff`), abutting-parenthesis historical variants (`lane(superseded):`) excluded from current state while spaced seat descriptors (`red-team (delivery):`) stay prose, last state line governs, and ONLY a value BEGINNING with an explicit canonical positive (`approved`/`passed`/`complete`) authorizes — `not approved`, `pending`, `blocked because previous checks passed`, `rejected`, `denied`, `failed`, `withdrawn`, `revoked`, `rescinded`, placeholders, and unknown wording all fail closed; prose evidence never authorizes operator/council lanes (word-boundary matching kills the prefix collisions). ATTACK MATRIX: all 20 reviewer-specified probes pass (fixture `test_full_attack_matrix` + live-record fixture); the live records parse correctly (1seav delivery+operator both blocked; closed 1sed7/1sc7c valid). LIVE POST-RELOAD VERIFICATION: `wave_mcp_reload` then `wave_review(wave_id='1seav')` and `wave_close(mode='dry_run')` on the RUNNING server — both report missing `wave-council-delivery` AND missing `operator-signoff`. P1 dry-run statements corrected everywhere (completion message names retained local stamps and the exact `git restore --` paths; module comment documents the full seven-step release order; release-flow wording; zero-mutation + honest-message test in `ReleaseOrchestrationOrderingTests`). P1 code_ask confidence contract unified across the six surfaces then in scope (constants comment, `_heuristic_confidence`, rerank helper docstrings, tool docstring, search-architecture, seed-211 confidence-levels → rendered guru.md): two `reranked=false` cases — healthy path = vector/coverage, medium cap, low at zero citations; `lexical_fallback` = BM25, low; every count-based claim removed (one historical reference retained as history). NOTE (round-5 correction): two seed/rendered citation-interpretation lines and the release-order comment were still drifted at this point — fixed in delivery-review-7 below. P1 1seaq AC-2 + Decision Log carry the no-published-epoch wording. P2 build-and-verification says `gh auth status` succeeds (the owner claim removed to match the implementation).
- **Release review round 5 [delivery-review-7] — 2026-07-13: both P0s confirmed CLOSED by the reviewer; two remaining documentation-contract repairs made.** (1) The seed `score` field description and both files' Citation-interpretation sections gained the two-case contract (healthy = vector/coverage, medium cap; `lexical_fallback` = BM25, low); the rendered "reranker unavailable" gap description states it is healthy-path-only. CORRECTION (round-6): the sweep was NOT complete — the rendered guru's citation-field `score` line had diverged from the seed before the edit (so the seed-targeted replacement missed it), and both files' `excerpt` line still claimed 300-char truncation the implementation does not perform; fixed in delivery-review-8 below with a byte-identity render fixture. (2) The release-order overview comment corrected to the IMPLEMENTATION's order (build → commit stamp → local tag → push main → push tag → publish — the ordering test is authoritative; the round-4 prescription's order was itself wrong); `release-flow.md` enumerates the seven steps separately and the recovery note references the publish step correctly.
- **Release review round 6 [delivery-review-8] — 2026-07-13: the final rendered-contract P1 repaired.** The `code_ask` citation-field block is now BYTE-IDENTICAL between the canonical seed and the rendered guru surface: `excerpt` = "the full matched chunk text (identical in both rerank spellings; no truncation)" — matching the implementation, which returns full text in both spellings since the round-4 parity fix — and `score` carries the two-case wording (healthy `reranked=false` = vector/coverage over mixed-model cosine; `lexical_fallback` = BM25 score/order). Divergence prevention: `GuruCitationContractRenderTests` pins (a) byte-identity of the block between seed and render and (b) the block's load-bearing claims against the implementation source (no `full[:300]` truncation in `code_ask_response`; both score cases stated) — this contract cannot silently diverge again.
- **Release review round 7 [delivery-review-9] — 2026-07-13: the QA repair made.** The dry-run local-effects fixture (`test_dry_run_local_build_side_effects_are_real`) previously passed an unsupported `repo_root=` kwarg and converted the TypeError into a skip — a contract fixture that never executed. Repaired per prescription: the correct `framework_dir=` call (plus `inject_install_templates=False`, the documented stub-tree convention), the try/except-to-skip removed entirely, and the test now EXECUTES and asserts the archive exists and is a valid zip, VERSION changed to the requested version, and the tracked stamp remains after the build returns. test_build_pack.py: 92/92, zero skips; full suite 4,980 OK.
- wave-council-delivery: approved 2026-07-13 — the external reviewer's final verdict: APPROVED, no remaining findings (nine review rounds: two initial delivery blocks, the fail-closed lifecycle-gate rebuild with the 20-probe attack matrix, the rendered-contract byte-identity repair, and the executing dry-run fixture — every finding fixed and fixture-locked).
- operator-signoff: approved 2026-07-13 — operator directed closure following the reviewer's APPROVED verdict.

## Dependencies

- DEPENDS on wave `1sed7 sqlite-only-index-state` (operator-decided order, 2026-07-12): `1sbxq`'s freshness helper consumes 1sed7's store API (build-state row + generation-keyed invalidation; zero meta.json reads). Builds on the shipped 1sbfk (trustworthy FTS) + 1sc7c (fresh FTS, per-layer state). Release planning updated accordingly.
