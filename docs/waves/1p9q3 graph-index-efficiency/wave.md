# Wave Record

Owner: Engineering
Status: closed
Last verified: 2026-07-04

wave-id: `1p9q3 graph-index-efficiency`
Title: Graph Index Efficiency

## Objective

Cut the graph index's storage, per-build write volume, and per-query latency by roughly an order of magnitude, and make per-edit build cost proportional to the change instead of the repository. (Delivered scope note, review 2026-07-04: storage 8.6× end-state; zero-change builds write 0 bytes; a CHANGED build's rows and re-resolution work are O(delta) while its bytes remain O(graph) — sidecar + payload, ~1.5× less than baseline — with the row-sharded store recorded as the lever if that term dominates at scale.) When this wave closes, graph artifacts are compact and compressed, the MCP server caches the constructed graph between tool calls, betweenness centrality works on graphs past 10k nodes (the self-hosted repo already exceeds the current cap), and incremental builds do O(delta) merge work with a provably full-rebuild-equivalent result.

## Changes

Change ID: `1p9py-enh graph-compact-compressed-persistence`
Change Status: `implemented`

Change ID: `1p9pz-enh graph-query-payload-cache`
Change Status: `implemented`

Change ID: `1p9q1-enh graph-buildtime-betweenness`
Change Status: `implemented`

Change ID: `1p9q2-enh graph-incremental-merge-state-store`
Change Status: `implemented`

Completed At: 2026-07-04

## Wave Summary

Wave `1p9q3` (Graph Index Efficiency) delivered 4 changes: Graph index: compact, gzip-compressed persistence for graph artifacts, Graph index: server-process payload and adjacency cache for graph query tools, Graph index: build-time betweenness centrality with size-tiered strategy; retire the per-query 10k-node cap, and Graph index: incremental merge with symbol-scoped cross-file invalidation, and a per-file state store. Notable adjustments during implementation: Graph index: compact, gzip-compressed persistence for graph artifacts: Delivery-review scope note: AC-1's 17.5× was measured at this change's own boundary (all three artifacts gzip JSON). After the sibling incremental-merge change replaced the gzip state JSON (1.4 MB) with the SQLite store (3.5 MB incl. the merge-state sidecar), the wave's END-STATE combined footprint is **8.6×** vs the 36.0 MB baseline — still order-of-magnitude, recorded here so the headline is not quoted stale. Independently re-measured by the performance review lane. Also: reader-copy parity gate added (`test_all_sniffing_reader_copies_handle_gzip_magic` — the gzip sniff is mirrored in four modules; a format change that drifts a reader now fails tests). Release-note callout recorded in the wave watchpoints: stale pre-upgrade server sessions cannot read gzip artifacts and must reconnect/`wave_mcp_reload` after upgrading.; Graph index: compact, gzip-compressed persistence for graph artifacts: Scoped from the graph-index efficiency evaluation. Baseline measured on the self-hosted repo: payload 11,701,810 B, state 22,661,853 B, clusters 1,100,292 B; gzip -9 on payload = 536,711 B (21.8×); cold `json.load` ~30 ms / ~47 MB resident. Writer confirmed `indent=2, sort_keys=True`, no compression.; Graph index: server-process payload and adjacency cache for graph query tools: Scoped from the graph-index efficiency evaluation. Confirmed: fresh `GraphQueryIndex.from_root()` per call at ~15 `server_impl.py` sites; full `json.loads` + three-structure rebuild per call (`graph_query.py:896-940, 278-294`); only cache today is the version-check mtime cache (`graph_query.py:69-74`); measured ~30 ms / ~47 MB per cold construction on the self-hosted repo (10,776 nodes / 30,899 edges). Also confirmed the AGENTS.md layer/networkx drift (layers removed in 1p4ww; networkx never imported by graph modules — igraph+leidenalg is the real optional dep).

**Changes delivered:**

- **Graph index: compact, gzip-compressed persistence for graph artifacts** (`1p9py-enh graph-compact-compressed-persistence`) — 8 ACs completed. Key decisions: Gzip + compact JSON behind sniffing readers (approach A).; Keep existing filenames; sniff content magic bytes rather than renaming to `.json.gz`.
- **Graph index: server-process payload and adjacency cache for graph query tools** (`1p9pz-enh graph-query-payload-cache`) — 8 ACs completed. Key decisions: In-process stat-validated cache of the constructed `GraphQueryIndex` (approach A).; Validate by `(mtime_ns, size)` with `generated_at`/fingerprint tiebreak, not content hashing.
- **Graph index: build-time betweenness centrality with size-tiered strategy; retire the per-query 10k-node cap** (`1p9q1-enh graph-buildtime-betweenness`) — 8 ACs completed. Key decisions: Build-time tiered computation (exact / igraph `cutoff` / degree fallback), persisted top-N (approach A).; Deterministic `cutoff` approximation rather than sampling for the middle tier.
- **Graph index: incremental merge with symbol-scoped cross-file invalidation, and a per-file state store** (`1p9q2-enh graph-incremental-merge-state-store`) — 10 ACs completed. Key decisions: Incremental merge with symbol-scoped invalidation over persistent merged maps (approach A).; State-store backend decided by spike (per-file gzip blobs + manifest vs stdlib SQLite), not up front.
## Journal Watchpoints

- Sequencing: land `1p9py` (I/O helpers) before `1p9q2` (state store writes through them); `1p9pz`'s cache must stay format-agnostic (keys on file stats, not content).
- Coordinate a single final `GRAPH_BUILDER_VERSION` bump across `1p9py`/`1p9q1`/`1p9q2` at integration; `1p9q1` may also bump `cluster_builder_version`.
- `1p9q2` requires an adversarial faithfulness review lane at wave review (security-control-faithfulness rule for resolution/binding changes) — differential-green alone is not sufficient.
- Follow-up watchpoint: `1p9pz` includes the AGENTS.md graph-layer/networkx docs correction rider — if the paragraph is seed-rendered, the `seed_edit_allowed` gate applies; verify ownership before editing.
- Blocking: `finalize()` analysis-pass seam is shared between `1p9q1` and `1p9q2` — settle the fingerprint-gated-skip interface before both lanes touch it.
- **URGENT operator follow-up (performance lane, outside this wave's scope):** on this repo the zero-change fast path is currently NULLIFIED by a pre-existing, non-converging Lance drift-repair loop. Root framing (reality-checker correction 2026-07-04): the defect is an EXCLUSION-vs-DRIFT-SCOPE mismatch — files excluded from semantic chunking upstream never reach the write path that would reconcile their `chunks_emitted` meta, so `_detect_lance_drift` re-flags them on EVERY build (whether the field says 99 or is absent entirely — the self-healing assumption in the drift docstring does not hold for excluded files), forcing a ~1 s incremental merge + ~1.35 MB writes per hook fire, forever. Needs its own change: drift detection must exclude semantically-out-of-scope paths. Until then every repo with a drift-flagged unchunkable file pays this.
- Release-note callout for the next version shipping this wave: stale pre-upgrade MCP server sessions cannot read the new gzip artifacts mid-session (they see `present: False` and their rebuild hint cannot heal the running process) — reconnect or `wave_mcp_reload` after upgrading.
- Cross-process residual (performance lane, recorded in 1p9q2 AC-9): concurrent hook-spawned builds are last-writer-wins at store commit — a stale build can transiently resurrect an old row; self-healing on re-detection, same shape as the pre-change monolithic state; the single-process differential harness cannot see it. Watch for field reports of transiently stale graph rows under rapid edit bursts.
- Follow-up candidates (recorded, not admitted): doc-impact add-direction miss (a doc mentioning a name that only LATER becomes a code symbol never rescans — proven pre-existing against the HEAD baseline by the adversarial lane); row-sharded fragment store (fragments into per-file rows) if large-corpus measurement shows the O(graph) sidecar cycle dominating changed-build cost.

## Participants

- code-reviewer — all four changes touch `.wavefoundry/framework/scripts/*.py`
- qa-reviewer — all change docs carry AC priority tables
- architecture-reviewer — MCP tool response shape changes (`wave_graph_report` betweenness section) and indexing module seams
- performance-reviewer — chunker/indexer hot paths (artifact I/O, per-query cache, build-time analysis pass, incremental merge)
- docs-contract-reviewer — rotating council seat (the `1p9pz` AGENTS.md/seed docs-accuracy rider)
- red-team, reality-checker — council seats (prepare phase); adversarial faithfulness lane re-runs at implementation review for `1p9q2`

## Review Checkpoints

- Prepare wave — readiness verdict (2026-07-03): READY. Council ran at standard primer depth. Red-team's strongest challenge — in-place artifact writes (`_write_json` uses direct `write_text`) combined with `1p9pz`'s stat-keyed cache could pin a torn cross-process read — was accepted and resolved by amendment: `1p9py` gained Requirement 8 + AC-8 (atomic temp + `os.replace` writes) and `1p9pz` Requirement 2 now names that as a precondition. Architecture seat's ambiguity finding (what "persistent merged maps" means under spawn-per-build processes) resolved by amendment to `1p9q2` Requirement 1: the payload artifact is the persistent map, plus an incrementally-maintained name-to-candidates lookup. Strongest alternative (skip gzipping the monolithic state file in `1p9py` since `1p9q2` replaces it) recorded and declined — the helpers are generic and the application is trivial, and `1p9py` must stand alone if `1p9q2` slips. Remaining seats found the AC set testable and the sequencing sound; performance seat confirmed gzip cost is noise against build wall time and flagged the exact-tier betweenness threshold as measurement-owned (already in `1p9q1` Requirement 2). AC priorities recorded on all four change docs. Product-owner acknowledgment: not applicable (framework-internal efficiency work; no product behavior shift).
- Docs-contract seat (rotating): the `1p9pz` AGENTS.md graph-layer/networkx correction must flow through the owning seed if the paragraph is seed-rendered (`seed_edit_allowed` gate) — already required by `1p9pz` Requirement 7 and AC-7; the capability-wording updates in `docs/specs/mcp-tool-surface.md` are audit-and-update per the "audit means audit" rule. No contract regressions found in the planned tool response changes (`wave_graph_report` gains method metadata; no field removals).
- **Prepare-phase Wave Council [prepare-council] — 2026-07-03: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, architecture-reviewer, security-reviewer, qa-reviewer, reality-checker, docs-contract-reviewer; rotating-seat: docs-contract-reviewer; strongest-challenge: stat-keyed query cache over non-atomic in-place artifact writes can pin a torn cross-process read — resolved by amendment, 1p9py Requirement 8/AC-8 atomic temp-plus-replace writes with 1p9pz naming it a precondition; strongest-alternative: defer monolithic-state gzip to 1p9q2's store rework — declined, helpers are generic and 1p9py must stand alone if 1p9q2 slips)

- pre-implementation-review: passed (2026-07-03) — Pre-mortem (likely churn causes): (1) the shared `finalize()` analysis-pass seam between `1p9q1` and `1p9q2` — settled up front: `1p9q1` persists its section via the clusters-artifact default and `1p9q2` owns the fingerprint-gated-skip wrapper; the interface is "analysis passes are functions of the merged maps, gated by the merged `input_fingerprint`"; (2) consumer-audit misses in the 8k-line `graph_indexer.py` — mitigated by an MCP evidence pass (`code_references`/`code_keyword` on the filename constants) before the first edit; (3) `1p9q2`'s symbol-scoped invalidation is the correctness core with the highest silent-wrongness risk — mitigated by spike-first, ws3 isolated as the smallest reviewable diff, the differential oracle, and the mandated adversarial lane at review; (4) the `1p9pz` AGENTS.md rider may be seed-owned — verify ownership before editing (the just-closed wave's lesson: hand-edits to rendered surfaces die on upgrade), seed gate if so; (5) triple `GRAPH_BUILDER_VERSION` bump churn — one coordinated final bump at integration. Packet completeness: all four change docs carry Requirements, ACs, and prepare-recorded AC priorities; lanes recorded in Participants; architecture-doc audits named (`docs/specs/mcp-tool-surface.md`); known unknowns are explicitly measurement-owned (state-store backend spike, betweenness tier thresholds, artifact placement default = clusters). Ordered lane sequence: `1p9py` (I/O foundation) → `1p9pz` (cache over atomic writes; includes docs rider) → `1p9q1` (build-time betweenness) → `1p9q2` (incremental merge; spike first) — 1p9pz/1p9q1 serialized rather than parallel because both edit `graph_query.py`. Builder lanes: software-engineer-profile subagents per change with coordinator integration; MCP-first exploration directive carried in every lane prompt.

- **Delivery-phase Wave Council [delivery-council] — 2026-07-04: PASS** (moderator: wave-council/coordinator; primer-depth: standard, consistent with prepare; fixed seats: red-team (primer), architecture-reviewer, security-reviewer, qa-reviewer, reality-checker; rotating fifth seat: docs-contract-reviewer). Specialist delivery lanes ran first — code-reviewer, qa-reviewer, architecture-reviewer, performance-reviewer, plus the mandated **adversarial faithfulness lane** for `1p9q2` (all verdicts in Review Evidence). **The adversarial lane earned its mandate:** initial verdict BROKEN — a real, deterministic incremental≡full divergence (merged-epoch symbol-delta collapse; `reads` edges in untouched files escaping scope-(b) re-resolution on same-file qualification-depth swaps; missing bound edge + persistent dangling edge + fingerprint divergence) that the green differential harness had structurally missed. Fixed in-session (Level-2 loop, operator-directed): per-side delta computation + `DepthSwapDeltaKeyTests`; the lane's reproducer re-runs green; post-fix verdict faithful-with-residuals, residuals recorded in `1p9q2` AC-9. **Red-team primer summary:** strongest challenge — AC-1 certified by counters structurally blind to the O(graph) `merge_state` sidecar cycle, and the 17.5× size headline decaying to 8.6× end-state; both discharged in-session (blob-aware counters + `sidecar:` log segment + test pins; honest re-scope recorded in both owning docs; Q1 discharged as re-scope + 11k-node quantification rather than a 5k end-to-end run — judged sufficient by all four fixed seats). The performance lane independently reproduced every headline number (~1,000× warm calls; 0-byte zero-change builds; 8.6× live-disk-verified) and surfaced the out-of-wave drift-loop nullifier (URGENT watchpoint) and the cross-process last-writer-wins residual (routed to AC-9 residual (e) + watchpoint). Fixed-seat outcomes on the FIXED state: architecture PASS (per-side fix boundary-sound, never-pin guard composes with version-rebuild machinery; two low nits — never-pin observability, Objective wording — both applied); security PASS (fix exhaustively complete — 1 definition, 4 per-side call sites; regression tests pin both directions; zero silent narrowing in the fixes); qa PASS (every 2026-07-04 claim code-verified; new tests proven non-vacuous by mechanism; AC-10 evidence refreshed to the post-fix suite); reality-checker PASS (reproducer green live; 8.6× reproduces exactly from disk; dead-code sweep exhaustive; one doc-drift correction to the drift-loop watchpoint framing — applied). **seat_agreement_aggregate:** seat_agreement `unanimous`; max_severity `high` at finding time (the adversarial equivalence break), every finding at that severity resolved in-session — no challenge round triggered. **Correlated-finding flags:** the sidecar-blindness finding was surfaced independently by the red-team primer and the performance lane (parallel, isolated — genuinely two signals); fixed-seat confirmations of the primer are correlated by design (shared primer) and were weighted as verification, not independent discovery. Pre-primer reads differed in angle (boundary, control-faithfulness, evidence-integrity, falsifiability) — no verbatim primer echo. **Rotating fifth seat (best alternative):** the strengthened option C′ — per-file store + fingerprint-gated skips + unconditional full re-merge on non-empty extraction deltas, deleting the symbol-scoped invalidation machinery and sidecar — was arguably the better PLAN-time move (identical zero-change wins; ~58% fewer bytes per changed build; structurally zero correctness surface — the wave's one real bug lived exactly in the machinery C′ deletes; ~20 LOC vs the delta pipeline; the Decision Log underweighted it). At DELIVERY time it is not stronger: the differential harness + discharged adversarial lane now exist as standing regression armor, the full-merge path remains one internal switch away (removable-by-bypass, no schema change), and ripping out reviewed working machinery to save 0.1 s would itself be the risk event. Blessed with two riders (below). Betweenness-in-clusters placement and defer-row-sharding both re-affirmed as correct. **Recommendations verdict table (initial + red-team reconciliation):** adversarial F1 equivalence break → fix now — DONE (per-side delta + tests + reproducer green) — held. Counter blindness + stale headline → fix now — DONE (counters/log/tests + re-scope in both docs) — held. Two un-invalidated refresh paths → fix now — DONE — held. Never-pin guard + observability → fix now — DONE (guard + test + once-per-process stderr) — held. WAL pragma check / reader parity gate / dead code / prune+error-posture docs → fix now — DONE — held. Full-merge kill-switch env (rotating-seat rider) → defer as follow-up candidate — red-team challenged the defer (the highest-risk machinery lacks the opt-out its lower-risk sibling got); upheld as genuine: the switch touches the correctness-critical mode selection and deserves its own reviewed change; recorded in watchpoints. Row-sharded fragment store → defer, gated on real large-corpus measurement — held. Drift-loop nullifier → out-of-wave URGENT operator follow-up (reframed per reality-checker: exclusion-vs-drift-scope mismatch) — held. Doc-impact add-direction miss → follow-up candidate (proven pre-existing) — held. Stale-session release-note callout → record now, execute at ship — held. `1p9q1` cross-pointer to the serving-limit disposition → fix now — DONE (pointer row added) — new row at reconciliation, held. **Falsification check:** working verdict — PASS with all severe findings resolved in-session and residuals honestly recorded. Strongest argument against (rotating seat): the wave bought its complexity with unmeasured large-corpus extrapolation, and C′ would have delivered most wins bug-free. Why it does not change the verdict: the extrapolation is mechanically sound (costs linear in rows/edges), large targets are the framework's declared product thesis, the machinery is now regression-armored and bypass-reversible, and the honest re-scope makes the residual O(graph) term visible in every build log rather than hidden — the wave ships with its claims matching its evidence. No unresolved disagreements; no waived lanes.

## Review Evidence

- wave-council-readiness: approved 2026-07-03 — prepare council synthesis verdict READY after amendments (1p9py atomic writes, 1p9pz cache precondition, 1p9q2 persistence-model clarification); no unresolved blocking findings
- code-reviewer: approved 2026-07-04 — pass-with-findings; store/finalize/cache/tier machinery verified correct with genuinely behavioral tests; surfaced the two un-invalidated refresh paths (fixed in-session) and four minor items (all addressed or dispositioned)
- qa-reviewer: approved 2026-07-04 — pass-with-findings; all 30 wave ACs independently attested against code/tests/measurements (own suite run + exhaustive grep-gate re-runs + live artifact probes); fix-round claims re-verified by the qa council seat
- architecture-reviewer: approved 2026-07-04 — pass-with-findings; module seams, cross-process contracts, and the version-invalidation lattice verified sound; surfaced the propagation and old-code-window hardening items (fixed); council seat confirmed the fixed state boundary-sound
- performance-reviewer: approved 2026-07-04 — pass-with-findings; every headline independently reproduced (~1,000× warm calls, 0-byte zero-change builds, 8.6× end-state live-verified); surfaced the sidecar quantification, the out-of-wave drift-loop nullifier (urgent watchpoint), and the cross-process residual
- adversarial-faithfulness (1p9q2 AC-9, security-control-faithfulness rule): approved 2026-07-04 — initial verdict was a genuine equivalence break the differential harness missed; fixed in-session (per-side symbol delta + regression tests + reproducer re-run green); final disposition faithful-with-residuals, residuals recorded in the change doc
- security-reviewer (council seat): approved 2026-07-04 — fix exhaustively complete, both-direction-pinned, zero silent narrowing; crash/consistency posture intact
- reality-checker (council seat): approved 2026-07-04 — every fix-round claim verified against live disk state; nothing falsified; one watchpoint framing corrected in place
- wave-council-delivery: approved 2026-07-04 — delivery council synthesis verdict PASS; seats unanimous; every severe finding fixed in-session at operator direction; residuals and follow-up candidates recorded with named mechanisms; full synthesis in Review Checkpoints
- operator-signoff: pending operator confirmation at closure

## Dependencies

- No external wave dependencies.
