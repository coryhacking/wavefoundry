# Graph index: incremental merge with symbol-scoped cross-file invalidation, and a per-file state store

Change ID: `1p9q2-enh graph-incremental-merge-state-store`
Change Status: `implemented`
Owner: Engineering
Status: implemented
Last verified: 2026-07-04
Wave: `1p9q3 graph-index-efficiency`

## Rationale

The graph build is incremental at **extraction** but full-cost at **merge**. `GraphIndexSession` (`graph_indexer.py:5484`) re-extracts only changed/removed files against a per-file artifact cache, but `finalize()` then **re-merges every cached artifact from every file into a fresh node/edge map on every build** (`graph_indexer.py:7404-7541`), re-runs the cross-file resolution rewrite over the whole graph, and rewrites all artifacts in full. The per-file cache that enables incremental extraction — `project-graph-state.json` — is itself a single monolithic JSON document (22.6 MB on the self-hosted repo, **twice the size of the graph it supports**) that is fully parsed and fully rewritten per build.

The post-edit hook triggers this on every reindex (`_build_graph_artifacts` runs unconditionally, `indexer.py:2756-2828`), so a one-file edit pays: parse 22.6 MB state + re-merge 453 artifacts + full cross-file rewrite + rewrite everything. On the self-hosted repo this is tolerable; at 5k files / 100k+ nodes the same design is O(repo) work and hundreds of MB of I/O per edit. `1p9py` (compression) shrinks the bytes ~20× but not the work — this change fixes the work:

1. **Incremental merge:** keep the merged node/edge maps across builds and apply per-file deltas (remove the old file's fragment, insert the new one), rather than re-merging from scratch.
2. **Symbol-scoped cross-file invalidation:** the subtle correctness core. Cross-file resolution binds an edge only when the candidate set for a name is unique (the v24/v25 mechanism in the `GRAPH_BUILDER_VERSION` changelog, `graph_indexer.py:35`). A changed file can flip uniqueness **elsewhere**: adding a second `Foo` must demote a previously-bound `calls → Foo` edge in an *untouched* file back to `external::Foo`; deleting one must promote. Invalidation must therefore be keyed by **symbol name** (the names a changed file defines/exports, before and after), re-running resolution only for edges referencing affected names — not merely by file.
3. **Per-file state store:** replace the monolithic state document with a store supporting per-file reads/writes (a directory of per-file compressed artifact blobs with a small manifest, or an equivalent SQLite table — decided by a measurement-informed spike, see Decision Log), so a one-file build reads and writes O(changed) state, not O(repo).

The faithfulness stakes are high: the "never bind the wrong twin" property is guarded by an adversarial test pattern (`test_graph_indexer.py` — e.g. `test_ambiguous_simple_name_stays_external:807`, `test_ambiguous_cross_file_simple_name_not_promoted:2201`, `test_cross_file_resolution_still_faithful:5683`). The non-negotiable invariant for this change: **an incremental build must produce a graph logically identical to a from-scratch build of the same tree.** Per the standing security-control-faithfulness rule, this change requires an adversarial review lane before close — green unit tests have missed silent narrowing in resolution changes before.

## Requirements

1. **Delta merge.** `finalize()` (or its successor) maintains persistent merged node/edge maps and applies per-file deltas for changed/removed files. A build with zero changed files performs no merge work and no artifact rewrite (today it re-merges everything and rewrites all files regardless). **Persistence model (council clarification, prepare review 2026-07-03):** builds run in hook-spawned, short-lived processes, so "persistent" means *on disk*, not in a process — the merged maps ARE the payload artifact: an incremental build loads the current payload, applies the delta, and atomically rewrites it (via `1p9py`'s writer). The symbol-scoped re-resolution additionally needs a name → candidate-ids lookup; maintain it incrementally alongside the merged maps (persisted with the payload or state store, or rebuilt from the loaded payload at build start if measured cheap — spike decides) rather than recomputed repo-wide, or the invalidation step silently reintroduces O(repo) work.
2. **Symbol-scoped invalidation.** For each changed/removed file, compute the symbol-name delta (names defined/exported before vs after, including class/function/constant simple and qualified names, plus import-graph effects used by import-edge disambiguation). Re-run cross-file resolution exactly for: (a) all edges originating in changed files, and (b) all edges in *any* file whose target name's candidate set may have changed — i.e., edges referencing a name in the delta (bound or `external::`). Promotion (external → bound) and demotion (bound → external) both work.
3. **Equivalence invariant.** For any edit sequence, the incrementally-maintained graph equals a full rebuild of the same tree: same node set, same edge set (including confidence levels), same `input_fingerprint` (`graph_indexer.py:8172-8182`). This is the primary acceptance surface, enforced by a randomized/differential test harness, not only hand-picked cases.
4. **Per-file state store.** The state layer exposes per-file get/put/delete plus iteration, with per-file (not whole-store) write granularity. Store-level metadata (builder/chunker/walker versions, epoch) preserves the existing whole-store invalidation semantics (`_load_state`, `graph_indexer.py:5552-5569`): version mismatch still invalidates everything and forces full re-extraction. Backend selected by spike measurement (per-file gzip blobs + manifest vs stdlib SQLite); selection criteria: per-build I/O at 1-file change on a 5k-file synthetic corpus, crash-consistency story, Windows behavior (no new native deps — stdlib only either way).
5. **Full-rebuild path retained.** `full=True` / version-mismatch / missing-state builds do a from-scratch extract+merge exactly as today, and that path also (re)seeds the persistent merged maps and state store. The differential harness uses it as the oracle.
6. **Crash consistency.** An interrupted incremental build never leaves a torn store or a merged map inconsistent with the state manifest — writes are atomic per file with a last-writer manifest (or SQLite transaction); a detected inconsistency degrades to full rebuild (loudly, via the existing stderr build log), never to a silently wrong graph.
7. **Downstream analysis passes.** Clusters (and betweenness, if `1p9q1` lands) still recompute from the merged maps per build. Re-analysis cost on unchanged-graph builds is skipped via the fingerprint (if the merged fingerprint is unchanged, analysis artifacts are not recomputed or rewritten).
8. **Version bump + migration.** `GRAPH_BUILDER_VERSION` bumped; a legacy monolithic state file is either migrated in one pass or discarded in favor of a one-time full re-extract (spike decides — migration is preferred if cheap, discard is acceptable given version-mismatch already forces re-extract today; the upgrade path must be explicit either way).
9. **Instrumentation.** The existing build log line (`indexer.py:2815-2819`) gains merge-phase timing and delta sizes (files changed, symbols invalidated, edges re-resolved), so field reports can distinguish extraction cost from merge cost.

## Scope

**Problem statement:** Every build re-merges all cached per-file artifacts, re-runs cross-file resolution repo-wide, fully parses and rewrites a monolithic 22.6 MB state document, and rewrites all graph artifacts — O(repo) work per one-file edit, hook-triggered on every edit.

**In scope:**

- Persistent merged maps + per-file delta merge in the finalize path.
- Symbol-scoped cross-file invalidation (promotion and demotion), including import-disambiguation effects.
- Per-file state store (spike → blobs-vs-SQLite decision → implementation), with version/epoch semantics preserved and crash consistency.
- Fingerprint-gated skip of unchanged analysis/artifact rewrites.
- Differential test harness (incremental vs full-rebuild oracle over randomized edit sequences on fixture corpora) plus targeted twin-flip cases; adversarial review lane at wave review.
- Migration/discard path for legacy state; version bump; instrumentation.
- Before/after measurement: 1-file-edit build wall time and bytes written on the self-hosted repo and a large synthetic corpus.

**Out of scope:**

- Changing extraction, per-language resolvers, or resolution *semantics* — identical output graphs is the invariant; only *when* resolution re-runs changes.
- Artifact serialization format (owned by `1p9py`; this change writes through whatever that lands).
- Query-side behavior (`1p9pz` owns caching; the payload artifact remains the query contract).
- Incremental clustering/betweenness algorithms (analysis passes remain recompute-on-change; only the unchanged-skip is added).
- Any new non-stdlib dependency (explicitly: no kuzu/duckdb/rustworkx; SQLite candidate is stdlib `sqlite3`).

## Acceptance Criteria

- [x] AC-1: A one-file edit build performs merge work proportional to the delta: state I/O touches only changed files plus the manifest (verified by instrumentation/counters in test), and a zero-change build rewrites no artifacts. Unit/integration-tested; measured 1-file-edit wall time and bytes written on the self-hosted repo recorded in the Progress Log with the pre-change baseline. *(Met 2026-07-03: `DeltaCostTests` — zero-change build: `state_reads=0`/`state_writes=0` and payload mtime unchanged; 1-file edit on a 6-file corpus: `state_reads=0`, `state_writes=1`. Live self-hosted measurements in the Progress Log. Note: the merge-state sidecar — the persistent merged map Requirement 1 itself mandates — is one O(graph) blob rewritten per changed build (the "manifest" analog); per-file rows are strictly O(changed).)*
- [x] AC-2: Differential equivalence — over randomized edit sequences (add/modify/delete files that define/reference shared names) on fixture corpora, the incremental graph equals the full-rebuild oracle after every step: node set, edge set with confidences, and `input_fingerprint` all identical. Harness lands in the suite with a fixed seed set; any divergence is a test failure printing the offending sequence. *(Met 2026-07-03: `DifferentialEquivalenceTests` in `test_graph_incremental_merge.py` — seeds 1337/2026/40961 × (5-file seed corpus + 10 mutation steps); fixtures use same-basename twin modules across two package dirs plus `import X` member calls so candidate-set ambiguity flips occur by construction; comparison = full node dicts + edge keys incl. confidence + `input_fingerprint`; failures print seed, step, and the full op sequence.)*
- [x] AC-3: Twin-flip faithfulness — targeted tests: (a) adding a second `Foo` in file B demotes a previously-bound edge → `external::Foo` in untouched file A; (b) deleting one of two `Foo`s promotes A's `external::Foo` edge to bound; (c) same via import-disambiguation change; (d) rename flows (old name released, new name bound) — each asserted equal to the full-rebuild oracle, in the spirit of the existing ambiguity suite. *(Met 2026-07-03: `TwinFlipFaithfulnessTests` — demote-on-twin-add in an UNTOUCHED caller (confidence restored to EXTRACTED), promote-on-twin-delete (RECEIVER_RESOLVED), import-flip rebind + import-disambiguated bind surviving a third twin, rename release+bind, doc-reference-follows-symbol; every case also asserted equal to the from-scratch oracle. Fixture note: a bare unknown-name call emits no Python edge by design, so fixtures use the `import helpers` + `helpers.helper()` member form whose dotted-suffix candidate sets carry the twin ambiguity.)*
- [x] AC-4: Version/epoch semantics — a builder/chunker/walker version mismatch still invalidates the whole store and full-re-extracts; the legacy monolithic state file is migrated or discarded per the decided path, exactly once, idempotently. Unit-tested. *(Met 2026-07-03: `GraphStateStoreTests.test_version_mismatch_resets_whole_store` + `VersionAndMigrationTests` — a meta poke to builder_version=0 forces full-corpus recovery on a no-change build; the legacy JSON is discarded exactly once with a loud stderr note (second build silent); version-probe fallback covered by `test_read_state_builder_version_probe` and the updated `StaleGraphAutoRebuildTests`.)*
- [x] AC-5: Crash consistency — a build interrupted between per-file writes (fault-injection test) leaves the store detectably consistent-or-degraded: the next build either resumes correctly or performs a loud full rebuild; it never serves a merged graph inconsistent with the oracle. Unit-tested. *(Met 2026-07-03: `CrashConsistencyTests` — fault injection at all three persist windows: mid-store-transaction (full rollback, then clean recovery), after store commit before payload write (pending binding), before the binding-stat commit; plus out-of-band payload deletion. Every recovery build prints the loud "full re-merge" stderr line and equals the oracle. SQLite atomic commit/rollback under real SIGKILL was validated by the AC-7 spike's fault-injection runs.)*
- [x] AC-6: Unchanged-graph builds skip analysis recompute and artifact rewrites (fingerprint-gated); a changed-graph build still refreshes clusters (and betweenness if present). Unit-tested. *(Met 2026-07-03: `FingerprintAnalysisSkipTests` — same fingerprint ⇒ clusters artifact mtime unchanged + stderr "fingerprint match" skip line; changed fingerprint or cluster-builder-version change ⇒ recompute+rewrite. The clusters artifact now records `input_fingerprint` (added under the not-yet-released CLUSTER_BUILDER_VERSION 11, so no bump needed). Verified live: a comment-only code edit reused the clusters/betweenness artifact.)*
- [x] AC-7: State-store spike recorded — the blobs-vs-SQLite measurement (1-file-change I/O on a 5k-file synthetic corpus, crash story, Windows notes) and the selection rationale are in the Decision/Progress Log before the backend implementation lands. (Evidence: Progress Log 2026-07-03 spike-measurement row + Decision Log 2026-07-03 SQLite-selection row; raw numbers in the retained spike scratch dir referenced there. Recorded ahead of implementation as required.)
- [x] AC-8: Instrumentation — the build log reports merge timing and delta sizes (files, symbols invalidated, edges re-resolved). Verified by test on the log line shape. *(Met 2026-07-03: the `finished graph` stderr line gains `merge[<mode>]: N.Ns | delta: files=… removed=… symbols=… edges_reresolved=… | state io: reads=… writes=…`; shape locked by `BuildLogInstrumentationTests` regex. Stats flow via a transient `merge_stats` key attached to the returned payload AFTER the artifact write — never persisted.)*
- [x] AC-9: Adversarial review lane run at wave review for the resolution-invalidation logic (security-control-faithfulness rule: differential green is necessary, not sufficient); findings addressed or explicitly dispositioned before close. — Discharged 2026-07-04: the lane's initial verdict was **BROKEN** — a real, deterministic equivalence divergence (its F1): `_candidate_delta_keys` was fed one MERGED old+new node subset, whose per-`(file, simple-name)` winner-picking dropped the loser node's qualified keys from the symbol delta, so `reads` edges (exact-bare lookup only) in UNTOUCHED files escaped scope-(b) re-resolution on same-file qualification-depth swaps (top-level `CONST` ↔ `Class.CONST`) — producing a missing bound edge (probe P5) and a persistent DANGLING payload edge one build later (probe P6), fingerprints diverging. Exactly the class differential-green missed (the harness fixtures never depth-swapped). **Fixed in-session:** per-side delta computation (old ∪ new, and DI prev-side ∪ new-side) in `finalize()`; regression class `DepthSwapDeltaKeyTests` (2 tests from the lane's probes) added; the lane's own reproducer re-run green (P5/P6 equal, fingerprints identical). Post-fix verdict per the lane's stated criterion: **faithful-with-residuals** — residuals recorded: (a) DI-synth-delta variant covered structurally by the same fix, not separately probed; (b) Swift/Rust collapse interplay covered structurally, no per-language probe; (c) stat-preserving edits escape the WALKER (pre-existing whole-index contract, not a 1p9q2 hole); (d) case-only renames on case-insensitive filesystems unprobed; (e) cross-process last-writer-wins interleaving (perf lane observation): two concurrent builds can transiently commit a stale row — self-healing on re-detection, same shape as the pre-change monolithic state, single-process harness structurally cannot see it; recorded as a wave watchpoint. Lane confirmed sound: same-dir fallback under moves, C# namespace rekeying, config-key edits, import-flip/third-twin, crash-window replay, version/epoch reset. Its F3 (doc-impact add-direction miss) was proven PRE-EXISTING against the HEAD baseline — follow-up candidate, not a 1p9q2 regression.
- [x] AC-10: `python3 .wavefoundry/framework/scripts/run_tests.py` passes; `wave_validate` clean; no `__pycache__` under `scripts/`. *(Met 2026-07-03: full suite `Ran 4342 tests across 42 files — OK` (28 new `test_graph_incremental_merge.py` tests); re-verified post-fix-round 2026-07-04: `Ran 4346 tests across 42 files — OK` (the file now carries 30 tests incl. `DepthSwapDeltaKeyTests`, plus the reader parity gate and the never-pin cache test); `wave_validate` → docs-lint ok; zero `__pycache__` dirs under `scripts/`.)*

## Tasks

- [x] Spike: per-file gzip blobs + manifest vs stdlib SQLite for the state store; measure on a 5k-file synthetic corpus; record decision (AC-7). (Done 2026-07-03 — see Progress/Decision Log rows; SQLite selected.)
- [x] Implement the state-store abstraction (get/put/delete/iterate, store-level version metadata, atomic per-file writes / transactions, migration-or-discard of legacy state). (`GraphStateStore` in `graph_indexer.py`: `meta`/`files`/`blobs` tables, WAL + synchronous=NORMAL + busy_timeout, single-transaction `apply_build`, `ensure_current` whole-store reset, one-time legacy discard, I/O counters, `read_state_builder_version` probe for `graph_query`.)
- [x] Restructure finalize: persistent merged node/edge maps, per-file fragment removal/insertion, zero-change fast path. (Unified three-mode pipeline — zero-change / incremental / full-merge — over the `merge_state` sidecar: per-file raw node lists + resolved edge fragments with raw-recoverable provenance; store-commit → payload-write → binding-stat persist order.)
- [x] Implement symbol-delta computation (defined/exported names before/after per changed file + import-graph effects) and symbol-scoped re-resolution (promotion + demotion), reusing the existing unique-candidate resolution functions unchanged. (Delta = candidate-index keys contributed by old+new nodes of changed/removed files + DI-synth node delta, computed by the SAME `_build_candidate_indexes` used for resolution — zero drift; the per-edge resolution bodies were extracted mechanically into `_resolve_external_call_target` / `_resolve_external_read_target`, semantics untouched, shared by both modes.)
- [x] Fingerprint-gated skip for analysis passes and artifact rewrites. (`update_graph_clusters` early-exit on matching `input_fingerprint` + cluster/graph builder versions; clusters artifact records the fingerprint.)
- [x] Differential harness (randomized edit sequences vs full-rebuild oracle, fixed seeds) + targeted twin-flip/import-flip/rename tests + fault-injection crash tests. (`test_graph_incremental_merge.py`, 28 tests — see AC-2/AC-3/AC-5 evidence.)
- [x] Instrumentation in the build log; version bump with changelog entry. (Log line per AC-8; the coordinated `GRAPH_BUILDER_VERSION` bump RIDES the existing wave-level bump to 36 made by `1p9py` — its changelog line already names this change; no second bump per the serialization point.)
- [x] Measure before/after (self-hosted repo + synthetic large corpus); record in Progress Log. (Self-hosted before/after recorded; the 5k-file synthetic corpus numbers were captured by the AC-7 spike — the store's 1-file cycle 0.71 ms / ~4 KB written is the large-corpus datum.)
- [x] Run `run_tests.py` + `wave_validate`; clean `__pycache__`; flag the adversarial review lane in wave review. (Suite + validate evidence in AC-10; adversarial lane flagged per AC-9.)

## Agent Execution Graph


| Workstream | Owner | Depends On | Notes |
| ---------- | ----- | ---------- | ----- |
| ws1-spike-state-store | implementer | — | Backend spike + measurement + decision record; then the store abstraction with atomicity/versioning/migration. |
| ws2-delta-merge | implementer | ws1-spike-state-store | Persistent merged maps; per-file fragment delta; zero-change fast path; fingerprint-gated skips. |
| ws3-symbol-invalidation | implementer | ws2-delta-merge | Symbol-delta computation + scoped re-resolution (promote/demote); the correctness core — smallest possible diff over existing resolution functions. |
| ws4-differential-harness | implementer | ws2-delta-merge, ws3-symbol-invalidation | Randomized differential harness + twin-flip/rename/crash fault-injection tests + instrumentation checks. |
| ws5-adversarial-review | reviewer | ws3-symbol-invalidation, ws4-differential-harness | Faithfulness red-team on invalidation completeness (what edit shapes could the symbol delta miss?); runs in wave review. |


## Serialization Points

- Sequenced **after** `1p9py` (this change writes state through the compressed I/O helpers; doing it in the other order rewrites this change's store code).
- ws1's store interface gates ws2; ws3 is deliberately isolated so the resolution-scoping diff is reviewable on its own.
- `finalize()` is the hub file region shared with `1p9q1`'s analysis placement — coordinate the analysis-pass seam (fingerprint-gated skip) between the two changes at integration.
- One coordinated `GRAPH_BUILDER_VERSION` bump at wave integration with `1p9py`/`1p9q1`.

## Affected Architecture Docs

Audit `docs/specs/mcp-tool-surface.md` (index build/status descriptions) and any architecture/reference doc describing the graph build pipeline or the state file's role; document the per-file state store and the incremental-merge invariant (incremental ≡ full rebuild). If a data-and-control-flow doc covers indexing, the merge path description changes. The build-pipeline contract change warrants a decision record for the store-backend selection (spike outcome).

## AC Priority

(Populated at Prepare wave.)


| AC | Priority | Rationale |
| ---- | -------- | --------- |
| AC-1 | required | O(delta) builds are the point of the change. |
| AC-2 | required | The equivalence invariant is the correctness contract; without the differential oracle the change is unverifiable. |
| AC-3 | required | Twin promotion/demotion is exactly where incremental resolution silently goes wrong; these are the adversarial cases. |
| AC-4 | required | Version invalidation and one-time migration protect every target repo at upgrade. |
| AC-5 | required | A torn store yielding a silently wrong graph is the worst failure mode; loud degradation is mandatory. |
| AC-6 | important | Skip-on-unchanged is a real win but a missed skip only costs time, not correctness. |
| AC-7 | required | The backend decision must be evidence-based and recorded; it shapes crash-consistency and Windows behavior. |
| AC-8 | important | Field diagnosability; no correctness impact. |
| AC-9 | required | Standing rule for detection/binding-faithfulness changes; differential tests alone have missed narrowing before. |
| AC-10 | required | Suite + docs-lint green is the standing merge gate for framework code. |


## Progress Log


| Date | Update | Evidence |
| ---- | ------ | -------- |
| 2026-07-04 | DELIVERY-REVIEW FIXES (Level-2 loop, in-session): (1) **Adversarial F1 fixed** — per-side symbol-delta computation (see AC-9); `DepthSwapDeltaKeyTests` added (30 tests in the file now); lane reproducer green. (2) **Blob-aware instrumentation** (red-team + performance finding: `state io: reads=0 writes=1` hid the O(graph) sidecar cycle — measured 48 ms decode + 139 ms encode + ~790 KB write per changed build at 11k nodes): `GraphStateStore` now counts `blob_reads`/`blob_writes`/`blob_bytes_written`; `merge_stats` + the build log line carry a `sidecar:` segment; `DeltaCostTests` extended to pin zero-change `blob_writes=0` and 1-file `blob_reads=1/blob_writes=1/blob_bytes>0`. (3) **WAL pragma result check** — a refused `journal_mode=WAL` (network filesystems) now warns loudly on stderr instead of proceeding silently. (4) Error-posture asymmetry documented in the `GraphStateStore` docstring; defense-in-depth note added at the dangling-edge prune (payload-only repair masks a scope-(b) miss for exactly one build). (5) Dead code removed (`_fresh_state`, `_current_artifact_for`, `_current_hash_for`, `_file_meta_hash`, `graph_query._graph_state_path` — zero callers, grep-verified). **Honest-claims re-scope (red-team Q1, taken as re-scope + quantification rather than a 5k end-to-end run):** per-edit cost is O(delta) for state ROWS and re-resolution work; state BYTES per changed build remain O(graph) via the merge-state sidecar (the persistent merged map Req-1 mandates) + payload rewrite — quantified at 11k nodes above, now visible in the log line; the row-sharded fragment store (fragments into `files` rows, small blob for `di_synth_nodes`+format) is the named lever if large-corpus measurement shows the sidecar dominating. Combined end-state artifact footprint: **8.6×** vs the 36.0 MB baseline (payload 0.56 + store 3.5 + clusters 0.11 MB) — the 17.5× in the sibling doc was measured pre-store. | Adversarial lane report + probes 2026-07-04; performance lane measurement table; red-team primer F1/F2/Q1; test runs post-fix. |
| 2026-07-03 | Scoped from the graph-index efficiency evaluation. Confirmed: per-file extraction cache with full re-merge in `finalize()` (`graph_indexer.py:7404-7541`); monolithic state 22.6 MB vs 11.7 MB payload on the self-hosted repo; hook-triggered on every reindex (`indexer.py:2756-2828`); whole-store version invalidation (`graph_indexer.py:5552-5569`); cross-file unique-candidate resolution per v24/v25 changelog (`graph_indexer.py:35`); reverse-invalidation precedent for dangling edges exists (`graph_indexer.py:7593` region). Faithfulness oracle: the ambiguity test pattern in `test_graph_indexer.py` (:807, :2201, :5683 et al.). | `graph_indexer.py:35,5484,5552-5569,7404-7541,7593`; `indexer.py:2756-2828`; measurements 2026-07-03. |
| 2026-07-03 | **Implementation landed (builder lane).** `GraphStateStore` (stdlib sqlite3; `meta`/`files`/`blobs`; WAL/NORMAL/busy_timeout; single-transaction `apply_build`; I/O counters) + a unified three-mode `finalize()` (zero-change / incremental / full-merge) over a persistent `merge_state` sidecar: per-file raw node lists + resolved edge fragments carrying provenance (`_x` original external name, `_c` pre-promotion confidence, `_d` dropped-read tombstone) so every raw edge is recoverable without reading unchanged rows. Symbol delta = candidate-index keys contributed by old+new nodes of changed/removed files plus the DI-synthesized-node delta, computed by the SAME `_build_candidate_indexes` function resolution uses (zero drift by construction); scope (a) = changed files' fragments recomputed wholesale, scope (b) = untouched files' edges whose `_edge_lookup_keys` (bare name, final segment, Go inner key — mirroring the resolver's lookup surface exactly) intersect the delta. The former in-place rewrite loop's per-edge bodies were extracted MECHANICALLY into `_resolve_external_call_target` / `_resolve_external_read_target` (comments retained; every branch still unique-or-stay-external) and are shared by full and incremental modes, so the oracle exercises the identical resolution code. Persist order store-commit → payload-write → binding-stat-commit; any crash window degrades to a loud stderr full re-merge. Two determinism hardenings recorded for the reviewer lane (both covered by the wave's existing v36 bump): (1) the DI pass now receives artifacts in SORTED file order in both modes — its `_pick_node`/`_ensure_type_node` picks are list-order-sensitive, and full-vs-incremental equivalence requires one canonical order; (2) analytics node flags (`is_entry_point`/`dead_code_risk`/`is_chokepoint`) are stripped when nodes enter the merge and recomputed fresh per build — previously the flags leaked into cached state artifacts by shared reference and could go stale (set-only, never cleared) on incremental builds; fresh recompute matches the from-scratch oracle. Node-locality guard: a non-`external::` node whose id lies outside its own file's id space flags the sidecar and pins the layer to full-merge (correct, just not incremental) — shared `external::` import-endpoint nodes are exempt (the node map is re-unioned from per-file lists every build, first-wins in sorted-file order, so removing one contributor never removes another's copy; the initial guard version wrongly tripped on these — 1,709 legitimate shared nodes on the self-hosted repo). AC-9 probe list for the adversarial lane: delta completeness for cs_file_ns-driven C# namespace effects (covered via any-candidate-in-changed-file keys), DI-order sensitivity, evidence-field survivor drift on key collapse (values may differ from the pre-change builds; keys/confidences/fingerprint are the contract), and the doc-impact `mentioned∩changed` keying (pre-existing semantics, reproduced unchanged). | `graph_indexer.py` (store + helpers + finalize rewrite), `graph_query.py` (probe), `graph_cluster.py` (fingerprint gate), `indexer.py` (log line); tests `test_graph_incremental_merge.py` (28), updated `test_graph_indexer.py` / `test_server_tools.py`. |
| 2026-07-03 | **Measurements (self-hosted repo, venv python, `indexer.py --content graph` subprocess).** BASELINE (pre-change): 1-file-edit build = 1.5 s in-build (2.69 s process wall); bytes written per build = 2,071,539 B (state 1,404,524 + payload 553,038 + clusters 113,977 — all three rewritten EVERY build). AFTER: (i) the SAME edit (a `docs/waves/` file — graph-scan-excluded, i.e. the dominant hook-fire case) takes the zero-change fast path: 0.3 s in-build (1.45 s wall), **0 bytes written** (no payload/state/cluster rewrites, `state io: reads=0 writes=0`); (ii) a 1-code-file edit (comment append; graph shape unchanged): 1.2 s in-build (merge[incremental] 0.9 s, 2.38 s wall), delta `files=1 symbols=5 edges_reresolved=17`, `state io: reads=0 writes=1`, bytes written ≈ 1.34 MB (payload 554,799 + merge sidecar 785,145 + 1 row ≈ 4 KB WAL) with the clusters/betweenness recompute + 114 KB rewrite SKIPPED via fingerprint; (iii) loud full re-merge degrade path: merge 1.0 s over 460 rows (reads=460); (iv) one-time migration/seed build (legacy discard + full re-extract): 22.9 s — the established version-bump upgrade cost. Store on disk: 3.5 MB (460 rows = 1.63 MB records + sidecar 0.79 MB). Candidate-index rebuild at build start: **42.6 ms** at 11,060 nodes (7,850 simple + 46,218 qualified keys) — the measured basis for rebuilding the name→candidate lookup per build instead of persisting it incrementally. Large-corpus datum: the AC-7 spike's 5k-file store measurements (1-file update cycle 0.71 ms, ~4.2 KB written). | Runs 2026-07-03 (`build_index: finished graph…` stderr lines with the new merge/delta/state-io fields); sqlite size query. |
| 2026-07-03 | **Instrumentation sample** (Req-9): `build_index: finished graph: 2 changed, 0 removed \| nodes: 11060 \| edges: 31743 \| merge[incremental]: 0.9s \| delta: files=1 removed=0 symbols=5 edges_reresolved=17 \| state io: reads=0 writes=1 in 1.2s`. | Live run 2026-07-03. |
| 2026-07-03 | **Architecture-doc updates + spec audit.** `docs/architecture/graph-index-system.md`: Disk Artifacts table + a new per-file-state-store paragraph (schema, WAL/durability, legacy discard, probe fallback); Versioning + "Incremental vs. Full Rebuild" rewritten for the three-mode pipeline, symbol-scoped invalidation, crash-consistency binding, and the fingerprint-gated cluster skip. `docs/architecture/embedding-model.md`: two stale factual claims in the Merkle-evaluation note corrected (state filename; "merge runs over the full edge set every build" → symbol-scoped). `docs/specs/mcp-tool-surface.md`: audited (`grep` over graph/state/merge/rebuild wording) — the index build/status tool descriptions never describe the state document; **no changes needed** (audit-and-skip per the standing rule). | Doc diffs 2026-07-03; grep audit same date. |
| 2026-07-03 | **Version bump (Req-8): rides the coordinated wave-level `GRAPH_BUILDER_VERSION` 36 made by `1p9py`** — its changelog line already names this change's state-store/incremental-merge shape change; no second bump per the wave's single-bump serialization point. Clusters-artifact `input_fingerprint` key added under the not-yet-released `CLUSTER_BUILDER_VERSION` 11 (`1p9q1`'s bump), so no cluster bump either. | `graph_indexer.py:37` changelog text; `graph_cluster.py:19`. |
| 2026-07-03 | **State-store backend spike (AC-7) — measurements.** Synthetic corpus: 5,000 per-file records mimicking the real artifact shape (`kind/path/source_hash/nodes/edges/defined_symbols/simple_names/...`), sizes lognormal calibrated to the self-hosted repo's 22.6 MB / 1,277 files: 104.1 MB plain total, mean 20.8 KB, median 7.2 KB, p95 80.9 KB, max 1.97 MB per record; gzip ratio 9.7x (real repo 16x — synthetic entropy compresses less). Interpreter: wavefoundry venv Python 3.13.5, macOS (Darwin 25.5.0, APFS). Both prototypes store identical gzip-level-6 compact-JSON record bytes; every op measured as a full short-lived-process cycle (open store, op, commit, close) because builds are hook-spawned processes. Medians — cold seed (all 5,000): blobs 2,092 ms vs SQLite 1,361 ms. **1-file update cycle: blobs 22.65 ms vs SQLite 0.71 ms (32x)** — blob cost is the O(repo-count) manifest decode+re-encode+rewrite, 336,945 bytes written per 1-file build vs ~4,152 WAL bytes for SQLite (81x). Metadata/staleness read: blobs 3.31 ms vs SQLite 0.49 ms. Full iteration (decode all, the full-rebuild read path): blobs 516 ms vs SQLite 394 ms. Delete 1: blobs 23.2 ms vs SQLite 0.79 ms. Store size on disk: blobs 11.06 MB across 5,001 files vs SQLite 13.28 MB in 1 file (+20% page overhead). Crash fault-injection (SIGKILL mid-write, child process): blobs interrupted before manifest commit → manifest parses, 0 missing/mismatched referenced blobs, all 100 updated paths still serve old state, 100 orphan new-generation blobs detectable for GC; blobs killed mid-blob-write → old blob intact, 1 orphan `.tmp` detectable; SQLite killed before COMMIT → rollback to old values (100/100), `integrity_check` ok; SQLite killed after COMMIT → new values durable (100/100), `integrity_check` ok. Spike finding: the blob protocol is only crash-sound with generation-qualified blob names (hash of path+source_hash), manifest-last commit, and unlink deferred to post-commit — the naive path-keyed blob name clobbers the old blob before the manifest commit and tears the store. Spike scripts + raw JSON results retained for the implementation lane at `/Users/coryhacking/.claude/jobs/e29ad14d/tmp/spike-1p9q2/` (`corpus.py`, `stores.py`, `bench.py`, `crash_test.py`, `results.json`, `crash_results.json`). | Spike run 2026-07-03; raw numbers in `results.json` / `crash_results.json` at the scratch path. |


## Decision Log


| Date | Decision | Reason | Alternatives |
| ---- | -------- | ------ | ------------ |
| 2026-07-03 | Incremental merge with symbol-scoped invalidation over persistent merged maps (approach A). | Converts per-edit cost from O(repo) to O(delta) while keeping resolution *semantics* untouched (only re-run scope changes); the differential-oracle harness plus the existing full-rebuild path make equivalence checkable; symbol-name keying is the minimal invalidation unit that is still provably sufficient for unique-candidate resolution (uniqueness of a binding depends only on the candidate set for that name). | (B) File-scoped invalidation (re-resolve only changed files' edges) — weakness: **incorrect** — misses demotion/promotion of edges in untouched files when a twin appears/disappears; exactly the wrong-twin failure the test suite guards. (C) Full re-merge but only when the extraction delta is non-empty, plus skip rewrites on unchanged fingerprint — weakness: honest and simple but still O(repo) merge on every real edit; acceptable fallback if the spike reveals blocking complexity, not the goal. |
| 2026-07-03 | State-store backend decided by spike (per-file gzip blobs + manifest vs stdlib SQLite), not up front. | Both are stdlib-only and satisfy per-file granularity; they differ in crash-consistency ergonomics (SQLite transactions vs atomic-rename + manifest) and small-file I/O behavior (Windows filesystems penalize many small files) — measurable, not arguable. Deciding on evidence honors the calibrate-don't-guess rule; the store sits behind an abstraction either way. | Pick SQLite now — likely winner on crash story but thousands-of-blobs vs one-db I/O on Windows deserves the measurement; pick blobs now — simplest but weakest crash story. Embedded graph DBs (kuzu) — rejected outright: unmaintained-vendor risk (company ceased operations 2025), native-dep Windows risk, and the query side stays in-memory regardless (evaluation finding). |
| 2026-07-03 | Analysis passes stay recompute-on-change (fingerprint-gated skip only). | Incremental Leiden/betweenness maintenance is research-grade complexity; the passes are seconds-scale at current sizes and gated off entirely when the graph is unchanged — the dominant hook-fire case. | Incremental community maintenance — rejected as speculative complexity with no measured need (simplest-solution-first). |
| 2026-07-03 | Sequence after `1p9py` in the same wave. | The store writes through the compressed I/O helpers; landing format first avoids rewriting this change's persistence twice. Tier-1 changes also de-risk independently if this change slips. | Land together unordered — rejected: avoidable churn on the shared writer seam. |
| 2026-07-03 | **State-store backend: stdlib `sqlite3` (single db, one `files` table keyed by path + a `meta` table for schema/builder/walker/chunker/layer/epoch; records stored as the same gzip-level-6 compact-JSON bytes; `journal_mode=WAL`, `synchronous=NORMAL`, plus `busy_timeout` for concurrent hook fires).** Selected per the AC-7 spike (Progress Log row, same date). | (1) Measurement: SQLite wins every per-build criterion on the 5k-file corpus — the dominant hook-fire op (1-file update, full process cycle) is 0.71 ms vs 22.65 ms (32x) and ~4.2 KB written vs ~337 KB (81x), because the blob manifest is itself an O(repo-count) document re-read and rewritten per build, quietly reintroducing the exact O(repo) I/O this change exists to remove (and it grows with repo size); cold seed 1.36 s vs 2.09 s, full-scan iteration 394 ms vs 516 ms, delete 0.79 ms vs 23.2 ms, metadata read 0.49 ms vs 3.31 ms. Only disk size favors blobs (11.1 vs 13.3 MB, +20% — immaterial). (2) Crash story: both passed SIGKILL fault injection, but the blob path is only sound with a subtle hand-rolled protocol (generation-qualified blob names, manifest-last commit point, deferred unlink, orphan GC) that the spike had to discover after the naive design tore; SQLite gets atomic commit + rollback + `PRAGMA integrity_check` detectability from one battle-tested mechanism. `synchronous=NORMAL` is app-crash safe (WAL rollback); an OS-level crash can at worst lose the last commit — acceptable, a lost build is re-buildable, never a torn store. (3) Windows (analysis, not measurement — no live Windows here): the blob design multiplies exactly the failure surface this repo's Windows field history (1.9.4-1.9.8 reports) keeps hitting — NTFS many-small-files metadata cost, per-file AV/minifilter scanning on 5,001 creates, per-read handle-open cost on iteration, and mandatory locking making `os.replace` fail transiently when AV/Search briefly holds a blob (5k renames per seed = 5k chances); SQLite is 1 file (+`-wal`/`-shm`), one held handle, and its Windows locking is battle-tested. Version-mismatch invalidation stays whole-store: delete db + `-wal` + `-shm` (or `DROP TABLE`) and full re-extract. **Would overturn:** field evidence of unresolvable `SQLITE_BUSY` contention/corruption from concurrent hook-spawned builds; target repos whose `.wavefoundry/index/` lands on a network filesystem (SQLite locking unsafe there — blobs would win); a supported host Python shipping without the `sqlite3` module; or write-amplification pathology at much larger corpora (none observed at 5k). | Per-file gzip blobs + manifest (Backend A) — loses on every measured criterion except disk size; crash-sound only via the generation-naming protocol; worst-case Windows profile. Retained as the documented fallback since the store sits behind a get/put/delete/iterate abstraction. Hybrid (blobs + SQLite manifest) — rejected: two mechanisms, no measured benefit. |
| 2026-07-03 | **Store filename `project-graph-state.sqlite`; legacy monolithic `project-graph-state.json` is DISCARDED one-time (not migrated).** | A distinct `.sqlite` name is honest about the format (nothing ever gzip-sniffs the database) and keeps the legacy `.json` name unambiguous for the pre-upgrade version-probe fallback. Discard over migrate: every pre-upgrade state carries builder_version ≤ 35, and version mismatch ALREADY forces a full re-extract on upgrade — migration code would be single-use complexity that saves nothing for real repos; the discard is loud (stderr), idempotent (file gone afterward), and the one-time full re-extract measured 22.9 s on the self-hosted repo (the established version-bump upgrade cost). | Migrate rows from the legacy JSON — rejected: only helps the same-version edge case (a dev repo that ran v36 pre-store), at the cost of a permanent parse-and-convert path. Keep the `.json` filename for the store — rejected: misleading format signal and collides with the sniffing-reader convention. |
| 2026-07-03 | **Name→candidate lookup is REBUILT from the assembled node map at build start (not incrementally persisted); the symbol delta is computed with the same builder on per-file node subsets.** | Measured 42.6 ms at 11,060 nodes (Progress Log) — noise next to the payload serialize; scaling is O(nodes) with tiny constants. Rebuilding guarantees the resolution context is IDENTICAL to a from-scratch build by construction (no incremental-index drift class at all), and computing delta keys with the same function eliminates a whole category of "the delta missed a key shape" bugs (dotted suffixes, collapsed-pair nodes, C# namespace nodes are all covered because they're the same code path). Req-1 explicitly allows rebuild-at-start "if measured cheap — spike decides". | Persist the index in the sidecar and maintain it incrementally — rejected: saves ~40 ms but introduces the drift class the differential harness exists to catch; revisit only if a measured corpus makes the rebuild dominant. |
| 2026-07-03 | **Edge VALUES are contract-exempt on key collapse; the equivalence contract is node dicts + edge keys (source/target/relation/confidence) + `input_fingerprint`.** | When multiple raw edges collapse onto one final key, the surviving edge dict's optional metadata (`evidence`, `self_edge_kind`) depends on merge order; the former in-place rewrite and the fragment-union produce the same KEY set by construction but may keep a different survivor's metadata. The fingerprint (wave 1p66e) is defined over keys only; AC-2's stated surface is node set + edge set with confidences + fingerprint, and the harness enforces exactly that (plus FULL node-dict equality, which held). | Reproduce the exact survivor — rejected: would couple the incremental path to the historical apply-order of the in-place loop for zero consumer-visible benefit (no consumer distinguishes collapsed-duplicate metadata). Flagged to the AC-9 adversarial lane for confirmation. |


## Risks


| Risk | Mitigation |
| ---- | ---------- |
| Symbol-scoped invalidation misses an edit shape (re-export/barrel chains, qualified-vs-simple name aliasing, package-authoritative Go resolution) → stale wrong-twin edge in an untouched file. | The symbol delta includes qualified names and import-graph effects; the randomized differential harness (AC-2) explores composed edit sequences beyond hand-written cases; targeted twin/import/rename tests (AC-3); mandatory adversarial review lane (AC-9) hunts the miss classes explicitly. Fallback: any detected inconsistency degrades loudly to full rebuild. |
| Persistent merged maps drift from state-store truth after a crash. | AC-5 fault-injection; manifest/transaction consistency check on load; degrade-to-full-rebuild is always available and loud. |
| Many-small-files store behaves badly on Windows (open-handle cost, AV scanning). | The spike measures on a 5k-file corpus with Windows notes; SQLite is the standing alternative; store is behind an abstraction so the backend is swappable. |
| Complexity lands but wins are marginal on small repos. | Zero-change fast path and O(delta) merge cost nothing extra on small repos; the differential harness keeps the added complexity honest; the C fallback (skip-rewrites-only) is recorded if the spike kills the approach. |
| Interaction with the in-query synchronous rebuild path (`graph_query.py:122-275`) during a version-bump upgrade. | Existing inflight-lock coordination unchanged; version mismatch takes the full-rebuild path which reseeds store + maps atomically before the payload is republished. |


## Session Handoff

See `docs/agents/session-handoff.md` for current session state.
