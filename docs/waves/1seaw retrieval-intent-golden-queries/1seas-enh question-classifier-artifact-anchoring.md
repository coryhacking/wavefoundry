# Question Classification: Artifact Anchoring + Low-Information-Path Penalty (Eval-Gated)

Change ID: `1seas-enh question-classifier-artifact-anchoring`
Change Status: `implemented`
Owner: Engineering
Status: implementing
Last verified: 2026-08-31
Wave: `1seaw retrieval-intent-golden-queries`

## Rationale

External code review (2026-07-12), validated against `2952df8f`: `_classify_question` (`server_impl.py` ~17145) is phrase-driven — any question containing "where are" classifies as navigational before broader intent is considered. Live effect in the review session: broad assessment queries ("where are the biggest gaps/opportunities…") were treated as navigational lookups and ranked `.aiignore` comment lines above substantive implementation evidence — twice. Two distinct weaknesses:

1. **Intent precedence:** phrase signals fire before artifact/path anchoring; assessment/review intent ("where are the gaps", "what are the weaknesses") has no class of its own and collides with navigational ("where is the rate limiter").
2. **No low-information-path prior:** ignore files, lock metadata, manifests, and generated surfaces rank purely on token match, though they are almost never the answer — EXCEPT when the query names them, which must keep working.

Hard constraint: this is ranking-behavior change, so it ships **eval-gated on `1sear`** (the golden-query suite) — improvement demonstrated, no regression on the other classes, or it does not merge. That is the discipline that gated out `code_risk_score` v1.

## Requirements

1. **Anchoring precedence:** artifact/path anchoring (query names a file, extension, config key, or tool) is evaluated BEFORE phrase-signal classification; a query that names `.aiignore` still reaches it.
2. **Assessment intent:** separate the assessment/review intent ("where are the gaps/weaknesses/opportunities", "review X", "assess X") from navigational; assessment routes like explanatory (broad synthesis, doc demotion applied) rather than symbol-lookup.
3. **Low-information-path penalty:** a bounded down-weight (mirroring `_demote_doc_results` mechanics — down-weight, never exclusion) for ignore files, lockfiles, dependency manifests, and generated surfaces, suppressed when the query names the artifact class. Constants named and documented.
4. **Eval gate:** `1sear` baseline exists first; this change's evidence is the before/after suite report — improved on the misranked classes, non-regressing elsewhere (documented tolerance). A regression on any class blocks merge.
5. **Tests:** classifier unit fixtures for the new precedence + intent; penalty fixtures (penalized by default, exempt when named); the golden-suite diff as the AC evidence.
6. **Question-type contract:** assessment intent SHALL be an explicit public `question_type="assessment"` value, route through every explanatory-like candidate/retrieval gate (window, second hop, and infrastructure/validation signaling), and be added to the MCP tool description and architecture documentation. Existing `navigational`, `explanatory`, `instructional`, and `artifact_anchored` values remain compatible.
7. **Five-value parity:** the exact public set `navigational`, `explanatory`, `instructional`, `artifact_anchored`, and `assessment` plus assessment's explanatory-like behavior SHALL agree across the runtime MCP description, `docs/specs/mcp-tool-surface.md`, `docs/architecture/search-architecture.md`, canonical seed 211, and regenerated `docs/agents/guru.md`. A focused shipped-reference parity test SHALL fail on enum, explanatory-window, or classification-section drift; seed 211 remains the source of truth for rendered Guru content.

## Scope

**Problem statement:** phrase-driven classification misroutes assessment queries and lets low-information paths outrank implementation evidence.

**In scope:** `_classify_question` + candidate-side penalty in the selection path; constants; tests; suite-report evidence.
**Out of scope:** the eval suite itself (`1sear`); reranker/embedding changes; chunking.

## Acceptance Criteria

- [~] AC-1: The review session's misranked queries (encoded verbatim in the golden suite) rank implementation evidence above ignore-file comments post-change. Status note (operator-approved scope reconsideration, 2026-08-31, option (a) of the Decision Log): the classification half is delivered (both queries type as `assessment`; no ignore-file row appears in either answer; question-type accuracy for the class 0.0 to 1.0), but the fixtures' implementation evidence (`_classify_question`, the audit-dispositions sections) is not retrieved by any content mechanism with the local models on either the before or the after side. The recall floor on that holdout fixture is removed; assessment retrieval is recorded as a measured baseline weakness the standing gate reports per class, and a typed findings-register surface is the recorded follow-up plan.
- [x] AC-2: Queries NAMING an ignore file/manifest still surface it top-ranked (exemption fixture-pinned).
- [x] AC-3: Assessment intent classifies distinctly from navigational; "where is <symbol>" stays navigational (both fixture-pinned).
- [x] AC-4: Golden-suite before/after: improved on targeted classes, within tolerance on all others — the merge gate.
- [x] AC-5: Full suite bytecode-free + docs validation; classifier/penalty constants documented in the spec.
- [x] AC-6: `code_ask` returns the documented public `assessment` question type for assessment queries while preserving existing question-type fixtures and explanatory routing semantics.
- [x] AC-7: Runtime description, MCP spec, search architecture, seed 211, and rendered Guru expose the same five question types and assessment routing clauses, with focused parity tests proving seed/render and runtime/doc agreement.

## Tasks

- [x] Classifier: anchoring precedence + assessment intent.
- [x] Selection path: low-information-path penalty (bounded, named constants, exemption).
- [x] Unit fixtures and spec/seed wording.
- [x] Golden-suite before/after runs.
- [x] Update the public `code_ask` question-type description and search architecture documentation for `assessment`.
- [x] Update canonical seed 211, regenerate `docs/agents/guru.md`, and add the focused five-value/explanatory-window parity regression.
- [x] Suite + validate.

## Agent Execution Graph


| Workstream | Owner | Depends On | Notes |
| ---------- | ----- | ---------- | ----- |
| classifier | implementer | 1sear baseline | Precedence + intent |
| penalty | implementer | 1sear baseline | Bounded down-weight |
| gate-evidence | qa-reviewer | both | Suite diff = the verdict |


## Serialization Points

- BLOCKED on `1sear`'s recorded baseline (same wave, ordered). No ranking edit before the gate exists.

## Affected Architecture Docs

- `docs/specs/mcp-tool-surface.md` and `docs/architecture/search-architecture.md` (question-type semantics); canonical seed 211 and its generated `docs/agents/guru.md` surface.

## AC Priority

(Proposed; confirmed at Prepare wave.)


| AC | Priority | Rationale |
| ---- | -------- | --------- |
| AC-1 | required | The live-observed failure. |
| AC-2 | required | The penalty must not break direct questions. |
| AC-3 | required | Intent separation without navigational regression. |
| AC-4 | required | The eval gate IS the merge criterion. |
| AC-5 | required | Standard gate. |
| AC-6 | required | The new classification must be an explicit, documented public contract rather than enum drift. |
| AC-7 | required | All public and generated contract surfaces must expose one five-value enum and one assessment routing rule. |


## Progress Log


| Date | Update | Evidence |
| ---- | ------ | -------- |
| 2026-08-31 | Merge gate re-passed on the final tree with the reverification repairs included: `retrieval-quality-post-1seas-vs-before.json`, verdict `pass`, `production_change_same_generation`, zero violations, baseline identity `05ab0836` (HEAD `server_impl.py`, `matches_head: true`) versus shipped `dab71287`, fixture digest `bda67546`. Holdout `code_ask`: recall 0.562 to 0.594, nDCG 0.435 to 0.527, question-type accuracy 0.36 to 0.91, abstention 1.0 both sides, agentic MRR 0.39 unchanged; the other three tools byte-stable run to run. | `docs/reports/retrieval-quality-post-1seas-vs-before.json` (run `979e96c8`). |
| 2026-08-31 | Focused re-reverification (fresh actor, code and docs-contract lanes, both approved-with-notes at firm confidence): all ten inline repairs verified by execution and mutation probe (the parity test fails when the old docstring sentence is substituted on a scratch copy; owner rows verified for mixed case, wildcard protection, and the end-to-end pin; `index_state_store.py` hash-equal to HEAD; the grade-2 lint-validator anchor resolves through the evaluator's own `code_outline` path). Its three boundary reproductions are repaired with the reproductions as test assertions: the pin exemption lookahead could not cross a path separator, so a path-named subject ("how does docs/agents/guru.md describe retrieval") lost the pin (RV3-1); a dot-directory prefix of an extensionless path leaked as the cue (`.wavefoundry/bin/wf` cued `.wavefoundry`; the cue extractor now skips any match that stops at a path separator, RV3-2); a noun between article and filename defeated the exemption ("the file .aiignore", RV3-3). Carrier follow-through: the twins gain the subject-pins clause and the `section` field sentence byte-identically (RV3-6, seed gate opened and closed), search-architecture states the exact-case fetch boundary (RV3-7), the spec's definition-boost example no longer echoes a calibration fixture query (RV3-4), and the CHANGELOG stray comma is fixed (RV3-5). Probes 17/17; 929 retrieval + 25 parity; docs lint ok. Tree frozen for the evidence re-run. | Focused reverification report (session record); retrieval and parity shards; `wave.md` reverification checkpoints. |
| 2026-08-31 | Cycle-2 reverification by five fresh lanes: every cycle-2 repair held under execution (no report-class rows, true reranker scores, mechanism-framed pin gate, dot-directory cues, abstention, kill switches bound to receipts). The code lane found one regression introduced by the exact-ownership repair and it is fixed: owner-row ownership compared a casefolded indexed path against the cue as typed, so `AGENTS.md`, `CLAUDE.md`, and `README.md` questions were fetched and then dropped (RV-1; both sides are casefolded now, with a mixed-case unit test and an end-to-end pin test). Two boundary repairs from the same reverification: the concrete-path cue alternative requires a dotted, lettered final segment so "input/output" and "and/or" no longer classify `artifact_anchored` (RV-2), and the mechanism-frame exemption tolerates a leading article so "how does the `.aiignore` file exclude paths" keeps the pin (RED-RV-3). The runtime `code_ask` description now carries the spec's `section` and pin-exception wording, pinned by the parity test (DOCS-RV-1). Recorded, not repaired: a "biggest gaps in <file>" question takes the owner pin rather than assessment routing (RED-DEL-9 disposition, artifact anchoring first); a pinned summary chunk can lead with a low score while the envelope reports high confidence (QA-RV-3, for `1wpih`). The five receipts predate these edits; the evidence set is re-run before close. | Lane reports in `wave.md`; retrieval shard with the new tests; parity tests. |
| 2026-08-31 | Merge gate PASSED on the decided corpus (fixture digest `5bd2ff12`, seven floors): `retrieval-quality-post-1seas-vs-before.json`, verdict `pass`, `comparison_kind: production_change_same_generation`, zero violations, generation 12 (attempt `06c20179`), baseline identity `cfc1a7a5…` (HEAD `server_impl.py`, `matches_head: true`) versus current `b993c301…`. Holdout `code_ask`: recall@10 0.562 to 0.625, nDCG@10 0.435 to 0.540, question-type accuracy 0.36 to 0.91; `direct_path_anchored` 0.0 to 1.0; `direct_ignore_manifest` and `abstention` type accuracy to 1.0; `architecture_review_intent` type accuracy 0.0 to 1.0 with recall 0.0 on both sides (the recorded baseline weakness); `code_search`, `docs_search`, `code_lexical` holdout metrics unchanged; warm p95 within 1% of the before pair; largest `code_ask` envelope 23,919 to 24,628 bytes (3%). The HEAD before pair fails five floors. AC-4 closes on this receipt. | The receipt and its four sibling reports (`retrieval-quality-baseline-run1.json`, `retrieval-quality-baseline.json`, `retrieval-quality-before-1seas-run1.json`, `retrieval-quality-before-1seas.json`), all on generation 12. |
| 2026-08-31 | Cycle-2 merge-gate receipt recorded: `retrieval-quality-post-1seas-vs-before.json`, `comparison_kind: production_change_same_generation` (attempt `34b04eef`; baseline identity `cfc1a7a5…` = HEAD `server_impl.py` with `matches_head: true`, current identity `b993c301…`). Holdout `code_ask`: recall@10 0.562 to 0.625, nDCG@10 0.435 to 0.540, question-type accuracy 0.36 to 0.91, abstention 1.0 on both sides with the assessment-phrased controls now typed correctly (class type accuracy 0.5 to 1.0); `direct_path_anchored` recall and MRR 0.0 to 1.0; `direct_ignore_manifest` type accuracy 0.0 to 1.0 with the named manifest at rank 1 (MRR 0.125 to 1.0) and the `.gitignore` renderer question led by the renderer (nDCG 0.917); `code_search`, `docs_search`, and `code_lexical` holdout metrics unchanged; warm p95 within 4% of the before pair; largest `code_ask` envelope 23,912 to 24,625 bytes (3%). The before pair fails six floors, the receipt exactly one: `architecture-review-holdout-remediation recall_at_10 = 0.0`, unchanged from HEAD, the measured limit awaiting the operator's decision. `architecture_review_intent` type accuracy 0.0 to 1.0 is the classification half of the wave's motivating defect; its retrieval half is not delivered by any content mechanism. | The receipt and the four sibling reports; per-class holdout table in the receipt's `metrics.by_tool.code_ask.by_split_and_class`. |
| 2026-08-31 | Review cycle 2 (five fresh lanes plus the standard-depth delivery council) blocked the report-class evidence source and it is REMOVED: every seat that executed probes found it was a subject-blind path-class injector with a synthetic score (dashboard, memory-purge, and nonsense assessment questions each received eight to ten `docs/reports/` rows at 0.30 above on-subject evidence; a nonsense query scored the same holdout recall as the real one; the floor raised the selection cutoff, inverted the `weak` flag, and suppressed the abstention gap; it was inert in any target without `docs/reports/`). What stays: the five-value classifier, the derived docs query with true reranker scores, the score-only report-class versus wave-history prior, walker 16, owner-row injection, and `section` on citations. Repairs that survived review: an explanatory lead ("how does", "why", "explain") keeps a mechanism question explanatory even when an assessment noun appears later (`_EXPLANATORY_LEAD_RE`; the assessment regex now needs a review frame, not a bare noun); a mechanism-framed question about a named file (a which-function question about the `.gitignore` managed block) keeps injection and the artifact type but does not pin the file over reranked evidence (`_MECHANISM_FRAME_RE`); the direct-artifact cue regex tries the concrete-path alternative first so `.wavefoundry/framework/scripts/indexer.py` anchors as a whole path, and `.NET`/`.5` are no longer cues; owner rows are filtered by exact ownership after the LIKE fetch. Executed on the repaired code: `.aiignore`, `pyproject.toml`, and `.wavefoundry/framework/scripts/retrieval_eval.py` pin at rank 1; `render_gitignore_block` leads the `.gitignore` renderer question at 0.999; the assessment-phrased nonsense query abstains (low band, every citation weak, the no-confident-match gap present); the negative control stays explanatory with the posture-gap code leading. Consequence recorded honestly: without the injector the two original assessment fixtures retrieve none of their labels (calibration top ranks are `planner.md`, `guru.md`, a 2025 wave record; the grade-3 `_classify_question` label never enters the pool by any content path), so the class recall floor fails on the holdout split. AC-1 and AC-4 are reopened pending the operator's decision on that floor. | 928 retrieval tests (six new: no path-class injection, assessment abstention, section metadata on code and docs rows, explanatory lead, cue regex, mechanism-framed pin); the cycle-2 review evidence in `wave.md`. |
| 2026-08-30 | Merge-gate receipt recorded: `retrieval-quality-post-1seas-vs-before.json`, verdict `pass`, `comparison_kind: production_change_same_generation` (attempt `6dea4da7`; baseline production digest `552da6c4…` is HEAD's `server_impl.py` with every other production module byte-identical to the working tree, not to HEAD (walker 16 on both sides, so the walker contribution versus HEAD is unmeasured), current digest `3fdadd79…`), no quality, latency, or envelope violation. Holdout `code_ask`: recall@10 0.591 to 0.621, nDCG@10 0.391 to 0.457, question-type accuracy 0.50 to 0.83; `architecture_review_intent` recall 0.0 to 0.333 and question-type accuracy 0.0 to 1.0; `direct_ignore_manifest` question-type accuracy 0.0 to 1.0 with the named manifest at rank 1 (MRR 0.125 to 1.0); abstention 1.0 both sides; `code_search`, `code_lexical`, and `docs_search` holdout metrics unchanged; warm p95 within 1.3% of the before pair; largest `code_ask` envelope 23,047 to 26,028 bytes (12.9%, under the 15% bound; the evidence rows are the cost). The before pair fails four quality floors: two behavioral (assessment recall, manifest rank one) and two definitional (question types HEAD cannot emit). Superseded by review cycle 2: the receipt was produced with a report-class injector the council removed, so it is provenance, not current authority. | The receipt, `retrieval-quality-before-1seas-run1.json`, `retrieval-quality-before-1seas.json`, `retrieval-quality-baseline-run1.json`, `retrieval-quality-baseline.json`. |
| 2026-08-30 | Second executed round on the rebuilt index (walker 16): (1) `code_ask` citations carried no `section` field, so a section anchor was structurally unmatchable on the assessment class's only applicable tool even once the evidence rows were present; citations now expose the chunk's section path when the metadata supplies it. (2) The vector-only evidence pass reached the calibration label but not the holdout label; measured inside the five-document report class, dense similarity ranks the priority tables high while BM25 ranks the planning and interpretation prose high, so the evidence pass is now hybrid (vector and BM25 rows, `ASSESSMENT_EVIDENCE_TOP_K` each, rank-fused, union-bounded by `ASSESSMENT_EVIDENCE_ROWS`; `fts_search` gained a `path_prefix` filter). Executed probe on the frozen generation: calibration `Needs Fixed Now` at rank 8, holdout `Framework Interpretation` at rank 7, both `.aiignore` and `pyproject.toml` direct cases at rank 1 with `mrr_at_10` 1.0. (3) An operational finding, not a code change: the full rebuild left FTS5 unmerged (7,013 data blocks) and the per-call store integrity probe pushed `code_lexical` warm p95 to 1.14 s over the 1 s operator-review ceiling; the framework's own maintenance verb (`index_optimize`: Lance reclaim plus SQLite `optimize`/`VACUUM`) restored 0.6 s. | 926 retrieval tests (`test_server_tools_retrieval.py`: hybrid fusion order, BM25 half bounded to the report class and degrading to empty, citation `section` exposure); `test_index_state_store.py` 40; probe transcripts in the session scratchpad. |
| 2026-08-30 | Review-cycle-1 repair, production side, from executed measurement of the failing report: (1) the calibration assessment query's labeled evidence never entered the 162-candidate pool under any content path (dense, lexical, derived vocabulary, wave-archive filtering), while the holdout query reached it at cosine rank 14 only for the cross-encoder to score it 0.0036 against a 0.0754 top; (2) `.aiignore` itself was walked but dropped by the source filter, so the documented rank-one pin had no owner to pin. Repairs: a bounded assessment evidence source (`ASSESSMENT_EVIDENCE_TOP_K` report-class rows for the derived query, own selection floor, `ASSESSMENT_EVIDENCE_FLOOR_SCORE` ordering floor, confidence read from reranker relevance only); direct-artifact owner-row injection from the published tables before reranking (`DIRECT_ARTIFACT_OWNER_ROWS`); the seven ignore-file names admitted to the code index (`WALKER_VERSION` 16); the `.aiignore` fixture gains the named file itself as a grade-3 path target for `code_ask`/`code_search` (AC-2 says the named file surfaces top-ranked); public prose corrected to say the report-class recovery evaluates no currentness predicate. Measured limit recorded: the holdout fixture's rotated section labels (`Planning Decisions`, `Framework Interpretation`) rank 18 to 28 even inside the five-document report class for their own query, so no content-similarity mechanism can reach them; the class floor is met through the calibration fixture. | Probe logs in the session scratchpad; 924 retrieval tests (`test_server_tools_retrieval.py`, six new: evidence source bounded and floored, assessment-only, confidence unaffected by the floor, owner rows from published tables, pin without vector recall, dedupe of an evidence row already in the pool); 25 parity tests; `test_indexer.py` walker admission pin. |
| 2026-08-30 | Formal Review cycle 1 repaired assessment/instructional precedence, direct-artifact broad routing, low-information exemptions, and five-value parity; added bounded assessment-only derived evidence retrieval. The strengthened live gate still returns zero strictly matched architecture-review evidence and ranks `.aiignore` evidence sixth, so AC-1, AC-2, and AC-4 remain open. Public docs also overstate “current” report preference because runtime boosts the entire `docs/reports/` class without a freshness predicate, so AC-7 remains open despite mechanical parity. | 919 retrieval tests; 25 shipped-reference tests; `docs/reports/retrieval-quality-baseline-run1.json` (`verdict=fail`); fresh delivery council. |
| 2026-08-30 | Formal Review wave reopened AC-1, AC-2, AC-3, AC-4, and AC-7: the frozen report leaves the motivating review targets at zero recall, named artifacts rank 2/4 rather than first, aggregate-only comparison misses class regressions, assessment overrides common instructional forms, and Guru's direct-artifact routing prose contradicts runtime. | Independent code-reviewer, qa-reviewer, and standard-depth red-team delivery primer; frozen-report inspection and public classifier probes. |
| 2026-08-30 | Historical pre-review implementation: direct-artifact-before-phrase classification, public `assessment`, shared explanatory-like routing, and the bounded low-information prior were implemented. The earlier generation-4 comparison is retained as `docs/reports/retrieval-quality-post-1seas-pre-review.json` and was superseded by Formal Review cycle 1's stricter evaluator contract. | `docs/reports/retrieval-quality-post-1seas-attempt1-fail.json`; `docs/reports/retrieval-quality-post-1seas-pre-review.json`; 910 pre-review retrieval tests; 23 pre-review parity tests. |
| 2026-07-12 | Drafted from the external code review (P1 classification), validated: `"where are"` in `navigational_signals` at ~17148, checked before artifact anchoring; the reviewer's two live misrankings recorded for the golden corpus. Explicitly eval-gated on `1sear` per house ranking-change discipline. | Review report; `_classify_question` source; AC-8/AC-10 precedents. |


## Decision Log


| Date | Decision | Reason | Alternatives |
| ---- | -------- | ------ | ------------ |
| 2026-08-31 | OPERATOR DECISION (option a): the nonzero recall floor on `architecture-review-holdout-remediation` is removed from `docs/evals/retrieval-quality-golden.json`; the class keeps its `question_type_accuracy = 1.0` floor; assessment retrieval is reported per class as a measured baseline weakness beside the `docs_search` and `enumeration` holdout zeros; a typed findings-register surface (an MCP resource or a `report` kind on `docs_search`) is parked as a follow-up plan. AC-1 is marked `[~]` with the status note above. | The operator chose (a) after the cycle-2 receipts showed the floor failing identically on HEAD and on the repaired tree, with every other floor passing. | **(b) keep the floor and park the wave:** deferred value of the delivered classification, pins, and receipts. **(c) re-scope labels through an unconsulted reviewer:** still leaves the grade-3 code label unreachable by construction. |
| 2026-08-31 | REVERSED the 2026-08-30 evidence-source decision: citations stay reranker-ordered and no path class is injected or given a synthetic score. The findings register, if wanted as assessment context, is a typed surface for a follow-up plan (an MCP resource or a `report` kind on `docs_search`), not a ranking prior inside `code_ask`. | Cycle-2 council, unanimous on the substance: the source contributed only when it was wrong and was redundant when it was right (on the two subjects the register covers, the ordinary passes had already found the rows at true scores 0.31 to 0.95); it broke the citations-are-semantic invariant the graph signal honors by living in its own section; and the gate could not see the damage because both assessment fixtures live in the injected class. | **Labeled `evidence` side channel (the `graph_related` shape):** truthful contract, but it quarantines the same subject-blind pass and the gate would still measure the prior if it counted that section. **Subject-conditioned admission:** cannot reach the current labels (true relevance 0.0 to 0.005), so it ends at the same floor question with more code. **Keep with mitigations:** every mitigation still needs the synthetic floor to reach the labels. |
| 2026-08-31 | The `architecture_review_intent` holdout recall floor is left in the fixture as written and the report is allowed to say `fail` until the operator decides: (a) drop the nonzero recall floor for the class, keep its question-type floor, and record assessment recall as a measured baseline weakness beside the `docs_search` and `enumeration` zeros (the architecture seat's recommendation), (b) keep the floor and park the wave until a typed findings-register surface exists, or (c) re-scope the class labels through a reviewer who has not read the mechanism. | A standing-gate floor is a scope contract the operator owns; an agent must not amend it to make its own change pass. | **Amend the floor now:** the same act the council just blocked, at the gate instead of the runtime. **Relabel the fixtures to what the system returns:** fixture-fitting, forbidden by the reviewers. |
| 2026-08-30 | Assessment carries a bounded report-class evidence source with its own selection floor and a fixed ordering floor score, while the confidence band keeps reading reranker relevance. | Executed measurement showed broad assessment vocabulary never embeds near the findings register and the cross-encoder scores findings tables near zero, so no weight on existing candidates can surface current findings; a bounded structural source is the only honest lever, and keeping confidence on reranker relevance means the floor orders citations without over-claiming. | **Raise `_ASSESSMENT_REPORT_RECOVERY_WEIGHT`:** cannot lift a candidate that is absent from the pool, and a 2x prior cannot close a 20x reranker gap. **Rerank evidence against the derived query:** still scores near zero. **Represent evidence only through the shared floor without an ordering floor:** the rows land beyond the top ten behind reranked noise, so representation is invisible. **Add a currentness predicate to the report class:** deferred; a calendar-relative predicate makes the standing gate drift with time, and the class-wide recovery is now documented truthfully. |
| 2026-08-30 | Index the seven ignore-file names as line-window code units (walker 16) and inject a directly named path's published rows before reranking. | The direct-artifact exemption and rank-one pin were hollow: `.aiignore` was walked but never eligible, so a question asking what `.aiignore` excludes could only cite code that renders the file. Injection makes the pin independent of vector recall of a tiny file. | **Relabel the fixture to the rendering code only:** contradicts AC-2's "surface it top-ranked". **Pin without indexing:** nothing to pin. **Index every extensionless dotfile:** admits arbitrary noise; the seven names are exactly the low-information ignore class the prior already governs. |
| 2026-08-30 | Expose `assessment` as an explicit public question type while routing it through explanatory retrieval behavior. | Preserves truthful diagnostics and the plan's distinct-intent requirement without conflating assessment with navigation. | **Internal-only assessment normalized to explanatory:** hides the classifier outcome. **Reuse navigational:** preserves the reproduced defect. **Undocumented new enum:** repeats existing descriptor drift. |
| 2026-07-12 | Penalty is a bounded down-weight with a query-names-it exemption — never exclusion, never unconditional. | Mirrors the proven `_demote_doc_results` shape; direct questions about ignore files are legitimate (the review itself noted this). | **Exclusion list:** breaks direct questions. **No exemption:** same. **ML classifier:** heuristics + eval gate are proportionate; revisit only if the suite shows heuristics plateauing. |


## Risks


| Risk | Mitigation |
| ---- | ---------- |
| Intent changes regress existing recipes (seed-211 routing) | Golden suite covers every class; tolerance-gated. |
| Penalty misclassifies legitimate generated-surface questions | Exemption on naming + bounded weight + fixtures. |


## Session Handoff

See `docs/agents/session-handoff.md` for current session state.
