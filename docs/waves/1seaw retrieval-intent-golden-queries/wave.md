# Wave Record

Owner: Engineering
Status: closed
Last verified: 2026-09-08

wave-id: `1seaw retrieval-intent-golden-queries`
Title: Retrieval Intent Golden Queries

## Objective

Make retrieval-quality work repeatable and gated: build the standing golden-query eval suite (Recall@k, nDCG, agentic fix-localization MRR, abstention correctness, latency/size — the permanent replacement for the bespoke AC-8/AC-10 gates each ranking change has rebuilt and discarded), then land the review-validated classifier improvements THROUGH it — artifact/path anchoring before phrase signals, a distinct assessment/review intent, and a bounded low-information-path penalty (ignore files, lockfiles, manifests, generated surfaces — exempt when the query names them). The suite also asks a practical agentic question: can a symptom-level change description retrieve the current implementation and supporting files that contain its fix, using the existing local model stack? The live failure being fixed: broad review queries ("where are the gaps…") classified as navigational and ranking `.aiignore` comments above implementation evidence. No ranking edit merges without the suite showing improvement-without-regression.

## Changes

Change ID: `1sear-enh golden-query-retrieval-eval-suite`
Change Status: `implemented`

Change ID: `1seas-enh question-classifier-artifact-anchoring`
Change Status: `implemented`

Completed At: 2026-08-31

## Wave Summary

Wave `1seaw` (Retrieval Intent Golden Queries) delivered two changes: Golden-Query Retrieval Eval Suite (the Standing Gate for Ranking Changes) and Question Classification: Artifact Anchoring + Low-Information-Path Penalty (Eval-Gated). Notable adjustments during implementation: Golden-Query Retrieval Eval Suite (the Standing Gate for Ranking Changes): Final evidence set re-recorded on the frozen post-reverification tree (fixture digest `bda67546` with the docs-lint grade-2 label, seven floors, index epoch attempt `8916932c`, `end_digest_verified` on all five receipts, no active kill switch): standing pair `retrieval-quality-baseline-run1.json` (`baseline`) and `retrieval-quality-baseline.json` (`pass`, `same_generation_pair`, identical production identity `dab71287`, metric jitter 0.00% run to run, warm p95 `code_ask` 3.74 s and the other tools at or under 0.66 s, all under thresholds, max envelope 23,832 B against a 28,006 B threshold); HEAD before pair `retrieval-quality-before-1seas*.json` (`fail`, five floors: assessment-class question type, manifest holdout type and MRR, docs-lint holdout type and MRR; `server_impl.py` `matches_head: true` from the disclosed scratch directory, siblings byte-identical to the tree); receipt `retrieval-quality-post-1seas-vs-before.json` (`pass`, `production_change_same_generation` `05ab0836` to `dab71287`, zero violations). Supersedes and overwrites the generation-12 attempt `06c20179` set in place. Holdout `code_ask` recall reads 0.594 against the earlier set's 0.625 solely because the added grade-2 label measures beyond the pin (the delegated validator chunk is not retrieved; honest metric, no floor on it).; Golden-Query Retrieval Eval Suite (the Standing Gate for Ranking Changes): Cycle-2 reverification (five fresh lanes, independent of the repairer): architecture, qa, red-team, security, and performance approved-with-notes; docs-contract and code needs-revision on bounded items, all repaired in this cycle. Evaluator side: `_validate_baseline_compatibility` is annotated as the two-tuple it returns (CODE-DEL-4); `matches_head` hashes the raw git blob bytes so a CRLF checkout does not read as a mismatch (RV-4); a clean receipt records `production_identity.end_digest_verified` (RED-RV-6); toggle recording is documented as a superset of the production parsers (RED-RV-5); the caller-less `fts_search(path_prefix=)` parameter is removed, returning `index_state_store.py` to HEAD (RV-3). Corpus side: `direct-path-holdout-docs-lint` gains a grade-2 label on the change-doc checkbox validator the shim delegates to, so recall measures more than the pin (RED-RV-2); fixture queries quoted verbatim in prose carriers are paraphrased so the corpus does not self-quote them (RED-RV-4); CHANGELOG counts eleven classes and names the receipt fields (DOCS-RV-2, RV-5). Measurement notes recorded for `1wpih`: a path-only grade-3 anchor cannot see a weak pinned summary chunk (QA-RV-3); the gitignore-renderer holdout floor discriminates only against an ungated pin (QA-RV-4); the wave's own commentary is now corpus content for the assessment queries (RED-RV-7). The five receipts predate these edits, so the evidence set is re-run before close.; Golden-Query Retrieval Eval Suite (the Standing Gate for Ranking Changes): Standing baseline pair recorded on the decided corpus (fixture digest `5bd2ff12`, seven floors) on one frozen generation (attempt `06c20179`, generation 12): `retrieval-quality-baseline-run1.json` verdict `baseline` and `retrieval-quality-baseline.json` verdict `pass`, `comparison_kind: same_generation_pair`, identical production identity `b993c301…`, no active kill switch, HEAD `ca5e6a5f` disclosed with a dirty module tree. Jitter: `code_ask` 1.55%, `code_lexical` 0.92%, `code_search` 2.67%, `docs_search` 1.09% (permitted regression 25% for every tool); warm p95 3.68 s / 0.68 s / 0.64 s / 0.61 s; largest envelope 24,628 bytes; total run 5.7 minutes. This pair is the reference line future ranking changes compare against. The earlier generation-9 set (one failing floor, since removed by the operator's option (a)) is superseded.

**Changes delivered:**

- **Golden-Query Retrieval Eval Suite (the Standing Gate for Ranking Changes)** (`1sear-enh golden-query-retrieval-eval-suite`) — 11 ACs completed. Key decisions: Symbol anchors score declaration identity as a line-span intersection with the declaration resolved through the public structural paths, and a report is a receipt bound to the production retrieval modules as well as the evaluator.; Make the public production response paths the standing gate and retain dense cosine only as a clearly separated component benchmark if it remains useful.
- **Question Classification: Artifact Anchoring + Low-Information-Path Penalty (Eval-Gated)** (`1seas-enh question-classifier-artifact-anchoring`) — 6 ACs completed. Key decisions: OPERATOR DECISION (option a): the nonzero recall floor on `architecture-review-holdout-remediation` is removed from `docs/evals/retrieval-quality-golden.json`; the class keeps its `question_type_accuracy = 1.0` floor; assessment retrieval is reported per class as a measured baseline weakness beside the `docs_search` and `enumeration` holdout zeros; a typed findings-register surface (an MCP resource or a `report` kind on `docs_search`) is parked as a follow-up plan. AC-1 is marked `[~]` with the status note above.; REVERSED the 2026-08-30 evidence-source decision: citations stay reranker-ordered and no path class is injected or given a synthetic score. The findings register, if wanted as assessment context, is a typed surface for a follow-up plan (an MCP resource or a `report` kind on `docs_search`), not a ranking prior inside `code_ask`.
## Journal Watchpoints

- Watchpoint (ordering is the contract): `1seas` is BLOCKED on `1sear`'s recorded baseline — no classifier or ranking edit before the gate exists and has scored the status quo. This ordering is the wave's reason for being.
- Watchpoint (regression tolerance): the suite's run-to-run tolerance must be measured and documented BEFORE it gates anything — an unreproducible gate gates nothing.
- Watchpoint (exemption fidelity): the low-information-path penalty must keep direct questions working (a query naming `.aiignore` surfaces it top-ranked) — the review itself flagged this as the boundary.
- Watchpoint (anecdotes become cases): the quality-log memory items (constant-value ranking, multi-token summary-first, review-session misrankings) are encoded as suite cases, not left as prose.
- Watchpoint (agentic labels stay honest): fix-localization fixtures use symptom-only holdouts, local provenance, and graded human-reviewed implementation/supporting paths rather than treating every touched file as equally relevant.

## Review Checkpoints

- **Delivery evidence final 2026-08-31: five receipts on the frozen post-reverification tree, every after-side verdict passing, delivery approved.** Standing pair `baseline`/`pass` (`same_generation_pair`, production `dab71287`, metric jitter 0.00% run to run, warm p95 and envelopes under every threshold); HEAD before pair `fail` on five floors; receipt `retrieval-quality-post-1seas-vs-before.json` `pass` (`production_change_same_generation` `05ab0836` to `dab71287`, zero violations; holdout `code_ask` recall 0.562 to 0.594, nDCG 0.435 to 0.527, question-type accuracy 0.36 to 0.91, abstention 1.0). Fixture digest `bda67546` (docs-lint grade-2 label included); index epoch attempt `8916932c`; `end_digest_verified` on all five receipts; no active kill switch; evaluator `61ec60da`; walker 16 / chunker 39 / graph builder 45. Supersedes the generation-12 attempt `06c20179` set (overwritten in place); holdout recall reads lower than that set solely because the added label measures beyond the pin. Only `operator-signoff` remains before close (confirmed by `wf_close_wave` dry-run).
- **Focused re-reverification 2026-08-31: code-reviewer and docs-contract-reviewer approved-with-notes (firm confidence); three boundary reproductions repaired inline; tree frozen for the evidence re-run.** A fresh actor verified all ten inline repairs by execution: mixed-case owner rows inject and pin end to end (wildcard protection intact), the cue and mechanism-frame boundaries hold on 30+ phrasings, `index_state_store.py` is hash-equal to HEAD, the evaluator changes are tested (CRLF blob binding, `end_digest_verified`, two-tuple annotation), the runtime/spec/architecture carriers agree, the parity test provably fails with the old docstring sentence substituted on a scratch copy, and the grade-2 lint-validator anchor resolves through the evaluator's `code_outline` path (22 anchors). Its new findings were bounded: RV3-1 (the pin exemption could not cross a path separator, so a path-named subject lost the pin), RV3-2 (a dot-directory prefix of an extensionless path leaked as the cue), RV3-3 (a noun between article and filename defeated the exemption) are repaired with the reviewer's reproductions as test assertions; RV3-4 (spec example echoed a calibration fixture query), RV3-5 (CHANGELOG comma), RV3-6 (twins gain the subject-pins clause and the `section` field sentence, byte-identically), RV3-7 (exact-case fetch wording) applied. Two pre-existing-at-HEAD calibration self-quotes (the 2026-07-12 council note in this record and one historical spec sentence, now paraphrased) are recorded. Probes 17/17; 929 retrieval + 25 parity green; docs lint ok. No further code edits follow; the five-report evidence run on this tree is the delivery evidence.
- **Cycle-2 reverification 2026-08-31: five lanes reverified by fresh actors, two bounded revisions repaired inline, focused re-reverification and evidence re-run pending.** Every cycle-2 repair was reverified by execution by actors independent of the repairer. Architecture approved-with-notes (all blocking items repaired; ARCH-RV-1 recorded as a 1sear risk: the labelled `docs/reports/` audit files stay in place at close; ARCH-RV-2 widened the 1sear AC-6 note to every cycle-authored class). Qa approved-with-notes (7,772 tests uncached, five mutation probes killed, every AC attested; QA-RV-1 status note on 1sear AC-2; QA-RV-2 handoff counts corrected; QA-RV-3/4 measurement notes routed to `1wpih`). Red-team approved-with-notes (nine cycle-2 findings executed as repaired, no regression; RED-RV-1 CHANGELOG count and example phrase, RED-RV-2 grade-2 label on the docs-lint holdout, RED-RV-3 article-tolerant mechanism frame, RED-RV-4 paraphrased fixture self-quotes, RED-RV-6 `end_digest_verified` all applied; RED-RV-5 documented, RED-RV-7 recorded). Security approved-with-notes (kill switches, production drift, and the isolated git spawn verified against the live store; `path_prefix` escaping verified and the caller-less parameter then removed). Performance approved-with-notes (per-fixture envelope growth gone with the injector; the store-probe memoization stays a follow-up). Docs-contract needs-revision (DOCS-RV-1: the runtime `code_ask` description still carried the older `section` clause and an unconditional pin sentence; DOCS-RV-2: CHANGELOG said nine classes; DOCS-RV-3: review-and-evals lacked the floor-violation precedence) and code needs-revision (RV-1: the exact-ownership repair casefolded the indexed path but not the cue, so `AGENTS.md`, `CLAUDE.md`, and `README.md` owner rows were fetched and dropped, an executed regression of the wave's own guarantee; RV-2: prose slash pairs such as "input/output" classified `artifact_anchored`; RV-3: caller-less `fts_search(path_prefix=)`; RV-4: text-decoded blob hashing in `matches_head`; CODE-DEL-4: return annotation). All repaired in this cycle with falsifying tests (mixed-case ownership unit test and end-to-end pin test, prose-slash and directory cue assertions, article-tolerant frame assertions, CRLF blob binding, runtime-description parity assertions). Focused suites green (929 retrieval, 25 parity, 42 evaluator, 40 state-store, 313 indexer); docs lint ok. Because the repairs touch `server_impl.py` and the corpus (one added label), the five receipts no longer bind the shipped production digest or the corpus digest: a focused independent reverification of the RV-1/RV-2/RED-RV-3/DOCS-RV-1 repairs and a fresh five-report evidence run precede the delivery approval.
- **Final evidence recorded 2026-08-31 after the operator's option (a): five receipts on generation 12 (attempt `06c20179`), corpus digest `5bd2ff12`, seven floors, every after-side verdict passing.** Standing pair `retrieval-quality-baseline-run1.json` (`baseline`) / `retrieval-quality-baseline.json` (`pass`, jitter 0.9% to 2.7%); HEAD before pair `fail` on five floors; receipt `retrieval-quality-post-1seas-vs-before.json` `pass` (`production_change_same_generation`, zero violations; holdout `code_ask` recall 0.562 to 0.625, nDCG 0.435 to 0.540, question-type accuracy 0.36 to 0.91; dot-directory paths 0.0 to 1.0; manifest, ignore-file, and abstention type accuracy 1.0; the other three tools unchanged; envelope +3%). Assessment retrieval (recall 0.0 on both sides for the original fixtures) is recorded as a measured baseline weakness with 1seas AC-1 and 1sear AC-6 carrying `[~]` notes and follow-up plan `1wq0b-enh findings-register-assessment-surface`. Reverification of the cycle-2 repairs by fresh reviewers is the remaining step before signoff.
- **Cycle-2 evidence recorded 2026-08-31: five receipts on generation 9 (attempt `34b04eef`), one failing floor, operator decision pending.** Same-generation pair `retrieval-quality-baseline-run1.json` / `retrieval-quality-baseline.json` (jitter 1.2% to 14.2% per tool, all warm p95 under the absolute ceilings, envelope 24,620 bytes); HEAD before pair `retrieval-quality-before-1seas*.json` (`server_impl.py` `matches_head: true`, six floor failures); receipt `retrieval-quality-post-1seas-vs-before.json` (`production_change_same_generation`: holdout `code_ask` recall 0.562 to 0.625, nDCG 0.435 to 0.540, question-type accuracy 0.36 to 0.91; dot-directory paths 0.0 to 1.0; manifest rank one; abstention 1.0 both sides; the other three tools unchanged). Every receipt on the after side fails exactly one floor, `architecture-review-holdout-remediation recall_at_10 = 0.0`, unchanged from HEAD: the assessment class's original labels are not retrievable by any content mechanism with the local models, and the floor is the operator's scope decision (1seas Decision Log 2026-08-31). Reverification of the cycle-2 repairs by fresh reviewers follows that decision, since the decision changes the fixture digest and the receipts.
- **Formal Review wave — cycle 2 opened 2026-08-31: BLOCKED / repair applied, evidence re-run pending, operator decision required.** Five fresh lanes and a standard-depth delivery council (moderator: wave-coordinator; primer: red-team; fixed seats: architecture-reviewer, security-reviewer, qa-reviewer, reality-checker; rotating seat: docs-contract-reviewer). Lanes: code-reviewer approved-with-notes (every load-bearing claim survived execution; one major: the artifact cue regex collapsed dot-directory paths to `.wavefoundry`); qa-reviewer approved-with-notes (all seven mutation probes killed; one major: AC-1's wording claimed implementation evidence the receipt never showed); architecture-reviewer needs-revision; performance-reviewer approved-with-notes (protocol enforced as specified; the assessment fixtures' own envelopes had grown 53 to 61 percent inside a 12.9 percent pooled maximum); docs-contract-reviewer needs-revision (code citations do carry `section`; the CHANGELOG still said "current"). Strongest challenge (red-team, executed): the report-class evidence source was a subject-blind `docs/reports/` injector with a synthetic 0.30 score whose gate recovery was the injector finding a fixture whose labels live in the injected class; dashboard, memory-purge, and nonsense assessment questions each received eight to ten off-subject report rows above on-subject evidence, a nonsense query scored the same holdout recall as the real one, the floor raised the selection cutoff, inverted the `weak` flag, and suppressed the abstention gap, and the mechanism was inert in any target without `docs/reports/`. Best alternative: a labeled `evidence` side channel with truthful scores (rotating seat) versus removal with a re-pointed class floor (architecture seat); the council chose removal because the side channel quarantines the same subject-blind pass and the gate would still measure the prior. Material disagreement resolved: the qa seat's demand for fixture-scoped floors is met for the new named-file and dot-directory cases; its demand for an unconsulted assessment holdout is recorded as unmet (every fixture an implementer authors after reading the mechanism is a regression detector, not out-of-sample evidence). Further executed findings repaired this cycle: kill switches unbound by the receipt (`retrieval_toggles`, refused on pairs, operator review when active); production identity hashed once (re-hashed at the end, `production_drift`; `git` disclosure of HEAD match and dirty tree); the abstention oracle blind to synthetic rows; the pin overriding a reranked declaration on mechanism-framed questions; a bare assessment noun turning "how does the chunker handle a gap" into an assessment; LIKE wildcards in the owner and prefix clauses; two prose overstatements about the before pair's provenance. Recorded and not repaired: the assessment class holdout recall floor fails without the injector (the labeled sections rank 18 to 28 inside their own five-document class; the grade-3 code label never enters the pool), which is the operator's scope decision (change doc Decision Log 2026-08-31); both assessment fixtures label a wave-period report the close procedure may relocate (architecture seat, option-independent); the per-call store integrity probe that dominates `code_lexical` latency is a follow-up plan. Seat agreement: unanimous block on the evidence source; approved-with-notes on the evaluator receipts.
- **Repair round after Formal Review cycle 1 — 2026-08-30: EVIDENCE RECORDED, cycle 2 pending.** Every cycle-1 block was traced to an executed cause before repair. Evaluator: symbol anchors are declaration-strict (resolved through the public `code_outline`/`code_constants` paths; a result matches only when its line span intersects the declaration; same-file call sites, comment mentions, and summary chunks never match; unresolved or ambiguous symbols invalidate the run); section anchors fall back to the chunker's baked breadcrumb so `code_ask` citations and `code_lexical` rows score heading identity like `docs_search`; reports bind `production_identity`, `run_time`, and `anchor_resolution`; a comparison records its kind and measures jitter only from a same-generation pair with identical production identity. Production: the calibration assessment query's labeled evidence never entered the candidate pool by any content path and the holdout label is reachable only by BM25 inside the report class, so assessment carries a bounded hybrid report-class evidence source (`ASSESSMENT_EVIDENCE_TOP_K` vector and BM25 rows, rank-fused, own selection floor, `ASSESSMENT_EVIDENCE_FLOOR_SCORE` ordering floor, confidence still read from reranker relevance); `.aiignore` was walked but never eligible, so the seven ignore-file names are indexed (`WALKER_VERSION` 16) and a directly named path's published rows are injected before reranking; `code_ask` citations expose `section`; the five public carriers say the report-class recovery evaluates no currentness predicate. Evidence on one frozen generation (`6dea4da7`): `docs/reports/retrieval-quality-baseline-run1.json` (`baseline`), `retrieval-quality-baseline.json` (`pass`, same-generation pair, jitter 0.06% to 2.2%), `retrieval-quality-before-1seas-run1.json` and `retrieval-quality-before-1seas.json` (HEAD `server_impl.py`, `fail` on four floors), `retrieval-quality-post-1seas-vs-before.json` (`pass`, `production_change_same_generation`: holdout `code_ask` recall 0.591 to 0.621, nDCG 0.391 to 0.457, question-type accuracy 0.50 to 0.83, no violation). The failing intermediate attempts are retained as `retrieval-quality-review-repair-attempt{2,3,4}-fail.json`. Recorded limits: the holdout fixture's rotated section labels are reachable only through the BM25 half of the evidence pass; `docs_search` holdout recall stays 0.0 and `enumeration` holdout recall stays 0.0 on both sides (measured baseline weaknesses outside this wave); the currentness predicate is deferred. Review Evidence below still carries the cycle-1 lane verdicts; cycle 2 must re-run every lane with fresh reviewers.
- **Formal Review wave — cycle 1 synthesis 2026-08-30: BLOCKED / changes required.** The red-team primer and every fresh post-repair correctness seat agree that the strengthened production-path report is authoritative and must block delivery: `architecture_review_intent` has `code_ask` Recall@10 `0.0` for both calibration and holdout, below the explicit nonzero critical floor, while the live `.aiignore` case's first relevant evidence is rank 6 rather than rank 1. The evaluator repairs are material and verified (strict per-tool labels, per-class comparison, critical floors, stronger baseline identity, monitor-free construction, rotated holdout, and 32 focused tests), but symbol anchors can still accept a same-file call-site mention, reports do not bind the production retrieval implementation, and no current-schema same-generation pair/before-after receipt exists. The 7,752-test post-repair framework suite passes; performance is below absolute ceilings, but final relative performance evidence remains pending the missing pair. No delivery approval is recorded.
- **Formal Review wave — cycle 1 opened 2026-08-30: BLOCKED / repair initiated.** Fresh independent code-reviewer and qa-reviewer lanes plus the standard-depth red-team delivery primer reproduced blocking defects: evaluator-owned runs start the production model-prewarm worker; comparison is aggregate-only and insufficiently binds store/attempt/model/provider identity; symbol/content/section anchors accept arbitrary substrings in a same-file wrong chunk; two exact holdout queries leaked into unit tests; named direct artifacts rank 2/4 instead of first; motivating assessment targets remain at zero recall; common instructional forms can be classified as assessment; and Guru's direct-artifact routing prose contradicts runtime. The affected required ACs were reopened and the repair/reverification outcome is summarized above.

- **Implementation evidence review — 2026-08-30: HISTORICAL / SUPERSEDED BY FORMAL REVIEW CYCLE 1.** The earlier review used the weaker evaluator contract and cannot close current ACs. Its generation-4 pass is retained as `docs/reports/retrieval-quality-post-1seas-pre-review.json`; the formal review's stricter anchors, per-class floors, and baseline identity supersede its verdict.
- **Golden-gate evidence — 2026-08-30: HISTORICAL / SUPERSEDED BY FORMAL REVIEW CYCLE 1.** The old generation-103 pair is retained as `docs/reports/retrieval-quality-baseline-pre-review-run1.json` and `docs/reports/retrieval-quality-baseline-pre-review.json`; the earlier failed post-change artifact remains `docs/reports/retrieval-quality-post-1seas-attempt1-fail.json`. These artifacts use the old fixture/evaluator identity and are not current comparison authority.

- **Plan review — 2026-08-30: COMPLETE.** Requirements, scope, acceptance criteria, and wave sequencing were walked for both admitted changes. No operator-only product decision remains: applicability is explicit per tool/query pair; shared-file relevance requires chunk-level anchors; the evaluator is monitor-free and generation-frozen per run; controlled pre/post comparisons record both generations; degraded tests use a disposable store; and `assessment` is a public question type that shares all explanatory-like gates while preserving `navigational`, `explanatory`, `instructional`, and `artifact_anchored`.
- **Prepare-phase Wave Council [prepare-council] — 2026-08-30: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, docs-contract-reviewer; rotating-seat: docs-contract-reviewer; strongest-challenge: a standing gate and fifth question type could pass ordinary lint while public enum descriptions, generated Guru guidance, evaluator invocation, and report semantics still disagree; strongest-alternative: reuse the dense-only fixture runner and update prose selectively — rejected because it bypasses shipped hybrid/reranked behavior and preserves cross-surface drift)
- Council synthesis: READY. The five-value parity contract, canonical seed-to-render ownership and drift tests, pinned Python CLI/report/exit ABI, monitor-free construction, and generation-change invalidation resolve the challenge. Docs-contract verdict approved at high confidence; no blocking finding remains.
- Council seat evidence — docs-contract-reviewer: approved at high confidence after verifying the five-value runtime/spec/architecture/seed/render parity contract, canonical seed ownership, and exact evaluator CLI/report/exit ABI; no blocking finding.
- **Prepare specialist readiness — 2026-08-30:** code-reviewer approved-with-notes, qa-reviewer approved, architecture-reviewer approved-with-notes, and performance-reviewer approved after two amendment rounds. The final plan pins public response execution, chunk-level anchors, disposable degradation, exact timeout/jitter/payload gates, five-value assessment parity, and every explanatory-like branch. No specialist blocker remains.

- **Scope amendment — 2026-08-18:** added the ninth, agentic fix-localization class and its local-provenance/graded-relevance contract. The 2026-07-12 council evidence below predates this material scope addition and remains historical evidence only; run a fresh **Prepare wave** review before implementation.

- **Prepare-phase Wave Council [prepare-council] — 2026-07-12: PASS** (moderator: wave-council; primer-depth: standard; seats: reality-checker, red-team, qa-reviewer, architecture-reviewer; rotating-seat: qa-reviewer; strongest-challenge: an unreproducible eval gates nothing — retrieval scores can vary run-to-run (reranker nondeterminism, index drift), so a naive before/after diff could pass noise as improvement or block real gains as regression; resolved structurally by AC-3: the tolerance is MEASURED (two same-index runs) and documented BEFORE the suite gates anything, reports record the index meta signature, and comparisons are valid same-signature only; strongest-alternative: skip the standing suite and run another bespoke gate for `1seas` — rejected because this is the third ranking change to need one (1p41o, 1p4hj precedents) and the anecdote backlog in the quality log is exactly the corpus a standing suite executes)
- Council seat notes: reality-checker — the classifier claim is source-verified (`"where are"` in `navigational_signals` at ~17148, phrase check preceding artifact anchoring), the two live misrankings occurred in the review session against this repo, and the bespoke-gate precedents are real (AC-8 gated OUT `code_risk_score` v1 on measured degeneracy; AC-10 ran an 8-query recall comparison) — both were then discarded, proving the standing-suite gap. red-team — pressed overfitting (a suite tuned to this repo's quirks): accepted explicitly for the gate's purpose (this repo IS the dogfood; consumer packs extend later via p4ea) and recorded in the change doc; pressed the exemption boundary hardest: the penalty must not break \"what does .aiignore exclude?\" — AC-2 pins the named-artifact exemption with fixtures; pressed gaming-the-gate (tuning `1seas` to the suite): bounded by the suite covering ALL eight classes with tolerance-gated non-regression, not just the targeted queries. qa-reviewer — the eight query classes are enumerated with negative controls, the misranked queries are encoded VERBATIM, and the suite is deliberately outside the hermetic unit run (needs a built index + cached models) with its own one-command entry; the ordering contract (`1seas` blocked on the recorded baseline) is the wave's serialization point and is fixture-independent (a process gate, enforced at review). architecture-reviewer — the suite becomes the standing gate documented in the contributing/review surfaces (replacing per-change bespoke harnesses), which is an architecture-of-process improvement with the same shape as the docs gate; the classifier changes mirror proven mechanics (`_demote_doc_results` down-weight, never exclusion) and stay heuristic — the ML-classifier alternative is parked unless the suite shows heuristics plateauing. seat_agreement: unanimous.
- AC priority: confirmed at prepare as proposed (all required across both changes). Product-owner acknowledgment: operator-directed 2026-07-12 ("build these waves now"); sequenced after `1seav` by priority.

- **Prepare-council amendment [prepare-council-amendment] — 2026-07-12:** seat-alignment audit (triggered by the external plan review) — the brief's required rotating seat is `architecture-reviewer`, which WAS run with substantive findings in the original council (standing-gate-replaces-bespoke, `_demote_doc_results` mechanics reuse, ML-classifier parked); the original verdict line mislabeled the rotating seat as qa-reviewer. This amendment corrects the label; no seat content was missing. Verdict unchanged: READY.

## Review Evidence

- wave-council-readiness: approved 2026-07-12 — prepare council synthesis verdict READY: the ordering contract (suite baseline before any ranking edit) is the structural control, reproducibility is a measured precondition rather than an assumption, and the exemption boundary is fixture-pinned. Seats unanimous; full synthesis in Review Checkpoints.
- wave-council-delivery: approved 2026-08-31 — standard-depth council synthesis over the cycle-2 reverification (five fresh lanes) and the focused re-reverification (code and docs-contract): every blocking finding repaired with falsifying tests, no regression outstanding, and the authoritative receipt on the frozen shipped tree passes with zero violations while the HEAD before pair fails five floors. Strongest challenge: the repairs landed after the five lanes read the tree; resolved by the focused independent re-reverification of exactly those repairs and a fresh five-receipt evidence set bound to the shipped digests (production `dab71287`, corpus `bda67546`, `end_digest_verified`). Remaining notes are recorded follow-ups (`1wq0b` findings-register surface, `1wpih` measurement-fidelity items).
- code-reviewer: approved-with-notes 2026-08-31 (focused reverification after the inline repairs) — firm confidence; the mixed-case ownership regression is repaired and pin/cue/frame boundaries verified on 30+ phrasings; the RV3 boundary reproductions were repaired in-cycle with the reproductions as tests.
- docs-contract-reviewer: approved-with-notes 2026-08-31 (focused reverification after the inline repairs) — firm confidence; runtime, spec, architecture, and twin carriers agree; the parity test provably rejects the pre-repair docstring sentence.
- code-reviewer: approved-with-notes 2026-08-31 — strong confidence; declaration-strict anchors, comparison kinds, receipt digests, and the rank-one pins all survived execution; the dot-directory cue regex defect was repaired in the same cycle.
- qa-reviewer: approved-with-notes 2026-08-31 — strong confidence; every AC had executed evidence and all seven mutation probes were killed; the AC-1 wording overstatement is now reflected by the reopened AC.
- architecture-reviewer: needs-revision 2026-08-31 — strong confidence; the evidence floor distorted selection and the holdout gain was in-sample; the council removed the source and reopened the class floor as an operator decision.
- performance-reviewer: approved-with-notes 2026-08-31 — strong confidence; the AC-11 protocol is enforced as specified and every receipt figure recomputes by hand; the maintenance precondition is now documented.
- docs-contract-reviewer: needs-revision 2026-08-31 — strong confidence; the `section` clause and the CHANGELOG "current" wording were false and are repaired; parity re-verification is due with the re-run.
- wave-council-delivery: blocked 2026-08-31 — standard-depth council; fixed seats architecture-reviewer, security-reviewer, qa-reviewer, and reality-checker; rotating seat docs-contract-reviewer. Strongest challenge: a subject-blind path-class injector with a synthetic score is not retrieval and its gate recovery was fixture-shaped. Best alternative: a labeled evidence side channel; rejected in favour of removal. Consequence of the delivered path had it shipped: every assessment question off the index-quality subject would have led with unrelated report rows. Seat agreement: unanimous block on the evidence source; the evaluator receipts approved-with-notes. Re-run of the five-report evidence set and an operator decision on the assessment class floor precede any delivery approval.
- code-reviewer (cycle 1): needs-revision 2026-08-30 — high confidence; the live critical floor and `.aiignore` rank-one contract fail, and symbol anchors still allow a bounded same-file mention to satisfy a declaration target.
- qa-reviewer: blocked 2026-08-30 — high confidence; repaired false-pass protections pass, but the current golden report is red, AC-1/AC-2/AC-4 remain unmet, and no compatible comparison pair exists.
- architecture-reviewer: blocked 2026-08-30 — high confidence; the assessment fixture is valid and accepts multiple substantive targets, yet retrieves none; AC-9 remains open until declaration identity is objective.
- performance-reviewer: conditionally-approved 2026-08-30 — absolute warm p95 and envelope ceilings pass (`code_ask` 3.752s, `code_search` 0.642s, `docs_search` 0.662s, `code_lexical` 0.664s; maximum envelope 22,760 bytes), but final AC-11 evidence requires a compatible second run.
- docs-contract-reviewer: blocked 2026-08-30 — five-value parity passes 25 focused tests, but current public/tracking claims overstate `.aiignore` rank-one behavior and “current” report preference, and report receipts omit production runtime identity.
- wave-council-delivery: blocked 2026-08-30 — standard-depth council; fixed seats architecture-reviewer, security-reviewer, qa-reviewer, and reality-checker; rotating seat docs-contract-reviewer. Strongest challenge: a deterministic gate still cannot prove delivery if it binds the evaluator wrapper but not the production retrieval code and its authoritative report says FAIL. Best alternative: bind typed declaration/runtime evidence, repair principled assessment and excluded-artifact retrieval, then require a floor-passing same-generation pair and before/after receipt. Consequence of the current path: users receive an `assessment` label but historical/procedural citations, while the wave would contradict its own merge gate. Seat agreement: unanimous block; performance separately found no absolute-budget blocker.
- operator-signoff: approved 2026-08-31 — operator confirmed closure in session ("ok, close the wave") after the final five-receipt evidence set, the reverification round, and the delivery approval were recorded.

## Dependencies

- No external wave dependencies. Sequenced after `1seav` by priority, not necessity. CI scheduling of the suite is a separate operator infra decision.

## Context Efficiency

<!-- wave:context-efficiency begin -->

Estimated token savings use phase-unique returned source versions and mapped workflow prompts, minus recorded request and response tokens. Saved model output or avoided tool loops count only through quality-equivalent paired evidence.

| Stage | Tool calls | Estimated token savings |
| --- | ---: | ---: |
| plan | 478 | 2,694,154 |
| implement | 600 | 10,085,920 |
| review | 328 | 6,475,860 |
| **Total** | **1,406** | **19,255,934** |

<!-- wave:context-efficiency-state {"generation":921,"measurement_status":"healthy","pending":false,"schema_version":1,"stages":{"implement":{"calls":600,"content_source_credit":11865304,"derived_artifact_credit":0,"direct_net":10085920,"estimated_tokens_saved":10085920,"matched_pair_residual":0,"paired_evaluation_count":0,"request_debit":18413,"response_debit":1765243,"source_credit_count":379,"source_credit_drop_count":0,"structural_source_credit":0,"workflow_prompt_credit":4272},"plan":{"calls":478,"content_source_credit":4547823,"derived_artifact_credit":0,"direct_net":2694154,"estimated_tokens_saved":2694154,"matched_pair_residual":0,"paired_evaluation_count":0,"request_debit":15337,"response_debit":1896480,"source_credit_count":349,"source_credit_drop_count":0,"structural_source_credit":55958,"workflow_prompt_credit":2190},"review":{"calls":328,"content_source_credit":7524676,"derived_artifact_credit":911,"direct_net":6475860,"estimated_tokens_saved":6475860,"matched_pair_residual":0,"paired_evaluation_count":0,"request_debit":8898,"response_debit":1042175,"source_credit_count":263,"source_credit_drop_count":0,"structural_source_credit":0,"workflow_prompt_credit":1346}},"store_instance_id":"f294635fbf24489a9a50af63451b2532","totals":{"calls":1406,"content_source_credit":23937803,"derived_artifact_credit":911,"direct_net":19255934,"estimated_tokens_saved":19255934,"matched_pair_residual":0,"paired_evaluation_count":0,"request_debit":42648,"response_debit":4703898,"source_credit_count":991,"source_credit_drop_count":0,"structural_source_credit":55958,"workflow_prompt_credit":7808},"wave_id":"1seaw retrieval-intent-golden-queries"} -->
<!-- wave:context-efficiency end -->

## Estimated Exploration Avoided

<!-- wave:exploration-avoided begin -->

This is a bounded estimate from exact-match memory advisories. It is not added to measured Context Efficiency.

| Advisory surfaces | Citations | Records credited | Estimated tokens avoided |
| ---: | ---: | ---: | ---: |
| 23 | 0 | 11 | 17,879,290 |

estimated: a surfaced (or cited) advisory does not prove a re-exploration was avoided; this is grounded in the measured cost of the original exploration, scaled by a bounded exact-match attribution, and is NEVER summed into the measured Context Efficiency token total.

<!-- wave:exploration-avoided-state {"cited_events":0,"credited_records":11,"estimated_exploration_avoided":17879290,"surfaced_events":23} -->
<!-- wave:exploration-avoided end -->
