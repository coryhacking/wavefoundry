# Wave Record

Owner: Engineering
Status: closed
Last verified: 2026-07-14

wave-id: `1ro44 agent-memory-and-retrieval-decay`
Title: Agent Memory And Retrieval Decay

## Objective

Ship the Wave Memory layer (typed, evidence-backed, graph-attached memory records surfaced at action time) on top of the graph platform landed in waves 1p9q3/1p9qh, together with churn-aware temporal decay for retrieval: index-time freshness/churn metadata, doc-code drift detection, and kind-aware memory-confidence decay. When this wave closes, agents receive prior-learning advisories before risky actions and can distinguish current documentation from drifted documentation in every retrieval response.

## Changes

Change ID: `1p8gy-enh graph-backed-agent-memory`
Change Status: `implemented`

Change ID: `1ro43-enh churn-aware-retrieval-decay`
Change Status: `implemented`

Completed At: 2026-07-14

## Wave Summary

Wave `1ro44` (Agent Memory And Retrieval Decay) delivered two changes: Graph-backed agent memory and Churn-Aware Retrieval Decay. Notable adjustments during implementation: Graph-backed agent memory: Self-directed adversarial pass (operator-requested; 4 parallel executed-probe reviewers). Core held: ancestry pruning self-defending, fail-closed walks, delimiter injection, cache/generation/symlink atomicity all survived. Fixed further findings: merge-DAG attribution (`-c` in the history walk); parser/lint bullet parity (unsafe `*`-bullet surfaced → mirror lint exactly) + confidence/date grammar alignment (lint now calendar-validates); drift torn write → single `replace_attribution_and_drift` transaction; gardener confirm fail-open + DoS → one `git cat-file --batch` (real error fails closed, one subprocess) + pass-1 truncation guard; heading-less normalization scoped to frontmatter; reader/lint nested-record parity (`is_relative_to`); hash-prefixed drift fingerprint. +14 regression tests; suite 5,352 OK. Full per-finding disposition in the wave record.; Graph-backed agent memory: Delivery re-review ROUND 3 remediated (6 findings): per-record symlink read (loader per-candidate containment); cache failure aliasing (typed `read_memory_generation`, atomic bump in a dedicated `memory-state.sqlite` decoupled from canonical state, unconditional post-mutation cache eviction, bypass when generation unreadable); gardener parse-failure + line-shape (patch-structure validation + metadata-scoped content comparison; fenced/body date stays material); history walk fail-open (typed `_collect_git_history` with SHA/parent validation; BOTH walks must succeed before replace); indexer over-invalidation (gated on changed/removed via `_memory_record_touched`); evidence drift (AC-2 ancestry wording, round-2 count/parse-claim corrected). +11 regression tests; suite 5,266 OK.; Graph-backed agent memory: Delivery re-review ROUND 2 remediated (7 blockers): symlink boundary now covers reads + validates-before-mkdir via the single `canonical_memory_root` chokepoint; runtime parse mirrors all load-bearing lint rules (superseded-link, section bullets); gardener detector returns typed `(ok, pairs)` and preserves prior drift on failure; normalization scoped to the canonical header date line; churn counted over `anchor..HEAD` ancestry (merge-DAG correct) via the `%P` graph; advisory cache keyed on a bounded monotonic memory generation (tool + indexer bumped) — no O(N) walk, no aliasing; centrality task checked + AC-5 warm-cache wording corrected. +23 regression tests; suite 5,254 OK. Full per-finding disposition in the wave record.

**Changes delivered:**

- **Graph-backed agent memory** (`1p8gy-enh graph-backed-agent-memory`) — 13 ACs completed. Key decisions: Integrate memory graph writes into the 1p9q3 incremental-merge/query-cache infrastructure rather than a standalone memory graph or write path.; Select a local typed memory layer backed by Markdown records, semantic indexing, and graph edges.
- **Churn-Aware Retrieval Decay** (`1ro43-enh churn-aware-retrieval-decay`) — 14 ACs completed. Key decisions: Index-time temporal metadata + annotation-first surfacing, with evidence-gated post-rerank drift partition; raw scores untouched.; Storage choice finalized: consume the `1rq4h` `index-state.sqlite` tables (`file_freshness`, `file_commits`, `wave_landing`, `wave_change_files`, `doc_drift`) landed with wave `1rsh9` — no new store, no schema bump for population-only work.
## Journal Watchpoints

- Watchpoint (cross-change seam): the `1ro43` freshness primitive signature blocks `1p8gy` kind-aware decay (AC-13) — sequence the `1ro43` churn-metadata workstream first or pin the interface early.
- Watchpoint (graph write path): graph writes from memory records must reuse the 1p9q3 incremental-merge SQLite store and invalidate the graph query cache correctly; a parallel write path is a defect. `GRAPH_BUILDER_VERSION` bump required with the new node/edge shapes.
- Watchpoint (ranking safety): the drift partition never reorders on raw age, is evidence-gated behind the census (`1ro43` AC-8), and ships with a kill switch; flipping the demotion default is blocked until census findings are recorded.
- Watchpoint (content safety): memory forbidden-content lint (no secrets/transcripts/personal facts) lands with the schema, not deferred until after the first records exist.
- Watchpoint (historical class boundary): `docs/waves/` chunks are annotation-only (landing-commit anchor, waves-behind) — never drift-flagged, never worklisted, never routed to verify/amend; any ranking treatment for the class is deferred pending census evidence.
- Watchpoint (verification semantics): gardener `Last verified` stamps are mechanical file-touch records and never count as verification events; drift anchors only to doc content changes or commit-SHA verification stamps, and `docs_gardener` is blocked from touching those stamps. The stamp-writing agentic review loop is deferred to `1rolq-enh verify-docs-agentic-review` (docs/plans, future wave after this one lands); this wave must leave the worklist/stamp contracts documented and stable for it.

## Participants

- code-reviewer — framework-script changes across `server_impl.py`, `indexer.py`, `graph_indexer.py`, `docs_lint.py`
- qa-reviewer — AC priority tables present on both changes; census/evidence gates verified
- architecture-reviewer — new MCP tool contracts (`wave_memory_*`), graph schema extension, index/retrieval boundary changes
- docs-contract-reviewer — lifecycle seed/prompt updates (pause/review/close/Guru), worklist/stamp consumer contracts
- performance-reviewer — churn extraction in the build path, advisory surfacing on hot read tools, graph-cache interplay
- security-reviewer — memory forbidden-content rules and lint, evidence-ref scoping

## Review Checkpoints

- **Round-4 re-review (7) SCOPE CORRECTION (operator decision) — 2026-07-14.** The re-review (7) proposed a P1: git's GLOBAL/SYSTEM config selectors weren't covered, so an ambient `GIT_CONFIG_GLOBAL` `diff.renames=false` reshaped derived path history. The interim fix NEUTRALIZED global/system config (`GIT_CONFIG_NOSYSTEM=1` + `GIT_CONFIG_GLOBAL`/`GIT_CONFIG_SYSTEM`→`os.devnull`). **The operator ruled this overreach and the safe.directory follow-up a non-blocker, and directed a simplification.** A user's git config influencing rename detection is normal git behavior, not corruption; and nuking protected config discards operator-configured `safe.directory` trust, turning ordinary shared/differently-owned checkouts (containers, CI mounts, WSL, shared workspaces) into "dubious ownership" failures → `probe_failed` → no git freshness/drift. Applied simplification:
  - **Reverted the global/system config neutralization.** Protected global/system config now passes through UNCHANGED, so `safe.directory` keeps working. (A genuinely-untrusted repo still fails `rev-parse` → `probe_failed` → drift preserved / mtime freshness fallback — the existing graceful path; no re-injection mechanism is built.)
  - **Pinned the one parser-critical setting with a command flag instead:** `--no-renames` on `_collect_git_freshness` and `_collect_git_history` (command flags outrank all config levels), so rename attribution is deterministic regardless of the user's `diff.renames` — consistent with the gardener pass, which already uses `--no-renames`.
  - Kept the genuinely-real, executed-proof fixes untouched: `GIT_DIR`/work-tree/common-dir/shallow/graft/replace-ref/object+index override stripping via the `--local-env-vars` census + fallback, the single `_run_git` chokepoint, and the AST census (retained as hardening).
  Evidence: `test_doc_drift.AmbientGitDirIsolationTests.test_sanitized_env_preserves_protected_global_config` (protected config passes through; no forced `GIT_CONFIG_NOSYSTEM`) and `test_rename_attribution_is_deterministic_via_pinned_flag` (invariant to ambient `diff.renames=true|false` via the pinned flag, with a forced-`-M` non-vacuity guard); `test_indexer.AmbientGitEnvBuildIsolationTests.test_full_build_matches_clean_control_under_git_config_global` (full `build_index` invariance). **The git-config-determinism scope is intentionally CLOSED** — there is NO hermetic-git project, NO planned future wave, and NO standing watch item; git config influencing git behavior is normal, and this is revisited only if concrete field evidence of a real problem appears. Full suite **5,418 tests across 48 files, OK** (via `run_tests.py`) bytecode-free, docs-lint clean, `git diff --check` clean. Also removed the pre-existing mid-file `unittest.main()` blocks in `test_indexer.py` (a touched file) so direct execution collects all tests.
  Suite-count note: the authoritative count is from `run_tests.py` (canonical per `AGENTS.md`; runs each of the 48 files in an ISOLATED subprocess and sums). A monolithic top-level `unittest discover` reports a different total because several test files register modules under shared `sys.modules` names (`index_state_store`/`indexer`/`server_impl` via `spec_from_file_location`) that collide in one process — hence the per-file runner.
  `wave-council-delivery` remains WITHHELD pending the reviewer's independent re-verification; no operator signoff is inferred and no close action is taken.
- **Round-4 re-review (6) blocker REMEDIATED — 2026-07-14.** The re-review confirmed the ambient-`GIT_DIR` chokepoint and found the sanitized ENV SET was incomplete; fixed with the authoritative census + real regressions; full suite **5,415 tests across 48 files, OK** bytecode-free, docs-lint clean, `git diff --check` clean.
  1. **P1 `GIT_SHALLOW_FILE` (and the graft/replace/config-injection class) still corrupted derived history → FIXED.** The strip denylist omitted git's repository-LOCAL *interpretation* vars (`GIT_SHALLOW_FILE`, `GIT_GRAFT_FILE`, `GIT_REPLACE_REF_BASE`, `GIT_NO_REPLACE_OBJECTS`, `GIT_CONFIG*`, `GIT_IMPLICIT_WORK_TREE`) — executed repro: an ambient `GIT_SHALLOW_FILE` silently shrank the history walk (drift published `commits_since: 3`→`1`). Fix: the strip-set is now the AUTHORITATIVE `git rev-parse --local-env-vars` census (git's own list, robust across versions) unioned with a hardcoded fallback superset, computed once and applied by the single `_run_git` chokepoint. Evidence: `test_doc_drift.AmbientGitDirIsolationTests` — `test_ambient_git_shallow_file_does_not_truncate_history` and `test_ambient_git_graft_file_does_not_reshape_history` (both guard that the ambient var actually perturbs raw git before asserting the sanitized path is unaffected), `test_local_env_vars_census_all_stripped` (fixture: every `--local-env-vars` entry is stripped), and `test_indexer.AmbientGitEnvBuildIsolationTests` (full `build_index` freshness+drift match a clean-env control exactly under `GIT_SHALLOW_FILE`).
  Non-blocking evidence cleanups also done: the AST census (`GitSubprocessCensusTests`) is strengthened to catch `Popen`, aliased modules, tuple/variable-built commands (flags any process-runner call outside `_run_git`/`_git_strip_vars`, not just literal git lists); the contradictory suite counts in `session-handoff.md` are reconciled to a single authoritative Current-State count; and the premature mid-file `unittest.main()` in `test_doc_drift.py` is removed.
  `wave-council-delivery` remains WITHHELD pending the reviewer's independent re-verification; no operator signoff is inferred and no close action is taken.
- **Round-4 re-review (5) blocker REMEDIATED — 2026-07-14.** The re-review confirmed the corrupt-`.git` and changed-build fixes and found one adjacent P1; fixed with a real target-vs-decoy regression and a structural census; full suite **5,411 tests across 48 files, OK** bytecode-free, docs-lint clean, `git diff --check` clean.
  1. **P1 derived git state still read the ambient `GIT_DIR` → FIXED.** `_git_authority` sanitized its OWN env, but the downstream git subprocesses did not — so an ambient `GIT_DIR` pointing at an unrelated DECOY repo redirected the freshness history, drift history, `git cat-file` blob reads, and gardener classification to the decoy even though `-C <root>` named the target (executed repro: a target row at `commits_since: 3` was replaced with `drifted: false, commits_since: 0` from the decoy). Fix: every git subprocess in the derivation chain — including the previously-unsanitized `_git_head` fingerprint probe and the DIRECT `subprocess.run(... cat-file ...)` — now routes through ONE shared `_run_git` wrapper that applies the sanitized env (C locale + stripped `GIT_DIR`/`GIT_WORK_TREE`/`GIT_COMMON_DIR`/`GIT_CEILING_DIRECTORIES`/index+object-dir overrides). Evidence: `test_doc_drift.AmbientGitDirIsolationTests` (target-vs-decoy: history + freshness + end-to-end drift all derive from the TARGET under an ambient `GIT_DIR`/`GIT_WORK_TREE`) + `GitSubprocessCensusTests` (AST census: no raw `subprocess.run`/`isolated_run` with a git command exists outside `_run_git`).
  `wave-council-delivery` remains WITHHELD pending the reviewer's independent re-verification; no operator signoff is inferred and no close action is taken.
- **Round-4 re-review (4) blockers REMEDIATED — 2026-07-14.** The re-review verified the no-op fix and found two remaining P1s in the git→non-git handling; both fixed with real-git regression coverage; full suite **5,408 tests across 48 files, OK** bytecode-free, docs-lint clean, `git diff --check` clean. Per finding:
  1. **P1 real corrupt-git states still authorized clearing → FIXED.** Matching only the "not a git repository" string was insufficient: git emits it for an empty/corrupt `.git/`, a broken `.git` worktree pointer, and (via an inherited `GIT_DIR=/missing`) even a valid repo — each was mis-read as `confirmed_non_git` and a real `build_index` then destructively cleared persisted drift. Two-part fix: (a) `_git_c_locale_env` now STRIPS the repository/discovery env overrides (`GIT_DIR`, `GIT_WORK_TREE`, `GIT_COMMON_DIR`, `GIT_CEILING_DIRECTORIES`, index/object-dir overrides, …) so the probe reflects the real on-disk tree; (b) `confirmed_non_git` now requires BOTH the positive fatal AND the genuine ABSENCE of any `.git` marker at root/ancestors (`_git_marker_present`, which treats a present dir/file/symlink/broken-pointer/unreadable marker as present) — a present-but-invalid marker forces `probe_failed` (preserve). Evidence: `test_doc_drift.GitAuthorityTypedStateTests` — real empty-`.git/`, broken-worktree-pointer, and inherited-bad-`GIT_DIR` cases, plus preservation through BOTH the build-tail and no-op reconcile paths under corruption.
  2. **P1 changed-build drift-clear failure still published success → FIXED.** The build-tail discarded `update_drift_from_build`'s result, so a `drift_clear_failed` on a CHANGED build finalized the epoch and returned normal success over a preserved-but-stale row. It now captures the result and returns `_build_failed_result` on `drift_clear_failed` BEFORE epoch finalization; ordinary drift COMPUTATION failures (`drift_detect_failed`/`git_probe_failed`) stay optional per the resident policy. Evidence: `test_indexer.NoOpDriftClearFailureBuildTests.test_changed_build_clear_failure_fails_and_retry_recovers` (real changed build fails + preserves + retry clears/finalizes) + `test_ordinary_drift_compute_failure_stays_optional` (contract guard).
  `wave-council-delivery` remains WITHHELD pending the reviewer's independent re-verification; no operator signoff is inferred and no close action is taken.
- **Round-4 re-review (3) blockers REMEDIATED — 2026-07-14.** The re-review verified the re-review-(2) repairs but found two remaining P1s in the git→non-git handling; both fixed with regression coverage; full suite **5,402 tests across 48 files, OK** bytecode-free, docs-lint clean, `git diff --check` clean. Per finding:
  1. **P1 no-op drift-clear failure reported success → FIXED.** The no-op build path discarded `reconcile_non_git_drift`'s result and swallowed exceptions with a blanket `except: pass`, then returned `up_to_date: True` over an un-cleared stale `drifted: true` row. It now CAPTURES the result and returns `_build_failed_result` on `drift_clear_failed` / `error` / exception (the blanket swallow is gone), so the stale drift is never served behind a clean result and the retry re-attempts the clear. Evidence: `test_indexer.NoOpDriftClearFailureBuildTests.test_noop_clear_failure_fails_build_and_retry_recovers` (REAL `build_index` no-op: build fails, drift preserved, retry clears) + `test_noop_clear_success_reports_up_to_date` (no spurious failure on the happy path).
  2. **P1 ambiguous git failures classified as confirmed non-git → FIXED.** `_git_authority` treated EVERY nonzero `--is-inside-work-tree` as `confirmed_non_git`, so `fatal: detected dubious ownership` (and permission / corrupt-repo / bad-config errors) authorized a destructive clear. It now returns `confirmed_non_git` ONLY for the POSITIVE "`not a git repository`" fatal (matched under a forced `LC_ALL=C` locale for determinism); every other completed-but-failing invocation → `probe_failed` → PRESERVE. Evidence: `test_doc_drift.GitAuthorityTypedStateTests` — dubious-ownership / permission / corrupt / unexpected-nonzero all `probe_failed`, positive "not a repository" `confirmed_non_git`, plus end-to-end preservation on BOTH the build-tail and zero-change reconcile paths under dubious ownership.
  `wave-council-delivery` remains WITHHELD pending the reviewer's independent re-verification; no operator signoff is inferred and no close action is taken.
- **Round-4 re-review (2) blockers REMEDIATED — 2026-07-14.** The fresh red-team/QA/architecture pass found four deeper P1 gaps beyond the first re-review remediation; all fixed at their chokepoints with regression coverage; full suite **5,393 tests across 48 files, OK** bytecode-free, docs-lint clean, `git diff --check` clean. Per finding:
  1. **P1 fully-wedged invalidation still "succeeded" → FIXED with a structured build failure before bookkeeping.** `memory_invalidate` now returns True ONLY when the generation DURABLY advances (a raw content edit does not move `dir_mtime`, so a non-advancing generation is not real invalidation). On failure it sets a best-effort short-lived fence token and returns False, and the indexer returns `_build_failed_result` BEFORE recording file metadata — so the edited record's old file_meta is preserved and the recovered retry re-detects the edit and advances the generation (a "clean" build can no longer strand a warm reader, and the retry no longer returns up_to_date over the un-invalidated edit). Evidence: `test_indexer.MemoryInvalidationBuildTailTests.test_advance_failure_fails_build_before_bookkeeping` (real `build_index`: build fails, file_meta preserved, recovery advances).
  2. **P1 concurrent writers cleared each other's fence → FIXED with writer-owned tokens.** The single shared `dirty` flag is replaced by a `memory_writers` table of per-writer tokens: `memory_fence` registers a unique token; `memory_finalize` deletes ONLY that token; readers bypass while any *live* token exists (`read_memory_state` synthesizes `dirty` from the live-token count, so the reader key is unchanged). A crashed writer's token is TTL-bounded (300s) with lazy reap. Evidence: `test_memory_records.WriterTokenInterleaveTests` (A fence→B fence→A finalize leaves B's fence live; per-finalize generation; TTL self-heal; failed-finalize keeps only its own token live).
  3. **P1 git→non-git handling incomplete → FIXED with typed authority + no-op reconcile + fail-closed clear.** A typed `_git_authority` (`git` / `confirmed_non_git` / `probe_failed`) replaces the empty-string conflation: a probe timeout/exception/missing-git now PRESERVES last-good drift (was destructively clearing it), an unborn-HEAD repo is still git (no clear), and only a CONFIRMED non-git transition clears. The no-op build path also reconciles (`reconcile_non_git_drift`, gated on the cheap `has_drift_state` so a normal git repo pays no probe on a zero-change build), closing the unchanged-copied-index gap. A failed clear on a confirmed transition surfaces `drift_clear_failed` (fail-closed, retried) instead of a silent clean skip. Evidence: `test_doc_drift.GitAuthorityTypedStateTests` (states + probe-failure preserves + clear-failure fail-closed), `NoOpBuildDriftReconcileTests` (no-op reconcile + real-git preserve).
  4. **P1 evidence reconciliation → CLOSED.** Reopened ACs/tasks updated to the implemented contracts (`1p8gy` AC-5/AC-9 + invalidation task; `1ro43` AC-9). The `TwoProcessCacheCoherenceTests` claim is corrected (two independently loaded module instances in one interpreter) and a genuine separate-OS-process probe added (`RealChildProcessCoherenceTests` spawns a child interpreter that reads the shared seqlock and observes fence/clear). `data-and-control-flow.md` extended with the writer-token model and typed git authority.
  `wave-council-delivery` remains WITHHELD pending the reviewer's independent re-verification; no operator signoff is inferred and no close action is taken.
- **Round-4 re-review blockers REMEDIATED — 2026-07-14.** The three remaining P1 items from the re-verification below (two correctness defects + one evidence gap) are fixed at their chokepoints with regression coverage; full suite **5,380 tests across 48 files, OK** bytecode-free, docs-lint clean, `git diff --check` clean. Per finding:
  1. **P1 ignored `memory_advance` failure → FIXED.** The indexer no longer swallows the invalidation failure. A new `index_state_store.memory_invalidate` is the checked, typed entry point: it advances the generation and, when that FAILS, falls closed by setting `dirty` (`memory_fence`) so every process bypasses the cache regardless of the unchanged `(epoch, generation, dir_mtime)` key — a raw content edit does not change `dir_mtime`, so a swallowed failure is exactly what let a second warm process keep serving the pre-edit advisory. `memory_invalidate` returns `False` only when the store is so wedged that neither advance nor fence succeeds, and the indexer then warns loudly instead of reporting a silent clean build. Evidence: `test_indexer.MemoryInvalidationBuildTailTests.test_advance_failure_falls_closed_to_dirty` (real build), `test_memory_records.TwoProcessCacheCoherenceTests.test_indexer_invalidate_fallback_reaches_second_process`.
  2. **P1 Git→non-Git transition served stale drift → FIXED.** `update_drift_from_build`'s non-git branch returned before opening the store, so a git-built index copied into (or a repo that dropped git under) a non-git root kept serving the old `drifted: true` rows. It now transactionally CLEARS the git-derived attribution + drift rows + fingerprint via the new `IndexStateStore.clear_attribution_and_drift` (idempotent — reports `cleared_git_derived` only when something was removed, so it does not re-clear every non-git build; only touches the store if it already exists, never materializing it for a fresh non-git project). Evidence: `test_doc_drift.NonGitProjectTests.test_git_to_non_git_transition_clears_stale_drift` + `test_non_git_clear_is_idempotent_no_repeat_work`.
  3. **P1 evidence gap → CLOSED.** The simulated "second process" and helper/manual invalidation tests are replaced with genuine integration coverage: `TwoProcessCacheCoherenceTests` loads TWO independently loaded `server_impl` instances (distinct `_MEMORY_RECORDS_CACHE` + distinct `index_state_store`) sharing only the on-disk seqlock, warms BOTH caches, and proves the second (never-cleared) process bypasses after a failed finalize and reloads after a normal generation advance; `MemoryInvalidationBuildTailTests` drives the real `build_index` add / edit / delete / no-op / unrelated / README matrix plus the forced late-build-failure and forced advance-failure (fail-closed) paths. `data-and-control-flow.md` extended with the `memory_invalidate` fail-closed fallback and the non-git drift-clear.
  `wave-council-delivery` remains WITHHELD pending the reviewer's independent re-verification; no operator signoff is inferred and no close action is taken.
- **Round-numbering reconciliation (per the round-4 P2 record correction).** Prior checkpoints double-labelled cycles as "round 3". Canonical sequence: R1 = the `../` memory-id escape; R2 = the 9-finding set; R3 = the 7-finding set; **R4 = the 6-finding set** (the "round 3 findings REMEDIATED" entry below is R4's initial remediation, withdrawn by the round-4 re-review); the current 4-finding council block is R4's re-review, and the remediation immediately below is R4's final fix. Test/handoff counts in earlier entries are point-in-time; the current authoritative suite count is stated in the remediation below.
- **Delivery re-verification after round-4 remediation — 2026-07-14: BLOCKED.** The four reported repairs were re-read against source and replayed with executable probes. The durable seqlock itself is correct: two independently loaded server modules with distinct warm caches observed `dirty=1` after a forced finalize failure, and the non-mutating process discarded its stale candidate. The early-placement control is also correct on the ordinary path: a real memory-file edit advanced the generation before a forced late bookkeeping failure and the warm advisory reloaded the edited record. Gardener sentinel/diff/header truncations and strict history framing passed 20 focused tests with zero skips. Three blockers remain:
  1. **P1 — the indexer silently ignores failure of the early invalidation write.** `_build_index_locked` calls `memory_advance`, ignores its `None` result, swallows exceptions, and continues the build. Executed reproduction: warm an `AAAA` advisory in another server cache; raw-edit the record to `BBBB`; force `memory_advance` to return `None`; the build reports success, `generation` remains unchanged, and the other server still serves `AAAA`. The early signal needs a checked/typed success contract and a cross-process fail-closed outcome when SQLite cannot advance (for example an independently verified O(1) invalidation signal already included in the reader key); a diagnostic or build failure alone is insufficient if warm readers remain authoritative. Fixture the exact independent-cache failure path.
  2. **P1 — Git-to-non-Git transition serves stale derived drift.** `update_drift_from_build` says it will "leave the tables empty" when `_git_head` is absent, but it returns before opening/replacing the store. Executed reproduction: derive `docs/g.md` as `drifted: true, commits_since: 3`, remove Git metadata, run the non-Git path, and read the same stale row back unchanged. A project with no current VCS authority must transactionally clear the Git-derived attribution/drift/fingerprint or mark that state unavailable and suppress it at every reader; do not preserve old Git claims while reporting a clean non-Git skip. Add a Git→non-Git (or copied-index-into-non-Git-root) fixture, not only a fresh empty-store fixture.
  3. **P1 evidence gap — the required regression matrix is still structural/manual.** `SeqlockCoherenceTests` and `CacheFailureCorrectnessTests` clear the same module's cache and call that a second process; they do not keep a second independent cache warm across the mutation. `MemoryGenerationGateTests` exercises only `_memory_record_touched`; `test_indexer_style_bump_invalidates_after_raw_edit` manually invokes `memory_advance`. None executes the build-tail add/edit/delete/no-op/unrelated/README matrix or the forced-late-failure path required by the preceding block. Replace the simulated claims with two independently loaded server instances and real `build_index` integration cases; keep helper tests only as unit-level complements. The review's ad-hoc controls prove the implementation's happy paths, but they are not durable regression evidence.
  Documentation repair verified: `data-and-control-flow.md` now records the dedicated store, keys, writers, reader key, and dirty/unreadable semantics. `wave-council-delivery` remains WITHHELD; no operator signoff is inferred.
- **Delivery re-review round 4 findings REMEDIATED — 2026-07-14.** The four R4 P1 blockers validated by reproduction, fixed with regression coverage; full suite **5,367 tests across 48 files, OK** bytecode-free, docs-lint clean, `git diff --check` clean, live index rebuilt; non-git-project support explicitly re-verified. Per finding:
  1. **P1 cross-process cache coherence + ABA → FIXED with a durable seqlock.** In-process eviction cannot coordinate a second MCP process when the durable write fails, so invalidation is now a DURABLE SEQLOCK in the dedicated `memory-state.sqlite`: a random `epoch` (minted once at creation — defeats the delete/recreate ABA), a monotonic `generation`, and a `dirty` flag. A tool mutation FENCES (`memory_fence` sets dirty) BEFORE the filesystem write and only clears it on `memory_finalize`; if finalize fails, dirty stays set so EVERY process bypasses the cache (no stale serve), and a mutation that cannot establish the fence is REFUSED. Readers key on `(epoch, generation, dir_mtime)` and bypass while dirty/unreadable. Evidence: `SeqlockCoherenceTests` (failed-finalize → dirty → cross-process bypass; random-epoch defeats delete/recreate ABA; fence refusal blocks the write), updated `GenerationCacheTests`/`CacheFailureCorrectnessTests`.
  2. **P1 raw-edit invalidation independent of semantic-build success → FIXED.** The indexer now calls `memory_advance` immediately after the changed/removed path sets are known — before any optional Lance / FTS / freshness / drift work — so an earlier build failure cannot leave a raw-edited record's advisory stale. Gated on actual changed/removed memory paths (`_memory_record_touched`). Evidence: `MemoryGenerationGateTests` (gate) + the early-placement in `indexer.py`.
  3. **P1 gardener truncation frames → FIXED.** Pass-1 now validates complete commit/file/hunk frames (`--no-merges` guarantees every listed commit carries a diff), so sentinel-only / diff-only / header-only truncated streams return `ok=False`. Evidence: `GardenerFrameCompletenessTests`.
  4. **P1 history strict framing → FIXED.** `_collect_git_history` rejects orphan pre-sentinel content, invalid timestamps (no longer coerced to 0), and duplicate SHAs; each malformed exit-0 shape preserves last-good drift + attribution. Evidence: `HistoryStrictFramingTests` (per-shape + end-to-end last-good preservation).
  Also: `data-and-control-flow.md` now documents `memory-state.sqlite` ownership/writers/failure semantics; non-git projects verified to SKIP drift cleanly with the memory seqlock still functional (`NonGitProjectTests`). `wave-council-delivery` remains WITHHELD pending the reviewer's independent re-verification.
- **Delivery re-review round 4 — 2026-07-14: BLOCKED.** A full-depth independent council re-ran the six claimed repairs against current source and executed adjacent failure probes. The per-record symlink repair, normal atomic generation increments, canonical-store isolation, metadata-scoped gardener comparison, explicit dual-walk failure preservation, and changed/removed-only helper all verified. The mid-review replacement of per-candidate `git show` calls with one `git cat-file --batch` subprocess closes the reported scaling concern on the current tree. Four P1 correctness/reliability blockers remain, so the remediation checkpoint below is withdrawn as a delivery verdict: (1) a successful memory reconcile followed by a failed durable generation bump evicts only the mutating process; a second MCP process retains the same `(generation, dir_mtime)` key and continues serving an `active` advisory after disk status is `stale`; delete/recreate of `memory-state.sqlite` also recreates generation `1` and exposes an ABA because the key has no store epoch; (2) raw-edit invalidation is delayed until the end of the semantic build, so an earlier structured build failure can leave direct memory advisories stale even though the record file changed; (3) `_gardener_only_pairs` accepts exit-0 sentinel-only, diff-only, and header-only truncated streams as `(True, set())`, allowing a gardener commit to become the false material anchor again; (4) `_collect_git_history` accepts orphan pre-sentinel content, invalid timestamps coerced to zero, duplicate SHAs, and error text as filenames, then authorizes replacement of last-good drift/attribution state. Required evidence: a genuine two-independent-cache failure fixture and reset-ABA fixture; invalidation independent of later semantic-build success; complete patch-frame fixtures; strict history-record framing fixtures plus end-to-end last-good preservation; and build-tail integration coverage for no-op/unrelated/README/add/edit/delete. P2 record corrections: this six-finding set is round 4, not a second round 3; the handoff/test counts and dedicated-store description are stale; `data-and-control-flow.md` omits `memory-state.sqlite` ownership/writers/failure semantics. Council seats: red-team primer, code-reviewer, qa-reviewer, security/reliability reviewer, architecture/performance reviewer, and docs-contract/reality reviewer. Seat agreement: unanimous BLOCK; strongest challenge: local cache eviction cannot provide cross-process coherence when the only durable invalidation write fails; strongest alternative: a durable memory seqlock with a random store epoch plus dirty/clean sequence, fenced before filesystem mutation and read as bypass while dirty, with raw-edit invalidation established before optional semantic-build work. No operator signoff is inferred.
- **Self-directed adversarial pass — 2026-07-14 (operator-requested; findings fixed).** Four parallel adversarial reviewers ran executed reproductions against the newest/riskiest code. The core held under attack: the ancestry position-pruning is structurally self-defending (could not be falsely tripped), the fail-closed git walks, delimiter-injection resistance, and the entire cache/generation/symlink core (cross-process bump atomicity exact at 400; store decoupling; corrupt-store bypass) all survived. They confirmed a further set of defects, now fixed with regression coverage; full suite **5,352 tests across 48 files, OK** bytecode-free, docs-lint clean, `git diff --check` clean, live index rebuilt. Findings:
  1. **P2 merge-DAG attribution — a ref changed only inside a merge commit's own tree was invisible** (`git log --name-only` omits merge diffs), mis-counting drift both directions. FIXED: added `-c` so the merge's own (evil-merge/conflict) changes are attributed under `--name-only`; a clean merge lists nothing (no over-count). Evidence: `EvilMergeAttributionTests` — matches `git rev-list --count anchor..HEAD` (1) where the old code returned 0, and clean-merge does not double-count.
  2. **MEDIUM (unsafe direction) parser/lint bullet parity — a lint-invalid `*`-bulleted record parsed valid and surfaced as an advisory.** FIXED: `_section_has_bullet` now character-mirrors the lint (`line.lstrip().startswith("- ")`), rejecting `*` and tab-after-dash. Also aligned the two safe-direction value grammars: confidence (`\S+`+float, so lint-valid `1e-1`/`+0.5` parse) and Created/Updated (lint now calendar-validates via `date.fromisoformat`, so `2020-13-40` fails both). Evidence: `ParserLintValueParityTests`, `MemoryRecordValueParityLintTests`.
  3. **MEDIUM torn write — the two drift replaces were separate transactions**, so a crash between left attribution advanced while drift/fingerprint stayed stale. FIXED: `replace_attribution_and_drift` writes attribution + drift + fingerprint in ONE transaction. Evidence: `DriftTornWriteTests` — a fault mid-write rolls back all of it.
  4. **MEDIUM gardener confirm fail-open + LOW DoS — the parent `git show` swallowed real errors as empty (false gardener-only on a material commit), and spawned 2 subprocesses per candidate.** FIXED together by replacing per-candidate `git show` with ONE `git cat-file --batch`: a real git error → None → fail closed (preserve prior drift); a `missing` object → legitimate absence; and the whole confirmation is one subprocess regardless of candidate count. Added a pass-1 truncation guard (non-empty output with no commit sentinel → `ok=False`). Evidence: `GardenerBatchAndFailClosedTests`.
  5. **LOW heading-less normalization — `_strip_gardener_field` treated a heading-less doc's whole body as header**, so a body date edit could be misclassified. FIXED: scoped to the leading metadata frontmatter block (stops at the first non-metadata line), so a body/fenced date stays material. Evidence: `GardenerBatchAndFailClosedTests.test_heading_less_body_date_edit_is_material`.
  6. **LOW reader/lint nesting parity — a lint-valid record in a real nested subdir never surfaced** (`parent != resolved_root` dropped it). FIXED: `is_relative_to(resolved_root)` accepts nested records while still blocking symlinks/escapes. Evidence: `NestedRecordTests`.
  Hardening also applied: the drift skip-fingerprint is now hash-prefixed (no crafted path/body boundary can alias two doc sets); the `_collect_git_history` empty-repo docstring corrected. Known non-issue documented: an out-of-band raw in-place record edit (no tool call, no build) can serve a stale cached advisory until the next build — outside the sanctioned-mutation contract. `wave-council-delivery` remains WITHHELD pending the reviewer's independent re-verification.
- **Reviewer severity correction — 2026-07-13.** Round 3 finding 1 reproduced a real external-read/advisory-injection behavior, but its original **P0** classification overstated the threat under Wavefoundry's local, trusted-repository model. The remaining case is read-only and requires an actor already able to modify the repository filesystem; unlike the already-closed memory-root/write escapes, it grants no new mutation authority outside the configured root. Memory has greater ambient influence than plans/waves because its records are attached automatically to unrelated tool and lifecycle responses, but plans and waves also follow filesystem symlinks when enumerating and reading their Markdown, so the current product does not enforce a universal symlink-proof read sandbox. Finding 1 is therefore **P2 defense in depth and non-blocking for this release**. The proportionate repair is a small per-candidate regular-file/containment check in the memory loader, with focused file-symlink coverage; do not expand `1ro44` into a repository-wide symlink audit. A universal read-containment policy, if desired, belongs in a separate cross-surface change.
- **Delivery re-review round 3 findings REMEDIATED — 2026-07-14.** All 6 findings (1 downgraded-P2, 3 P1, 2 P2) validated by reproduction, fixed with regression coverage; full suite **5,266 tests across 48 files, OK** bytecode-free, docs-lint clean, `git diff --check` clean, live index rebuilt. Per finding:
  1. **P2 defense-in-depth — per-record symlink read → FIXED.** `load_memory_records` now applies a per-candidate check (skip symlinked entries and any whose resolved parent leaves the canonical memory root), matching the reviewer-scoped repair. Evidence: `PerRecordSymlinkTests` — a symlinked `mem-ext.md` is excluded from both load and the advisory path while an in-repo record surfaces.
  2. **P1 cache failure aliasing → FIXED.** `read_memory_generation` returns typed `Optional[int]` (None ⇒ the cache is BYPASSED, never a bogus 0); `bump_memory_generation` is an atomic `INSERT … ON CONFLICT … value+1` in its OWN dedicated `memory-state.sqlite` (never `ensure_current`/reset of the canonical index-state store — reconciled with the 1sed7 writer-lock/reset census); and `_bump_memory_generation` UNCONDITIONALLY evicts the in-process repo cache after every mutation. Evidence: `CacheFailureCorrectnessTests` — a forced-failed bump after reconcile no longer serves the stale candidate, unreadable generation bypasses caching, concurrent increments are atomic, and a bump touches no canonical store.
  3. **P1 gardener parse-failure + line-shape classification → FIXED.** `_gardener_only_pairs` validates the `-p` patch structure (commit/file/hunk framing; malformed ⇒ `ok=False`) and CONFIRMS each candidate by comparing metadata-scoped normalized content at the commit vs its first parent — content, not line shape — so a fenced/body exact-date edit stays material. Evidence: `GardenerContentClassificationTests` (body-date edit material; header bump gardener) + `GitWalkFailClosedTests.test_malformed_gardener_patch_is_not_ok`.
  4. **P1 primary history walk fails open → FIXED.** `_collect_git_history` returns typed `(ok, commits)` with 40-hex SHA/parent validation and garbage/truncation detection; `update_drift_from_build` requires BOTH git walks to succeed before replacing attribution/drift/fingerprint, else preserves prior state. Evidence: `GitWalkFailClosedTests` — typed result, malformed output ⇒ `ok=False`, forced history failure preserves `commits_since: 3`.
  5. **P2 over-invalidation → FIXED.** The indexer bump is gated on the changed/removed sets (never all indexed files) via the testable `_memory_record_touched` helper; the schema README is ignored. Evidence: `MemoryGenerationGateTests`.
  6. **P2 evidence drift → FIXED.** `1ro43` AC-2 now says the churn range is git ANCESTRY (not position); the round-2 checkpoint's severity miscount and overstated parse-failure claim are corrected in the note above.
  `wave-council-delivery` remains WITHHELD pending the reviewer's independent re-verification.
- **Delivery re-review round 3 — 2026-07-13: BLOCKED.** Independent probes confirm the round-2 root/ancestor containment, parser/lint cases, linear + sibling-merge ancestry case, successful generation invalidation, task closure, and prior three closed repairs. Focused suites pass (`test_memory_records`: 53; `test_doc_drift`: 39). Three correctness blockers and two required evidence/efficiency repairs remain; one additional symlink hardening item is non-blocking defense in depth. The blanket round-3 remediation claim below is withdrawn pending the blocking repairs:
  1. **P2 defense in depth (non-blocking) — per-record symlinks can cross the read boundary.** `canonical_memory_root` proves only the directory path. `load_memory_records` then `rglob`s and reads each entry without resolving/containing the individual record. An executed fixture placed `docs/agents/memory/mem-ext.md` as a symlink to a valid external record; both `load_memory_records` and `_memory_advisories_for_path` returned its external summary. If repaired in this wave, keep the scope to rejecting non-regular/symlinked candidates or requiring the resolved candidate to remain under the canonical memory root, plus focused loader/advisory coverage. This finding does not block delivery and does not require hardening plans, waves, indexing, or every repository reader in `1ro44`.
  2. **P1 cache correctness — generation failure still aliases distinct states.** `bump_memory_generation` and `_bump_memory_generation` swallow every failure, while `_memory_cache_key` treats unreadable/absent generation as valid `0`. An executed tool-path fixture warmed an active advisory, forced the persisted bump to fail, reconciled the record to `stale`, and still received the cached pre-write candidate advisory. Make generation increment atomic and return a typed success result; evict the in-process repo cache unconditionally after a successful file mutation, and when durable generation cannot be read/advanced, bypass caching rather than using `0`. Reconcile this new SQLite writer with 1sed7's writer-lock/reset census so `ensure_current` cannot reset or mutate canonical state outside the agreed lifecycle fence. Fixture bump failure, read failure, concurrent increments, and cross-process reconciliation.
  3. **P1 drift correctness — gardener “parse failure” is still reported as success.** `_gardener_only_pairs` returns `(True, set())` for successful exit with malformed/truncated stdout; no parse validation exists despite the remediation evidence claiming parse failures fail closed. It also classifies any changed line exactly shaped `Last verified: YYYY-MM-DD` as gardener-only regardless of Markdown position: an executed patch changing only a fenced/body date line returned a gardener-only pair. Validate commit/file/hunk structure and classify by comparing metadata-scoped normalized old/new document content, not line shape alone; malformed/truncated output must return `ok=False`. Fixture exit-0 garbage/truncation and fenced/body exact-date edits.
  4. **P1 drift correctness — the parent/name history subprocess still fails open.** `_collect_git_history` returns `[]` for git failure and does not validate `%P` parse completeness. `update_drift_from_build` treats that as a valid empty history and replaces healthy rows/fingerprint. Executed fixture: healthy `commits_since: 3`; one new HEAD commit; forced empty history; build reported `written: 1` and republished `commits_since: 0`. Give the history walk a typed success/failure result, validate SHA/parent records, and preserve prior attribution + drift + fingerprint unless BOTH git walks succeed. Fixture nonzero, timeout, exit-0 malformed parent output, and empty repository as a distinct valid state.
  5. **P2 efficiency — the indexer invalidates memory on every build once any record exists.** The bump condition tests all `current_file_meta`, not the changed/removed path sets, so unrelated and zero-change builds advance the generation and force an avoidable reparse. Gate the bump on actual changed/removed memory paths (while retaining add/edit/delete coverage) and pin true-no-op behavior.
  6. **P2 docs/evidence — closure prose still overstates the repaired contracts.** The remediation checkpoint says parse failures are handled when they are not; `1ro43` AC-2 still says topology is counted by git position, and the checkpoint classification says “2 P0-adjacent, 6 P1, 1 P2” for seven numbered blockers. Correct these claims after code repair so the delivery record describes the implemented contracts exactly.
  Review-lane disposition: security-reviewer is **not blocked** by the downgraded finding 1; code/QA remain blocked by 2–4; performance/reliability remain blocked by 2 and 5; docs-contract remains blocked by 6. `wave-council-delivery` remains withheld and no operator signoff is inferred.
- **Correction (2026-07-14, per round-3 finding 6 and round-4 finding 6):** the round-2 remediation checkpoint below (a) miscounted the seven numbered blockers as "2 P0-adjacent, 6 P1, 1 P2" — the accurate tally is **2 P0, 4 P1, 1 P2** (findings 1 and 4 were P0; 2, 3, 5, 6 were P1; 7 was P2-doc); and (b) its finding-3 line claimed a "parse failure" path that did not exist until round 4. The *subprocess-error/timeout* branch of `_gardener_only_pairs` did return `ok=False` as stated, but true output-parse/malformed detection and content-based (not line-shape) classification landed only in the round-4 remediation above. Both the count and the parse-failure claim are corrected here rather than by rewriting the historical entry.
- **Delivery re-review round 2 findings REMEDIATED — 2026-07-14.** The seven numbered round-2 blockers (accurate severities: 2 P0, 4 P1, 1 P2 — see the correction note above) validated by reproduction, then fixed with regression coverage; full suite after remediation **5,254 tests across 48 files, OK** bytecode-free, docs-lint clean, `git diff --check` clean, live index rebuilt. Per finding:
  1. **P0 symlink boundary (reads + mkdir-order) → FIXED.** Single containment chokepoint `canonical_memory_root(root)` proves the memory root resolves to its canonical in-repo location; ALL read paths (`load_memory_records`, the advisory cache key) and write paths route through it, and `write_memory_record` validates containment BEFORE `mkdir` (parent-only mkdir, after the check). Evidence: `SymlinkReadAndMkdirTests` — a symlinked memory dir surfaces zero records via load AND `_memory_advisories_for_path`; a symlinked `docs/agents` ancestor refuses the write with no external `memory/` created.
  2. **P1 parser/lint disagreement → FIXED.** `parse_memory_record` now mirrors the load-bearing lint rules: a `superseded` record without `Superseded by:` and an Evidence/Targets section with backticks-but-no-bullets both parse to None. Evidence: `ParserLintParityTests`.
  3. **P1 gardener detector fails open → FIXED.** `_gardener_only_pairs` returns `(ok, pairs)`; on subprocess non-zero/timeout/parse failure `update_drift_from_build` preserves the prior drift rows + fingerprint (no write) and reports a non-fatal `gardener_detect_failed` diagnostic, so a classifier error can never republish the reset. Evidence: `GardenerDetectorFailClosedTests` — forced failure after a committed gardener bump keeps `commits_since: 3` and the fingerprint byte-identical.
  4. **P1 normalization overbroad → FIXED.** Normalization/classification is scoped to the canonical header `Last verified: <date>` line only (`_GARDENER_DATE_LINE_RE`, before the first `## `). A body/fenced `Last verified: see \`src/a.py\`` is real content — a change to it invalidates the skip and is material. Evidence: `HeaderScopedNormalizationTests`.
  5. **P1 position is not ancestry → FIXED.** `_commits_after` counts commits touching a ref in the `anchor..HEAD` ANCESTRY range (reachable from HEAD, not from the anchor) via the `%P` parent graph, matching `git rev-list`; `waves_behind` uses landing-commit reachability. Evidence: `MergeDagAncestryTests` — a merged sibling commit counts as 1 (== `git rev-list --count anchor..HEAD -- src/a.py`), was 0 under log-position.
  6. **P1 cache O(N)/collision → FIXED.** The advisory cache keys on `(persisted memory generation, dir mtime_ns)` — the monotonic generation is an O(1) meta read bumped by every sanctioned mutation (the `wave_memory_*` tools and the indexer's memory pass), so no per-call tree walk and no aliasing of distinct states. Evidence: `GenerationCacheTests` — warm calls do not re-parse, a same-size reconcile advances the generation and changes the key (no alias), an indexer-style bump invalidates despite a restored mtime.
  7. **P1 lifecycle/docs contract → FIXED.** The `1p8gy` centrality-ranking task is checked with evidence; AC-5's stale "directory-scan cost" wording is replaced with the warm-cache contract (generation-keyed cache, O(1) per hot call). No unchecked `[ ]` remains on either change doc.
  `wave-council-delivery` remains WITHHELD pending the reviewer's independent re-verification.
- **Delivery re-review round 2 — 2026-07-13: BLOCKED.** Independent re-verification confirms three original repairs fully closed: exclusive generated/explicit creation behavior, all-persisted-field secret scanning with non-echoing diagnostics, and exact lifecycle-id matching. The focused suites are green (`test_memory_records`: 45, `test_doc_drift`: 35, `test_docs_lint`: 667), but executed adjacent probes found the following unresolved blockers; the prior remediation checkpoint's blanket “all 9 fixed” claim is withdrawn pending repair:
  1. **P0 security — the symlink trust boundary protects writes only, and even writes mutate before validation.** `load_memory_records` and `_memory_dir_signature` still traverse a symlinked `docs/agents/memory`; an executed fixture placed a valid record outside the repo and `_memory_advisories_for_path` surfaced its external summary. Separately, `write_memory_record` calls `mkdir` before `_contained_record_path`; with `docs/agents` symlinked outside and no `memory` child, the refused write still created `outside/memory`. Centralize a read/write memory-root resolver that proves canonical in-repo containment before traversal or mutation; make every loader/cache/search/advisory/add/reconcile path use it; move `mkdir` after parent containment is proven; fixture external-read refusal and symlinked-ancestor no-side-effects.
  2. **P1 schema/output integrity — runtime parsing still does not enforce the lint schema.** A `superseded` record without `Superseded by:` parses successfully, and an active record whose Evidence/Targets contain backticks but no bullets also parses and can surface, although both fail `check_memory_docs`. Make the runtime validator share or exactly mirror every load-bearing schema rule (including status-dependent links and section bullet/ref shape), then add parity fixtures asserting every lint-invalid record is reader-invalid.
  3. **P1 drift reliability — patch-classification failure republishes the original P0 defect.** `_gardener_only_pairs` returns the same empty set for “success, no gardener-only commits” and subprocess error/timeout. On failure, `compute_doc_drift` treats every naming commit as material; the exact committed-gardener history therefore returns `commits_since: 0` again and can overwrite healthy stored drift. Return a typed success/failure result and, on classification failure, leave the prior drift rows/fingerprint intact while reporting the non-fatal build-tail diagnostic; fixture nonzero exit, timeout, and parse failure.
  4. **P1 drift correctness — gardener normalization is not metadata-scoped.** `_GARDENER_FIELD_RE` removes every line beginning `Last verified:` anywhere in Markdown, including fenced examples/body content. Changing a body example from ``Last verified: see `src/a.py` `` to `src/b.py` produces an identical normalized digest, so the uncommitted-reference edit can still be skipped; a committed body-only edit can also be misclassified gardener-only. Normalize/classify only the canonical header metadata occurrence and fixture fenced/body lookalikes.
  5. **P1 drift correctness — git-log position is not ancestry in a merge DAG.** The linear clock-skew fixture is closed, but sibling branches have arbitrary relative positions. Executed merge fixture: doc anchor on the first parent, code change on the merged sibling; `git rev-list <anchor>..HEAD -- src/a.py` reports 1 while `compute_doc_drift` reports 0 because the sibling appears after the anchor in log position. Count commits in the anchor-to-HEAD ancestry range (and define equivalent landing reachability for `waves_behind`), with merge/sibling fixtures in addition to the linear skew cases.
  6. **P1 cache correctness/performance — the hot path is still O(record count) and its aggregate signature collides.** Every warm advisory call recursively stats every memory file to recompute `(count, max_mtime_ns, total_size)`; an executed 50-record fixture made 50 stats on both cold and warm calls. A same-length record edit with restored mtime produces the same aggregate signature and returns stale cached content (`AAAA` after disk changed to `BBBB`). Use a bounded state key that does not require a full tree walk per hot call and cannot alias distinct record states (for example a persisted memory generation/manifest integrated with the existing index/cache invalidation path); test warm-call filesystem-operation counts at scale plus same-size/mtime and delete/replace collisions.
  7. **P1 lifecycle/docs contract — an implementation task remains open.** `1p8gy` still has `[ ] Implement centrality-weighted advisory ranking ...` even though its ACs and wave status claim implementation complete; AC-5 also records “directory-scan cost,” contradicting Requirement 6's warm-cache/no-meaningful-latency contract. Complete and evidence the task (or mark it `[~]` with an honest scope decision), then reconcile AC/task evidence after the cache repair. This is a hard close-time checkbox blocker independently of code correctness.
  Review-lane disposition: security-reviewer blocked by 1; code/QA blocked by 2–6; performance/architecture blocked by 6; docs-contract and lifecycle readiness blocked by 7. `wave-council-delivery` remains withheld and no operator signoff is inferred.
- **Delivery re-review findings REMEDIATED — 2026-07-14.** All 9 findings (2 P0, 6 P1, 1 P2) validated with executed probes, then fixed at their chokepoints with regression coverage; full suite after remediation **5,242 tests across 48 files, OK** bytecode-free (+96 tests), docs-lint clean, `git diff --check` clean. Per finding:
  1. **P0 symlink escape → FIXED.** `_contained_record_path` now asserts the memory root's own resolved location equals its canonical in-repo path (`root.resolve()/docs/agents/memory`), so a symlinked memory directory or ancestor pointing outside the repo is rejected before any write; the record path is additionally required to be `is_relative_to` the resolved repo. Evidence: `SymlinkContainmentTests` — symlinked-root add/reconcile refused with the external target untouched, plus a normal in-repo control.
  2. **P1 malformed records surface → FIXED.** `parse_memory_record` now FAILS CLOSED — id/stem agreement, valid status enum, in-range confidence, well-formed created/updated dates, and non-empty summary/evidence/targets are all required or the record parses to None; `check_memory_docs` now requires `Status:` and a non-empty `## Summary`. Evidence: `FailClosedParseTests` (8 malformed shapes → None; malformed record never surfaces via load), `MemoryRecordSchemaCompletenessTests` (lint fails missing-status / empty-summary).
  3. **P1 creation TOCTOU → FIXED.** `write_memory_record` uses exclusive `open(..., "x")`; new `create_memory_record` retries generated-id collisions atomically and preserves the conflict for explicit ids. Evidence: `CreationAtomicityTests` — 8-thread barrier concurrent create yields 8 distinct files with no lost update; explicit-id collision raises; generated-id retry produces `-2`.
  4. **P0 gardener-only anchor → FIXED.** `compute_doc_drift` derives the last MATERIAL content commit via a batched `git log -p -U0` gardener-only detector (`_gardener_only_pairs`); a committed `Last verified:` bump no longer resets the clock. Evidence: `GardenerAnchorTests` — committed gardener bump after 3 code commits returns `commits_since: 3` (was 0); a material edit alongside the stamp still anchors.
  5. **P1 stale drift fingerprint → FIXED.** The skip key now includes a per-doc normalized-content digest (`Last verified:` stripped), so uncommitted reference add/remove/change at stable HEAD invalidates the skip while mechanical stamp edits do not thrash it. Evidence: `DriftFingerprintContentTests` — changed/added/removed refs each force recompute; gardener-only working-tree edit still skips.
  6. **P1 timestamps-as-topology → FIXED.** `_commits_after` and `waves_behind` now count by git-log POSITION (ancestry order), never `%ct`. Evidence: `TopologyNotTimestampTests` — clock-skewed churn (topologically later, timestamped earlier) counts for both a living doc and a historical waves-behind case (was 0).
  7. **P1 secret scan incomplete + echo → FIXED.** The pre-write scan now covers title + summary + evidence + targets, and the diagnostic names only the offending FIELD (never echoes the rejected text). Evidence: `SecretScanCoverageTests` — secret in title/targets refused with no file side-effect and no echo of `sk-live-abc`/`hunter2`.
  8. **P1 hot-path contract → FIXED.** State-keyed caches for parsed records (dir signature) and betweenness (artifact stat); one batched `file_commit_times` freshness read per advisory call instead of per-target opens. Evidence: `HotPathBoundedIOTests` — warm-path call counts pinned (records parse once across two calls, one batched freshness read per call, cluster artifact decompresses once) plus a bounded-latency assertion; cache-invalidation-on-write proven.
  9. **P2 substring matching → FIXED.** `_memory_advisories_for_wave` matches exact lifecycle-id tokens (`_lifecycle_id_tokens`, 5-char base36 bounded by non-alphanumerics), so `1abcd` never attaches to `1abcde`. Evidence: `ExactLifecycleMatchingTests` — prefix-collision control + exact-match attach.
  Ready to resume delivery review; `wave-council-delivery` remains WITHHELD pending the reviewer's independent re-verification.
- **Delivery re-review — 2026-07-13: BLOCKED.** The earlier `../` memory-id escape is closed, and the focused memory/drift suites pass (31 + 28 tests), but the implementation is not delivery-ready. The full run passed 5,145/5,146 tests; the sole failure was the known load-sensitive 100K-row `<200ms` timing assertion at 302ms, which passed in isolation at 97ms. Executed adversarial probes and contract review found the following unresolved blockers; `wave-council-delivery` is withheld until all are repaired and independently re-verified:
  1. **P0 security — symlinked memory-root escape.** `_contained_record_path` proves only that the record's resolved parent equals the resolved memory directory; it never proves that the resolved memory directory remains inside the repository. A fixture that symlinks `docs/agents/memory` to a sibling outside the repo makes `write_memory_record(..., "mem-symlink")` write outside the allowed root. Resolve and contain the memory root against `root.resolve()` (or reject a symlinked memory root), and cover add/reconcile plus a normal in-repo control.
  2. **P1 schema/output integrity — malformed records still surface.** `check_memory_docs` still permits a missing `Status:`, while `parse_memory_record` defaults it to `candidate`; the executed fixture surfaced the malformed record through `DEFAULT_SURFACED_STATUSES`. The runtime parser also tolerates other required schema omissions/defaults. Make lint require status, make readers fail closed unless all required fields/sections are valid (including id/filename agreement, enums, confidence, dates, nonempty evidence/targets), and fixture each malformed shape.
  3. **P1 creation atomicity — TOCTOU remains.** `write_memory_record` still performs `exists()` followed by `write_text()`, and generated ids are selected by a separate existence loop. Use exclusive creation (`x`/`O_EXCL`); retry only generated-id collisions and retain the typed conflict for explicit ids. Add a barrier-based concurrent-create fixture proving no overwrite.
  4. **P0 drift correctness — gardener-only commits still reset the anchor.** `compute_doc_drift` still treats the newest commit that names a doc as its newest material content change. The executed git fixture committed only a `Last verified:` edit after three code commits and incorrectly returned `commits_since: 0`. Derive the last material-content commit after normalizing the gardener-only field and add the committed-history regression.
  5. **P1 drift correctness — the incremental fingerprint misses uncommitted reference edits.** The skip key includes HEAD, docs path set, and verification stamps, but not material doc content/path references. An executed fixture changed an uncommitted doc reference from `src/a.py` to `src/b.py`; the second update returned `skipped: true` and retained `src/a.py`. Include a material-content or extracted-reference digest and fixture changed, added, and removed references at stable HEAD/stamp.
  6. **P1 drift correctness — timestamps are used as topology.** `_commits_after` and `waves_behind` compare `%ct` values even though the adjacent comment acknowledges clock skew. An executed newest-first history with a topologically later code commit timestamped below the doc commit returned `commits_since: 0`. Count by commit position/ancestry (and landing order for waves), with equal/skewed timestamp fixtures for living and historical docs.
  7. **P1 content safety — the pre-write scan is incomplete and echoes rejected content.** `wave_memory_add_response` scans only summary and evidence; title and target strings are persisted without the required forbidden-content check, and the diagnostic repeats the first 60 characters of a rejected line. Scan every persisted caller-controlled field before render/write and return a non-echoing diagnostic; fixture each input surface and prove no file/index-refresh side effect.
  8. **P1 performance/architecture — hot-path advisories do not satisfy the recorded cache contract.** Every `code_read`/`code_impact`/`code_callhierarchy` advisory call recursively reads/parses all memory files; matched calls also reopen/decompress the graph cluster artifact and issue per-target freshness store reads before applying the cap. This does not ride the warm graph-query-cache path and has no bounded-I/O/latency proof. Add a state-keyed memory snapshot/cache, cache or reuse centrality state, batch freshness reads, cap work before expensive ranking where semantics allow, and add warm-path call-count plus latency evidence.
  9. **P2 relevance — lifecycle matching is substring-based.** `_memory_advisories_for_wave` uses `ident in ref`, so a memory reference for a longer wave/change id can attach to a prefix id. Parse exact lifecycle-id tokens (or use bounded matching) and add prefix-collision controls.
  Review-lane disposition: security-reviewer blocked by 1 and 7; code-reviewer blocked by 2–6 and 9; QA blocked by the missing adversarial/concurrency/history fixtures; architecture- and performance-reviewers blocked by 8; docs-contract-reviewer blocked by the implementation/evidence mismatch in the completed AC claims. No delivery approval is recorded by this checkpoint.
- **Delivery-review blocker REMEDIATED — 2026-07-14.** The finding below was validated (three entry points, not two: `wave_memory_add(memory_id=...)`, `wave_memory_reconcile(memory_id=...)`, and `wave_memory_add(supersedes=...)` which flows into reconcile as a path component — the reconcile escape could flip the `Status:` line of any doc, wave records included). Fix at the `memory_records.py` chokepoint: `validate_memory_id` enforces the documented grammar (`[a-z0-9][a-z0-9-]*`, max 64 chars) before EVERY filesystem access, `_contained_record_path` adds resolved-path containment as defense in depth (comparison on resolved copies only, so macOS `/var`→`/private/var` symlinks don't break repo-relative math), and `superseded_by` references are grammar-validated before being written into content. Tool layer refuses invalid ids/supersedes with typed `invalid_memory_record` diagnostics pre-write; a residual `ValueError` from the chokepoint is also caught. Traversal regressions: `tests/test_memory_records.py` MemoryIdTraversalTests (6 fixtures, 13 evil-id grammar cases incl. `../`, backslash, absolute-ish, uppercase, underscore, oversize) — refused adds provably create no file anywhere in the tree, the reconcile escape target stays byte-identical, refusals never half-apply, valid ids still work end-to-end. Full suite after remediation: 5,146 tests across 48 files, OK bytecode-free. Ready to resume delivery review.
- **Delivery review — 2026-07-13: BLOCKED (security/code-review finding).** `wave_memory_add_response` and `wave_memory_reconcile_response` pass caller-supplied `memory_id` directly to `memory_records.write_memory_record` / `reconcile_memory_record`, which construct `docs/agents/memory/<memory_id>.md` without validating that the id is a plain slug or that the resolved path remains under the memory root. A `../` component can escape the memory directory and create or alter an unintended repository file through the MCP surface. Required remediation: validate IDs (and `superseded_by` references) against the documented memory-id grammar before every filesystem access, enforce containment after resolution as defense in depth, and add traversal regression tests for add and reconcile. This blocks the security-reviewer and code-reviewer lanes, therefore delivery council and closure.
- **Delivery review — 2026-07-13: BLOCKED (reliability/output-contract findings).** (1) `check_memory_docs` permits a missing `Status:` line, while the runtime parser defaults a missing status to `candidate`; a manually authored, schema-incomplete record can therefore pass docs-lint and surface as an advisory, contradicting the documented lint-enforced schema contract. Require `Status:` in lint and reject/skip incomplete records at read time; add regression coverage. (2) `write_memory_record` performs `exists()` followed by `write_text()`, so two concurrent `wave_memory_add` calls that choose the same generated id can both pass the check and the latter silently overwrites the former. Create records exclusively (`open(..., "x")`) and retry generated ids on collision; explicit-id collisions should return the existing conflict response. This blocks the QA and code-reviewer lanes until covered by tests.
- **Delivery review — 2026-07-13: BLOCKED (drift-anchor correctness).** `compute_doc_drift` labels its anchor as the last *content* change, but derives it from the latest commit that merely names the document (`max(path_history[doc])`). A committed `docs_gardener` run changes only `Last verified:` yet still advances that timestamp and resets `commits_since`, violating the stated invariant that gardener stamps are never drift anchors. The existing test changes the stamp only in the working tree, so it cannot expose this committed-history case. Derive the anchor from the latest diff that changes material content after normalizing/removing `Last verified:`, preserve the batched build posture, and add a fixture with a committed gardener-only update followed by code churn. This blocks 1ro43 QA, docs-contract, and delivery review.
- **Prepare-phase Wave Council [prepare-council] — 2026-07-04: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, architecture-reviewer, security-reviewer, qa-reviewer, reality-checker, docs-contract-reviewer; rotating-seat: docs-contract-reviewer; strongest-challenge: kind-aware decay was directionally wrong for `fragile_file` — churn on a fragile file is ambiguous evidence, meaning refactored-away or actively unstable, so silent confidence attenuation would hide the warning exactly when the file is being churned — resolved by amendment to 1p8gy Req 13: churn sets a needs-reverification flag, never drops the advisory below briefing inclusion, and only reconciliation retires the record; strongest-alternative: split 1p8gy into a schema+tools wave followed by a surfacing wave — rejected because the action-time surfacing is the value thesis of the change and the schema alone ships nothing an agent experiences, while the real coupling risk, the 1ro43 freshness-primitive seam, is already serialized inside the wave)
- Council seat notes: red-team also pressed advisory latency/response bloat on hot read tools (resolved by amendment to 1p8gy Req 6: named caps, warm graph-cache path, graceful absence) and the cross-wave storage seam with 1rsh9 (accepted as recorded: either landing order works, one store file, seam mirrored in both wave records). architecture-reviewer: single write path through the 1p9q3 incremental-merge store is correct, require graceful absence at every surfacing point — folded into the Req 6 amendment. security-reviewer: forbidden-content lint must land with the schema (already a wave watchpoint); memory records are repo-visible and reviewable, local-git-only churn — pass. qa-reviewer: require synthetic git fixtures rather than live-repo assumptions for churn/derivation tests, and per-kind fixtures for all eight memory kinds — accepted as implementation-test guidance. reality-checker: gardener mechanical stamping, status-only close drift, landing-commit convention (including the five-wave bundle commit), and chunk_hash reuse all verified against source this session; the "wave docs are the bulk of the docs index" claim is directional, not measured — accepted as directional. docs-contract-reviewer (rotating): the close-time distillation checkpoint in 1p8gy Req 7 is a deliberate new close-step and must stay cheap (propose candidates, quick promote/reject/defer decisions) — accepted with the burden note recorded; 1ro43 Req 11 consumer contracts must be published on the drift-summary surface, not only in the change doc. seat_agreement: unanimous; no challenge round required; amendments applied in-session before readiness was recorded.
- AC priority: confirmed at prepare for both changes as proposed in each doc (1p8gy AC-1..10 required, AC-11 required, AC-12/13 important; 1ro43 AC-1..6, 8..11, 13, 14 required, AC-7/12 important). Product-owner acknowledgment: wave scope and both changes were operator-directed in-session during planning on 2026-07-04.
- implementation-complete (2026-07-13): both changes `implemented`, all required ACs `[x]` with evidence notes (one `[~]`: the 1p8gy conditional dashboard task, audited and recorded as optional follow-on scope). Full suite 5,140 tests across 48 files OK bytecode-free; docs-lint clean. Version bumps this wave: `GRAPH_BUILDER_VERSION` 43→44 (memory nodes/edges; field graphs re-extract on upgrade per the standing Phase 4b path); no `STATE_STORE_SCHEMA_VERSION` bump (population-only on the landed v6 tables); no `CHUNKER_VERSION` bump. New ADRs: `1sihk` (annotation-first temporal decay), `1sk58` (local typed agent memory records). The drift partition ships default-OFF per the census + standing ranking-eval gate; live census evidence recorded in the `1ro43` doc (75 waves attributed, annotation-only decision, `docs/reports/` exemption). Seed edits (100/180/190/211/004) ran under the `seed_edit_allowed` gate, opened and closed in-session; framework gate closes at pause/close per lifecycle. Delivery-phase review (Review wave) not yet run — next lifecycle step.
- pre-implementation-review: passed (2026-07-13) — operator-directed final review before implementation: four seam reviewers (storage/index, retrieval surfacing, graph/memory, policy/history/gardener) ran executed probes reconciling the 2026-07-04 plans with waves landed since readiness (1rsh9, 1sbfi/1sbfk, 1sc7c, 1sed7, 1seav). No blocking findings. Highest risk was a stale plan premise: the per-citation `demoted`/`partition_reason` mechanic `1ro43` planned to mirror was removed from the code in change `12q5v` — addressed by amending Req 5 to *reintroduce* the fields on the surviving `_partition_infra` stable-partition pattern and reconcile the five doc surfaces that still describe the removed mechanic. Favorable drift: `1rq4h` (wave 1rsh9) already landed the freshness/attribution/drift storage, build-time extraction, and the exact `freshness_for_path` primitive of the cross-change seam — the seam watchpoint is satisfied by landed code. Other amendments applied inline to both change docs: drift partition suppressed off the healthy reranked path and default-ON additionally gated on the `1seaw` golden-query eval (or operator waiver); `code_lexical` added to annotation surfaces; historical class resolves by path prefix (production docs tables carry no `tags` column); landing-commit derivation covers censused variants incl. "Close wave"-as-landing; 1p8gy graph edges narrowed to file/symbol/doc/config targets with a zero-edge-prune constraint; community refs pinned to hub node ids; the feature is named the "agent memory layer" in shipped surfaces (the framework term "Wave Memory" is already taken by seeds 004/110). Also converts the reality-checker's directional claim to measured fact: docs/waves chunks are 68.8% of live docs rows.

## Review Evidence

- wave-council-readiness: approved 2026-07-04 — prepare council synthesis verdict READY after amendments (fragile_file needs-reverification semantics; advisory caps and graceful absence); seats unanimous; full synthesis in Review Checkpoints
- wave-council-delivery: approved 2026-07-14 (operator decision; supersedes the 2026-07-14 withdrawal) — delivery was WITHHELD/withdrawn through re-review rounds 4–7 after independent verification reproduced P1 blockers; every one was repaired with regression coverage (memory seqlock + writer-token fences; typed git authority + `.git`-marker check; fail-closed drift clearing on the build-tail and no-op paths; and the three executed-proof git-env fixes — ambient `GIT_DIR` redirect, `GIT_SHALLOW_FILE`/`GIT_GRAFT_FILE` truncation, failed-clear-reported-success). The round-7 scope correction REJECTED the over-scoped git-config neutralization (safe.directory preserved), pinned rename determinism via `--no-renames`, reconciled the suite count, removed the premature `unittest.main()`, and strengthened the AST census. The operator then directed approval without a further unrestricted review round. Recorded as an OPERATOR acceptance of the corrected scope, not an nth independent council pass.
- operator-signoff: approved 2026-07-14 — operator explicitly directed closure after the round-7 scope correction and delivery approval.

## Dependencies

- No external wave dependencies.
