# Wave Record

Owner: Engineering
Status: closed
Last verified: 2026-07-05

wave-id: `1p9qi sql-graph-accuracy`
Title: SQL Graph Accuracy and Embedded SQL

## Objective

Turn SQL from the graph's noisiest, most hollow language into a first-class data layer, and connect code to it. When this wave closes: keyword tokens no longer mint fake external nodes (live-verified bug), SQL references are clause-aware with read/write direction and view lineage, procedures the grammar can't parse are recovered instead of vanishing, and — the enterprise payoff — SQL embedded in Java/C# data-access code and JPA/EF entity declarations bind to table nodes, making "which code touches table X" answerable.

## Changes

Change ID: `1p9qc-bug sql-keyword-noise-suppression`
Change Status: `implemented`

Change ID: `1p9qd-enh sql-structured-statement-extraction`
Change Status: `implemented`

Change ID: `1p9qe-enh sql-ddl-error-recovery`
Change Status: `implemented`

Change ID: `1p9qf-enh embedded-sql-capture-and-bind`
Change Status: `implemented`

Change ID: `1p9qg-enh orm-entity-table-mapping`
Change Status: `implemented`

Completed At: 2026-07-05

## Wave Summary

Wave `1p9qi` (SQL Graph Accuracy and Embedded SQL) delivered 5 changes: SQL extraction: suppress keyword tokens leaking into call/import edges, SQL extraction: clause-aware table references, view lineage edges, and honest object kinds, SQL extraction: DDL recovery tier for statements the grammar cannot parse (procedures, functions, dialect forms), Embedded SQL: capture SQL string literals at known Java/C# sinks and bind code to table nodes, and ORM mapping: bind JPA and EF entity classes to SQL table nodes via declared table-name annotations. Notable adjustments during implementation: SQL extraction: suppress keyword tokens leaking into call/import edges: Scoped from the Java/SQL accuracy investigation. Live extraction run reproduced the noise (`external::FROM/JOIN/ON/SELECT/WHERE`, dotted column tokens); root cause traced to import-only keyword strip (then `graph_indexer.py:5249`) over substring-matched SQL call nodes (then `:2428-2429`) with regex fallback (then `:5240`); sole SQL test asserts nothing about reference edges (`test_graph_indexer.py:296,317-331`).; SQL extraction: suppress keyword tokens leaking into call/import edges: **Findings recorded for later changes (not fixed here, scope discipline):** (a) String-literal contents leak as call candidates in SQL mode — `INSERT ... VALUES ('x')` emits `calls → external::x` (the regex fallback scans the literal's text; not a keyword, not a column node). 1p9qd's structured statement extraction should replace the regex candidate source entirely; until then the exact-set fixture uses numeric literals to avoid encoding this residue. (b) The schema-qualified field-name path splits `analytics.events` into THREE import externals (`analytics.events`, `analytics`, `events`) — pre-existing today-behavior retained per Requirement 3; 1p9qd owns consolidating it. (c) `_ts_is_import_node`/`_ts_is_call_node` SQL substring matching remains broad (`object_reference` matches "reference"; `keyword_*` nodes match their keyword) — harmless now that candidates are filtered, but 1p9qd's clause-aware extraction should tighten node selection rather than rely on the filter. (d) Table aliases (`SELECT u.id FROM users u`) still mint `external::u`-style candidates (alias `u` is a bare regex token; `u.id` reduces to `u`) — pre-existing, unchanged by this fix; 1p9qd's alias exclusion (AC-6 family) covers it.; SQL extraction: clause-aware table references, view lineage edges, and honest object kinds: Scoped from the Java/SQL accuracy investigation. Confirmed by live runs: no table/view kind distinction (then `graph_indexer.py:1976-1981,2049-2050`), token-scraped references with no direction/qualification, no view lineage; grammar exposes object_reference clause structure; cross-file unique-name table resolution already works (the foundation to build on).

**Changes delivered:**

- **SQL extraction: suppress keyword tokens leaking into call/import edges** (`1p9qc-bug sql-keyword-noise-suppression`) — 4 ACs completed. Key decisions: SQL-gated stoplist filter at the candidate stage (approach A).; Dotted-token classification uses the grammar's structural signal (dotted `field` descendant = column ref → reduce to its `object_reference` table; dotted `object_reference` descendant = schema-qualified table → keep whole) instead of blind text-segment reduction.
- **SQL extraction: clause-aware table references, view lineage edges, and honest object kinds** (`1p9qd-enh sql-structured-statement-extraction`) — 8 ACs completed. Key decisions: Clause-aware extraction with a reusable statement unit; table-level only (approach A).; Read/write distinction representation decided at implementation (new `writes` relation vs `mode` property on `reads`).
- **SQL extraction: DDL recovery tier for statements the grammar cannot parse (procedures, functions, dialect forms)** (`1p9qe-enh sql-ddl-error-recovery`) — 6 ACs completed. Key decisions: Statement-granular ERROR-region recovery scan, grammar unchanged (approach A).; Body references attach to the recovered procedure node.
- **Embedded SQL: capture SQL string literals at known Java/C# sinks and bind code to table nodes** (`1p9qf-enh embedded-sql-capture-and-bind`) — 7 ACs completed. Key decisions: Two-stage capture/bind mirroring `reads_config`, sink-gated, unique-match-or-drop, `LITERAL_DERIVED` (approach A).; Native-SQL only; JPQL excluded pending `1p9qg`.
- **ORM mapping: bind JPA and EF entity classes to SQL table nodes via declared table-name annotations** (`1p9qg-enh orm-entity-table-mapping`) — 6 ACs completed. Key decisions: Declared table names only; conventions refused and counted (approach A).; Separate change from `1p9qf` despite the shared seam.
## Journal Watchpoints

- Hard sequencing: `1p9qc` first (clean candidate space; later tests must not encode noise), then `1p9qd` (its statement-analysis unit is the frozen contract `1p9qe` body-recovery and `1p9qf` bind both consume), then `1p9qe`/`1p9qf`/`1p9qg`.
- Shared seams to build once: the annotation/attribute-argument capture mechanism (`1p9qf` + `1p9qg`), the read/write edge representation decision (`1p9qd` + `1p9qg`, one consumer sweep), and the `external::sql::` namespace (`1p9qf` + `1p9qg`).
- Single coordinated `GRAPH_BUILDER_VERSION` bump (37→38) across all five changes at integration. (Freshness note 2026-07-04: wave `1p9qh` has landed, so the originally-planned vocabulary-edit coordination is moot — `extends`/`implements` are already in `docs/specs/mcp-tool-surface.md` at four tool entries, and the canonical relation table lives in `docs/architecture/graph-index-system.md`; any `writes`/`maps_to` relation edits against that current text.)
- Blocking gate before close: `1p9qf`/`1p9qg`'s real-corpus locality census (standing literal-edge field lesson) — the default-on decision must be recorded on census evidence, not fixtures.
- All binding changes require the consolidated adversarial faithfulness review lane at implementation review (standing security-control-faithfulness rule); `1p9qe`'s recovery scan counts as a detection surface for that review.
- Follow-up candidates deliberately out of scope: JPQL binding (layers on `1p9qg`), column-level lineage, convention-derived table names, Python/Go/TS SQL sinks.
- `CLUSTER_BUILDER_VERSION` no-bump CONFIRMED at integration (2026-07-05): the cluster projection's input surface (`graph_cluster.py` `_build_projection` region) excludes external + `constant`-kind nodes and `reads` edges, weights every other relation via `_RELATION_WEIGHTS` dict-default 1 — the wave's `writes`/`maps_to` relations enter at default weight with no code change, node kinds are unchanged (`sql_kind` is a property invisible to the projection), and the clusters artifact shape is untouched; the coordinated `GRAPH_BUILDER_VERSION` 37→38 bump already forces re-clustering, and `1p9qg`'s lane confirmed `graph_cluster.py` untouched.
- Live MCP sessions need `wave_mcp_reload` after this wave lands — `server_impl.py` changed (`_GRAPH_SIGNAL_RELATIONS` + `_GRAPH_REL_LABEL`/`_GRAPH_REL_BUCKET` gained `writes`/`maps_to` with `writers`/`mapped_entities` buckets); a long-lived server keeps executing pre-edit code until reload.

## Participants

- code-reviewer — all five changes touch `.wavefoundry/framework/scripts/*.py`
- qa-reviewer — required (bug fix `1p9qc` per `review_policies.require_qa_reviewer_for_bug_fixes`) and all change docs carry AC priority tables
- architecture-reviewer — potential relation-vocabulary addition (`writes`), the new statement-analysis unit seam, and the first code→data-layer edge family
- performance-reviewer — new scan patterns and per-file extraction additions (`1p9qe` recovery scan, `1p9qf` sink capture)
- red-team, reality-checker — council seats (prepare phase); consolidated adversarial faithfulness lane re-runs at implementation review

## Review Checkpoints

- Prepare wave — readiness verdict (2026-07-03): READY. Council ran at standard primer depth. Red-team's strongest challenge — the `1p9qf` bind stage can bind through query-local names that are not schema objects (temp tables `#t`/`@t`, derived-table aliases, CTEs), minting false code→table edges that would poison exactly the impact analysis the wave exists to enable — was accepted and resolved by amendment: `1p9qd` AC-6 now explicitly excludes temp-table/table-variable sigil forms and subquery aliases alongside CTEs/aliases, and the statement unit is the single shared extraction path so the exclusion covers `1p9qe` body recovery and `1p9qf` binds uniformly. Reality seat confirmed every claimed defect is live-verified (keyword-noise edges, ERROR-node procedure loss, dangling body references) and endorsed the census gate on `1p9qf`/`1p9qg` per the standing literal-edge field lesson. QA seat confirmed the exact-edge-set test discipline replaces the vacuous single SQL test and that both bug-fix flip directions are pinned. Architecture seat flagged the read/write representation decision (`writes` relation vs edge property) as correctly deferred to a consumer sweep shared with `1p9qg`, and the statement-unit contract freeze as the wave's key serialization point. Performance seat: recovery scan and sink capture are bounded and run only on ERROR regions / already-visited AST nodes — acceptable. Strongest alternative (repo-wide SQL-sniffing of all string literals instead of sink gating) recorded and declined — sink knowledge is what makes precision achievable; the false-positive surface of literal trawling scales with the repo. AC priorities recorded on all five change docs. Product-owner acknowledgment: not applicable (framework-internal accuracy work).
- Security seat (rotating): the recovery scan (`1p9qe`) and sink capture (`1p9qf`) process repo-local content with static anchored patterns, hard byte/line bounds, and no user-supplied patterns — no denial-of-build or injection surface; comment-stripping before the recovery scan prevents commented-out DDL from minting schema objects. The embedded-SQL edge family feeds review-support impact analysis, so false binds are an integrity concern — covered by the sniff gate, origin checks, unique-match-or-drop, the `external::sql::` namespace isolation, the temp-object exclusion amendment, and the census + adversarial lane before close. No security findings.
- **Prepare-phase Wave Council [prepare-council] — 2026-07-03: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, architecture-reviewer, security-reviewer, qa-reviewer, reality-checker, security-reviewer; rotating-seat: security-reviewer; strongest-challenge: the bind stage could bind through query-local names that are not schema objects, temp tables and derived-table aliases, poisoning code-to-table impact — resolved by the 1p9qd AC-6 temp-object and alias exclusion applied at the shared statement unit; strongest-alternative: repo-wide SQL-sniffing of all string literals instead of sink gating — declined, sink knowledge is what makes precision achievable)

- Freshness re-verification [prepare-freshness] — 2026-07-04: PASS (reality-checker freshness lane, run at activation because all five plans were readied before waves `1p9q3` and `1p9qh` rewrote `graph_indexer.py`). All five docs READY with corrections applied in place; no mechanism invalidated — the SQL-mode code paths the plans anchor (`_ts_is_call_node`, `_ts_relation_field_names`, `_ts_relation_candidates`, `_ts_kind_for_definition`) survived both waves behaviorally intact with line drift only. Substantive corrections beyond anchors: (1) `1p9qe`/`1p9qf` cited the `1p9q6`/`1p9q7` line-scan/origin-check conventions as landed precedent — they are NOT in tree (wave `1p9q8` still planned); reframed as design-mirroring with a cross-wave serialization note (whichever wave lands first defines the degradation-convention family). (2) `1p9qf`'s claim that `external::sql::` "can never collide" with source symbols was false (mintable via a Rust `use sql::…` import, same class as `1p9qh`'s F4); corrected to a relation-scoped invariant contract mirroring the reserved-marker treatment, with a required invariant test. (3) `1p9qc`'s defect re-reproduced live on the v37 tree with new evidence: keyword noise also mints on the `imports` relation (case-sensitive `_RELATION_KEYWORD_NOISE` misses `FROM`) and self-referential `schema.sql::users → external::users` edges exist — recorded in the change doc. (4) `1p9qg` upgraded: C# attribute-name capture already exists (`_ts_extract_csharp_attributes`), so the shared annotation/attribute seam is extend-not-create on both languages. Implementation traps recorded: new fragment keys must join the merge passthrough list or cached fragments silently drop them; reusing the `reads` relation inherits its opt-in-1-hop/excluded-from-clustering policy — the consumer sweep must decide that consciously; `_GRAPH_SIGNAL_RELATIONS` + `_GRAPH_REL_LABEL`/`_GRAPH_REL_BUCKET` added to the consumer-sweep scope.

- pre-implementation-review: passed (2026-07-04) — Pre-mortem: (1) sequencing is the plan of record: `1p9qc` alone first (clean candidate space — its exact-edge-set tests become the baseline later changes build on), then `1p9qd` alone (the statement-analysis unit is the frozen contract), then `1p9qe` ∥ `1p9qf` ∥ `1p9qg` in parallel — but `1p9qf`/`1p9qg` share the annotation/attribute capture seam and the `external::sql::` namespace, so those two lanes get an explicit shared-seam brief and `1p9qf` builds the seam (`1p9qg` extends it); all three converge on one `graph_indexer.py`, so parallel lanes work symbol-disjoint regions with serialized integration. (2) The `external::sql::` invariant test is a required deliverable of `1p9qf` (freshness finding — the namespace IS mintable from source). (3) The census gate is pre-close blocking for the `1p9qf`/`1p9qg` default-on decision — census on a real enterprise corpus (Tomcat precedent from `1p9qh` review), not fixtures. (4) Single `GRAPH_BUILDER_VERSION` bump 37→38 at integration, not per-change; `CLUSTER_BUILDER_VERSION` bumps only if `1p9qd`'s kind representation changes community shape (consumer sweep confirms). (5) The adversarial faithfulness lane is REQUIRED at delivery review (binding changes; `1p9qe` recovery is a detection surface). (6) Cross-wave serialization: wave `1p9q8` is still planned and shares extractor regions — this wave defines the degradation/origin-check conventions; `1p9q8`'s freshness pass must mirror them. MCP-first directive in every lane prompt; measurements via fresh venv subprocesses (running server executes pre-edit code until reload).

- implementation-complete — 2026-07-05: ALL FIVE admitted changes are implemented (`1p9qc`, `1p9qd`, `1p9qe`, `1p9qf`, `1p9qg`) and the coordinated integration step is done. Per change: `1p9qc` all-relation SQL keyword stoplist + column-token reduction + self-referential import skip (4 tests, flip-verified); `1p9qd` clause-aware statement-unit rewrite — SQL emits `reads` + NEW `writes` relation instead of `calls`/`imports`, `sql_kind` property, view/FK/index lineage, frozen `sql_statement_references` contract (9 tests + 2 rewritten; calibration 47→16 edges on the shared fixture); `1p9qe` ERROR-region recovery tier with `sql_recovery` provenance + loud per-file counts + body-reference re-attachment (5 tests; migration calibration 4 parsed + 4 recovered definitions, 6/6 routine-body references bound); `1p9qf` embedded-SQL capture at Java/C#/MyBatis-XML sinks → `LITERAL_DERIVED` binds + relation-scoped `external::sql::` externals, Fineract census 11/11 = 100% precision, Tomcat negative control 0 false positives, DEFAULT-ON (26 tests); `1p9qg` NEW `maps_to` entity→table relation, declared names only, Fineract census 28/28 = 100% precision, DEFAULT-ON (22 tests). Integration: single coordinated `GRAPH_BUILDER_VERSION` 37→38 bump naming all five contributions (constant changelog + test pin + architecture-doc snippet); `CLUSTER_BUILDER_VERSION` no-bump assessment verified against the cluster projection's input surface (watchpoint above); deferred spec-doc capability notes woven (`docs/specs/mcp-tool-surface.md` — graph augmentation, `graph_related` buckets incl. `mapped_entities`, `code_impact` data-layer + `LITERAL_DERIVED` down-weighting, `code_graph_path` cost tier, `external::sql::` note; `docs/architecture/graph-index-system.md` — embedded-SQL capture/bind decision note added beside the existing `writes`/`maps_to` rows and statement-unit/recovery notes, version snippets refreshed); the `reads external::NOW` invocation-noise finding MEASURED on the Fineract corpus (42/953 = 4.4% of unit references) and FIXED in the statement unit (function-name skip preserving argument-subquery reads + TVF handling untouched, ~7 LOC + flip test — recorded in `1p9qd` Decision Log and `1p9qe` Progress Log); live upgrade-heal verified on this repo's v37 index (loud `graph_auto_rebuilt` 37→38 diagnostic, `merge[full-merge]`, 53.9s; healed payload 11,660 nodes / 33,320 edges; SQL signatures honestly zero on this SQL-free repo — `writes` 0, `maps_to` 0, `external::sql::` 0, `sql_recovery` 0; zero-change incremental afterwards is a no-op, 1.0s). Full suite 4,596 tests / 43 files OK (baseline 4,595 + the invocation-noise flip test); `wave_validate` clean; no `__pycache__`. Remaining before close: the consolidated adversarial-faithfulness review lane at delivery review (`1p9qf` AC-7 / `1p9qg` AC-6 second clauses) and the review-lane roster in Participants.

- **Delivery-phase Wave Council [wave-council-delivery] — 2026-07-05: PASS (seats unanimous ship)** (moderator: wave-council; primer-depth: standard; seats: red-team, architecture-reviewer, security-reviewer, qa-reviewer, reality-checker, senior-engineering-challenger; rotating-seat: senior-engineering-challenger; strongest-challenge: the red-team primer's live-probed finding that `handle_create_routine` flattened natively-parsed PL/pgSQL `$$`/`$tag$` routine BODIES through the generic `walk_reads` on the TRUSTED parse path — mis-filing every in-body INSERT/UPDATE/DELETE as a `reads` edge (write-direction inverted, defeating 1p9qd's headline read/write feature for stored-procedure logic), minting phantom reads for nested CREATE, and bypassing the council-mandated AC-6 temp exclusion; the recorded residual had understated this as one cosmetic phantom, and both census corpora lack natively-parsing PL/pgSQL bodies so the "100% precision" evidence was silent on the entire class — RESOLVED in-session by routing in-body statement nodes through `analyze_statement` (the single canonical clause→direction dispatch, unifying the native path with the recovery tier), with the four fixed seats + a dedicated fix lane confirming the flip on every sub-case (in-body DML now `writes`, nested CREATE mints neither read nor phantom def, AC-6 temp exclusion honored in-body, genuine subquery reads preserved) and the security seat's ~20 hostile probes plus a live incremental-equals-full spot-probe verifying no inversion, double-count, dropped-read, or invariant break survives; strongest-alternative: a direction-aware generic `walk_reads` instead of routing through `analyze_statement` — declined by the rotating seat as forking the direction logic into a second bug surface that would not have helped the residual loop-body shape anyway. Disposition: SHIP. The precision-destroying inversion is eliminated; every remaining residual the seats found is safe under-capture (dropped edge, a recall gap) or reserved-name noise contained by `LITERAL_DERIVED` + unique-match-or-drop, none worse than the pre-wave baseline, all recorded as honest follow-ups — the convergent cursor `FOR-LOOP` / control-flow body under-capture (in-loop DML writes silently dropped, found independently by the security and rotating seats) is now made observable by a new `sql_partial_bodies` loudness counter this council added, with actual in-loop DML recovery deferred (needs plpgsql control-flow parsing tree-sitter-sql lacks). Census default-on decision NOT gated on a Postgres slice — a slice would measure the recall gap, not change correctness, since the failure mode is under-capture not confidently-wrong binding.
  - Delivery-lane + fixed-seat evidence (each seat's findings, distinct from the verdict line): **red-team primer** live-probed and escalated the routine-body defect from "one phantom" to systematic in-body write-direction inversion on the trusted path, and posed the census-gap and AC-6-in-body-bypass questions the council then resolved. **architecture-reviewer** (approve-with-notes) confirmed routing-through-`analyze_statement` is the correct single-direction-authority fix and every relation consumer (`_GRAPH_SIGNAL_RELATIONS`, opt-in set, impact data-layer exception, path cost, report fan, clustering) stays coherent; two non-blocking notes — community detection excludes `reads` but not `writes`/`maps_to`, an asymmetry the denser in-body writes amplify (a hot write-target table can bridge modules; recorded rationale added), and the body-definition drop is name-parse-gated so the module-scope-definition invariant is non-total for an unnamed-but-parseable routine. **security-reviewer** (approve-with-notes) ran ~20 hostile bodies confirming the fix binds with no inversion/double-count/dropped-read/phantom, re-verified the `$tag$` masking and same-file-impostor prior findings stayed fixed/recorded, and confirmed all three cross-cutting invariants including a live incremental-equals-full spot-probe; notes: the cursor `FOR-LOOP` write-drop with `error_regions:0` contradicting the doc's loud-degradation claim, and an uncommon non-temp body-local scratch table cross-file unique-bind. **qa-reviewer** (pass) re-ran the full suite fresh (4,599/43), verified all five new tests are load-bearing exact-set assertions tied to real source changes, confirmed both compound-AC flips honest (both clauses satisfied) and zero silent `[ ]` across the five change docs, and reconciled both censuses to the digit. **reality-checker** (pass) verified all headline claims live including a rebuild of the routine-body probe (every edge flipped), `GRAPH_BUILDER_VERSION` 38 with no second bump, the prior-fix-round items on disk, and both edit gates closed. **senior-engineering-challenger** (rotating, approve-with-notes) hunted sibling body-bearing constructs — confirmed triggers/DO-blocks/nested-BEGIN…END do NOT carry the inversion — and surfaced the strongest remaining concern (the `error_regions:0` loudness leg is unreliable for control-flow bodies the census never exercised) with a ship-with-recorded-follow-up disposition and the invocation-exclusion↔misparsed-INSERT interaction named. **performance-reviewer** (delivery lane, approve-with-findings) measured SQL indexing 33% faster / 93% fewer edges on real DDL, non-SQL rebuild within noise against a pre-wave worktree baseline, and one honest correction that Java-heavy corpora pay a measured +1.3–2.6% capture tax the change doc had rounded to noise. **code-reviewer** (delivery lane, approve-with-findings) found the now-production-dead `1p9qc` filter path (removed) and six minor maintainability items (all fixed or recorded).

## Review Evidence

- wave-council-readiness: approved 2026-07-03 — prepare council synthesis verdict READY after the 1p9qd temp-object exclusion amendment; census gate on 1p9qf/1p9qg affirmed; no unresolved blocking findings
- code-reviewer: approved with findings, findings fixed 2026-07-05 — delivery lane over the full diff; correct, well-gated, 11 adversarial grammar probes clean; strongest finding (the production-dead 1p9qc candidate filter retained with live-sounding comments after 1p9qd's dispatch bypassed it) removed in the fix round; six minor items fixed or recorded.
- qa-reviewer: approved 2026-07-05 — delivery lane + fixed council seat; every required-priority AC across the five change docs reconciles to load-bearing exact-set tests with absence sweeps and recorded flip evidence, the census artifacts match the docs to the digit and are re-runnable (corpora pinned by commit), and the full suite independently reproduced at 4,599/43; the two compound ACs flipped honestly after the adversarial lane ran.
- architecture-reviewer: approved with notes 2026-07-05 — fixed council seat, code-grounded; the routine-body fix is the correct single-direction-authority design and the reads/writes/maps_to relation asymmetry stays coherent at every consumer; non-blocking notes — the deliberate non-exclusion of writes/maps_to from clustering (amplified by denser in-body writes) now carries a recorded rationale, and the name-parse-gated body-definition drop leaves the module-scope-definition invariant non-total for an unnamed-but-parseable routine.
- security-reviewer: approved with notes 2026-07-05 — delivery adversarial-faithfulness lane + fixed council seat; the routine-body fix fully binds under ~20 hostile probes (no direction inversion, double-count, dropped genuine read, or phantom), the two prior adversarial findings stayed fixed/recorded, and all three cross-cutting invariants hold including a live incremental-equals-full spot-probe; non-blocking notes — cursor FOR-LOOP body DML writes are silently under-captured (now made observable by the `sql_partial_bodies` counter) and an uncommon non-temp body-local scratch table can cross-file unique-bind.
- performance-reviewer: approved with findings 2026-07-05 — delivery lane; SQL indexing 33% faster and 93% fewer edges on real DDL, non-SQL repo rebuild within noise against a pre-wave worktree baseline, recovery-scan bounds and empty-candidate short-circuits verified real; one honest finding that Java-heavy corpora pay a measured +1.3–2.6% capture tax the change doc had rounded to noise (corrected in the doc).
- reality-checker: approved 2026-07-05 — fixed council seat; all headline claims verified on the live tree including a rebuild of the routine-body probe (every edge flipped correctly), the four sub-cases, `GRAPH_BUILDER_VERSION` 38 with no second bump, the prior-fix-round items on disk, a clean 4,599/43 full suite, and both edit gates closed.
- senior-engineering-challenger: approved with notes 2026-07-05 — rotating fifth seat; confirmed the inversion fix is architecturally correct and that sibling body-bearing constructs (triggers, DO-blocks, nested blocks) do not share it, surfaced the strongest remaining concern (the `error_regions:0` loudness signal is unreliable for control-flow bodies the census never exercised) with a ship-with-follow-up disposition since the mode is under-capture not inversion, and named the invocation-exclusion interaction — all recorded as honest follow-ups.
- wave-council-delivery: approved 2026-07-05 — PASS, seats unanimous ship (structured synthesis in Review Checkpoints). All five changes implemented with required lanes green; the delivery review's red-team primer caught a severe routine-body write-direction defect that was fixed in-session and adversarially re-verified; convergent seat follow-ups recorded honestly and the one code-worthy convergent finding (loudness of nested-in-body ERROR) closed with the `sql_partial_bodies` counter. Final suite 4,599 (+ the loudness-signal test the council fix adds); `GRAPH_BUILDER_VERSION` 38, no second bump; both edit gates closed.
- operator-signoff: approved 2026-07-05 — operator confirmed closure ("close 1p9qi then implement 1rrx5-enh") after the close-readiness readout.

## Dependencies

- Soft dependency on wave `1p9qh` only for shared spec-doc edits; no lifecycle blocking either direction.
