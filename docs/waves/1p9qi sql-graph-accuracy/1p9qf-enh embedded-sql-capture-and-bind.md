# Embedded SQL: capture SQL string literals at known Java/C# sinks and bind code to table nodes

Change ID: `1p9qf-enh embedded-sql-capture-and-bind`
Change Status: `implemented`
Owner: Engineering
Status: implemented
Last verified: 2026-07-05
Wave: TBD

## Rationale

Nothing today detects SQL inside host-language code: grep-confirmed zero hits for `@Query`, `prepareStatement`, `JdbcTemplate`, MyBatis, Dapper, or EF anywhere in `graph_indexer.py`/`graph_di_signals.py` (guru investigation, 2026-07-03). Yet the machinery to do it faithfully is precedented three times over:

- the `reads_config` literal pipeline — shape-gated capture → distinctiveness gate → **unique-match-or-drop** → `LITERAL_DERIVED` confidence (finalize bind pass `graph_indexer.py:9745-9793`, `_config_literal_is_distinctive` `:445`);
- the AOP matcher-string capture — walk string arguments of a fixed call-shape vocabulary (`_java_aop_matcher_strings`, `graph_indexer.py:5699`);
- SQL table nodes already exist and cross-file resolve (live-verified), and enterprise repos typically carry their DDL in-repo as Flyway/Liquibase migration `.sql` files — which this indexer already indexes.

The capability this unlocks is the one enterprises actually ask of a code graph: **code ↔ database impact** — "which services touch table X", "what breaks when this table is refactored". Design mirrors `reads_config` exactly: a per-language **capture stage** collects SQL-candidate literals only at known sinks, and a finalize **bind stage** extracts referenced tables (via `1p9qd`'s statement unit) and binds method → table at `LITERAL_DERIVED` confidence on unique match. Dynamic/concatenated SQL stays unbound; missing schema binds to `external::<table>` honestly.

A standing field lesson gates this change: **literal-derived edges must census target locality on a real repo before shipping** — synthetic fixtures hide false positives, and a literal family whose targets are mostly out-of-project binds nothing useful (the OTel `instruments` precedent). The census is an explicit AC, not a footnote.

## Requirements

1. **Java capture sinks.** SQL-candidate literals are collected only from: MyBatis annotations (`@Select`/`@Insert`/`@Update`/`@Delete`), Spring/JPA `@Query(..., nativeQuery=true)` and `@NamedNativeQuery` values, JDBC `prepareStatement(...)`/`prepareCall(...)` string arguments, and `JdbcTemplate`/`NamedParameterJdbcTemplate` query-method string arguments. Sink identification is alias-aware and import-origin-checked where checkable (same discipline as `1p9q7` — a readied change in wave `1p9q8`, still `planned` as of 2026-07-04, so the discipline is its documented contract, not in-tree code to reuse): a user-defined `prepareStatement` on a non-JDBC type must not fire when the receiver type is resolvable to a non-JDBC class.
2. **C# capture sinks.** `new SqlCommand("...")` (and `CommandText = "..."` assignment), Dapper extension calls (`Query`/`Query<T>`/`Execute`/`ExecuteScalar`/`QueryFirst*` with a string first-arg), EF `FromSqlRaw`/`ExecuteSqlRaw`(+`Async`). Same origin-checking discipline where the receiver/namespace is resolvable.
3. **MyBatis XML mappers.** Mapper XML files (`<mapper namespace=...>` with `<select>/<insert>/<update>/<delete>` elements — XML mode already parses markup) contribute statement text with the owning mapper interface/namespace as the source symbol.
4. **SQL sniff gate.** A captured literal enters the pipeline only if it sniffs as SQL: leading keyword in {SELECT, INSERT, UPDATE, DELETE, WITH, MERGE, CALL, EXEC} after whitespace/parens, case-insensitive. Non-SQL strings at the sinks (rare) drop silently; SQL-looking strings NOT at a sink are never captured (no repo-wide literal trawling).
5. **Bind stage.** At finalize, each captured statement runs through `1p9qd`'s statement-analysis unit; each referenced table binds source-method → table node with a `reads`-family edge (read/write kind per the `1p9qd` representation) at `LITERAL_DERIVED` confidence, only when the table name (schema-qualified first, then bare) matches exactly **one** SQL-defined node; ambiguity drops the edge; no SQL-defined match emits `external::sql::<table>` (a namespaced external keeping the SQL domain's externals disjoint from host-language candidate sets). Freshness caveat (2026-07-04): the prefix is **not globally unmintable** — a Rust module literally named `sql` mints `external::sql::…` ids on `imports` edges (the same precedent `1p9qh`'s red-team F4 established for `external::super.*`), so the safety contract must be **relation-scoped and invariant-tested**, mirroring the `_SUPER_CALL_PREFIX`/`_STATIC_OR_INHERITED_PREFIX` treatment (`graph_indexer.py:3251-3293`): the bind stage only ever mints these ids on its own `reads`-family/`LITERAL_DERIVED` edges, and phase-1 cross-file resolution must pass them through untouched.
6. **Dynamic SQL refusal.** Only single-literal arguments and adjacent compile-time string concatenation of literals bind; anything involving variables, formatting, or builders is refused (counted in the build log as unbound-dynamic, so the gap is visible).
7. **JPQL exclusion.** Non-native `@Query` (JPQL) is out of scope — it references entities, not tables; entity→table mapping is `1p9qg`'s job. The capture explicitly checks `nativeQuery=true` for `@Query`.
8. **Locality census (gating).** Before the bind stage ships enabled, run capture+bind on at least one real enterprise-shaped repo (or the most realistic available corpus) and record: sink hit counts, sniff pass rate, bind rate vs external rate, and a hand-verified precision sample (≥30 bound edges, ≥95% correct). If in-project bind rate is negligible (schema not in repo), the feature still ships (external edges are honest) but the census result is recorded and the default-on decision revisited.
9. **Version bump + adversarial review.** `GRAPH_BUILDER_VERSION` bumped; adversarial faithfulness review at wave review (this is a new literal-derived binding surface — the exact class the standing rule exists for).

## Scope

**Problem statement:** SQL embedded in Java/C# data-access code is invisible to the graph, so code↔database impact questions are unanswerable even when the repo contains both the queries and the DDL they target.

**In scope:**

- Java/C# sink capture (AST-anchored, origin-checked), MyBatis XML mappers, the SQL sniff gate.
- Finalize bind via the `1p9qd` unit with unique-match-or-drop, `LITERAL_DERIVED`, namespaced externals, dynamic refusal with visible counts.
- Locality census on a real corpus with precision sample; exact-set fixtures; version bump.

**Out of scope:**

- JPQL/HQL and entity-based queries (`1p9qg` owns entity→table mapping; JPQL binding would layer on it later).
- Python/Go/TS SQL sinks (the sink table is extensible; start where the operator's enterprise need is — Java/C#).
- SQL string reconstruction across variables/builders (StringBuilder chains, criteria APIs) — refused by design.
- Stored-procedure argument tracing (`CALL proc(?)` binds to the procedure node if `1p9qe` recovered it; tracing arguments does not).

## Acceptance Criteria

- [x] AC-1: Java — each sink form (MyBatis annotations, native `@Query`, `prepareStatement`, `JdbcTemplate` methods) captures its literal and binds method → table for a fixture repo containing matching migration DDL; JPQL `@Query` without `nativeQuery=true` captures nothing. Exact-set unit tests per sink. (`EmbeddedSqlJavaCaptureTests`, 5 tests incl. adjacent-concat and positive-origin negative.)
- [x] AC-2: C# — each sink form (`SqlCommand`/`CommandText`, Dapper, EF raw) captures and binds analogously. Exact-set unit tests per sink. (`EmbeddedSqlCSharpCaptureTests`, exact-set + interpolated-string dynamic refusal.)
- [x] AC-3: MyBatis XML mapper statements bind with the mapper namespace/interface as source. Unit-tested. (`EmbeddedSqlMyBatisXmlTests` — interface-source bind, dynamic-tag/`${…}` refusal with counts, non-mapper XML negative.)
- [x] AC-4: Faithfulness negatives — same-named non-JDBC/non-Dapper impostor methods don't fire; SQL-looking strings outside sinks are never captured; dynamic/concatenated-with-variable SQL refuses and is counted; ambiguous table names (two `users` tables) drop the edge; unmatched tables emit namespaced `external::sql::` targets only. Adversarial unit tests each. (`EmbeddedSqlFaithfulnessTests`, 9 tests incl. schema-qualified-beats-bare and backtick-DDL normalization; `ExternalSqlNamespaceInvariantTests` pins the relation-scoped namespace invariant + fragments-never-carry-bind-edges.)
- [x] AC-5: All bound edges carry `LITERAL_DERIVED` confidence and are down-weighted in impact/path exactly as existing literal edges are (consumer parity test). (`EmbeddedSqlConsumerParityTests` — `_edge_confidence_weight` parity with `reads_config`@LITERAL_DERIVED, `_path_edge_cost` structural tier.)
- [x] AC-6: Locality census recorded in the Progress Log per Requirement 8, including the hand-verified ≥95% precision sample; the default-on decision is explicit in the Decision Log after the census. (Fineract + Tomcat census 2026-07-04; 11/11 = 100% precision on the FULL bind population — smaller than the ≥30-sample the AC contemplated because only 11 binds exist on the corpus; deviation recorded, supplemented with 9 external-target + 5 dynamic-refusal + 2 impostor-refusal hand-checks = 27 verified decisions. **Sample-bar rule** (kept consistent with `1p9qg`'s census, which states the same rule against its own 25-edge bar): hand-verify the FULL bound population whenever that is the more rigorous option available — here 11/11, below this AC's ≥30-edge floor because only 11 binds exist, so the full population is what gets checked rather than a partial sample.)
- [x] AC-7: `GRAPH_BUILDER_VERSION` bumped; adversarial review lane run and findings dispositioned; `python3 .wavefoundry/framework/scripts/run_tests.py` passes; `wave_validate` clean; no `__pycache__` under `scripts/`. (Compound AC — bump portion DONE at wave integration 2026-07-05: coordinated 37→38 bump landed with a changelog naming 1p9qf's contribution (capture fragment keys `sql_capture_candidates`/`sql_capture_dynamic`, `LITERAL_DERIVED` code→table binds, relation-scoped `external::sql::` externals); suite/validate/pycache green post-bump. Remaining clause: the adversarial-faithfulness review lane runs at delivery review (wave review phase) — flip to `[x]` only after its findings are dispositioned. **Flipped 2026-07-05 (post-review-fix round):** the adversarial-faithfulness lane ran and returned verdict PASS WITH FINDINGS (2026-07-05); its findings against this change ($tag$ dollar-tag masking gap, adversarial finding 1) are fixed (`_SQL_RECOVERY_DOLLAR_TAG_RE` + tests); the cross-file origin-check-scope finding (adversarial finding 2) is a recorded, evidence-backed residual (Risks table above), not a defect requiring a fix before close. Full suite green, `wave_validate` clean post-fix-round.)

## Tasks

- [x] Java sink capture in the Java extraction path (annotations via the captured-annotations machinery + argument capture; call-shape sinks via AST with origin checks).
- [x] C# sink capture in the C# extraction path (same discipline).
- [x] MyBatis XML mapper statement capture in markup mode.
- [x] Sniff gate + capture buffering (mirroring `config_read_candidates` plumbing; incremental-merge passthrough pinned by `EmbeddedSqlCaptureIncrementalTests`).
- [x] Finalize bind stage: `1p9qd` unit → unique-match-or-drop → `LITERAL_DERIVED` edges → namespaced externals → dynamic-refusal counters + logging.
- [x] Exact-set + adversarial fixtures per AC-1..AC-5.
- [x] Locality census on a real corpus; precision sample; Decision Log entry for default-on.
- [x] Bump `GRAPH_BUILDER_VERSION`; run `run_tests.py` + `wave_validate`; clean `__pycache__`. (Done at wave integration 2026-07-05: coordinated 37→38 bump with 1p9qf named in the changelog; suite/validate/pycache gates re-verified post-bump. The adversarial review lane — AC-7's other clause — remains for delivery review.)

## Agent Execution Graph


| Workstream | Owner | Depends On | Notes |
| ---------- | ----- | ---------- | ----- |
| ws1-java-capture | implementer | — | Java sinks (annotation args + call shapes, origin-checked) + sniff gate + buffering. |
| ws2-csharp-capture | implementer | — | C# sinks (parallel; different extractor). |
| ws3-mybatis-xml | implementer | — | Mapper XML statement capture (parallel; markup mode). |
| ws4-bind-stage | implementer | ws1-java-capture | Finalize bind via the `1p9qd` unit; unique-match; externals; counters. |
| ws5-tests-census | implementer | ws2-csharp-capture, ws3-mybatis-xml, ws4-bind-stage | Exact-set/adversarial fixtures; real-corpus locality census + precision sample. |
| ws6-adversarial-review | reviewer | ws5-tests-census | Faithfulness red-team: sink impostors, sniff bypasses, wrong-table binds, census honesty. |


## Serialization Points

- Hard dependency on `1p9qd`'s statement-analysis unit (freeze its signature first) and soft dependency on `1p9qc` (clean SQL candidate space) and `1p9qe` (procedure nodes as CALL targets).
- The annotation-argument capture mechanism (ws1) should be built once and shared with `1p9qg` (`@Table` arguments) — coordinate the seam.
- Single wave-level `GRAPH_BUILDER_VERSION` bump.

## Affected Architecture Docs

`docs/specs/mcp-tool-surface.md` capability notes gain the embedded-SQL surface (sinks, confidence, refusal semantics). The census outcome and the default-on decision are decision-record material. Cross-boundary note: this creates the first code→data-layer edge family — worth a short architecture note wherever the graph model is described.

## AC Priority

(Populated at Prepare wave.)


| AC | Priority | Rationale |
| ---- | -------- | --------- |
| AC-1 | required | Java sinks are the primary enterprise surface. |
| AC-2 | required | C# sinks are the operator-directed second surface. |
| AC-3 | important | MyBatis XML is widespread in the Java enterprise but is one idiom among the required set. |
| AC-4 | required | The faithfulness negatives are what keep a literal-derived surface from polluting the graph. |
| AC-5 | required | Confidence parity keeps downstream weighting sound. |
| AC-6 | required | The standing field lesson: literal-edge families ship only with a real-corpus locality census. |
| AC-7 | required | Standing version/adversarial/merge gates. |


## Progress Log


| Date | Update | Evidence |
| ---- | ------ | -------- |
| 2026-07-03 | Scoped from the Java/SQL accuracy investigation. Confirmed: zero existing SQL-in-literal detection (case-insensitive grep of both modules); `reads_config` pipeline is the design template (then `graph_indexer.py:7543-7591,:424`); AOP string-arg capture precedent (then `:4945`); SQL table nodes cross-file resolve (live-verified); annotations captured but arguments not. Field lesson applied: locality census required before literal-edge families ship. | Guru investigation 2026-07-03. |
| 2026-07-04 | Reality-check freshness lane (wave open): zero-sink grep re-confirmed on the current tree (`@Query`/`prepareStatement`/`JdbcTemplate`/`Dapper`/`FromSqlRaw`/`SqlCommand`/`MyBatis` — 0 hits in `scripts/*.py`; `external::sql` unclaimed). Anchors refreshed after `1p9qh`/`1p9q3` drift (finalize bind `:9745-9793`, distinctiveness gate `:445`, AOP capture `:5699`). Incremental-merge compatibility confirmed: capture candidates ride per-file fragments and the finalize bind is recomputed each build over `merge_files` (the `config_read_candidates` pattern) — **implementation note:** any new fragment key must join the incremental-merge fragment-key passthrough list (`graph_indexer.py:9537` region) or cached fragments silently drop it. `external::sql::` mintability caveat added to Requirement 5 (Rust `use sql::…` precedent). `1p9q7` origin-check precedent is still unlanded (wave `1p9q8` `planned`). Coordinated wave version bump is now 37→38. | Reality-check 2026-07-04. |
| 2026-07-04 | **Implemented.** (1) **Capture stage** (`graph_indexer.py`, new "Embedded-SQL capture" section after the C# attribute extractor): Java annotation sinks (`_java_annotation_sql_captures` — MyBatis `@Select/@Insert/@Update/@Delete` incl. array-form values, `@Query` gated on `nativeQuery = true` (JPQL captures nothing), `@NamedNativeQuery.query`; wired at the annotation-attachment site, gated on already-extracted names so the record walk runs only at sinks); Java call sinks (`_java_call_sql_capture` — `prepareStatement`/`prepareCall` with negative origin check, `JdbcTemplate`/`NamedParameterJdbcTemplate` methods with POSITIVE receiver-type origin check); C# sinks (`_csharp_call_sql_capture` — ADO.NET `new SqlCommand(…)` family, Dapper extension methods, EF `FromSqlRaw`/`ExecuteSqlRaw(Async)`/`SqlQueryRaw`; `_csharp_commandtext_sql_capture` for `<expr>.CommandText = "…"` assignments); MyBatis mapper XML (`_mybatis_mapper_captures` in markup mode — statement text with `mybatis::<ns>::<id>` source markers, dynamic-tag/`${…}` refusal). Sniff gate `_sql_text_sniffs_as_sql` (Requirement-4 keyword set) applied at every capture; adjacent literal `+`-concatenation folded (`_java_literal_string_expr`/`_csharp_literal_string_expr`, incl. text blocks/verbatim/raw strings); anything touching a variable refused and counted per file (`sql_capture_dynamic`). (2) **Fragment plumbing:** `sql_capture_candidates` + `sql_capture_dynamic` ride the per-file artifact and joined the incremental-merge passthrough list; pinned by `EmbeddedSqlCaptureIncrementalTests` (bind edge + refusal count survive a cached-fragment merge; schema-twin flip drops/restores the bind in an UNTOUCHED file, oracle-equivalent). (3) **Bind stage** (finalize, after the config-read pass): dedup captures → `#{…}`→`?` sanitize → frozen `sql_statement_references` unit → per reference: qualified-exact then bare/leaf match against `sql_kind` table/view nodes, unique → `reads`/`writes` @ `LITERAL_DERIVED`, ambiguous → drop (counted), none → `external::sql::<name>`; identifier-quote normalization on both match sides (census-driven, see below); counters in `merge_stats.sql_capture` + verbose build-log line. MyBatis sources resolve namespace→interface-method→interface→XML-module, unique-or-fallback. (4) **Origin-check convention defined (1p9q8 mirrors it):** distinctive sink names (prepare*, EF raw, `new SqlCommand`) = NEGATIVE check (refuse only when the receiver/type resolves to a project-defined symbol — same-file `symbol_lookup`); generic names (`query`/`update`/`execute`) = POSITIVE check (receiver type must resolve to the known library type); Dapper (open receiver-type set) = negative check + sniff, census-measured. (5) **Tests:** 26 new (24 in `test_graph_indexer.py` across 7 classes, 2 in `test_graph_incremental_merge.py`); full suite 4,573/43 green (baseline 4,541; +26 this lane, remainder from the parallel `1p9qe` lane); `wave_validate` clean; no `__pycache__`. `GRAPH_BUILDER_VERSION` bump + adversarial lane deferred to wave integration per serialization points. | `graph_indexer.py`; `test_graph_indexer.py`, `test_graph_incremental_merge.py`; venv runs 2026-07-04. |
| 2026-07-04 | **Locality census (AC-6, Requirement 8) — real corpora, working-tree indexer.** **Corpus A: Apache Fineract @ 2e3b96df** (4,454 files: 4,272 .java, 23 .sql with 749 raw CREATE TABLE statements, 170 JdbcTemplate files — enterprise financial platform). Result: 24 sniff-passing captured literals, **11 bound** (unique-match), **19 external** (9 distinct `external::sql::` targets), **1 ambiguous-dropped**, **561 dynamic-refused**. **Precision: 11/11 bound edges hand-verified correct = 100%** (full population — only 11 binds exist on the corpus, so the ≥30-edge sample the AC contemplated is not reachable; deviation covered by hand-checking the FULL population plus 9 external targets — all genuinely schema-absent: Spring Batch metadata tables `BATCH_JOB_*` and Liquibase-era Fineract tables (`m_loan_account_locks`, `m_share_account`, …) with no in-repo `.sql` DDL — plus 5 sampled dynamic refusals (all genuine `String sql = …`/StringBuilder idioms) = 27 verified decisions). Verified binds include the native `@Query` text-block write to `m_loan`, `NamedParameterJdbcTemplate` adjacent-concat read of `schema_version`, and JdbcTemplate reads/writes of `m_savings_account`/`m_loan_arrears_aging`/`x_registered_table` — aliases (`mla`, `l`) never polluted. **Census-driven fix shipped:** MySQL dump DDL registers backtick-quoted names (`` `m_loan` ``) while embedded SQL references bare names — bind-side identifier-quote normalization added (`_sql_normalize_object_name`, bind-local; the frozen 1p9qd names-as-written contract untouched); before the fix 0/24 bound, after 11 bound. **Corpus B: Apache Tomcat 9.0.84 `java/`** (1,823 files, the 1p9qh census precedent; no in-repo DDL): **0 captures, 89 dynamic refusals, 0 false positives** — hand-verified faithful: all 111 `prepareStatement` sites pass variables (SQL assembled from configurable table/column names, e.g. `JDBCAccessLogValve`), and dbcp2's own `prepareStatement` wrapper methods were correctly refused by the project-impostor origin check. **Observations recorded:** (a) sniff-keyword set omits TRUNCATE/ALTER/DROP — a live Fineract site (`jdbcTemplate.execute("truncate table m_loan_arrears_aging")`) drops silently; kept to the Requirement-4 spec set this change, extension is a recorded follow-up; (b) Fineract's unique binds partially depend on the backup-dump `.sql` files failing to parse (mysqldump headers) — if a future recovery tier registers those duplicate definitions, affected binds DROP to ambiguous (never wrong-bind), the designed dev/prod-DDL behavior (multi-candidate name evidence: `m_client*` families defined in both sample-data files already drop today — the 1 ambiguous count); (c) `docs/specs/mcp-tool-surface.md` capability note + graph-model architecture note deferred to wave integration (shared-doc seam with the parallel lanes). Census artifacts: `~/.claude/jobs/e29ad14d/tmp/census-1p9qf/` (`build_census.py`, `census-fineract.json`, `census-tomcat.json`). | Census runs 2026-07-04 (venv, fresh subprocesses). |
| 2026-07-04 | **Shared seam for 1p9qg** (annotation/attribute-argument capture — build-once contract): `_ts_java_annotation_records(node, source_bytes)` and `_ts_csharp_attribute_records(node, source_bytes)` return, for any Java/C# declaration node, a list of `{"name": str, "args": [value_node, …], "pairs": {ident: value_node}}` — `name` verbatim (possibly qualified; match by trailing segment), `args` = positional argument AST nodes in order, `pairs` = named arguments (`element_value_pair` idents on Java; `Name = expr` AND `name: expr` forms on C#). Values stay **AST nodes**, not strings, so each consumer applies its own interpretation: string values via `_java_literal_string_expr`/`_csharp_literal_string_expr` (literal + adjacent-concat folding, text-block/verbatim/raw handling), booleans/enums via `_ts_node_text` on the node. For `1p9qg`: `@Table(name = "users")` → `records` where `name.rsplit('.',1)[-1] == "Table"`, table name = `_java_literal_string_expr(pairs["name"])`; EF `[Table("users")]` → `args[0]` via `_csharp_literal_string_expr`. Both are pinned by `AnnotationArgumentSeamTests` (shape + value extraction on both languages). The pre-existing name-only extractors (`_ts_extract_java_annotations`/`_ts_extract_csharp_attributes`) are untouched — the records helpers are additive and only invoked at sink-gated call sites. | `graph_indexer.py` seam helpers; `AnnotationArgumentSeamTests`. |


## Decision Log


| Date | Decision | Reason | Alternatives |
| ---- | -------- | ------ | ------------ |
| 2026-07-03 | Two-stage capture/bind mirroring `reads_config`, sink-gated, unique-match-or-drop, `LITERAL_DERIVED` (approach A). | Reuses the proven faithful-literal-binding discipline end to end; sink gating keeps capture O(known idioms) instead of O(all strings); the `1p9qd` unit provides table extraction without a second SQL parser; namespaced externals prevent cross-domain symbol collisions. | (B) Repo-wide SQL-sniffing of all string literals — weakness: false-positive surface scales with every string in the repo; sink knowledge is what makes precision achievable. (C) Regex-only capture (no AST) — weakness: the exact string/comment failure mode `1p9q7` rejects; both extractors already hold trees. (D) Defer until a SQL-lineage product need is proven — weakness: the operator has named the enterprise need now; the census requirement bounds the downside. |
| 2026-07-03 | Native-SQL only; JPQL excluded pending `1p9qg`. | JPQL names entities, not tables; binding it to tables requires the entity→table mapping layer — doing both in one change entangles two evidence classes. | Include JPQL via naive entity-name≈table-name matching — rejected: that is a guess, precisely what the confidence taxonomy forbids. |
| 2026-07-03 | `external::sql::` namespace for unmatched tables. | A bare `external::users` could collide with a host-language symbol candidate set; namespacing keeps the SQL domain's externals disjoint by construction. | Bare externals — rejected: cross-domain candidate pollution is a silent-wrong-bind vector the adversarial review would flag anyway. |
| 2026-07-04 | **DEFAULT-ON: the capture+bind stage ships enabled** (census evidence, AC-6). | Precision on the real corpus is 100% (11/11 hand-verified binds on Apache Fineract, full population; 0 false positives on Apache Tomcat where the entire JDBC surface is dynamic/impostor and was correctly refused — 89 dynamic refusals + dbcp2 wrapper impostor refusals). The failure vectors are structurally bounded: sniff gate + origin checks + unique-match-or-drop + `LITERAL_DERIVED` down-weighting + namespaced externals; recall on concat-heavy codebases is modest by DESIGN (561 visible dynamic refusals on Fineract) and the gap is counted in build stats, not silent. **Capture-cost measurement (1p9qi review, replacing the earlier unmeasured "unchanged within noise" claim):** Java-heavy corpora pay a real, small extraction tax — Apache Fineract (4,272 `.java` files, min-of-N-runs vs median-of-N-runs on the working-tree indexer) measured +1.3–2.6% extraction wall-time from the added sink/annotation-argument walking; non-SQL/non-JPA repos (Tomcat) stayed within run-to-run noise, as expected (the capture gates never fire). The SQL-heavy corpus (the shared 4-file 1p9qd/1p9qc calibration fixture) is net FASTER, not slower: -33% wall time / -93% edge count, because the clause-aware rewrite this wave also ships eliminates far more phantom-edge work than the new capture stage adds. | Ship disabled behind a config flag — rejected: census evidence shows no wrong-bind vector to guard against, and a dark-launched literal family would never accumulate the field evidence the wave exists to produce. Ship external-only (no project binds) — rejected: the 11 verified Fineract binds are exactly the code↔table impact facts the enterprise ask names. |
| 2026-07-04 | Bind-side identifier-quote normalization (`_sql_normalize_object_name` — strip `` ` ``/`"`/`[`/`]` on BOTH match sides + the minted external name); the 1p9qd unit's names-as-written output is untouched. | Census-driven: MySQL dump DDL registers `` `m_loan` `` while embedded Java SQL references bare `m_loan` — without normalization 0/24 Fineract captures bound; with it, 11. Normalizing at the bind consumer keeps the frozen statement-unit contract intact and leaves SQL-file-internal resolution (same quoting style on both sides) unchanged. | Normalizing inside the statement unit — rejected: renegotiates the frozen 1p9qd contract mid-wave and changes the SQL file-extraction node names other consumers already pin. |
| 2026-07-04 | Sniff-keyword set kept exactly to Requirement 4 ({SELECT, INSERT, UPDATE, DELETE, WITH, MERGE, CALL, EXEC}); TRUNCATE/ALTER/DROP extension recorded as follow-up, not shipped. | The set is the reviewed spec; extending it mid-implementation would be silent scope expansion. The census measured the cost precisely: one live Fineract site (`execute("truncate table m_loan_arrears_aging")`) drops silently — a bounded, recorded recall gap. | Extend now — rejected: cheap (one frozenset entry + tests) but belongs to an operator-approved follow-up with its own negative tests (bare `ALTER`/`DROP` prose strings are a new false-positive class the current set cannot hit). |


## Risks


| Risk | Mitigation |
| ---- | ---------- |
| Sink impostors (user types with `Query`/`Execute` methods) over-fire. | Origin checks where the receiver/namespace resolves; sniff gate as second filter; refusal when origin is ambiguous; adversarial impostor tests (AC-4); `LITERAL_DERIVED` down-weighting bounds damage. |
| **Recorded residual (1p9qi review, adversarial finding 2):** the origin check's receiver/type resolution scope is SAME-FILE `symbol_lookup` only — a project-defined impostor sink class (e.g. a class literally named `JdbcTemplate` with a `query(String)` method) declared in a DIFFERENT file than the call site is outside the same-file check's reach at capture time. Accepted, not fixed. | Real-corpus census (Fineract + Tomcat) found zero false positives from this gap: a cross-file class shaped like a sink is overwhelmingly a genuine wrapper around the real library, not an unrelated same-named type, so the captured literal still sniffs as SQL and binds to a real table when it binds at all. Broadening to full cross-file symbol resolution is future field-demand work (see the origin-check convention comment in `graph_indexer.py`). |
| Schema not in repo → mostly `external::sql::` edges, low perceived value. | Explicitly anticipated: census (AC-6) measures it, externals are still honest "this code runs SQL against table X" facts, and the default-on decision is made on the census evidence, not hope. |
| Table-name ambiguity across schemas/environments (dev vs prod DDL both in repo). | Schema-qualified match first; bare-name binds only on repo-wide uniqueness; ambiguity drops. The migration fixture in `1p9qd` AC-7 doubles as the multi-DDL testbed. |
| Capture stage slows Java/C# extraction. | Sinks are checked only at annotation/call nodes already visited; sniff is O(prefix); measured in the calibration pass. |


## Session Handoff

See `docs/agents/session-handoff.md` for current session state.
