# ORM mapping: bind JPA and EF entity classes to SQL table nodes via declared table-name annotations

Change ID: `1p9qg-enh orm-entity-table-mapping`
Change Status: `implemented`
Owner: Engineering
Status: implemented
Last verified: 2026-07-05
Wave: TBD

## Rationale

ORM entities are the other half of code↔database visibility: `1p9qf` covers explicit SQL, but most enterprise data access flows through JPA/Hibernate (Java) and EF Core (C#) entities, where the table name lives in a declaration — Java `@Entity` + `@Table(name = "users")`, C# `[Table("users")]` (System.ComponentModel.DataAnnotations.Schema) or `ToTable("users")` fluent configuration. The Java extractor already captures annotation **names** as a node property (`_ts_extract_java_annotations`, `graph_indexer.py:5926`, attached at `:8387-8390`) but not their **arguments**, so the mapping fact is seen and discarded. The C# extractor likewise already captures attribute **names** (`_ts_extract_csharp_attributes`, surfaced as `annotations` for Java parity, attached at `:8391-8395`) — so Requirement 1's C# argument capture extends an existing seam, mirroring the Java side.

Binding entity class → table node (declared-name, unique-match, `LITERAL_DERIVED`) makes impact flow across the object-relational seam: refactoring a table shows the entities riding it and — through existing `calls` edges into repository/service layers — the code above them. It also lays the ground for future JPQL binding (deliberately excluded from `1p9qf`): once entities map to tables, entity-referencing queries can bind transitively without guessing.

Scope discipline: declared names only. Convention-derived names (JPA's default snake-casing of class names, EF pluralization conventions) are refused in this change — deriving them means reimplementing each ORM's naming strategy, wrong versions of which produce exactly the silent mis-binds the confidence taxonomy forbids. Convention support, if ever, arrives as its own measured change.

## Requirements

1. **Annotation-argument capture.** The Java annotation machinery gains argument capture for a fixed vocabulary (`@Table(name=...)`, `@Entity(name=...)`, plus `@NamedNativeQuery` already relevant to `1p9qf`) — built as the shared seam `1p9qf` ws1 also uses. C# attribute arguments (`[Table("...")]`) capture analogously in the C# extractor; EF `ToTable("...")` call-shape capture follows the `1p9qf` sink pattern with origin checks.
2. **Mapping edge.** An entity class with a declared table name binds class → table node (`maps_to`-family edge, or `reads` with a mapping property — same representation decision process as `1p9qd`'s write edges, decided by consumer sweep) at `LITERAL_DERIVED` confidence, unique-match-or-drop against SQL-defined tables (schema-qualified first via `@Table(schema=...)` when present); no match → `external::sql::<table>` (the `1p9qf` namespace).
3. **Refusals.** `@Entity` with no `@Table` and no explicit name binds nothing (convention refusal, logged as a counted unbound-convention case so the gap is visible); dynamic/computed names refuse; two tables matching one declared name refuse.
4. **Impact semantics.** The mapping edge participates in impact traversal (table → entities → their callers) at literal-edge weighting; path search treats it as structural.
5. **Version bump + census + adversarial review.** `GRAPH_BUILDER_VERSION` bumped; the mapping shares `1p9qf`'s real-corpus locality census (entity counts, declared-name rate vs convention rate, bind rate, ≥95% precision hand-sample); adversarial review lane at wave review.

## Scope

**Problem statement:** Entity classes' declared table mappings are parsed but discarded (names captured, arguments dropped), leaving the object-relational seam invisible even when both the entity and the DDL are in-repo.

**In scope:**

- Annotation/attribute argument capture (shared seam with `1p9qf`); EF `ToTable` sink.
- The mapping edge with unique-match/refusal semantics, namespaced externals, impact weighting.
- Convention-refusal counters; census participation; fixtures; version bump.

**Out of scope:**

- Convention-derived table names (JPA implicit naming, EF pluralization) — explicit standing refusal in this change; revisit only with a measured, per-ORM strategy proposal.
- JPQL/HQL query binding (future layer on top of this mapping).
- Column-level mapping (`@Column`) — table-level is the impact tier.
- Other ORMs (Hibernate XML mappings, Doctrine, ActiveRecord) — extensible vocabulary, added on field evidence.

## Acceptance Criteria

- [x] AC-1: Java — `@Entity @Table(name="users")` (and schema-qualified form) binds the class to the unique matching table node with the chosen mapping representation at `LITERAL_DERIVED`; `@Entity` alone binds nothing and increments the convention counter. Exact-set unit tests. (`OrmEntityMappingJavaTests`, 6 tests — collapsed + nested entity ids, schema-qualified, `@Entity(name=…)`, convention temptation refused, constant-ref dynamic refusal, bare-`@Table`-without-`@Entity` negative, JPQL-untouched negative.)
- [x] AC-2: C# — `[Table("users")]` and `ToTable("users")` (origin-checked) bind analogously; impostor `ToTable` on a non-EF type refuses. Unit tests. (`OrmEntityMappingCSharpTests`, 5 tests — `[Table]`/Schema, both ToTable origin forms (`.Entity<T>()` chain + `EntityTypeBuilder<T>` param), impostor refusal, variable-name + `nameof` dynamic refusals.)
- [x] AC-3: Refusals — ambiguous table match drops; computed/constant-reference names refuse (only string literals bind); unmatched names emit `external::sql::` targets. Adversarial unit tests. (`OrmEntityMappingRefusalTests`, 4 tests — dev/prod twin drop, schema-qualified-beats-bare-twin, namespaced-external-only, ambiguous entity type drop; `ExternalSqlNamespaceInvariantTests` extended to the `maps_to` relation scope.)
- [x] AC-4: Impact — `code_impact` on a table reaches its mapped entities and their existing callers at down-weighted confidence (fixture graph test). (`OrmEntityMappingImpactTests` — table → entity (hop 1, `maps_to`) → constructing caller (hop 2, `calls`); explicit `relations=("calls",)` opts out; weight/path-cost/`_SQL_DATA_LAYER_RELATIONS` parity pins; report fan_in counts mappings.)
- [x] AC-5: Census — the `1p9qf` corpus census includes entity mapping metrics (declared vs convention rate, bind rate, precision sample) recorded in the Progress Log; the annotation-argument seam is shared with `1p9qf`, verified by both changes' tests passing on one build. (Fineract + Tomcat census 2026-07-05, Progress Log; seam sharing pinned by `OrmEntityMappingSeamAndFragmentTests.test_shared_seam_serves_both_changes_on_one_build` + the full-file run where all `1p9qf` and `1p9qg` tests pass on one build.)
- [x] AC-6: `GRAPH_BUILDER_VERSION` bumped; adversarial review lane run and findings dispositioned; `python3 .wavefoundry/framework/scripts/run_tests.py` passes; `wave_validate` clean; no `__pycache__` under `scripts/`. (Compound AC — bump portion DONE at wave integration 2026-07-05: coordinated 37→38 bump landed with a changelog naming 1p9qg's contribution (NEW `maps_to` relation + fragment keys `orm_entity_candidates`/`orm_entity_dynamic`/`orm_entity_convention`); suite/validate/pycache green post-bump. Remaining clause: the adversarial-faithfulness review lane runs at delivery review (wave review phase) — flip to `[x]` only after its findings are dispositioned. **Flipped 2026-07-05 (post-review-fix round):** the adversarial-faithfulness lane ran and returned verdict PASS WITH FINDINGS (2026-07-05); its two findings ($tag$ dollar-tag masking, adversarial finding 1 — shared with `1p9qf`; cross-file impostor shape, adversarial finding 2 — this change's origin-check convention shares the same same-file-scope limitation) are now fixed/recorded: the masking gap is fixed at the shared `graph_indexer.py` mechanism; the cross-file origin-check scope is a recorded, evidence-backed residual (zero real-corpus false positives), not a defect requiring a fix before close. Full suite green, `wave_validate` clean post-fix-round.)

## Tasks

- [x] Build/extend the annotation-argument capture seam (Java) and attribute-argument capture (C#); EF `ToTable` sink with origin check. — Consumed the `1p9qf`-built seam unchanged (`_ts_java_annotation_records`/`_ts_csharp_attribute_records`); added the interpretation layer (`_java_entity_table_mapping`/`_csharp_entity_table_mapping`) + the `ToTable` sink (`_csharp_totable_capture`, two positive-origin forms).
- [x] Mapping-edge emission with unique-match/refusal semantics + namespaced externals + convention counters; representation decided by the shared consumer sweep. — New `maps_to` relation (`GRAPH_MAPS_TO_RELATION`), finalize-only bind pass sharing the `1p9qf` `sql_kind` bind index; `merge_stats.entity_mapping` counters.
- [x] Impact/path weighting for the mapping edge. — `maps_to` joined `graph_query._SQL_DATA_LAYER_RELATIONS` (impact data-layer exception + report fan counting); path cost structural tier and blast-radius down-weighting inherited by construction, parity-pinned.
- [x] Fixtures/tests per AC-1..AC-4; census metrics with `1p9qf`. — 20 new tests in `test_graph_indexer.py` (5 classes) + 2 in `test_graph_incremental_merge.py`; Fineract/Tomcat census in the Progress Log.
- [x] Bump `GRAPH_BUILDER_VERSION`; run `run_tests.py` + `wave_validate`; clean `__pycache__`. (Done at wave integration 2026-07-05: coordinated 37→38 bump with 1p9qg named in the changelog; suite/validate/pycache gates re-verified post-bump. The adversarial review lane — AC-6's other clause — remains for delivery review.)

## Agent Execution Graph


| Workstream | Owner | Depends On | Notes |
| ---------- | ----- | ---------- | ----- |
| ws1-argument-capture | implementer | — | Annotation/attribute argument seam (shared with `1p9qf`); EF sink. |
| ws2-mapping-edges | implementer | ws1-argument-capture | Edge emission, refusals, externals, counters, weighting. |
| ws3-tests-census | implementer | ws2-mapping-edges | Fixtures; census metrics alongside `1p9qf`. |
| ws4-adversarial-review | reviewer | ws3-tests-census | Faithfulness red-team: convention temptation, impostor sinks, wrong-table binds. |


## Serialization Points

- The annotation-argument seam is shared with `1p9qf` ws1 — build once, in whichever change lands first; the other consumes it.
- The mapping-edge representation follows the same consumer-sweep decision as `1p9qd`'s write edges — one sweep, both decisions.
- Shares the census corpus and pass with `1p9qf` (one census, two metric sections).
- Single wave-level `GRAPH_BUILDER_VERSION` bump.

## Affected Architecture Docs

`docs/specs/mcp-tool-surface.md` capability notes: the entity→table mapping surface and its refusal stance (declared names only — the convention refusal is a standing decision worth recording where the graph model is described, so it isn't re-litigated per wave).

## AC Priority

(Populated at Prepare wave.)


| AC | Priority | Rationale |
| ---- | -------- | --------- |
| AC-1 | required | JPA declared-name mapping is the primary surface. |
| AC-2 | required | C# parity per the wave's operator-directed scope. |
| AC-3 | required | The refusal set is what separates this from a naming-convention guesser. |
| AC-4 | required | The mapping edge exists to make impact flow; untested weighting would corrupt results. |
| AC-5 | required | Standing literal-edge census rule; the shared seam must be proven shared. |
| AC-6 | required | Standing version/adversarial/merge gates. |


## Progress Log


| Date | Update | Evidence |
| ---- | ------ | -------- |
| 2026-07-03 | Scoped from the Java/SQL accuracy investigation. Confirmed: annotation names captured but arguments discarded (then `graph_indexer.py:5105,6625-6628`); no ORM idiom coverage anywhere (grep-confirmed); SQL table nodes resolvable (live-verified); design mirrors `reads_config`/`1p9qf` discipline with declared-names-only stance. | Guru investigation 2026-07-03. |
| 2026-07-04 | Reality-check freshness lane (wave open): anchors refreshed after `1p9qh` drift (annotation extractor `:5926`, Java attachment `:8387-8390`); confirmed C# attribute-name capture already exists (`_ts_extract_csharp_attributes`, attached `:8391-8395`, surfaced as `annotations`) — the C# side of the shared argument-capture seam extends existing machinery. The mapping-edge representation decision shares `1p9qd`'s consumer sweep, which (per its refreshed Serialization Points) now explicitly includes `_GRAPH_SIGNAL_RELATIONS` (`server_impl.py:251`) and the landed `extends`/`implements` vocabulary text. Coordinated wave version bump is now 37→38. | Reality-check 2026-07-04. |
| 2026-07-05 | **Implemented.** (1) **Capture** (`graph_indexer.py`): consumes the `1p9qf` seam records unchanged — `_java_entity_table_mapping` (`@Entity` gate + `@Table(name/schema)` / `@Entity(name)` literals via `_java_literal_string_expr`; same-file impostor check against project-defined `Entity`/`Table` types), `_csharp_entity_table_mapping` (`[Table("…")]` positional + `Schema` pair; `Table`/`TableAttribute` impostor check), `_csharp_totable_capture` (EF fluent sink, POSITIVE origin by construction: `.Entity<T>()` receiver chain OR enclosing-method `EntityTypeBuilder<T>` parameter; `entitytype::<T>` source marker resolved at finalize, unique-class-or-drop). **Critical placement fact:** entity classes are buffered in `register_symbol` BEFORE the basename-collapse early return — a file-dominant entity (`User.java` defining `class User`, the normal JPA layout) merges into the module node and never reaches the existing annotation-attachment block; captures drain post-walk when `symbol_lookup` exists. (2) **Fragments:** `orm_entity_candidates` + `orm_entity_dynamic` + `orm_entity_convention` ride the per-file artifact and joined the incremental-merge passthrough list; pinned by `OrmEntityMappingIncrementalTests` (mapping + counters survive a cached-fragment merge; schema-twin flip drops/restores the bind in an untouched file, oracle-equivalent). (3) **Bind** (finalize, right after the `1p9qf` embedded-SQL pass, SHARING its `sql_kind` bind index + `_sql_normalize_object_name` quote normalization): new `maps_to` relation at LITERAL_DERIVED, normalized qualified-exact then unique bare/leaf, ambiguity drops, no match mints `external::sql::<name>` (the relation-scoped invariant now covers reads/writes/maps_to — `ExternalSqlNamespaceInvariantTests` extended); counters in `merge_stats.entity_mapping` + verbose build-log line. (4) **Consumers:** `maps_to` joined `graph_query._SQL_DATA_LAYER_RELATIONS` (default-impact data-layer exception + report fan counting, both `sql_kind`-gated) and `server_impl._GRAPH_SIGNAL_RELATIONS` + label/bucket maps (`mapped_entity`/`mapped_entities`); path cost structural, blast-radius weight 0.25 via LITERAL_DERIVED (parity-pinned); clustering untouched (dict-default weight 1, the `writes` precedent). (5) **Tests:** 22 new (20 in `test_graph_indexer.py` across 5 classes incl. AC-4's table→entity→caller impact chain with `relations=("calls",)` opt-out; 2 in `test_graph_incremental_merge.py`); `test_server_tools.py` signal pin updated. **Flip evidence** (both directions): the identical 8-fixture probe set yields 6 binds + convention/dynamic counters on the new tree and ZERO `maps_to` edges / no counters on the pre-change HEAD tree (`/tmp/flip-1p9qg` probe run 2026-07-05). Full suite 4,595/43 green (+22 over the 4,573 baseline); `wave_validate` clean; no `__pycache__`. | `graph_indexer.py`, `graph_query.py`, `server_impl.py`; `test_graph_indexer.py`, `test_graph_incremental_merge.py`, `test_server_tools.py`; venv probe runs 2026-07-05. |
| 2026-07-05 | **Findings for wave integration:** (a) `GRAPH_BUILDER_VERSION` 37→38 REQUIRED by this change at the coordinated bump (new `maps_to` relation + new fragment keys `orm_entity_candidates`/`orm_entity_dynamic`/`orm_entity_convention` — cached consumer graphs must re-extract); the integration changelog must mention 1p9qg. (b) `docs/specs/mcp-tool-surface.md` capability notes (entity→table mapping surface + the declared-names-only refusal stance where the graph model is described) deferred to wave integration per the shared-doc seam — the `docs/architecture/graph-index-system.md` relation-table row for `maps_to` WAS added in-lane (the `1p9qd` precedent: a live relation must not be undocumented), including the standing convention-refusal decision so it isn't re-litigated per wave. (c) `server_impl.py` changed (`_GRAPH_SIGNAL_RELATIONS` + label/bucket maps) — a long-lived MCP server session needs `wave_mcp_reload` to serve the new buckets. (d) JPQL binding remains the deliberate next layer: entity→table now exists, so entity-referencing JPQL can bind transitively in a future change without naming guesses. (e) Fineract observation for any future convention proposal: the census's 144 externals are dominated by Liquibase-XML-defined tables (no in-repo `.sql` DDL) — a Liquibase-changelog definition source, not name conventions, is the higher-recall follow-up there. | Implementation observations 2026-07-05. |
| 2026-07-05 | **Locality census (AC-5) — shared 1p9qf corpora, working-tree indexer.** **Corpus A: Apache Fineract @ 2e3b96df** (4,454 files; 214 `@Entity` classes; 210 `@Table(name=…)` declarations — grep-confirmed): **209 declared-name candidates (97.7% declared rate)**, **4 convention refusals** (grep-verified exactly the 4 `@Entity`-without-`@Table` JPA-inheritance subclasses: `FixedDeposit*`/`RecurringDeposit*`), **1 dynamic refusal** (verified: `AccountNumberFormat.java`'s `@Table(name = AccountNumberFormatConstants.ACCOUNT_NUMBER_FORMAT_TABLE_NAME)` — the exact constant-reference case AC-3 refuses). Binds: **28 bound / 144 external / 37 ambiguous-dropped**. **Precision: 28/28 hand-verified correct = 100% on the FULL bound population** (mechanical check per edge: the entity file declares exactly the target table name AND the target `.sql` file contains its `CREATE TABLE`; population 28 ≥ the 25-edge sample bar, so no deviation). **Sample-bar rule** (kept consistent with `1p9qf`'s census, which states the same rule against its own ≥30-edge bar): hand-verify the FULL bound population whenever that is the more rigorous option available — here 28/28, at/above this AC's 25-edge floor, so the full population is what gets checked rather than stopping at a partial sample of 25. Externals verified honest: 6/6 sampled external targets (`glim_accounts`, `interop_identifier`, …) have ZERO in-repo `CREATE TABLE` anywhere — modern Fineract schema lives in Liquibase XML changelogs, not `.sql`, so the external edges are true "maps to a table this repo's SQL surface doesn't define" facts. Ambiguity verified honest: `m_client`-family tables are defined in 7 tenant-dump files (partially parseable) — dropped, never wrong-bound (the designed dev/prod-DDL behavior, same as `1p9qf`'s finding (b)). All 28 binds target the one fully-parseable legacy DDL (`0001a-mifosplatform-core-ddl-latest.sql`), backtick-normalized via the shared `_sql_normalize_object_name`. **Corpus B: Apache Tomcat 9.0.84 `java/`** (negative control, no JPA/EF): **0 candidates, 0 counters, 0 false positives** — the capture gates (`@Entity` presence / `Table` attribute / ToTable positive origin) fire nowhere on a JPA-free enterprise codebase. Census artifacts: `~/.claude/jobs/e29ad14d/tmp/census-1p9qg/` (`build_census_orm.py`, `census-orm-fineract.json`, `census-orm-tomcat.json`). | Census runs 2026-07-05 (venv, fresh subprocesses). |


## Decision Log


| Date | Decision | Reason | Alternatives |
| ---- | -------- | ------ | ------------ |
| 2026-07-03 | Declared table names only; conventions refused and counted (approach A). | Convention derivation means reimplementing each ORM's (version-dependent) naming strategy; a wrong strategy silently mis-binds — the exact failure the confidence taxonomy exists to prevent. Counting refusals makes the recall cost measurable, so a future convention change is evidence-based. | (B) Implement JPA/EF default naming strategies — weakness: version- and configuration-dependent (physical naming strategies are pluggable); silent wrongness at scale. (C) Fuzzy name matching (class `User` ≈ table `users`) — weakness: a guess with a similarity threshold; rejected outright. |
| 2026-07-03 | Separate change from `1p9qf` despite the shared seam. | Different evidence classes (declaration facts vs query-text analysis) with different failure modes and different census metrics; the shared argument-capture seam is a build-once coordination point, not a reason to entangle review scopes. | Fold into `1p9qf` — rejected: would make the wave's largest change larger and blur two distinct faithfulness arguments. |
| 2026-07-05 | **DECIDED: new `maps_to` relation** (entity class → table node, LITERAL_DERIVED, finalize-only) — the same consumer-sweep process and outcome as `1p9qd`'s `writes` decision. **Sweep evidence:** (1) every downstream consumer filters by RELATION (`_SQL_DATA_LAYER_RELATIONS` for impact/report, `_GRAPH_SIGNAL_RELATIONS` for the code_ask signal, `_path_edge_cost`, `_edge_confidence_weight`) — a property on `reads` would hide the mapping from all of them; (2) a mapping is NEITHER a read nor a write — it is a declaration fact with no query text behind it, so `reads`(+`writes`) edges would overstate the evidence class (the "reads is semantically false for a DELETE target" argument, one level up); (3) riding `reads` would also entangle the 1p4ls constant-read policies (opt-in 1-hop, clustering exclusion, constant-gated fragment resolution). Consumer registrations: joined `_SQL_DATA_LAYER_RELATIONS` (impact data-layer exception delivers AC-4 for free, sql_kind-gated; report fan counting) and `_GRAPH_SIGNAL_RELATIONS` (`mapped_entity`/`mapped_entities` buckets — "what uses table X" includes its entities; sparse, high-value, NOT neighbor-opt-in like `writes`); path cost structural tier and blast-radius down-weight (0.25 via LITERAL_DERIVED) inherited by construction, parity-pinned; clustering needs no change (dict-default weight 1, the `writes` precedent). Bind-pass placement: finalize-only, after and SHARING the `1p9qf` pass's `sql_kind` bind index + quote normalization — edges never enter fragments, so no `_resolve_fragment_edge` routing is needed and the `external::sql::` relation-scoped invariant extends cleanly to `maps_to`. | `reads` + `mode`/mapping property — rejected (hidden from relation filters; false semantics; 1p4ls entanglement). Emitting `reads`+`writes` pairs per mapping — rejected: doubles edge count to state one declaration fact and fabricates query-evidence the capture never saw. |
| 2026-07-05 | `@Entity(name = "…")` binds (when `@Table` declares no name); bare `@Entity` refuses. | The JPA spec defaults the table name to the ENTITY name verbatim, and an explicit `@Entity(name=…)` is a developer-declared string literal — a declaration fact, not a derived convention (deriving from the CLASS name is where the refusal line sits). A pluggable physical naming strategy could still transform it, but unique-match-or-drop bounds that risk and the census measured zero such cases (Fineract uses `@Table(name=…)` for 210 of 214 entities). Requirement 1 lists `@Entity(name=...)` in the capture vocabulary and Requirement 3 scopes the refusal to "no `@Table` and no explicit name". | Refusing `@Entity(name=…)` too — rejected: it refuses a literal declaration the requirement text explicitly includes, for a risk class (physical-strategy transforms) the unique-match gate already bounds. |
| 2026-07-05 | EF `ToTable` origin = positive identification by construction: bind only when the entity type is recoverable from the `.Entity<T>()` receiver chain or an enclosing-method `EntityTypeBuilder<T>` parameter; anything else is not a sink (silent). | `ToTable` alone is not distinctive enough for a negative check (any report/export API can carry the name), and the capture is USELESS without the entity type anyway — so requiring the type-bearing shape IS the origin check (the origin-check convention's generic-name arm). The two shapes cover the standard `OnModelCreating` and `IEntityTypeConfiguration<T>` idioms; the census negative control (Tomcat) confirms zero impostor fires. | Negative same-file impostor check only — rejected: without the entity type there is nothing to bind, and cross-file receiver types are unresolvable at capture time. Supporting local-variable `EntityTypeBuilder` bindings — deferred: adds flow analysis for an idiom the two positive shapes already dominate. |
| 2026-07-05 | **DEFAULT-ON: the entity-mapping capture+bind ships enabled** (census evidence, AC-5). | Precision on the real corpus is 100% (28/28 hand-verified binds on Apache Fineract — the FULL bound population); every refusal bucket independently verified faithful (4 convention refusals = exactly the 4 `@Entity`-without-`@Table` inheritance subclasses; the 1 dynamic refusal = the corpus's one constant-reference `@Table` name; 6/6 sampled externals genuinely schema-absent; ambiguous drops = multi-tenant-dump twins, never wrong-bound); the negative-control corpus (Tomcat, JPA-free) produced zero candidates and zero false positives. Failure vectors are structurally bounded: declared-literals-only + `@Entity`/attribute origin gates + ToTable positive origin + unique-match-or-drop + LITERAL_DERIVED down-weighting + namespaced externals; the recall gap (convention-mapped entities) is counted per file, not silent. | Ship disabled behind a flag — rejected: no wrong-bind vector in evidence, and a dark-launched mapping family would never accumulate field evidence (the `1p9qf` reasoning verbatim). |


## Risks


| Risk | Mitigation |
| ---- | ---------- |
| Declared-name-only scope yields low coverage on convention-heavy codebases. | The convention counter + census quantify exactly how much is left on the table (literally); the decision to chase it becomes evidence-based rather than speculative. |
| Attribute/annotation argument parsing variance (constants as arguments, `@Table(name = TABLE_NAME)`). | Only string literals bind; constant references refuse (AC-3) — a future change could resolve same-file `static final` constants (the graph already models them) but that is explicitly deferred. |
| Shared seam drift between this and `1p9qf` (two changes touching one new mechanism). | Serialization point: build once in the first-landing change; the second change's tests run against the shared implementation; wave integration verifies both green on one build. |


## Session Handoff

See `docs/agents/session-handoff.md` for current session state.
