# SQL extraction: clause-aware table references, view lineage edges, and honest object kinds

Change ID: `1p9qd-enh sql-structured-statement-extraction`
Change Status: `implemented`
Owner: Engineering
Status: implemented
Last verified: 2026-07-05
Wave: TBD

## Rationale

SQL extraction today is substring-and-regex over the parse tree: any node whose type contains `select`/`from`/`join`/etc. is a "call node" (`_ts_is_call_node`, `graph_indexer.py:2844-2845`) and its identifiers become undifferentiated `calls` candidates via the generic regex (`_ts_relation_candidates` fallback, `graph_indexer.py:6061`). Live runs confirm the consequences (guru investigation, 2026-07-03; anchors re-verified 2026-07-04): tables and views are indistinguishable (both generic `kind="class"`, `_ts_kind_for_definition` SQL branch `graph_indexer.py:2383-2388` — the branch also maps `cte`/`data`/`column` tokens to `class` and `procedure`/`function`/`trigger`/`query`/`statement` tokens to `function`), no column nodes exist, view definitions don't record what they read from, `schema.table` qualification isn't modeled, and reference direction/statement kind (read vs write) is lost — an `INSERT INTO audit_log` and a `SELECT FROM audit_log` produce the same edge shape.

The grammar exposes clause structure (`object_reference` nodes under from/join/insert/update clauses), so the extractor can be clause-aware instead of token-scraping: precise table references with read/write distinction, view → source-table lineage, and honest node kinds. This is also the foundation `1p9qf` (embedded SQL) reuses — its bind stage needs exactly "extract referenced tables from a SQL statement" as a callable unit.

## Requirements

1. **Clause-aware reference extraction.** Table references come from `object_reference` positions in FROM/JOIN/INSERT INTO/UPDATE/DELETE FROM/MERGE clauses — not from the generic identifier regex. Each reference records the statement kind; reads (`SELECT`/`FROM`/`JOIN` sources) emit `reads` edges, writes (`INSERT`/`UPDATE`/`DELETE`/`MERGE` targets) emit a write-flavored edge (`writes` relation added to the vocabulary, or `reads` with a `mode: write` edge property — decide by consumer impact at implementation, Decision Log notes the tradeoff). The generic regex path is retired for SQL mode.
2. **Qualified names.** `schema.table` references resolve first against a `schema.table`-keyed lookup of SQL-defined objects, then by unique table-name match (the existing cross-file unique-candidate rule); ambiguity refuses as everywhere else.
3. **View lineage.** `CREATE VIEW v AS SELECT ... FROM t1 JOIN t2` emits `reads` edges from the view node to each source table/view — making view → table lineage traversable (and impact through views real).
4. **Honest kinds.** Table, view, and (when `1p9qe` recovers them) procedure/function/trigger nodes carry distinguishable kinds or a `sql_kind` node property (`table`/`view`/`procedure`/`function`/`trigger`) — decide representation by what `wave_map`/report/community consumers handle without special-casing; labels in reports must distinguish them either way.
5. **Extraction unit reusable for embedded SQL.** The statement→referenced-tables extraction is a standalone function taking SQL text (parse + clause walk + reference list with read/write kind), callable by `1p9qf`'s bind stage without file-node context.
6. **Version bump + tests + calibration.** `GRAPH_BUILDER_VERSION` bumped; exact-edge-set fixtures for each clause form; a realistic migration-directory fixture (Flyway-style numbered DDL files) demonstrating lineage; before/after edge quality counts recorded.

## Scope

**Problem statement:** SQL references are token-scraped without clause, direction, qualification, or kind information, so the graph can't distinguish a view from a table, a read from a write, or trace view lineage — and there is no reusable statement-analysis unit for embedded SQL to build on.

**In scope:**

- Clause-aware reference extraction (read/write kinds), qualified-name resolution, view lineage edges, object-kind honesty, the reusable extraction unit.
- Retirement of the generic regex candidate path for SQL mode.
- Exact-edge-set + migration-fixture tests; calibration counts; version bump.

**Out of scope:**

- Column-level nodes/edges (column lineage is a scale and dialect quagmire; table-level is the value tier).
- Procedure body recovery (`1p9qe`) and embedded SQL in host languages (`1p9qf`).
- Dialect-specific statement forms beyond the common core + what the installed grammar parses (documented limitation; ERROR-node recovery is `1p9qe`).
- CTE/subquery alias resolution beyond not-emitting-false-references (aliases must not mint external nodes; full alias lineage deferred).

## Acceptance Criteria

- [x] AC-1: For a fixture covering SELECT/JOIN/INSERT/UPDATE/DELETE/MERGE forms, the payload contains exactly the expected table-reference edges with correct read/write kinds and zero regex-derived extras. Exact-edge-set unit test. — `test_sql_keyword_and_column_noise_suppressed_exact_edge_set` (rewritten to the clause-aware model: SELECT/JOIN/INSERT/UPDATE/DELETE + schema-qualified, exact 8-edge set, keyword/column/string-literal absence sweeps) + `test_sql_merge_statement_directions_exact_edge_set` (MERGE INTO write / USING read).
- [x] AC-2: `schema.table` references resolve to the schema-qualified object when defined, fall back to unique bare-name match, and refuse on ambiguity (two `users` tables in different schemas, unqualified reference → refusal). Unit-tested all three. — `test_sql_qualified_name_resolution_three_tiers` (definitions register dotted; cross-file resolution is qualified-index-first, unique-simple-name fallback, refusal — the existing `_resolve_external_call_target` contract, now `sql_kind`-gated).
- [x] AC-3: A `CREATE VIEW` chain (view → view → tables) yields traversable lineage edges; `code_impact` on a base table includes dependent views. Unit-tested against the fixture graph. — `test_sql_view_lineage_chain_traversable_and_impact`: view→view→table `reads` lineage; `graph_impact("users")` includes both views transitively via the new data-layer default-traversal exception; explicit `relations=("calls",)` opts out (pinned).
- [x] AC-4: Table/view kinds are distinguishable in the payload and in `wave_graph_report`/`wave_map` labels; existing consumers ingest without error. Integration-shaped test. — `sql_kind` node property (`table`/`view`; kind stays `class` so every existing consumer ingests unchanged); `wave_graph_report` fan rankings count data-layer references and rows carry `sql_kind`; `test_sql_kind_property_and_report_labels` pins payload + report + one-hop ingestion.
- [x] AC-5: The statement-analysis unit, called directly with SQL text, returns the same reference list the file path produces (parity test) — the `1p9qf` contract. — `sql_statement_references(sql_text)` (public, module-level) + `test_sql_statement_unit_parity_with_file_path` (unit reference list mapped 1:1 onto the file path's edge set, owners included).
- [x] AC-6: CTE names, table aliases, derived-table (subquery) aliases, and temp-table/table-variable sigil forms (`#temp`, `##temp`, `@tablevar`, `TEMPORARY`/`TEMP TABLE` creations) do not mint external nodes or bindable references — temp objects are statement-scoped, not schema objects (council finding, prepare review 2026-07-03). Adversarial fixture with WITH-clauses, aliases, subquery aliases, and temp forms. Unit-tested. — `test_sql_query_local_names_never_mint_nodes` (exact edge set over the full adversarial fixture: zero phantom definitions, zero externals; also kills 1p9qc finding (d)'s cross-file alias wrong-bind — `db/queries.sql::u` no longer exists to bind to).
- [x] AC-7: Migration-directory fixture (numbered Flyway-style files defining then altering tables) produces a coherent single node per table with all references bound; counts recorded in the Progress Log. — `test_sql_migration_directory_coherent_table_nodes` (V1..V4 Flyway set: exactly one `users` node; CREATE/FK/ALTER/INDEX/VIEW/UPDATE/INSERT-SELECT references all bound at RECEIVER_RESOLVED; zero externals).
- [x] AC-8: `GRAPH_BUILDER_VERSION` bumped; `python3 .wavefoundry/framework/scripts/run_tests.py` passes; `wave_validate` clean; no `__pycache__` under `scripts/`. — Satisfied at wave integration (2026-07-05): the coordinated 37→38 bump landed with a changelog naming 1p9qd's contribution (clause-aware statement-unit rewrite, `calls`/`imports` → `reads`/NEW `writes` relation migration, `sql_kind`/`sql_error_regions` properties, phantom-definition node-set change); full suite green post-bump, `wave_validate` clean, no `__pycache__` (integration gates, wave.md Review Checkpoints).

## Tasks

- [x] Implement the standalone statement-analysis unit (parse, clause walk, reference list with read/write + qualification), with the ERROR-node behavior delegated to `1p9qe`'s hooks. — `_sql_analyze_program` + public `sql_statement_references` in `graph_indexer.py`; top-level ERROR regions skipped and counted (`sql_error_regions` module-node property = 1p9qe's hook).
- [x] Wire SQL file extraction through it; retire the regex candidate path for SQL mode; add the write-flavored edge representation (decide relation vs property with a consumer sweep). — `_sql_apply_file_extraction` replaces the generic walker for SQL mode entirely (`_ts_is_import_node`/`_ts_is_call_node`/`_ts_is_definition_node` SQL branches now return False — 1p9qc finding (c)); new `GRAPH_WRITES_RELATION = "writes"` (Decision Log).
- [x] Qualified-name lookup keying; view lineage emission; kind representation + report/map label plumbing. — dotted registration + `_resolve_fragment_edge`/`_edge_lookup_keys` SQL routing (call machinery, `sql_kind` bind gate, stay-external); view/FK/index lineage as `reads`; `sql_kind` property + report fan-ranking rows + `graph_related` `writers` bucket.
- [x] Fixtures/tests per AC-1..AC-7; calibration counts. — 9 new tests + 2 rewritten baselines in `test_graph_indexer.py`; server pin updated in `test_server_tools.py`; calibration counts in the Progress Log.
- [x] Bump `GRAPH_BUILDER_VERSION`; run `run_tests.py` + `wave_validate`; clean `__pycache__`. — Done at wave integration 2026-07-05: coordinated 37→38 bump with 1p9qd named in the changelog; suite/validate/pycache gates re-verified post-bump.

## Agent Execution Graph


| Workstream | Owner | Depends On | Notes |
| ---------- | ----- | ---------- | ----- |
| ws1-statement-unit | implementer | — | The reusable parse/clause-walk/reference unit — the core. |
| ws2-file-wiring | implementer | ws1-statement-unit | SQL-mode extraction through the unit; regex retirement; write-edge representation. |
| ws3-lineage-kinds | implementer | ws2-file-wiring | View lineage; qualified lookup; kinds + labels. |
| ws4-tests-calibration | implementer | ws3-lineage-kinds | Exact-set/adversarial/migration fixtures; parity test; calibration. |


## Serialization Points

- After `1p9qc` (noise fix) — this change's exact-edge-set tests assume a keyword-clean baseline.
- The statement-analysis unit's signature (ws1) is the contract `1p9qf` consumes — freeze it before `1p9qf` implementation starts.
- The write-edge representation decision (relation vs property) affects the relation-vocabulary docs. `1p9qa`'s `extends`/`implements` additions have **landed** (wave `1p9qh` closed 2026-07-03) — there is no in-flight coordination left; edit against the current text: the relation table in `docs/architecture/graph-index-system.md` (`extends`/`implements` rows, ~lines 105-108) and the per-tool mentions in `docs/specs/mcp-tool-surface.md` (search-graph augmentation ~`:223`, `graph_related` ~`:238`, `code_impact` default relations ~`:495`, `code_graph_path` cost tiers ~`:496`; there is no single "vocabulary" section). The consumer sweep must also cover `_GRAPH_SIGNAL_RELATIONS` (`server_impl.py:251`, currently `("calls", "imports", "reads", "extends", "implements")`) plus the adjacent `_GRAPH_REL_LABEL`/`_GRAPH_REL_BUCKET` maps — a new `writes` relation either joins them or is deliberately excluded. Note also that reusing the existing `reads` relation inherits its standing consumer policy (opt-in for default 1-hop traversal; `reads` edges excluded from clustering per `1p4ls`) — the sweep must decide whether table references want that policy.
- Single wave-level `GRAPH_BUILDER_VERSION` bump.

## Statement-Unit Contract (FROZEN — consumed by 1p9qe and 1p9qf)

`graph_indexer.sql_statement_references(sql_text: str) -> dict | None` — module-level, no file-node context required.

**Canonical text lives in the `graph_indexer.py` module comment** (the block immediately above `_SQL_REF_CLAUSES`, ~line 7140) — that comment is updated in-place as later lanes (`1p9qe`) extend the return shape, and this section is a summary pointer, not a second source of truth. As of `1p9qe` (ERROR-region recovery):

- Returns `None` only when the SQL grammar is unavailable; otherwise `{"references": [...], "definitions": [...], "error_regions": int, "recovery": {"recovered_definitions": int, "unrecovered_regions": int}}` with script-level exclusions (temp-object names) already applied.
- Reference dict: `{"name", "schema", "direction": "read"|"write", "clause", "statement", "owner", "extraction": None|"sql_recovery"}` — `name` is the reference text as written (schema-qualified stays dotted: `analytics.events`); `schema` is the schema segment or `None`; `clause` ∈ `_SQL_REF_CLAUSES` (`from`, `join`, `insert_into`, `update`, `delete_from`, `merge_into`, `merge_using`, `references`, `index_on`, `alter`, `drop`, `truncate`); `owner` is the enclosing definition's name (view lineage) or `None` for script-level statements; `extraction` is `"sql_recovery"` when the reference came from `1p9qe`'s ERROR-region recovery tier (either the region-tail re-parse or a dangling-`block` re-attribution — both forms carry the marker), else `None` for a trusted parse.
- Definition dict: `{"name", "schema", "sql_kind": "table"|"view"|"procedure"|"function"|"trigger", "temporary": bool, "extraction": None|"sql_recovery"}` — `extraction` marks a recovery-tier-recovered object the same way.
- Guarantees: zero references from keywords, column tokens, string literals, aliases, CTE names, derived-table aliases, or temp forms (`#t`/`##t`/`@t`, `TEMPORARY`/`TEMP TABLE`); direction is statement-derived (reads: FROM/JOIN/MERGE USING/view bodies/FK REFERENCES/CREATE INDEX ON; writes: INSERT INTO/UPDATE/DELETE FROM/MERGE INTO/ALTER/DROP/TRUNCATE); statements inside top-level ERROR regions are NOT scanned by the trusted path — `error_regions` counts them and `1p9qe`'s recovery tier processes them (on the file path the counts land as the module node's `sql_error_regions`/`sql_recovered_definitions`/`sql_unrecovered_regions` properties).
- Parity: the file extraction path (`_sql_apply_file_extraction`) derives its `reads`/`writes` edges from exactly this reference list (AC-5 parity test pins it). `1p9qe` re-attributes recovered-body references through the same unit; `1p9qf`'s bind stage calls the public function on captured literals.

## Affected Architecture Docs

`docs/specs/mcp-tool-surface.md` — relation mentions (if `writes` is added; no single vocabulary section exists — the exact sites are listed in Serialization Points) and SQL extraction capability notes; `docs/architecture/graph-index-system.md` — the relation table where `1p9qh` added `extends`/`implements`. The statement-analysis unit is a new intra-module seam consumed by `1p9qf` — document its contract where indexer internals are specified.

## AC Priority

(Populated at Prepare wave.)


| AC | Priority | Rationale |
| ---- | -------- | --------- |
| AC-1 | required | Clause-aware precision is the point; exact-set assertion is the only honest test shape here. |
| AC-2 | required | Qualification handling is where multi-schema enterprise DBs live or die. |
| AC-3 | required | View lineage is the impact-analysis payoff. |
| AC-4 | required | Kind honesty is cheap and every consumer benefits; ingestion must be verified. |
| AC-5 | required | The parity contract is what `1p9qf` builds on; without it the reuse claim is untested. |
| AC-6 | required | Alias/CTE false references would recreate the pollution `1p9qc` just removed. |
| AC-7 | important | The migration fixture is realism insurance; a weak result prompts iteration, not blockage. |
| AC-8 | required | Standing gates. |


## Progress Log


| Date | Update | Evidence |
| ---- | ------ | -------- |
| 2026-07-03 | Scoped from the Java/SQL accuracy investigation. Confirmed by live runs: no table/view kind distinction (then `graph_indexer.py:1976-1981,2049-2050`), token-scraped references with no direction/qualification, no view lineage; grammar exposes object_reference clause structure; cross-file unique-name table resolution already works (the foundation to build on). | Guru investigation 2026-07-03. |
| 2026-07-04 | Reality-check freshness lane (wave open): anchors refreshed after `1p9qh`/`1p9q3` line drift (call-node match `:2844-2845`, kind branch `:2383-2388`, regex fallback `:6061`); cross-file table resolution re-verified live (binds at `RECEIVER_RESOLVED`). Incremental-merge compatibility confirmed for the planned edges: per-file extraction edges merge via fragments, and finalize-computed passes are recomputed each build over the merged output maps (fragments stay pristine — `_apply_inheritance_output_passes` precedent, `graph_indexer.py:9735-9743`), so `reads`/write-flavored edges and view lineage need no full-rebuild-only assumption. Consumer-sweep surface updated in Serialization Points (`_GRAPH_SIGNAL_RELATIONS`, `reads` clustering policy). Coordinated wave version bump is now 37→38. | Reality-check 2026-07-04. |
| 2026-07-04 | **Implemented.** (1) **Statement-analysis unit** (`_sql_analyze_program` + public `sql_statement_references(sql_text)` in `graph_indexer.py`, contract documented in the module comment + `docs/architecture/graph-index-system.md`): grammar-probed against the real tree-sitter-sql AST for every statement family (SELECT/JOIN/INSERT[-SELECT]/UPDATE/DELETE/MERGE/CREATE TABLE|VIEW|MATERIALIZED VIEW|INDEX/ALTER/DROP/TRUNCATE/CTE/derived tables/temp forms). References come only from `object_reference` clause positions with statement-derived direction; `field`/`column`/`list` subtrees are never descended (kills alias-qualifier, column-token, and string-literal leakage structurally — 1p9qc findings a/c/d). (2) **File wiring**: SQL mode bypasses the generic walker entirely (`_sql_apply_file_extraction`); `_ts_is_import_node`/`_ts_is_call_node`/`_ts_is_definition_node` SQL branches return False; SQL emits NO `imports`/`calls` edges. (3) **Resolution**: definitions register dotted (`analytics.events`); same-file binds at RECEIVER_RESOLVED (unique), cross-file via `_resolve_fragment_edge` routed through the call machinery (qualified-first/unique-bare/refuse), `sql_kind`-gated (a same-name host-language twin is refused), STAY-EXTERNAL when unresolved (reads tombstoning stays constant-read-only); `_edge_lookup_keys` mirrors the routing for incremental scope-(b) re-resolution — no new fragment summary keys needed (edges ride the existing `edges` fragment + node-borne properties). (4) **Consumers**: impact data-layer default-traversal exception (graph_query), report fan rankings count data-layer edges + rows carry `sql_kind`, `writes` joins `_GRAPH_SIGNAL_RELATIONS`/`_GRAPH_REL_LABEL`/`_GRAPH_REL_BUCKET` (`writers` bucket), path cost keeps reads/writes structural, short-symbol prune exempts `sql_kind` nodes. **Calibration (shared 4-file fixture, before→after):** 12→8 nodes (4 phantom definitions gone: CTE `recent`, temp `tmpstage`/`staging_x`, MERGE alias `u`-as-function), 47→16 edges (11 alias/CTE/temp/derived externals + 14 duplicate `imports` eliminated; every surviving reference direction-aware; schema-qualified refs consolidated to one `external::analytics.events` — no more `analytics`+`events` split). Migration fixture (AC-7): 4 Flyway files, 1 node per table, 7/7 inbound references bound at RECEIVER_RESOLVED, 0 externals. **Flip evidence** (both directions, real grammar): pre-change probe on the same fixtures reproduced every defect family the new exact-set tests forbid (phantom `db/queries.sql::u` binding cross-file at RECEIVER_RESOLVED, CTE/temp defs, alias externals `x`/`a`/`o`/`u`/`e`, dup imports); the former 1p9qc exact-set baseline fails against the new extractor with precisely the predicted diff (calls/imports → reads/writes + consolidation), captured before rewriting it. Full suite 4,541/43 green (+9 tests); `wave_validate` clean. | `graph_indexer.py`, `graph_query.py`, `server_impl.py`; `test_graph_indexer.py` (9 new + 2 rewritten `test_sql_*`), `test_server_tools.py` (signal pin); venv probe runs 2026-07-04. |
| 2026-07-04 | **Findings for later changes:** (a) **1p9qe** — top-level ERROR regions are counted on the module node (`sql_error_regions`) and skipped; statements that parse under non-ERROR wrappers (e.g. the dangling `block` after an unparsable `CREATE PROCEDURE` header) ARE analyzed but attribute to the module — recovery should re-attribute them to the recovered routine definition; `_SQL_CREATE_KIND_BY_NODE` already maps `create_procedure`/`create_function`/`create_trigger` for grammars that parse them (T-SQL headers currently ERROR out). (b) **1p9qf** — the `writes` fragment-resolution route accepts writes from ANY source suffix (an external `writes` edge from a host-language file resolves with the `sql_kind` bind gate for free), but external `reads` from non-SQL sources still hit the constant-read tombstone path — the bind stage must either bind at emission (as planned, LITERAL_DERIVED) or extend `_sql_table_reference_edge` routing. (c) Function invocations inside SQL (`SELECT fn(x)`) no longer emit any edge (the retired regex path emitted noisy `calls`); if field demand appears, the unit's clause vocabulary can grow a `call` clause. (d) `wave_graph_report` fan_in previously surfaced tables via their `calls` in-edges; it now counts data-layer reads/writes (sql_kind-gated) — SQL-heavy repos will see fan-in composition shift accordingly. | Implementation observations 2026-07-04. |
| 2026-07-05 | **Cosmetic finding (1p9qi review, not a defect — recorded for awareness):** `CREATE TABLE archive AS SELECT * FROM users` (CTAS) reads its source table's reference through `handle_create_table`'s FK-target scan (the "every OTHER `object_reference` in the create subtree" walk), so `users` carries `clause: "references"` instead of a SELECT-derived clause like `from`. Direction (`read`) and the edge itself are correct — a real `reads` edge from the CTAS table to `users` is emitted exactly as it should be — only the `clause` metadata label is misleading (reads as if it were an FK/constraint reference rather than the CTAS source-select). Left as-is: no consumer branches on the `clause` string value for CTAS specifically, and reclassifying it means teaching `handle_create_table` to distinguish an FK-constraint object_reference from a nested SELECT's object_reference, a bigger structural change than the cosmetic label warrants. | Live probe 2026-07-05 (`CREATE TABLE archive AS SELECT * FROM users;` → `sql_statement_references`). |


## Decision Log


| Date | Decision | Reason | Alternatives |
| ---- | -------- | ------ | ------------ |
| 2026-07-03 | Clause-aware extraction with a reusable statement unit; table-level only (approach A). | Precision comes from the grammar's clause structure the current code ignores; the standalone unit is designed once and shared with embedded SQL; table-level is where impact questions live ("who touches this table"), column-level is a different cost class. | (B) Patch the regex path with better filtering — weakness: direction/qualification/lineage are structurally unavailable to token scraping; filtering can't add information. (C) Column-level lineage now — weakness: dialect variance and node-count explosion for a consumer that doesn't exist yet; revisit on field demand. |
| 2026-07-03 | Read/write distinction representation decided at implementation (new `writes` relation vs `mode` property on `reads`). | A new relation is cleaner semantics but touches every relation-vocabulary consumer; a property is non-breaking but weaker for query tools. The consumer sweep (report/community/path/impact) is a half-day fact-finding task that should decide it — not a guess now. | Predetermining either — rejected: the tradeoff is empirical (consumer handling), and the wave already coordinates a vocabulary update with `1p9qa` if the relation route wins. |
| 2026-07-04 | **DECIDED: new `writes` relation** (writes = INSERT INTO/UPDATE/DELETE FROM/MERGE INTO/ALTER/DROP/TRUNCATE targets); read direction reuses `reads`. **Consumer-sweep evidence (all sites from the Serialization Points list):** (1) `_resolve_fragment_edge` — the decisive fact: the `reads` branch is constant-gated (unique-CONSTANT-or-TOMBSTONE, 1p4ls), so table references need dedicated routing REGARDLESS of representation; a `mode` property would not have avoided that and would additionally hide direction from every relation filter. (2) `_GRAPH_SIGNAL_RELATIONS` (`server_impl.py:251`) + `_GRAPH_REL_LABEL`/`_GRAPH_REL_BUCKET`: `writes` JOINS them (`writer`/`writers` — "what uses table X" includes its writers); pinned test updated. (3) 1p4ls `reads` policy inheritance decided CONSCIOUSLY: table reads inherit opt-in 1-hop + clustering exclusion (graph-signal passes `reads` explicitly so `code_ask` is unaffected); `writes` is NOT opt-in (sparse, high-value). (4) `_DEFAULT_IMPACT_RELATIONS`: unchanged — instead a narrow data-layer exception in `graph_impact` (default traversals follow `reads`/`writes` edges touching a `sql_kind` node; explicit `relations` opts out, dispatch-seed precedent) delivers AC-3 without letting constant reads into blast radius. (5) `_path_edge_cost`: reads/writes stay structural tier (100) — a shared table never out-competes a call chain. (6) `graph_cluster`: no code change — `reads` exclusion inherited; `writes` clusters at dict-default weight 1 (was `calls` weight 3 — SQL clustering signal shifts, accepted; revisit on field evidence). (7) `wave_graph_report`: fan rankings count data-layer edges so tables stay visible (they lost their `calls` in-edges); rows carry `sql_kind`. (8) Dashboard (`dashboard_lib.py`/`dashboard_server.py`): grep-verified ZERO relation-name consumers — out of scope with evidence. (9) Incremental merge: no new fragment summary keys needed (edges ride the existing `edges` fragment; `sql_kind`/`sql_error_regions` are node-borne, recovered from per-file fragments by construction); `_edge_lookup_keys` extended so scope-(b) re-resolution consults call-shaped keys for SQL reads/writes. | `mode: write` property on `reads` — rejected: hides direction from relation-level filtering (impact/path/signal/report all filter by relation), still requires the SQL-origin resolution routing, and leaves "reads" semantically false for a DELETE target. Reusing `calls` — rejected: perpetuates the direction-less model the change exists to fix. |
| 2026-07-04 | Kind representation: tables/views keep node kind `class` + a `sql_kind` property (`table`/`view`; routines `function` + `sql_kind`). | Every existing consumer (cluster projection, candidate indexes, merge gates, kind filters) ingests `class` unchanged — zero special-casing; distinguishability comes from the property, surfaced in report rows and the node payload. `CLUSTER_BUILDER_VERSION`: NO bump — no cluster-code change, node kinds unchanged (the property is invisible to the projection), and the coordinated `GRAPH_BUILDER_VERSION` bump already forces re-clustering; community shape on SQL-heavy repos may still shift because SQL reference edges left `calls` (weight 3) for `reads` (excluded)/`writes` (weight 1) — recorded as an accepted, graph-bump-covered consequence. | New first-class kinds (`table`/`view`) — rejected: every kind-switch consumer would need cases, and cluster/report behavior would change for zero added information over the property. |
| 2026-07-04 | SQL-origin routing signal = source-file suffix (`_SQL_SOURCE_SUFFIXES`, derived from `_TS_EXTENSION_TO_LANGUAGE`), not an edge property. | Constant reads can never originate from SQL files (SQL mode emits no identifier reads), so the suffix is sound; deriving from the language map means the two can never drift; an edge property would be a payload-shape addition carried on every SQL edge forever for the same information. | Edge `origin` property — rejected as redundant state; revisit only if 1p9qf needs external reads from host files routed here (finding (b)). |
| 2026-07-04 | FK `REFERENCES` and `CREATE INDEX ... ON` emit read-direction references (clauses `references`/`index_on`); ALTER/DROP/TRUNCATE emit writes. | FK/index are schema-dependency facts (the old path emitted an unresolvable `imports` edge for FK; now it binds — the migration fixture proves the value); ALTER/DROP/TRUNCATE modify the object, and migration scripts are where they matter (AC-7's single-node-per-table requires the ALTER file to reference, not re-define). | Dropping FK/index references (strict FROM/JOIN/DML-only scope) — rejected: retiring the regex path would have silently LOST today's FK signal instead of upgrading it. |
| 2026-07-04 | Short-symbol prune exempts `sql_kind` nodes (mirrors the 1p4q4 constant exemption). | A 2-char table/view name (`v1`, `t1`) is a real CREATE-declared schema object, never the loop-var/type-param noise the prune targets; pruning one silently drops its lineage edges (caught live by the AC-5 parity test). | Renaming test fixtures around the prune — rejected: hides a real behavior hole 1p9qf's binds would hit on real schemas. |
| 2026-07-05 | **FIXED at wave integration: scalar-invocation names skipped in the reference walk** (the `reads external::NOW` finding from 1p9qe's calibration — pre-existing in this change's walk: `invocation > object_reference` collected as reads, so `WHERE created < NOW()` emitted `reads external::NOW`). **Measurement first (Fineract census corpus, 23 `.sql` files, working-tree unit):** 42 of 953 unit references (4.4%) were scalar-invocation-derived noise (`ifnull` ×29, `concat` ×13); zero names appeared in both invocation and genuine table positions; post-fix 911 references, 0 noise. **Fix (bounded, ~7 LOC + flip test):** `walk_reads` gains an `invocation` branch that skips ONLY the direct `object_reference` child (the function name) and keeps walking the argument subtree — a genuine table read inside a function-argument subquery (`COALESCE((SELECT n FROM orgs), 'x')` → `orgs`) is preserved (probe-verified both before and after). Relation-position invocations (`FROM generate_series(...)` table-valued functions) are UNTOUCHED: the `relation` branch never emitted them (probe-verified pre-fix), consistent with the recorded routine-invocation stance (finding (c) — a `call` clause is future field-demand work), so nothing is lost. All pre-existing exact-set tests pass unmodified (none had encoded the noise); new `test_sql_scalar_invocation_names_never_become_references` pins both directions incl. TVF and argument-subquery preservation; frozen-contract consumers (1p9qe re-attribution, 1p9qf bind) re-run green. | Blanket `invocation`-subtree skip — rejected: probe-proven recall loss (drops `orgs` from argument subqueries). Emitting TVF names as table reads — rejected: a routine call is not a table reference; would re-mint the same noise class in relation position (`FROM ifnull(...)` mis-parses exist on the census corpus). Defer as follow-up — rejected: the fix is well under the bounded-fix threshold and the measurement was already in hand. |
| 2026-07-05 | **FIXED at 1p9qi review: `CREATE FUNCTION/PROCEDURE ... RETURNS <type>` no longer emits the return type as a table reference** (adversarial finding 3 — `handle_create_routine` treated the routine's own name as the only excluded `object_reference`; a SECOND top-level `object_reference` — the RETURNS type, e.g. `void`/`integer` — fell to the generic walk and minted `reads external::void`, reproducing with a named dollar-tag body `$tag$...$tag$` distinct from the bare-`$$` form the dialect-forms test already covers). Fix (bounded, ~10 LOC): track whether the immediately-preceding child was `keyword_returns`; the next `object_reference` in that position is skipped (never walked), mirroring the landed scalar-invocation-name exclusion's stance that "a name is not a reference." All 440 pre-existing tests pass unmodified; new `test_sql_routine_return_type_never_becomes_a_reference` pins both the dollar-tag-body and bare-`$$` forms. **Residual (not fixed, out of this item's named scope):** the SAME dollar-tag fixture's function body still separately parses a nested `CREATE TABLE ghost_in_body (...)` as if it were a `FROM`-clause table reference (`walk_reads`'s generic descent does not recognize a nested `statement`/`create_table` node as opaque) — a distinct, deeper defect in the generic body-descent, not the return-type exclusion; recorded here as a follow-up candidate, not attempted (fixing it touches the shared generic-descent fallback used by every clause form, well past a bounded single-item fix). | Reworking the shared generic `walk_reads` descent to special-case every non-reference-bearing nested-statement shape — rejected for this item: broad blast radius across every clause form for a fix scoped to the named `void`-return finding; the ghost_in_body residual is recorded, not silently absorbed into a larger unreviewed change. |
| 2026-07-05 | **FIXED at 1p9qi delivery review (supersedes the `ghost_in_body` residual recorded in the return-type entry above): in-body routine statements on the TRUSTED native parse path now route through `analyze_statement`, not the generic read walk.** The delivery council's red-team primer confirmed with a live `sql_statement_references` probe that the residual was NOT one phantom read but a **systematic in-body write-direction loss on the native PL/pgSQL `$$`/`$tag$` path** (`error_regions: 0` — the trusted path, not recovery): `handle_create_routine` flattened the entire routine body through `walk_reads`, which hard-codes `direction="read"` at every `object_reference`. So every in-body `INSERT`/`UPDATE`/`DELETE`/`MERGE` was a WRITE emitted as a `read` (direction inverted → polluting the `writes`/`writers` bucket and the `_SQL_DATA_LAYER_RELATIONS` impact traversal, defeating 1p9qd's headline read/write feature for stored-procedure logic); a nested `CREATE TABLE` was a phantom `read`; and a bare `CREATE TEMP TABLE` was a phantom `read` that bypassed AC-6's temp-object exclusion (`make_ref`'s temp gate only catches `#`/`@` sigils, and `handle_create_table`'s `keyword_temporary` check was never reached for in-body creates). **AST-shape finding (venv probe FIRST):** a natively-parsed routine body wraps each of its statements in a `statement` node inside a `function_body` child — the SAME node type the top level uses — and unparseable control flow (e.g. `IF (SELECT count(*) FROM t) …`) lands in a nested `ERROR` region carrying loose `select`/`from`/`relation` subtrees with genuine reads; a `subquery` never contains a bare `statement` node (verified — so the fix cannot mis-fire on ordinary subqueries). **Fix (two bounded edits):** (1) `walk_reads` gains a `statement` branch that dispatches to `analyze_statement(node, owner, cte_names)` and returns — symmetric with the existing `cte` branch — so every in-body statement gets its correct clause-derived direction and owner (`INSERT/UPDATE/DELETE/MERGE`→write, `SELECT`/`FROM`→read) while loose reference positions with no enclosing `statement` (the `ERROR`-region subquery) still fall through to the generic read descent and are preserved; (2) `handle_create_routine` drops any definitions the body mints (`del definitions[body_def_floor:]` after the routine's own def), matching the recovery tier's established stance that routine bodies never mint module-scope schema objects. **AC-6 in-body extension:** an in-body `CREATE TEMP TABLE` now registers its temp-name exclusion exactly as at top level, so later in-body references to it (`INSERT INTO staging …`) are filtered. **This makes the trusted native path consistent with the recovery tier**, which already re-enters `analyze_statement` for recovered bodies (1p9qe) — the recovery case is not double-processed (its bodies re-parse through a separate `_sql_analyze_program` call; the native fix only changes how the already-parsed native subtree is walked). **Flip evidence (primer probe rebuilt):** pre-fix, `real_audit`/`accounts`/`sessions` emitted `read` + `ghost_in_body`/`staging_bare` emitted phantom `read`s; post-fix, `real_audit`→`write`, `accounts`→`write`, `sessions`→`write`, `users`/`real_src`/`cond_tbl`→`read`, `ghost_in_body`/`staging` absent from both refs and defs. New `test_sql_in_body_routine_statements_get_correct_direction` pins the exact flipped reference set incl. the ERROR-region subquery read preservation, in-body temp exclusion, and no-phantom-def; all pre-existing exact-set tests (incl. `test_sql_routine_return_type_never_becomes_a_reference`, whose body-form assertion checks names only and stays green) pass UNMODIFIED. No `GRAPH_BUILDER_VERSION` bump (already 38 this wave; node/edge shape unchanged — only direction/owner of emitted edges corrected). Full suite green (4,598 baseline + 1 new = 4,599; the sole flake, `test_100k_rows_under_200ms`, is a parallel-run timing contention — 0.087s in isolation, unrelated to SQL). No residual: every sub-case in the primer (write-direction, nested-create phantom, in-body temp AC-6, subquery-read preservation) is fully fixed. | Localizing the fix to only the `function_body` node type — considered, but routing at the `statement` level in `walk_reads` is more robust (handles statements wherever they nest in the body subtree, including after `ERROR` regions) and symmetric with the existing `cte`-statement routing. Minting in-body creates as real definitions — rejected: inconsistent with the recovery tier and would surface spurious module-scope schema objects from function bodies. |
| 2026-07-05 | **RECORDED (1p9qi review, not fixed): bracket-/backtick-/quote-qualified T-SQL identifiers on the SQL-FILE extraction path (`_sql_object_reference_parts`) mint a mangled name** (adversarial finding 5 — `SELECT * FROM [dbo].[users]` mints `external::dbo].[users` instead of `external::dbo.users`; `_sql_normalize_object_name` already exists and is used by the recovery tier and the `1p9qf` bind stage, but applying it here breaks `1p9qf`'s pinned `test_backtick_quoted_ddl_binds_bare_reference`, whose docstring documents the current shape as "backticks preserved by the names-as-written unit contract" — this is a DELIBERATE, already-tested design point: the statement unit preserves the reference text exactly as written (this section's own "as written" language above), and normalization is a BIND-STAGE concern applied on both match sides (the 2026-07-04 Decision Log entry below), not a statement-unit concern. Per the house rule (bounded fix + all existing exact-set tests green UNMODIFIED, else record), this is recorded rather than landed: fixing it would either (a) break the pinned backtick-preservation test, or (b) require threading a "normalize the name but keep an as-written field too" shape change through the frozen contract mid-wave — a renegotiation, not a bounded fix. Follow-up: if bracket-quoted T-SQL identifiers prove common in a real corpus, normalize at the SQL-FILE EXTRACTION consumer site (`_sql_apply_file_extraction`'s external-id construction) rather than inside `_sql_object_reference_parts`, so the "as written" unit contract and the backtick-preservation test stay intact while the file path's OWN external-id minting gets the same bind-side normalization `1p9qf` already applies. | Normalizing inside `_sql_object_reference_parts` (attempted, reverted) — breaks the pinned backtick-as-written contract test. Leaving it unrecorded — rejected: a mangled `external::dbo].[users` node id is a real, reproducible defect that a future SQL-graph-accuracy pass should pick up. |
| 2026-07-05 | **FIXED at 1p9qi delivery council (loudness only): cursor `FOR … LOOP` / `WHILE` control-flow bodies now flag `sql_partial_bodies` instead of dropping in-loop DML silently.** Two independent adversarial council seats (security + rotating) converged and live-probed the finding: a natively-parsed PL/pgSQL routine (`error_regions: 0`, the TRUSTED `$$`/`$tag$` path) whose body contains a loop tree-sitter-sql cannot parse mangles the loop body into a **NESTED** `ERROR` under `function_body`. In-loop DML (`INSERT INTO loop_audit …`) is then dropped: the misparsed `loop_audit (id)` becomes an `invocation` node, which **this wave's own scalar-invocation-name exclusion** (2026-07-05 entry above) skips — so the interaction between the two 1p9qi changes is what makes the drop total AND silent. Because the ERROR is nested inside a `statement` that parsed at top level, `scan_top` never sees it, so `error_regions`/`sql_unrecovered_regions` stay 0 — under-capture (a safe recall gap, NOT a direction inversion) with NO loud signal. **This is why the 1p9qd risk-table "not silent" claim was false for this case** (corrected in the Risks table this review). **Fix (bounded, ~15 LOC + test, no `GRAPH_BUILDER_VERSION` bump — see reasoning below):** a new module helper `_sql_subtree_has_error` walks the `create_node` subtree; `handle_create_routine` increments a new `partial_bodies` counter (threaded through `_sql_analyze_program` → `sql_statement_references` recovery dict AND `_sql_apply_file_extraction`'s module-node property `sql_partial_bodies`, set OUTSIDE the `error_regions` gate since this case has zero top-level regions). Kept DISTINCT from `sql_unrecovered_regions` (top-level ERROR regions) — the semantics differ. The per-file verbose build-log line now renders the count (`_sql_recovery_log_line` gained a `partial_bodies` arg). **No version bump:** the module node is rebuilt every build (not a cached fragment), the added property is additive/informational, and NO edge or relation shape changed — the standing bump rule (consumer caches must re-extract) does not apply; confirmed against the v38 coordinated-bump reasoning. **Deliberately NOT attempted:** actually recovering the dropped in-loop DML — that needs plpgsql control-flow parsing tree-sitter-sql lacks (the loop body is unparseable), out of scope; recorded follow-up. New `test_sql_partial_body_loudness_for_loop_control_flow` pins the flag (module node + unit recovery dict + build-log line) on a `FOR … LOOP` body and its absence on a clean fully-parsed body; full suite green (4,599 baseline + 1 = 4,600). **Minor residuals recorded by the seats (each a recorded follow-up, NOT fixed this review):** (1) Postgres `EXECUTE FUNCTION <name>` trigger action mints a phantom read of the function name (same invocation-name family as the scalar-invocation and NOW findings; a `LITERAL_DERIVED`-contained low-value external). (2) MERGE `WHEN … THEN` branch subqueries (`WHEN MATCHED THEN UPDATE SET x=(SELECT … FROM t)`) drop the subquery's `t` read — the `analyze_statement` MERGE branch's "when_clause / predicate expressions carry only field qualifiers, never table references" comment is wrong for the subquery case. (3) `DECLARE <var> <type>` in a routine body phantom-reads the type name (observed in the live probe: `RECORD` emitted as a read). (4) A non-temp body-local `CREATE TABLE` (its def correctly dropped by the body-def-drop) whose later in-body read can cross-file unique-bind a same-named REAL table (uncommon; standard unique-bare-name model, not specific to this path). (5) **F-B (very low, architecture seat):** the body-def-drop is name-parse-gated — `handle_create_routine` only drops definitions when the routine header's name parsed, so an unnamed-but-parseable routine header would leave the "bodies never define at module scope" invariant non-total; recorded, not fixed. | Recovering the in-loop DML now — rejected: unparseable-loop-body recovery needs control-flow parsing the grammar lacks; far past a bounded loudness fix. Overloading `sql_unrecovered_regions` for the nested case — rejected: it specifically means top-level ERROR regions; conflating the two would make both counts ambiguous. Leaving the nested-ERROR case silent and merely correcting the risk-table claim to admit silence — rejected: the honest move is to make the claim TRUE by adding the signal, not to document a blind spot. |


## Risks


| Risk | Mitigation |
| ---- | ---------- |
| The installed grammar mis-parses some dialect's clause forms, silently dropping references. | ERROR-node paths route to `1p9qe`'s recovery tier (loudly counted); exact-set fixtures cover the common core; residual dialect gaps are documented limitations. **Loudness is now two counters, not one:** a **TOP-LEVEL** ERROR region is counted (`sql_error_regions` / `sql_unrecovered_regions`, build log). A routine body's **NESTED** ERROR — a PL/pgSQL loop / control-flow construct (`FOR … LOOP`, `WHILE`) the grammar cannot parse — does NOT reach the top-level scan (the `CREATE` header parses, so `error_regions` stays 0) and would otherwise drop in-loop DML silently; it is now flagged by the `sql_partial_bodies` module-node count (added at 1p9qi delivery review) and printed in the per-file build-log line, so this case is loud, not silent. Recovering the dropped in-loop DML itself is a recorded follow-up (needs plpgsql control-flow parsing tree-sitter-sql lacks). |
| Retiring the regex path loses some reference the clause walk misses. | The migration fixture (AC-7) + before/after calibration counts surface recall regressions; the regex path's "recall" today is mostly noise (`1p9qc` evidence), so the trade is measured, not assumed. |
| `writes` relation (if chosen) breaks a consumer that hard-codes relation names. | The AC-4/consumer-sweep step verifies ingestion before the representation is locked; cluster projection is relation-agnostic by design (landed fact: the `docs/architecture/graph-index-system.md` cluster-projection weight table gives "any other relation" the dict-default weight 1). |


## Session Handoff

See `docs/agents/session-handoff.md` for current session state.
