# Wave Record

Owner: Engineering
Status: closed
Last verified: 2026-09-17
review-evidence-source: events.jsonl

review-policy-reprepare-required: false
wave-id: `1wpif index-content-and-retrieval-correctness`
Title: Index Content And Retrieval Correctness

## Objective

Restore trustworthy indexed content and retrieval behavior: distinct source chunks remain searchable, source ranges are accurate, FTS state heals honestly, filters are honored during candidate generation, and the declared ANN tuning constants are resolved honestly (retired with documented defaults; measured tuning relocated to `1wpih` by the 2026-08-31 prepare council).

## Changes

Change ID: `1wngv-bug chunk-identity-source-range-integrity`
Change Status: `implemented`

Change ID: `1wpag-bug fts-reconciliation-query-honesty`
Change Status: `implemented`

Change ID: `1wpah-bug retrieval-candidate-generation-correctness`
Change Status: `implemented`

## Participants

- Coordinator: coordinator
- Write-owning roles: implementer
- Requested review lanes: architecture-reviewer, code-reviewer, qa-reviewer, performance-reviewer
- Required review lanes: code-reviewer, qa-reviewer, architecture-reviewer, performance-reviewer
- Product-owner admission review: operator-approved on 2026-08-30 by the request to plan and admit the audited findings into waves; no new end-user feature scope is introduced.

Completed At: 2026-09-02

## Wave Summary

Wave `1wpif` (Index Content And Retrieval Correctness) delivered 3 changes: Preserve Chunk Identity and Source Ranges, Heal FTS State and Report Honest Query Failures, and Make Retrieval Candidate Generation Honor Filters and ANN Tuning. Notable adjustments during implementation: Preserve Chunk Identity and Source Ranges: Delivery reverification cycles 3 and 4, harness scope. **`CODE-RV2-2`:** an independent AST census found three `setUp`-level bare imports of the server module beyond the file already repaired; two genuinely failed standalone and are converted to the shared loader, the third already ran standalone and was left alone. The next lane re-derived the census independently over 42 import sites across the whole tests tree, confirmed exactly one remains at `setUp` scope (the deliberate exclusion), and ran every class holding a bare import in isolation with all green, so nothing was missed.; Preserve Chunk Identity and Source Ranges: Delivery reverification cycle 2, oracle repairs. The independent qa lane re-derived the coordinate census from a fresh corpus walk (1,743 files, 23,206 prose chunks, zero wrong) and independently covered the table row-group class the original attestation missed (2,016 parts, zero wrong), killing a source-level pre-repair mutant at 1,962 wrong. **QA-RV1-1**: its `_classify` has a fourth verdict, `empty`, and the census test asserted only `wrong == 0`, so a chunk that escaped classification was scored neither right nor wrong; under the pre-repair mutant the bucket absorbed chunks that would otherwise have been flagged. The test now asserts `empty == 0` as well. **CODE-RV1-1**: the independent code lane confirmed `_candidate_merge_key` is applied at all four merge seams but proved only the hybrid fusion seam was pinned, reverting either of the other three individually left the whole 8,051-test suite green. A source pin now fixes the seam count at eight call expressions inside `search_combined` and asserts the legacy `(path, tuple(lines))` key exists nowhere in production.; Preserve Chunk Identity and Source Ranges: Thought: delivery-repair order for the builder lane, seams read through MCP before any edit. (1) RED-DEL-1 table row groups: the coordinate defect is that `_decompose_oversized_table_chunk` anchors every emitted part at the table head (`table_start_line`) and the `split_large_chunks` remap re-derives the index from that same head, so parts 2..n carry the header's lines; the repair moves the mapping INTO the decomposer, which alone knows each group's row offset, and emits a per-part `line_map` so an over-cap part still line-wraps with absolute coordinates. Reproduced header/separator on parts 2..n becomes generated context (map entry `None`), so a part's range is exactly its own rows. (2) RED-DEL-4 `_dedupe_chunk_ids` seeds its counter from the FULL original id multiset and loops until the resolved id is unused, so a generated `-L{n}` can never equal a natively emitted one. (3) CODE-DEL-3 rst/adoc preambles carry per-line absolute numbers (the doc-title block is excised from the middle of the body, so a single base offset cannot describe it). (4) Version 41 with the corrected version-40 comment scope. Then the coordinate census over the real repository md/rst/adoc corpus as the falsifying oracle for the whole class.

**Changes delivered:**

- **Preserve Chunk Identity and Source Ranges** (`1wngv-bug chunk-identity-source-range-integrity`) — 9 ACs completed. Key decisions: Delivery-repair clarification of the AC-2 coordinate contract: `lines` is a contiguous one-based ABSOLUTE range that CONTAINS the chunk's source-derived payload, and exactness is SCOPED — a whole section keeps its section span, while an oversized-section window and a table row-group part carry the MINIMAL span containing their own payload; a window straddling an excised block carries the minimal span containing its payload; reproduced context (breadcrumb, splice artifact, and the prelude plus table header repeated on row-group parts after the first) is generated and never anchors a range. The census over the real corpus is the oracle for the whole class.; Prepare-council amendment A4 plus the red-team census hazard: the prose line-window task requires splice-aware mapping to original body coordinates across extracted fence spans (a base offset alone still miscites post-fence windows because `chunk_markdown`/`_emit_prose_sections` character-splice fences out before windowing), with a fence-before-prose fixture; the collision census detector consumes real materialized build rows and the synthetic corpus proves scaling only.
- **Heal FTS State and Report Honest Query Failures** (`1wpag-bug fts-reconciliation-query-honesty`) — 8 ACs completed. Key decisions: Prepare-council amendments A2/A3/A5/A7: healing authority is pinned (serving errors probe and schedule only; healing executes solely under the build lock through the ordinary reconcile/build path; the per-epoch marker is lock-owner-written; the public query path performs no store writes); the integrity digest contract is defined (FTS-served payload fields, order-independent aggregation, publication-boundary computation, corruption-detection threat model under the single-writer model); AC-3 enumerates `code_lexical`, the hybrid FTS half, and degraded fallbacks behind one shared chokepoint with bounded, path-sanitized `detail` strings.; Make the existing FTS probe part of normal reconciliation and serving.
- **Make Retrieval Candidate Generation Honor Filters and ANN Tuning** (`1wpah-bug retrieval-candidate-generation-correctness`) — 5 ACs completed. Key decisions: AC-5's receipt clause is resolved as `[~]`: the wave's before/after evidence is the signed same-generation pair on the delivered chunker-40 index plus a data-level reproduction of the evaluator's comparison arithmetic against the standing `1sear` pair; the evaluator's compatibility gate is not loosened.; Prepare-council amendment A1/A6: the ANN-tuning threshold battery is relocated to `1wpih` and Requirement 5/AC-5 now resolve the inert constants under the retire-and-document branch; Requirement 2/AC-2 state the multi-source public-call aggregate (worst case 16 substrate queries / 960 examined rows for `code_ask`) and require an aggregate assertion.
## Watchpoints

- Activation dependency: implement `1sear` first so chunking/filter/ANN changes have a standing production-path baseline and holdout gate; plan/council review may complete earlier, but readiness is recorded only after the baseline artifact exists.
- Baseline authority: readiness requires two current-schema, same-generation `1sear` baseline runs plus their comparison receipt, with matching fixture/evaluator/production identities and the final pre-`1wpif` index generation recorded.
- Watchpoint: preserve unaffected chunk IDs and compare full versus incremental builds before accepting the compatibility bump.
- Watchpoint: FTS recovery and filter pushdown share state-store/query boundaries; serialize overlapping edits and run parity tests after both land.
- Watchpoint: no index rebuild should publish as evidence until the new collision and parity checks are available (the exact-search reference relocated to `1wpih` with the ANN battery).

## Operator Decisions Outstanding

Recorded 2026-09-02 at the end of the delivery reverification. Each is stated with
its evidence so the operator can decide rather than ratify.

1. **Does this wave owe a fresh retrieval receipt for its own cycle-2 repairs?**
   (finding `ARCH-RV1-3`.) The evaluator's `_production_identity` hashes the WORKING
   TREE, and the cycle-2 repairs moved two `PRODUCTION_RETRIEVAL_MODULES` members
   past HEAD, so the tree's production digest `00dac006` is bound by no receipt on
   file. Under the policy wave `1wybs` shipped, the wave that moves production
   retrieval bytes records a before-and-after pair. **Evidence against a run being
   informative:** of the six changed functions, only `_refill_windows` is reachable
   from retrieval and it is behaviourally identical to HEAD across 308 inputs (the
   clamp was reverted; only its docstring changed); `_bounded_failure_detail` has
   three call sites, all on damage or exception paths; and the four store-side
   changes are referenced zero times in the serving module. **Evidence for running
   it anyway:** whole-module identity cannot see any of that, which is precisely the
   `1wybs` ARCH-DEL-1 lesson, and a pair costs one quiet-machine window. The
   coordinator did not run it, because a receipt is a signed artifact and choosing
   to skip one is not a coordinator call.

2. **Is PERF-DEL-6's adjudication the wave's final word on the two latency rows?**
   The refutation stands on re-derivation: the pair is contended on all four tools
   under the estimator this repository now ships (`code_search` 18.10%,
   `docs_search` 20.76%, `code_ask` 25.03%, `code_lexical` 36.71% against a 5%
   threshold), and the shipped latency clause routes such a shortfall to operator
   review for every comparison kind. Nothing further is owed unless the operator
   wants a quiet-machine pair recorded as the latency reference.

3. **`SEC-RV1-2` is routed to `1wpih`, not repaired here.** The oversized-window
   fallback above `REFILL_MAX_ROWS_PER_SOURCE` is unreachable from every public
   entry and is a frozen admitted contract with its own pin. A clamp was implemented
   and reverted rather than change a pinned design inside a delivery review; the
   reachable envelope is pinned instead, so the question reopens automatically if any
   public clamp ever rises above the ceiling.

## Process Notes Against This Review

Recorded because the wave's own churn controls say a lapse is worth more than a
clean-looking record.

- **The coordinator's first `SEC-RV1-1` test passed against its own mutant.** It
  asserted a literal path spelling the leak does not use, so the guard it was meant
  to hold was not held. The oracle was made separator-blind and only then killed the
  mutant. This is the landing rule catching the person applying it.
- **A trial clamp on a frozen constant was caught by that constant's own pin** and
  reverted, which is the outcome the pin exists for; it is recorded as `SEC-RV1-2`
  rather than quietly dropped.
- **One ledger evidence record carries a stray non-Latin glyph** in its `expected`
  field (the `EVID-DEL-2` reverification, `ev-evid-del-2-3`). The ledger is
  append-only and the defect is cosmetic, so it is disclosed here rather than
  churned over.

## Finding Synthesis

<!-- wave:finding-synthesis begin -->
| Current finding | Disposition | Open block | Repair | Approval recheck |
| --- | --- | --- | --- | --- |
| ARCH-DEL-2 | do_now | no | completed | — |
| ARCH-RV1-1 | do_now | no | completed | — |
| ARCH-RV1-2 | do_now | no | completed | — |
| ARCH-RV1-3 | do_now | no | completed | — |
| ARCH-RV2-1 | do_now | no | completed | — |
| ARCH-RV3-1 | do_now | no | completed | — |
| CODE-DEL-2 | do_now | no | completed | — |
| CODE-DEL-3 | do_now | no | completed | — |
| CODE-RV1-1 | do_now | no | completed | — |
| CODE-RV1-2 | do_now | no | completed | — |
| CODE-RV2-1 | do_now | no | completed | — |
| CODE-RV2-2 | do_now | no | completed | — |
| CODE-RV3-1 | do_now | no | completed | — |
| CODE-RV4-1 | do_now | no | completed | — |
| EVID-DEL-1 | not_issue | no | not_required | — |
| EVID-DEL-2 | do_now | no | completed | — |
| EVID-RV1-1 | do_now | no | completed | — |
| PERF-DEL-6 | do_now | no | completed | — |
| PERF-RV1-1 | do_now | no | completed | — |
| QA-RV1-1 | do_now | no | pending | — |
| RED-DEL-1 | do_now | no | completed | — |
| RED-DEL-2 | do_now | no | completed | — |
| RED-DEL-3 | not_issue | no | not_required | — |
| RED-DEL-4 | do_now | no | completed | — |
| SEC-RV1-1 | do_now | no | completed | — |
| SEC-RV1-2 | dont_do_later | no | not_required | — |
| SEC-RV2-1 | do_now | no | completed | — |
| SEC-RV2-2 | do_now | no | completed | — |
| SEC-RV3-1 | do_now | no | completed | — |
| SEC-RV3-2 | do_now | no | completed | — |
| SEC-RV3-3 | do_now | no | completed | — |
| SEC-RV4-1 | do_now | no | completed | — |

*Machine review state — 32 findings; current: do_now 29, maybe_later 0, dont_do_later 1, not_issue 2*
<!-- wave:finding-synthesis end -->

## Review Evidence

<!-- wave:review-status begin -->
| Signoff | State | Why | Next action |
| --- | --- | --- | --- |
| wave-council-readiness | approved | current executed approval follows every affected repair | none |
| wave-council-delivery | approved | current executed approval follows every affected repair | none |
| code-reviewer | approved | current executed approval follows every affected repair | none |
| qa-reviewer | approved | current executed approval follows every affected repair | none |
| architecture-reviewer | approved | current executed approval follows every affected repair | none |
| performance-reviewer | approved | current executed approval follows every affected repair | none |
| operator-signoff | approved | current executed approval follows every affected repair | none |
<!-- wave:review-status end -->

- operator-signoff: <approved when operator confirms closure>

## Dependencies

- Depends on `1seaw retrieval-intent-golden-queries`, specifically completion and baseline publication for `1sear-enh golden-query-retrieval-eval-suite`.
- Within the wave, establish `1wngv` and `1wpag` integrity invariants before final `1wpah` relevance/latency comparison.

## Current Assumptions

- The current Snowflake embedding and MiniLM reranker stack remains fixed.
- SQLite FTS5 and Lance remain the lexical/vector stores.
- A one-time deterministic rebuild is acceptable after the compatibility/version change.

## Outputs Produced or Expected

- Collision-free chunks with absolute source ranges and stable unaffected IDs.
- Self-healing, honestly diagnosed FTS state.
- Filter-aware candidate generation with the inert ANN constants retired and the actual library defaults documented.
- Regression fixtures, corpus censuses, before/after retrieval metrics, and updated architecture contracts.

## Review Checkpoints

- **Delivery reverification cycle 5 and CONVERGENCE — 2026-09-02.** The fifth independent lane audited the cycle-4 repairs and returned the first verdict of this review that names a stopping point: two items worth acting on before close, everything else follow-up, and explicitly *"none of it justifies a fifth repair cycle"*. Both were done.

  **`SEC-RV4-1`: the longest-first prefix ordering was justified in a comment and pinned by nothing.** Sorting shortest-first, or not sorting at all, left all forty-one tests in the module green. It is materially live rather than theoretical: a repository nested under the home directory is the ordinary layout, this machine's is, and the wrong order strips the home prefix first so every failure detail degrades from repo-relative to home-relative and discloses where the repository sits. This is the wave's recurring failure shape one more time — a mechanism whose comment asserts a property no assertion holds. Pinned. The first version of the pin killed only the shortest-first mutant, because the no-sort case is correct by accident of insertion order; a second case with a home directory longer than the root discriminates the ordering itself, and both mutants now fail.

  **`CODE-RV4-1`: the deferral had no destination.** The record said the per-seam fixture work was "routed to a follow-up (`CODE-RV3-1`)", which names the finding rather than a place. The lane's judgment is worth quoting because it is the sharpest sentence in five rounds of review: *"an unrouted deferral is indistinguishable from a dropped one"*, and this wave had already declined the recommendation once. The work is now routed to wave `1wpih index-quality-evaluation-and-ranking`, the successor this wave already feeds.

  **What the lane declined to escalate**, and this record accepts: two residual over-match shapes in the sanitizer that mangle but never leak (a perfect string-level boundary is unreachable, and widening the character class regresses the standalone case, so the tension is genuine rather than an oversight); one further clamp-pin bypass reachable only by deliberate construction, folded with the merge-seam fixtures as the same structural class whose durable replacement is behavioural; and a wall-clock advisory bound that flaked once under concurrent mutation load and passes three of three in isolation. The lane also confirmed the cycle-4 order repair fully, including a vacuity control proving its assertion measures the retry's own write rather than leftover rows, and endorsed the decision to narrow the merge-seam docstring rather than build fixtures inside a delivery review.

  **On the untracked file the lane flagged:** `.wavefoundry/framework/UPGRADE-PROTOCOL.json` is a transient artifact of the upgrade-protocol machinery, has never been tracked, has no git history, and the working tree now carries zero untracked files. Nothing was lost.

- **Delivery reverification cycles 3 and 4 — 2026-09-02: the review turned on the reviewer's own repairs, three times.** Each cycle-2 repair was audited by an independent lane, each audit found real defects in it, and the pattern is worth stating rather than smoothing over: **every round of repairs the coordinator made was defective, and every defect was found by an independent lane rather than by the coordinator.**

  **Cycle 3, from the audit of the cycle-2 repairs.** The most serious item is a **recovery regression the coordinator introduced**: the new store-error classifier omitted `has no column named`, sqlite's message for an insert against a stale-shaped table, which is genuinely structural and DID self-heal before. A lane reproduced it with a real store whose registry was recreated without a column, showing the pre-repair code recovering and the repaired code hard-failing. Repaired, with the marker list then audited for completeness (two further structural messages are unreachable on the current schema) and for over-matching (zero false positives across nineteen operational messages). Three more: the sanitizer still leaked three spellings, including one the oracle scored clean because it compared case-sensitively; the merge-seam pin was defeated by one extra pair of parentheses plus decoy calls; and the clamp test's docstring promised something the test could not do, proven by raising a clamp tenfold with the test green.

  **Cycle 4, from the audit of the cycle-3 repairs.** The separator-insensitive sanitizer had introduced an **over-match**: with no boundary after the last segment, a root that is a string prefix of a sibling directory matched inside it, so on this machine's layout a target repository beside the framework rendered as `-target/...`, a path that exists nowhere and reads as if it were inside this repository. That is squarely in this project's own model, and no assertion caught it because asserting the root is ABSENT is satisfied by a mangled result. Repaired with a boundary, and the home-directory branch, which had stayed a literal case-sensitive replace one line below and leaked in the same three spellings, was routed through the same matcher. Two pins were also strengthened: the clamp helper bound to the first `min(x, int)` in walk order, which a decoy earlier in the function silently repointed, and the store control pinned that the reset RAN but not that it ran BEFORE the retry write, a swap that produced a false success over an emptied registry with the whole module green.

  **One recommendation was declined, and the decline is recorded rather than hidden.** The lane defeated the AST merge-seam pin with a helper defined outside the function reading the coordinate by subscript: the call count stayed at eight, the legacy list stayed empty, 2,027 tests passed, and the legacy key executed ten times. Its recommendation was one behavioural fixture per unpinned seam. The coordinator did not build them. Instead the pin's docstring, which had claimed that spelling tricks could not hide the legacy key, is narrowed to what the pin actually catches — an accidental revert in two spellings — with the lane's bypass and the remaining ones named in place, and the fixture work routed to wave `1wpih index-quality-evaluation-and-ranking`, the successor this wave already feeds. The destination was added on a later lane's insistence: the first wording named the finding rather than a place, and an unrouted deferral is indistinguishable from a dropped one. The three seams are correct today; only their pinning is weak. **This is the second time in this wave a test shipped with a docstring promising more than it delivered**, which is why the narrowing is recorded as a finding rather than as an edit.

  Every cycle-3 and cycle-4 repair is pinned by a named test proven load-bearing under a scratch mutant, including the two mutants the lanes themselves used to defeat the earlier versions.

- **Delivery reverification and cycle-2 repairs — 2026-09-02.** The wave was reopened for review (`wf_reopen_wave(purpose='review')`) with the attestation layer outstanding: implementation and the round-1 repairs were on disk since 2026-08-31, but the ledger still carried every repair as pending, no lane had reverified, and the delivery security seat had never run. Five independent lanes ran on the settled tree, each required to kill its own claim with a scratch mutant.

  **Round-1 repairs reverified.** ARCH-DEL-2 confirmed under four mutants, each precisely killed by a named test (reverting the `OperationalError` arm fails `test_locked_open_is_not_treated_as_corruption` with `OperationalError not raised` while the other five tests in the class stay green; dropping either durable log fails it too; making the corruption branch propagate fails the corruption polarity). RED-DEL-1 confirmed: the qa lane re-derived the census from its own corpus walk (1,743 files, 23,206 prose chunks, zero wrong) rather than trusting the recorded 1,726/22,848, independently covered the **table row-group class the original attestation missed** (2,016 parts, 1,965 exact, 51 superset, zero wrong), verified the cited live example now reads (140, 141), and killed a source-level pre-repair mutant at 1,962 wrong with all four named tests failing. RED-DEL-4 confirmed, with the useful detail that only the test's **reverse-order** case discriminates the partial-seed defect: that single assertion is the whole gate and must not be simplified away. RED-DEL-3 confirmed by re-derivation (`indexer.py::RERANKER_MODEL` is exactly 590 chars with the note absent; the counterfactual reabsorbs it at 1,280). CODE-DEL-2 confirmed under three mutants, including the builder's rejected `chunk_sync_counts`-only alternative, which reproduces the silent kill of the `chunk_index_undercovered` warning and is now caught by two named tests; an independent live probe measured the saving as **larger** than the finding claimed (`probe_state_store` 1,064-1,082 ms against `_chunk_index_coverage` 4.8-8.0 ms). CODE-DEL-3 confirmed under two mutants, the whole-repository census flipping 0 to 5 wrong under either.

  **PERF-DEL-6 terminal: the refutation stands, on stronger ground than it was made.** The finding's premise was a 2.6% pair jitter. An independent lane reproduced that figure to eight decimals from the estimator in force when the receipts were written (`abs(cur_p95 - base_p95) / lower`) and then showed the estimator this repository now ships takes `max(floor, median)` and deliberately excludes p95. Re-derived for the same pair, `code_search` jitter is floor 11.79% / median 18.10% / p95 2.56% — the reported figure understates the median by 7.1x — and against the shipped `PAIR_JITTER_THRESHOLD` of 0.05 **all four tools are contended** (code_search 18.10%, docs_search 20.76%, code_ask 25.03%, code_lexical 36.71%), where the quiet `1seaw` baseline pair tops out at 1.72% floor / 0.98% median. `docs_search` has exactly nine warm samples, so its "p95" is literally the maximum. The pair also refutes itself: both runs share generation 26, attempt `1c7bf6e0`, production digest, fixture digest and environment, yet their `code_search` floors differ by 11.79%, and run1's floor of 612.3 ms is below **both** baseline floors. The two latency exceedances are contention, and the shipped latency clause routes such a shortfall to `operator_review_reasons` for every comparison kind rather than to `violations`.

  **Delivery security seat (first run against the delivered code): approve-with-notes, high confidence.** SEC-1 holds on an executed probe — nine damaged public reads all return typed `query_failed` with every file in the index directory byte-identical, and `server_impl.py` holds **zero** non-comment references to any store-write primitive, so the serving module has no write authority at all. SEC-2 holds: the payload digest is order-independent, written only at publication, and catches both a count-preserving substitution and a delete-plus-duplicate; XOR self-cancellation is unreachable because it needs an even-multiplicity duplicate payload that the registry's uniqueness constraint rejects first. SEC-4 holds: hostile `language` values (quote-breaking, comment-injection, oversize) all resolve to `pushdown=False` and never reach a Lance clause, the allowlist is double-gated by vocabulary membership and a shape regex, and the FTS half binds parameters. SEC-5 holds: every public entry clamps its limit far below the frozen windows.

  **Cycle-2 findings and repairs.** Five defects were found and repaired, each pinned by a named test proven load-bearing under a scratch mutant: **ARCH-RV1-1** and **ARCH-RV1-2**, two residual instances of ARCH-DEL-2's own defect class in the same file, both resetting the store on an operational error — the second silently, returning success after erasing the in-flight fence; **SEC-RV1-1**, the repr-doubled Windows root spelling escaping the public error-string sanitizer; **CODE-RV1-1**, three of RED-DEL-2's four converted merge seams unpinned, so reverting any one of them left the whole suite green; **SEC-RV1-2**, the refill window fallback exceeding its own declared per-source ceiling; plus **QA-RV1-1** (the census scored an escape hatch neither right nor wrong) and **CODE-RV1-2** (seven test classes that could not run in isolation). One process note recorded against this coordinator: the first version of the SEC-RV1-1 test PASSED against the mutant, because it asserted a literal path spelling the leak does not use; the oracle was made separator-blind and only then killed the mutant. **EVID-DEL-2** and **PERF-RV1-1** are record corrections, not code: the computed comparison receipt reads `fail` on two latency rows rather than the `pass` this record previously claimed, and RED-DEL-3's rank-1-to-7 magnitude is withdrawn as unwitnessed.

- **Delivery evidence re-recorded at chunker 41 and PERF-DEL-6 adjudicated — 2026-08-31.** The live index was rebuilt at chunker 41 and a fresh same-generation pair recorded (generation 26, attempt `1c7bf6e0`, production `6107c5da`, `end_digest_verified`, `pass` with zero violations of its own). A coordinator error is recorded here rather than hidden: the first quiet re-run script wrote evaluator output directly onto the published receipt paths, so an invalid run overwrote the baseline half of an already-good pair and the evidence had to be re-recorded; the script now writes to temp files and promotes only after both runs succeed on one frozen generation, with a validator that refuses promotion unless both reports are complete and share an attempt id. On the re-recorded pair the earlier `assessment_boundary_control` nDCG movement did NOT reproduce (0.7309, identical to baseline; recorded as EVID-DEL-1, `not_issue`), and three metrics improved. **Production-identity supersession disclosed 2026-09-02:** the pair binds `server_impl.py` at `00cff68a`, and waves `1wur7`/`1wuju`/`1wybs` moved that module to `c731adb6` **at HEAD**; between the pair and the `1wybs` receipt the other six `PRODUCTION_RETRIEVAL_MODULES` members are byte-identical. **Corrected 2026-09-02 (ARCH-RV1-3):** that is a HEAD reading, and the evaluator's `_production_identity` hashes the WORKING TREE. This wave's own cycle-2 repairs move two production modules past HEAD, `server_impl.py` to `126f00ed` and `index_state_store.py` to `dd430966`, so the working tree's production digest is `00dac006` and **no receipt on file binds it** — including `retrieval-quality-post-1wybs.json`, which the record below names as the standing reference. The pair therefore remains valid evidence for what `1wpif` delivered on the tree it was recorded against, and is HISTORICAL with respect to the current tree, whose standing reference receipt is `docs/reports/retrieval-quality-post-1wybs.json`. No `1wpif` receipt is re-run for the later waves' bytes: under the policy wave `1wybs` shipped, the wave that moves production retrieval bytes records its own pair, and `1wybs` did. Two latency exceedances remained (`code_search` 807.1 ms against a computed 793.9 ms, `docs_search` 783.3 against 763.2), and because the pair's reported jitter was 2.6% the coordinator recorded PERF-DEL-6 stating that the round-1 lane's contention explanation was falsified. An independent performance actor then refuted PERF-DEL-6 at strong confidence, and the refutation stands: a paired ABBA A/B of the delivered `server_impl.py` against HEAD on one index measures plus 0.9 ms at the minimum over 112 samples per arm; the call is 96.1% cross-encoder inference at a fixed 40x512 padded tensor whose cost is structurally invariant to candidate count, text length, corpus size and chunker version (reranked candidate count 30.0 on all 224 calls, candidate text byte-identical between arms), and the entire non-reranker residual is 18.3 ms, so 5% corpus growth cannot contribute more than about 1 ms; the pair refutes itself, since its two runs share every controlled variable yet differ by 11.8% at the MINIMUM, and run1's `code_search` floor of 612.3 ms is below both baseline floors; six induced CPU hogs reproduced a plus 383 ms median shift that fully reversed on removal. The coordinator's premise was itself an artifact: `apply_baseline_comparison` derives pair jitter from `warm_p95_ms` alone, reporting 2.6% where the median jitter was 18.1%, and `docs_search`'s nine warm samples make its "p95" the maximum. Contamination source named: nine long-lived MCP server processes run staleness and projection monitors that fire index refreshes invisibly to `pgrep` and to the evaluator between runs. PERF-DEL-6 therefore carries a `repair_start` recording that the repair is adjudication rather than a code change, and its reverification is held for the performance lane's delivery approval, which is the lane reassessment the ledger requires to clear a required-lane block. The estimator defect is filed as its own change doc (`docs/plans/1wuuh-bug retrieval-eval-jitter-estimator-and-sample-floor.md`), joining `1wtpl` for the store-identity binding.
- **Delivery repair round 1 landed — 2026-08-31: all seven findings repaired, chunker 41, evidence re-run pending.** One builder pass repaired the six open findings plus the two performance notes, each with a falsifying test. RED-DEL-1: `_decompose_oversized_table_chunk` now owns the mapping (parallel text/absolute-source-line pairs, the per-group row offset applied, the prelude/header reproduced on parts 2..n marked generated, a per-part `line_map` so an over-cap part line-wraps with absolute coordinates); the broken remap in `split_large_chunks` is deleted; the live example reads `(140, 141)` instead of `(133, 136)`. A whole-repository coordinate census (1,726 md/rst/adoc files, 22,848 prose chunks) reports **zero wrong**, and is proven non-vacuous by a paired test that restores the pre-repair anchoring and catches it. RED-DEL-2: a new `_candidate_merge_key` (id first, canonical key as fallback) replaces the legacy `(path, lines)` key at **four** seams, one more than the review named (the pinned direct-artifact owner's duplicate removal carried the same defect); a paired test restores the legacy key and asserts the misattribution returns. RED-DEL-3: the retirement note moved above the LanceDB constants block (the `RERANKER_MODEL` chunk is 1,274 to 590 chars). RED-DEL-4: the terminal id guard seeds from the full original id set and loops until unused; the golden-corpus test is no longer tautological (the guard proved a no-op on that corpus, so the emitter output's own uniqueness is asserted). CODE-DEL-2/PERF-DEL-3: coverage moved behind an `include_coverage` flag with a shared `_chunk_index_coverage` producer, so the first hybrid call of a cold epoch calls neither `probe_state_store` nor the coverage producer; the builder's measurement corrected the plan's assumption (the cost is entirely `probe_state_store`; the Lance and registry counts are about 2 ms) and caught that sourcing coverage from `chunk_sync_counts` alone would have silently killed the `chunk_index_undercovered` warning. CODE-DEL-3: both rst/adoc `process_body` passes and `_emit_prose_sections` carry per-line absolute numbers (a base offset cannot describe a body with the title block excised from its middle). PERF-DEL-4: an unknown stored language exits with zero refill rounds and the honest exhausted reason (scoped to `search_code`; the similar walk in `_fts_degraded_serve` is disclosed as unchanged). `CHUNKER_VERSION` is 41 with both coordinate corrections named and the version-40 comment's exactness overclaim scoped per chunk class. Full suite 7,889 tests across 69 files, 3 skipped, one red file (the foreign `test_review_policy` case from another session's `1wpig` edit); focused modules all green with no regression of anything the delivery proved; docs validation ok. The qa lane independently re-verified four repairs against their known-bads at chunker 41 and corrected its own earlier AC-2 attestation (its census covered prose windows only and never probed table row groups, which is exactly what the primer caught). Remaining before approvals: the live rebuild at chunker 41 on a quiet machine, a fresh receipt pair and computed comparison, the AC-2 wording reconciliation the qa lane sharpened, the full-versus-incremental build comparison the wave watchpoint asked for, then the security seat and reverification of every finding by fresh actors.
- **Delivery review round 1 — 2026-08-31: seven typed findings, repair pass in flight.** Independent lanes on the delivered tree: architecture approve-with-notes (write authority, publication-time digest, chokepoint layering, version discipline all verified; ARCH-DEL-1 public-description drift repaired in the carriers; ARCH-DEL-2 escalated to a typed finding), qa approve-with-notes (every AC attested with a non-vacuous oracle; 13 of 13 mutation probes killed by delivered tests; the `[~]` on 1wpah AC-5 and the computed comparison judged honest; QA-DEL-1..7 notes applied or recorded), code approve-with-notes (fifteen contracts executed on the live store, a live-size scratch copy, a whole-repo HEAD differential, and a real end-to-end `indexer.py` heal cycle; CODE-DEL-2 and CODE-DEL-3 escalated), and the red-team delivery primer, whose coordinate census refuted 1wngv AC-2 for table decomposition (RED-DEL-1: 1,075 live table row-group chunks store lines at the table head) and surfaced RED-DEL-2 (dedupe identity delivered at one of three merge seams), RED-DEL-3 (the ANN-retirement comment absorbed into the `RERANKER_MODEL` chunk, originally reported as moving that calibration fixture from rank 1 to 7 and misattributed in the earlier evidence checkpoint as re-chunk movement; **corrected 2026-09-02, PERF-RV1-1:** the rank movement is UNWITNESSED by any frozen receipt. The fixture `constant-value-calibration-reranker` sits at `first_relevant_rank: 1` / `mrr 1.0` for `code_ask` and `code_search` in every receipt on file, including the chunker-39 baseline and both chunker-41 post-`1wpif` runs, so the degradation was seen only on a transient working-tree index that was never receipted. The repair is correct and the post-state is proven good by re-derivation; the stated magnitude has no artifact behind it and is recorded here as a claim withdrawn, not as evidence), and RED-DEL-4 (a latent duplicate in the terminal id guard). ARCH-DEL-2 (the store's corrupt-open recovery deleting a healthy store on a busy-timeout, reproduced on a scratch store; the true cause of the store recreation that blocked the signed cross-generation receipt) is repaired with a lock-wait test and a durable-logged reset. The remaining six findings are being repaired in one builder pass (table row-group coordinates with a multi-group fixture and a whole-repo coordinate census, id-keyed merge seams, the comment move, a self-checking id guard, lazy coverage on the hybrid path, rst/adoc preamble offsets, chunker 41), after which the live index is rebuilt, the receipt pair and computed comparison are re-recorded, and every finding is reverified by fresh actors before the lane, council, and operator approvals. The evaluator identity defect is filed as its own plan (`docs/plans/1wtpl-bug retrieval-eval-store-identity-binding.md`). Performance lane (approve-with-notes, strong confidence): the flagged `code_search`/`docs_search` p95 rise is run contention, not code or index layout (paired A/B on one live index: +5 ms p50 `code_search`, minus 10 ms `docs_search`; the cross-encoder is about 95% of a call and moves only with load; category filters dropped from a 200-candidate rerank to 40 while returning 10 results instead of 1.3); `code_lexical` 671 to 697 ms and 69.8k statements per call at HEAD versus 2 to 4 ms and 23 to 135 statements delivered, by sqlite statement census; refill ceilings, the 16/960 aggregate, envelope growth (about 1.8 KiB worst case against the 4 KiB floor), publication-only census and digest, and the computed receipt's arithmetic all reproduced independently. PERF-DEL-2 (broad): the chunker-40 pair carries about 15% contention inflation (its own jitter 3.9 to 4.9% versus 1.5 to 1.8% for the `1sear` pair), so the next pair is recorded on a quiet machine before it becomes a latency reference; PERF-DEL-3 folded into the CODE-DEL-2 repair (coverage from `chunk_sync_counts`, cutting the first read per epoch from about 795 to about 175 ms); PERF-DEL-4 (unknown-language values walk four empty windows) folded into the repair pass; PERF-DEL-5 hygiene noted.
- **Delivery evidence recorded — 2026-08-31: new standing pair on the chunker-40 index passes; cross-generation comparison against the `1sear` pair computed with zero violations; one evaluator limitation surfaced.** The live index was rebuilt under chunker 40 (the first live exercise of the 1wpag reconcile gate: both FTS tables verify `digest: ok` with the digest recorded at publication, no heal attempt, `id_collisions` zero) and optimized; on one frozen generation (6, attempt `a4298f9b`) the evaluator recorded `retrieval-quality-post-1wpif-run1.json` (`baseline`) and `retrieval-quality-post-1wpif.json` (`pass`, `same_generation_pair`, zero violations, production `8c62c3d0`, chunker 40 / walker 16 / graph builder 45, `end_digest_verified`), reproducibly across three pipeline attempts. The evaluator refused the `cross_generation` comparison against the standing `1sear` pair with `invalid_baseline: baseline repository/index store identity differs`: `_index_identity` binds the sqlite store file inode for every comparison kind, and the store file had been recreated during the evidence window (inode 634552732 to 635682939; no build path recreates it, the cause is the store's corrupt-open recovery branch treating a busy-timeout as corruption, reproduced on a scratch store and recorded as finding ARCH-DEL-2, repaired in this cycle) while every other binding is equal (corpus `bda67546`, evaluator `61ec60da`, models, providers, environment, toggles). The evaluator's own `cross_generation` kind is therefore unusable across the controlled rebuild it exists for; the gate was NOT loosened. Instead `docs/reports/retrieval-quality-post-1wpif-vs-1sear-computed.json` reproduces the evaluator's comparison arithmetic at the data level over the two receipts (holdout aggregate and per-class quality regressions, the seven corpus floors, the `max(25%, 3x jitter)` latency and `max(15%, 4 KiB)` envelope thresholds; no evaluator code imported or patched; both receipts' content digests recorded): verdict `pass`, zero violations. **Superseded 2026-08-31 and corrected 2026-09-02:** that verdict described the chunker-40 computation. The file at that path was overwritten by the chunker-41 recomputation, which reads verdict `fail` on exactly two rows, both `latency_regression` (`code_search` 807.1 ms against a 793.9 ms threshold, `docs_search` 783.3 against 763.2) and both the subject of PERF-DEL-6. Quality is unchanged: zero quality-regression rows, zero critical-floor rows, zero envelope rows. The `fail` is a documented property of the reproduction tool, not a quality result: `compare_retrieval_receipts.py` states in its own divergence note that it classifies a latency shortfall under `violations` while the signed evaluator routes the same shortfall to `operator_review_reasons`, so a `latency_regression` row there reads as "the signed gate would route this to operator review", not "the signed gate would fail". Re-running the tool on 2026-09-02 over the same two receipts reproduced every recorded number (same two violations, identical `metric_deltas`, `critical_floors` and `bindings`; the re-run additionally emits a per-tool `jitter_source` key the recorded file predates). Holdout `code_ask` is identical to the `1sear` baseline (recall 0.594, nDCG 0.527, question-type accuracy 0.91, abstention 1.0); `code_search`, `docs_search`, and `code_lexical` holdout metrics unchanged; calibration-side movement from the re-chunk is below the gate's holdout scope and is disclosed for the delivery lanes. **Corrected 2026-09-02:** overall `code_search` nDCG did not fall to 0.647 as recorded; on the chunker-41 receipt it ROSE from 0.7028 to 0.7286. `code_ask` recall 0.5806 to 0.5726 stands as recorded. Performance (**figures corrected 2026-09-02, EVID-DEL-2:** the values below were the chunker-40 readings and were left standing when the receipt was recomputed at chunker 41; they are now the recomputed ones, and they no longer contradict the corrected verdict recorded above): `code_lexical` warm p95 672.9 ms to 28.6 ms (the epoch-cached O(1) healthy path); `code_ask` 3,697.3 to 4,424.0 ms (threshold 4,622); `code_search` 635.1 to 807.1 ms (threshold 793.9) and `docs_search` 610.5 to 783.3 ms (threshold 763.2), the two exceedances PERF-DEL-6 adjudicated as contention; every envelope within threshold. Evaluator follow-up recorded: bind the store by repository identity and path (or bind the file inode only for same-generation pairs) so `cross_generation` receipts survive a compatibility rebuild; owner is the `1wpih` successor evaluator. Pipeline note: the pre-1wpif MCP server (chunker 39 in memory) judged the chunker-40 index stale and re-triggered builds every quiet period until `wf_reload_mcp` ran; reload the server before any post-rebuild evidence run.
- **Implementation complete for all three changes — 2026-08-31: evidence run pending.** Builder lanes (software-engineer, serialized in the recorded order): `1wngv` (chunker 40; per-file ID uniqueness unconditional for every emitter after a whole-repo census exposed a structured-JS collision class beyond the flat emitter; splice-aware absolute line mapping through every prose caller and the universal oversize guard; content-aware retrieval dedupe identity; ast-based Python module summaries; materialized-row collision census with health/build diagnostics; 56,387-chunk census now reads zero collisions against the pre-fix 150 groups / 563 losses), `1wpag` (order-independent keyed FTS payload digest written only at publication under the build lock; the reconcile in-sync return now requires liveness, parity, and digest; one probed-serving chokepoint `_fts_probed_fetch` for `code_lexical`, the hybrid FTS halves, and the degraded fallbacks; serving errors probe, return typed failure, and only schedule healing through the existing background-refresh trigger with a lock-owner-written once-per-epoch marker; epoch-cached O(1) healthy reads proven by a sqlite trace hook on `code_lexical`; nine-state fault battery in `test_fts_query_honesty.py`), `1wpah` (allowlisted language/category predicates pushed into Lance and FTS5 before the bounded window; four-window refill with typed `bounded_ceiling_reached`/`substrate_exhausted` and per-source plus public-call accounting, the `code_ask` aggregate proven at 16/960 with the keyword pass as a fifth source and the assessment expansion counted; reranker input pinned at the former window; inert ANN constants retired from both definition sites with defaults documented and an executed Lance `explain_plan` proof). Coordinator verification: full suite 7,867 tests across 69 files uncached, docs validation ok; the single red test (`test_review_policy.py` corpus sweep) is caused by another session's in-flight edit to wave `1wpig`'s `1wpaj` change doc and is outside this wave. Open before review: the chunker-40 rebuild of the live index, a fresh same-generation pair on the new generation, and the `cross_generation` receipt against the standing `1sear` pair (1wpah AC-5 and both AC-6 full-suite clauses close on that evidence and on the foreign failure clearing).
- **Plan review — 2026-08-30: COMPLETE.** Requirements, scope, and acceptance criteria were walked across chunk identity/source ranges, FTS recovery/query honesty, and candidate generation. Resolved branches pin source-derived range payloads separately from breadcrumbs, make healthy-semantic/failed-lexical hybrid behavior non-destructive but diagnostic, preserve typed failure for lexical-only/degraded substrate failure, and require individually frozen evaluation runs with a recorded two-generation controlled rebuild comparison.
- **Prepare-phase Wave Council review — 2026-08-30: REVIEWED, READINESS DEFERRED** (moderator: wave-council; primer-depth: standard; seats: red-team, security-reviewer; strongest-challenge: the wave cannot claim a trustworthy before/after improvement without the production-path `1sear` baseline it explicitly depends on; strongest-alternative: remove the dependency and use local bespoke fixtures only — rejected because the wave changes indexed content, filtering, and ANN behavior together and therefore needs the standing end-to-end gate). Security verdict: approved-with-notes, no credible threat under the local/operator-owned model; keep degraded fault injection on copied stores, all refill/probe work hard-bounded, query filters parameterized, and read-only retrieval paths non-healing. No readiness approval is recorded until `1sear` publishes its baseline.
- **Prepare-phase lane reviews — 2026-08-31: all four required lanes approve-with-notes on the amended packet (independent fresh actors).** code-reviewer (strong confidence): fifteen load-bearing seams verified at their symbols, every claimed defect reproduced from source, no invariant conflicts; six letter-precision pins applied (CODE-RDY-1 healthy `docs_search` has no FTS half; CODE-RDY-2 the `code_ask` aggregate formula counts the assessment-class expansion and declares injection passes outside the substrate-query definition; CODE-RDY-3 the universal oversize guard `split_large_chunks`/`_line_wrap_chunk` joins the splice-mapping caller set; CODE-RDY-4 both constant definition sites named; CODE-RDY-5 detection-at-probe-boundary wording; CODE-RDY-6 note on lock-site counting). qa-reviewer (strong confidence): every AC executable with a non-vacuous oracle against reproducible known-bads; pins applied (QA-RDY-1 stale exact-versus-ANN wording swept from the wave record; QA-RDY-2 re-damage-same-epoch and FTS-ahead fault states added to the AC-5 battery; QA-RDY-3 named zero-COUNT measurement mechanism; QA-RDY-4 aggregate ceiling pinned; QA-RDY-5 census timing protocol declared). architecture-reviewer (strong confidence): ownership, seam placement, and compatibility contracts sound; pins applied (ARCH-RDY-1 wave-record prose sweep; ARCH-RDY-2 AC-5 demands a fresh `production_change_same_generation` receipt; ARCH-RDY-3/4 `performance-budget.md` and `data-and-control-flow.md` added to the Affected lists; ARCH-RDY-5/6/7 notes carried to the implementer brief). performance-reviewer (strong confidence): cost contracts coherent and bounded within the standing gate; pins applied (PERF-RDY-3 the AC-8 scan prohibition reaches the per-call `_state_store_health_summary` tie-in; PERF-RDY-4 the reranker input window is unchanged by refill or is part of the accounting; PERF-RDY-5 timing protocol); bookkeeping corrections recorded (the binding `code_lexical` constraint is the relative threshold, about 170 ms of headroom at p95; `fts_probe` is one MATCH plus four COUNTs per table). Implementer-brief notes carried forward: reuse an existing sanctioned trigger path for heal scheduling (ARCH-RDY-6); the constant-value pin tests at their current sites retire with the constants (QA-RDY-6); AC-5 collision injection happens at the store-row layer below the fixed chunker (QA-RDY-7).
- **Prepare-phase Wave Council [prepare-council] — 2026-08-31: PASS** (moderator: wave-council; primer-depth: standard; seats: red-team, security-reviewer; rotating-seat: security-reviewer; strongest-challenge: 1wpah's ANN gate was written as a measured decision no shipped tooling can compute — no exact-search reference, no overlap metric, no improvement certifier, per-query floors that quantize to 1.0 — so the wave's most likely failure was recording an unmeasured "measured" rejection; strongest-alternative: make the wave write-authority-neutral and measurement-honest at once — retire the inert constants under the retire-and-document branch, relocate the threshold battery to `1wpih`, and land 1wpag as honest-diagnostics-plus-digest with healing kept at today's two build-lock seams — ADOPTED as amendments A1/A2).
- Council seat evidence — red-team (adversarial primer, standard depth): ten load-bearing plan claims verified by execution against the tree (150 colliding ID groups / 563 dedupe losses reproduced in-process; 278 duplicate-ID Lance rows in the live store; the registry-parity early return, healthy-zero `code_lexical` path, post-window language filtering, and inert `LANCEDB_NPROBES`/`LANCEDB_REFINE_FACTOR` constants all confirmed at their symbols); seven primer questions issued; hazards recorded on fixture-shaped census risk, fence-splice under-scope, and probe-cost headroom (~327 ms under the 1seaw `code_lexical` ceiling).
- Council seat evidence — security-reviewer (rotating): answered all seven primer questions with code evidence; found SEC-1 (severe: AC-8 as drafted let a serving error heal inline, granting the read path write authority — pinned by amendment A2), SEC-2 (broad: undefined digest cannot falsify equal-count substitution — pinned by A3), SEC-3/SEC-4 (minor: unsanitized `detail` strings, string-built Lance predicates — pinned by A7 and the allowlist note), SEC-5 (refill/oversize abuse adequately bounded once A2 lands); verdict READY-WITH-AMENDMENTS at strong code-evidence confidence.
- Council synthesis: PASS with amendments A1-A7 applied to the change docs in this cycle (see the three Decision Logs dated 2026-08-31); the 2026-08-30/31 dependency blocker is discharged — the `1sear` standing pair and comparison receipt exist and were verified by the primer (`retrieval-quality-baseline.json` verdict pass, same-generation pair, zero violations, production `dab71287`); readiness is recorded as the typed `wave-council-readiness` approval on the amended packet.
- **Prepare re-review — 2026-08-31: PLAN REPAIRED, READINESS DEFERRED.** Independent red-team, code, and performance reviews confirmed the dependency blocker and found four underspecified contracts. The packet now freezes deterministic chunk/dedupe identity, equal-count keyed FTS integrity with epoch-cached O(1) healthy reads, a four-round/240-row refill ceiling with typed termination, and exact ANN recall/latency/payload thresholds. Readiness still requires the final current-schema `1sear` two-run baseline pair and comparison receipt; no typed readiness approval is recorded before that evidence exists.
- Prepare: after `1sear` lands, confirm its two-run baseline pair and comparison receipt, compatibility/rebuild plan, and public diagnostic contracts. Each before/after evaluation run is individually generation-frozen; a controlled chunker rebuild comparison records its two generations explicitly.
- Mid-wave: review the shared state-store/server boundary after chunk/FTS repairs and before the candidate-generation comparison.
- Delivery: replay duplicate-ID, wrong-line, missing-FTS, rank-31 language, and per-file fill probes, plus the ANN-constant-retirement proof (constants absent from both definition sites, defaults documented, fresh comparison receipt clean).

## Completion Criteria

- Every required AC in all three admitted changes is `[x]` or operator-rationalized `[~]`.
- Production-path golden evaluation holds or improves on its required metrics.
- Full framework tests, docs validation, full/incremental equivalence, and deterministic rebuild evidence pass.

## Handoff or Next-Wave Notes

- After closure, `1wpih index-quality-evaluation-and-ranking` may use the corrected lexical/candidate baseline for measured relevance improvements.

## Context Efficiency

<!-- wave:context-efficiency begin -->

Estimated context avoided uses whole eligible text-file, workflow-prompt and derived-artifact credits, minus recorded request and response tokens. This baseline does not prove what an agent otherwise would have read or spent. Any quality-equivalent paired-evaluation residual is recorded separately in the checkpoint state and included in the total.

| Stage | Tool calls | Estimated context avoided |
| --- | ---: | ---: |
| plan | 121 | 2,482,203 |
| implement | 430 | 8,268,996 |
| review | 363 | 15,717,020 |
| **Total** | **914** | **26,468,219** |

<!-- wave:context-efficiency-state {"generation":782,"measurement_status":"healthy","pending":false,"schema_version":1,"stages":{"implement":{"calls":430,"content_source_credit":9286236,"derived_artifact_credit":0,"direct_net":8268996,"estimated_tokens_saved":8268996,"matched_pair_residual":0,"paired_evaluation_count":0,"request_debit":16455,"response_debit":1366176,"source_credit_count":237,"source_credit_drop_count":0,"structural_source_credit":362097,"workflow_prompt_credit":3294},"plan":{"calls":121,"content_source_credit":2610005,"derived_artifact_credit":8179,"direct_net":2482203,"estimated_tokens_saved":2482203,"matched_pair_residual":0,"paired_evaluation_count":0,"request_debit":8978,"response_debit":392607,"source_credit_count":156,"source_credit_drop_count":0,"structural_source_credit":262098,"workflow_prompt_credit":3506},"review":{"calls":363,"content_source_credit":16723921,"derived_artifact_credit":4660,"direct_net":15717020,"estimated_tokens_saved":15717020,"matched_pair_residual":0,"paired_evaluation_count":0,"request_debit":124771,"response_debit":888679,"source_credit_count":497,"source_credit_drop_count":0,"structural_source_credit":0,"workflow_prompt_credit":1889}},"store_instance_id":"f294635fbf24489a9a50af63451b2532","totals":{"calls":914,"content_source_credit":28620162,"derived_artifact_credit":12839,"direct_net":26468219,"estimated_tokens_saved":26468219,"matched_pair_residual":0,"paired_evaluation_count":0,"request_debit":150204,"response_debit":2647462,"source_credit_count":890,"source_credit_drop_count":0,"structural_source_credit":624195,"workflow_prompt_credit":8689},"wave_id":"1wpif index-content-and-retrieval-correctness"} -->
<!-- wave:context-efficiency end -->

## Estimated Exploration Avoided

<!-- wave:exploration-avoided begin -->

This is a bounded estimate from exact-match memory advisories. It is not added to measured Context Efficiency.

| Advisory surfaces | Citations | Records credited | Estimated tokens avoided |
| ---: | ---: | ---: | ---: |
| 13 | 0 | 8 | 7,890,126 |

estimated: a surfaced (or cited) advisory does not prove a re-exploration was avoided; this is grounded in the measured cost of the original exploration, scaled by a bounded exact-match attribution, and is NEVER summed into the measured Context Efficiency token total.

<!-- wave:exploration-avoided-state {"cited_events":0,"credited_records":8,"estimated_exploration_avoided":7890126,"surfaced_events":13} -->
<!-- wave:exploration-avoided end -->
