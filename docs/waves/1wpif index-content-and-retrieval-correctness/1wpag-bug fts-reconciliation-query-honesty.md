# Heal FTS State and Report Honest Query Failures

Change ID: `1wpag-bug fts-reconciliation-query-honesty`
Change Status: `implemented`
Owner: Engineering
Status: implemented
Last verified: 2026-09-02
Wave: 1wpif index-content-and-retrieval-correctness

## Rationale

FTS reconciliation can declare the lexical layer synchronized when registry IDs match Lance even if an FTS virtual table is missing, recreated empty, or internally corrupt. Direct lexical search then reports a healthy zero-result query instead of an infrastructure failure. This makes exact-token recall silently unavailable on a supported path and gives operators and agents incorrect diagnostics.

## Requirements

1. Reconciliation SHALL verify registry-to-FTS liveness, row parity, and keyed payload integrity before taking the in-sync early return. Equal-count substitution or payload corruption SHALL not pass as synchronized. The integrity digest is defined (prepare-council A3): it covers the FTS-served payload fields per row (id, path, kind, language, tags, lines, text) — or an equivalent registry digest paired with exact FTS-versus-registry content parity — aggregated order-independently, computed and written only at the publication boundary under the build lock, and its stated threat model is corruption/partial-write detection under the local single-writer model, not tamper evidence.
2. Missing, empty, or corrupt FTS derived state SHALL heal from authoritative rows during the next ordinary reconcile/build path.
3. Public lexical and hybrid retrieval SHALL distinguish query failure or undercoverage from a genuine zero-hit corpus result.
4. Lexical health SHALL report per-table Lance, registry, and FTS parity plus the last keyed-integrity result without scanning corpus-sized state on each query. Healthy public retrieval SHALL perform only an O(1) completed-epoch/cache lookup; full probes run at reconcile/open/epoch change or after a serving error.
5. Documentation SHALL consistently identify SQLite FTS5 as the active lexical engine.
6. When semantic retrieval remains healthy but lexical state is unavailable, hybrid tools SHALL return successful semantic results plus a typed lexical-undercoverage diagnostic. Lexical-only or degraded requests that cannot serve their promised path SHALL return `query_failed` rather than a healthy zero.
7. Healing SHALL remain build-lock and epoch owned, run at most once per damaged table per epoch, and never be triggered by a read-only public query. Pinned (prepare-council A2): a serving error performs one bounded probe, returns the typed failure, and at most schedules healing; healing executes solely under the build lock through the ordinary reconcile/build path; the once-per-damaged-table-per-epoch marker is written only by the lock owner and lives with the build state; the public query path performs no store writes (the epoch-cached integrity result is read-compare-only, written at publication or held in-process keyed by the state token).

## Scope

**Problem statement:** Derived lexical state can be absent while reconciliation and public search claim it is healthy.

**In scope:** FTS probes, reconcile/heal decisions, public query diagnostics, parity health, corruption fixtures, and stale Tantivy documentation.

**Out of scope:** Tokenizer changes, ranking changes, query history, dashboard presentation already covered by `1uqec`, and replacing SQLite FTS5.

## Acceptance Criteria

- [x] AC-1: Dropping either FTS virtual table is detected by ordinary reconciliation and rebuilt from authoritative rows. (Evidence: `ReconcileHonestyTests.test_dropped_table_is_detected_and_rebuilt` and `ZeroChangeHealProbeTests.test_dropped_table_routes_the_idle_build_to_the_reconcile`; `reconcile_chunk_index` result carries `fts_repaired: probe_failed`.)
- [x] AC-2: Corrupting or removing an FTS shadow row, or substituting one keyed row/payload while preserving every relevant count, is detected at the next probe boundary (reconcile/open/epoch change/serving error; never on a warmed healthy read, per AC-8) and repaired without requiring a manual rebuild command. (Evidence: `test_corrupt_shadow_row_is_detected_and_rebuilt`, `test_equal_count_substitution_is_detected_and_rebuilt` (asserts the pre-1wpag `fts_probe` still reads the substitution as live, then the digest catches it), `test_truncated_table_is_detected_and_rebuilt`, `ZeroChangeHealProbeTests.test_equal_count_substitution_routes_the_idle_build_to_the_reconcile`, and `ProbedServingTests.test_substitution_after_a_warmed_read_is_detected_at_the_next_epoch_transition`.)
- [x] AC-3: Before repair, every affected public query returns a typed failure/undercoverage diagnostic and never a healthy zero-result explanation. The affected set is enumerated (prepare-council A5, refined by CODE-RDY-1): `code_lexical`, the hybrid FTS half of `code_search`/`code_ask` (healthy `docs_search` is dense-only and participates via its degraded lexical fallback), and the degraded fallbacks, all routed through one shared probed-serving chokepoint so the paths cannot re-diverge; typed-failure `detail` strings are bounded and path-sanitized (repo-relative, capped length; prepare-council A7). (Evidence: `test_code_lexical_dropped_table_is_typed_query_failed`, `test_code_lexical_equal_count_substitution_is_typed_at_the_probe_boundary`, `test_every_public_fts_path_serves_through_the_chokepoint` (source pin: one `iss.fts_search(` call inside `_fts_probed_fetch`, plus one chokepoint call per tool), `test_failure_detail_is_bounded_and_repo_relative` (cap 240 chars, root prefix absent, applied to the former `_fts_degraded_serve` `str(exc)` detail too).)
- [x] AC-4: Healthy zero-result queries remain distinguishable and preserve current response contracts. (Evidence: `test_code_lexical_healthy_zero_and_healthy_hits_keep_their_contract`, `test_hybrid_healthy_controls_carry_no_undercoverage`, `test_cold_open_without_a_store_keeps_the_not_built_contract`; the 933-test `test_server_tools_retrieval.py` module and `CodeLexicalToolTests` pass unchanged apart from two fixtures that now publish an empty layer through the canonical producer.)
- [x] AC-5: Health exposes per-table FTS parity and the epoch-cached keyed-integrity result; tests cover cold open, interrupted publication, missing table, empty table, corrupt row, equal-count substitution, healthy controls, re-damage of an already-healed table within the same epoch (the marker is set: no second heal, and typed failure persists until the next epoch; QA-RDY-2), and FTS-ahead orphan rows (FTS count above registry, the symmetric parity direction). (Evidence: `test_health_exposes_parity_and_the_epoch_cached_verdict` (`state_store.fts` per table + `fts_integrity_failed`), battery: `test_cold_open_control_is_provisioning_not_damage`, `test_interrupted_publication_heals_under_the_open_attempt`, `test_interrupted_publication_refuses_then_recovers`, `test_dropped_table_is_detected_and_rebuilt`, `test_emptied_table_beside_registry_is_detected_and_rebuilt`, `test_corrupt_shadow_row_is_detected_and_rebuilt`, `test_equal_count_substitution_is_detected_and_rebuilt`, `test_healthy_control_is_quiet_and_not_rebuilt`, `test_heal_marker_suppresses_a_second_heal_within_the_same_epoch`, `test_fts_ahead_orphan_row_is_detected_and_rebuilt`.)
- [x] AC-6: FTS architecture documentation names SQLite FTS5 consistently; this change's own suites and every test it adds pass, the documents this change edits validate, and no failure elsewhere is attributable to this change. (Evidence: `chunking-and-indexing-pipeline.md` "Index creation" names SQLite FTS5 and the only remaining Tantivy mentions are historical retirement notes; `wf_validate_docs` passes; the 36-test `test_fts_query_honesty.py` battery this change adds passes alongside `test_fts_lexical_layer.py` and the 933-test `test_server_tools_retrieval.py` module. Locality repair and closure 2026-09-02: the criterion formerly asserted whole-repository suite state, which this change does not control, and it was held at `[ ]` by a failure outside this wave (`test_review_policy.ReviewLoopFrictionPolicyTests.test_declaration_change_loses_no_lane_anywhere_in_the_corpus`, tripped by wave `1wpig`'s in-flight `1wpaj` declaration edit). That edit was repaired on operator instruction on 2026-09-01 and the suite has been green since; the whole-suite result is now recorded as a close-time gate in the wave record instead of as this change's acceptance criterion. Advisory sensor `ac_asserts_repository_state`, wave `1wur7`.)
- [x] AC-7: Hybrid requests preserve healthy semantic results with a lexical-undercoverage diagnostic, while lexical-only/degraded requests return typed `query_failed`; adjacent healthy-zero controls remain unchanged. (Evidence: `test_hybrid_code_search_keeps_semantic_results_and_flags_undercoverage`, `test_hybrid_code_ask_keeps_citations_and_flags_undercoverage`, `test_fusion_kill_switch_suppresses_the_hybrid_diagnostic`, `test_code_lexical_dropped_table_is_typed_query_failed` (docs table still serves lexical-only), `test_hybrid_healthy_controls_carry_no_undercoverage`.)
- [x] AC-8: A warmed healthy public query executes no FTS/registry/shadow `COUNT(*)` or corpus-sized integrity scan, including the per-call `_state_store_health_summary` quick_check/count tie-in on the healthy path (PERF-RDY-3); the assertion is measured through a named mechanism (a sqlite trace hook via `set_trace_callback` or a store-seam spy counting queries; QA-RDY-3) and asserted on `code_lexical` specifically; an epoch transition performs one bounded probe/heal sequence under the build lock, a serving error performs one bounded probe plus typed failure and at most schedules healing through the build path, the result is cached, and subsequent reads are non-healing and non-writing. (Evidence: `test_warmed_healthy_read_runs_no_counts_no_health_scan_no_probe` (sqlite `set_trace_callback` on every read-only store connection plus spies on `fts_state_verdict` and `_state_store_health_summary`, asserted on `code_lexical` for tables both/code/docs), `test_serving_error_probes_once_types_failure_and_schedules_at_most_once` (exactly one re-probe, one `_start_background_index_refresh` request), `test_query_path_performs_no_store_writes` (store bytes and mtime unchanged across damaged reads; no marker written), `test_heal_marker_suppresses_a_second_heal_within_the_same_epoch`; probe-boundary cost measured at 28.6 ms median for the full verdict on 8,000 rows / 4.6 MB text, recorded in `performance-budget.md`.)

## Tasks

- [x] Integrate `fts_probe` into reconcile and derived-state healing decisions. (`index_state_store.fts_state_verdict` wraps `fts_probe` + digest compare; `reconcile_chunk_index` gates its in-sync return on it and heals through the ordinary rebuild; `indexer._chunk_index_needs_heal` consults it on the zero-change path.)
- [x] Route direct lexical and hybrid lexical candidates through shared probed serving behavior. (`server_impl._fts_probed_fetch` is the only `fts_search` caller; `code_lexical_response`, `_fts5_lexical_search`, `_lexical_candidates`, `_fts_degraded_serve` all serve through it; pinned by `test_every_public_fts_path_serves_through_the_chokepoint`.)
- [x] Extend health payloads with cheap per-table parity state. (`_state_store_health_summary` gains `fts.{docs,code}` = ok/reason/live/fts_rows/registry_rows/digest/digest_recorded/heal_attempt/epoch; `index_health` emits `fts_integrity_failed`.)
- [x] Add a keyed FTS integrity digest/check at publication boundaries and cache its result by completed epoch. (Digest written inside the writer transactions of `rebuild_chunk_index` and `_apply_chunk_deltas_locked` (XOR maintenance); serving verdict cached per build-state token in `_FTS_SERVE_STATE`.)
- [x] Add injected-failure and recovery tests. (`tests/test_fts_query_honesty.py`: 36 tests, real store copies damaged with direct sqlite; two `DegradedFtsFallbackTests` fixtures moved from raw DELETEs to the canonical empty-layer producer.)
- [x] Remove stale Tantivy references from current architecture documentation. (`chunking-and-indexing-pipeline.md` Index creation section now names SQLite FTS5; the remaining Tantivy mentions are the historical retirement notes.)
- [x] Add hybrid-success and lexical-only/degraded-failure response-contract fixtures. (`ProbedServingTests`: hybrid `code_search`/`code_ask` keep semantic results with `lexical_undercoverage`; `code_lexical` returns typed `query_failed`; degraded fallbacks keep the typed serve result; healthy-zero controls unchanged.)

## Agent Execution Graph

| Workstream | Owner | Depends On | Notes |
| --- | --- | --- | --- |
| State reconciliation | implementer | — | Probe and healing contract |
| Serving diagnostics | implementer | State reconciliation | Shared public behavior |
| Verification/docs | qa-reviewer | Both | Fault injection and current engine docs |

## Serialization Points

- `.wavefoundry/framework/scripts/index_state_store.py`, `.wavefoundry/framework/scripts/indexer.py`, `.wavefoundry/framework/scripts/server_impl.py`
- `.wavefoundry/framework/scripts/tests/`, `docs/architecture/chunking-and-indexing-pipeline.md`, `docs/architecture/search-architecture.md`

## Affected Architecture Docs

- `docs/architecture/chunking-and-indexing-pipeline.md`
- `docs/architecture/search-architecture.md`
- `docs/architecture/data-and-control-flow.md` (Path 5 steps 12-13 document the reconcile/derived-state flow this change alters; ARCH-RDY-4)
- `docs/architecture/performance-budget.md` (AC-8's warmed-read O(1) and bounded-probe invariants belong in the 1sed7 structural-budget table; ARCH-RDY-4)

## AC Priority

| AC | Priority | Rationale |
| --- | --- | --- |
| AC-1 | required | Missing FTS state must not publish as healthy. |
| AC-2 | required | Ordinary recovery is the reliability outcome. |
| AC-3 | required | False healthy zeroes mislead every retrieval consumer. |
| AC-4 | required | Honest failure reporting must not break genuine misses. |
| AC-5 | required | Fault-state coverage guards the complete recovery contract. |
| AC-6 | important | Documentation and full validation prevent contract drift. |
| AC-7 | required | Graceful degradation must preserve available results without disguising lexical failure. |
| AC-8 | required | Correctness checks must not turn every lexical query into a corpus-sized health scan or mutation path. |

## Progress Log

| Date | Update | Evidence |
| --- | --- | --- |
| 2026-09-02 | Delivery reverification cycles 3 and 4. An independent lane audited the cycle-2 store repairs and found a **recovery REGRESSION the coordinator introduced**: `_is_missing_object_error` omitted `has no column named`, which is structural and DID self-heal before, so a stale-shaped table went from reset-and-retry to a hard failure. Marker added (`ARCH-RV2-1`), with the exclusion of `no such module` explained in place: `reset()` drops virtual tables and needs the module, so a reset could not repair that case. The lane's marker-completeness audit found no other live omission and its over-match audit found zero false positives across 19 operational messages. The next lane found the control still pinned only that the reset ran, not that it ran BEFORE the retry write, and demonstrated a write-then-reset order returning success over an emptied registry with the module green; the control now asserts the retry's row survives (`ARCH-RV3-1`). Sanitizer: cycle 3 replaced literal prefix matching with a separator- and case-insensitive matcher after three more spellings were found leaking (`SEC-RV2-1`); cycle 4 then repaired an **over-match that repair introduced** — with no boundary after the last segment a sibling directory sharing the root prefix was mangled, so on this layout `wavefoundry-target` rendered as `-target` (`SEC-RV3-1`) — and routed the home directory through the same matcher, since it had been a literal case-sensitive replace one line below and leaked in the same three spellings (`SEC-RV3-2`). | `StoreSubstrateTests.test_rebuild_recovers_from_a_stale_column_shape`, `...test_rebuild_still_resets_on_structural_damage` (now order-pinning), `ProbedServingTests.test_failure_detail_does_not_mangle_a_sibling_of_the_root`, `...test_failure_detail_strips_the_home_directory_in_every_spelling`. Landing rule: deleting the marker fails the stale-column test; swapping write/reset fails the control; dropping the boundary fails the sibling test on 2 subtests; reverting home to a literal replace fails on 3. |
| 2026-09-02 | Delivery reverification cycle 2, store-integrity repairs. The independent architecture lane confirmed ARCH-DEL-2's repair under four scratch mutants but found the same defect class alive at two more sites in the same file, both of which reset the store on an operational error. **ARCH-RV1-1**: `meta_all` swallowed every `sqlite3.Error` into `{}`, so `versions_current()` read False against a current store and `ensure_current()` dropped every resident table with a self-refuting diagnostic (`found '6', expected '6'`). **ARCH-RV1-2**: `rebuild_chunk_index`'s reset-and-retry arm caught every `sqlite3.Error`, so a lock or IO error wiped the store, erased the in-flight build fence, and then RETURNED SUCCESS. Both now route through a shared `_is_missing_object_error` classifier: a genuinely absent object still resets, every other operational error propagates with the store preserved and the deferral durably logged. The `rebuild_chunk_index` docstring claiming "on a sqlite error ... recovery is free" is corrected in place. **SEC-RV1-1**: the delivery security seat found `_bounded_failure_detail` stripped only single-separator root spellings, so the repr-doubled Windows form survived the backslash normalization and left an absolute root in a public error string, regressing the 1uu9z no-path-leak contract that `1wybs` re-hardened one commit earlier. | `StoreSubstrateTests.test_meta_read_lock_error_never_resets_the_store`, `...test_rebuild_defers_on_a_lock_error_and_preserves_the_store`, `...test_rebuild_still_resets_on_structural_damage` (control), `ProbedServingTests.test_failure_detail_strips_every_root_spelling_including_repr_doubled`. Landing rule: mutant A (meta_all swallows again) fails with `reset must not run`; mutant B (reset on every error) fails with `OperationalError not raised` while the control still passes; the sanitizer mutant fails showing the leak `C://Users//op//repo//.wavefoundry//index//s.sqlite`. |
| 2026-09-02 | AC-6 locality repair and CLOSURE. The criterion asserted whole-suite state this change does not control and had been held at `[ ]` since 2026-08-31 by a failure outside this wave: wave `1wpig`'s in-flight `1wpaj` declaration edit tripping `test_review_policy.ReviewLoopFrictionPolicyTests.test_declaration_change_loses_no_lane_anywhere_in_the_corpus`. That edit was repaired on operator instruction on 2026-09-01 (the glob removed from its Serialization Points bullet), so the external blocker is gone. The criterion is narrowed to what this change controls and marked `[x]`; the whole-suite result is recorded as a close-time gate in the wave record. No code changed. | Advisory sensor `ac_asserts_repository_state` cleared for this document; the resolution is recorded in `docs/agents/session-handoff.md` under the foreign-failure note. |
| 2026-08-31 | Thought: delivery-repair plan for CODE-DEL-2 and PERF-DEL-3 on this change's seams. The chokepoint's `_coverage_for()` runs on every serving outcome, and `_fts_serving_coverage` sourced the dict from `_state_store_health_summary`, which re-runs `probe_state_store` (structural quick_check) and opens Lance to count rows AFTER `fts_state_verdict` already established liveness, parity and the keyed digest for the same epoch. Two moves, both keeping AC-8: (a) source coverage from the epoch-cached verdict's live `registry_rows` plus the recorded `chunk_sync_counts` meta, so the probe boundary computes coverage from work it has already done; (b) let the two hybrid halves that never read `coverage` (`_fts5_lexical_search`, `_lexical_candidates`) opt out through an explicit `include_coverage=False`, so the first hybrid call in an epoch does no coverage work at all while `code_lexical` and the degraded fallbacks keep reporting it. | This log row; the repair row below. |
| 2026-08-31 | Delivery repair landed (PERF-DEL-3). The `chunk_index` coverage computation moved out of `_state_store_health_summary` into a shared `_chunk_index_coverage(root, tables)` producer that both consumers now use, so the serving boundary computes the SAME compare `index_health` reports without the `probe_state_store` structural quick_check that measured 674 ms on this repository's store (against 1 ms for the registry count and roughly 1 ms for the Lance metadata row count). `_fts_probed_fetch` gained `include_coverage`, and the two hybrid halves that never read `coverage` (`_fts5_lexical_search`, `_lexical_candidates`) pass False, so the first hybrid `code_search` / `code_ask` in an epoch does no coverage work at all. The per-epoch verdict cache and the AC-8 warmed-read contract are unchanged (coverage is now cached per table in the same build-state-keyed entry), and the `chunk_index_undercovered` warning on `code_lexical` keeps its exact pre-repair behavior because the compare itself did not change. | test_fts_query_honesty.py 38 tests OK (2 new: coverage computed with no `_state_store_health_summary` / `probe_state_store` call, and the chokepoint's `include_coverage` contract per caller); test_retrieval_candidate_generation.py `ColdEpochHybridCostTests` (public level: the first hybrid `code_search` and `code_ask` of a cold epoch call neither `probe_state_store` nor `_chunk_index_coverage`, while `code_lexical` on the same cold epoch still reports covered coverage). |
| 2026-08-31 | Live exercise (coordinator): the chunker-40 rebuild of the real index ran through the new reconcile gate; both FTS tables verify `live`, row parity 26,617/26,617 and 8,245/8,245, `digest: ok` with the digest recorded at publication, no heal attempt; `index_health` now carries the `fts` block per table. Standing-gate effect: `code_lexical` warm p95 673 ms to 25 ms on the new same-generation pair (`retrieval-quality-post-1wpif.json`), the epoch-cached healthy path replacing the per-call health summary. | `index_health` (reloaded server) and the in-process `fts_state_verdict` probe; `docs/reports/retrieval-quality-post-1wpif.json`. |
| 2026-08-31 | Implemented. `index_state_store.py`: keyed FTS payload digest (`_fts_payload_row_digest`, `_fts_digest_of_rows`, `_fts_table_digest`, `_fts_affected_rows_digest`, `_write_fts_digest_meta`; meta keys `fts_payload_digest_<table>` / `fts_heal_attempt_<table>`), written inside the `rebuild_chunk_index._write` transaction and maintained by XOR inside `_apply_chunk_deltas_locked`; `fts_state_verdict`, `fts_recorded_integrity`, `_record_fts_payload_digest`, `_record_fts_heal_marker`, `fts_search(strict=)`; `reconcile_chunk_index` gates the in-sync return on the verdict, heals through the ordinary rebuild, writes the heal marker, and reports `fts_verified` / `fts_repaired` / `fts_heal_attempt`. `indexer.py`: `_chunk_index_needs_heal` consults the verdict. `server_impl.py`: `_fts_probed_fetch` chokepoint + `_fts_serving_verdict` / `_fts_serving_coverage` epoch-keyed cache + `_fts_schedule_heal` (reuses `_start_background_index_refresh`, suppressed by the heal marker) + `_bounded_failure_detail` + `_lexical_undercoverage_diagnostic`; `_fts_degraded_serve`, `code_lexical_response`, `WaveIndex._fts5_lexical_search`, `WaveIndex._lexical_candidates` routed through it; `code_search_response` / `code_ask_response` carry `lexical_undercoverage`; `_state_store_health_summary` gains `fts` and `index_health_response` emits `fts_integrity_failed`. Docs: the four Affected Architecture Docs updated (Tantivy bullet corrected, honest-serving section, Path 5 step 13, two 1sed7 budget rows with the 28.6 ms / 8,000-row probe measurement). | Focused modules green: `test_fts_query_honesty.py` 36 (new), `test_server_tools_retrieval.py` 933, `test_fts_lexical_layer.py` 56, `test_index_state_store.py` 47, `test_indexer.py` 315, `test_docs_constants_lint.py` 26. Full suite (`run_tests.py --no-cache`): 7838 tests across 68 files, 3 skipped, one failure outside this change (see the AC-6 status note). `wf_validate_docs` passes. No `__pycache__` residue. The live `.wavefoundry/index/` was not touched: every fixture is a scratch store in a temp dir. |
| 2026-08-31 | Gapfill: shell fallback was used only where MCP could not do the job: `jq` over the saved `code_outline` output for `server_impl.py` (the outline exceeded the MCP response cap), `grep`/`sed` to locate the four documentation regions and the two pinned source counts (`INSERT INTO {fts_name} ` = 2, `recovery_usage="index_build(content='fts')"` = 2), the venv test runner, and one measurement script for the performance-budget row. | Retrieval otherwise ran through `code_outline`, `code_read`, `code_keyword`, `code_definition`. |
| 2026-08-31 | Thought: ordered implementation sequence, seams verified by MCP retrieval (`code_outline`, `code_read`, `code_keyword`, `code_references`) before any edit. (1) `index_state_store.py`: add the keyed FTS payload digest (per-row sha256 over id/path/kind/language/tags/lines/text, XOR-aggregated so it is order-independent and maintainable in O(delta)); write it inside the same store transaction at both derived-state publication seams (`rebuild_chunk_index._write` from the materialized rows; `_apply_chunk_deltas_locked` by XOR-out of the pre-delete payloads and XOR-in of the added rows, so a pre-existing corruption is never re-blessed); add `fts_state_verdict` (liveness via `fts_probe` + row/shadow parity + digest compare) and a `strict` mode on `fts_search`; gate `reconcile_chunk_index`'s in-sync early return on the verdict, heal through the existing rebuild path, and write the once-per-attempt heal marker (lock owner only). (2) `indexer.py`: `_chunk_index_needs_heal` consults the verdict so the zero-change build path heals FTS-only damage. (3) `server_impl.py`: one epoch-keyed in-process verdict/coverage cache (`_fts_serving_verdict`), one chokepoint (`_fts_probed_fetch`) used by `_fts_degraded_serve`, `code_lexical_response`, `_fts5_lexical_search`, `_lexical_candidates`; typed `query_failed` for lexical-only, `lexical_undercoverage` diagnostic for hybrid; bounded path-sanitized `detail`; heal scheduling only via `_start_background_index_refresh`, suppressed when the heal marker matches the current attempt; health gains per-table FTS parity + cached verdict. (4) Fault battery + AC-8 sqlite trace/spy tests in a new module. (5) Four architecture docs. | Seam census: `fts_search` callers in `server_impl.py` are exactly lines 762 (`_fts5_lexical_search`), 1982 (`_lexical_candidates`), 19779 (`_fts_degraded_serve`), 19898 (`code_lexical_response`); `_state_store_health_summary` callers are 9479 (health), 19930 (code_lexical, the per-call tie-in to remove), 32774 (audit). Production `reconcile_chunk_index` caller is `indexer._sync_chunk_derived_state` (3328), reached from the end-of-build pass (5446) and the idle heal branch (4894) behind `_chunk_index_needs_heal` (4757). Writer census pin (`test_fts_text_comes_only_from_chunk_rows`) requires exactly two `INSERT INTO {fts_name} ` sites; the digest code adds only SELECTs. |
| 2026-08-30 | Planned from the FTS audit lane. | Bounded missing-table and corrupt-state probes; source validation of reconcile early return and public lexical diagnostics. |

## Decision Log

| Date | Decision | Reason | Alternatives |
| --- | --- | --- | --- |
| 2026-08-31 | Prepare-council amendments A2/A3/A5/A7: healing authority is pinned (serving errors probe and schedule only; healing executes solely under the build lock through the ordinary reconcile/build path; the per-epoch marker is lock-owner-written; the public query path performs no store writes); the integrity digest contract is defined (FTS-served payload fields, order-independent aggregation, publication-boundary computation, corruption-detection threat model under the single-writer model); AC-3 enumerates `code_lexical`, the hybrid FTS half, and degraded fallbacks behind one shared chokepoint with bounded, path-sanitized `detail` strings. | The security seat showed AC-8's "serving error performs one bounded probe/heal sequence" as drafted let a read path acquire write authority (repeated-write amplification on a damaged table) and that an undefined digest cannot falsify the equal-count substitution class; `code_lexical` today swallows FTS damage to a healthy zero outside any probed seam. | **Leave healing wording loose:** an implementer can heal inline in a public query and satisfy the literal AC. **Digest over registry rows only:** misses contentful-FTS payload swaps with preserved counts. |
| 2026-08-30 | Make the existing FTS probe part of normal reconciliation and serving. | Reuses the current integrity primitive and repairs the authoritative lifecycle boundary. | **Trust registry/Lance counts:** reproduces the defect. **Always rebuild FTS:** correct but imposes unnecessary cost on every healthy open/build. |

## Risks

| Risk | Mitigation |
| --- | --- |
| Per-query probes add latency. | Cache build-bound health and reserve direct probes for reconcile/open or suspected failure. |
| Healing races a build publication. | Preserve the existing epoch fence and test interrupted-build states. |

## Session Handoff

See `docs/agents/session-handoff.md` for current session state.
