# Session Handoff

Owner: Engineering
Status: active
Last verified: 2026-07-06

## Current State (2026-07-06 — 1p9q8 REVIEW COMPLETE, awaiting operator close decision)

**Wave `1p9q8 graph-index-accuracy` is OPEN and close-ready except operator signoff.** Four changes implemented + integrated (GRAPH_BUILDER_VERSION 40, CLUSTER 11 no-bump). Full re-readiness (after the wave's stale 2026-07-03 readiness) + implementation (1p9q6→1p9q5→1p9q4→1p9q7) + integration + delivery review (5 lanes + delivery-council primer) + a fix round + a primer-finding fix. wave_review green (wave-council-delivery + all lane signoffs recorded); wave_close dry-run CLEAN; wave_validate clean; suite 4,663; live v39→v40 heal verified; both edit gates closed. **AWAITING OPERATOR CLOSE + a 1p9q5 disposition decision (see readout).**

**Honest outcome:** 1p9q6 real value (oversized-file line-scan tier); 1p9q4 modest +10 self-host binds (200 CONSTRUCTION_RESOLVED) + two faithfulness FIXES the review caught (union/generic over-bind; confidence-label honesty on unresolved external edges); 1p9q7 DI Python/TS coverage + a WRONG-BIND bug (TS binds ambiguity) caught+fixed; **1p9q5 Rust module model DURABLE but tier ZERO-delta today** (module≈file; candidate-index collapse) — operator disposition. Adversarial-faithfulness PASS (no wrong binds). Recorded follow-ups: 1rs45 (loop recovery), 1rtar (SQL strict-validity spike), _source_location splitlines hotspot (perf), 1rqh2 (tomllib), 1p9q5 candidate-collapse-relaxation (would activate the Rust tier). **Close is operator-owned — do NOT wave_close(create) without explicit words.** Live MCP sessions need wave_mcp_reload.

## Prior State (2026-07-05 — 1rs44 CLOSED+committed; 1p9q8 was in review)

**Implementing `1p9q8 graph-index-accuracy`** (operator: "commit then implement 1p9q8"; handoff committed `52cfb28c`). 1p9q8 is a READIED wave (readiness 2026-07-03) but STALE — predates 1p9qh/1p9qi/1rs44/1p9q3 rewrites of graph_indexer.py (now v39). Four changes: `1p9q4` python-receiver-annotation-resolution, `1p9q5` same-package-disambiguation-rust-csharp, `1p9q6` oversized-file-degraded-extraction, `1p9q7` di-signal-ast-and-language-expansion. Freshness pass DONE + re-readiness primer DONE. Findings: `1p9q5` C# half ALREADY DONE (Wave 1p4ev, not 1p9qh) → C# reduced to verify-and-close; Rust half NOT a bounded elif (extractor models NO Rust module scope) → **OPERATOR CHOSE to FUND Rust module-tree modeling in this wave** (mod-decl file mapping, inline-mod nesting, #[path], crate-root, per-file fallback — a new faithfulness surface). 1p9q5 doc rewritten accordingly (AC-1 module model, AC-2 tier, AC-3 [~] C# verify). `1p9q4`/`1p9q6`/`1p9q7` READY with anchor refreshes + convention-alignment (1p9q6 mirrors 1p9qe recovery convention; 1p9q7 origin-check reconciled to 1p9qi, unresolved DI → plain `external::` not `external::di::`, collect-side needs merge plumbing). Coordinated bump 39→40. Re-readiness DONE: fresh prepare-council PASS WITH NOTES (clean roster red-team/reality-checker/qa; 2 qa AC-text gaps applied — 1p9q6 AC-1b per-file count-property pin, 1p9q7 AC-2 TS alias/impostor origin-check). **Wave `1p9q8` OPENED** via wave_prepare(create) — active. Old 2026-07-03 architecture-reviewer roster warning cleaned (evidence line added). **CURRENT STEP: implementing SEQUENTIALLY** (all 4 touch graph_indexer.py → serial lanes avoid edit collisions; each leaves the framework gate OPEN for the next; single 39→40 bump deferred to integration). Order: 1p9q6 **DONE** (line-scan tier mirrors 1p9qe; +7 tests) → 1p9q5 **DONE** (Rust module model built: crate-root/mod-decl/2018/mod.rs/#[path]/inline-mod/per-file-fallback; C# verify=already-correct; +10 tests, suite 4,628; gate open) → 1p9q4 **DONE** (extended existing Python receiver res to attrs/string-forward-refs/module-globals/construction; +10 self-host binds 100% precision, zero regressions, + a faithfulness fix on union/generic over-bind; +18 tests, suite 4,646) → 1p9q7 **DONE** (Python FastAPI + TS NestJS/Inversify DI collectors, AST-anchored; origin-check reconciled to 1p9qi negative/positive + alias/impostor both langs; unresolved DI→plain external:: recorded; collect-side merge proven; JVM/.NET regression pinned; +10 tests, suite 4,656). **ALL FOUR IMPLEMENTED + INTEGRATED.** v40 bump (all-four changelog); CLUSTER_BUILDER_VERSION no-bump (fingerprint+graph-version force recluster anyway, verified — 47 communities restamped); AC flips done honestly; spec/arch docs updated (graph-index-system.md Rust tier + DI Python/TS; 1p9q6 pipeline doc); live v39→v40 rebuild clean (11,794 nodes/33,160 edges, +5 net); version-pin test →"40"; suite 4,656; gate CLOSED. Self-host signatures: 1p9q4 = 200 CONSTRUCTION_RESOLVED + 9,715 RECEIVER_RESOLVED (productive!); 1p9q5/q6/q7 = 0 (no Rust/oversized/DI on this repo, as anticipated). **DELIVERY REVIEW IN PROGRESS — NOT close-ready, needs a fix round.** Lanes in: architecture APPROVE WITH NOTES, qa PASS WITH NOTES, **code REQUEST CHANGES (2 SEVERE bugs)**. Adversarial-faithfulness = PASS (no wrong-binds anywhere; all 4 surfaces refuse over-bind under live probes). Awaiting performance (last lane). **SEVERITY RESOLVED by direct verification:** Finding 1 (confidence label on external-target edges) is MODERATE not severe — impact weights INCOMING edges to project nodes + path treats external:: as non-transitive terminal leaves (graph_query.py:1414), so downstream is largely INERT; but the label is semantically wrong (RECEIVER_RESOLVED on an unresolved edge) → fix anyway (cheap+honest+closes test gap). Finding 2 (TS DI binds ambiguity) is the WORSE one — a potential WRONG bind (arbitrary same-name pick via _resolve_di_target/_pick_node); adversarial tested injects-path (refuses OK) not binds-path, so fix lane REPRODUCES first then fixes with unique-candidate refusal. Performance = APPROVE WITH FINDINGS (1p9q7 DI walk un-gated — full AST walk on every Python/TS file even DI-free; fix = substring idiom pre-check). ALL 5 LANES IN. Fix round DONE (suite 4,663; gate closed; FIX1 = finalize-stage `_downgrade_unresolved_typed_calls` for dotted external calls [subtler than emit-gate — scoped past Java-inheritance/Go-bare-type]; FIX2 TS binds arbitrary-pick REPRO-CONFIRMED+fixed [code lane right]; FIX3 DI idiom pre-check with no-false-negative superset arg; FIX4 arch-doc Python row; FIX5 cosmetics; _source_location hotspot follow-up recorded). **CURRENT STEP: delivery-council red-team primer over fixed state** (verify FIX1 finalize-scoping doesn't over/under-reach, FIX2 dedup, FIX3 no-false-negative). THEN synthesize wave-council-delivery → wave_review → close readout (surface 1p9q5 zero-delta). PRIOR: consolidated fix round — — FIX1 confidence label (external→EXTRACTED) + FIX1b test blind spot, FIX2 TS binds refusal (repro-first), FIX3 DI idiom pre-check, FIX4 1p9q4 arch-doc gap, FIX5 cosmetics (version snippet, [~]/[ ] consistency), doc-record the pre-existing _source_location splitlines() hotspot follow-up. THEN delivery council (red-team primer over fixed state + synthesis) → wave_review → close readout (surface 1p9q5 zero-delta). No re-bump (stays v40). **FIX-NOW SET so far:** (1) SEVERE — 1p9q4 stamps RECEIVER_RESOLVED/CONSTRUCTION_RESOLVED on calls edges that dangle to `external::` targets (self.client:ThirdParty→external::…fetch, foo:Foo missing-method→external::Foo.missing) — corrupts _path_edge_cost + _edge_confidence_weight (the wave's own target consumers); fix = emit loop only trusts explicit conf when target not external:: else EXTRACTED; (2) SEVERE — 1p9q7 TS DI `binds` (_resolve_di_target/_pick_node) doesn't refuse on same-name cross-file ambiguity (picks arbitrary), contradicts AC-2; fix = unique-candidate len==1-or-external:: + dedup index; (3) test blind spot — Python1p9q4ReceiverTests `_call_confs` filters external:: so confidence regressions invisible → add EXTRACTED-on-stays-external assertions + a TS binds-ambiguity test; (4) CONVERGENT (arch+qa) — 1p9q4 architecture-doc gap: graph-index-system.md Python row still says "no receiver resolution/EXTRACTED"; (5) cosmetics — stale version snippet graph-index-system.md:132 "38"→"40"; [~]/[ ] consistency on the review-pending AC clause. THEN council over fixed state → wave_review → close readout (surface 1p9q5 zero-delta). Live MCP sessions need wave_mcp_reload. **Honest wave outcome to surface at close:** 1p9q6 solid (line-scan tier); 1p9q4 modest +10 self-host binds + a faithfulness fix; 1p9q5 module model durable but tier ZERO-delta (operator disposition); 1p9q7 DI Python/TS coverage. Mature-graph theme: accuracy follow-ups yield modest deltas, as plans anticipated. **⚠ 1p9q5 KEY FINDING for operator close-disposition:** the Rust disambiguation tier is faithful + never wrong-binds but produces ZERO productive binds today (structural: Rust module ≈ 1 file so cross-file candidates never share the caller's module; same-file defs resolve at extraction; candidate-index collapse hides intra-file inline-mod ambiguity). This is the "near-no-op" the primer predicted + operator accepted when funding. The MODULE MODEL (AC-1) is the durable deliverable; tier is zero-delta. Per the doc's "re-scope on negligible delta" stance — surface at close for operator disposition (accept model-only value, or revisit). THEN: integration (single 39→40 bump + full suite + upgrade-heal) → delivery review (MANDATORY adversarial-faithfulness — Rust module model + all binding changes) + council → close readout. NOTE: LARGE wave; 1p9q5 Rust modeling is substantial new work.

## Prior State (2026-07-05 — 1rs44 CLOSED+committed `a75cd93d`; no wave open)

**Wave `1rs44 sql-graph-accuracy-followups` CLOSED + committed `a75cd93d`** (operator: "close now"). Delivered `1rrx5` (7 bounded SQL-accuracy fixes: EXECUTE-FUNCTION/DECLARE/bracket-quote phantom removals, MERGE-subquery read, TRUNCATE/ALTER/DROP sniff, unconditional body-def drop, clustering diagnostic, + R8 DELETE/UPDATE/INSERT prose-gate). Full delivery review + council + two fix rounds; GRAPH_BUILDER_VERSION 38→39; suite 4,611; live v39 heal verified. Commit included the two follow-up plans; operator's untracked files (1ro44, 1rsh9, 1rolq, 1rppn, 1p8gy-deletion, reindex-05) left untouched.

**RECORDED RESIDUALS / OPEN FOLLOW-UPS (all planned, unadmitted):**
- `1rs45-enh sql-plpgsql-loop-body-dml-recovery` — split from 1rrx5 at readiness (in-loop DML recovery needs new recovery-tier machinery). Has an OPEN mechanism decision (regex-over-masked-text vs reparse-after-scaffolding-strip).
- `1rtar-task spike-embedded-sql-strict-validity-gate` — the future spike (authored at operator direction): evaluate a real SQL parser (sqlglot) strict-validity gate to close the embedded-prose false-positive class (trailing-DELETE-prose + SELECT-prose residuals). Zero live-corpus occurrences — synthetic-only, so spike-then-decide, don't pre-adopt the dependency.
- Pre-existing accepted limitations recorded in 1rrx5: TRUNCATE `RESTART IDENTITY` over-rejection (safe under-capture); trailing-DELETE-prose + SELECT-prose confidently-wrong-edge residuals (pre-existing from 1p9qi, R8 monotonic — not worsened; the `1rtar` spike is their robust-fix path).
- Also still queued: `1p9q8 graph-index-accuracy` (needs a freshness pass — plans predate 1p9q3/1p9qh/1p9qi/1rs44), `1rqh2` (tomllib).

**Waves closed+committed this session:** 1p9qm, 1p9q3, 1roqn, 1p9qh, 1p9pe (`59b25d22`), 1p9qi (`d848ef01`), 1rs44 (`a75cd93d`). Operator's untracked files still NOT to commit: 1ro44 wave dir+journal, 1rsh9 wave dir+journal, 1rolq plan, 1rppn plan, 1p8gy deletion, reindex-2026-07-05.

## Previous State (2026-07-05 — 1p9qi CLOSED+committed; 1rs44/1rrx5 was implementing)

**Wave `1rs44 sql-graph-accuracy-followups` is OPEN (active/implementing)** — `1rrx5` admitted, readied (prepare-council PASS WITH NOTES: red-team primer + reality-checker + qa), and opened via `wave_prepare(mode=create)`. **Readiness split R6 out:** the in-loop DML recovery residual is now its own plan `docs/plans/1rs45-enh sql-plpgsql-loop-body-dml-recovery.md` (planned, unadmitted) — the primer's live-tree evidence showed the `1p9qe` recovery tier has NO DML patterns / NO CTE exclusion and the FOR-LOOP ERROR shreds the insert target, so it needs new machinery (materially larger, own change). `1rrx5` is now the bounded set: R1 EXECUTE FUNCTION action-name phantom, R2 MERGE WHEN-subquery read, R3 DECLARE type phantom (non-builtin only), R4 bracket-quote external-id normalize at emit site via `_SQL_IDENT_QUOTE_CHARS`, R5 sniff-set TRUNCATE/ALTER/DROP, R6 two edge cases (unconditional body-def drop + body-local cross-bind decision), R7 clustering-asymmetry diagnostic. Readiness notes applied: AC-6 `[~]`-requirement + AC-7 fixture-assertion vacuity guards, wave.md synced post-split, GRAPH_BUILDER_VERSION bumps 38→39. **R8 DONE + re-reviewed — wave close-ready again (with an honest residual for operator decision).** R8 (operator-directed: generalize prose defense to DELETE/UPDATE/INSERT) implemented via interior-vs-trailing ERROR discrimination — closes the operator's cited `"delete the row from cache"` case + the interior-prose subclass; empirically validated against the live grammar (no valid-SQL over-rejection: RETURNING/ON CONFLICT/USING/CTE kept; Fineract `dirty_prose_refused=0`); suite 4,611. Mandatory adversarial re-review = PASS WITH FINDINGS: interior half clean/no-regression, BUT the SYMMETRIC **trailing-prose DELETE** (`delete from sessions periodically` → `writes sessions`) still mints a confidently-wrong write — PRE-EXISTING (R8 monotonic, worsens nothing), fundamentally hard (parse-shape can't separate trailing English from valid trailing clauses like RETURNING; a keyword allowlist has holes). AC-9 CORRECTED to interior-half-only; trailing-DELETE-prose + SELECT-prose recorded as residuals; synthesis + wave-council-delivery updated. `wave_review` green, `wave_close` dry-run CLEAN, wave_validate clean, version 39, gates closed. **AWAITING OPERATOR CLOSE DECISION** — and a call on the trailing-prose residual: accept + close with it recorded, OR pursue a robust embedded-prose-vs-SQL discriminator as a dedicated follow-up (`1rs45` or new change). Was close-ready on R1–R7 before R8.

**PRIOR STATE (R1–R7 REVIEW COMPLETE — close-ready except operator signoff).** Implemented R1–R7 (suite 4,607); delivery review = 5 lanes + delivery council, unanimous SHIP. Adversarial-faithfulness lane found 2 moderate (R5 TRUNCATE prose → confidently-wrong `writes events`; R3 `%ROWTYPE`/`%TYPE` recall regression) — BOTH fixed in the review fix round + adversarially re-verified by the delivery primer (suite 4,609). `wave_review` green (wave-council-delivery + lanes recorded); `wave_close` dry-run CLEAN; version 39; live v39 upgrade-heal verified; gates closed. **Close is operator-owned — awaiting explicit approval.** TWO recorded non-blocking items: TRUNCATE `RESTART IDENTITY` over-rejection (accepted limitation, safe under-capture); **PRE-EXISTING `1p9qi` DELETE/UPDATE-prose confidently-wrong vector** (`"delete the row from cache"` → `writes cache`) — NOT introduced here, flagged to operator as a follow-up (correct fix = general option-aware clean-parse gate across all sniff keywords; candidate for `1rs45` or a new change). Next queue after close: `1rs45` (loop recovery), `1p9q8` (needs freshness). Operator's untracked files still NOT to commit: 1ro44, 1rsh9, 1rolq, 1rppn, 1p8gy-deletion.

## Previous State (2026-07-05 — 1p9qi CLOSED+committed; readiness done)

**Wave `1p9qi sql-graph-accuracy` CLOSED + committed `d848ef01`** (operator-approved "close 1p9qi then implement 1rrx5-enh"). All five changes + integration shipped (GRAPH_BUILDER_VERSION 38); delivery council unanimous SHIP; the primer-caught routine-body write-direction inversion fixed + adversarially re-verified; the FOR-LOOP under-capture made loud via `sql_partial_bodies`. Suite 4,600/43. Commit included the five 1p9qi changes + the `1rrx5` plan; operator's untracked files (1ro44, 1rsh9, 1rolq, 1rppn, 1p8gy-deletion) left untouched.

**NOW IMPLEMENTING the follow-ups:** plan `1rrx5-enh sql-graph-accuracy-followups` admitted into NEW wave **`1rs44 sql-graph-accuracy-followups`** (created + admitted; AC priorities populated: R1–R5 important, R6 important+split-candidate, R7/R8 nice-to-have, R9 gates required). Prepare dry-run: lint+garden clean, only wave-council-readiness signoff missing. **CURRENT STEP: readiness council running** — red-team primer (isolation, code-grounded feasibility: is each R1–R5 truly bounded? is R6 a genuine split candidate? does R4's normalize-at-emit-site avoid the contract-test breakage that reverted the in-review attempt?), then fixed seats + rotating → wave-council-readiness → `wave_prepare(mode='create')` (prepare-and-open; single-OPEN slot free now) → implement lanes → review (MANDATORY adversarial-faithfulness lane, binding+recovery) → close readout (operator-owned).

**1rrx5 requirements:** R1 EXECUTE FUNCTION name phantom; R2 MERGE WHEN-subquery read drop; R3 DECLARE type phantom; R4 bracket-quote external-id normalize at `_sql_apply_file_extraction` emit site (NOT frozen unit); R5 sniff-set TRUNCATE/ALTER/DROP; R6 in-loop DML recovery (extend 1p9qe tier into nested-in-body ERROR regions; SPLIT candidate); R7 edge cases (body-local cross-bind, name-parse-gated def-drop totality); R8 clustering writes/maps_to instrumentation. OUT of scope (recorded): JPQL, column lineage, Python/Go/TS/JS SQL sinks, Liquibase defs; separately tracked: 1rqh2, 1p9q8. NOTE: main-loop model = Opus 4.8 after the Fable 5 limit. Operator's untracked files still NOT to commit: `docs/waves/1ro44 .../` + `docs/waves/1rsh9 .../` + their journals, `docs/plans/1rolq-...md`, `docs/plans/1rppn-...md`, `1p8gy-...md` deletion.

## Previous State (2026-07-04 — 1p9qi OPEN: freshness pass running)

**Wave `1p9qi sql-graph-accuracy` is OPEN (implementing)** — activated after 1p9pe closed (`59b25d22`). Five changes, hard sequencing from wave.md watchpoints: `1p9qc` (noise suppression) FIRST, then `1p9qd` (structured statement extraction = frozen contract), then `1p9qe`/`1p9qf`/`1p9qg` in parallel. Single coordinated `GRAPH_BUILDER_VERSION` bump (37→38) at integration. Blocking pre-close gate: real-corpus locality census for `1p9qf`/`1p9qg` default-on decision; adversarial faithfulness lane required at review (binding changes). Freshness pass DONE (all five READY-with-corrections). **IMPLEMENTED: `1p9qc`** (noise suppression, flip stash-verified), **`1p9qd`** (frozen contract `sql_statement_references`; NEW `writes` relation + reads reuse with conscious 1p4ls inheritance; impact data-layer exception; fixture calibration 47→16 edges), **`1p9qe`** (ERROR-region recovery; degradation convention DEFINED — masking incl. strings, `extraction: "sql_recovery"` provenance, counted-unrecovered bounds; 1p9q8 mirrors), **`1p9qf`** (Java/C#/MyBatis-XML sinks + sniff gate + finalize bind @ LITERAL_DERIVED; census: Fineract 11/11 precision + census-driven backtick normalization, Tomcat honest 0/89-dynamic; DEFAULT-ON recorded; `external::sql::` invariant + passthrough tests). Suite 4,573/43 OK. CURRENT STEP: `1p9qg` lane running (entity→table via declared names only, extends the 1p9qf annotation-argument seam, Fineract census; closes the framework gate when done — left open across the parallel lanes deliberately).

**REVIEW STATE (2026-07-05):** Integration DONE (GRAPH_BUILDER_VERSION 38 + all-five changelog; cluster no-bump confirmed against projection surface; NOW() noise measured 4.4% then fixed ~7 LOC preserving argument-subquery reads; spec weaves done; v37→v38 heal verified live; suite 4,596/43). Five delivery lanes COMPLETE, none blocking: adversarial-faithfulness PASS WITH FINDINGS ($tag$ named-dollar-quote masking bypass mints phantom recovery table — moderate, ~4 LOC fix; cross-file sink impostor same-file-scope limitation — document; oracle re-verification reproduced both censuses exactly, all invariants hold), code APPROVE WITH FINDINGS (1p9qc filter path now production-dead via 1p9qd's dispatch — delete; 6 minor), qa PASS WITH NOTES (census artifacts match to the digit; cosmetics), architecture APPROVE WITH NOTES (code_ask bucket gate omits writers/mapped_entities — silent drop, derive from _GRAPH_REL_BUCKET.values(); 2 one-clause doc drifts), performance APPROVE WITH FINDINGS (SQL -33% wall/-93% edges; non-SQL within noise; Java corpora +1.3–2.6% measured tax — correct the 1p9qf "within noise" claim). CURRENT STEP: consolidated review fix lane — first lane died on a usage limit mid-Fix-5; a continuation lane is finishing. **DONE on disk (verified):** Fix 1 `$tag$` named-dollar-quote masking (`_SQL_RECOVERY_DOLLAR_TAG_RE`) + tests; Fix 2 `code_ask` bucket gate derived from `_GRAPH_REL_BUCKET.values()` (`_GRAPH_RELATED_SIGNAL_KEYS`, server_impl.py:288, wired :16753) + test; Fix 3 dead 1p9qc `_sql_relation_candidate_filter` fully removed; Fix 4 `source_unresolved`/`grammar_unavailable` counters; Fix 5 provenance code; all five change-doc progress logs + wave.md fix rows. **REMAINING (continuation lane):** Fix 5 dangling-block provenance test assertion; Fix 6 dup-annotation-extraction cache + 2 assess-or-record (bracket-quote `external::dbo].[users`, dollar-body `external::void`); doc records (frozen-contract guarantees clause, 1p9qd contract-section refresh, same-file impostor limitation, 1p9qf perf-honesty numbers, QA cosmetics, finalize-pass inventory, CTAS cosmetic); flip compound ACs 1p9qf AC-7 / 1p9qg AC-6; full suite + wave_validate + close the still-open framework gate. **Adversarial-faithfulness verdict: PASS WITH FINDINGS** (both findings fixed/recorded). Fix-round completion lane DONE (suite 4,598/43; gate closed; dollar-body `void` fixed, bracket-quote reverted+recorded, all doc records, compound ACs flipped). **DELIVERY COUNCIL STARTED:** red-team primer (isolation) found a SEVERE confirmed defect the recorded residual understated — `handle_create_routine`→`walk_reads` flattens natively-parsed PL/pgSQL `$$`/`$tag$` routine BODIES to `reads` on the TRUSTED path: in-body INSERT/UPDATE/DELETE mis-filed as reads (write-direction inverted — defeats 1p9qd headline), nested CREATE = phantom reads, bare CREATE TEMP bypasses AC-6. Both census corpora lack native PL/pgSQL bodies so "100% precision" was silent on the class. Routine-body fix DONE (suite 4,599; every sub-case flipped — in-body DML→writes, nested CREATE no phantom, AC-6 in-body honored, subquery reads preserved; gate closed). **DELIVERY COUNCIL COMPLETE — unanimous SHIP:** architecture APPROVE WITH NOTES, qa PASS, reality-checker PASS, rotating (senior-engineering-challenger) + security PASS WITH NOTES. Both adversarial seats INDEPENDENTLY converged on a NEW residual: cursor `FOR-LOOP`/control-flow routine bodies parse the shell natively (`error_regions:0`) but tree-sitter-sql mangles the loop body → in-loop DML writes SILENTLY DROPPED (interacts with this wave's own invocation-name exclusion). This is UNDER-CAPTURE (safe recall gap), NOT inversion → SHIP with follow-up. wave.md delivery-council synthesis + all 7 seat/lane signoffs RECORDED. **CURRENT STEP: council fix-now lane running** — one code-worthy convergent item (make nested-in-body ERROR loud via new `sql_partial_bodies` counter, since 1p9qd doc falsely claimed drops are "not silent") + doc records (correct the false claim; record the loop-body under-capture, EXECUTE-FUNCTION-name phantom, MERGE-WHEN-subquery drop, DECLARE-type phantom, non-temp body-local cross-bind, arch F-A clustering rationale, arch F-B name-parse-gated def-drop). THEN: wave_review (implementation) → wave_close dry-run → close-readiness readout (operator-owned; do NOT close without explicit operator words). Suite 4,599 pre-loudness-fix. NOTE: main-loop model = Opus 4.8 after the Fable 5 limit.

**ORIGINAL INTEGRATION CHECKLIST (all done):** (1) single `GRAPH_BUILDER_VERSION` 37→38 bump — changelog must name all five changes; flip the deferred AC checkboxes (1p9qc AC-4, 1p9qd AC-8, 1p9qe AC-6, 1p9qf AC-7, 1p9qg equivalent); (2) confirm CLUSTER_BUILDER_VERSION assessment (1p9qd recorded: no bump needed); (3) deferred spec-doc capability notes from 1p9qf (+1p9qg?) — `docs/specs/mcp-tool-surface.md` + graph architecture doc, one integration owner; (4) coordinated decision on the `reads external::NOW` invocation-noise finding (1p9qe found, pre-existing from 1p9qd — fix trades TVF refs and touches the frozen contract); (5) real-index rebuild + v37→v38 upgrade-heal verification (1p9qh precedent); (6) full suite + wave_validate. THEN delivery review: five lanes incl. MANDATORY adversarial faithfulness lane (binding changes; 1p9qe recovery is a detection surface) + council; census gate already satisfied for 1p9qf, 1p9qg census pending its lane. Follow-ups recorded: sniff set omits TRUNCATE/ALTER/DROP (one live Fineract site); Fineract dump-DDL recovery interaction (flips binds to ambiguous-drop, never wrong-bind).

## Previous State (2026-07-04 — 1p9pe CLOSED + committed `59b25d22`)

**Wave `1p9pe post-release-followup-hardening` CLOSED 2026-07-04 (operator-approved: "close and commit").** All five changes delivered (`1p9p6` parse diagnostics, `1p9p7` renderer overwrite safety, `1p9p8` incremental post-write lint, `1p9p9` HF Hub socket timeouts, `1p9pk` prepare-council verification rigor). Delivery review: five lanes + full delivery council (red-team primer, four fixed seats, rotating senior-engineering-challenger) — seats unanimous, synthesis PASS. Two in-review fix rounds with reproducer-flip evidence; the council round's semantic tomllib-equivalence exit guard caught a NEW balanced-bracket-desync silent-loss shape no prior guard saw. Headline: post-write advisory lint 4.7 s → 0.25–0.28 s per write (~17–30x, performance-lane measured). Final: suite 4,528 / 43 files OK; wave_validate clean; both edit gates closed. The 1p9p7 standing watchpoint (re-render deletes the operator `wave_close` block) is RETIRED — split-ownership marker region live and idempotence-verified on disk.

**Next in queue:** `1p9qi sql-graph-accuracy` (readied) and `1p9q8 graph-index-accuracy` (REQUIRES a freshness pass first — its plans predate the 1p9q3/1p9qh rewrites; expect its architecture-reviewer roster warning to surface on activation). Follow-up plan authored during 1p9pe review (committed, not admitted): `docs/plans/1rqh2-debt remove-tomllib-import-fallback.md`.

**Operator's untracked files NOT to sweep into commits:** `docs/waves/1ro44 agent-memory-and-retrieval-decay/`, `docs/agents/journals/1ro44-...md`, `docs/plans/1rolq-enh verify-docs-agentic-review.md`, the working-tree deletion of `docs/plans/1p8gy-enh graph-backed-agent-memory.md`, and `docs/plans/1rppn-enh wave-change-manifests-close-advisory.md` (unfilled template skeleton minted 2026-07-04 21:37, provenance unconfirmed — ask operator).

## Previous State (2026-07-04 — 1p9qh CLOSED + committed)

**Wave `1p9qh java-csharp-enterprise-accuracy` CLOSED 2026-07-04 (operator-approved: "close and commit")** — delivery review PASS unanimous; upgrade path (v36→v37 heal) and the new Java surface live-verified post-close (dispatch impact, implementor navigation, provenance honesty on real Tomcat data). No wave currently open.

Delivered: structured Java imports (wildcard/static — truncation + `external::static` pollution + member resolution fixed); `extends`/`implements` edges Java+C# with single-definer inherited-method resolution (`via_supertype` provenance) and dispatch-aware `code_impact`; `this.field` receivers, annotation-kind fix, package-declaration keying. `GRAPH_BUILDER_VERSION` 37 (single wave bump). The review's adversarial lane found TWO blocking wrong-bind classes (JLS 6.4.1 static-import inversion incl. the JUnit-idiom steal; the wildcard guard's directory-vs-declared keying) — both fixed in-session with probe-verified flips + 12 regression tests; the red-team primer forced a Tomcat-scale census (36/36 hand-verified binds → precision >92%; post-fix recount identical). Suite ~4,438 OK. Suggested commit subject: `Land wave 1p9qh: Java/C# enterprise accuracy (imports, inheritance edges, receivers)`.

Wave `1roqn` closed + committed earlier (`d430494c`); `1p9q3` closed + committed (`38c52ccd`); `1p9qm` closed + committed (`6fb035da`). **Wave `1p9pe` is READIED** (freshness-verified 2026-07-04 with corrective-pass ACs applied) — next in queue with `1p9qi` and `1p9q8`.

The drift fix is live-proven: the previously permanent loop (`repairing 1 drifted file(s)` + ~1 s merge + ~1.35 MB per hook fire) now takes `merge[zero-change]` with 0 bytes; suite 4,358 OK (12 new tests); eligibility gated per-build on `files_for_content` with the graph-mode write-capability skip; reaper firewall intact. Key close-time watchpoint: `docs/workflow-config.json` is a LIVE per-kind-residual instance shielded only by 20 all-mode-setup rows in code.lance — any code-layer table recreation re-triggers the loop via the residual (named trigger + follow-up instrument recorded in wave.md watchpoints).

Suggested commit subject: `Land wave 1roqn: gate lance drift detection on chunk eligibility`.

Earlier this session: **`1p9q3 graph-index-efficiency` CLOSED + committed (`38c52ccd`)**; `1roqn` planning committed (`5f214be5`).

### Delivered (headline numbers, all independently reproduced by the performance lane)

- `1p9py`: compact/gzip/atomic graph artifacts — 17.5× at its own boundary, **8.6× wave end-state** (payload 0.56 + SQLite store 3.5 + clusters 0.11 MB vs 36.0 MB); `GRAPH_BUILDER_VERSION` 36.
- `1p9pz`: stat-validated in-process query cache — warm graph-tool calls 42.8 ms → 0.04 ms (~1,000×); 17 sites migrated; docs rider fixed stale layer/union/networkx claims.
- `1p9q1`: build-time tiered betweenness — 10k query cap retired; exact tier 14-62 ms at 11k nodes; `CLUSTER_BUILDER_VERSION` 11.
- `1p9q2`: incremental merge + symbol-scoped invalidation + SQLite state store — zero-change builds 0 bytes/0.3 s; rows + re-resolution O(delta); bytes O(graph) via the merge_state sidecar (honestly re-scoped + instrumented).

### Review outcome (2026-07-03/04)

Five delivery lanes (code, qa, architecture, performance, **adversarial faithfulness**) + red-team primer + four fixed council seats + rotating docs-contract seat. **Synthesis PASS, seats unanimous.** The adversarial lane found a REAL equivalence bug (merged-epoch symbol-delta collapse → `reads` edges in untouched files escaping re-resolution; missing + dangling edges) — **fixed in-session** (per-side delta + `DepthSwapDeltaKeyTests`; reproducer green; AC-9 discharged faithful-with-residuals). Also fixed in-session: blob-aware sidecar counters + `sidecar:` log segment; two un-invalidated cache-refresh paths; never-pin-stale-builder cache guard (+ once-per-process stderr note); WAL pragma result check; reader-copy parity gate; dead-code removal; honest claims re-scope; stale seed-211/guru.md cap wording; architecture-doc updates.

Gates: full suite **4,346 tests / 42 files OK** post-fix; docs-lint clean; `wave_review` green (`wave-council-delivery` recorded); `wave_close` dry-run clean; both edit gates closed; no `__pycache__`.

### Approved sequence in flight (operator: "close 1p9q3 and continue", 2026-07-04)

Close DONE → commit → open `1roqn` → implement `1rmaf-bug` → review → operator close decision for `1roqn`.

### Follow-up candidates recorded in wave watchpoints (not admitted)

- ~~URGENT: Lance drift-repair loop~~ — now planned + council-READIED as wave `1roqn lance-drift-eligibility` (`1rmaf-bug`); implementation next in this session.
- Full-merge kill-switch env for the incremental machinery (rotating-seat rider; cheap field insurance).
- Row-sharded fragment store — gated on real large-corpus measurement.
- Doc-impact add-direction miss (proven pre-existing); seed-gardening pass for pre-existing internal wave refs in seeds 211/214.
- **At ship time**: release notes must carry the "reconnect / `wave_mcp_reload` after upgrading" callout (stale sessions can't read gzip artifacts).

## Other Session Work

Wave `1p9qm subagent-mcp-retrieval-posture` closed + committed earlier (`6fb035da`); planning waves committed (`5f5e5db8`). Readied waves awaiting their turn after `1p9q3` closes: `1p9q8 graph-index-accuracy`, `1p9qh java-csharp-enterprise-accuracy`, `1p9qi sql-graph-accuracy` (suggested order: 1p9qh → 1p9qi; 1p9q8 anywhere). Pre-existing planned: `1p9pe post-release-followup-hardening`, `1p6lp cross-host-skills`. From 1p9qm: registry-derived wrapper-allowlist pin + docs-lint factor-wrapper `tools:` check remain follow-up candidates.

**⚠ Standing until `1p9p7 renderer-overwrite-safety` (wave `1p9pe`) lands:** every `render_agent_surfaces` run rewrites `.codex/config.toml` and deletes the operator's `wave_close approval_mode` block — restore after ANY re-render.

## Coordination Watchpoints

- `docs/specs/mcp-tool-surface.md`: waves `1p9qh`/`1p9qi` also want vocabulary edits — one integration owner.
- Solaris reporter re-runs their transcript count after upgrading (1p9qm field verification).
- MCP server old-code window: the running server executes pre-edit graph modules until reconnect/`wave_mcp_reload` — measurements via fresh venv subprocesses, not `wave_index_build`.
- Operator decision recorded 2026-07-04: keep SQLite for the state store; do NOT move the graph payload into SQLite (assessed and declined — it would break the stat-validated cache design for no measured benefit; revisit only with large-repo parse-dominance evidence).

## Current Session

**Active wave:** *(none)*
