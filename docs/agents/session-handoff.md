# Session Handoff

Owner: wave-coordinator
Status: active
Last verified: 2026-06-21

## Hotfix — Claude hook commands anchored on $CLAUDE_PROJECT_DIR (2026-06-18)

**Operator-requested fix ("fix the stop hook error"), no-wave (hotfix waiver).** Root cause: Claude Code runs hook commands via `/bin/sh -c` from a cwd not guaranteed to be the repo root, so the bare-relative `command: ".claude/hooks/session-capture"` failed every Stop with `/bin/sh: .claude/hooks/session-capture: No such file or directory` (reproduced from a non-repo cwd; exec bit fine — pure cwd/relative-path issue). Fix: `render_platform_surfaces.launcher_command` gained a `project_dir_var` param; the Claude caller now emits the quoted, anchored form `"$CLAUDE_PROJECT_DIR/.claude/hooks/<hook>"` (POSIX) / `cmd.exe /c "%CLAUDE_PROJECT_DIR%\.claude\hooks\<hook>.cmd"` (nt). Regenerated `.claude/settings.json` (plugins preserved); test command-assertions + the simulate-map name-extraction helper updated; full suite 3315 green. **Scope:** Claude only (confirmed). Cursor/Copilot/Windsurf still emit relative commands (their project-root var is unverified — `${workspaceFolder}` may not expand in a command field) — likely the same latent issue, tracked as a follow-up. **Takes effect on the next Claude Code session / settings reload** (the current session loaded the old command). UNCOMMITTED.

## Stage-gate waiver (2026-06-17)

**Operator-approved, no-wave change to `.wavefoundry/framework/install/install-block.md`** — added an `## Upgrade` section (the "Upgrade wave framework" path) to the GitHub Release notes header, alongside the existing `## Install` block. Operator explicitly waived the wave requirement ("just do it. don't create a wave for it."). Content-only (no `build_pack.py` logic change); the block flows into release notes above the CHANGELOG body via `_assemble_release_notes`. +2 `test_build_pack` tests (shipped block has both sections; upgrade block reaches assembled notes); suite green, docs-lint ok. Applies from the next release (v1.7.1 already published). UNCOMMITTED.

## Last Closed Wave

`1p47e cross-file-resolution-and-risk-score` — **closed 2026-06-09** (3 changes; UNCOMMITTED). Prior: `1p44n` (2026-06-09, secrets hardening), `1p41l`, `1p45n`.

Shipped: **`1p470`** — Python sibling-loader return-type inference (closed the `from_root` 0→12-in-edges hole) + a language-agnostic ambiguous-import disambiguation in the cross-file rewrite pass (**verified Python + Java**; C#/Go/Rust need the `1p4ef` import-head fix before it fires there). Pivoted from the planned per-language `import_targets` threading (rewrite-pass approach instead; lazy-loader reversed from deferred per operator). `GRAPH_BUILDER_VERSION` 23→24. **`1p41o`** — new `code_risk_score` MCP tool (`risk = affected_file_count × log1p(fan_in)`, `fan_out` surfaced-not-folded); AC-8 gate **PASS** (pooled ρ=0.80 ≤ 0.95; per-module degeneracy 39%, down from `1p41l`'s 81%). **`1p4dc`** — `install-log-format.md` provisioned to targets (closes the latent gap flagged in `1p455`). `run_tests.py` = **2946 green**. **Delivery-council READY-WITH-NOTES** — red-team caught a Spearman **tie-handling bug** that had deflated the recorded per-module ρ (corrected; gate *conclusion* unchanged — pooled ρ reproduces) + 2 doc over-claims (typed-lang coverage, gate-enabler narrative — both corrected). Validated by the **1.6.0+p4ea test pack across 3 teams** (Java/Swift/JS-TS): `code_risk_score` PASS everywhere, cross-file resolution strong on unambiguous + imported cases.

## Open Waves

- **`1p4hi code-ask-agent-rerank`** — **implementing** (5 changes: `1p4hj` landed; `1p4mf`, `1p4lr`, `1p4n4` implemented; `1p4ls`, `1p4hu` pending). Makes `code_ask` agent-fused + structure-aware; the constant-retrieval substrate (`1p4mf`) is the current focus. See `docs/waves/1p4hi code-ask-agent-rerank/`.

## Open Questions / Deferred

- **1.6.0 NOT shipped yet — operator directive (2026-06-09): do NOT change the version.** The follow-up wave lands BEFORE shipping 1.6.0. CHANGELOG drafted into the unreleased `## [1.6.0]` (graph-tools entries added beside the secrets work); VERSION unstamped, no `v1.6.0` tag, no ship. Latest released tag is `v1.5.1`.
- **Follow-up wave (next up) — full scope captured in `docs/plans/1p4ef-bug graph-qualified-index-leaked-loop-var.md`.** Lead item **`1p4ef`** (leaked-`qualified` loop-var bug at `graph_indexer.py:6341` — silently suppresses cross-file resolution on collapsed/basename-merge languages C#/Swift/Rust/Ruby; the likely cause of any misses the Swift/C# teams report; ~3-line fix + `GRAPH_BUILDER_VERSION` bump). Plus the team-found **same-package ambiguous-receiver gap** (Java `JreCompat.canAccess` dropped — HIGH; fix = same-package/same-directory fallback in the disambiguation block); **`code_impact` polish** (edges array not bounded by `max_results` → 227K-char blowout on high-fan-in symbols; graph-mode `resolved: null`); and the **C#/Go/Rust resolver improvements** (Go method-keying as `Type.method`, Rust `Type::assoc_fn()`, the shared membership-disambiguation that generalizes `1p470`).
- **`code_risk_score` reproducible-gate harness** — the AC-8 gate is a one-time manual measurement against the gitignored/per-machine graph; the recorded ρ/CoV are not re-derivable bit-for-bit (delivery-council N1). Follow-on: commit a measurement harness so the gate is auditable.
- **UNCOMMITTED** — all of this session's wave work (1p47e close + 1p4ef plan + CHANGELOG draft) is uncommitted; operator commits when ready (no AI attribution / Co-Authored-By).
- **Test pack `1.6.0+p4ea` built + distributed** (2026-06-09) carrying secrets fixes + graph tools; validated by 3 teams (see above). `~/.wavefoundry/dist/` holds p3zo…p4db, **p4ea** (newest; upgrade selector takes newest mtime) + `wavefoundry-1.6.0.p4ea-test-instructions.md`. Rebuild after the follow-up wave before shipping 1.6.0.
- **Consumer re-test** (the other projects): the 26 newly-active detectors' false-positive impact on real repos (`1p4d1`); the manifest-stamp / `confirmation_valid_days` / field-name fixes (p4a4); `exc-001`-style phantom `pending` auto-clear on the next full scan (`1p4a2`).
- ~~`install-log-format.md` latent provisioning gap~~ — **ADDRESSED** by `1p4dc` in `1p47e` (shipped as a framework template + seed-012/160 provisioning, mirroring `1p455`).
- Carried deferred: 1p44s oversized/binary skip count in the scan RESULT (surfaced only via stderr today); `12tm5` duplicate ADRs renumber (`1p45b` prevents NEW collisions only).

## Current Session

**Active wave:** *(none)*

**`1p4mf` constant chunking — COMPLETE across all 10 languages (UNCOMMITTED).** `chunk_python` (committed `81bae0c`) + JS/TS (both tree-sitter + regex paths) + the 8 source-absent languages (Go/Java/Kotlin/C#/Rust/Swift/Ruby/PHP). Bespoke walkers (Go/Rust/Java/C#/Kotlin) emit constants in-function; the generic-walker languages (Swift/Ruby/PHP) drive a new `const_emitter` hook on `_ts_generic_structured_chunker` (dormant by default). Per-language mechanism+scope gates, no casing gate (casing only for Python); FAIL-not-skip fixtures. Shared fixes: `constant` added to `_TS_NAME_CHILD_TYPES` (Ruby class names). `CHUNKER_VERSION=26`. **Full framework suite 3068 green.** AC-1..AC-6 satisfied; AC-2/AC-5 ✅; the 8 langs' recall/bloat = p4ea post-bump checkpoint (AC-7). Req-7 resolved (documented-divergent + watchpoint; shared-detector unification folded into `1p4ls`).

**`1p4ls` graph constant nodes + `reads` edges — COMPLETE (UNCOMMITTED).** All 11 languages: module/type-level constants are `kind="constant"` graph nodes (carrying a simple-literal `value`), reusing the 1p4mf chunk-lane detection via a lazy `_chunker_module()` import (Req-7 — one detector, two consumers); a faithfulness-gated function→constant `reads` edge (same-scope; `symbol_lookup` uniqueness + const-kind gate + Python local-shadow guard). All 4 council must-fixes: `resolve_symbol` kind-aware, `one_hop` `reads` opt-in + `max_neighbors` bound, cluster excludes constants/`reads` (`CLUSTER_BUILDER_VERSION` 8→9), `reads` out of impact/call defaults. `GRAPH_BUILDER_VERSION` 25→26. Consumers: `code_definition` resolves constants; `code_references` distinct graph-sourced `reads` bucket; `graph_neighbors` includes a constant seed's readers. **AC-6 real-graph: 40/48 constants with correct reader edges; `RERANKER_MODEL` resolvable with value.** ALL ACs met (AC-1..AC-7). AC-2 covers same-scope AND cross-module imported reads (post-merge `finalize()` resolution that binds a unique constant-kind node or drops — imported functions/ambiguous twins dropped). Full framework suite **3081 green**. Architecture doc updated. No open 1p4ls follow-ups.

**`1p4hu` graph-signal retrieval fusion — COMPLETE (UNCOMMITTED).** Agent-mode `code_ask` adds a labeled `source="graph"` lane: query's resolved symbol(s) → 1-hop neighbors (callers/callees/importers + the 1p4ls `reads` edge) → structural candidates surfaced via the per-index FLOOR (so they appear below the semantic cutoff). Bounded (`AGENT_GRAPH_SIGNAL_CAP`), graceful no-graph fallback. **AC-5 (gate) PASS** on 4 live-project queries (e.g. "what reads LANCEDB_COMPACT_THRESHOLD" → its 3 reader functions). All 6 ACs; +5 `GraphSignalTests`. No `GRAPH_BUILDER_VERSION` bump.

**`1p4lr` definition-match boost — COMPLETE (UNCOMMITTED).** GENERALIZED from constant-only to ALL definitions (operator directive): `_definition_match_boost`/`_definition_qname` boost a constant OR a function/method/class/interface/type/enum definition chunk whose declared-name tokens all appear in the query; `_query_content_terms` camel-splits so camelCase/PascalCase names match. **AC-5 value gate RE-RUN: 11/11 PASS, no regression.** All ACs.

**`mode='rechunk'` (1p4n4 extension) — COMPLETE (UNCOMMITTED).** `wave_index_build(mode='rechunk')` / `indexer.py --rechunk`: re-chunk every file + reuse embeddings by hash, WITHOUT a version change (for a chunker logic change not version-bumped). Live-validated on the project index.

**WAVE FULLY IMPLEMENTED** — all 6 changes `implemented` (`wave_current` → `next_action: close_wave`). Full framework suite **3102 green**, docs-lint ok. Live-MCP smoke test PASSED (constants + reads + graph-signal on real code). Everything UNCOMMITTED.

**DELIVERY REVIEW COMPLETE (2026-06-10) — `wave-council-delivery` signoff RECORDED; verdict READY-WITH-FIXES → READY (pending operator).** Adversarial 6-agent red-team (all probes under `~/.wavefoundry/venv`). **Dimension E CLEAN** (4 must-fixes genuinely applied; version coherence; no calls/imports/defines regression — HEAD-vs-WT byte-identical; no fail-open scope narrowing — no local leaked as a constant in any of 11 langs). **6 verified bugs FIXED in-session** (+6 regression tests, suite 3091→3097, AC-5 eval 11/11, live boost probe confirms firing): A1 Go ≤2-char consts dropped→chunked; B1 Kotlin object/companion `const val` reads silently zeroed (const-intercept `simple_names` dup)→fixed; B2 imported-read wrong-twin bind via bare simple-name fallback→fallback removed (qualified-unique-or-DROP); C1+C3 boost INERT on split chunks (`:L…`/`(part N/M)` id suffix junk tokens + qualified-only match)→suffix-strip + leaf-name match (`search_combined` now boosts, was 0); D2 cross-source dedup keyed on full line-tuple that never matches→dedup on `(path,start_line)`. **Claim-corrected (faithfulness):** A2 TS enum/namespace/`declare const` scoped OUT (not chunked); B3/B6 carve-outs documented; B4/B5 Kotlin bare-`val`/TS-enum graph nodes scoped to actual; F1 `code_definition` keyword-fallback (not structural const node) softened; C2 1p4lr Progress-Log corrected; graph-index-system.md + chunking-and-indexing-pipeline.md (`CHUNKER_VERSION` 22→26 + constant note = 1p4mf AC-7 doc half). **Deferred follow-ups (documented, non-gating):** A3 generic-walker nested-type wrong owner; A6/A7 nits; D1 direction-aware graph signal (currently bidirectional bag); D3 query-token stoplist; D4 `from_root` per-query reload (memoize); TS-enum + Kotlin-bare-`val` graph nodes; regex-fallback gaps; `code_definition` structural const node. `wave_review(implementation)` → only `operator-signoff` outstanding (lint_passed, max_severity none).

**1p4hu GRAPH SIGNAL — empirically measured + TIGHTENED (operator-directed, after the review).** A 17-query live sweep (graph-ON vs OFF, verified against the live graph) showed the as-shipped signal was ~3/14 wins, direction-blind, with 2 relevant-displacement cases (the AC-5 4-query gate was the favorable band). Operator directive: "tighten it; add to the results, don't kick anything out — we have room (24k→16k)." Implemented: **direction-aware** (`one_hop_neighbors(direction=…)` — "what calls/reads/uses X" → edges INTO the named symbol only), **seed-scoped** (targeted queries seed only the named symbol; behavioral keep semantic breadcrumbs), **additive** (graph cands appended after the full semantic selection — no floor slot, not budget-charged → never evicts a semantic cite), **noise gates** (generic-word seed stoplist + test-file + module-node suppression). Re-measured: **6 wins / 5 correct no-fires / 2 mild behavioral / 0 displacement.** Then (operator follow-up) structural matches moved OUT of `citations` into a dedicated **`graph_related`** response section grouped by relationship (`callers`/`readers`/`importers`/`related`, each with `symbol`/`path`/`lines`/`kind`/`confidence`) — the relationship is the answer to "what calls/reads X" that a 0.0-scored citation discarded; citations are now purely semantic. **Semantic dedup kept as text-dedup** (`also_cited` flag + excerpt dropped — chunk text never sent twice). `search_combined` now returns an 8th tuple element (`graph_related`). Suite **3102 green**, AC-5 eval 11/11. **Post-signoff RE-REVIEW done (6-dimension adversarial, the changes post-dated the original signoff):** NO blockers/majors; dimensions C (8-tuple contract) + D (citation regression — citations byte-identical ON vs OFF) CLEAN; fixed in-session (all minor/nit): `entries` stoplist gap, subject-first "X calls what" + past-tense "is X called" intent classification, stale docstrings, `build_index` doc example, no-fire framing (extractor-coverage gaps), dedup-scope note. +2 regression tests. Recorded in 1p4hu AC-1/AC-2/AC-5 + Progress Log + the wave delivery sign-off addenda (incl. addendum 3 = the re-review). **Verdict: READY** — only the operator-owned `CHUNKER_VERSION` 26→27 bump + signoff remain.

**Operator-owned close actions (do NOT do autonomously):**
1. **⚠️ Bump `CHUNKER_VERSION` 26 → 27** (operator-confirmed at close — the interim `81bae0c` `26` was Python-only; the wave extended it to all 11 languages under the same `26`, breaking version=shape; see the wave.md CLOSE ACTION watchpoint).
2. git commit; wave close; version stamp/tag/ship.
3. The combined re-index window: `CHUNKER_VERSION 27` + `GRAPH_BUILDER_VERSION 26` + `CLUSTER_BUILDER_VERSION 9` (the chunker bump uses 1p4n4's incremental-reuse path).

**Specs from this session's workflow** `wf_fc1e2d4a-827` are in `/tmp/const_specs/` (transient).

**PACKAGING + VALIDATION (2026-06-11, operator-directed — wave still OPEN, NOT closed):**
- **`CHUNKER_VERSION` bumped 26→27** (the close-action watchpoint, operator-confirmed) + version test → `27` + doc refs synced. Full suite **3102 green**. Both local indexes rebuilt to chunker 27 (`wave_index_build` — incremental-reuse path); `wave_index_health` → both layers `current`, `semantic_ready`. AC-5 recall eval **11/11** on the v27 index.
- **Local test pack built:** `~/.wavefoundry/dist/wavefoundry-1.6.0.p4pu.zip` (VERSION `1.6.0+p4pu`), verified to carry CHUNKER 27 + `graph_related`/direction code + the prebuilt framework index. Test instructions: `~/.wavefoundry/dist/wavefoundry-1.6.0.p4pu-test-instructions.md` (per-language constant table + AC-7 bloat ask).
- **Agent-facing SEED + guru-doc updates** (the wave changed `code_ask` behavior but the seeds hadn't been updated — operator caught it): fixed the stale "`code_ask` is slow (5–30s cross-encoder)" framing (agent-mode is the fast default now), added the `graph_related` section + constant-retrieval/`reads`-bucket guidance in `seeds/211-guru`, `seeds/180-implement-feature`, `seeds/050-agent-entry-surface-bootstrap`, AND the local `docs/agents/guru.md` (incl. its response-field section: `rerank_mode`/`graph_related`/`second_hop`-graph — it was actively mis-guiding "`reranked:false` = low quality"). seed_edit_allowed gate opened+closed; docs-lint ok. **⚠️ These seed updates are NOT in the `p4pu` pack — a pack REBUILD is needed for them to reach consumer projects (operator holding the rebuild).**
- **`javaagent` (Java) p4pu validation — CLEAN PASS A/B/C** (first consumer report): constants retrievable (MAX_JSON value rank 1, `[const]` markers), **zero scope-gate false positives** (no method-local `static final` leaked), `graph_related.readers` + `code_references` `reads` bucket correct + direction-faithful, **coincidental-twin disambiguation holds** (same-named const in 2 classes attribute to own def), agent-mode 500–900ms (cross-encoder gone). **AC-7 bloat data point: Java +23.2%** (2538→3128 chunks, constant-dense repo, the high end) — recorded in `1p4mf` AC-7. Swift/JS-TS pending.
- **NEW 7th change `1p4pz` — `code_constants` multi-language — IMPLEMENTED** (operator follow-up on the javaagent `code_constants`-returns-0-for-Java gap). The tool was Python-only (column-0 `NAME=value`); now unions that with a chunk-lane pass (per-extension dispatch to the tree-sitter chunkers — the generic `chunk_file` dispatch silently falls back to a whole-file chunk for Go/JS-TS, worked around) + a language-agnostic value extractor (RHS after `=`, trailing `;` trim, PHP `define()`, multiline, per-declarator). All 11 languages + Python class-level constants; function/block locals excluded; range-dedup vs the regex. **No version bump (read tool).** +4 net CodeConstantsTests (22 total); full suite **3106 green**; docstrings + `mcp-tool-surface.md` updated. The `code_outline` Python-only constant gap is the remaining Req-7 four-surface reconciliation item (separate). The `code_outline` Python-only constant gap is the remaining Req-7 four-surface reconciliation item (separate, not in this wave).
- **NEW 8th change `1p4q4` — TS enum / const-enum members + namespace const + declare const — IMPLEMENTED** (operator: deliver what 1p4mf AC-2 claimed but the delivery review scoped out). Chunk lane (`chunk_js_ts_treesitter`): `enum_declaration`/`internal_module`/`ambient_declaration` branches → each enum member is a `Enum.Member` `[const]` chunk. Graph lane (`graph_indexer` is-definition path): each TS enum member is a `kind="constant"` node (`Enum.Member`), the enum type stays a class node. **Bumps `CHUNKER_VERSION` 27→28 + `GRAPH_BUILDER_VERSION` 26→27 → supersedes `p4pu`; the local v27 index + the pack need a v28 rebuild.** Claim corrections applied (1p4mf AC-2, 1p4ls AC-1, graph_indexer:28 comment — TS enums now SHIPPED). +4 net tests (chunker enum + graph enum + version asserts); full suite **3110 green**; arch-doc version refs synced (CHUNKER 28 / GRAPH 27). 2-char enum members (`OK`) are dropped from the GRAPH by the pre-existing short-symbol prune (chunk lane keeps them). **A NEW adversarial review of 1p4pz + 1p4q4 is running** (`wf_69990b0f-6f7`); synthesize + fix verified findings before close. **Operator-owned close actions now:** git commit; wave close; the v28 pack rebuild (CHUNKER 28 + GRAPH 27, + the held seed-doc updates) + re-validation (Swift/JS-TS + javaagent re-confirm); version stamp/tag/ship.

**1p4pz + 1p4q4 ADVERSARIAL RE-REVIEW SYNTHESIZED + FIXED (2026-06-11, `wf_69990b0f-6f7`, 6 dimensions).** 9 verified-real MAJORs + minors, all fixed in-session (no contract change). `code_constants`/`server_impl.py`: **A1** value extraction now string-aware (shared `_string_literal_end` primitive — `CSV_SEP=","`→`","`, Java `"a;b;c"` whole, dict past in-string `}` complete); **B1** leading comment block stripped before the NAME search (no value poisoning); **B2** Go grouped `const(...)`/iota → every member resolves to its own line+value; **B3** qualified lookup works (leaf-token pre-filter + dual short/qualified resolution); **B4** `.mts`/`.cts` dispatched. `chunker.py`: **C1** `module M{}` keyword form, **C2** non-export namespace const, **C3** `export namespace`/`declare namespace`/`declare enum` all chunk. `graph_indexer.py`: **D1** namespace-scoped enum members carry the enclosing-namespace prefix (no cross-namespace clobber), **D2/F1** constant nodes exempt from the ≤2-char short-symbol prune (`Status.OK`/`Dir.Up` resolve). Doc-truth: **E1** arch-doc version ref, **F2** wrong function reference. Nits **F3** (single-line enum chunk text) / **F4** (trailing-comment glue, column-0 path) deferred (no functional impact; F4 string-aware strip risks Python `//` corruption). **+11 regression tests.**

**VERSION RE-BUMPS (operator-directed, 2026-06-11) — the review fixes change chunk/graph SHAPE, so they need version bumps to auto-rebuild (not a manual force-rebuild):** `CHUNKER_VERSION` **28→29** (C1/C2/C3 namespace/module/declare-enum chunk-set change) + `GRAPH_BUILDER_VERSION` **27→28** (D1/D2 node-set change). `CLUSTER_BUILDER_VERSION` stays 9. Version-assertion tests updated (`test_chunker_version_bumped_to_29`, `test_graph_builder_version_is_28`). Both unshipped (p4pu = 27/26) so the bump is clean — any 27/28 dogfood/interim index now auto-re-extracts.

**`.mts`/`.cts` MADE FIRST-CLASS (completes B4 beyond `code_constants`):** added to indexer `SOURCE_CODE_EXTENSIONS`, chunker `JS_TS_EXTENSIONS` + `_EXT_TO_LANGUAGE` + `chunk_js_ts_treesitter` lang_key, graph_indexer `_CODE_EXTENSIONS` + `_TS_EXTENSION_TO_LANGUAGE` — all → `typescript`, so `code_ask`/`code_search`/`code_outline`/graph index handle them (enum members chunk, `language='typescript'`). +2 tests.

**GRAPH INDEX REBUILT + RE-STAMPED 28** (`wave_index_build(content='graph', mode='rebuild')`) — confirmed the build SUBPROCESS imports on-disk code fresh (spawn workers, `sys.path[0]=…/framework/scripts`), so the bump materialized (`builder_version: 28`). NOTE: the long-running MCP **server** holds STALE in-memory modules (health shows `current_chunker_version: 27`) — a server reconnect clears the cosmetic mismatch; the build subprocess is unaffected. The local CODE/docs semantic index (chunker 28) is now stale vs 29 → a `rechunk` will materialize C1/C2/C3 locally (the build_pack framework-index build + any consumer upgrade auto-escalates).

**DOC-FAITHFULNESS AUDIT (8 agents) + FIX (6 agents) before packaging.** Seeds 211-guru / 180-implement-feature / 050-entry verified READY (no change). Fixed: `docs/agents/guru.md` (added `code_constants` + constant/enum recipe; `graph_related` now lists all 7 relationship buckets), `mcp-tool-surface.md` (code_constants string-aware/Go-grouped/leading-comment/qualified/.mts-cts; code_definition constant+enum nodes + short-prune exemption; code_references `reads` bucket), `chunking-and-indexing-pipeline.md` + `graph-index-system.md` (TS enum/namespace coverage, CHUNKER 29 / GRAPH 28, fixed stale line-citations), 1p4q4 + 1p4pz change docs. Full suite **3122 green**, docs-lint ok.

**Operator-owned close actions now (UPDATED versions):** operator signoff; git commit; wave close; **the v29 pack rebuild (CHUNKER 29 + GRAPH 28 + CLUSTER 9, + the seed/guru-doc updates) + re-validation** (Swift/JS-TS + javaagent re-confirm, now incl. the review fixes); version stamp/tag/ship. (Building the new test pack now — supersedes `p4pu`.)

---

## 2026-06-11 (cont.) — 1p4hi CLOSED + p4su; then two p4su-validation fixes folded into 1p4u5

**1p4hi closed (operator); `1.6.0+p4su` pack built + distributed** carrying the 1p4pz/1p4q4 review fixes, CHUNKER 29 / GRAPH 28, `.mts`/`.cts` first-class, doc-audit fixes. **3-team p4su validation: all PASS except one member-access-reads gap** (Java clean; Swift PASS except F/H1 nested-static reads; JS-TS PASS — enum members resolve). The teton p3zo secrets/upgrade cluster was verified **already fixed** (waves 1p44o/1p44s/1p44u/…); only the JS-TS Phase 4b upgrade bug (H1) remained.

**Active wave: `1p4u5 hardware-aware-embedding-providers`** (GPU embedding) — two p4su-validation fixes folded in (operator-directed):

- **`1p4up-enh member-access-constant-reads` — IMPLEMENTED.** Delivers the deferred 1p4q4 member-expression read attribution: a CONSTANT accessed via a qualified member expression (`Status.ACTIVE`, `SolarisConstants.Network.userAgent`, `Outer.Inner.TOKEN`, Ruby/PHP `A::B::C`) now emits a function→constant `reads` edge by EXACT qname match (const-gated). All 8 member-access languages. **A prior `_simple_name` rsplit one-liner was REVERTED** (a review found 3 over-binds incl. a real-repo import-read regression). Faithfulness guards (2 adversarial reviews): F1 full-qname gate, F2 reject this/self, F4 qualifier-shadow guard (`func_locals` per-function binding names) + property-leaf skip (member-path resolves the trailing member, not a bare leaf). **Final review caught + fixed a BLOCKER** (`_ts_is_member_property_leaf` used `is` on tree-sitter wrappers → blanket-skip → dropped object/array const HEAD reads like `FRAMEWORK_FLOW.length`; fixed `is`→`==`). Regression diff NEW-vs-baseline incl. real dashboard.js: **LOST=NONE, calls unchanged**. `GRAPH_BUILDER_VERSION` **28→29** + version test. **+regression tests** (member-access per language, qualifier-shadow suppressed/legit fires, instance-access clean, object/array head read).
- **`1p4uq-bug phase4b-background-build-reliability` — IMPLEMENTED.** JS-TS H1 (Phase 4b background re-embed silently `idle`). Three fixes: `setup_index.py` stamps its own pid before the docs build (status → `completed` not `none`); `upgrade_wavefoundry.py` logs the launcher (→ `project-upgrade-bgcode.log`, not DEVNULL); `phase_cleanup` warns on code-vs-docs chunker-version mismatch. +3 `BackgroundCodeIncompleteWarningTests`.

**State:** full suite **3138 green**; docs-lint ok; no change-status drift (wave.md ↔ change docs reconciled, both `implemented`); local graph index rebuilt + re-stamped **builder_version 29**. **Current source versions: CHUNKER 29, GRAPH_BUILDER 29, CLUSTER 9.**

---

## 2026-06-11 (cont. 2) — embedding-retrieval architecture: FP16/CoreML investigation, 1p4u5 CLOSED, wave 1p4wz OPEN + fold mid-implementation

Deep FP16/CoreML embedding investigation (durable findings in memory `project_fp16_coreml_gpu_win` + `project_code_retrieval_quality_levers`), the docs-breadcrumb fix shipped into 1p4u5, **1p4u5 CLOSED** (delivery-council signoff recorded), and a new wave **`1p4wz embedding-retrieval-architecture`** opened and mid-implementation. All work UNCOMMITTED.

**Key findings (durable, see memory):**
- **FP16 + static-shape + CoreML(GPU) ≈ 8.75×** embedding throughput on M2 Max vs INT8/CPU; the win is the GPU/Metal, not the ANE; needs ALL three (FP16 weights + static shape + graph-opt ON). CoreML compile is per-`InferenceSession` (~40s) — cache via `ModelCacheDirectory` (cross-process ~7s). `server_impl._get_embedder` rebuilds the embedder per query (no cache guard, unlike `_get_reranker`) — a prerequisite fix for the CoreML query path.
- **Axes:** model = global QUALITY (CPU-floor-bounded, pinned, shipped); provider/format = per-machine SPEED (INT8↔CPU / FP16↔GPU; vectors cos≈1.0 interchangeable). Don't vary the model by machine — vary the provider.
- **Bake-off (62 code + 45 docs queries):** `arctic-embed-xs` beats `bge-small` on DOCS (+15–20pp); `bge-small` unbeaten on CODE (jina-base-code = 0pp; 0/8 deep-rank rescued by any of 5 models). Code-retrieval lever is hybrid-lexical/reranker/chunking, NOT a model swap.

**`1p4u5` closed** — final change `1p4w9-enh docs-chunk-context-injection` (chunker prepends the section breadcrumb to docs-chunk embed text; docs-only — regresses code; `CHUNKER_VERSION 29→30`; +10pp docs).

**Wave `1p4wz embedding-retrieval-architecture` — IMPLEMENTING** (all design decisions recorded in each change doc's Decision Log):
- **`1p4ww-ref fold-framework-index-into-core-docs`** — eliminate the separate shipped framework index; fold framework **seeds + README** into the project docs index (scope narrowed by operator; tagged `seed`). **Stages 1–3 + Stage 4a (read path) IMPLEMENTED + green (suite 3145 at every step).** Done: `indexer.FRAMEWORK_FOLD_DOCS_PREFIXES` + `_effective_project_include_prefixes`; `chunker.SEED_PATH_MARKERS` += framework README; `server_impl._ensure_loaded` single project layer (fw tables + search merges removed); `build_pack` stop-ship (`prebuild_index=False`, guard call removed); `WALKER_VERSION 5→6` (re-walk trigger; the stale framework index is pruned on upgrade by the existing manifest-prune). **REMAINING — see the 1p4ww Progress Log file:line map: Stage 4b** = the coupled ~30-site removal of `server_impl.framework_index_dir`, `_layer_health('framework')`, and the `layer == 'framework'` threading through the build/status/background-refresh helpers (line ~4682 still STARTS a framework refresh) — must change together; **Stage 5** = topology doc + ADR + AC verify + mark implemented. ACs met: 1,2,4,5,7; AC-6 N/A; AC-3 partial (read path done).
- **`1p4wx-enh docs-code-embedding-model-split`** — `DOCS_MODEL=arctic-embed-xs` / `CODE_MODEL=bge-small`. PLANNED, hard-depends on `1p4ww` (uniform docs model). Wiring: model constant + arctic query prefix in `EMBEDDING_PREFIXES` + `setup_index._MODEL_CACHE_DIR_ALIASES` + `_EXPECTED_DOCS_MODEL` anchor + ADR. Name change auto-triggers a docs-only re-embed.
- **`1p4wy-enh local-embedding-acceleration-and-cache`** — FP16 execution when a GPU provider is selected + `~/.wavefoundry/` static-ONNX + CoreML compile cache + the `_get_embedder` singleton. PLANNED. Decisions: download-and-cache the FP16 ONNX (provider-conditional — fetch only the format the provider uses); single static shape `64×512` (incremental-dominant workload), batch tunable; FP16 not FP32.

**RESUME (next session):** wave is already `implementing` (no re-open). For `1p4ww` Stage 4b: open `framework_edit_allowed`, work the 1p4ww Progress-Log file:line map (framework_index_dir + health + build/status/refresh layer threading) as ONE coupled push, gate on `python3 .wavefoundry/framework/scripts/run_tests.py` (~3145), close the gate. Then Stage 5 (docs/ADR + mark implemented), then `1p4wx`, then `1p4wy`. To make the fold LIVE in this repo, a `wave_index_build` re-index is needed (the `WALKER_VERSION` bump triggers it — operator-owned). **Current source versions: CHUNKER 30, GRAPH_BUILDER 29, CLUSTER 9, WALKER 6.** Operator-owned: git commit, wave close, re-index, version stamp/tag/ship.

---

## 2026-06-11 (cont. 3) — 1p4ww Stage 4b + Stage 5 COMPLETE → `1p4ww` IMPLEMENTED

**`1p4ww` is now fully implemented (UNCOMMITTED).** The framework index layer is removed from the docs/code semantic index across all surfaces; full framework suite **3123 green**, docs-lint ok, change-status reconciled (`1p4ww` = `implemented` in both wave.md and the change doc; `1p4wx`/`1p4wy` still `planned`).

- **`server_impl.py` (Stage 4b, the coupled ~30-site push):** dropped `framework_index_dir` + every use; `_layer_health`/`_layer_current_hashes`/`docs_health` are project-only (the fold prefixes `FRAMEWORK_FOLD_DOCS_PREFIXES` are unioned into the health include-set so folded seeds don't read as false `removed_paths`); `_ensure_loaded` single-layer meta/signature; the module path/state helpers (`_index_dir_for_layer`, `_index_build_state/log/stats_path`, `_background_refresh_state_path`, `_graph_health_summary`, `run_index_rebuild`, `_index_is_up_to_date`, `_start_background_index_refresh`) lost their `layer=='framework'` branches; `_trigger_background_index_refresh_for_paths` folds framework-seed/README path changes into the single PROJECT refresh; `wave_index_build`/`wave_index_build_status` reject `layer='framework'`.
- **`dashboard_server.py` + `dashboard_lib.py`:** `_INDEX_LAYERS=('project',)`; removed `_framework_index_inputs_stale`, the framework snapshot/health/graph blocks + watched paths, and the (now-erroring) `wave_index_build_status_response(layer='framework')` call in `collect_health`; `_project_index_inputs_stale` is fold-aware (a framework-seed change marks the PROJECT layer stale; MANIFEST/VERSION stay excluded).
- **`build_pack.py`:** removed `build_framework_index`/`_compact_framework_index`/`_framework_index_source_files`/`_assert_zip_contains_index`/`_load_indexer`/`prebuild_index`/`--skip-framework-index`+mutex (the pack ships framework SOURCE only — AC-4).
- **`render_platform_surfaces.py`:** the post-edit reindex hook renders a single bare project spawn (no separate framework-index spawn); the on-disk hooks under `.claude/`/`.cursor/`/`.github/`/`.windsurf/` were **regenerated** to match.
- **Tests:** framework-layer tests across `test_server_tools`/`test_dashboard_server`/`test_build_pack`/`test_render_platform_surfaces` migrated or removed; added fold + project-staleness coverage.
- **Stage 5 docs:** `search-architecture.md` Decision 4 rewritten (single project index, framework docs folded); **new ADR `docs/architecture/decisions/1p4xx-adr fold-framework-index-into-project-docs.md`** + README index row. (The graph **query** layer project/framework/union is a separate subsystem, intentionally left intact — with no framework graph built, a framework graph query just returns absent.)

**Note:** no version bump beyond the existing `WALKER_VERSION 5→6` (this change alters index TOPOLOGY, not chunk/graph SHAPE). **Current source versions unchanged: CHUNKER 30, GRAPH_BUILDER 29, CLUSTER 9, WALKER 6.**

**RESUME:** `1p4wx` (docs/code model split) is next — it hard-depends on `1p4ww`'s uniform docs model, now satisfied. Then `1p4wy`. To make the fold LIVE in this repo, an operator-owned `wave_index_build` re-index is still needed (the `WALKER_VERSION` bump triggers it). Operator-owned: git commit, wave close (after `1p4wx`/`1p4wy`), re-index, version stamp/tag/ship.

---

## 2026-06-12 — 1p4wx docs/code model split IMPLEMENTED

**`1p4wx` is implemented (UNCOMMITTED); full suite 3131 green, docs-lint ok.** Docs now embed with the asymmetric `arctic-embed-xs`; code stays on `bge-small`. Both 384-d (no dim ripple), so the docs-model swap reuses the code index unchanged.

- **Wiring gap found + closed (the doc didn't anticipate it):** `EMBEDDING_PREFIXES` was defined but **never consumed** — the pipeline embeds via fastembed `.embed()`, which does NOT auto-apply query/document prefixes. arctic is asymmetric (query prefix required), so I wired the QUERY prefix explicitly in `server_impl._embed_query` via new `indexer.query_embedding_prefix()`. The document side stays prefix-free (correct for arctic) and is guarded by an import-time `indexer._assert_active_models_have_empty_document_prefix()` invariant (forces any future asymmetric-document model to wire the build path deliberately).
- **Constants:** `DOCS_MODEL = "Snowflake/snowflake-arctic-embed-xs"`, `CODE_MODEL = "BAAI/bge-small-en-v1.5"`. `EMBEDDING_PREFIXES` arctic entry: query=`"Represent this sentence for searching relevant passages: "`, document=`""`.
- **Cache alias:** `setup_index._MODEL_CACHE_DIR_ALIASES["Snowflake/snowflake-arctic-embed-xs"] = ("snowflake/snowflake-arctic-embed-xs",)` — fastembed normalizes to lowercase and downloads to `models--snowflake--snowflake-arctic-embed-xs`, so the offline cache resolution must include it. **arctic-xs warmed locally** (dim 384, FP32 `onnx/model.onnx` — **not INT8** as the rationale guessed; loads fine on CPU). The `SemanticEmbeddingRegressionTests` now run (not skip) and pass on arctic.
- **Tests (+9):** `DocsCodeModelSplitTests` (constants, prefix registration, `_embed_query` applies/omits the prefix, doc-prefix invariant, unknown-model default) + `test_setup_index` cache-alias + `test_indexer.test_docs_model_change_reembeds_docs_only_code_untouched` (AC-4: content='docs' re-embeds docs, never loads the code embedder, code.lance byte-identical). Test anchors: `_EXPECTED_DOCS_MODEL`→arctic, added `_EXPECTED_CODE_MODEL`, dim stays 384.
- **Docs:** new ADR `1p50s-adr docs-code-embedding-model-split.md` + README index row; `embedding-model.md` current-model table corrected (was a **stale** `bge-base`/768 — the code had already moved to bge-small/384; now arctic/bge-small/384) + a new arctic-query-prefix section.
- **AC status:** AC-1/2/3/4/6 met + verified; **AC-5 (real retrieval delta on `retrieval_eval.json`) pending an operator re-index** (the docs-model name change auto-triggers a docs-only re-embed on the next `wave_index_build`). No version bump (the docs-model NAME is the version).

**1p4ww FOLD PIPELINE BUG found + fixed (2026-06-12).** Triggered by an operator question ("can we delete framework/index/docs.lance?"): the live project index had **0 framework seed/README rows** despite walker=6 — the fold had never actually reached the index. Root cause: `files_for_meta` used `_merged_project_include_prefixes_for_graph` (docs+code, no fold prefixes), stripping the seeds at the `files_for_meta` stage before the docs-content filter (which carries the fold prefixes) ran. Stage 1's unit tests wrote synthetic index rows and never exercised the real walk, so it shipped green-but-empty. **Fix:** `indexer._project_meta_include_prefixes()` (graph surface + `FRAMEWORK_FOLD_DOCS_PREFIXES`) at both `files_for_meta` sites + centralized in `server_impl._layer_current_hashes`. +1 real-pipeline regression test; 3 pre-fold "no framework in project meta" tests updated to fold-aware. **Re-indexed:** 67 seeds + README now in `.wavefoundry/index/docs.lance`; `docs_search(kind=seed)` returns framework seeds (`trusted_framework`, arctic+reranker); health `ready`. **The dead `.wavefoundry/framework/index/` (25M, gitignored) was deleted.** Suite **3132 green.** (Folded into 1p4ww, still UNCOMMITTED.)

**Framework/union GRAPH-query layer removed → graph is project-only (2026-06-12, folded into 1p4ww).** Operator decision: unify on the project config — `workflow-config.json` `project_include_prefixes` already drives BOTH the semantic index and the graph (no new `index_rules.toml`; this repo opts `.wavefoundry/framework/scripts` into `code`, so its scripts are graphed; consumers graph only their own code — folding framework scripts into every consumer graph = ~90% noise). The shipped framework graph was gone with the framework index, so `framework`/`union` were vestigial. Collapsed across `graph_query.py` (dropped `load_union` + the `cross_layer` report section + framework path branches), `graph_indexer`/`graph_cluster`/`dashboard_lib` (filename dicts → project-only), `indexer._graph_layer_for_index_dir` (always `project`), `server_impl._graph_layer_value` (coerces any layer → `project`, back-compat no-op) + docstrings, dashboard `/api/graph` (reject non-project). **No `GRAPH_BUILDER_VERSION` bump** (only layer plumbing, not node/edge shape). Tests migrated; arch `graph-index-system.md` + ADR `1p4xx` updated; suite **3132 green**. **DEFERRED (follow-on change, operator): a project-config `project_exclude_prefixes` "remove" lever** (today removal = `.gitignore`; the add lever is `project_include_prefixes`).

**`1p4wy` re-scoped + landed (2026-06-12): singleton shipped, FP16 core split into `1p517` — now the 4th change in this wave (operator).** `1p4wy` landed only **AC-5: the `_get_embedder` singleton** (`WaveIndex._get_embedder` + `indexer._EMBEDDER_CACHE`, per-process, so the CoreML compile is paid once not per query) + 2 tests; suite **3134 green**. The FP16/CoreML acceleration core moved to **`1p517-enh fp16-coreml-embedding-acceleration`** (admitted into `1p4wz`, status `planned`).

**FP16 picture CORRECTED (2026-06-12, verified by reading the cached ONNX initializer dtypes):** the deployed weights are **FP, not INT8** — **code/bge-small is already FP16** (149/149 FLOAT16), **docs/arctic is FP32** (101/101 FLOAT32). So the earlier "INT8 baseline / bge-small needs a clean FP32 source" framing was wrong: **bge-small needs no conversion**, and **CoreML MLProgram likely compiles FP16 itself** (downcasts FP32 at compile), so even arctic may need no pre-conversion. Net for `1p517`: feed both models' existing weights + a **static-shape pin (64×512, needs `onnx`)** + a bespoke ORT CoreML session (fastembed can't run static shapes) + `ModelCacheDirectory` cache. **FP16-vs-FP32 BENCHMARK (2026-06-12, M2 Max) RESOLVED the conversion question: don't convert.** Arctic-FP32-static on CoreML = **391 ch/s, GPU offload (cpu/wall 0.04), cos 1.00000** vs the CPU reference (≈24× INT8/CPU, perfect correctness) — CoreML computes FP16 internally regardless of weight precision, so feeding FP32 gets the full win with **no `onnxconverter_common`, no conversion**; the decisive ingredient is the static-shape pin, not the format. **`1p517`'s only new dep is `onnx`.** Full detail in the `1p517` change doc + memory `project_fp16_coreml_gpu_win`.

**`1p517` IMPLEMENTED for the docs layer (2026-06-12) — docs index build is GPU-accelerated; code is a follow-up.** New `accel_embedder.py`: static-shape (64×512) ONNX → raw ORT CoreML session (`ModelFormat=MLProgram`/`MLComputeUnits=ALL`/`ModelCacheDirectory`) → **CLS-pool** (NOT mean — verified cos 1.0 vs fastembed) + L2-normalize; dims pinned directly on the protobuf (bypasses `update_model_dims`'s strict checker, which rejects bge's opset-11 LayerNorm). `indexer._get_embedder` is provider-conditional with a **self-protecting `offloads_to_gpu()` probe** (cpu/wall ratio) that falls back to fastembed for any model that doesn't actually run on the GPU. `onnx` added as a GPU-conditional setup dep (`setup_index.GPU_ACCEL_IMPORTS`). +`test_accel_embedder.py` (mocked, 7 tests). **Server queries stay on fastembed** by design (single-query batch-64 padding = non-win). Suite **3141 green**. **Live full rebuild (this repo, GPU path):** docs/arctic **12,503 chunks in 62s (~202 ch/s, GPU offload)** vs code/bge **10,139 chunks in 632s (~16 ch/s, CPU)** — docs ~12.6× faster/chunk; index `ready`, fold intact (67 seeds), arctic+bge models. **bge (code) NOT GPU-accelerated:** its fastembed `model_optimized.onnx` (fused opset-11 LayerNorm) fragments on CoreML (42/92 nodes, 38 partitions → CPU-bound) → the probe correctly keeps it on fastembed. **Code-layer acceleration needs a CoreML-friendly bge ONNX** (decomposed LayerNorm) — the one remaining `1p517` piece (open: source/export a clean bge, or accept docs-only).

**`1p517` FULLY IMPLEMENTED — BOTH embedding layers GPU-accelerated (2026-06-12, operator chose to accelerate code too).** The code-layer blocker (bge's fastembed `model_optimized.onnx` is fused with `com.microsoft` contrib ops → CoreML fragments) was fixed by a **clean transformers.js export** (`Xenova/bge-small-en-v1.5` fp16, decomposed standard ops → 1 CoreML partition, 201 ch/s, cos 1.0). `accel_embedder.CLEAN_ONNX_SOURCES` maps bge → that export (downloaded+cached under `~/.wavefoundry/cache/onnx-src`, offline-safe; arctic uses its resident model); `setup_index._prewarm_gpu_accel` prefetches it + pays the CoreML compile at setup. **Full from-scratch rebuild, both layers on GPU: ~3.2 min (docs 102s + code 107s, sharing the GPU) vs ~11 min with code on CPU — code layer 632s→107s (~6×).** Suite **3144 green**; index `ready`, models correct (arctic docs / bge code), fold intact (67 seeds), cos 1.0 both layers. All 4 changes (`1p4ww`/`1p4wx`/`1p4wy`/`1p517`) now `implemented`.

**RESUME:** **`1p4wz` is close-ready (all 4 changes implemented)** — operator-owned: confirm + close the wave. Health flicker note: a `content='all'` rebuild reports a brief `stale` (folded seeds shown "added") right after finishing, then self-heals to `ready` on the next health check (meta finalizes just after the build returns) — benign. **Deps: `onnx` is installed in `~/.wavefoundry/venv`; the `Xenova/bge-small-en-v1.5` fp16 ONNX is cached — both real `1p517` artifacts on this Apple-Silicon machine.** A consumer pack rebuild + the `setup_index` GPU-deps/prefetch reach other projects on upgrade. The arctic docs model + the fold are now LIVE in this repo's index. **Note:** the live MCP server holds in-memory code from the reconnect *before* the graph-layer edits — a reconnect picks up the new graph-tool code, but the graph DATA is unchanged so live graph queries are unaffected. **Current source versions unchanged: CHUNKER 30, GRAPH_BUILDER 29, CLUSTER 9, WALKER 6.**

**Still pending (operator):** `1p4u5` has its GPU-embedding changes (`1p4u0` implemented, `1p4u1` planned) — wave not close-ready until those land. **`p4su` does NOT contain 1p4up/1p4uq** — a new pack (superseding p4su) is needed to deliver the member-access reads fix + H1 to the teams. Operator-owned: signoff; git commit; wave close; new pack + version stamp/tag/ship. (MCP server reconnect still clears the stale in-memory module view for live tools.)
